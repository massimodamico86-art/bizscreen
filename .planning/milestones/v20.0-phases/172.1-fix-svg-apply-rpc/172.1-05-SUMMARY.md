---
phase: 172.1-fix-svg-apply-rpc
plan: 05
status: complete
completed: 2026-04-22
requirements:
  - TPRV-05
---

# Plan 172.1-05 Summary — svg-rpc-atomicity integration test

## What was built

New file: `tests/integration/preview-apply/svg-rpc-atomicity.test.js` — 97 lines, 4 tests, structurally mirrors the existing `rpc-atomicity.test.js` but exercises the new SVG RPC.

## Tests

| # | Test | What it asserts |
|---|---|---|
| 1 | In-flight timing (fake timers) | Apply stays in-flight for the full 500 ms RPC delay (Pitfall 2 contract); call names `clone_svg_template_to_scene`. |
| 2 | Error propagation on D-05 fallback #3 | `supabase.rpc` rejects with `'Template has no SVG body'` → `applyTemplate` throws the same message unchanged; exactly 1 RPC call. |
| 3 | Single-call atomicity (TPRV-05) | `supabase.rpc.mock.calls.length === 1`, call names the new RPC, and `supabase.from` is `undefined` — catches any regression that tries a follow-up UPDATE. |
| 4 | Manual-verification placeholder | Points at Plan 06's live E2E as the real-DB proof. |

## vitest results

```
npx vitest run tests/integration/preview-apply/svg-rpc-atomicity.test.js
→ 1 test file, 4 tests, all passing (426 ms total). Exit code 0.
```

## Error-surface delta vs. Phase 172 analog

The analog `rpc-atomicity.test.js` asserts `/Access denied.*plan tier/` — the license-gate error raised by migration 168's `clone_template_with_customization`. The new RPC `clone_svg_template_to_scene` has **no license gate** (D-09 — `svg_templates` has no `license` column). The closest deterministic error surface is D-05 fallback #3: `RAISE EXCEPTION 'Template has no SVG body'`. Test 2 uses that exact literal string.

## Grep verification

| Check | Expected | Got |
|---|---|---|
| `clone_svg_template_to_scene` occurrences | ≥ 3 (Tests 1, 2, 3) | 3 |
| `clone_template_with_customization` occurrences | 0 | 0 |
| Contains `Template has no SVG body` | yes | yes |
| Contains `expect(supabase.from).toBeUndefined()` | yes | yes |
| Contains `vi.useFakeTimers()` | yes | yes |
| Contains `vi.mock('../../../src/supabase'` | yes | yes |
| Contains `vi.mock('dompurify'` | yes | yes |
| Contains `import { applyTemplate } from '../../../src/services/templateApplyService'` | yes | yes |
| `polotno` mentioned (regression — scope is SVG only) | no | no |

## Deviations from plan

None.

## Next

Wave 3 complete (plans 04 + 05 both landed with tests green). Wave 4 (plan 06 — live Playwright E2E against real Supabase credentials) is now unblocked.
