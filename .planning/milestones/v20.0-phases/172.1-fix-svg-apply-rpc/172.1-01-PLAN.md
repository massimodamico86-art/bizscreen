---
phase: 172.1-fix-svg-apply-rpc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/169_backfill_svg_content.sql
autonomous: true
requirements:
  - TPRV-04
  - TPRV-05
tags:
  - supabase
  - migration
  - backfill
  - svg
must_haves:
  truths:
    - "All 24 active svg_templates rows have svg_content IS NOT NULL after migration 169 applies"
    - "Migration is idempotent — re-running produces no new UPDATEs (WHERE svg_content IS NULL guard)"
    - "Self-asserting DO $$ block raises if any active row still has svg_content IS NULL"
  artifacts:
    - path: "supabase/migrations/169_backfill_svg_content.sql"
      provides: "Data-backfill migration for 24 svg_templates rows across 12 unique svg_url keys"
      contains: "UPDATE svg_templates SET svg_content ="
  key_links:
    - from: "supabase/migrations/169_backfill_svg_content.sql"
      to: "svg_templates.svg_content"
      via: "12 UPDATE ... WHERE svg_url = '/templates/svg/...' AND svg_content IS NULL"
      pattern: "UPDATE svg_templates\\s+SET svg_content"
    - from: "supabase/migrations/169_backfill_svg_content.sql"
      to: "self-verification"
      via: "DO $$ ... ASSERT v_missing = 0 ... END $$"
      pattern: "ASSERT v_missing = 0"
---

<objective>
Write migration 169 that backfills `svg_templates.svg_content` for every active row by reading the 12 unique static SVG files under `public/templates/svg/**` and emitting one idempotent UPDATE per file (keyed on `svg_url`). Migration closes with a self-asserting `DO $$ ... ASSERT ... END $$` block that raises if any active row still has `svg_content IS NULL`.

Purpose: Provide a server-side SVG body for every active template so the new Apply RPC (Plan 02) never observes a row where both `p_customized_svg` and `svg_content` are null (D-05 fallback #3 must be unreachable on seeded data per D-14 ordering).

Output: `supabase/migrations/169_backfill_svg_content.sql` — pure data migration, additive, no schema changes, no DOWN migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172.1-fix-svg-apply-rpc/172.1-CONTEXT.md
@.planning/phases/172.1-fix-svg-apply-rpc/172.1-PATTERNS.md
@supabase/migrations/167_gallery_templates_view_and_rls.sql
@supabase/migrations/097_seed_svg_templates.sql
@supabase/migrations/094_svg_templates.sql
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static file → migration | Build-time inlining of `public/templates/svg/**` into SQL. No runtime input; content is vendored in-repo and reviewed at commit time. |
| migration → svg_templates.svg_content | `SECURITY INVOKER` UPDATE run under the migration role (superuser). No auth.uid() involved — this is a maintenance data-fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172.1-01 | T (Tampering) | migration 169 body | mitigate | Dollar-quote SVG payloads with `$svg$...$svg$` to eliminate injection via template content; SVG content is committed in repo and reviewed in PR. |
| T-172.1-02 | I (Information Disclosure) | svg_content write | accept | SVG bodies are already public at `/templates/svg/**` (served by the static site). No new exposure. |
| T-172.1-03 | D (Denial of Service) | DO $$ ASSERT block | mitigate | Migration fails loudly (raises) if any active row lacks svg_content, preventing Plan 02 RPC from shipping against an under-backfilled DB. Compensating control for D-05 fallback #3 being silently reachable on seed data. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write migration 169 backfill SQL (24 rows via 12 URL-keyed UPDATEs + self-asserting DO $$ footer)</name>

  <read_first>
    - supabase/migrations/168_clone_template_with_customization.sql (header + footer style — this migration MUST precede 170)
    - supabase/migrations/167_gallery_templates_view_and_rls.sql (lines 1-12 header pattern, lines 287-320 DO $$ ASSERT self-verify block)
    - supabase/migrations/097_seed_svg_templates.sql (the 12 svg_url paths — reference only; do NOT copy its DELETE)
    - supabase/migrations/094_svg_templates.sql (table shape — confirms svg_content TEXT NULL)
    - .planning/phases/172.1-fix-svg-apply-rpc/172.1-PATTERNS.md §1 (lines 23-105) — the exact header, UPDATE, and DO $$ patterns to use
    - .planning/phases/172.1-fix-svg-apply-rpc/172.1-CONTEXT.md §D-06, §D-07, §D-14 and §Static SVG Assets list
    - public/templates/svg/restaurant-menu/menu-design.svg (largest — 28204 bytes)
    - public/templates/svg/cafe-special/design.svg
    - public/templates/svg/retail-sale/design.svg
    - public/templates/svg/welcome-sign/design.svg
    - public/templates/svg/holiday-sale/design.svg
    - public/templates/svg/real-estate/design.svg
    - public/templates/svg/healthcare-info/design.svg
    - public/templates/svg/corporate-welcome/design.svg
    - public/templates/svg/happy-hour/design.svg
    - public/templates/svg/fitness-promo/design.svg
    - public/templates/svg/hotel-amenities/design.svg
    - public/templates/svg/event-promo/design.svg
  </read_first>

  <action>
Create `supabase/migrations/169_backfill_svg_content.sql` following PATTERNS.md §1 verbatim.

**File structure (sections in order):**

1. **ASCII-bordered header** — copy PATTERNS.md §1 "Header preamble pattern" (lines 30-51 of PATTERNS.md) verbatim. It references D-06/D-07/D-14, states "No DOWN migration (matches 080/110/167/168 convention)", and names the regression ("Phase 172 live E2E, 2026-04-21").

2. **12 UPDATE blocks**, one per static file. Each block MUST use the exact pattern:
   ```sql
   -- <category-slug>: <human-readable caption, optional>
   UPDATE svg_templates
   SET svg_content = $svg$<VERBATIM_FILE_CONTENTS></svg>$svg$
   WHERE svg_url = '/templates/svg/<path>/<design|menu-design>.svg'
     AND svg_content IS NULL;
   ```
   - Use `$svg$...$svg$` dollar-quoted strings so no inner single-quote inside SVG attribute values needs escaping. PATTERNS.md §1 "UPDATE pattern" is the authoritative shape.
   - Inline the FULL bytes of each file verbatim — no minification, no whitespace stripping. The 12 files total ~73KB and are reviewable inline per CONTEXT.md §Claude's Discretion.
   - **The 12 `svg_url` values (exact strings — must match what 094+097+167 seeded):**
     1. `/templates/svg/restaurant-menu/menu-design.svg` — note: filename is `menu-design.svg`, NOT `design.svg`
     2. `/templates/svg/cafe-special/design.svg`
     3. `/templates/svg/retail-sale/design.svg`
     4. `/templates/svg/welcome-sign/design.svg`
     5. `/templates/svg/holiday-sale/design.svg`
     6. `/templates/svg/real-estate/design.svg`
     7. `/templates/svg/healthcare-info/design.svg`
     8. `/templates/svg/corporate-welcome/design.svg`
     9. `/templates/svg/happy-hour/design.svg`
     10. `/templates/svg/fitness-promo/design.svg`
     11. `/templates/svg/hotel-amenities/design.svg`
     12. `/templates/svg/event-promo/design.svg`
   - **Confirm the exact `svg_url` strings before writing** by grepping the two seed migrations:
     `grep -nE "svg_url|/templates/svg/" supabase/migrations/097_seed_svg_templates.sql supabase/migrations/167_gallery_templates_view_and_rls.sql`
     If any of the 12 seeded paths differs from the list above, use whatever string the seed migrations wrote (e.g., absence of leading `/`) — the UPDATE must match the seeded value exactly, or rows silently remain `svg_content IS NULL`.
   - Each UPDATE hits BOTH the 097-seeded (slug IS NULL) and 167-seeded (slug bearing) rows because the key is `svg_url`, not `slug` (D-06). Net effect: 12 UPDATEs touch 24 rows (2 per unique URL).
   - **DO NOT** include `DELETE FROM svg_templates ...` (097:7 anti-pattern — backfill is additive per PATTERNS.md §1 "What NOT to copy from 097").
   - **DO NOT** include `ON CONFLICT` (UPDATE-only migration; no INSERT happens).

3. **Self-asserting verification footer** — copy PATTERNS.md §1 "Self-verifying DO $$ footer" (lines 67-83 of PATTERNS.md) verbatim:
   ```sql
   -- ============================================================================
   -- SELF-ASSERTING VERIFICATION — all active rows have svg_content populated
   -- ============================================================================
   DO $$
   DECLARE
     v_missing INTEGER;
   BEGIN
     SELECT COUNT(*) INTO v_missing
       FROM svg_templates
       WHERE is_active = TRUE
         AND svg_content IS NULL;
     ASSERT v_missing = 0,
       format('expected 0 active svg_templates rows with svg_content IS NULL after backfill, got %s', v_missing);
   END $$;
   ```
   This block runs at end-of-migration; PostgreSQL raises and aborts the transaction if ASSERT fails, so Plan 02 cannot be applied on top of a broken backfill (D-14 ordering contract).

4. **No DOWN migration** (matches 080/110/167/168 convention — explicit in header).

**CRITICAL — verbatim file contents:** The full SVG bytes of each of the 12 files must be pasted into the SQL file verbatim between the `$svg$` markers. Do NOT hand-edit the SVGs for "readability" — any byte change would desync the backfill from the static assets served by the admin-upload path that future phases will add.

**Migration numbering:** File is `supabase/migrations/169_backfill_svg_content.sql` — next available after 168 (confirmed via `ls supabase/migrations/ | sort`, last file is `168_clone_template_with_customization.sql`). Plan 02 uses `170_...`.
  </action>

  <verify>
    <automated>test -f supabase/migrations/169_backfill_svg_content.sql && grep -cE "UPDATE svg_templates\s+SET svg_content" supabase/migrations/169_backfill_svg_content.sql | xargs -I {} test {} -eq 12 && grep -q "ASSERT v_missing = 0" supabase/migrations/169_backfill_svg_content.sql && grep -q "WHERE svg_content IS NULL" supabase/migrations/169_backfill_svg_content.sql && ! grep -q "DELETE FROM svg_templates" supabase/migrations/169_backfill_svg_content.sql && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `supabase/migrations/169_backfill_svg_content.sql`
    - `grep -cE "UPDATE svg_templates\s+SET svg_content" supabase/migrations/169_backfill_svg_content.sql` returns exactly `12`
    - File contains the literal string `ASSERT v_missing = 0` (self-verification footer)
    - File contains the literal string `WHERE svg_content IS NULL` (idempotency guard per D-06)
    - File contains the literal string `is_active = TRUE` (the ASSERT scope)
    - File does NOT contain `DELETE FROM svg_templates` (anti-pattern per PATTERNS.md §1)
    - File does NOT contain `CREATE OR REPLACE FUNCTION` (this is a data migration, not an RPC)
    - File contains exactly 12 distinct `svg_url = '/templates/svg/...` literal strings (verified by `grep -oE "svg_url = '/templates/svg/[^']+'" supabase/migrations/169_backfill_svg_content.sql | sort -u | wc -l` returning `12`)
    - File contains the literal string `menu-design.svg` (restaurant-menu is the only file whose basename is `menu-design.svg`, not `design.svg` — absence would indicate a broken URL list)
    - File contains a `$svg$` dollar-quoted opener at least 12 times (one per UPDATE): `grep -c "\\\$svg\\\$" supabase/migrations/169_backfill_svg_content.sql` returns a value >= 24 (opener + closer per UPDATE)
    - File does NOT contain the string `ON CONFLICT` (no INSERTs in this migration)
  </acceptance_criteria>

  <done>
    Migration 169 file exists with 12 URL-keyed idempotent UPDATE blocks (covering all 24 rows), a self-asserting DO $$ footer, and the standard ASCII-bordered header citing D-06/D-07/D-14. File is committable and ready for Plan 03 `supabase db push`.
  </done>
</task>

</tasks>

<verification>
- `ls supabase/migrations/169_backfill_svg_content.sql` succeeds
- All acceptance criteria above pass
- Plan 02 (Wave 1) is unblocked — both migrations can be written in parallel; ordering is enforced only at schema-push time (Plan 03)
</verification>

<success_criteria>
- Migration 169 file written and committable
- Idempotency guard (`WHERE svg_content IS NULL`) present on every UPDATE
- Self-verification ASSERT present at tail
- No DELETE, no INSERT, no DOWN migration — additive-only data fix
- 12 UPDATE blocks, 12 distinct svg_url literals, 24 rows covered at apply-time (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/172.1-fix-svg-apply-rpc/172.1-01-SUMMARY.md` capturing: file written, 12 URL-keyed UPDATEs written verbatim from static files, total SQL size (~75-80KB after inlining), and any discrepancy discovered between `public/templates/svg/**` filenames and the svg_url values in 097/167 seed migrations (document which string actually seeded, if different from the 12 listed above).
</output>
