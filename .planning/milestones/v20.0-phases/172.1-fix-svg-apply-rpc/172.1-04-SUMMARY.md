---
phase: 172.1-fix-svg-apply-rpc
plan: 04
status: complete
completed: 2026-04-22
requirements:
  - TPRV-04
---

# Plan 172.1-04 Summary — Client RPC-name swap + unit tests

## What was built

Two files modified, 6 lines changed total (3 insertions + 3 deletions per file).

### `src/services/templateApplyService.js` (2 edits, 4 lines changed)

| Line | Change |
|---|---|
| 14 | JSDoc: `clone_template_with_customization (new in migration 168, Phase 172)` → `clone_svg_template_to_scene (new in migration 170, Phase 172.1)` |
| 58 | `supabase.rpc('clone_template_with_customization', ...)` → `supabase.rpc('clone_svg_template_to_scene', ...)` |

### `tests/unit/services/templateApplyService.test.js` (4 occurrences swapped via replace_all)

| Line | Context |
|---|---|
| 41 | test name string (1st SVG-branch test) |
| 50 | `toHaveBeenCalledWith('clone_svg_template_to_scene', ...)` — 1st SVG-branch assertion |
| 66 | `toHaveBeenCalledWith('clone_svg_template_to_scene', {...})` — 2nd SVG-branch assertion |
| 108 | `toHaveBeenCalledWith('clone_svg_template_to_scene', ...)` — DOMPurify-order test assertion |

Plan called for "3 assertions + 2 test-name strings = 5 edits" but actual grep showed exactly 4 occurrences of the old name (one test-name edit didn't apply because line 60's description didn't mention the RPC name). All 4 swapped via a single `replace_all` edit.

## Grep diffs

| Check | Count |
|---|---|
| `clone_svg_template_to_scene` in src/services/templateApplyService.js | 2 (1 JSDoc + 1 RPC call) |
| `clone_svg_template_to_scene` in tests/unit/services/templateApplyService.test.js | 4 |
| `clone_template_with_customization` anywhere in either file | 0 |
| `'clone_template_to_scene'` (Polotno branch, MUST stay) | src: 1, test: 1 |

## Invariants preserved

| Invariant | Location | Verified |
|---|---|---|
| `SVG_MAX_BYTES = 500_000` | templateApplyService.js:32 | ✓ |
| `DOMPurify.sanitize(..., { USE_PROFILES: { svg: true, svgFilters: true } })` | templateApplyService.js:54–56 | ✓ |
| `if (error) throw error` idiom | templateApplyService.js:63 | ✓ |
| Polotno branch at templateApplyService.js:67–74 | untouched | ✓ |
| `editorRouteFor` (D-15 contract) | templateApplyService.js:91–95 | ✓ |
| `import { supabase } from '../supabase'; import DOMPurify from 'dompurify';` | templateApplyService.js:24–25 | ✓ |
| Polotno-branch test + `expect(DOMPurify.sanitize).not.toHaveBeenCalled()` | test file:73–84 | ✓ |
| 500KB-cap test (T-172-04) | test file:129–139 | ✓ |
| generic error-propagation test | test file:115–119 | ✓ |
| editorRouteFor test (D-15) | test file:121–127 | ✓ |

## Vitest results

```
npx vitest run tests/unit/services/templateApplyService.test.js
```
→ 1 test file, 8 tests, all passing (467 ms total). Exit code 0.

Note: the plan said "7 tests total"; the file actually contains 8 `it(...)` blocks. All 8 green.

## Deviations from plan

- Plan called for 5 test-file edits (3 assertions + 2 test-name strings). Only 4 edits needed — line 60's test name does not contain the RPC-name string, so no edit applied there. Plan line 273 already anticipated this possibility ("skip this edit if line 60's description does not contain 'clone_template_with_customization'"). Confirmed via grep.

## Next

Wave 3 half complete. Plan 05 (Wave 3 second half) adds the new integration test `tests/integration/preview-apply/svg-rpc-atomicity.test.js`. Plan 06 (Wave 4) runs the live E2E.
