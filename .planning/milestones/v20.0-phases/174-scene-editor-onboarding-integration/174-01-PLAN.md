---
phase: 174-scene-editor-onboarding-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tests/e2e/editor-return.spec.js
  - tests/e2e/gallery-tour.spec.js
  - tests/integration/apply-template-to-slide.test.js
  - tests/integration/onboarding-rpc.test.js
  - tests/unit/hooks/useGalleryTour.test.js
  - tests/unit/services/marketplaceService.test.js
  - tests/unit/services/onboardingService.test.js
autonomous: true
requirements: [TEDR-01, TEDR-02, TEDR-03, TONB-01, TONB-02, TONB-03, TONB-04]
must_haves:
  truths:
    - "driver.js@^1.4.0 is installed and importable"
    - "Eight test files exist in RED state covering all 7 phase requirements"
    - "ONBOARDING_STEPS test asserts length === 7 and starter_pack between logo and first_media"
    - "Tests run and FAIL deterministically (RED) — no spurious passes"
  artifacts:
    - path: "package.json"
      provides: "driver.js@^1.4.0 dependency"
      contains: "\"driver.js\""
    - path: "tests/e2e/editor-return.spec.js"
      provides: "TEDR-01..03 RED stubs"
      min_lines: 60
    - path: "tests/e2e/gallery-tour.spec.js"
      provides: "TONB-04 RED stubs (first/second visit + dismissal)"
      min_lines: 50
    - path: "tests/integration/apply-template-to-slide.test.js"
      provides: "TEDR-02 atomicity + RPC arg shape RED stubs"
      min_lines: 50
    - path: "tests/integration/onboarding-rpc.test.js"
      provides: "TONB-03 column-write + return-shape RED stubs"
      min_lines: 40
    - path: "tests/unit/hooks/useGalleryTour.test.js"
      provides: "useGalleryTour conditional logic RED stubs"
      min_lines: 50
  key_links:
    - from: "tests/unit/services/onboardingService.test.js"
      to: "src/services/onboardingService.js"
      via: "ONBOARDING_STEPS export length === 7 assertion"
      pattern: "ONBOARDING_STEPS.*toHaveLength\\(7\\)"
    - from: "tests/unit/services/marketplaceService.test.js"
      to: "src/services/marketplaceService.js"
      via: "applyTemplateToActiveSlide import (will throw until Plan 04 lands)"
      pattern: "applyTemplateToActiveSlide"
---

<objective>
Wave 0 — Nyquist sampling continuity gate. Install driver.js@^1.4.0 (the only new runtime dependency for Phase 174) and create RED test stubs for all 7 phase requirements so every downstream plan has a concrete test file to flip from RED to GREEN.

Purpose: prevents the 3-task gap-without-automated-verify failure mode. Every test asserts a specific behaviour that does not yet exist; tests MUST fail when run.
Output: 1 npm install + 8 modified/new test files committed in RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-scene-editor-onboarding-integration/174-CONTEXT.md
@.planning/phases/174-scene-editor-onboarding-integration/174-RESEARCH.md
@.planning/phases/174-scene-editor-onboarding-integration/174-PATTERNS.md
@.planning/phases/174-scene-editor-onboarding-integration/174-VALIDATION.md

<interfaces>
<!-- Reference test patterns the executor must mirror. Copy structure verbatim. -->

From tests/integration/preview-apply/apply-starter-pack-atomicity.test.js (Phase 173 — supabase mock pattern, lines 1–80):
```javascript
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('../../../src/supabase', () => ({
  supabase: { rpc: vi.fn() },
}));

import { applyStarterPack } from '../../../src/services/marketplaceService';
import { supabase } from '../../../src/supabase';

describe('apply_starter_pack atomicity', () => {
  beforeEach(() => vi.clearAllMocks());
  // it(...) blocks
});
```

From tests/e2e/preview-apply.spec.js (Phase 172 — skip guard + loginAndPrepare):
```javascript
import { test, expect } from '@playwright/test';
import { loginAndPrepare, waitForPageReady } from './helpers.js';

test.describe('Editor Return', () => {
  test.skip(() => !process.env.TEST_CLIENT_EMAIL, 'Client test credentials not configured');
  test.beforeEach(async ({ page }) => {
    await loginAndPrepare(page, {
      email: process.env.TEST_CLIENT_EMAIL,
      password: process.env.TEST_CLIENT_PASSWORD,
    });
  });
  // test(...) blocks
});
```

From tests/unit/services/onboardingService.test.js (existing — extend the existing describe block; do not replace):
- Already imports `ONBOARDING_STEPS` from `../../../src/services/onboardingService`
- Has an existing assertion `expect(ONBOARDING_STEPS).toHaveLength(6)` — flip to 7

From src/services/marketplaceService.js:632 (Phase 173 — applyStarterPack wrapper shape — blueprint):
```javascript
export async function applyStarterPack(packId) {
  const { data, error } = await supabase.rpc('apply_starter_pack', { p_pack_id: packId });
  if (error) throw error;
  return data ?? [];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install driver.js@^1.4.0 and create unit + integration RED test stubs</name>
  <read_first>
    - /Users/massimodamico/bizscreen/package.json (current dependencies)
    - /Users/massimodamico/bizscreen/tests/integration/preview-apply/apply-starter-pack-atomicity.test.js (mock pattern blueprint)
    - /Users/massimodamico/bizscreen/tests/integration/preview-apply/svg-rpc-atomicity.test.js (RPC return-shape mock)
    - /Users/massimodamico/bizscreen/tests/unit/services/marketplaceService.test.js (existing test file to extend)
    - /Users/massimodamico/bizscreen/tests/unit/services/onboardingService.test.js (existing test file to extend)
    - /Users/massimodamico/bizscreen/src/services/marketplaceService.js lines 1–50 + 632–636 (applyStarterPack pattern)
    - /Users/massimodamico/bizscreen/src/services/onboardingService.js lines 1–80 (ONBOARDING_STEPS array)
  </read_first>
  <files>
    package.json, package-lock.json,
    tests/integration/apply-template-to-slide.test.js,
    tests/integration/onboarding-rpc.test.js,
    tests/unit/hooks/useGalleryTour.test.js,
    tests/unit/services/marketplaceService.test.js,
    tests/unit/services/onboardingService.test.js
  </files>
  <action>
    **Step 1 — Install driver.js (Wave 0 gap, Claude's discretion: 1.4.0 latest stable, MIT license per RESEARCH.md §Standard Stack):**
    ```
    npm install driver.js@^1.4.0
    ```
    Verify `package.json` contains `"driver.js": "^1.4.0"` in `dependencies`. Do NOT use `--save-dev` — driver.js is a runtime dependency.

    **Step 2 — Create `tests/integration/apply-template-to-slide.test.js` (NEW, RED):**
    Mirror PATTERNS.md §`tests/integration/apply-template-to-slide.test.js` block verbatim. Mock `../../../src/supabase` with `supabase: { rpc: vi.fn() }`. Import `applyTemplateToActiveSlide` from `../../../src/services/marketplaceService` (this import will throw — that's the RED state, expected). Include exactly these four `it(...)` cases per VALIDATION.md (TEDR-02 row):
    1. `'resolves with slide UUID on success (D-05)'` — mock `supabase.rpc.mockResolvedValueOnce({ data: 'slide-uuid-1', error: null })`; assert returned id is `'slide-uuid-1'`; assert `supabase.rpc.mock.calls[0][0] === 'apply_template_to_active_slide'`; assert call args `{ p_scene_id: 'scene-1', p_slide_id: 'slide-1', p_template_id: 'tmpl-1', p_editor_type: 'svg' }`.
    2. `'throws on RPC error (atomicity contract)'` — mock error `'Template has no SVG body'`; assert promise rejects with matching message.
    3. `'zero follow-up calls — single RPC round-trip proves atomicity'` — assert `supabase.rpc` called exactly 1 time.
    4. `'polotno editor_type is rejected server-side (D-02)'` — mock error `'Only SVG templates supported'`; assert promise rejects with matching message.

    **Step 3 — Create `tests/integration/onboarding-rpc.test.js` (NEW, RED):**
    Mock `../../../src/supabase` same pattern. Import `updateOnboardingStep, getOnboardingProgress, markGalleryTourSeen` from `../../../src/services/onboardingService` (markGalleryTourSeen import will throw — RED). Three `it(...)` cases (TONB-03):
    1. `'updateOnboardingStep("starter_pack", true) calls update_onboarding_step RPC with p_step="starter_pack"'` — mock `{ data: { success: true }, error: null }`; assert call args `{ p_step: 'starter_pack', p_completed: true }`.
    2. `'getOnboardingProgress maps completed_starter_pack and completed_gallery_tour to camelCase'` — mock RPC return `{ data: [{ completed_welcome: true, completed_starter_pack: true, completed_gallery_tour: false, ...other 8 columns: false }], error: null }`; assert returned object has `completedStarterPack: true` AND `completedGalleryTour: false`.
    3. `'markGalleryTourSeen calls update_onboarding_step with p_step="gallery_tour", p_completed=true (D-17, D-19)'` — mock success; assert correct RPC name and args.

    **Step 4 — Create `tests/unit/hooks/useGalleryTour.test.js` (NEW, RED):**
    Mock `driver.js` (factory returning `{ drive: vi.fn(), destroy: vi.fn() }`), mock `driver.js/dist/driver.css` as `{}`, mock `../../../src/services/onboardingService` with `getOnboardingProgress` and `updateOnboardingStep`. Use `@testing-library/react` `renderHook` (already installed — verify by reading `package.json`; if absent, fall back to a render-only smoke test). Import `useGalleryTour` from `../../../src/hooks/useGalleryTour` (will throw — RED). Four `it(...)` cases per VALIDATION.md (TONB-04 unit row):
    1. `'completedGalleryTour=false → driver().drive() is called once'`
    2. `'completedGalleryTour=true → driver is never called'`
    3. `'isFetching=true → tour does not start'` — assert `getOnboardingProgress` not called.
    4. `'onDestroyStarted handler invokes updateOnboardingStep("gallery_tour", true)'` — capture the `onDestroyStarted` callback from the `driver()` call args, invoke it, assert mock call args.

    **Step 5 — Extend `tests/unit/services/marketplaceService.test.js`:**
    Append a new `describe('applyTemplateToActiveSlide (Phase 174 D-06)', () => {...})` block after the existing describes. Import `applyTemplateToActiveSlide` (will throw — RED). Two `it(...)` cases:
    1. `'calls apply_template_to_active_slide RPC with correct args (D-05 contract)'` — see Step 2.1.
    2. `'throws on error'` — see Step 2.2.

    **Step 6 — Extend `tests/unit/services/onboardingService.test.js`:**
    a. Find the existing `expect(ONBOARDING_STEPS).toHaveLength(6)` assertion — change `6` to `7`. Add comment `// Phase 174 TONB-01 — added starter_pack step (D-07)`.
    b. Append two new `it(...)` cases inside the existing `ONBOARDING_STEPS` describe:
       - `'has starter_pack step between logo and first_media (D-07)'` — derive `ids = ONBOARDING_STEPS.map(s => s.id)`; assert `ids.indexOf('starter_pack') === ids.indexOf('logo') + 1` AND `ids.indexOf('first_media') === ids.indexOf('starter_pack') + 1`.
       - `'starter_pack step has no navigateTo (stays inside wizard per D-08)'` — assert `ONBOARDING_STEPS.find(s => s.id === 'starter_pack').navigateTo === undefined`.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/apply-template-to-slide.test.js tests/integration/onboarding-rpc.test.js tests/unit/hooks/useGalleryTour.test.js tests/unit/services/marketplaceService.test.js tests/unit/services/onboardingService.test.js 2>&1 | tail -40</automated>
    Expected: tests RUN and FAIL with import-not-found / undefined-function errors (RED state). package.json contains driver.js@^1.4.0.
  </verify>
  <acceptance_criteria>
    - `grep -E '"driver\.js":\s*"\^1\.4\.\d"' package.json` exits 0
    - File exists: `tests/integration/apply-template-to-slide.test.js` with `>= 50 lines`
    - File exists: `tests/integration/onboarding-rpc.test.js` with `>= 40 lines`
    - File exists: `tests/unit/hooks/useGalleryTour.test.js` with `>= 50 lines`
    - `grep -c "applyTemplateToActiveSlide" tests/unit/services/marketplaceService.test.js` returns >= 2
    - `grep -c "toHaveLength(7)" tests/unit/services/onboardingService.test.js` returns >= 1 (the existing 6-asserter is now 7)
    - `grep -c "starter_pack" tests/unit/services/onboardingService.test.js` returns >= 2
    - `npx vitest run tests/unit/services/onboardingService.test.js 2>&1 | grep -E "(FAIL|Tests.*failed)"` exits 0 (test runs and FAILS — RED state)
  </acceptance_criteria>
  <done>driver.js installed; all 5 test files modified/created in RED state; vitest exits non-zero (failures) when run on these files (this proves RED — Plans 02–09 will flip them GREEN).</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E RED stubs for editor-return (TEDR-01..03) and gallery tour (TONB-04)</name>
  <read_first>
    - /Users/massimodamico/bizscreen/tests/e2e/preview-apply.spec.js (skip-guard, loginAndPrepare, readCurrentPage fiber-BFS pattern)
    - /Users/massimodamico/bizscreen/tests/e2e/onboarding.spec.js (existing onboarding spec — verify structure, may extend)
    - /Users/massimodamico/bizscreen/tests/e2e/helpers.js (loginAndPrepare + waitForPageReady exports)
  </read_first>
  <files>tests/e2e/editor-return.spec.js, tests/e2e/gallery-tour.spec.js</files>
  <action>
    **Create `tests/e2e/editor-return.spec.js` (NEW, RED):**

    Header: import `{ test, expect }` from `@playwright/test`; import `{ loginAndPrepare, waitForPageReady }` from `'./helpers.js'`. Add the same `BFS_NODE_CAP = 10000` and `readCurrentPage(page)` helper used in preview-apply.spec.js (fiber-BFS pattern that walks React fiber to extract `currentPage` state from App component) — copy verbatim from preview-apply.spec.js.

    Wrap in `test.describe('Editor Return (TEDR-01..03)', () => { ... })` with `test.skip(() => !process.env.TEST_CLIENT_EMAIL, 'Client test credentials not configured')` and `test.beforeEach` that calls `loginAndPrepare(page, { email: process.env.TEST_CLIENT_EMAIL, password: process.env.TEST_CLIENT_PASSWORD })`.

    Three `test(...)` blocks per VALIDATION.md TEDR-01..03 rows:

    1. `test('TEDR-01 — shows Browse Templates button in scene editor topbar', async ({ page }) => {...})` — Navigate to scenes via `page.evaluate(() => window.dispatchEvent(new CustomEvent('test:setCurrentPage', { detail: 'scenes' })))` (App.jsx:176–188 supports this). Wait for scenes page. Click first scene to enter editor (or use a known seeded scene id via `setCurrentPage('scene-editor-<id>')`). Assert `page.getByRole('button', { name: /Browse Templates/i })` is visible.

    2. `test('TEDR-03 — preserves editorReturn URL params after navigation to gallery', async ({ page }) => {...})` — From scene editor, click "Browse Templates". Wait for `readCurrentPage(page)` to return `'templates'`. Assert `page.url()` matches `/\?editorReturn=1.*returnSceneId=[a-f0-9-]+/`. Also assert `slideId=` query param is present (per Open Question 1 resolution + D-04 extended contract). Assert page does NOT show the StarterPacksStrip (locator: `page.getByText(/Starter packs/i).count()` returns 0). Assert primary CTA text on a card hover or in the preview modal reads "Use Template" (not "Apply").

    3. `test('TEDR-02 — Use Template round-trip applies to active slide and returns to scene editor', async ({ page }) => {...})` — From the editorReturn gallery, click first SVG template card. In the preview modal, click "Use Template". Wait for `readCurrentPage(page)` to start with `'scene-editor-'`. Assert returned scene id matches the originating scene id (capture it before navigating to templates). Assert the canvas DOM now contains an `<svg>` element (locator: `page.locator('canvas svg, .scene-canvas svg, [data-svg-content]')` exists or check inline SVG marker text from the seeded template).

    Note all three tests SHOULD fail today — Plan 05 wires the button (TEDR-01), Plan 06 wires the URL+filter (TEDR-03), Plan 04+06 wire the round-trip (TEDR-02).

    **Create `tests/e2e/gallery-tour.spec.js` (NEW, RED):**

    Same header + skip-guard + beforeEach as editor-return spec.

    Wrap in `test.describe('Gallery Tour (TONB-04)', () => {...})`.

    Two `test(...)` blocks:

    1. `test('TONB-04 — shows 4-step driver.js tour on first gallery visit', async ({ page }) => {...})` — Reset tour state via `page.request.post` to a Supabase RPC that sets `completed_gallery_tour=false` for the test user (or precondition: rely on a fresh test user; document the assumption inline). Navigate to `templates`. Wait for `[data-tour="filter-bar"]` to be visible. Wait for driver.js popover (`.driver-popover` or `[role="dialog"][class*="driver"]`) to appear. Assert popover title text matches "Filter Templates" (step 1). Click "Next" (driver.js next button selector: `.driver-popover-next-btn`). Verify step 2 title "Search". Continue through steps 3 and 4 ("Browse Templates", "Apply a Template"). After clicking final "Done", assert popover is hidden.

    2. `test('TONB-04 — tour does not re-appear on second gallery visit (dismissal persistence)', async ({ page }) => {...})` — After completing tour in test 1 (or precondition: `completed_gallery_tour=true`), navigate AWAY (to `dashboard`), then back to `templates`. Wait for filter bar. Assert `.driver-popover` count === 0 after a 2-second wait. Also assert `[data-tour="filter-bar"]` is still visible (proves we DID land on the gallery, the tour just didn't fire).

    Both tests should fail today: filter-bar/search-input/first-card/apply-cta `data-tour` attributes don't exist (Plans 06+09), `useGalleryTour` doesn't exist (Plan 09), `markGalleryTourSeen` doesn't exist (Plan 07).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/editor-return.spec.js tests/e2e/gallery-tour.spec.js --list 2>&1 | tail -20</automated>
    Expected: lists 5 tests total (3 + 2). Listing alone proves syntactic validity. Actual run will fail (RED).
  </verify>
  <acceptance_criteria>
    - File exists: `tests/e2e/editor-return.spec.js` with `>= 60 lines`
    - File exists: `tests/e2e/gallery-tour.spec.js` with `>= 50 lines`
    - `grep -c "TEDR-0[123]" tests/e2e/editor-return.spec.js` returns >= 3 (one per requirement)
    - `grep -c "TONB-04" tests/e2e/gallery-tour.spec.js` returns >= 2
    - `grep -c "data-tour" tests/e2e/gallery-tour.spec.js` returns >= 1
    - `grep -c "test\.skip" tests/e2e/editor-return.spec.js tests/e2e/gallery-tour.spec.js` returns >= 2 (skip-guard pattern present)
    - `npx playwright test tests/e2e/editor-return.spec.js tests/e2e/gallery-tour.spec.js --list 2>&1 | grep -c "›"` returns >= 5 (tests are discoverable)
  </acceptance_criteria>
  <done>Both E2E spec files exist with skip-guards, BFS helpers, and at-least-one assertion per requirement. Tests are syntactically valid (Playwright lists them). They will fail when actually run — that's the RED state Plans 04–09 flip GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → npm registry | driver.js@^1.4.0 fetched from npm — supply-chain trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01-01 | Tampering | driver.js dependency | accept | MIT-licensed, verified version 1.4.0 published 2025-11-18 by official driver.js maintainer; npm registry signature; pin to ^1.4.0 (no minor jumps without review). No new threat surface added by tests themselves (RED stubs reference symbols that don't exist yet). |

Phase 174 security gate: this plan only installs a dependency and writes test files; the substantive RPC + frontend threat model lives in Plans 02 and 04–09.
</threat_model>

<verification>
- `npx vitest run` includes the 5 vitest files in its discovery (they're in default include globs)
- `npx playwright test --list` enumerates the 2 E2E files
- All tests FAIL when actually run — confirms RED state
- `git status` shows the 8 expected modified/new files plus package.json + package-lock.json
</verification>

<success_criteria>
- driver.js@^1.4.0 listed in package.json `dependencies` (NOT `devDependencies`)
- 5 vitest files exist with required `describe`/`it` blocks per Acceptance Criteria
- 2 Playwright spec files exist with required `test(...)` blocks
- All tests run (no syntax errors) and fail with the expected RED-state errors (import-not-found, undefined function, missing DOM selectors)
- Phase 174 has a concrete RED→GREEN gradient that downstream plans can flip atomically
</success_criteria>

<output>
After completion, create `.planning/phases/174-scene-editor-onboarding-integration/174-01-SUMMARY.md` documenting:
- driver.js install confirmed
- 8 test files in RED state (list each with line count + per-requirement coverage)
- exact vitest/playwright commands that fail today and which plan flips each to GREEN
</output>
