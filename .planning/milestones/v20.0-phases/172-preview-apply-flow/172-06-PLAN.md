---
phase: 172-preview-apply-flow
plan: 06
type: execute
wave: 4
depends_on: [172-03, 172-05]
files_modified:
  - src/pages/TemplateGalleryPage.jsx
  - src/pages/SvgEditorPage.jsx
  - src/services/marketplaceService.js
autonomous: true
requirements: [TPRV-01, TPRV-04, TPRV-06]
tags: [wiring, integration, dead-code-removal, session-storage, url-params]

must_haves:
  truths:
    - "TemplateGalleryPage.jsx wires TemplateCard.onSelect with a closure over the displayedTemplates.map index so clicking a card opens TemplatePreviewModal at the correct index"
    - "TemplatePreviewModal is mounted on TemplateGalleryPage.jsx with open, templates, initialIndex, onClose, onNavigate props"
    - "SvgEditorPage.jsx loads a scene when the URL contains ?sceneId=<uuid> — fetches the scene + first slide, reads slide.design_json.svgContent, initializes FabricSvgEditor"
    - "SvgEditorPage.jsx no longer reads sessionStorage.getItem('pendingTemplate') anywhere (TPRV-06)"
    - "SvgEditorPage.jsx no longer handles ?templateId= query param (the associated sessionStorage branch is gone with its reader)"
    - "marketplaceService.js no longer exports installWithCustomization (dead code deleted per Focus Area 4)"
    - "installTemplateAsScene audit run: if zero callers outside marketplaceService → deleted; if callers exist (admin pages, edge funcs, scripts) → retained"
    - "Post-wave grep of sessionStorage.*pendingTemplate across the entire repo returns zero hits"
  artifacts:
    - path: "src/pages/TemplateGalleryPage.jsx"
      provides: "Gallery page with TemplatePreviewModal wired to card clicks"
      contains: "TemplatePreviewModal"
    - path: "src/pages/SvgEditorPage.jsx"
      provides: "SVG editor with ?sceneId= load branch; sessionStorage branch removed"
      contains: "urlSceneId"
    - path: "src/services/marketplaceService.js"
      provides: "Marketplace service with dead installWithCustomization removed"
      contains: ""
  key_links:
    - from: "src/pages/TemplateGalleryPage.jsx TemplateCard.onSelect"
      to: "TemplatePreviewModal (opened with current index)"
      via: "useState previewState + closure over map index"
      pattern: "setPreviewState\\(.*open:\\s*true"
    - from: "src/pages/SvgEditorPage.jsx"
      to: "supabase.from('scenes').select(...)"
      via: "sceneId-branch fetches scene + first slide"
      pattern: "\\.from\\(['\"]scenes['\"]"
    - from: "src/services/marketplaceService.js"
      to: "(removed) installWithCustomization"
      via: "deletion — no consumers remain"
      pattern: "installWithCustomization"
---

<objective>
Wire Phase 172's new components into the app surfaces:
1. TemplateGalleryPage.jsx — replace the Phase 171 placeholder in TemplateCard.onSelect with the modal open handler + mount `<TemplatePreviewModal>`.
2. SvgEditorPage.jsx — delete the dead sessionStorage branch (TPRV-06) AND add the new `?sceneId=` load branch (D-15) so Plan 03's editorRoute actually lands on a working editor.
3. marketplaceService.js — delete dead `installWithCustomization` function (zero callers confirmed in research).

Purpose: TPRV-01 (modal reachable from gallery card clicks), TPRV-04 (routing works end-to-end), TPRV-06 (sessionStorage gone). Without this plan, the modal exists in isolation and the editor-load path is broken.
Output: three modified files, zero new files.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-03-SUMMARY.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-05-SUMMARY.md

<interfaces>
Gallery wiring pattern (from 172-PATTERNS.md §File 5 lines 373-395):

```jsx
const [previewState, setPreviewState] = useState({ open: false, index: 0 });

// In the .map — change (t) => to (t, i) =>:
<TemplateCard
  title={t.name}
  description={t.description}
  imageUrl={t.thumbnail}
  orientation={t.orientation}
  onSelect={() => setPreviewState({ open: true, index: i })}
/>

// Render modal alongside existing grid:
<TemplatePreviewModal
  open={previewState.open}
  templates={displayedTemplates}
  initialIndex={previewState.index}
  onClose={() => setPreviewState((s) => ({ ...s, open: false }))}
  onNavigate={onNavigate}
  showToast={showToast}
/>
```

SvgEditorPage new sceneId branch (from 172-PATTERNS.md §File 6 lines 416-445, mirrors existing designId branch at 67-82):

```javascript
else if (urlSceneId) {
  const { data: scene, error: sceneErr } = await supabase
    .from('scenes').select('id, name, settings').eq('id', urlSceneId).single();
  if (sceneErr) throw sceneErr;

  const { data: slides, error: slidesErr } = await supabase
    .from('scene_slides').select('design_json').eq('scene_id', urlSceneId)
    .order('position', { ascending: true }).limit(1);
  if (slidesErr) throw slidesErr;

  const svgContent = slides?.[0]?.design_json?.svgContent;
  const svgUrl = svgContent
    ? `data:image/svg+xml;base64,${btoa(unescape(encodeURIComponent(svgContent)))}`
    : null;

  if (cancelled) return;
  setEditorConfig({
    svgUrl,
    templateId: null,
    templateName: scene.name,
    initialJson: null,
    designId: null,
    canvasWidth: scene.settings?.width  || 1920,
    canvasHeight: scene.settings?.height || 1080,
  });
}
```

Code to delete (from 172-RESEARCH.md §Focus Area 6 lines 673-719):
- SvgEditorPage.jsx lines 83-119: entire `else if (urlTemplateId) { ... }` block
- SvgEditorPage.jsx parseQueryParams: remove `templateId: params.get('templateId')` line
- SvgEditorPage.jsx `urlTemplateId` destructure + console.log reference
- SvgEditorPage.jsx useEffect dep array: `[urlDesignId, urlTemplateId]` → `[urlDesignId, urlSceneId]`
- marketplaceService.js lines 209-237: `installWithCustomization` function

EXACT line numbers above are from research 2026-04-20; run `grep -n` at execution time to confirm before deleting — files may have drifted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TemplatePreviewModal into TemplateGalleryPage.jsx</name>
  <files>src/pages/TemplateGalleryPage.jsx</files>
  <read_first>
    - /Users/massimodamico/bizscreen/src/pages/TemplateGalleryPage.jsx lines 1-50 (imports + component signature)
    - /Users/massimodamico/bizscreen/src/pages/TemplateGalleryPage.jsx lines 260-310 (displayedTemplates useMemo)
    - /Users/massimodamico/bizscreen/src/pages/TemplateGalleryPage.jsx lines 520-570 (TemplateCard placeholder at ~557-559 per research; LINE NUMBERS MAY HAVE DRIFTED — locate via `grep -n "onSelect" src/pages/TemplateGalleryPage.jsx`)
    - /Users/massimodamico/bizscreen/src/components/template-gallery/TemplatePreviewModal.jsx (verify prop interface matches)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 5 (lines 363-399)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md §Focus Area 10 (lines 812-831)
    - Run `grep -n "onSelect\|TemplateCard\|displayedTemplates" src/pages/TemplateGalleryPage.jsx | head -40` before editing
  </read_first>
  <action>
    Minimal-diff modifications to src/pages/TemplateGalleryPage.jsx:

    1. Add import: `import TemplatePreviewModal from '../components/template-gallery/TemplatePreviewModal';` near the top alongside other component imports.

    2. Add state near existing useState declarations (after other state hooks, before useMemo blocks):
       ```jsx
       const [previewState, setPreviewState] = useState({ open: false, index: 0 });
       ```

    3. Locate the `.map((t) => ...)` rendering `<TemplateCard ... onSelect={() => { /* Phase 172 wires the modal open hook here */ }} />`. Change the map callback from `(t)` to `(t, i)`. Replace the placeholder onSelect with:
       ```jsx
       onSelect={() => setPreviewState({ open: true, index: i })}
       ```
       Preserve every other TemplateCard prop untouched.

    4. Mount the modal before the closing PageLayout/JSX root — alongside the existing grid, inside the same "has templates" branch of the conditional render:
       ```jsx
       <TemplatePreviewModal
         open={previewState.open}
         templates={displayedTemplates}
         initialIndex={previewState.index}
         onClose={() => setPreviewState((s) => ({ ...s, open: false }))}
         onNavigate={onNavigate}
         showToast={showToast}
       />
       ```
       Confirm onNavigate and showToast are already destructured from component props (Phase 171 wired them). If showToast is not available, omit that prop.

    5. Do NOT touch: filter pipeline, displayedTemplates memo, Loading/Error/Zero-content/No-results branches.

    6. If `previewState` collides with an existing local, rename to `templatePreviewState`.
  </action>
  <verify>
    <automated>grep -q "import TemplatePreviewModal" src/pages/TemplateGalleryPage.jsx && grep -Eq "\.map\(\(t,\s*i\)" src/pages/TemplateGalleryPage.jsx && grep -q "setPreviewState.*open:\s*true" src/pages/TemplateGalleryPage.jsx && grep -q "<TemplatePreviewModal" src/pages/TemplateGalleryPage.jsx; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import TemplatePreviewModal from" src/pages/TemplateGalleryPage.jsx`
    - `grep -q "useState({ open: false, index: 0 })" src/pages/TemplateGalleryPage.jsx` OR equivalent previewState initializer
    - `grep -Eq "\.map\(\(t,\s*i\)" src/pages/TemplateGalleryPage.jsx` — map callback updated to capture index
    - `grep -Eq "setPreviewState\(\{\s*open:\s*true,\s*index:\s*i\s*\}\)" src/pages/TemplateGalleryPage.jsx`
    - `grep -q "<TemplatePreviewModal" src/pages/TemplateGalleryPage.jsx`
    - `grep -q "templates={displayedTemplates}" src/pages/TemplateGalleryPage.jsx`
    - `grep -q "initialIndex={previewState.index}" src/pages/TemplateGalleryPage.jsx`
    - Negative: no existing Phase 171 behaviors broken — run the Phase 171 gallery tests to confirm still green: `npx vitest run tests/unit/components/template-gallery 2>&1 | grep -E "passed"` shows all passing (from Plans 04+05 green = 14; nothing from Plan 06 adds unit tests but nothing should regress)
    - App still compiles: `npm run build 2>&1 | tail -5` shows no TypeScript/eslint errors that block build (if the project uses Vite's dev-only build this step may be skipped; at minimum `npx vite --version` and `npm run dev` do not error on startup)
  </acceptance_criteria>
  <done>Gallery page imports the modal, opens it on card click with the correct index, and passes displayedTemplates as the snapshot source. No regressions to existing green tests.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sessionStorage branch + add ?sceneId= branch in SvgEditorPage.jsx</name>
  <files>src/pages/SvgEditorPage.jsx</files>
  <read_first>
    - /Users/massimodamico/bizscreen/src/pages/SvgEditorPage.jsx (ENTIRE file — need the full picture to update parseQueryParams, destructure, two effect branches, dep array, and remove the sessionStorage block cleanly)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 6 (lines 402-448) — exact edits + mirror-shape for new branch
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md §Focus Area 6 (lines 673-719) — exact code to delete, line-by-line
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md §D-14 + §D-15
    - Run `grep -n "urlTemplateId\|urlDesignId\|pendingTemplate\|sceneId\|parseQueryParams" src/pages/SvgEditorPage.jsx` before editing — identifies EXACT current line numbers
    - Run `grep -n "import.*supabase" src/pages/SvgEditorPage.jsx` — confirm supabase client is imported; if NOT, you must add `import { supabase } from '../supabase';` at the top
  </read_first>
  <action>
    Five edits to src/pages/SvgEditorPage.jsx:

    **Edit 1 — parseQueryParams function (currently at ~lines 22-32 per research):**
    - REMOVE the line `templateId: params.get('templateId'),` from the returned object
    - ADD `sceneId: params.get('sceneId'),`
    - Preserve `designId: params.get('designId'),` and any other fields untouched

    **Edit 2 — destructure (currently at ~lines 53-56):**
    - REMOVE `urlTemplateId` from the destructure: `const { designId: urlDesignId, templateId: urlTemplateId } = queryParams;` → `const { designId: urlDesignId, sceneId: urlSceneId } = queryParams;`
    - Or if the destructure uses object spread / different naming, follow the same logic: drop urlTemplateId, add urlSceneId

    **Edit 3 — console.log reference (currently at ~line 65):**
    - Remove any `urlTemplateId` reference from console.log(s); replace with `urlSceneId` if the log retains template-context debugging value, otherwise drop that arg entirely

    **Edit 4 — effect body: DELETE the sessionStorage branch AND ADD the sceneId branch**
    - DELETE the entire `else if (urlTemplateId) { ... }` block (research says lines 83-119; verify first). This block contains `sessionStorage.getItem('pendingTemplate')`, `sessionStorage.removeItem`, the try/catch, the template-data parsing, the btoa encoding, and its own setEditorConfig. Remove all of it — including leading/trailing blank lines so the final structure is clean.
    - ADD a new `else if (urlSceneId) { ... }` branch in the same position (after the designId branch) with the body from the `<interfaces>` section above. This branch uses `supabase.from('scenes')` and `supabase.from('scene_slides')` — confirm the supabase import at top of file (add if missing).
    - The new branch must honor the existing `cancelled` flag pattern used by the surrounding effect (see the designId branch for precedent).

    **Edit 5 — useEffect dependency array (currently at ~lines 144-147):**
    - Change `[urlDesignId, urlTemplateId]` → `[urlDesignId, urlSceneId]`
    - If the dep array includes other values (cancelled flag guards, etc.), preserve them untouched

    After editing, run a final grep to confirm NO `urlTemplateId` remains anywhere in the file and NO `sessionStorage` reference to `pendingTemplate` remains.

    If the file shape differs from the research-time snapshot (e.g., the sessionStorage branch is in a helper function, not the main effect), adapt the same five logical edits. Do not leave orphan code — if removing urlTemplateId leaves an unused `params.get('templateId')` line in parseQueryParams, remove that too.
  </action>
  <verify>
    <automated>! grep -q "urlTemplateId" src/pages/SvgEditorPage.jsx && ! grep -q "sessionStorage.*pendingTemplate" src/pages/SvgEditorPage.jsx && ! grep -q "pendingTemplate" src/pages/SvgEditorPage.jsx && grep -q "urlSceneId" src/pages/SvgEditorPage.jsx && grep -q "from('scenes')" src/pages/SvgEditorPage.jsx && grep -q "from('scene_slides')" src/pages/SvgEditorPage.jsx && grep -q "design_json?.svgContent\|design_json.svgContent" src/pages/SvgEditorPage.jsx; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Negative: `grep -q "urlTemplateId" src/pages/SvgEditorPage.jsx` exits NON-zero (variable fully removed)
    - Negative: `grep -q "sessionStorage.*pendingTemplate" src/pages/SvgEditorPage.jsx` exits NON-zero
    - Negative: `grep -q "'pendingTemplate'" src/pages/SvgEditorPage.jsx` exits NON-zero (literal key gone)
    - `grep -q "urlSceneId" src/pages/SvgEditorPage.jsx`
    - `grep -q "params.get('sceneId')" src/pages/SvgEditorPage.jsx`
    - `grep -q "\.from('scenes')" src/pages/SvgEditorPage.jsx`
    - `grep -q "\.from('scene_slides')" src/pages/SvgEditorPage.jsx`
    - `grep -Eq "design_json\??\.svgContent" src/pages/SvgEditorPage.jsx`
    - `grep -q "btoa(unescape(encodeURIComponent(" src/pages/SvgEditorPage.jsx` (data-URL pattern preserved)
    - useEffect dep array updated: `grep -q "\[urlDesignId, urlSceneId\]\|\[urlSceneId, urlDesignId\]" src/pages/SvgEditorPage.jsx`
    - File still compiles — smoke run: `node -e "import('./src/pages/SvgEditorPage.jsx').then(() => console.log('ok')).catch(e => { console.error(e); process.exit(1); })"` (this may fail due to JSX + React env — skip if so; covered instead by build step)
    - Phase 172 tests still pass: `npx vitest run tests/unit/components/template-gallery tests/unit/services/templateApplyService.test.js tests/integration/preview-apply` returns exit 0
  </acceptance_criteria>
  <done>sessionStorage branch deleted entirely; new sceneId branch added mirroring the designId shape; dep array updated; supabase import present; repo-wide pendingTemplate-reader count is zero.</done>
</task>

<task type="auto">
  <name>Task 3: Delete installWithCustomization from marketplaceService.js; audit installTemplateAsScene</name>
  <files>src/services/marketplaceService.js</files>
  <read_first>
    - /Users/massimodamico/bizscreen/src/services/marketplaceService.js lines 186-240 (both installTemplateAsScene and installWithCustomization)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 7 (lines 452-465)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md §Focus Area 4 (lines 642-653) + §OQ-3 (line 980)
    - Run `grep -rn "installWithCustomization" src/ scripts/ supabase/functions/ tests/ 2>/dev/null` to confirm zero callers before deleting
    - Run `grep -rn "installTemplateAsScene" src/ scripts/ supabase/functions/ tests/ 2>/dev/null` to audit callers
  </read_first>
  <action>
    1. Confirm zero callers of `installWithCustomization` outside the function's own source line. Expected result: only one hit (the definition line). If ANY other call site exists, STOP and flag to the user — the deletion assumption is wrong.

    2. DELETE the `installWithCustomization` function (currently lines 209-237 per research) and any leading JSDoc comment dedicated to it (typically a `/**  Install template with customization ... */` block immediately above). Preserve the shared comment banner if it covers multiple functions — remove only the installWithCustomization-specific doc.

    3. Run the `installTemplateAsScene` audit:
       - If `grep -rn "installTemplateAsScene" src/ scripts/ supabase/functions/ tests/ 2>/dev/null | grep -v "marketplaceService.js:"` returns ZERO hits → proceed to delete `installTemplateAsScene` too (lines 186-200 per research). No callers = no retention value.
       - If the grep returns ≥1 hit outside marketplaceService.js → KEEP `installTemplateAsScene` untouched. Document the surviving caller(s) in the summary (file + line) so future phases can reason about it.

    4. Do NOT remove the top-level `supabase` import or any other service exports. Only the dead function(s) go.

    5. After deletion, run `grep -c "^export" src/services/marketplaceService.js` to count remaining exports — include the number in the summary for traceability.

    6. Check the file still parses: `node -e "import('./src/services/marketplaceService.js').then(() => console.log('ok')).catch(e => { console.error(e.message); process.exit(1); })"` — expect `ok`.
  </action>
  <verify>
    <automated>! grep -q "installWithCustomization" src/services/marketplaceService.js && ! grep -rq "installWithCustomization" src/ scripts/ supabase/functions/ tests/ 2>/dev/null && node -e "import('./src/services/marketplaceService.js').then(() => process.exit(0)).catch(() => process.exit(1))"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Negative: `grep -q "installWithCustomization" src/services/marketplaceService.js` exits NON-zero (function body removed)
    - Negative: `grep -rq "installWithCustomization" src/ scripts/ supabase/functions/ tests/` exits NON-zero (no callers anywhere)
    - `node -e "import('./src/services/marketplaceService.js').then(() => process.exit(0)).catch(() => process.exit(1))"` exits 0 (file still parses and loads)
    - If installTemplateAsScene was removed: `grep -q "installTemplateAsScene" src/services/marketplaceService.js` exits NON-zero AND `grep -rq "installTemplateAsScene" src/ scripts/ supabase/functions/ tests/` exits NON-zero
    - If installTemplateAsScene was retained: summary notes the caller locations
    - No other function exports of marketplaceService.js are accidentally affected (admin surfaces continue to work)
  </acceptance_criteria>
  <done>Dead installWithCustomization removed; installTemplateAsScene audit decision recorded; marketplaceService.js still loads.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-wide sessionStorage sanity sweep</name>
  <files></files>
  <read_first>
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md §D-14 (confirms TPRV-06 demands removal)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md §Runtime State Inventory (lines 385-401) — verified zero writers as of 2026-04-20
  </read_first>
  <action>
    1. Run `grep -rn "pendingTemplate" src/ tests/ 2>/dev/null` — expected: ZERO hits. If ANY hit appears (other than inside a test that asserts absence — e.g., a new E2E assertion), inspect and remove. A hit inside e.g., `tests/e2e/preview-apply.spec.js` that tests "pendingTemplate does not exist in sessionStorage" is fine and should remain.

    2. Run `grep -rn "sessionStorage" src/ 2>/dev/null` — list every remaining sessionStorage user. Confirm NONE relate to template handoff. Expected users (per research §Runtime State Inventory): canvaService (OAuth state), webVitalsService (analytics session), Player.jsx (display cache). Any unexpected hit = investigate.

    3. Record the counts in the task summary: "pendingTemplate repo-wide hits: 0 (or N legitimate assertions in tests/)"; "sessionStorage users in src/: [list]".

    4. This task does not modify any files — it is a verification gate that blocks the plan from completing if the cleanup is incomplete.
  </action>
  <verify>
    <automated>HITS=$(grep -rn "pendingTemplate" src/ 2>/dev/null | grep -v "^src/.*\.test\." | wc -l); echo "src pendingTemplate hits: $HITS"; [ "$HITS" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "pendingTemplate" src/` returns zero hits (or only hits inside tests that assert absence — document clearly)
    - `grep -rn "sessionStorage" src/ | grep -i "template\|pendingTemplate"` returns zero hits (no template-related sessionStorage remains)
    - No new sessionStorage usages introduced by Phase 172
  </acceptance_criteria>
  <done>Zero pendingTemplate readers/writers remain in src/. TPRV-06 verifiable at the repo level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params ↔ SvgEditorPage | `sceneId` is a uuid the client received from the atomic RPC response. Still attacker-influenceable via direct URL navigation. |
| TemplateGalleryPage state ↔ Modal snapshot | `displayedTemplates` array is passed to modal; modal snapshots internally (Plan 05). No trust issue at the page layer. |
| marketplaceService.js dead code | Deleting dead code removes attack surface (even if unreachable today, a future import could bring it back). Positive security posture change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-06 | Elevation of Privilege | `?sceneId=` branch fetches a scene the caller may not own | mitigate | Supabase RLS on `scenes` and `scene_slides` tables constrains SELECT by `tenant_id = auth.uid()`. If the user navigates with a sceneId they don't own, the `supabase.from('scenes').single()` call returns `error: { code: 'PGRST116' }` or an empty result. Editor handles this via the existing error state. No new RLS policy required — inherits Phase 170's audit. |
| T-172-16 | Tampering | Malicious `sceneId` URL param injects SQL-like values | mitigate | `supabase.from('scenes').eq('id', urlSceneId)` parameterizes the value through the PostgREST query builder. No string concatenation. Low risk. |
| T-172-17 | Information Disclosure | `design_json.svgContent` rendered in FabricSvgEditor could contain attacker-injected script if bypassed Plan 03's sanitization | mitigate | FabricSvgEditor (existing component, Phase 166 output) handles SVG rendering via fabric.js parsing — not `dangerouslySetInnerHTML`. Fabric.js parses SVG into canvas draw calls, which do not execute script tags. Defense-in-depth: Plan 03's DOMPurify is the primary gate. |
| T-172-18 | Tampering (dead code revival) | Deleting `installWithCustomization` permanently removes the non-atomic race path | mitigate | Hard deletion (not deprecation) ensures no future caller can import the function. Eliminates T-172-02 regression risk at the language level. |
| T-172-19 | Spoofing | User navigates directly to `svg-editor?sceneId=<own-scene>` bypassing the gallery → modal → Apply flow | accept | This is a feature, not a threat. The branch fetches any scene the user has RLS access to. No privilege boundary crossed. |
</threat_model>

<verification>
- `npx vitest run tests/unit/components/template-gallery tests/unit/services/templateApplyService.test.js tests/integration/preview-apply` still exits 0 — no regressions from wiring.
- `grep -rn "pendingTemplate" src/` returns zero hits.
- `grep -rn "installWithCustomization" src/ tests/` returns zero hits (dead code gone).
- `grep -n "TemplatePreviewModal" src/pages/TemplateGalleryPage.jsx` returns at least one hit (modal is mounted).
- `grep -n "urlSceneId" src/pages/SvgEditorPage.jsx` returns ≥ 3 hits (destructure, effect branch, dep array).
- `npm run build` (or `npx vite build`) completes without new errors (if the project has a production build step; otherwise `npm run dev` starts clean).
</verification>

<success_criteria>
- TPRV-01 reachable from the UI: clicking a card opens the modal.
- TPRV-04 routing end-to-end: Apply → editorRouteFor → onNavigate → SvgEditorPage loads scene by id.
- TPRV-06 verifiable: zero `pendingTemplate` hits in src/; no sessionStorage template-handoff code remains.
- T-172-02 (clone-then-patch race path at client) permanently closed: dead code removed.
- No Phase 171 regressions: gallery still browses, filters, sorts per TGAL-01..05 and TDSC-01..05.
</success_criteria>

<output>
After completion, create `.planning/phases/172-preview-apply-flow/172-06-SUMMARY.md` including:
- Diff summary for each of the 3 modified files (line counts added/removed)
- Result of the installTemplateAsScene audit: was it retained or deleted? (If retained, list the surviving callers with file:line references)
- Final count of exports in src/services/marketplaceService.js (pre vs post)
- Final grep count: `pendingTemplate` in src/ (must be 0)
- Handoff note for Plan 07: "E2E can now assert `expect(sessionStorage.getItem('pendingTemplate')).toBeNull()` after Apply; SvgEditorPage.jsx is wired to `?sceneId=` via the new branch."
</output>
