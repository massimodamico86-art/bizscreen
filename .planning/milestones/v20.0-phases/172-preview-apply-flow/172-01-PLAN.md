---
phase: 172-preview-apply-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx
  - tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx
  - tests/unit/services/templateApplyService.test.js
  - tests/integration/preview-apply/rpc-atomicity.test.js
  - tests/e2e/preview-apply.spec.js
autonomous: true
requirements: [TPRV-01, TPRV-02, TPRV-03, TPRV-04, TPRV-05, TPRV-06]
tags: [testing, wave-0, scaffolds, nyquist]

must_haves:
  truths:
    - "All 5 Wave 0 test files exist on disk and execute (not necessarily pass)"
    - "Each test file has at least one skipped placeholder test covering its primary requirement"
    - "`npx vitest run tests/unit/components/template-gallery tests/unit/services/templateApplyService.test.js tests/integration/preview-apply` reports skipped tests (no errors, no unresolvable imports)"
    - "`npx playwright test tests/e2e/preview-apply.spec.js --list` lists at least one test"
    - "Downstream plans' `<automated>` verify blocks can target these file paths without a MISSING fallback"
  artifacts:
    - path: "tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx"
      provides: "Scaffolds for TPRV-01 open/close/nav, TPRV-03 live-update, Pitfall 2/3/7"
      contains: "describe('TemplatePreviewModal'"
    - path: "tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx"
      provides: "Scaffolds for TPRV-02 sections, TPRV-03 swatch→swapColor, brand-theme prefill"
      contains: "describe('QuickCustomizePanel'"
    - path: "tests/unit/services/templateApplyService.test.js"
      provides: "Scaffolds for TPRV-04 dispatcher, DOMPurify sanitization, editorRouteFor"
      contains: "describe('templateApplyService'"
    - path: "tests/integration/preview-apply/rpc-atomicity.test.js"
      provides: "Scaffolds for TPRV-05 atomicity + license gate"
      contains: "describe('clone_template_with_customization atomicity'"
    - path: "tests/e2e/preview-apply.spec.js"
      provides: "Scaffolds for TPRV-06 sessionStorage absent + full flow"
      contains: "test.describe('Preview + Apply (Phase 172)'"
  key_links:
    - from: "tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx"
      to: "src/components/template-gallery/TemplatePreviewModal.jsx (future)"
      via: "dynamic import inside `it.skip` blocks so tests remain parse-clean without the component yet"
      pattern: "it\\.skip\\("
    - from: "tests/e2e/preview-apply.spec.js"
      to: "tests/e2e/helpers.js"
      via: "loginAndPrepare import"
      pattern: "import.*loginAndPrepare"
---

<objective>
Create 5 net-new test files as skip-stub scaffolds so every downstream plan's `<automated>` verify block can target a real file path (no MISSING placeholders). This is Wave 0 for Phase 172 — infrastructure only, no product code.

Purpose: satisfy Nyquist validation strategy (`172-VALIDATION.md` lines 52-60) which mandates Wave 0 creates all 5 test files before Wave 1+ begins implementation.
Output: 5 test files that parse, collect, and run with all assertions `skip`-guarded; they will be fleshed out in subsequent plans.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/massimodamico/bizscreen/.planning/PROJECT.md
@/Users/massimodamico/bizscreen/.planning/ROADMAP.md
@/Users/massimodamico/bizscreen/.planning/STATE.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-VALIDATION.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md

<interfaces>
<!-- Canonical test-file header extracted from tests/unit/services/marketplaceService.test.js lines 1-51 -->
<!-- All four vitest stubs use this shape (adjust `../` depth for location). -->

From tests/unit/services/marketplaceService.test.js (vitest + supabase-mock header):
```javascript
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('../../../src/supabase', () => ({
  supabase: {
    from: vi.fn(),
    rpc: vi.fn(),
    auth: { getUser: vi.fn() },
  },
}));

import { supabase } from '../../../src/supabase';
```

From tests/unit/services/svgCustomize.test.js (TEST_SVG fixture pattern for panel test):
```javascript
const TEST_SVG = `
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <rect fill="#ff0000" width="50" height="50"/>
  <text id="headline">Hello</text>
  <image id="logo" href="old-logo.png" width="20" height="20"/>
</svg>`;
```

From tests/e2e/template-gallery.spec.js (Phase 171 E2E — test.describe + skip-guard pattern):
```javascript
import { test, expect } from '@playwright/test';
import { loginAndPrepare } from './helpers.js';

test.describe('Preview + Apply (Phase 172)', () => {
  test.skip(() => !process.env.TEST_USER_EMAIL, 'Test credentials not configured');

  test.beforeEach(async ({ page }) => {
    await loginAndPrepare(page, {
      email: process.env.TEST_USER_EMAIL,
      password: process.env.TEST_USER_PASSWORD,
    });
  });

  // tests
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create vitest component-test directory scaffolds (TemplatePreviewModal + QuickCustomizePanel)</name>
  <files>
    tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx,
    tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx
  </files>
  <read_first>
    - /Users/massimodamico/bizscreen/tests/unit/services/marketplaceService.test.js (entire — 80 lines; canonical vitest mock-supabase header for depth `../../../../src/...`)
    - /Users/massimodamico/bizscreen/tests/unit/services/svgCustomize.test.js (lines 1-30 — TEST_SVG fixture to reuse in QuickCustomizePanel.test.jsx)
    - /Users/massimodamico/bizscreen/vitest.config.js (confirm env: jsdom, test include globs cover tests/unit/**)
    - /Users/massimodamico/bizscreen/tests/setup.js (confirm RTL matchers available via @testing-library/jest-dom)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 8 (lines 468-502) and §File 9 (lines 505-522) — required test-case lists for both files
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-UI-SPEC.md §QuickCustomizePanel Sections + §Copywriting Contract (for test-case assertion strings)
  </read_first>
  <behavior>
    - Both files parse with no syntax errors (vitest collects them).
    - Each file contains at least one `describe(...)` block.
    - Each file contains every required test case as `it.skip(...)` (not `it.todo`) with the exact test name specified below — so subsequent plans can remove `.skip` and fill the body without restructuring.
    - `npx vitest run tests/unit/components/template-gallery` exits with code 0 and reports skipped (not failing) tests.
    - No imports of not-yet-existing product files at module scope. If a product import is needed for a future assertion, use a `// eslint-disable-next-line` comment and a dynamic `await import(...)` INSIDE the skipped block only, so the file remains parseable today.
  </behavior>
  <action>
    Create `tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx` with:
    - Header copy-adapted from `tests/unit/services/marketplaceService.test.js:1-51`, adjusting import depth to `../../../../src/...` (4 levels up from this file).
    - Mock stubs at module scope:
      ```javascript
      vi.mock('../../../../src/services/templateApplyService', () => ({
        applyTemplate: vi.fn(),
        editorRouteFor: vi.fn(),
      }));
      vi.mock('../../../../src/services/brandThemeService', () => ({
        getBrandTheme: vi.fn().mockResolvedValue({ data: null, error: null }),
      }));
      ```
      Do NOT import the not-yet-existing `TemplatePreviewModal` at module scope — guard behind `it.skip` only.
    - One `describe('TemplatePreviewModal', () => { ... })` block containing these exact `it.skip(...)` entries (per 172-PATTERNS.md §File 8 required cases + 172-VALIDATION.md Wave 0 coverage list):
      1. `it.skip('opens with initialIndex and shows that template name (TPRV-01)', ...)`
      2. `it.skip('ArrowRight advances index; ArrowLeft decrements; wraps at boundaries (TPRV-01)', ...)`
      3. `it.skip('ArrowRight is a no-op when an <input> is focused (TPRV-01 keyboard guard)', ...)`
      4. `it.skip('Apply button disables and shows "Applying…" during in-flight RPC (Pitfall 2)', ...)`
      5. `it.skip('on applyTemplate rejection, inline Alert appears and Apply re-enables (D-13)', ...)`
      6. `it.skip('prev/next remounts QuickCustomizePanel via key={template.id} (Pitfall 3)', ...)`
      7. `it.skip('snapshot semantics — parent-side templates mutation does not shift modal content (Pitfall 7)', ...)`
      8. `it.skip('live preview reflects customized SVG via dangerouslySetInnerHTML (TPRV-03)', ...)`
    - Each skipped body = `// TODO: Plan 05 — TemplatePreviewModal shell` (plus a leading `expect(true).toBe(true);` as a placeholder assertion so the body is non-empty).

    Create `tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx` with:
    - Same header style; mock only `brandThemeService`:
      ```javascript
      vi.mock('../../../../src/services/brandThemeService', () => ({
        getBrandTheme: vi.fn().mockResolvedValue({ data: null, error: null }),
      }));
      ```
      Do NOT mock `svgCustomizeService` — tests will use the real helpers (they are pure DOM mutations).
    - Declare the TEST_SVG fixture verbatim from `tests/unit/services/svgCustomize.test.js` header (a small inline SVG with one `fill`, one `<text id="headline">`, one `<image id="logo">`).
    - One `describe('QuickCustomizePanel', () => { ... })` with `it.skip(...)` entries (per 172-PATTERNS.md §File 9):
      1. `it.skip('renders Colors / Logo / Text headers per Copywriting Contract (TPRV-02)', ...)`
      2. `it.skip('calls getBrandTheme() once on mount; swatches reflect primary/secondary/accent from theme (brand prefill)', ...)`
      3. `it.skip('color swatch change (50ms debounced) → onChange fires with serialized SVG where fill is swapped (TPRV-03)', ...)`
      4. `it.skip('text input onBlur → onChange fires with updated text; mid-typing onChange does not fire', ...)`
      5. `it.skip('empty-state — if extractColors/extractTextNodes/findLogoElement all empty → renders "This template has no customizable elements." (Pitfall 6)', ...)`
      6. `it.skip('Remove logo link fires replaceLogo(doc, null) and onChange emits updated SVG', ...)`
    - Each skipped body = `// TODO: Plan 04 — QuickCustomizePanel component` + `expect(true).toBe(true);`.

    Verify both files exist via `ls tests/unit/components/template-gallery/`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/components/template-gallery --reporter=default 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx` exits 0
    - `test -f tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx` exits 0
    - `grep -c "it.skip" tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx` returns ≥ 8
    - `grep -c "it.skip" tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx` returns ≥ 6
    - `grep -q "describe('TemplatePreviewModal'" tests/unit/components/template-gallery/TemplatePreviewModal.test.jsx`
    - `grep -q "describe('QuickCustomizePanel'" tests/unit/components/template-gallery/QuickCustomizePanel.test.jsx`
    - `npx vitest run tests/unit/components/template-gallery 2>&1 | grep -E "Tests.*skipped"` — at least 14 skipped total
    - Exit code of the vitest run is 0
  </acceptance_criteria>
  <done>Both files exist, parse, collect, and run with 14 skipped tests (0 failed, 0 passed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create vitest service + integration test scaffolds (templateApplyService + rpc-atomicity)</name>
  <files>
    tests/unit/services/templateApplyService.test.js,
    tests/integration/preview-apply/rpc-atomicity.test.js
  </files>
  <read_first>
    - /Users/massimodamico/bizscreen/tests/unit/services/marketplaceService.test.js (lines 1-80 — exact header + rpc-mock shape)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 10 (lines 525-559) and §File 11 (lines 563-577) — required test-case lists
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md §D-09, D-11, D-17 (for inline comments anchoring the skipped tests to decisions)
    - /Users/massimodamico/bizscreen/vitest.config.js (confirm tests/integration/** is in the test include glob; if missing, note for Plan 03)
  </read_first>
  <behavior>
    - Both files parse and collect.
    - `npx vitest run tests/unit/services/templateApplyService.test.js tests/integration/preview-apply/rpc-atomicity.test.js` exits code 0, reports skipped tests only.
    - Each `it.skip` name cites the requirement ID (TPRV-04 / TPRV-05) and/or the Pitfall number verbatim.
  </behavior>
  <action>
    Create `tests/unit/services/templateApplyService.test.js` with:
    - Header copied from `tests/unit/services/marketplaceService.test.js:1-66`, depth `../../../src/...` (3 levels up).
    - Module-scope mocks:
      ```javascript
      vi.mock('../../../src/supabase', () => ({
        supabase: { rpc: vi.fn() },
      }));
      vi.mock('dompurify', () => ({
        default: { sanitize: vi.fn((s) => s) },
      }));
      ```
      Do NOT import `templateApplyService` at module scope — guard behind skipped blocks.
    - One `describe('templateApplyService', () => { ... })` with these exact `it.skip(...)` entries (per 172-PATTERNS.md §File 10):
      1. `it.skip("editor_type='svg' with customizedSvg sanitizes, calls rpc('clone_template_with_customization', {p_template_id, p_scene_name, p_customized_svg}), returns sceneId (TPRV-04)", ...)`
      2. `it.skip("editor_type='svg' with no customizedSvg calls RPC with p_customized_svg: null (TPRV-04)", ...)`
      3. `it.skip("editor_type='polotno' calls rpc('clone_template_to_scene', {p_template_id, p_scene_name}) with NO third arg (TPRV-04)", ...)`
      4. `it.skip('unknown editor_type throws with literal error message (TPRV-04)', ...)`
      5. `it.skip('DOMPurify.sanitize is called before RPC; <script> tags removed from payload (T-172-01 / Pitfall 5)', ...)`
      6. `it.skip('RPC error propagates as thrown error (.rejects.toThrow)', ...)`
      7. `it.skip("editorRouteFor returns 'svg-editor?sceneId={id}' for SVG and 'scene-editor-{id}' for Polotno (D-15/D-12)", ...)`
    - Each skipped body: `// TODO: Plan 03 — templateApplyService dispatcher` + `expect(true).toBe(true);`.

    Create `tests/integration/preview-apply/rpc-atomicity.test.js` with:
    - Ensure the directory `tests/integration/preview-apply/` exists (`mkdir -p` via the shell before writing).
    - Same vitest + supabase-mock header (depth `../../../src/...`).
    - One `describe('clone_template_with_customization atomicity', () => { ... })` with these exact `it.skip(...)` entries (per 172-PATTERNS.md §File 11):
      1. `it.skip('RPC resolves after 500ms delay — Apply button stays disabled for full duration (TPRV-05 / Pitfall 2)', ...)`
      2. `it.skip("RPC rejects with 'Access denied: insufficient plan tier' — applyTemplate throws same message (TPRV-05 / T-172-03)", ...)`
      3. `it.skip('RPC resolves with scene uuid — zero follow-up UPDATE issued (atomic contract, TPRV-05)', ...)`
      4. `it.skip('live RPC verification is a manual checklist item — this test covers only the client-side contract', ...)`
    - Each skipped body: `// TODO: Plan 03 — RPC atomicity integration` + `expect(true).toBe(true);`.

    If `vitest.config.js` does NOT already include `tests/integration/**` in its test glob, call it out in the Wave-0 summary (do NOT modify the config in this plan — Plan 03 owns the integration test's actual fill-in and can adjust config if needed).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/services/templateApplyService.test.js tests/integration/preview-apply/rpc-atomicity.test.js --reporter=default 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/unit/services/templateApplyService.test.js` exits 0
    - `test -f tests/integration/preview-apply/rpc-atomicity.test.js` exits 0
    - `test -d tests/integration/preview-apply` exits 0
    - `grep -c "it.skip" tests/unit/services/templateApplyService.test.js` returns ≥ 7
    - `grep -c "it.skip" tests/integration/preview-apply/rpc-atomicity.test.js` returns ≥ 4
    - `grep -q "describe('templateApplyService'" tests/unit/services/templateApplyService.test.js`
    - `grep -q "describe('clone_template_with_customization atomicity'" tests/integration/preview-apply/rpc-atomicity.test.js`
    - Both files are collected by `npx vitest --list` (or equivalent dry-run invocation)
    - `npx vitest run tests/unit/services/templateApplyService.test.js tests/integration/preview-apply/rpc-atomicity.test.js` exits 0 with all tests skipped
  </acceptance_criteria>
  <done>Both files exist, parse, collect, and show ≥11 skipped tests on `npx vitest run`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Playwright E2E scaffold (preview-apply.spec.js)</name>
  <files>tests/e2e/preview-apply.spec.js</files>
  <read_first>
    - /Users/massimodamico/bizscreen/tests/e2e/template-gallery.spec.js (lines 1-80 — Phase 171 baseline with loginAndPrepare + test.skip guard + test.describe header)
    - /Users/massimodamico/bizscreen/tests/e2e/helpers.js (confirm `loginAndPrepare` and `waitForPageReady` exports)
    - /Users/massimodamico/bizscreen/playwright.config.js (confirm testDir: 'tests/e2e' so the new spec is picked up)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 12 (lines 580-611) — required E2E test-case list
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md §D-12, D-14, D-15 (route targets + sessionStorage removal contract)
  </read_first>
  <behavior>
    - `npx playwright test tests/e2e/preview-apply.spec.js --list` lists ≥5 tests.
    - Without `TEST_USER_EMAIL`, the entire describe block is skipped (per the test.skip guard) — this matches Phase 171 convention.
    - File uses `test.skip(() => !process.env.TEST_USER_EMAIL, ...)` at the describe level.
    - Every body is a `test.fixme` or `test.skip` with a TODO pointing to Plan 07.
  </behavior>
  <action>
    Create `tests/e2e/preview-apply.spec.js` with:
    - Header copied from `tests/e2e/template-gallery.spec.js:1-30` (imports of test/expect from @playwright/test and `loginAndPrepare` from `./helpers.js`).
    - `test.describe('Preview + Apply (Phase 172)', () => { ... })` containing a describe-level `test.skip(() => !process.env.TEST_USER_EMAIL, 'Test credentials not configured');` and a `test.beforeEach` that awaits `loginAndPrepare` (copy from the analog spec).
    - These exact `test.fixme(...)` entries (use `fixme` for E2E — marks as expected-to-fail so report shows "known pending"; Playwright supports `test.fixme(name, fn)` as skip-equivalent with a stronger semantic than `test.skip` for "will implement later"):
      1. `test.fixme('opens modal from gallery card click; toolbar shows Apply to new scene CTA and nav counter (TPRV-01)', ...)`
      2. `test.fixme('ArrowRight advances template — toolbar name changes (TPRV-01 keyboard nav)', ...)`
      3. `test.fixme('Click Apply to new scene lands on svg-editor?sceneId=… (TPRV-04 / D-15)', ...)`
      4. `test.fixme('after Apply, sessionStorage has no pendingTemplate key — evaluated in page context (TPRV-06)', ...)`
      5. `test.fixme('no error alerts appear during happy path — structural assertion page.locator([role="alert"]).toHaveCount(0) (structural)', ...)`
    - Each body: `// TODO: Plan 07 — E2E full flow + sessionStorage assertion` inside an `async ({ page }) => { expect(true).toBe(true); }` wrapper.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/preview-apply.spec.js --list 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/e2e/preview-apply.spec.js` exits 0
    - `grep -c "test.fixme" tests/e2e/preview-apply.spec.js` returns ≥ 5
    - `grep -q "test.describe('Preview + Apply (Phase 172)'" tests/e2e/preview-apply.spec.js`
    - `grep -q "loginAndPrepare" tests/e2e/preview-apply.spec.js`
    - `npx playwright test tests/e2e/preview-apply.spec.js --list 2>&1 | grep -cE "fixme|›"` ≥ 5
  </acceptance_criteria>
  <done>E2E spec exists, lists ≥5 tests via Playwright's --list, and is marked as pending-fill via test.fixme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev filesystem ↔ CI runner | Test files become executable code; a malicious test author could exfiltrate env vars. Out of scope — trusted solo dev. |
| Playwright spec ↔ auth credentials | `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` are read from env; spec must not log them. Mitigated by existing `loginAndPrepare` helper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-01 (mitigation scaffold) | Tampering | DOMPurify integration test — scaffolded here as `it.skip(...'<script> tags removed from payload'...)` | mitigate | Test body is filled in Plan 03; scaffold ensures the requirement is visible in CI runs from Wave 1 onward. |
| T-172-wave0-01 | Information Disclosure | Playwright spec logging `TEST_USER_*` env vars | mitigate | Use only `loginAndPrepare(page, {email, password})` helper; do not add `console.log` of credentials. Enforced by convention in the copied header. |
| T-172-wave0-02 | Tampering | Dynamic `await import(productFile)` inside test bodies could execute unverified code | accept | All dynamic imports are inside `it.skip` / `test.fixme` bodies that never run in Wave 0; risk materialises only when bodies are filled (Plans 03-07). |
</threat_model>

<verification>
- All 5 test files exist and are discoverable by their respective runners.
- `npx vitest run tests/unit/components/template-gallery tests/unit/services/templateApplyService.test.js tests/integration/preview-apply` exits 0, shows skipped tests only.
- `npx playwright test tests/e2e/preview-apply.spec.js --list` lists ≥5 tests.
- No product code has been imported at module scope (grep `^import` for `src/components` or `src/services` outside of module-scope `vi.mock` factories should return zero hits pointing to not-yet-existing files).
</verification>

<success_criteria>
- All 5 files committed on disk.
- Downstream plans reference these exact file paths in their `<automated>` blocks without needing a MISSING fallback.
- Feedback latency for quick runs stays under 20s (per 172-VALIDATION.md Sampling Rate).
- `172-VALIDATION.md` Wave 0 checklist (lines 53-58) can be flipped to `[x]` after this plan ships.
</success_criteria>

<output>
After completion, create `.planning/phases/172-preview-apply-flow/172-01-SUMMARY.md` using the standard summary template. Specifically note:
- All 5 Wave 0 file paths (absolute)
- Total skipped-test count (should be ≥25 across vitest + playwright)
- Any deviation from the research plan (e.g., if `tests/integration/**` was not in the vitest include glob and the task had to note it for Plan 03)
- Confirmation that no product code was imported at module scope
</output>
