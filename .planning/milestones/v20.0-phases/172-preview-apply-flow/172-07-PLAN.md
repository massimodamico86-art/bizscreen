---
phase: 172-preview-apply-flow
plan: 07
type: execute
wave: 5
depends_on: [172-06]
files_modified:
  - tests/e2e/preview-apply.spec.js
autonomous: false
requirements: [TPRV-01, TPRV-04, TPRV-06]
tags: [e2e, playwright, verification, session-storage, manual-checklist]

must_haves:
  truths:
    - "`tests/e2e/preview-apply.spec.js` contains at least 5 real (non-fixme) Playwright tests covering the full gallery → modal → customize → Apply → editor flow"
    - "At least one test asserts `sessionStorage.getItem('pendingTemplate') === null` after Apply (TPRV-06 end-to-end proof)"
    - "At least one test asserts the post-Apply URL matches `/svg-editor\\?sceneId=.+/` (TPRV-04 end-to-end proof)"
    - "Tests use structural assertions (role, label, regex) — no screenshot-diff or exact-template-count assertions (per TQAL-05 convention already inherited from Phase 171)"
    - "Tests honor the existing `test.skip(() => !process.env.TEST_USER_EMAIL, ...)` guard so CI without credentials skips cleanly"
    - "`npx playwright test tests/e2e/preview-apply.spec.js` exits 0 when credentials are present; exits 0 with all tests skipped when absent"
    - "Manual verification checklist items from `172-VALIDATION.md §Manual-Only Verifications` have been walked through by the developer, and sign-off captured in the plan summary"
  artifacts:
    - path: "tests/e2e/preview-apply.spec.js"
      provides: "End-to-end coverage of Phase 172 flow + TPRV-06 sessionStorage assertion"
      exports: []
      contains: "svg-editor?sceneId="
  key_links:
    - from: "tests/e2e/preview-apply.spec.js"
      to: "tests/e2e/helpers.js (loginAndPrepare)"
      via: "import"
      pattern: "import.*loginAndPrepare.*from\\s+['\"]\\./helpers"
    - from: "tests/e2e/preview-apply.spec.js"
      to: "sessionStorage API in page context"
      via: "page.evaluate(() => sessionStorage.getItem('pendingTemplate'))"
      pattern: "sessionStorage\\.getItem\\(['\"]pendingTemplate['\"]\\)"
---

<objective>
Flesh out the Playwright E2E spec from its Plan-01 scaffold into real tests that exercise the full preview → customize → apply → editor flow and assert TPRV-06 (no sessionStorage handoff remains). Run the human verification checklist from `172-VALIDATION.md` and capture sign-off in the summary. This is the final plan of Phase 172 — after this lands, `/gsd-verify-work 172` can run.

Purpose: TPRV-01 (open modal, navigate), TPRV-04 (Apply routes to correct editor), TPRV-06 (sessionStorage gone at runtime — not just at source level). Closes the phase by providing observable behavioral coverage and human-sign-off on the visual/interaction items that Playwright cannot automate.
Output: one fleshed-out E2E spec + one human verification pass.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-CONTEXT.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-RESEARCH.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-UI-SPEC.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-VALIDATION.md
@/Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-06-SUMMARY.md

<interfaces>
Phase 171 E2E baseline (copy patterns from tests/e2e/template-gallery.spec.js lines 1-80):

```javascript
import { test, expect } from '@playwright/test';
import { loginAndPrepare } from './helpers.js';

test.describe('Preview + Apply (Phase 172)', () => {
  test.skip(() => !process.env.TEST_USER_EMAIL, 'Test credentials not configured');

  test.beforeEach(async ({ page }) => {
    await loginAndPrepare(page, {
      email: process.env.TEST_USER_EMAIL,
      password: process.env.TEST_USER_PASSWORD,
    });
    await page.goto('/template-gallery');  // confirm exact route via grep of App.jsx pageMap
    await page.waitForSelector('[data-testid="template-card"]', { timeout: 10000 });  // adjust if Phase 171 did not add testid — use role="article" or similar structural selector
  });

  // tests...
});
```

SessionStorage assertion pattern (TPRV-06 runtime proof):
```javascript
const pending = await page.evaluate(() => sessionStorage.getItem('pendingTemplate'));
expect(pending).toBeNull();
```

URL assertion pattern (TPRV-04 proof):
```javascript
await expect(page).toHaveURL(/svg-editor\?sceneId=[0-9a-f\-]+/);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace test.fixme scaffolds with real Playwright tests</name>
  <files>tests/e2e/preview-apply.spec.js</files>
  <read_first>
    - /Users/massimodamico/bizscreen/tests/e2e/preview-apply.spec.js (Plan 01 scaffold — 5 test.fixme entries with exact names to preserve)
    - /Users/massimodamico/bizscreen/tests/e2e/template-gallery.spec.js (ENTIRE file — the Phase 171 baseline; confirm: how are templates located? selector used? describe structure?)
    - /Users/massimodamico/bizscreen/tests/e2e/helpers.js (ENTIRE file — signatures of loginAndPrepare, waitForPageReady, and any gotoTemplates helper)
    - /Users/massimodamico/bizscreen/playwright.config.js (baseURL, testDir, timeout, browsers)
    - /Users/massimodamico/bizscreen/src/pages/TemplateGalleryPage.jsx (locate the real test id or structural selector for cards — Phase 171 may have added one)
    - /Users/massimodamico/bizscreen/src/components/template-gallery/TemplatePreviewModal.jsx (locate stable selectors — role="dialog", aria-labelledby="preview-title", button aria-label="Apply to new scene", etc.)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-PATTERNS.md §File 12 (lines 580-611) — required E2E coverage list
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-VALIDATION.md §Manual-Only Verifications (what Playwright CANNOT cover → keep as manual)
    - /Users/massimodamico/bizscreen/src/App.jsx — confirm the gallery route slug (`template-gallery`? `templates`? grep the pageMap)
  </read_first>
  <behavior>
    Preserve the describe block + 5 test.fixme names. Remove each `.fixme` and fill the body with real Playwright assertions.

    **Preserved test names + real bodies:**

    1. `test('opens modal from gallery card click; toolbar shows Apply to new scene CTA and nav counter (TPRV-01)', async ({ page }) => { ... })`
       - Click the first template card (structural selector — e.g., `page.getByRole('article').first()` or `page.locator('[data-testid="template-card"]').first()` — use whatever Phase 171 established)
       - `await expect(page.getByRole('dialog')).toBeVisible()` (modal opened)
       - `await expect(page.getByRole('button', { name: 'Apply to new scene' })).toBeVisible()`
       - `await expect(page.getByText(/\d+ of \d+/)).toBeVisible()` (nav counter — regex structural, not exact count per TQAL-05)

    2. `test('ArrowRight advances template — toolbar name changes (TPRV-01 keyboard nav)', async ({ page }) => { ... })`
       - Open modal on first card
       - Capture the toolbar heading text before: `const name1 = await page.locator('#preview-title').textContent();`
       - `await page.keyboard.press('ArrowRight')`
       - `const name2 = await page.locator('#preview-title').textContent();`
       - `expect(name2).not.toBe(name1)` — template changed (structural, not "is this specific template" per TQAL-05)
       - Skip this test if only 1 template in the gallery (page.locator('[data-testid="template-card"]').count() === 1 → `test.skip(condition, reason)`)

    3. `test('Click Apply to new scene lands on svg-editor?sceneId=… (TPRV-04 / D-15)', async ({ page }) => { ... })`
       - Open modal on first card (need to ensure it's an SVG template; either filter the gallery first via URL param or pick a card whose orientation/name implies SVG; simplest — find the first card, open, inspect any indicator. Fallback: just click Apply and assert URL pattern matches EITHER svg-editor OR scene-editor depending on editor_type).
       - `await page.getByRole('button', { name: 'Apply to new scene' }).click()`
       - `await expect(page).toHaveURL(/(svg-editor\?sceneId=|scene-editor-)[0-9a-f\-]+/i)` — covers both branches (SVG: `?sceneId=`, Polotno: `scene-editor-`)
       - Stronger variant: filter gallery to SVG-only via URL (if Phase 171 supports `?editor_type=svg`), then assert the stricter `/svg-editor\?sceneId=/` pattern. If filter param doesn't exist, stick with the union regex.

    4. `test('after Apply, sessionStorage has no pendingTemplate key — evaluated in page context (TPRV-06)', async ({ page }) => { ... })`
       - Same setup as Test 3 (open modal, click Apply, wait for URL change)
       - `const pending = await page.evaluate(() => sessionStorage.getItem('pendingTemplate'))`
       - `expect(pending).toBeNull()` — TPRV-06 end-to-end proof
       - Also assert before Apply: pre-Apply sessionStorage should also be null (not set by gallery or modal)
       - `const preApply = await page.evaluate(() => sessionStorage.getItem('pendingTemplate')); expect(preApply).toBeNull();`

    5. `test('no error alerts appear during happy path — structural assertion page.locator([role="alert"]).toHaveCount(0) (structural)', async ({ page }) => { ... })`
       - Open modal
       - `await expect(page.locator('[role="alert"]')).toHaveCount(0)` before Apply
       - Click Apply, wait for URL change
       - After navigation to editor, scoped alert check: `await expect(page.locator('[role="alert"]')).toHaveCount(0)` (editor page should not show errors on successful load)

    Add 2 new tests to strengthen coverage (stretches the scaffold count from 5 to 7):

    6. `test('Escape key closes modal and returns focus to the originating card (TPRV-01 a11y)', async ({ page }) => { ... })`
       - Focus a card via Tab or direct focus call
       - Press Enter or click to open modal
       - Press Escape
       - `await expect(page.getByRole('dialog')).not.toBeVisible()`
       - Ideally: assert focus returns to the originating card via `document.activeElement` evaluation (may be design-system Modal behavior; if it fails consistently, downgrade to "modal closes" and document as manual-only)

    7. `test('Apply button stays disabled during in-flight request (Pitfall 2 runtime)', async ({ page }) => { ... })`
       - Open modal
       - Intercept the Supabase RPC with `page.route` to delay the response by 1500ms
       - Click Apply
       - Within 500ms: `await expect(page.getByRole('button', { name: 'Applying…' })).toBeDisabled()`
       - Wait for URL change (max 3s) to confirm the flow completes

    For each test, use `test.describe.configure({ mode: 'serial' })` only if tests share state; otherwise parallel default is fine.
  </behavior>
  <action>
    1. Read the scaffold (5 `test.fixme` entries with specific names).
    2. Remove each `.fixme`; keep names verbatim; replace body stubs with the real assertions from `<behavior>` above.
    3. Add Tests 6 and 7 as new `test(...)` blocks at the end of the describe.
    4. Ensure the describe-level `test.skip(() => !process.env.TEST_USER_EMAIL, ...)` guard is preserved so runs without credentials skip cleanly.
    5. Confirm selector fidelity against the real DOM:
       - `page.getByRole('dialog')` works because design-system Modal sets `role="dialog" aria-modal="true"` (UI-SPEC Accessibility Contract)
       - `page.getByRole('button', { name: 'Apply to new scene' })` works because Button has accessible name from its text content
       - `page.locator('#preview-title')` works because TemplatePreviewModal sets `<h2 id="preview-title">` (Plan 05)
       - `[data-testid="template-card"]` — CONFIRM Phase 171 added this; if not, use `page.getByRole('article')` or the selector Phase 171's own E2E uses
    6. Run `npx playwright test tests/e2e/preview-apply.spec.js` locally.
       - If `TEST_USER_EMAIL` is present → all 7 tests should pass.
       - If not → test.skip guard kicks in → entire describe skipped, exit 0.
    7. Do NOT add hard waits (`page.waitForTimeout`) — use `toHaveURL`, `toBeVisible`, `waitForResponse` with predicates instead.
    8. Keep tests independent — no cross-test state; each test re-runs `loginAndPrepare` via the beforeEach.
  </action>
  <verify>
    <automated>test -f tests/e2e/preview-apply.spec.js && ! grep -q "test.fixme" tests/e2e/preview-apply.spec.js && grep -q "sessionStorage.getItem('pendingTemplate')" tests/e2e/preview-apply.spec.js && grep -Eq "svg-editor\\\\\\?sceneId=|svg-editor.\\?.sceneId" tests/e2e/preview-apply.spec.js && grep -q "test.skip.*TEST_USER_EMAIL" tests/e2e/preview-apply.spec.js && npx playwright test tests/e2e/preview-apply.spec.js --list 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/e2e/preview-apply.spec.js` exits 0
    - Negative: `grep -q "test.fixme" tests/e2e/preview-apply.spec.js` exits NON-zero (all fixmes removed)
    - `grep -Ec "^\s*test\(" tests/e2e/preview-apply.spec.js` returns ≥ 7
    - `grep -q "sessionStorage.getItem('pendingTemplate')" tests/e2e/preview-apply.spec.js` (TPRV-06 runtime assertion)
    - `grep -q "toBeNull" tests/e2e/preview-apply.spec.js`
    - `grep -q "svg-editor" tests/e2e/preview-apply.spec.js` (URL pattern)
    - `grep -q "sceneId" tests/e2e/preview-apply.spec.js`
    - `grep -q "role.*dialog" tests/e2e/preview-apply.spec.js`
    - `grep -q "Apply to new scene" tests/e2e/preview-apply.spec.js`
    - `grep -q "ArrowRight" tests/e2e/preview-apply.spec.js`
    - `grep -q "Escape" tests/e2e/preview-apply.spec.js`
    - Negative: `grep -q "page.waitForTimeout" tests/e2e/preview-apply.spec.js` exits NON-zero (no hard waits)
    - Negative: `grep -q "toHaveScreenshot\|screenshot(" tests/e2e/preview-apply.spec.js` exits NON-zero (no screenshot-diff — TQAL-05 convention)
    - `grep -q "test.skip.*TEST_USER_EMAIL" tests/e2e/preview-apply.spec.js` (skip guard preserved)
    - `npx playwright test tests/e2e/preview-apply.spec.js --list 2>&1 | grep -cE "›"` ≥ 7
    - With credentials available: `npx playwright test tests/e2e/preview-apply.spec.js` exits 0 with all tests passed
    - Without credentials: `npx playwright test tests/e2e/preview-apply.spec.js` exits 0 with all tests skipped
  </acceptance_criteria>
  <done>E2E spec has 7 passing tests with credentials (or 7 cleanly-skipped without); asserts TPRV-06 at runtime via page.evaluate and TPRV-04 via URL regex; no hard waits; no screenshot-diffs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification checklist (items Playwright cannot automate)</name>
  <files></files>
  <read_first>
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-VALIDATION.md §Manual-Only Verifications (full table, lines 63-75)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-UI-SPEC.md (to cross-reference visual expectations)
  </read_first>
  <what-built>
    Phase 172 implementation is complete across Plans 01-06:
    - Wave 0 test scaffolds (Plan 01)
    - Atomic RPC + schema push (Plan 02)
    - templateApplyService with DOMPurify (Plan 03)
    - QuickCustomizePanel (Plan 04)
    - TemplatePreviewModal (Plan 05)
    - Gallery wiring + sessionStorage removal (Plan 06)
    - E2E coverage (Task 1 above)

    The automated test matrix covers TPRV-01/02/03/04/05/06 at unit, integration, and E2E levels. Human sign-off is needed for visual/device-specific items in `172-VALIDATION.md §Manual-Only Verifications` that neither vitest (no layout engine) nor Playwright (no native OS UI) can assert.
  </what-built>
  <how-to-verify>
    The executor runs `npm run dev` (or the project's dev server start command) first so the human can reach the app.

    Then walk the 172-VALIDATION.md §Manual-Only Verifications checklist (for each item, mark PASS / FAIL in the reply):

    1. **Split-view proportions 65/35 at 1440px** — Open Chrome DevTools, set viewport 1440x900, open a template's preview modal. Measure the preview pane vs panel. Expected: preview ≈ 65% width, panel ≈ 35%. Eyeballing is fine; pixel-perfect not required.

    2. **Stacked mobile layout at 375px** — DevTools device toolbar → iPhone SE (375px). Open the modal. Expected: preview pinned to top, panel scrolls beneath, Apply button sticky at the bottom.

    3. **Keyboard-only full flow** — From the gallery, press Tab until a template card has focus. Press Enter → modal opens. Press ArrowRight → next template. Press Escape → modal closes, focus returns to the originating card.

    4. **Color swatch opens native picker on touch** — On a real iOS or Android device (or BrowserStack), open a template, tap a color swatch in QuickCustomizePanel. Expected: native OS color picker appears. If no real device available, flag as DEFERRED and document.

    5. **Backdrop click does NOT close modal** — Open the modal. Click the dim area surrounding the modal card. Expected: modal stays open. This is a regression-prone behavior per D-01.

    6. **Apply failure Alert behavior** — Sign out of Supabase briefly (or corrupt the auth token in DevTools), then try Apply. Expected: inline Alert appears above the Apply button with the exact copy "Couldn't apply template. Your customizations are saved — tap Apply to try again." Apply button re-enables after the Alert.

    7. **Polotno apply lands on scene-editor** — Filter the gallery to a Polotno template (if one exists in the dev DB). Apply it. Expected URL: `scene-editor-<uuid>`. Rendering fidelity INSIDE scene editor is D-16's explicit out-of-scope note; do not hold the phase on it.

    8. **DOMPurify actually strips `<script>`/`on*`** — Open DevTools Network tab. Open a template. In the DOMPurify-aware environment, inject `<script>` into one of the QuickCustomizePanel text fields (if the input accepts it visually — it will because it's textContent-based, displaying the literal text). Apply. Intercept the `supabase.rpc('clone_template_with_customization', ...)` call (use DevTools breakpoints or a console log added temporarily). Inspect the `p_customized_svg` payload. Expected: no `<script>` tags, no `on*=` attributes. If any appear, DOMPurify integration is broken — block the phase.

    Reply with PASS or FAIL per line. If any item is DEFERRED (e.g., no iOS device available for Test 4), flag it and recommend follow-up.

    Document the results in the plan summary. If all items PASS (or acceptable DEFERRED), flip `172-VALIDATION.md` frontmatter: `nyquist_compliant: true` and `wave_0_complete: true`.
  </how-to-verify>
  <acceptance_criteria>
    - All 8 manual checklist items have been walked through.
    - Each item is recorded as PASS, FAIL, or DEFERRED in the reply.
    - No FAIL on Items 1, 2, 3, 5, 6, 8 (these are release-blockers). Items 4 and 7 may be DEFERRED if environment is unavailable (real mobile device / Polotno template in dev DB).
    - If DOMPurify verification (Item 8) FAILs: STOP, do not flip sign-off flags. Return to Plan 03 for diagnosis.
  </acceptance_criteria>
  <resume-signal>Reply with the pass/fail/deferred breakdown for all 8 items. Type "all passed" or "all passed with deferrals" to advance. Type any item as "FAIL" to trigger revision.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip nyquist + UI-SPEC sign-off flags; record sign-off metadata</name>
  <files>
    .planning/phases/172-preview-apply-flow/172-VALIDATION.md,
    .planning/phases/172-preview-apply-flow/172-UI-SPEC.md
  </files>
  <read_first>
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-VALIDATION.md frontmatter (lines 1-8)
    - /Users/massimodamico/bizscreen/.planning/phases/172-preview-apply-flow/172-UI-SPEC.md frontmatter (lines 1-9) and §Checker Sign-Off (lines 374-381)
    - Plan 07 Task 2 reply (capture the pass/fail summary into the plan summary)
  </read_first>
  <action>
    Only runs if Task 2 returned "all passed" (or acceptable deferrals). If Task 2 failed, STOP.

    1. Update `172-VALIDATION.md` frontmatter:
       - `nyquist_compliant: false` → `nyquist_compliant: true`
       - `wave_0_complete: false` → `wave_0_complete: true`
       - Add `signed_off_at: <today's ISO date>` under `reviewed_at`-style keys (match UI-SPEC pattern)
       - Add a note at the bottom: `**Approval:** approved` replacing the `pending` state

    2. Update `172-UI-SPEC.md` §Checker Sign-Off (lines 374-381): flip all 6 PASS checkboxes `- [ ]` → `- [x]`. Update `**Approval:** pending` → `**Approval:** approved`. Update `reviewed_at: 2026-04-20` to today's date.

    3. Do NOT modify any other content in those files — only the sign-off flags + dates.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/172-preview-apply-flow/172-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/172-preview-apply-flow/172-VALIDATION.md && grep -qc "\- \[x\]" .planning/phases/172-preview-apply-flow/172-UI-SPEC.md && grep -q "Approval:.*approved" .planning/phases/172-preview-apply-flow/172-UI-SPEC.md; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "nyquist_compliant: true" .planning/phases/172-preview-apply-flow/172-VALIDATION.md`
    - `grep -q "wave_0_complete: true" .planning/phases/172-preview-apply-flow/172-VALIDATION.md`
    - `grep -c "- \[x\]" .planning/phases/172-preview-apply-flow/172-UI-SPEC.md` returns ≥ 6 (all 6 dimensions signed off)
    - `grep -q "Approval:.*approved" .planning/phases/172-preview-apply-flow/172-UI-SPEC.md`
    - No unintended changes: `diff` between the files before and after this task should show ONLY frontmatter/sign-off lines, not body content.
  </acceptance_criteria>
  <done>Phase 172 is signed off: Nyquist-compliant + UI-SPEC approved + Wave 0 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ↔ TEST_USER_* credentials | env vars; test.skip guard prevents accidental runs without them. |
| Playwright page context ↔ sessionStorage | page.evaluate runs in the browser context; we READ only, never WRITE. |
| Human verification ↔ live DB | Manual testing against the dev Supabase project; real RPC calls happen. No production data at risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-01 (runtime proof) | Tampering | Manual Item 8 — DOMPurify live-flow verification | mitigate | Developer inspects the real p_customized_svg payload in DevTools. If `<script>` appears, phase blocks and returns to Plan 03 for diagnosis. |
| T-172-06 (runtime proof) | Information Disclosure | TPRV-06 sessionStorage runtime assertion | mitigate | Playwright Test 4 asserts sessionStorage.getItem('pendingTemplate') is null post-Apply. This is the observable proof that the source-level removal (Plan 06) holds at runtime. |
| T-172-20 | Information Disclosure | Playwright tests capturing screenshots with tokens or PII | accept | Default Playwright config only saves screenshots on failure. `loginAndPrepare` does not log credentials. Solo-dev environment; CI artifacts stay local. |
| T-172-21 | Spoofing | A test passing because the gallery has zero templates and the selector silently matches nothing | mitigate | beforeEach waits for `[data-testid="template-card"]` (or equivalent) with a 10s timeout before proceeding. If no templates exist, tests fail rather than vacuously pass. |
</threat_model>

<verification>
- tests/e2e/preview-apply.spec.js contains ≥ 7 real tests, zero fixmes.
- With TEST_USER_EMAIL set: `npx playwright test tests/e2e/preview-apply.spec.js` passes all 7.
- Without TEST_USER_EMAIL set: same command skips 7 cleanly.
- `grep -q "sessionStorage.getItem('pendingTemplate')" tests/e2e/preview-apply.spec.js` — TPRV-06 runtime proof present.
- `grep -q "svg-editor\\?sceneId=\|sceneId=" tests/e2e/preview-apply.spec.js` — TPRV-04 URL assertion present.
- Manual checklist sign-off captured in 172-07-SUMMARY.md.
- 172-VALIDATION.md and 172-UI-SPEC.md sign-off flags flipped to true/approved (or still pending if human reported failures).
- Full suite confirms no Phase 172 regressions: `npm run test && npm run test:e2e` (both exit 0).
</verification>

<success_criteria>
- TPRV-01 proven at runtime (modal opens, prev/next works via keyboard — Tests 1, 2, 6).
- TPRV-04 proven at runtime (Apply navigates to correct editor URL — Test 3).
- TPRV-06 proven at runtime (sessionStorage is empty pre- and post-Apply — Test 4).
- Pitfall 2 proven at runtime (button disabled during in-flight request — Test 7).
- UI-SPEC dimensions all signed off (Copywriting, Visuals, Color, Typography, Spacing, Registry Safety).
- Nyquist compliance flag flipped.
- Phase 172 ready for `/gsd-verify-work 172`.
</success_criteria>

<output>
After completion, create `.planning/phases/172-preview-apply-flow/172-07-SUMMARY.md` including:
- Final E2E test count passing (target: 7)
- Each of the 8 manual checklist items with PASS / FAIL / DEFERRED + a one-sentence note per item
- Confirmation that 172-VALIDATION.md frontmatter now reads `nyquist_compliant: true, wave_0_complete: true`
- Confirmation that 172-UI-SPEC.md §Checker Sign-Off all 6 items are checked and `Approval: approved`
- Any deferred items (e.g., "Item 4 (native color picker on iOS) DEFERRED — no physical iPhone available during sign-off; recommend adding to v20.0 post-launch checklist")
- Recommend: "run `/gsd-verify-work 172` next"
</output>
