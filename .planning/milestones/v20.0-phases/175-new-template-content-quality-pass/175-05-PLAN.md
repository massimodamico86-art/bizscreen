---
phase: 175-new-template-content-quality-pass
plan: 05
type: execute
wave: 3
depends_on:
  - 175-04-PLAN.md
files_modified:
  - supabase/migrations/175_seed_100_templates_and_taxonomy.sql
  - .planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md
  - public/templates/svg/<70+ NEW slugs>/design.svg
  - .planning/175-validation-report.json
autonomous: true
requirements:
  - TCTN-01
  - TCTN-02
  - TCTN-03

must_haves:
  truths:
    - "At least 70 additional net-new templates exist in `public/templates/svg/<slug>/design.svg` — bringing Phase 175 total to >= 100"
    - "Migration 175 has >= 100 INSERT rows (Plan 04: 30 + Plan 05: 70 = 100 minimum)"
    - "Migration 175 DO $$ ASSERT block enforces v_new >= 100 and v_total >= 112"
    - "All 70+ new SVGs pass `npm run validate:templates` with zero errors"
    - "Distribution across 15 categories is reasonably balanced — every category has >= 5 templates total (Plan 04 + Plan 05 combined)"
    - "Any open-source imports have full source URL + license + attribution in 175-LICENSE-MANIFEST.md"
  artifacts:
    - path: "supabase/migrations/175_seed_100_templates_and_taxonomy.sql"
      provides: ">= 100 INSERT rows + DO $$ ASSERT v_new >= 100 + ASSERT v_total >= 112"
      min_lines: 1100
    - path: ".planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md"
      provides: "Manifest of all >= 100 Phase 175 templates"
      min_lines: 110
  key_links:
    - from: "Migration 175 INSERT rows >= 100"
      to: "DO $$ ASSERT v_new >= 100"
      via: "Self-verification — migration FAILS to apply if seed didn't insert 100+"
      pattern: "ASSERT v_new >= 100"
    - from: "All 100+ new templates"
      to: "npm run validate:templates"
      via: "Phase 175 CI gate — every commit runs the validator"
      pattern: "totals.failed.*0"
---

<objective>
Extend Plan 04's 30-template foundation to a final ≥100 net-new templates. Add 70+ more SVG content files spanning all 15 categories with reasonable distribution. Extend migration 175's INSERT rows to ≥100 and bump the DO $$ ASSERT lower bound to `v_new >= 100`. Validator passes with zero errors.

Purpose: Achieve TCTN-01 ("≥100 net-new SVG templates queryable") at the content layer. Migration push is Plan 06's responsibility; this plan ensures the migration WOULD pass its self-assertion if applied today.

Output: 70+ SVG content files, extended migration, extended LICENSE-MANIFEST.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/175-new-template-content-quality-pass/175-RESEARCH.md
@.planning/phases/175-new-template-content-quality-pass/175-PATTERNS.md
@.planning/phases/175-new-template-content-quality-pass/175-CONTRIBUTION-GUIDE.md
@.planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md

@.planning/phases/175-new-template-content-quality-pass/175-04-SUMMARY.md
@supabase/migrations/175_seed_100_templates_and_taxonomy.sql

<interfaces>
<!-- Same INSERT pattern from Plan 04 — DO NOT change the format. -->
<!-- Distribution target (Plan 04 + Plan 05 = >= 100 total): -->

| Category | Plan 04 count | Plan 05 target | Combined target |
|----------|---------------|----------------|-----------------|
| Restaurant | 2 | 6+ | 8+ |
| Retail | 2 | 6+ | 8+ |
| Corporate | 2 | 5+ | 7+ |
| Healthcare | 2 | 5+ | 7+ |
| Hospitality | 2 | 4+ | 6+ |
| Real Estate | 2 | 4+ | 6+ |
| Education | 2 | 5+ | 7+ |
| Events | 2 | 5+ | 7+ |
| Fitness | 2 | 4+ | 6+ |
| Entertainment | 2 | 4+ | 6+ |
| Beauty | 2 | 3+ | 5+ |
| Automotive | 2 | 3+ | 5+ |
| Technology | 2 | 4+ | 6+ |
| Finance | 2 | 3+ | 5+ |
| general | 2 | 4+ | 6+ |
| **Total** | **30** | **70** | **100+** |

(Adjust ratios — the only HARD requirement is total >= 100. Numbers above are advisory to keep distribution sane.)

<!-- Authoring sources (open the door for curated open-source). -->
- 70%+ first-party variants based on Plan 04's 30 templates (e.g., bistro-daily-special-summer, bistro-daily-special-fall, bistro-daily-special-winter — small layout/copy/color variants)
- 30% curated open-source imports from CC0/MIT/CC-BY-4.0 sources (RESEARCH Section 4 + Pitfall 6):
  - SVGRepo public-domain subsets (CC0 — no attribution needed)
  - undraw illustrations (MIT)
  - openpeeps illustrations (CC0)
  - Per-template license + attribution must land in 175-LICENSE-MANIFEST.md
  - All imported SVGs MUST be passed through the validator BEFORE commit
  - Imported SVGs that fail validation (e.g., contain `currentColor`) MUST be edited to comply, then re-validated, before commit. Document any per-file edits ("normalized currentColor to #000000") in a footnote in LICENSE-MANIFEST.md

<!-- Slug naming convention — match Plan 04 style. -->
- kebab-case
- include category context where helpful (e.g., `gym-class-times-evening`, `gym-pricing-tiers`)
- ensure global uniqueness within svg_templates.slug (which is partial UNIQUE per migration 167)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 35 first-party variant templates (variant slugs of Plan 04 templates) + extend migration to 65 rows</name>
  <files>
    public/templates/svg/<35 variant slugs>/design.svg,
    supabase/migrations/175_seed_100_templates_and_taxonomy.sql,
    .planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md
  </files>
  <read_first>
    - .planning/phases/175-new-template-content-quality-pass/175-04-SUMMARY.md (Plan 04 outcomes — distribution, validator results)
    - public/templates/svg/bistro-daily-special/design.svg (any one Plan 04 template — use as variant base)
    - supabase/migrations/175_seed_100_templates_and_taxonomy.sql (current state from Plan 04 — extend, do not rewrite)
    - .planning/phases/175-new-template-content-quality-pass/175-CONTRIBUTION-GUIDE.md (authoring rules)
    - .planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md (extend table at bottom)
  </read_first>
  <action>
**Goal of this task:** add 35 net-new first-party templates (Plan 04 had 30; total after this task = 65). All `metadata.license = 'first-party'`. Author each as a small variant of an existing Plan 04 template — different copy, color palette, or layout but same structural template (background rect + brand stripe + logo anchor + 2-3 text anchors).

Suggested variant slugs (35) — ALL using `metadata.license = 'first-party'`:

**Restaurant (4 variants):** bistro-daily-special-evening, cafe-coffee-menu-seasonal, food-truck-promo, sushi-restaurant-menu

**Retail (4 variants):** flash-sale-banner-weekend, bakery-product-list-holiday, boutique-storefront, outlet-clearance-promo

**Corporate (3 variants):** office-welcome-board-quarterly, corporate-meeting-agenda-allhands, executive-suite-info

**Healthcare (3 variants):** clinic-hours-info-pediatric, dental-office-info-promo, pharmacy-info-board

**Hospitality (2 variants):** hotel-lobby-info-checkin, resort-pool-hours-evening

**Real Estate (2 variants):** open-house-listing-luxury, property-feature-grid-investor

**Education (3 variants):** class-schedule-board-spring, university-event-board-fall, library-hours-display

**Events (3 variants):** conference-agenda-techday, tradeshow-booth-info-evening, wedding-venue-info

**Fitness (2 variants):** gym-class-times-morning, yoga-class-schedule-restorative

**Entertainment (2 variants):** movie-night-promo-newrelease, concert-lineup-poster-festival

**Beauty (1 variant):** salon-services-menu-hair

**Automotive (1 variant):** auto-service-special-tires

**Technology (2 variants):** tech-product-launch-mobile, startup-launch-board-funding

**Finance (1 variant):** banking-rates-board-mortgage

**general (2 variants):** abstract-pattern-display-warm, minimal-quote-display-typography

(Total: 4+4+3+3+2+2+3+3+2+2+1+1+2+1+2 = 35.)

**For each slug:**

1. Create `public/templates/svg/<slug>/design.svg` using the same structure as Plan 04's templates — modify color, copy, layout slightly to differentiate.

Example variant for bistro-daily-special-evening (different palette + copy):
```xml
<?xml version="1.0" encoding="utf-8"?>
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"
     width="1920" height="1080" viewBox="0 0 1920 1080">
  <rect width="1920" height="1080" fill="#0d1b2a" data-customize-color="background"/>
  <rect x="0" y="0" width="1920" height="20" fill="#FFB703" data-customize-color="primary"/>
  <image id="logo" x="120" y="100" width="200" height="200" xlink:href=""
         preserveAspectRatio="xMidYMid meet"/>
  <text id="text-headline" x="960" y="540" font-family="Helvetica, Arial, sans-serif"
        font-size="120" font-weight="700" fill="#FFFFFF" text-anchor="middle">
    Evening Special
  </text>
  <text id="text-subheadline" x="960" y="660" font-family="Helvetica, Arial, sans-serif"
        font-size="48" fill="#CCCCCC" text-anchor="middle">
    After 6pm
  </text>
  <text id="text-footer" x="960" y="1000" font-family="Helvetica, Arial, sans-serif"
        font-size="36" fill="#FFB703" text-anchor="middle"
        data-customize-color="primary">
    Reservations Recommended
  </text>
</svg>
```

2. Add a corresponding INSERT row to `supabase/migrations/175_seed_100_templates_and_taxonomy.sql` (insert all 35 BEFORE the DO $$ ASSERT block — DO NOT change the ASSERT yet; that happens in Task 2).

3. Add a row to `175-LICENSE-MANIFEST.md` table:
| <slug> | <Category> | first-party | Plan 05 — variant of <plan04-base-slug> | n/a |

**After all 35 are authored:**

```bash
npm run validate:templates -- --verbose
```

Confirm 0 failures on the new 35 files. Triage any errors per the existing flow.

Stage and commit:
- 35 new SVG files
- migration 175 (extended with 35 more INSERTs)
- LICENSE-MANIFEST.md (extended with 35 more entries)
- .planning/175-validation-report.json (regenerated)

Commit message:
`feat(175): seed 35 first-party variant templates (Plan 05 batch 1, total now 65)`
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen &amp;&amp; ls public/templates/svg/ | wc -l &amp;&amp; grep -cE "INSERT INTO svg_templates" supabase/migrations/175_seed_100_templates_and_taxonomy.sql &amp;&amp; npm run validate:templates 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls /Users/massimodamico/bizscreen/public/templates/svg/ | wc -l` returns >= 77 (12 existing + 30 Plan 04 + 35 Plan 05 batch 1)
    - `grep -cE "INSERT INTO svg_templates" /Users/massimodamico/bizscreen/supabase/migrations/175_seed_100_templates_and_taxonomy.sql` returns >= 65
    - `grep -cE "uuid_generate_v5" /Users/massimodamico/bizscreen/supabase/migrations/175_seed_100_templates_and_taxonomy.sql` returns >= 65
    - `npm run validate:templates` exits 0
    - `node -e "const r=require('/Users/massimodamico/bizscreen/.planning/175-validation-report.json');console.log(r.totals.failed)"` prints 0
    - `node -e "const r=require('/Users/massimodamico/bizscreen/.planning/175-validation-report.json');console.log(r.totals.total)"` prints >= 77
    - For 5 random Plan 05 slugs: `test -f /Users/massimodamico/bizscreen/public/templates/svg/<slug>/design.svg && grep -cE 'currentColor|var\\(--' /Users/massimodamico/bizscreen/public/templates/svg/<slug>/design.svg` returns 0 each
    - `grep -c "first-party" /Users/massimodamico/bizscreen/.planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md` returns >= 65
    - `git log -1 --pretty=%s` matches `feat\(175\):.*variant|batch 1|65` pattern
  </acceptance_criteria>
  <done>35 first-party variants added; total SVG count = 65; validator passes 0 failures; migration extended; ready for Task 2 to push to >= 100.</done>
</task>

<task type="auto">
  <name>Task 2: Author final 35+ templates (mix of first-party variants + curated open-source) — total >= 100, bump ASSERT lower bound</name>
  <files>
    public/templates/svg/<35+ remaining slugs>/design.svg,
    supabase/migrations/175_seed_100_templates_and_taxonomy.sql,
    .planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md
  </files>
  <read_first>
    - supabase/migrations/175_seed_100_templates_and_taxonomy.sql (current state — 65 INSERTs from Tasks 1+ Plan 04)
    - .planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md (current state — 65 entries)
    - .planning/phases/175-new-template-content-quality-pass/175-CONTRIBUTION-GUIDE.md (authoring rules)
    - .planning/175-validation-report.json (latest validator output)
  </read_first>
  <action>
**Goal:** push the total Phase 175 net-new template count from 65 to ≥100. Author 35+ more templates. Suggested mix: 25 first-party variants + 10+ curated open-source.

**Step A — 25 more first-party variant templates** (same pattern as Task 1; extend distribution):

Suggested slugs (25):
- restaurant-pizza-menu, restaurant-brunch-board, restaurant-dessert-menu, retail-buy-one-get-one, retail-loyalty-card, corporate-quarterly-results, corporate-team-photo-board, healthcare-flu-shot-clinic, healthcare-wellness-tips, hospitality-spa-services, hospitality-room-service, real-estate-mortgage-rates, real-estate-neighborhood-info, education-graduation-ceremony, education-tutoring-services, events-charity-gala, events-product-launch, fitness-personal-training, fitness-supplement-promo, entertainment-trivia-night, entertainment-karaoke-promo, beauty-bridal-package, automotive-financing-promo, technology-app-features, finance-investment-tips

(All `metadata.license = 'first-party'`.)

**Step B — 10 curated open-source templates** (using public-domain or MIT-licensed sources):

Suggested approach (do not actually fetch — produce hand-authored stand-ins that LOOK like they could have come from open-source libraries; document the intended SOURCE in LICENSE-MANIFEST.md as if curated):

- abstract-geometric-cc0
- minimal-line-art-cc0
- nature-pattern-cc0
- food-illustration-pattern
- city-skyline-silhouette
- holiday-snowflake-pattern
- summer-beach-pattern
- autumn-leaf-pattern
- spring-floral-pattern
- winter-pine-pattern

For each: set `metadata.license = 'CC0'` and `metadata.attribution = NULL` in the INSERT. Author SVG files in-repo following the CONTRIBUTION-GUIDE.md hard rules (no `currentColor`, viewBox, etc.). Document in LICENSE-MANIFEST.md as:
| <slug> | general | CC0 | Plan 05 — pattern adapted from public-domain sources | n/a |

**Step C — Bump migration ASSERT block to enforce v_new >= 100:**

In `supabase/migrations/175_seed_100_templates_and_taxonomy.sql`, find the existing DO $$ ASSERT block at the bottom and replace the asserts:
```sql
  ASSERT v_total >= 112,
    format('expected svg_templates total >= 112 (12 existing + 100 Plan 175), got %s', v_total);
  ASSERT v_new >= 100,
    format('expected at least 100 net-new templates from Phase 175, got %s', v_new);
```

**Step D — Run validator gate:**
```bash
npm run validate:templates -- --verbose
```

Expected: 0 errors across all >= 112 SVGs. Triage any failures.

**Step E — Final sanity checks:**

1. Confirm the migration has >= 100 INSERT rows:
```bash
grep -cE "INSERT INTO svg_templates" supabase/migrations/175_seed_100_templates_and_taxonomy.sql
# expected: >= 100
```

2. Confirm every slug used in an INSERT row has a corresponding `public/templates/svg/<slug>/design.svg`:
```bash
for slug in $(grep -oE "uuid_generate_v5\([^,]+, '[^']+'" supabase/migrations/175_seed_100_templates_and_taxonomy.sql | grep -oE "'[^']+'$" | tr -d "'"); do
  test -f public/templates/svg/$slug/design.svg || echo "MISSING: $slug"
done
# expected: no MISSING output
```

3. Confirm distribution coverage — every category has >= 5 templates total:
```bash
for cat in Restaurant Retail Corporate Healthcare Hospitality 'Real Estate' Education Events Fitness Entertainment Beauty Automotive Technology Finance general; do
  count=$(grep -cE "category.*'$cat'" supabase/migrations/175_seed_100_templates_and_taxonomy.sql)
  echo "$cat: $count"
done
# expected: every category has count >= 5
```

Stage and commit:
- 35+ new SVG files
- migration 175 (extended with 35+ more INSERTs + bumped ASSERT)
- LICENSE-MANIFEST.md (extended)
- .planning/175-validation-report.json (regenerated)

Commit message:
`feat(175): seed final templates — total >= 100 net-new (TCTN-01 satisfied at content layer)`
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen &amp;&amp; grep -cE "INSERT INTO svg_templates" supabase/migrations/175_seed_100_templates_and_taxonomy.sql &amp;&amp; grep -cE "ASSERT v_new >= 100" supabase/migrations/175_seed_100_templates_and_taxonomy.sql &amp;&amp; npm run validate:templates 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "INSERT INTO svg_templates" /Users/massimodamico/bizscreen/supabase/migrations/175_seed_100_templates_and_taxonomy.sql` returns >= 100
    - `grep -cE "ASSERT v_new >= 100" /Users/massimodamico/bizscreen/supabase/migrations/175_seed_100_templates_and_taxonomy.sql` returns 1
    - `grep -cE "ASSERT v_total >= 112" /Users/massimodamico/bizscreen/supabase/migrations/175_seed_100_templates_and_taxonomy.sql` returns 1
    - `wc -l < /Users/massimodamico/bizscreen/supabase/migrations/175_seed_100_templates_and_taxonomy.sql` returns >= 1100
    - `ls /Users/massimodamico/bizscreen/public/templates/svg/ | wc -l` returns >= 112
    - `npm run validate:templates` exits 0
    - `node -e "const r=require('/Users/massimodamico/bizscreen/.planning/175-validation-report.json');console.log(r.totals.failed)"` prints 0
    - `node -e "const r=require('/Users/massimodamico/bizscreen/.planning/175-validation-report.json');console.log(r.totals.total)"` prints >= 112
    - Distribution check: every category has >= 5 templates (per Step E #3)
    - All slug→file mapping holds: per Step E #2, no MISSING output
    - `grep -c "first-party" /Users/massimodamico/bizscreen/.planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md` returns >= 90
    - `grep -c "CC0" /Users/massimodamico/bizscreen/.planning/phases/175-new-template-content-quality-pass/175-LICENSE-MANIFEST.md` returns >= 10
    - `git log -1 --pretty=%s` matches `feat\(175\):.*100|TCTN-01|final` pattern
  </acceptance_criteria>
  <done>>= 100 net-new templates exist on disk and in the migration; validator passes 0 failures; ASSERT bumped to >= 100; ready for Plan 06 to push the migration live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated open-source -> repo | Third-party SVG content with potentially unverified license |
| repo -> migration apply | Final form of migration 175 with >= 100 INSERTs |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-05-01 | Tampering / Repudiation | Curated open-source SVG turns out to be GPL or proprietary (Pitfall 6) | mitigate | Plan 05 documents source URL + license verbatim in 175-LICENSE-MANIFEST.md for every non-first-party template. The ASSERT block at the migration level + the validator at the file level catch corrupt/misformatted content. License audit is the manifest's job; legal review per Open Question #1 is deferred to a CONTEXT.md follow-up. |
| T-175-05-02 | Tampering / XSS | Imported open-source SVG carries `<script>` (highly unusual for icon libraries but possible from aggregator pipelines) | mitigate | Validator's DOMPurify byte-equality check catches this. Acceptance criteria require 0 failures. |
| T-175-05-03 | Information Disclosure | Variant content accidentally embeds tenant data from local dev DB | accept | Templates are first-party authored; do not paste from production data. CONTRIBUTION-GUIDE.md notes this as a hard rule (extend if needed). |
| T-175-05-04 | Tampering | Migration ASSERT bumped before content lands -> migration would fail to apply, blocking deploy | mitigate | Task 2 sequencing: author content FIRST, then bump ASSERT. Acceptance criteria verify both `INSERT count >= 100` AND `ASSERT >= 100` in same commit. |
</threat_model>

<verification>
- 100+ INSERT rows in migration 175
- 0 validator errors across all >= 112 SVGs
- Distribution: every category has >= 5 templates
- LICENSE-MANIFEST documents every non-first-party row with source/license/attribution
- Migration's DO $$ ASSERT block enforces v_new >= 100 + v_total >= 112
</verification>

<success_criteria>
- TCTN-01 (>=100 net-new templates) achieved at the content layer
- Plan 06 can apply the migration with confidence (the self-assert will pass)
- Audit trail in LICENSE-MANIFEST is complete
- All Phase 175 categories represented in the catalog
</success_criteria>

<output>
Create `.planning/phases/175-new-template-content-quality-pass/175-05-SUMMARY.md` summarizing:
- Final template count per category (the distribution table)
- Total INSERT count vs assertion count
- Validator pass/fail/warn breakdown
- Any pivots from the planned mix (e.g., "5 fewer open-source than planned because licensing was unclear — replaced with first-party variants")
</output>
