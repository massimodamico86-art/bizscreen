---
phase: 70-screen-scene-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/scene-editor/PropertiesPanel.jsx
  - src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx
autonomous: true
requirements: [SEDT-01, SCRN-01, SCRN-02]

must_haves:
  truths:
    - "User can select a menu-board widget element in the scene editor and configure which menu board to display via a board selector dropdown"
    - "User can configure menu board visual appearance (theme, accent color, show images, show descriptions, page interval, currency) in the scene editor"
    - "User can set a screen to portrait or landscape orientation from the Edit Screen modal and it persists to the database"
    - "User can assign a display language to a screen from the Edit Screen modal and the language-aware player content resolver uses it"
    - "YodeckLayoutEditorPage renders without missing component errors"
  artifacts:
    - path: "src/components/scene-editor/PropertiesPanel.jsx"
      provides: "Menu board widget controls in scene editor WidgetControls function"
      contains: "MenuBoardWidgetControls"
    - path: "src/components/scene-editor/MenuBoardWidgetControls.jsx"
      provides: "Reusable menu board configuration UI (already exists from 69-02)"
      exports: ["MenuBoardWidgetControls"]
    - path: "src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx"
      provides: "Layout editor page with all required component imports"
      contains: "import.*TopToolbar"
    - path: "src/pages/components/ScreensComponents.jsx"
      provides: "EditScreenModal with orientation and display_language fields (already exists)"
      contains: "orientation"
  key_links:
    - from: "src/components/scene-editor/PropertiesPanel.jsx"
      to: "src/components/scene-editor/MenuBoardWidgetControls.jsx"
      via: "import and conditional render for widgetType === 'menu-board'"
      pattern: "MenuBoardWidgetControls"
    - from: "src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx"
      to: "src/components/layout-editor/index.js"
      via: "barrel import for TopToolbar, LeftSidebar, LayoutEditorCanvas, etc."
      pattern: "import.*from.*layout-editor"
    - from: "src/pages/components/ScreensComponents.jsx"
      to: "src/services/screenService.js"
      via: "handleUpdateScreen passes orientation and displayLanguage to updateScreen"
      pattern: "orientation.*display_language"
---

<objective>
Add menu board widget controls to the scene editor, fix missing imports in YodeckLayoutEditorPage, and verify screen orientation/language controls are complete end-to-end.

Purpose: Close the remaining scene editor widget gap (menu-board is the only widget type without controls in the scene editor PropertiesPanel), fix the integration gap from the v5.0 audit where YodeckLayoutEditorPage has 7 components used in JSX but never imported, and confirm that SCRN-01/SCRN-02 are already wired end-to-end.

Output: Updated PropertiesPanel.jsx with menu-board controls, fixed YodeckLayoutEditorPage.jsx with all required imports, verified screen settings pipeline.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-layout-editor-widget-parity/69-02-SUMMARY.md
@src/components/scene-editor/PropertiesPanel.jsx
@src/components/scene-editor/MenuBoardWidgetControls.jsx
@src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx
@src/components/layout-editor/index.js
@src/pages/components/ScreensComponents.jsx
@src/services/screenService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add menu board widget controls to scene editor PropertiesPanel</name>
  <files>src/components/scene-editor/PropertiesPanel.jsx</files>
  <action>
In the scene editor PropertiesPanel.jsx, the WidgetControls function renders type-specific controls for each widget type (QR, data-table, RSS, social-feed, countdown) but is missing controls for the menu-board widget type. Fix this by:

1. Add import for MenuBoardWidgetControls at the top with the other widget control imports (after the CountdownWidgetControls import, around line 43):
   ```
   import { MenuBoardWidgetControls } from './MenuBoardWidgetControls';
   ```

2. In the WidgetControls function, add a conditional render block for menu-board AFTER the countdown widget block (after line 959, before the Text Color section). Follow the exact same pattern as the other widget controls:
   ```jsx
   {/* Menu Board Widget Controls */}
   {widgetType === 'menu-board' && (
     <MenuBoardWidgetControls
       props={props}
       onPropChange={handlePropChange}
     />
   )}
   ```

This follows the exact pattern established by QRCodeWidgetControls, DataTableWidgetControls, RssWidgetControls, SocialFeedWidgetControls, and CountdownWidgetControls. The MenuBoardWidgetControls component already exists (created in phase 69-02) and accepts the same {props, onPropChange} interface.
  </action>
  <verify>
1. Run `npx eslint src/components/scene-editor/PropertiesPanel.jsx` -- no errors
2. Grep for MenuBoardWidgetControls in PropertiesPanel.jsx -- should find import and render block
3. Confirm the import path resolves: `ls src/components/scene-editor/MenuBoardWidgetControls.jsx`
  </verify>
  <done>
Scene editor PropertiesPanel renders MenuBoardWidgetControls when a menu-board widget block is selected, allowing users to configure menuBoardId, theme, accentColor, showImages, showDescriptions, pageIntervalSeconds, and currencyCode directly from the scene editor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix YodeckLayoutEditorPage missing imports and verify screen settings</name>
  <files>src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx</files>
  <action>
The YodeckLayoutEditorPage.jsx uses 8 components in its JSX that are not imported: TopToolbar, LeftSidebar, LayoutEditorCanvas, LayoutPropertiesPanel, PixieEditorModal, LayoutPreviewModal (from layout-editor barrel), InsertContentModal (from modals), and Button (from design-system). Fix by adding the missing imports after the existing lucide-react import (line 19):

1. Add layout-editor barrel import:
   ```
   import {
     TopToolbar,
     LeftSidebar,
     LayoutEditorCanvas,
     LayoutPropertiesPanel,
     PixieEditorModal,
     LayoutPreviewModal,
   } from '../../components/layout-editor';
   ```

2. Add InsertContentModal import:
   ```
   import InsertContentModal from '../../components/modals/InsertContentModal';
   ```

3. Add Button import from design-system:
   ```
   import { Button } from '../../design-system';
   ```

Also remove the blank line between the first import and `import { useLayout }` (line 16-17 has a blank line that should be cleaned up).

For SCRN-01 and SCRN-02: These are already fully implemented. Verify by confirming the complete pipeline:
- EditScreenModal in ScreensComponents.jsx has orientation select (landscape/portrait) and displayLanguage select (SUPPORTED_LOCALES)
- handleUpdateScreen in useScreensData.js passes both fields to updateScreen
- screenService.updateScreen has both 'display_language' and 'orientation' in allowedFields
- SQL migration 133 uses display_language in get_resolved_player_content for language-aware content resolution
- Player ViewPage.jsx reads orientation from content.screen.orientation for rotation handling

No code changes needed for SCRN-01/SCRN-02 -- they are complete as-is.
  </action>
  <verify>
1. Run `npx eslint src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx` -- no errors
2. Grep for 'import.*TopToolbar' in YodeckLayoutEditorPage.jsx -- should find barrel import
3. Grep for 'import.*InsertContentModal' in YodeckLayoutEditorPage.jsx -- should find import
4. Grep for 'import.*Button' in YodeckLayoutEditorPage.jsx -- should find design-system import
5. Verify SCRN-01: grep 'orientation' in ScreensComponents.jsx -- should find field in EditScreenModal
6. Verify SCRN-02: grep 'display_language\|displayLanguage' in ScreensComponents.jsx and screenService.js -- should find both
  </verify>
  <done>
YodeckLayoutEditorPage has all 8 missing component imports resolved (TopToolbar, LeftSidebar, LayoutEditorCanvas, LayoutPropertiesPanel, PixieEditorModal, LayoutPreviewModal, InsertContentModal, Button). SCRN-01 (screen orientation) and SCRN-02 (device language) verified as complete end-to-end: UI controls exist in EditScreenModal, service layer persists to DB, and player uses both values.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/components/scene-editor/PropertiesPanel.jsx src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx` -- both files lint clean
2. `npm run build` -- no build errors (verifies all imports resolve)
3. Grep confirms MenuBoardWidgetControls import and render block in PropertiesPanel.jsx
4. Grep confirms all 8 missing imports added to YodeckLayoutEditorPage.jsx
5. Grep confirms orientation and display_language fields in EditScreenModal, useScreensData handleUpdateScreen, and screenService updateScreen
</verification>

<success_criteria>
- Menu board widget type has configuration controls in both scene editor (PropertiesPanel) and layout editor (LayoutPropertiesPanel -- already done in 69-02)
- YodeckLayoutEditorPage renders without missing component reference errors
- Screen orientation (landscape/portrait) is settable from Edit Screen modal and persisted to DB
- Screen display language is settable from Edit Screen modal and used by player content resolver
- All modified files pass ESLint
- Build succeeds with no missing import errors
</success_criteria>

<output>
After completion, create `.planning/phases/70-screen-scene-controls/70-01-SUMMARY.md`
</output>
