---
phase: 116-scenes-svg-editor-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/svg-editor-advanced-screenshots.spec.js
autonomous: true
requirements:
  - SCENE-12
  - SCENE-13
  - SCENE-14
  - SCENE-15
  - SCENE-16
  - SCENE-17

must_haves:
  truths:
    - "Running svg-editor-advanced-screenshots.spec.js produces screenshots showing undo/redo buttons in the SVG editor toolbar"
    - "Running svg-editor-advanced-screenshots.spec.js produces screenshots of save feedback, export dialog, and context menu"
    - "Running svg-editor-advanced-screenshots.spec.js produces screenshots of cloud import panel and AI Designer panel"
  artifacts:
    - path: "tests/e2e/svg-editor-advanced-screenshots.spec.js"
      provides: "SVG editor advanced features E2E test suite"
      min_lines: 120
  key_links:
    - from: "tests/e2e/svg-editor-advanced-screenshots.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import { loginAndPrepare, waitForPageReady }"
      pattern: "import.*helpers"
---

<objective>
Create screenshot-verified E2E tests for SVG editor advanced features: undo/redo, save feedback, export dialog, context menu, cloud import panel, and AI Designer panel.

Purpose: Establish screenshot evidence that all advanced SVG editor features render correctly.
Output: tests/e2e/svg-editor-advanced-screenshots.spec.js covering SCENE-12 through SCENE-17.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@tests/e2e/helpers.js
@playwright.config.js
@src/components/svg-editor/FabricSvgEditor.jsx
@src/components/svg-editor/TopToolbar.jsx
@src/components/svg-editor/ContextMenu.jsx
@src/components/svg-editor/LeftSidebar.jsx
@src/pages/SvgEditorPage.jsx
</context>

<interfaces>
<!-- SVG Editor Advanced UI (from FabricSvgEditor.jsx and TopToolbar.jsx) -->

TopToolbar buttons:
- Back/Close button (ChevronLeft or X icon)
- Design name (editable text)
- Save button (Save icon) -- shows "Saved" / "Saving..." status
- Undo button (Undo2 icon) -- disabled when nothing to undo
- Redo button (Redo2 icon) -- disabled when nothing to redo
- Export/Download button (Download icon) -- opens export dialog with format options (PNG, JPEG, SVG), quality slider
- View/zoom controls

ContextMenu (right-click on canvas element):
- Shows options like: Copy, Paste, Delete, Bring to Front, Send to Back, Duplicate, Lock/Unlock
- Rendered as a positioned overlay div

LeftSidebar panels include:
- Cloud/Uploads tab: Import from Google Drive, Dropbox, or upload files
- Elements tab: May contain AI-powered or preset elements

AI Designer: The SceneEditorPage has an AI panel (AiSuggestionsPanel). The SVG editor (FabricSvgEditor) may have an AI-related button or the "Elements" panel may have AI features. Look for sparkles icon or "AI" text in the toolbar/sidebar.

Note: The SVG editor (FabricSvgEditor) is different from the Scene Editor (SceneEditorPage). The scene editor has the explicit AI panel toggle (Sparkles button). For the SVG editor, check if there's an AI feature in the sidebar or toolbar.

Screenshots convention: `screenshots/116/116-XX-description-desktop.png`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SVG editor advanced features screenshot E2E spec</name>
  <files>tests/e2e/svg-editor-advanced-screenshots.spec.js</files>
  <action>
Create a NEW spec file `tests/e2e/svg-editor-advanced-screenshots.spec.js`. Use existing helpers.

Skip if `TEST_USER_EMAIL` not set. Use `chromium` project only.

Create the same `screenshotStep` and `openSvgEditor` helpers as in 116-02 (copy the pattern from svg-editor-tools-screenshots.spec.js -- read that file first to reuse the exact openSvgEditor function that was proven to work).

Create these test cases inside `test.describe('SVG Editor Advanced Screenshots', ...)`:

**SCENE-12: Undo/redo keyboard shortcuts**
- Use `test.slow()`
- Call `loginAndPrepare`, then `openSvgEditor`
- Screenshot the toolbar area showing undo/redo buttons: `screenshots/116/116-12-undo-redo-toolbar-desktop.png`
- Add a text element to canvas (click text tool, add text)
- Now the undo button should become active/enabled
- Screenshot showing undo button enabled: `screenshots/116/116-12-undo-enabled-desktop.png`
- Press Ctrl+Z (undo shortcut) or click the undo button
- Screenshot after undo: `screenshots/116/116-12-after-undo-desktop.png`

**SCENE-13: Save with success feedback**
- Use `test.slow()`
- Call `loginAndPrepare`, then `openSvgEditor`
- Look for the save button in the top toolbar (Save icon, or button with text "Save")
- Click the save button
- Wait for save feedback -- look for "Saved" text, checkmark icon, or success indicator in toolbar
- Screenshot showing save feedback: `screenshots/116/116-13-save-feedback-desktop.png`
- If save shows a toast notification, capture that too

**SCENE-14: Export dialog with format/quality options**
- Use `test.slow()`
- Call `loginAndPrepare`, then `openSvgEditor`
- Look for the export/download button in toolbar (Download icon, or button with text "Export" / "Download")
- Click it to open export dialog
- Wait for dialog/modal to appear with format options (PNG, JPEG, SVG) and quality slider
- Screenshot: `screenshots/116/116-14-export-dialog-desktop.png`
- Close dialog

**SCENE-15: Context menu on right-click**
- Use `test.slow()`
- Call `loginAndPrepare`, then `openSvgEditor`
- Add a text element first (so there's something to right-click on)
- Right-click on the canvas area: `await page.locator('canvas').click({ button: 'right', position: { x: 400, y: 300 } })`
- Wait for context menu to appear (a positioned div/overlay with menu items like Copy, Delete, etc.)
- Screenshot: `screenshots/116/116-15-context-menu-desktop.png`
- Click elsewhere to dismiss the context menu

**SCENE-16: Cloud import panel (Google Drive, Dropbox)**
- Use `test.slow()`
- Call `loginAndPrepare`, then `openSvgEditor`
- In the left sidebar, find and click the Cloud/Uploads tab (Cloud icon, or button with upload/cloud label)
- Wait for cloud import panel to show (Google Drive, Dropbox options, or upload area)
- Screenshot: `screenshots/116/116-16-cloud-import-panel-desktop.png`
- If the panel has Google Drive / Dropbox buttons, screenshot those: `screenshots/116/116-16-cloud-providers-desktop.png`

**SCENE-17: AI Designer panel generates layout from prompt**
- Use `test.slow()`
- Call `loginAndPrepare`, then `openSvgEditor`
- Look for an AI button or panel in the editor. The SVG editor may have:
  - A Sparkles/Wand icon button in the toolbar or sidebar
  - An "AI" or "Smart" panel in the left sidebar
  - An "Elements" panel that includes AI-generated presets
- If an AI panel/button exists, click to open it
- Screenshot the AI panel/feature: `screenshots/116/116-17-ai-designer-panel-desktop.png`
- If no AI panel found in SVG editor, navigate to the Scene Editor instead (go to scenes page, open a scene, click the sparkles button in the top bar) and screenshot the AI Suggestions panel
- If neither is accessible, screenshot the editor with a note in the test that AI panel was not found: `screenshots/116/116-17-ai-not-available-desktop.png`

IMPORTANT: For the AI Designer test (SCENE-17), the AI panel is more prominent in the Scene Editor (SceneEditorPage) than the SVG editor. If the SVG editor doesn't have it:
1. Navigate to Scenes page
2. If scenes exist, click "Open" on a scene to go to scene-detail page
3. From scene detail, click "Edit" to open the scene editor
4. In the scene editor top bar, click the Sparkles icon to toggle the AI panel
5. Screenshot the AiSuggestionsPanel
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/svg-editor-advanced-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>All 6 advanced editor feature tests pass producing screenshot files for undo/redo, save, export, context menu, cloud import, and AI designer</done>
</task>

<task type="auto">
  <name>Task 2: Verify all advanced editor screenshots exist and fix failures</name>
  <files>tests/e2e/svg-editor-advanced-screenshots.spec.js</files>
  <action>
Run the full SVG editor advanced spec and verify screenshot artifacts:

1. Run `npx playwright test tests/e2e/svg-editor-advanced-screenshots.spec.js --project=chromium`
2. Verify these files exist and are non-empty:
   - screenshots/116/116-12-* (undo/redo)
   - screenshots/116/116-13-* (save feedback)
   - screenshots/116/116-14-* (export dialog)
   - screenshots/116/116-15-* (context menu)
   - screenshots/116/116-16-* (cloud import)
   - screenshots/116/116-17-* (AI designer)

If tests fail, debug and fix. Common issues:
- Export dialog may be a custom modal, not a native dialog -- look for `[role="dialog"]` or fixed overlay
- Context menu on right-click may not appear if no element is selected -- add an element first, select it, then right-click ON the element
- Cloud import panel may be behind a sub-menu in the sidebar -- try multiple sidebar buttons
- AI panel may only exist in Scene Editor, not SVG editor -- the test should gracefully handle this by navigating to Scene Editor as fallback
- Save button may auto-save without explicit click -- capture the "Saved" indicator instead
  </action>
  <verify>
    <automated>ls -la screenshots/116/116-1[2-7]*.png 2>/dev/null | wc -l</automated>
  </verify>
  <done>At least 6 screenshot files exist covering all advanced SVG editor features (SCENE-12 through SCENE-17), all non-zero size, all tests passing</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/svg-editor-advanced-screenshots.spec.js --project=chromium` passes all tests
- `ls screenshots/116/116-1*.png` shows files for requirements SCENE-12 through SCENE-17
- Each advanced feature has at least one corresponding screenshot
</verification>

<success_criteria>
- SCENE-12 through SCENE-17 each have passing E2E tests with screenshot artifacts
- Export dialog, context menu, and cloud panel are all captured
- AI Designer is tested (either in SVG editor or Scene Editor as fallback)
- Tests use established helpers pattern
- Screenshots follow 116-XX naming convention in screenshots/116/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/116-scenes-svg-editor-e2e/116-03-SUMMARY.md`
</output>
