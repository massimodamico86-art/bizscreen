---
phase: 116-scenes-svg-editor-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scenes-screenshots.spec.js
autonomous: true
requirements:
  - SCENE-01
  - SCENE-02
  - SCENE-03

must_haves:
  truths:
    - "Running scenes-screenshots.spec.js produces a screenshot of the scene list page showing scene cards or empty state"
    - "Running scenes-screenshots.spec.js produces a screenshot of the create scene modal (AutoBuild/industry modal)"
    - "Running scenes-screenshots.spec.js produces a screenshot of the SVG editor loaded with toolbar and canvas"
  artifacts:
    - path: "tests/e2e/scenes-screenshots.spec.js"
      provides: "Scene list and SVG editor loading E2E test suite"
      min_lines: 100
  key_links:
    - from: "tests/e2e/scenes-screenshots.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import { loginAndPrepare, waitForPageReady }"
      pattern: "import.*helpers"
---

<objective>
Create screenshot-verified E2E tests for scene list CRUD operations (create, duplicate, delete), the scene creation modal, and SVG editor initial loading.

Purpose: Establish screenshot evidence that scene list page and SVG editor load correctly with all expected UI elements.
Output: tests/e2e/scenes-screenshots.spec.js covering SCENE-01, SCENE-02, SCENE-03.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@tests/e2e/helpers.js
@tests/e2e/dashboard-screenshots.spec.js
@playwright.config.js
@src/pages/ScenesPage.jsx
@src/pages/SvgEditorPage.jsx
@src/App.jsx

@.planning/phases/115-dashboard-media-e2e/115-01-SUMMARY.md
</context>

<interfaces>
<!-- Existing test helpers -->
From tests/e2e/helpers.js:
```javascript
export async function loginAndPrepare(page, options = {})
export async function dismissAnyModals(page)
export async function waitForPageReady(page)
export async function navigateToSection(page, section) // 'scenes' is supported
export function generateTestName(prefix = 'Test')
```

<!-- Navigation structure -->
IMPORTANT: "Scenes" is NOT in the sidebar navigation. The page exists at route `scenes` but must be navigated to via direct URL or `navigateToSection(page, 'scenes')`.

Scenes page route: `currentPage='scenes'` renders ScenesPage
SVG editor route: `currentPage='svg-editor'` or `currentPage='svg-editor?templateId=X'` renders SvgEditorPage
Scene editor route: `currentPage='scene-editor-{id}'` renders SceneEditorPage (Canva-style block editor)

ScenesPage UI:
- PageHeader with title "Scenes" and "Create Scene" button (sparkles icon)
- Empty state: icon + "No scenes yet" + "Generate My First Scene" button
- If scenes exist: Grid of SceneCard components with Open/Publish/Duplicate/Delete buttons
- Delete confirmation modal with "Delete Scene" title
- "Create Scene" button triggers the AutoBuild/industry modal (external to this page)

SVG editor (via Templates): Navigate to Templates sidebar item, pick a template, or go to `/app` and set page to `svg-editor`
- FabricSvgEditor loads with TopToolbar, LeftSidebar, canvas area
- Has loading spinner, then canvas with toolbar buttons

Screenshot naming convention: `screenshots/116/116-XX-description-desktop.png`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create scenes and SVG editor screenshot E2E spec</name>
  <files>tests/e2e/scenes-screenshots.spec.js</files>
  <action>
Create a NEW spec file `tests/e2e/scenes-screenshots.spec.js`. Use existing helpers (`loginAndPrepare`, `waitForPageReady`, `dismissAnyModals`).

The spec must use the `chromium` project only. Skip if `TEST_USER_EMAIL` not set.

Use `beforeEach` to call `loginAndPrepare` with client credentials and navigate to `/app`.

Create a `screenshotStep` helper (like in media-screenshots.spec.js):
```javascript
async function screenshotStep(page, name) {
  await page.screenshot({ path: `screenshots/116/${name}-desktop.png`, fullPage: false });
}
```

Ensure `screenshots/116/` directory exists (use `fs.mkdirSync` in a `beforeAll`).

Create the following test cases inside `test.describe('Scenes & SVG Editor Screenshots', ...)`:

**SCENE-01: Scene list page with create, duplicate, delete actions**
- Navigate to scenes page: `await page.goto('/app');` then click to navigate or use direct page navigation. Since Scenes is NOT in the sidebar, navigate by going to the app and programmatically setting the page. The simplest approach: `await page.evaluate(() => { window.history.pushState({}, '', '/app'); });` then try `navigateToSection(page, 'scenes')`. If that fails, navigate via URL hash or by finding Scenes in the UI.
- Alternative reliable approach: After login, use `page.evaluate(() => { document.querySelector('[data-testid="scenes-nav"]')?.click(); })` or simply wait for the app to load and navigate. The most reliable method given the app uses `setCurrentPage('scenes')` internally is to look for any navigation element that goes to scenes, OR use the Templates page as an entry point.
- BEST APPROACH: After `loginAndPrepare`, use `page.goto('/app')` and then execute JavaScript to navigate: Since the app uses a state-based router (`currentPage`), we can try navigating via the URL. Check if there's a direct URL path. If not, find the "Scenes" text in the page. As a fallback, use the sidebar if navigateToSection supports it.
- Try `navigateToSection(page, 'scenes')` first. The helpers.js function may support it. If it errors, fall back to `page.goto('/app#scenes')` or use page evaluation.
- Once on the scenes page, wait for either scene cards or the empty state ("No scenes yet")
- Screenshot the page: `screenshots/116/116-01-scenes-list-desktop.png`
- If scenes exist: verify Duplicate and Delete buttons are visible on a card, screenshot a card showing these actions: `screenshots/116/116-01-scenes-card-actions-desktop.png`
- If scenes exist: click the Delete button on the first scene card, wait for the delete confirmation modal (`[role="dialog"]` with "Delete Scene" text), screenshot: `screenshots/116/116-01-scenes-delete-modal-desktop.png`, then click Cancel to close
- If scenes exist: click the Duplicate button on the first card and wait briefly for toast, screenshot: `screenshots/116/116-01-scenes-duplicate-desktop.png`

**SCENE-02: Scene creation modal with name and dimensions**
- On the scenes page, click the "Create Scene" button (contains text "Create Scene" or "Generate My First Scene")
- Wait for the AutoBuild/industry modal to appear (it's an IndustrySelectionModal or similar dialog)
- If a modal/dialog appears, screenshot: `screenshots/116/116-02-scenes-create-modal-desktop.png`
- Close the modal (click Cancel/Close or press Escape)
- If no modal appears (e.g., it navigates directly), screenshot whatever state results: `screenshots/116/116-02-scenes-create-action-desktop.png`

**SCENE-03: SVG editor loads with toolbar and canvas**
- Navigate to the Templates page via sidebar: click the "Templates" nav item
- Wait for the template gallery to load (look for template cards or gallery grid)
- Screenshot the templates page: `screenshots/116/116-03-templates-gallery-desktop.png`
- If template cards exist, click the first "Use Template" or similar action button on a template card. Or look for a "New Design" / "Create" button.
- Alternative: Navigate directly to the SVG editor by going to a URL that triggers it. After loading templates page, look for any button that opens the editor.
- If the SVG editor loads (look for canvas element, or the TopToolbar with save/export buttons, or the LeftSidebar), wait for loading to complete (spinner gone), then screenshot: `screenshots/116/116-03-svg-editor-loaded-desktop.png`
- If editor doesn't load (no templates available, button not found), screenshot current state: `screenshots/116/116-03-svg-editor-fallback-desktop.png`
- Verify toolbar area is visible (buttons for text, shapes, images, etc.)

Each test should use `test.slow()` since they involve multiple navigation steps and waits.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/scenes-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 scene/editor tests pass and produce PNG files in screenshots/116/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Verify all scene screenshots exist and fix failures</name>
  <files>tests/e2e/scenes-screenshots.spec.js</files>
  <action>
Run the full scenes screenshot spec and verify screenshot artifacts:

1. Run `npx playwright test tests/e2e/scenes-screenshots.spec.js --project=chromium`
2. Verify these files exist and are non-empty:
   - screenshots/116/116-01-scenes-list-desktop.png
   - screenshots/116/116-02-scenes-create-*-desktop.png (at least 1)
   - screenshots/116/116-03-*-desktop.png (at least 1 showing templates or SVG editor)

If any test fails, fix the selectors or waits in the spec. Common issues:
- `navigateToSection(page, 'scenes')` may not work since Scenes is not in sidebar -- fall back to direct app navigation or page.evaluate
- The "Create Scene" button may trigger an industry modal (IndustrySelectionModal) not a simple form -- look for `[role="dialog"]` or `.fixed` overlay
- SVG editor may take time to load the Fabric.js canvas -- add longer timeout (15s) for canvas element
- Template cards may use different selectors -- try `article`, `.template-card`, or cards with "Use Template" text
  </action>
  <verify>
    <automated>ls -la screenshots/116/116-0*.png 2>/dev/null | wc -l</automated>
  </verify>
  <done>At least 4 screenshot files exist covering scenes list, create modal, and SVG editor/templates, all non-zero size, all tests passing green</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/scenes-screenshots.spec.js --project=chromium` passes all tests
- `ls screenshots/116/116-0*.png` shows at least 4 files
- Each SCENE requirement (01-03) has corresponding screenshot evidence
</verification>

<success_criteria>
- SCENE-01, SCENE-02, SCENE-03 each have passing E2E tests with screenshot artifacts
- Tests use established helpers pattern (loginAndPrepare, screenshotStep)
- Screenshots follow 116-XX naming convention in screenshots/116/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/116-scenes-svg-editor-e2e/116-01-SUMMARY.md`
</output>
