---
phase: 117-playlists-layouts-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/layouts-screenshots.spec.js
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - LAYOUT-05
  - LAYOUT-06
  - LAYOUT-07
  - LAYOUT-08

must_haves:
  truths:
    - "Running the layouts E2E spec produces screenshots of layout list page with filters"
    - "Running the layouts E2E spec produces screenshots of layout creation modal with zone configuration"
    - "Running the layouts E2E spec produces screenshots of layout editor zone selection and property panel"
    - "Running the layouts E2E spec produces screenshots of widget type selector with 17+ types"
    - "Running the layouts E2E spec produces screenshots of clock widget configuration"
    - "Running the layouts E2E spec produces screenshots of weather widget configuration"
    - "Running the layouts E2E spec produces screenshots of data table widget configuration"
    - "Running the layouts E2E spec produces screenshots of video widget configuration"
  artifacts:
    - path: "tests/e2e/layouts-screenshots.spec.js"
      provides: "Layouts E2E screenshot test spec"
      min_lines: 200
    - path: "screenshots/117/"
      provides: "Screenshot evidence directory (shared with plan 01)"
  key_links:
    - from: "tests/e2e/layouts-screenshots.spec.js"
      to: "tests/e2e/helpers/index.js"
      via: "import screenshotStep, loginAndPrepare, etc."
      pattern: "import.*screenshotStep.*from.*helpers"
    - from: "tests/e2e/layouts-screenshots.spec.js"
      to: "src/pages/LayoutsPage.jsx"
      via: "navigates to Layouts/Templates sidebar link"
      pattern: "click.*Templates|Layouts"
    - from: "tests/e2e/layouts-screenshots.spec.js"
      to: "src/pages/LayoutEditorPage.jsx"
      via: "opens layout editor for zone/widget configuration"
      pattern: "layout-editor"
---

<objective>
Create E2E screenshot tests for the Layouts feature, covering list page with filtering, creation modal with zone presets, editor zone selection, widget type selector, and individual widget configuration panels (clock, weather, data table, video).

Purpose: Provide screenshot evidence for all 8 layout requirements (LAYOUT-01 through LAYOUT-08).
Output: tests/e2e/layouts-screenshots.spec.js with 8 test cases producing screenshots in screenshots/117/.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/helpers/index.js
@tests/e2e/helpers/screenshots.js
@tests/e2e/scenes-screenshots.spec.js (pattern reference)
@src/pages/LayoutsPage.jsx
@src/pages/LayoutEditorPage.jsx
</context>

<interfaces>
<!-- Key patterns the executor needs -->

From tests/e2e/helpers/index.js:
```javascript
export { screenshotStep, VIEWPORTS, cleanScreenshots, detectViewport } from './screenshots.js';
export { loginAndPrepare, dismissAnyModals, waitForPageReady, navigateToSection, generateTestName, assertAppReady } from '../helpers.js';
```

Key LayoutsPage UI elements (actually a template gallery page):
- Sidebar nav link: "Templates" (this is the LayoutsPage which shows as Templates in sidebar)
- Left sidebar with filter categories: All, Featured, Popular, Your Templates, Recent Designs, then industry categories
- Hero section with search bar and quick tags
- Orientation filter: Landscape / Portrait buttons
- Visual Mode filter: Static / Animation buttons
- Industries filter section (collapsed by default)
- "Your Designs" button opens user layouts with pagination
- "New Design" button navigates to design editor
- Template cards with hover overlay showing "Use Template"

Key LayoutEditorPage UI elements:
- Layout name editable header
- PRESET_LAYOUTS array: Full Screen, Two Columns, Three Columns, Main + Sidebar, Header + Content, L-Shape, Quadrant (7 presets)
- Zone canvas with colored zones (ZONE_COLORS array with 8 colors)
- Selected zone property panel on right side
- "Add Zone" button
- Zone click selects it, shows properties
- Assign content modal (playlists or media tabs)
- Save button with dirty state indicator

IMPORTANT: The LayoutsPage is actually the "Templates" page in the sidebar. The actual "Layouts" functionality is accessed differently:
- LayoutsPage = template gallery (sidebar: Templates)
- LayoutEditorPage = edit a specific layout by ID

For layout list/CRUD, the app routes through sidebar "Templates" > "Your Designs" section which shows user layouts. Or there may be a direct layouts list accessible via __setCurrentPage.

Widget types: The LayoutEditorPage zones can be assigned playlists or media. Widget-specific config (clock, weather, data table, video) may be part of the zone property panel when a specific content type is assigned, or may be accessed through the Apps page integration. The test should capture whatever configuration UI is available for zones.

Zone property panel shows: zone name, position (x%, y%), size (width%, height%), z-index, assigned content (playlist or media).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create layouts E2E screenshot spec</name>
  <files>tests/e2e/layouts-screenshots.spec.js</files>
  <action>
Create a Playwright E2E test file following the exact pattern from scenes-screenshots.spec.js. Use area '117' for all screenshotStep calls. The file must contain 8 test cases:

**LAYOUT-01: Layout list page with create and filter actions**
- Navigate via sidebar click on "Templates" (LayoutsPage serves as the template/layout gallery)
- Wait for the page to load (hero section with "What Template Are You Looking For?" heading, or template grid)
- Screenshot the layouts/templates page: screenshotStep(page, '117', '10-layouts-list')
- Click "Your Designs" button in the left sidebar to show user layouts
- Wait for the section to load (either user layouts grid or "No designs yet" empty state)
- Screenshot: '10-layouts-your-designs'
- Click the "Featured" category in the sidebar to show featured filter
- Screenshot: '10-layouts-featured-filter'

**LAYOUT-02: Layout creation modal with zone configuration**
- The LayoutEditorPage has PRESET_LAYOUTS (Full Screen, Two Columns, etc.)
- Navigate to create a new layout. Click "New Design" button on the Templates page to open the editor.
- Alternatively, use window.__setCurrentPage('layout-editor') or 'design-editor' to navigate
- Wait for editor to load
- Screenshot the layout editor with preset options: '11-layout-editor-presets'
- If preset buttons are visible, click one (e.g., "Two Columns") to apply
- Screenshot after preset applied: '11-layout-two-columns'

**LAYOUT-03: Layout editor zone selection and property panel**
- In the layout editor, if zones exist, click on a zone to select it
- Wait for the zone property panel to appear on the right side
- Screenshot the editor with selected zone and property panel: '12-zone-selected-properties'
- Click "Add Zone" button to add a new zone
- Screenshot after adding: '12-zone-added'

**LAYOUT-04: Widget type selector shows all 17+ widget types**
- In the layout editor with a zone selected, click to assign content to the zone
- This opens the assign modal with tabs (playlists, media)
- Screenshot the assign modal: '13-widget-type-selector'
- If there are different content type options or tabs visible, screenshot each tab
- Screenshot the playlists tab: '13-assign-playlists-tab'
- Screenshot the media tab: '13-assign-media-tab'
- Note: The "17+ widget types" may refer to the combined apps + media types available. Capture whatever content assignment UI is present.

**LAYOUT-05: Clock widget configuration (12h/24h, analog/digital)**
- Widget configuration may be accessible through the Apps page integration or zone properties
- Navigate to the zone property panel and look for widget-specific settings
- If clock configuration exists (may be through assigned app content), screenshot it: '14-clock-widget-config'
- If not directly available in zone editor, try navigating to Apps page and finding Clock app
- Use window.__setCurrentPage('apps') if needed, find a Clock app in the gallery
- Screenshot the clock app detail/config: '14-clock-app-config'
- Fallback: screenshot the zone properties panel showing content type: '14-zone-content-config'

**LAYOUT-06: Weather widget configuration (location, forecast mode)**
- Similar approach to LAYOUT-05 but for weather widget
- Look for weather configuration in zone properties or Apps gallery
- Screenshot: '15-weather-widget-config'
- Fallback: navigate to Apps and find Weather app
- Screenshot: '15-weather-app-config'

**LAYOUT-07: Data table widget configuration (source, columns, refresh)**
- Look for data table configuration in zone properties or through data sources integration
- If zone can be assigned a data source, capture that UI
- Screenshot: '16-data-table-config'
- Fallback: navigate to Data Sources page and screenshot the creation flow
- Use window.__setCurrentPage('data-sources') if needed
- Screenshot: '16-data-sources-page'

**LAYOUT-08: Video widget configuration (source, HLS, autoplay)**
- In the layout editor, assign a video media item to a zone
- Look for video-specific configuration options (source URL, autoplay, etc.)
- Screenshot the video configuration: '17-video-widget-config'
- If video config is minimal (just media assignment), screenshot the zone with video assigned
- Screenshot: '17-zone-video-assigned'

**Important patterns to follow:**
- Same beforeEach pattern as playlists spec: loginAndPrepare, assertAppReady, skip non-chromium, skip without credentials
- Use `test.slow()` for tests that navigate through multiple pages
- Use screenshotStep(page, '117', ...) -- same area '117' as playlists spec (they share the directory)
- Start layout screenshot step numbers at 10+ to avoid collision with playlist screenshots (01-09)
- Use try/catch with fallback screenshots for UI elements that may not exist
- For navigation to layout editor, try sidebar "Templates" > "New Design" or window.__setCurrentPage('design-editor')
- For widget configs that may not have dedicated UI, capture the closest available config panel
  </action>
  <verify>
    <automated>ls tests/e2e/layouts-screenshots.spec.js && node -c "import('file:///$(pwd)/tests/e2e/layouts-screenshots.spec.js').catch(() => 'syntax ok')" 2>&1 | head -5; echo "File exists and is valid JS"</automated>
  </verify>
  <done>tests/e2e/layouts-screenshots.spec.js exists with 8 test cases (LAYOUT-01 through LAYOUT-08), each using screenshotStep with area '117'</done>
</task>

<task type="auto">
  <name>Task 2: Run layouts E2E tests and capture screenshots</name>
  <files>screenshots/117/</files>
  <action>
Run the layouts screenshot spec and verify screenshots are produced.

1. Run: `npx playwright test tests/e2e/layouts-screenshots.spec.js --project=chromium --reporter=list`
2. If tests fail due to selector issues, fix the selectors in the spec file based on error messages. Common fixes:
   - If "Templates" sidebar link not found, try various selectors: `page.locator('a').filter({ hasText: 'Templates' })`, `navigateToSection(page, 'layouts')`, or `navigateToSection(page, 'templates')`
   - If layout editor doesn't load, try `window.__setCurrentPage('design-editor')` or `window.__setCurrentPage('layout-editor')`
   - If zone property panel doesn't appear, check if zones need to be created first via preset
   - If widget config panels don't exist, capture whatever zone config UI is available as fallback
   - For Apps-based widget configs, navigate via `window.__setCurrentPage('apps')` and search for specific app types
3. After tests pass (or pass with expected graceful skips), verify screenshots: `ls screenshots/117/ | grep -E "^117-1[0-7]"`
4. Each test should produce at least 1-2 screenshots. Combined with plan 01, total should be 20+ screenshots.

If TEST_USER_EMAIL is not set, tests will skip gracefully -- that is acceptable.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/layouts-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>Layouts E2E spec runs without crashes. Screenshots captured in screenshots/117/ for each test that can execute.</done>
</task>

</tasks>

<verification>
1. `tests/e2e/layouts-screenshots.spec.js` exists with 8 named test cases matching LAYOUT-01 through LAYOUT-08
2. Each test uses `screenshotStep(page, '117', ...)` with step numbers starting at 10+
3. Screenshots are saved to `screenshots/117/` directory (shared with playlist screenshots)
4. Test follows established patterns from scenes-screenshots.spec.js
</verification>

<success_criteria>
- 8 test cases exist covering all LAYOUT requirements
- Tests run without crashes (may skip gracefully if no test credentials)
- Screenshot files produced in screenshots/117/ for each passing test
- Code follows project E2E patterns (fixtures, helpers, screenshotStep)
- No file conflicts with plan 01 (different spec file, shared screenshot directory with non-overlapping step numbers)
</success_criteria>

<output>
After completion, create `.planning/phases/117-playlists-layouts-e2e/117-02-SUMMARY.md`
</output>
