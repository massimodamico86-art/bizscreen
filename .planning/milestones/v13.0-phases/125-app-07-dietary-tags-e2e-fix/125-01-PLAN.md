---
phase: 125-app-07-dietary-tags-e2e-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/apps-menuboards-screenshots.spec.js
  - screenshots/120/120-12-menu-board-dietary-tags-desktop.png
autonomous: true
requirements:
  - APP-07
gap_closure: true

must_haves:
  truths:
    - "Screenshot 120-12 is visually distinct from screenshot 120-10"
    - "The dietary tag badges or expanded DietaryTagPicker are visible in the screenshot"
    - "The APP-07 test does not fall through to the dialog fallback screenshot"
  artifacts:
    - path: "screenshots/120/120-12-menu-board-dietary-tags-desktop.png"
      provides: "Distinct dietary tag evidence screenshot"
    - path: "tests/e2e/apps-menuboards-screenshots.spec.js"
      provides: "Fixed APP-07 test with reliable locator"
  key_links:
    - from: "tests/e2e/apps-menuboards-screenshots.spec.js"
      to: "src/components/menu-boards/MenuItemRow.jsx"
      via: "Locator finds Caesar Salad row and clicks Tag toggle button"
      pattern: "title.*Dietary tags"
---

<objective>
Fix the APP-07 dietary/allergen tag E2E test so it produces a visually distinct screenshot (120-12) proving tag assignment works, instead of falling through to a dialog-level fallback that duplicates screenshot 120-10.

Purpose: Close the APP-07 gap identified in the v13.0 milestone audit. The current test's CSS compound class locator (`.border.border-gray-200.rounded-lg.p-3`) fails to match the Caesar Salad item row, causing the fallback to screenshot the entire dialog -- identical to 120-10.

Output: Updated test file with reliable locator, regenerated screenshot 120-12 showing dietary tag evidence.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/120-data-sources-apps-moderation-e2e/120-VERIFICATION.md

<interfaces>
<!-- Key component structure the executor needs to write correct locators -->

From src/components/menu-boards/MenuItemRow.jsx:
- Outer div: `<div class="border border-gray-200 rounded-lg p-3 bg-white">`
- Item name is in an `<input>` with `defaultValue={item.name}` and `placeholder="Item name"`
- Tag toggle button: `<button title="Dietary tags">` with Tag icon, has class `text-green-600 bg-green-50` when item has tags
- Dietary tag badges (visible when showTags=false and item has tags): `<div class="flex flex-wrap gap-1">` containing colored `<span>` badges
- DietaryTagPicker (visible when showTags=true): `<div class="flex flex-wrap gap-1.5 py-1">` with toggleable buttons showing tag labels

From mock data (line 101-105):
- Caesar Salad has `dietary_tags: ['vegetarian', 'gluten-free']`
- It's in category cat-001 (Starters), order_index 0, so it should be the first item rendered

From src/components/menu-boards/DietaryTagPicker.jsx:
- Selected tags: `style={{ backgroundColor: tag.color, color: '#fff' }}`
- Unselected tags: `style={{ border: '1px solid #d1d5db', color: '#6b7280' }}`
- Each button has `title={tag.label}` (e.g., "Vegetarian", "Gluten-Free")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix APP-07 locator and capture distinct dietary tag screenshot</name>
  <files>tests/e2e/apps-menuboards-screenshots.spec.js</files>
  <action>
Replace the APP-07 test body (lines 517-556 in apps-menuboards-screenshots.spec.js) with a reliable approach:

1. **Keep the existing setup** (loginAndPrepare, setupMenuBoardMocking, navigate to menu-boards, open editor dialog). This code (lines 517-535) works correctly -- the dialog opens fine.

2. **Replace the item locator** (lines 537-555). Instead of the unreliable compound CSS selector `.border.border-gray-200.rounded-lg.p-3` with hasText filter, use this strategy:
   - First, scroll to the "Starters" category section (Caesar Salad is in Starters, cat-001)
   - Locate "Caesar Salad" text within the dialog using `dialog.getByDisplayValue('Caesar Salad')` (it's an input with defaultValue). If getByDisplayValue doesn't work (React defaultValue), use `dialog.locator('input[value="Caesar Salad"]')` or `dialog.locator('input').filter({ has: page.locator('[placeholder="Item name"]') })` and check values. Simplest reliable approach: `dialog.locator('text=Caesar Salad')` to find any element containing that text.
   - Once found, traverse up to find the item row container. Use `dialog.locator('text=Caesar Salad').locator('xpath=ancestor::div[contains(@class, "rounded-lg")]').first()` or simply find the parent border div.

3. **Alternative simpler approach (PREFERRED):** Since Caesar Salad has dietary_tags and the Tag button gets class `text-green-600 bg-green-50` when tags exist, find the first green Tag button in the dialog:
   - `const tagButton = dialog.locator('button[title="Dietary tags"]').first();`
   - This gets the first item's tag button. Since Caesar Salad is the first item in the first category (Starters), this should be the Caesar Salad row's tag button.
   - Click the tag button to expand the DietaryTagPicker: `await tagButton.click();`
   - Wait 500ms for the picker to render.
   - Now screenshot the area. The item row now contains the expanded DietaryTagPicker with "Vegetarian" and "Gluten-Free" shown as selected (solid colored background) -- visually very distinct from 120-10.

4. **Screenshot strategy:** After expanding the tag picker, screenshot the dialog. This produces a distinct screenshot because:
   - The DietaryTagPicker is now visible (it was hidden before)
   - Two tags ("Vegetarian", "Gluten-Free") have solid colored backgrounds
   - The inline tag badges are hidden (they only show when showTags=false)
   - This is visually different from 120-10 which shows the plain editor

5. **Remove the fallback entirely.** The test should fail explicitly if it can't find the tag button, not silently produce a duplicate screenshot. Replace the if/else with a direct assertion flow.

The replacement code for lines 537-555 should be approximately:

```javascript
    // Click the Dietary Tags button on the first item (Caesar Salad in Starters category)
    // Caesar Salad has dietary_tags: ['vegetarian', 'gluten-free'] so its Tag button is green
    const tagButton = dialog.locator('button[title="Dietary tags"]').first();
    await tagButton.waitFor({ state: 'visible', timeout: 5000 });
    await tagButton.scrollIntoViewIfNeeded();
    await tagButton.click();
    await page.waitForTimeout(500);

    // The DietaryTagPicker is now expanded showing all dietary tags
    // Vegetarian and Gluten-Free are selected (solid colored backgrounds)
    // This is visually distinct from the base editor screenshot (120-10)
    await dialog.screenshot({
      path: 'screenshots/120/120-12-menu-board-dietary-tags-desktop.png',
    });
```

Do NOT change any other test in the file. Only modify the APP-07 test (lines 517-556).
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/apps-menuboards-screenshots.spec.js --grep "APP-07" --project=desktop 2>&1 | tail -20</automated>
  </verify>
  <done>
    - APP-07 test passes without falling through to fallback
    - Screenshot 120-12 exists and shows the DietaryTagPicker expanded with selected tags
    - Screenshot 120-12 is visually distinct from 120-10 (not byte-identical)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify screenshot distinctness</name>
  <files>screenshots/120/120-12-menu-board-dietary-tags-desktop.png, screenshots/120/120-10-menu-board-editor-desktop.png</files>
  <action>
After Task 1 produces the new screenshot, verify the fix worked by comparing 120-12 and 120-10:

1. Run: `md5 screenshots/120/120-10-menu-board-editor-desktop.png screenshots/120/120-12-menu-board-dietary-tags-desktop.png`
2. Confirm the MD5 hashes are DIFFERENT (they were identical before: f1b13d944465112675364403895d606b)
3. Check file sizes: `ls -la screenshots/120/120-10-menu-board-editor-desktop.png screenshots/120/120-12-menu-board-dietary-tags-desktop.png`
4. Both should be >5KB and have different sizes

If hashes are still identical, the fix did not work -- investigate by checking if the tag button click actually expanded the picker (add a debug screenshot or check console output).
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && md5 -q screenshots/120/120-10-menu-board-editor-desktop.png > /tmp/md5_10.txt && md5 -q screenshots/120/120-12-menu-board-dietary-tags-desktop.png > /tmp/md5_12.txt && diff /tmp/md5_10.txt /tmp/md5_12.txt && echo "FAIL: screenshots are identical" || echo "PASS: screenshots are distinct"</automated>
  </verify>
  <done>
    - MD5 hashes of 120-10 and 120-12 are different
    - Both screenshots are >5KB (not empty/corrupt)
    - APP-07 gap from 120-VERIFICATION.md is closed
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/e2e/apps-menuboards-screenshots.spec.js --grep "APP-07" --project=desktop` passes
2. `md5 screenshots/120/120-10-*.png screenshots/120/120-12-*.png` shows different hashes
3. No other tests in the file are broken: `npx playwright test tests/e2e/apps-menuboards-screenshots.spec.js --project=desktop` all pass
</verification>

<success_criteria>
- APP-07 test passes reliably without fallback
- Screenshot 120-12 shows expanded DietaryTagPicker with selected Vegetarian and Gluten-Free tags
- Screenshot 120-12 is byte-different from 120-10
- All other APP-* tests in the file still pass
</success_criteria>

<output>
After completion, create `.planning/phases/125-app-07-dietary-tags-e2e-fix/125-01-SUMMARY.md`
</output>
