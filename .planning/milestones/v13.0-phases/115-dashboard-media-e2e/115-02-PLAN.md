---
phase: 115-dashboard-media-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/media-screenshots.spec.js
autonomous: true
requirements:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
  - MEDIA-06

must_haves:
  truths:
    - "Running media-screenshots.spec.js produces a screenshot of the upload modal with file selection UI"
    - "Running media-screenshots.spec.js produces screenshots showing both grid and list view modes"
    - "Running media-screenshots.spec.js produces screenshots of type filtering across media sub-pages"
    - "Running media-screenshots.spec.js produces a screenshot of the media preview popover with metadata"
    - "Running media-screenshots.spec.js produces a screenshot of inline rename editing state"
    - "Running media-screenshots.spec.js produces a screenshot of the delete confirmation dialog"
  artifacts:
    - path: "tests/e2e/media-screenshots.spec.js"
      provides: "Media library core E2E screenshot test suite"
      min_lines: 150
  key_links:
    - from: "tests/e2e/media-screenshots.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import { loginAndPrepare, navigateToSection, waitForPageReady }"
      pattern: "import.*helpers"
---

<objective>
Create screenshot-verified E2E tests for media library core operations: upload flow, grid/list toggle, type filtering, preview popover, rename, and delete.

Purpose: Establish screenshot evidence that all core media library interactions render correctly.
Output: tests/e2e/media-screenshots.spec.js covering MEDIA-01 through MEDIA-06.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@tests/e2e/helpers.js
@tests/e2e/media.spec.js
@playwright.config.js
</context>

<interfaces>
<!-- Existing test helpers -->
From tests/e2e/helpers.js:
```javascript
export async function loginAndPrepare(page, options = {})
export async function navigateToSection(page, section) // 'media' expands sidebar > clicks "All Media"
export async function waitForPageReady(page)
export async function dismissAnyModals(page)
```

From existing media.spec.js patterns:
- Media page heading: `main` > heading matching /all media/i
- Add Media button: `button:has-text("Add Media")` in bottom action bar
- Upload modal has tabs: "Upload Files" and "Web Page URL"
- Search input: `getByPlaceholder(/search media/i)`
- View toggle: `.flex.border.rounded-lg` container with two buttons (grid/list)
- Media items use `.aspect-video` class in grid view
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create media core screenshot E2E spec</name>
  <files>tests/e2e/media-screenshots.spec.js</files>
  <action>
Create a NEW spec file `tests/e2e/media-screenshots.spec.js` (do NOT modify existing `media.spec.js`). Use the `chromium` project only, skip if `TEST_USER_EMAIL` not set.

Use `beforeEach` to `loginAndPrepare` with client credentials, then `navigateToSection(page, 'media')` to land on All Media page.

Create these test cases with screenshot capture:

**MEDIA-01: Upload flow**
- On All Media page, click "Add Media" button
- Wait for dialog to appear
- Screenshot the upload modal: `screenshots/115-06-media-upload-modal.png`
- If upload tab visible, screenshot it showing file drop zone / file selection area
- Switch to "Web Page URL" tab if available, screenshot: `screenshots/115-06-media-webpage-tab.png`
- Close modal

**MEDIA-02: Grid/list view toggle**
- On All Media page, locate the view toggle buttons (`.flex.border.rounded-lg` or similar)
- Screenshot current view (grid): `screenshots/115-07-media-grid-view.png`
- Click list view button (second toggle button)
- Wait for view to change
- Screenshot list view: `screenshots/115-07-media-list-view.png`
- Click grid view button to restore

**MEDIA-03: Type filtering via sub-pages**
- Navigate to Images sub-page: click sidebar "Media" to expand, then click "Images" button
- Wait for page heading to show "Images" or similar
- Screenshot: `screenshots/115-08-media-filter-images.png`
- Navigate to Videos sub-page similarly
- Screenshot: `screenshots/115-08-media-filter-videos.png`
- Navigate to Audio sub-page
- Screenshot: `screenshots/115-08-media-filter-audio.png`
- For sub-page navigation: after expanding Media in sidebar, look for buttons matching "Images", "Videos", "Audio", "Documents", "Web Pages" text

**MEDIA-04: Preview popover**
- On All Media page, check if any media items exist (`.aspect-video` or media card elements)
- If media exists: hover over first media item to trigger preview popover
- Wait for popover/tooltip to appear (look for `[role="tooltip"]`, `[data-radix-popper-content-wrapper]`, or popover elements)
- Screenshot: `screenshots/115-09-media-preview-popover.png`
- If no media: screenshot the empty state instead: `screenshots/115-09-media-empty-state.png`

**MEDIA-05: Rename inline editing**
- If media items exist: look for a rename action (right-click context menu, or actions dropdown on media card)
- Try clicking the actions menu (three-dot icon or dropdown trigger on a media card)
- Look for "Rename" option and click it
- Screenshot the inline editing state: `screenshots/115-10-media-rename-editing.png`
- If no items or no rename action available: screenshot the page state with a note in test name

**MEDIA-06: Delete with confirmation**
- If media items exist: find delete action via actions dropdown on a media card
- Click delete to trigger confirmation dialog
- Screenshot the confirmation dialog: `screenshots/115-11-media-delete-confirmation.png`
- Click Cancel to dismiss (do NOT actually delete)
- If no items: screenshot empty state

For items that depend on existing media (MEDIA-04, 05, 06): use conditional logic -- if no media items found after page load, mark the test as `test.skip('No media items available for this test')` but still take a screenshot of the empty state.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/media-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 media core tests pass (or skip gracefully if no media items), producing screenshot files 115-06 through 115-11</done>
</task>

<task type="auto">
  <name>Task 2: Verify media screenshots and fix failures</name>
  <files>tests/e2e/media-screenshots.spec.js</files>
  <action>
Run the full media screenshot spec and verify:

1. Run `npx playwright test tests/e2e/media-screenshots.spec.js --project=chromium`
2. Check that screenshot files exist:
   - screenshots/115-06-media-upload-modal.png
   - screenshots/115-07-media-grid-view.png
   - screenshots/115-07-media-list-view.png
   - screenshots/115-08-media-filter-*.png (at least images and videos)
   - screenshots/115-09-media-preview-popover.png OR screenshots/115-09-media-empty-state.png
   - screenshots/115-10-media-rename-editing.png (or skip)
   - screenshots/115-11-media-delete-confirmation.png (or skip)

3. If tests fail, debug by:
   - Checking if media sub-page navigation buttons exist in sidebar (they may be nested under expanded Media menu)
   - Checking hover/popover selectors match actual DOM
   - Checking if actions dropdown exists on media cards
   - Adjusting selectors based on actual page structure

Fix any failing selectors. The key is that each MEDIA requirement has EITHER a passing test with screenshot OR a graceful skip with explanation.
  </action>
  <verify>
    <automated>ls -la screenshots/115-0[6-9]*.png screenshots/115-1[0-1]*.png 2>/dev/null | wc -l</automated>
  </verify>
  <done>At least 6 screenshot files exist for MEDIA-01 through MEDIA-06, all tests pass or skip gracefully</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/media-screenshots.spec.js --project=chromium` all pass/skip
- `ls screenshots/115-0[6-9]*.png screenshots/115-1[0-1]*.png` shows at least 6 files
- Each MEDIA requirement (01-06) has corresponding screenshot evidence
</verification>

<success_criteria>
- 6 MEDIA requirements (01-06) have passing E2E tests with screenshots
- No modifications to existing media.spec.js
- Tests use established helpers pattern
- Conditional logic handles missing media items gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/115-dashboard-media-e2e/115-02-SUMMARY.md`
</output>
