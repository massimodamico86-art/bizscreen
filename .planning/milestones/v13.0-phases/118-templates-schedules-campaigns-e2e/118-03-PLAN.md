---
phase: 118-templates-schedules-campaigns-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/campaigns-screenshots.spec.js
autonomous: true
requirements:
  - CAMP-01
  - CAMP-02
  - CAMP-03
  - CAMP-04
  - CAMP-05
  - CAMP-06
  - CAMP-07
  - CAMP-08
  - CAMP-09

must_haves:
  truths:
    - "Running the campaigns E2E spec produces screenshots of campaign list with status indicators"
    - "Running the campaigns E2E spec captures campaign creation with priority and dates"
    - "Running the campaigns E2E spec captures content assignment"
    - "Running the campaigns E2E spec captures screen targeting"
    - "Running the campaigns E2E spec captures emergency push modal"
    - "Running the campaigns E2E spec captures campaign analytics"
    - "Running the campaigns E2E spec captures rotation controls"
    - "Running the campaigns E2E spec captures seasonal date picker"
    - "Running the campaigns E2E spec captures template picker modal"
  artifacts:
    - path: "tests/e2e/campaigns-screenshots.spec.js"
      provides: "Campaign screenshot E2E tests"
      min_lines: 180
    - path: "screenshots/118/"
      provides: "Screenshot evidence for CAMP-01 through CAMP-09"
  key_links:
    - from: "tests/e2e/campaigns-screenshots.spec.js"
      to: "CampaignsPage"
      via: "__setCurrentPage('campaigns') with feature gate handling"
      pattern: "__setCurrentPage.*campaigns"
    - from: "tests/e2e/campaigns-screenshots.spec.js"
      to: "CampaignEditorPage"
      via: "__setCurrentPage('campaign-editor-{id}')"
      pattern: "__setCurrentPage.*campaign-editor"
---

<objective>
Create Playwright E2E screenshot tests for campaign management covering all 9 CAMP requirements: list with status, creation with priority/dates, content assignment, screen targeting, emergency push, analytics, rotation controls, seasonal picker, and template picker.

Purpose: Provide screenshot evidence that campaign management workflows work end-to-end.
Output: tests/e2e/campaigns-screenshots.spec.js with 9 test cases producing screenshots in screenshots/118/
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/e2e/helpers.js
@tests/e2e/helpers/screenshots.js
@src/pages/CampaignsPage.jsx
@src/pages/CampaignEditorPage.jsx
@src/components/campaigns/RotationControls.jsx
@src/components/campaigns/SeasonalDatePicker.jsx
@src/components/campaigns/TemplatePickerModal.jsx

<interfaces>
<!-- Navigation and feature gate patterns -->
- Campaigns page: window.__setCurrentPage('campaigns') -- FEATURE GATED (Feature.CAMPAIGNS)
- If user plan doesn't include CAMPAIGNS, FeatureUpgradePrompt renders instead
- Campaign editor: window.__setCurrentPage('campaign-editor-{uuid}') or 'campaign-editor-new' for new
- CampaignsPage has: search, status filter dropdown (Draft/Scheduled/Active/Completed/Paused), create button, TemplatePickerModal
- CampaignEditorPage has: SeasonalDatePicker, RotationControls, CampaignAnalyticsCard, TargetPickerModal, ContentPickerModal
- STATUS_CONFIG maps to: Draft(gray), Scheduled(blue), Active(green), Completed(gray), Paused(yellow)
- Emergency push: look for emergency/push related UI in campaigns or screen groups
- Use screenshotStep(page, '118', 'NN-description') with 20-28 range for campaigns
- Handle feature gate: if campaigns page shows upgrade prompt, screenshot that and skip gracefully
- Use page.route() to mock Supabase API for campaigns data if backend unavailable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create campaigns screenshot E2E spec</name>
  <files>tests/e2e/campaigns-screenshots.spec.js</files>
  <action>
Create tests/e2e/campaigns-screenshots.spec.js with 9 test cases covering CAMP-01 through CAMP-09.

Structure:
- Import { test, expect } from '@playwright/test'
- Import { loginAndPrepare, navigateToSection, waitForPageReady, dismissAnyModals } from './helpers.js'
- Import { screenshotStep } from './helpers/screenshots.js'
- test.describe('Campaigns Screenshots') with beforeEach that skips non-chromium and logs in

IMPORTANT: Campaigns is feature-gated. The test user may not have access. Each test should:
1. Navigate to campaigns via window.__setCurrentPage('campaigns')
2. Check if FeatureUpgradePrompt is shown (look for "Upgrade" text or plan-related messaging)
3. If feature-gated, capture upgrade prompt screenshot and skip gracefully
4. If accessible, proceed with the test

Create a helper function navigateToCampaigns(page) that handles this pattern and returns {gated: boolean}.

Test cases:
1. CAMP-01 (list with status indicators): Navigate to campaigns. Screenshot the list page. If campaigns exist, verify status badges are visible (Draft, Active, Scheduled etc.). If empty, screenshot empty state. If feature-gated, screenshot the upgrade prompt.

2. CAMP-02 (creation with priority and dates): On campaigns page, click create/add button. If a create modal or form appears, fill in campaign name, set priority, set date range. Screenshot the creation form. If using TemplatePickerModal for creation, screenshot that instead.

3. CAMP-03 (content assignment): Navigate to campaign editor (via __setCurrentPage('campaign-editor-new') or click into existing campaign). In the editor, look for content assignment section (ContentPickerModal trigger). Click to open content picker. Screenshot showing playlist or template assignment options. Use page.route() to mock campaign data if needed.

4. CAMP-04 (screen targeting): In campaign editor, look for screen targeting section (TargetPickerModal trigger, Target icon). Click to open target picker. Screenshot showing screen/group selection. Mock data via page.route() if backend unavailable.

5. CAMP-05 (emergency push modal): Look for emergency push button on campaigns page or in the editor. This may be a "Push Now" or emergency broadcast button. Click to open the modal. Screenshot the emergency push confirmation dialog. If not found on campaigns, check if it exists on screens page and note in screenshot.

6. CAMP-06 (campaign analytics): In campaign editor, look for CampaignAnalyticsCard component showing impressions, clicks, or performance data. Screenshot the analytics section. If no data, screenshot the empty analytics state.

7. CAMP-07 (rotation controls): In campaign editor, find the RotationControls component showing weight/order settings for content rotation. Screenshot the rotation controls UI. These may include weight sliders or order drag handles.

8. CAMP-08 (seasonal date picker): In campaign editor, find the SeasonalDatePicker component. Screenshot it showing seasonal/recurring date configuration. This allows setting seasonal recurrence for campaigns.

9. CAMP-09 (template picker modal): On campaigns page, look for "Create from Template" or template picker trigger. Click to open TemplatePickerModal. Screenshot the modal showing available templates for campaign creation. If no trigger found, navigate to campaign creation flow that uses templates.

Key patterns:
- Use screenshotStep(page, '118', '20-campaigns-list') naming with 20-28 range
- Handle feature gate as first check in every test -- capture upgrade prompt if shown
- Use page.route() to mock Supabase API responses for campaigns, returning mock data with various statuses
- For campaign editor, navigate via: page.evaluate(() => window.__setCurrentPage('campaign-editor-new'))
- Use dispatchEvent for modal button clicks intercepted by overlay
- Scope modal inputs to [role="dialog"]
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/campaigns-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 CAMP test cases pass (or skip gracefully with screenshots) producing screenshot files in screenshots/118/</done>
</task>

<task type="auto">
  <name>Task 2: Verify all campaign screenshots exist</name>
  <files>screenshots/118/</files>
  <action>
Run the campaigns screenshot spec and verify that screenshot files are produced in screenshots/118/ directory. List the files to confirm at least 9 new screenshots exist for campaign tests (118-20 through 118-28 range). If any test failed in Task 1, review the error and fix the spec.

Also do a final count of ALL screenshots in screenshots/118/ to confirm the phase total (templates + schedules + campaigns should be >= 23 total screenshots).

Verification: ls screenshots/118/118-2* | wc -l should show >= 9 files.
  </action>
  <verify>
    <automated>ls /Users/massimodamico/bizscreen/screenshots/118/118-2* 2>/dev/null | wc -l | grep -E '^[9]|^[1-9][0-9]'</automated>
  </verify>
  <done>At least 9 screenshot files exist in screenshots/118/ covering CAMP-01 through CAMP-09</done>
</task>

</tasks>

<verification>
- All 9 CAMP requirements have corresponding test cases in campaigns-screenshots.spec.js
- Tests pass on chromium project without crashes
- Feature gate handling works: captures upgrade prompt if campaigns not available
- Screenshots are saved to screenshots/118/ with consistent naming (20+ range)
- Tests handle backend-unavailable gracefully with page.route() mocking
</verification>

<success_criteria>
- campaigns-screenshots.spec.js contains 9 test cases (CAMP-01 through CAMP-09)
- All tests pass or skip gracefully
- At least 9 screenshot files exist in screenshots/118/
- Feature gate scenario handled (upgrade prompt captured if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/118-templates-schedules-campaigns-e2e/118-03-SUMMARY.md`
</output>
