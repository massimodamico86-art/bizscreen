---
phase: 118-templates-schedules-campaigns-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/templates-screenshots.spec.js
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - TMPL-06
  - TMPL-07
  - TMPL-08

must_haves:
  truths:
    - "Running the templates E2E spec produces screenshots of gallery browse with category filters"
    - "Running the templates E2E spec captures template search with debounced results"
    - "Running the templates E2E spec captures template card hover state"
    - "Running the templates E2E spec captures 'Use Template' opening the editor"
    - "Running the templates E2E spec captures Your Designs tab"
    - "Running the templates E2E spec captures portrait/landscape filter"
    - "Running the templates E2E spec captures industry category expansion"
  artifacts:
    - path: "tests/e2e/templates-screenshots.spec.js"
      provides: "Template marketplace screenshot E2E tests"
      min_lines: 150
    - path: "screenshots/118/"
      provides: "Screenshot evidence for TMPL-01 through TMPL-08"
  key_links:
    - from: "tests/e2e/templates-screenshots.spec.js"
      to: "SvgTemplateGalleryPage"
      via: "sidebar navigation to 'templates' page"
      pattern: "navigateToSection.*templates|__setCurrentPage.*templates"
---

<objective>
Create Playwright E2E screenshot tests for the template gallery/marketplace covering all 8 TMPL requirements: gallery browse with category filters, debounced search, card hover animation, one-click editor open, quick customize panel, Your Designs tab, orientation filter, and industry categories.

Purpose: Provide screenshot evidence that the template browsing experience works end-to-end.
Output: tests/e2e/templates-screenshots.spec.js with 8 test cases producing screenshots in screenshots/118/
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/e2e/helpers.js
@tests/e2e/helpers/screenshots.js
@tests/e2e/dashboard-screenshots.spec.js
@src/pages/SvgTemplateGalleryPage.jsx
@src/pages/TemplateMarketplacePage.jsx
@src/components/templates/TemplateGrid.jsx

<interfaces>
<!-- Navigation patterns from prior phases -->
- Templates page: sidebar nav item 'templates' -> SvgTemplateGalleryPage
- Template Marketplace: window.__setCurrentPage('template-marketplace') -> TemplateMarketplacePage
- SVG Gallery has: SearchBar (debounced 300ms), filter sidebar (categories, industries, styles), orientation filter (Monitor/Smartphone icons), "Your Designs" tab, TemplateCard with hover
- The 'templates' sidebar item loads SvgTemplateGalleryPage (the main gallery). 'template-marketplace' loads the marketplace variant.
- Campaigns is feature-gated (Feature.CAMPAIGNS) -- if user's plan doesn't have it, shows FeatureUpgradePrompt
- Use screenshotStep(page, '118', 'NN-description') for consistent screenshot naming
- Use loginAndPrepare + navigateToSection pattern from prior phases
- Handle backend-unavailable gracefully (capture fallback screenshots)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates screenshot E2E spec</name>
  <files>tests/e2e/templates-screenshots.spec.js</files>
  <action>
Create tests/e2e/templates-screenshots.spec.js with 8 test cases covering TMPL-01 through TMPL-08.

Structure:
- Import { test, expect } from '@playwright/test'
- Import { loginAndPrepare, navigateToSection, waitForPageReady, dismissAnyModals } from './helpers.js'
- Import { screenshotStep } from './helpers/screenshots.js'
- test.describe('Templates Screenshots') with beforeEach that skips non-chromium and logs in

Test cases:
1. TMPL-01 (gallery browse with categories): Navigate to 'templates' via sidebar. Wait for gallery grid to render (TemplateCard elements or skeleton loaders). Look for filter sidebar with category items. Screenshot the gallery page. If categories visible in sidebar, click one and screenshot filtered view.

2. TMPL-02 (search with debounced results): On templates page, find SearchBar input. Type a search term (e.g., "restaurant"). Wait 500ms for debounce. Screenshot search results (or empty state if no templates exist).

3. TMPL-03 (card hover animation): On templates page, if any template cards exist, hover over the first card. Screenshot the hovered state showing card lift animation. If no cards, screenshot empty state.

4. TMPL-04 (one-click Use Template): If template cards exist, look for a "Use" or "Edit" or "Use Template" button on card hover or click. Click it and screenshot whatever opens (editor or modal). If no templates, screenshot the gallery with note.

5. TMPL-05 (quick customize panel): This corresponds to the TemplateCustomizationWizard or TemplatePreviewPanel in the marketplace. Navigate to 'template-marketplace' via __setCurrentPage. If a template card is present, click to open preview panel. Screenshot the customize panel. Handle gracefully if marketplace has no templates.

6. TMPL-06 (Your Designs tab): On templates gallery page (not marketplace), look for "Your Designs" tab/button. Click it. Screenshot showing the user's designs list (likely empty for test user). Capture empty state.

7. TMPL-07 (portrait/landscape filter): On templates gallery, look for orientation filter toggle (Monitor icon for landscape, Smartphone for portrait). Click the portrait filter. Screenshot the filtered view. If filter not found, screenshot the gallery with available filters.

8. TMPL-08 (industry category expansion): On templates gallery, look for industry categories in the sidebar. Click to expand one. Screenshot the expanded category showing sub-items or filtered results.

Key patterns:
- Use screenshotStep(page, '118', '01-templates-gallery') naming convention
- Handle missing backend gracefully -- capture whatever state the page shows
- Use page.waitForTimeout(500) after search input for debounce
- For card hover, use page.locator('.template-card, [data-testid*="template"]').first().hover()
- Navigate templates gallery via navigateToSection(page, 'templates')
- Navigate marketplace via page.evaluate(() => window.__setCurrentPage('template-marketplace'))
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/templates-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 TMPL test cases pass (or skip gracefully with screenshots) producing screenshot files in screenshots/118/</done>
</task>

<task type="auto">
  <name>Task 2: Verify all template screenshots exist</name>
  <files>screenshots/118/</files>
  <action>
Run the templates screenshot spec and verify that screenshot files are produced in screenshots/118/ directory. List the files to confirm at least 8 screenshots exist (one per test case minimum). If any test failed in Task 1, review the error and fix the spec.

Verification: ls screenshots/118/118-0* | wc -l should show >= 8 files.
  </action>
  <verify>
    <automated>ls /Users/massimodamico/bizscreen/screenshots/118/118-0* 2>/dev/null | wc -l | grep -E '^[8-9]|^[1-9][0-9]'</automated>
  </verify>
  <done>At least 8 screenshot files exist in screenshots/118/ covering TMPL-01 through TMPL-08</done>
</task>

</tasks>

<verification>
- All 8 TMPL requirements have corresponding test cases in templates-screenshots.spec.js
- Tests pass on chromium project without crashes
- Screenshots are saved to screenshots/118/ with consistent naming
- Tests handle backend-unavailable gracefully (no hard failures)
</verification>

<success_criteria>
- templates-screenshots.spec.js contains 8 test cases (TMPL-01 through TMPL-08)
- All tests pass or skip gracefully
- At least 8 screenshot files exist in screenshots/118/
</success_criteria>

<output>
After completion, create `.planning/phases/118-templates-schedules-campaigns-e2e/118-01-SUMMARY.md`
</output>
