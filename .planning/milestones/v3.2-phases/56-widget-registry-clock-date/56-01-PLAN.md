---
phase: 56-widget-registry-clock-date
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/registry.js
  - src/components/layout-editor/LayoutElementRenderer.jsx
  - src/components/layout-editor/LeftSidebar.jsx
  - src/components/layout-editor/LayoutPropertiesPanel.jsx
  - src/components/layout-editor/types.js
  - src/components/scene-editor/EditorCanvas.jsx
  - src/components/scene-editor/LivePreviewWindow.jsx
  - src/components/scene-editor/SceneRenderer.jsx
  - src/components/scene-editor/PropertiesPanel.jsx
  - src/services/sceneDesignService.js
  - src/player/components/widgets/index.js
autonomous: true

must_haves:
  truths:
    - "Adding a new widget type requires registering in ONE place (the registry), not editing 3+ switch statements"
    - "Switching a zone's widget type in the editor resets props to the new type's defaults (no stale props from previous type)"
    - "All existing widget types still render correctly in all rendering paths (editor canvas, live preview, scene renderer, layout element renderer)"
  artifacts:
    - path: "src/widgets/registry.js"
      provides: "Central widget registry with component, icon, label, defaultProps for every widget type"
      exports: ["WIDGET_REGISTRY", "getWidgetComponent", "getWidgetDefaults", "getWidgetTypes"]
    - path: "src/player/components/widgets/index.js"
      provides: "Barrel export including CountdownWidget (currently missing)"
      contains: "CountdownWidget"
  key_links:
    - from: "src/components/scene-editor/SceneRenderer.jsx"
      to: "src/widgets/registry.js"
      via: "getWidgetComponent lookup replaces switch statement"
      pattern: "getWidgetComponent"
    - from: "src/components/layout-editor/LayoutElementRenderer.jsx"
      to: "src/widgets/registry.js"
      via: "getWidgetComponent lookup replaces inline widget implementations"
      pattern: "getWidgetComponent"
    - from: "src/components/scene-editor/PropertiesPanel.jsx"
      to: "src/widgets/registry.js"
      via: "getWidgetDefaults in handleTypeChange for prop reset"
      pattern: "getWidgetDefaults"
    - from: "src/components/layout-editor/types.js"
      to: "src/widgets/registry.js"
      via: "getWidgetDefaults replaces inline defaultProps map"
      pattern: "getWidgetDefaults"
---

<objective>
Create a centralized widget registry and refactor all 9 duplication sites to consume it, eliminating the need to edit multiple switch statements when adding new widget types. Also fix the stale-props bug when switching widget types in the editor.

Purpose: This is the foundational infrastructure for all widget work in v3.2. Without a single registry, every subsequent phase (QR, weather, video, menu board) would need to edit 9 files to add a widget type. The registry makes adding a new widget type a one-line registration.

Output: `src/widgets/registry.js` module consumed by all rendering paths, properties panels, and factory functions.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-widget-registry-clock-date/56-RESEARCH.md

# Key source files to understand current duplication
@src/widgets/registry.js
@src/player/components/widgets/index.js
@src/player/components/widgets/ClockWidget.jsx
@src/player/components/widgets/DateWidget.jsx
@src/player/components/widgets/CountdownWidget.jsx
@src/components/scene-editor/SceneRenderer.jsx
@src/components/scene-editor/EditorCanvas.jsx
@src/components/scene-editor/LivePreviewWindow.jsx
@src/components/scene-editor/PropertiesPanel.jsx
@src/components/layout-editor/LayoutElementRenderer.jsx
@src/components/layout-editor/LeftSidebar.jsx
@src/components/layout-editor/LayoutPropertiesPanel.jsx
@src/components/layout-editor/types.js
@src/services/sceneDesignService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create widget registry and update barrel export</name>
  <files>
    src/widgets/registry.js
    src/player/components/widgets/index.js
  </files>
  <action>
Create `src/widgets/registry.js` that exports:

1. `WIDGET_REGISTRY` -- a plain object mapping widget type keys to `{ component, icon, label, defaultProps }`. Register ALL existing widget types:
   - `clock`: component=ClockWidget, icon=Clock (from lucide-react), label='Clock', defaultProps from current types.js (textColor, format, showSeconds)
   - `date`: component=DateWidget, icon=Calendar, label='Date', defaultProps from current types.js
   - `weather`: component=WeatherWidget, icon=CloudSun, label='Weather', defaultProps from current types.js
   - `qr`: component=QRCodeWidget, icon=QrCode, label='QR Code', defaultProps from current types.js
   - `data-table`: component=DataTableWidget, icon=Table2, label='Data Table', defaultProps: { dataSourceId: '', textColor: '#ffffff', fontSize: 16 }
   - `rss-ticker`: component=RssTickerWidget, icon=Rss, label='RSS Ticker', defaultProps from types.js ticker entry
   - `rss-card`: component=RssCardWidget, icon=Newspaper, label='RSS Card', defaultProps: { feedUrl: '', textColor: '#ffffff', cardCount: 3 }
   - `social-feed`: component=SocialFeedWidget, icon=Share2, label='Social Feed', defaultProps: { platform: '', textColor: '#ffffff' }
   - `countdown`: component=CountdownWidget, icon=Timer, label='Countdown', defaultProps from current types.js
   - `data`: (keep for backward compat) component=DataTableWidget, icon=Table2, label='Data', defaultProps from current types.js data entry

2. `getWidgetComponent(widgetType)` -- returns component or null
3. `getWidgetDefaults(widgetType)` -- returns shallow copy of defaultProps or empty object
4. `getWidgetTypes()` -- returns array of `{ key, icon, label }` for UI selectors (exclude 'data' legacy alias)

Import components from `../../player/components/widgets/` using the barrel export. Import icons from `lucide-react`.

IMPORTANT: The registry must NOT be imported BY widget components (to avoid circular deps). It only imports FROM them. Widget components receive their props from parents, they never look up the registry.

Also update `src/player/components/widgets/index.js` to add the missing `CountdownWidget` export:
```
export { CountdownWidget } from './CountdownWidget.jsx';
```
  </action>
  <verify>
Run `npx eslint src/widgets/registry.js src/player/components/widgets/index.js` -- no errors. Run `npx vite build --mode development 2>&1 | head -30` -- no import resolution errors.
  </verify>
  <done>
`src/widgets/registry.js` exists, exports WIDGET_REGISTRY with 10 entries (9 unique + 1 legacy alias), getWidgetComponent, getWidgetDefaults, getWidgetTypes. CountdownWidget is included in barrel export.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor all 9 duplication sites to consume registry</name>
  <files>
    src/components/scene-editor/SceneRenderer.jsx
    src/components/scene-editor/EditorCanvas.jsx
    src/components/scene-editor/LivePreviewWindow.jsx
    src/components/scene-editor/PropertiesPanel.jsx
    src/components/layout-editor/LayoutElementRenderer.jsx
    src/components/layout-editor/LeftSidebar.jsx
    src/components/layout-editor/LayoutPropertiesPanel.jsx
    src/components/layout-editor/types.js
    src/services/sceneDesignService.js
  </files>
  <action>
Refactor each file to use the registry instead of its own switch/map. For each file:

**SceneRenderer.jsx** (SceneWidgetRenderer):
- Replace the `switch (widgetType)` with: `const WidgetComp = getWidgetComponent(widgetType);` then render `<WidgetComp props={widgetProps} timezone={timezone} />` or fallback to "Unknown widget" div.
- Remove individual widget component imports (they come via registry now).
- Import `getWidgetComponent` from `../../widgets/registry.js`.

**EditorCanvas.jsx**:
- Replace the `WIDGET_ICONS` map with `import { WIDGET_REGISTRY } from '../../widgets/registry.js'` and derive icons from `WIDGET_REGISTRY[widgetType]?.icon`.
- Replace the inline widget rendering `switch` in the widget element rendering section with `const WidgetComp = getWidgetComponent(widgetType)` and render it. For editor canvas, it is acceptable to render the live widget component (the 1s interval is lightweight). If the existing code uses a simple text preview instead, keep that approach but derive the icon/label from registry.
- Remove duplicate widget imports.

**LivePreviewWindow.jsx** (PreviewWidget):
- Replace the `switch (widgetType)` in PreviewWidget with registry lookup: `const WidgetComp = getWidgetComponent(widgetType)`.
- Remove inline clock/date implementations (they were duplicating ClockWidget/DateWidget).
- Pass props through to the actual widget component.

**PropertiesPanel.jsx**:
- Replace the hardcoded `widgetTypes` array with `getWidgetTypes()` from registry.
- In the `handleTypeChange` function (or wherever widget type switching happens), add prop reset: when `widgetType` changes, call `getWidgetDefaults(newType)` and replace the element's props entirely (not merge). This fixes INFRA-02 -- no stale props from previous widget type.
- Keep existing widget-specific controls rendering (CountdownWidgetControls, etc.) -- these are NOT part of the registry pattern since they are editor-specific UI.

**LayoutElementRenderer.jsx** (WidgetElement):
- Replace the `switch (widgetType)` with registry lookup.
- Remove the inline ClockWidget/DateWidget implementations (lines ~108-141 per research) -- use the real components from the registry.
- Pass `timezone` prop through to widget components (it is already available from parent via ZonePlayer).

**LeftSidebar.jsx** (layout editor):
- Replace the hardcoded `WIDGET_ITEMS` array with `getWidgetTypes()` from registry.
- Replace the `getDefaultWidgetProps` function with `getWidgetDefaults` from registry.
- This ensures the layout editor shows the same widget types as the scene editor.

**LayoutPropertiesPanel.jsx**:
- Replace the hardcoded `widgetTypes` array (currently only 4 items) with `getWidgetTypes()` from registry.
- Add prop reset on type change using `getWidgetDefaults(newType)`, same as PropertiesPanel.

**types.js** (createWidgetElement):
- Replace the inline `defaultProps` map (lines 302-310) with `getWidgetDefaults(widgetType)` from registry.
- Keep the rest of the function unchanged (id generation, position defaults, overrides spreading).

**sceneDesignService.js** (createWidgetBlock):
- Replace the inline default props in `createWidgetBlock` with `getWidgetDefaults(widgetType)` from registry.

CRITICAL: After refactoring, every file that previously had widget-type-specific logic should now import from `src/widgets/registry.js` instead of maintaining its own list. The ONLY place widget types are enumerated is the registry.

CRITICAL: Preserve all existing behavior -- same visual output, same widget types available, same controls. This is a pure refactoring task. No new features added here.

CRITICAL: Keep fallback/default cases -- if a widgetType is not in the registry, show an "Unknown widget" placeholder (same as existing switch default cases). Do NOT let unknown types crash.
  </action>
  <verify>
Run `npx eslint src/components/scene-editor/SceneRenderer.jsx src/components/scene-editor/EditorCanvas.jsx src/components/scene-editor/LivePreviewWindow.jsx src/components/scene-editor/PropertiesPanel.jsx src/components/layout-editor/LayoutElementRenderer.jsx src/components/layout-editor/LeftSidebar.jsx src/components/layout-editor/LayoutPropertiesPanel.jsx src/components/layout-editor/types.js src/services/sceneDesignService.js` -- no errors.

Run `npx vite build --mode development 2>&1 | tail -5` -- build succeeds.

Grep to verify no remaining widget switch statements: `grep -rn "switch.*widgetType\|switch.*widget_type" src/components/scene-editor/SceneRenderer.jsx src/components/scene-editor/EditorCanvas.jsx src/components/scene-editor/LivePreviewWindow.jsx src/components/layout-editor/LayoutElementRenderer.jsx` should return NO matches.

Grep to verify registry is consumed: `grep -rn "getWidgetComponent\|getWidgetDefaults\|getWidgetTypes\|WIDGET_REGISTRY" src/components/ src/services/ src/widgets/` should show imports in all 9 files.
  </verify>
  <done>
All 9 duplication sites consume the registry. No file has its own widget type switch/map. Widget type switching in PropertiesPanel and LayoutPropertiesPanel resets props to new type's defaults. Build passes. Existing behavior preserved.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build` completes without errors
2. No widget switch statements remain in rendering paths: grep for `switch.*widgetType` in the 4 renderer files returns nothing
3. Registry is the single source of truth: only `src/widgets/registry.js` enumerates widget types
4. Prop reset works: In PropertiesPanel and LayoutPropertiesPanel, `handleTypeChange` calls `getWidgetDefaults(newType)` and replaces props entirely
5. All existing widgets still render in all paths (EditorCanvas, LivePreview, SceneRenderer, LayoutElementRenderer)
</verification>

<success_criteria>
- INFRA-01 satisfied: Adding a new widget type means adding ONE entry to WIDGET_REGISTRY, not editing 9 files
- INFRA-02 satisfied: Widget type switching resets props to new type's defaults
- Zero regressions: All existing widgets render identically in all rendering paths
- Build succeeds with no ESLint errors
</success_criteria>

<output>
After completion, create `.planning/phases/56-widget-registry-clock-date/56-01-SUMMARY.md`
</output>
