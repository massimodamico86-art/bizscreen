---
phase: 61-portrait-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/147_portrait_mode.sql
  - src/services/screenService.js
  - src/pages/components/ScreensComponents.jsx
  - src/pages/hooks/useScreensData.js
autonomous: true
requirements:
  - PORT-01
  - PORT-04

must_haves:
  truths:
    - "User can set a screen's orientation to landscape or portrait in the EditScreenModal"
    - "Orientation is stored per device in tv_devices.orientation column"
    - "At least 3 portrait layout templates exist in layout_templates table"
    - "get_resolved_player_content RPC returns device orientation in the device object"
    - "get_layout_content RPC returns aspect_ratio in the layout object"
  artifacts:
    - path: "supabase/migrations/147_portrait_mode.sql"
      provides: "orientation column on tv_devices, updated RPCs, portrait template seeds"
      contains: "orientation"
    - path: "src/services/screenService.js"
      provides: "orientation in allowedFields for updateScreen"
      contains: "orientation"
    - path: "src/pages/components/ScreensComponents.jsx"
      provides: "Orientation selector in EditScreenModal"
      contains: "orientation"
  key_links:
    - from: "src/pages/components/ScreensComponents.jsx"
      to: "src/pages/hooks/useScreensData.js"
      via: "onSubmit passes orientation in data object"
      pattern: "orientation"
    - from: "src/pages/hooks/useScreensData.js"
      to: "src/services/screenService.js"
      via: "updateScreen call includes orientation field"
      pattern: "orientation"
---

<objective>
Add screen orientation setting (landscape/portrait) to the database and UI, seed portrait layout templates, and update RPCs to return orientation and aspect_ratio data for player use.

Purpose: Enables per-device orientation storage and provides the data layer needed for player rotation and mismatch warnings in Plan 02.
Output: Migration with orientation column + RPC updates + portrait template seeds, screen service update, EditScreenModal orientation selector.

Note: PORT-02 (portrait canvas in layout editor) is already satisfied by existing code — CreateLayoutModal has a portrait-hd preset (1080x1920), LayoutEditorCanvas handles dynamic canvasSize, and YODECK_ORIENTATIONS includes 9:16 and 3:4 portrait presets. No new tasks required for PORT-02.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-portrait-mode/61-RESEARCH.md
@src/services/screenService.js
@src/pages/components/ScreensComponents.jsx
@src/pages/hooks/useScreensData.js
@supabase/migrations/138_campaign_rotation_weights.sql
@supabase/migrations/086_layout_templates.sql
@supabase/migrations/016_player_content_resolution.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration for orientation column, RPC updates, and portrait template seeds</name>
  <files>supabase/migrations/147_portrait_mode.sql</files>
  <action>
Create migration 147_portrait_mode.sql with four sections:

**Section 1: Add orientation column to tv_devices**
```sql
ALTER TABLE public.tv_devices
ADD COLUMN IF NOT EXISTS orientation TEXT DEFAULT 'landscape'
CHECK (orientation IN ('landscape', 'portrait'));

COMMENT ON COLUMN public.tv_devices.orientation IS 'Physical mounting orientation of the screen';
```

**Section 2: Update get_layout_content to include aspect_ratio**
`CREATE OR REPLACE` the `get_layout_content` function (currently defined in migration 016). The function body is identical to the existing one EXCEPT the final `RETURN jsonb_build_object(...)` adds `'aspect_ratio', COALESCE(v_layout.aspect_ratio, '16:9')` alongside the existing `'id'`, `'name'`, `'zones'` fields. Read migration 016 to get the full current function body, then add the aspect_ratio field.

**Section 3: Update get_resolved_player_content device object**
`CREATE OR REPLACE` the `get_resolved_player_content` function (latest version in migration 138). The ONLY change: in every `'device', jsonb_build_object(...)` block (there are ~8 occurrences), add `'orientation', COALESCE(v_device.orientation, 'landscape')` after the `display_language` field. Read migration 138 to get the full current function body, then add orientation to each device object.

IMPORTANT: Copy the ENTIRE function body from migration 138 exactly, only adding the orientation field. Do NOT modify any other logic. The function is long (~520 lines). Every `'device', jsonb_build_object(` pattern must include orientation.

**Section 4: Seed 3 portrait layout templates**
Insert 3 portrait (9_16, 1080x1920) layout templates into `layout_templates` with `tenant_id = NULL` (global). Use categories appropriate for portrait signage:

1. **"Portrait Info Board"** - category "Information", single full-screen zone, white background. Description: "Full-screen portrait layout for announcements and information displays."
2. **"Portrait Menu Strip"** - category "Restaurant", two stacked horizontal zones (top 60%, bottom 40%), dark background (#1a1a2e). Description: "Vertical split layout ideal for menu items on top with promotions below."
3. **"Portrait Social Wall"** - category "Social Media", three equal horizontal zones stacked vertically (33% each), gradient background (#0f172a). Description: "Triple-zone portrait layout for social feeds, news tickers, or multi-content displays."

Each template should have:
- `orientation = '9_16'`, `width = 1080`, `height = 1920`
- `is_featured = true`, `is_active = true`
- `data` field with `elements` array containing zone definitions using percent-based positioning (matching the layout_zones pattern)
- `thumbnail_url = NULL` (no thumbnails for seeded templates)

Use the layout_templates schema from migration 086 as reference for column names.
  </action>
  <verify>
Run: `grep -c "orientation" supabase/migrations/147_portrait_mode.sql` should return many matches.
Run: `grep "aspect_ratio" supabase/migrations/147_portrait_mode.sql` should find the get_layout_content update.
Run: `grep -c "portrait" supabase/migrations/147_portrait_mode.sql` should find template seeds.
Verify the migration file has all 4 sections.
  </verify>
  <done>Migration file exists with: (1) orientation column on tv_devices, (2) get_layout_content returns aspect_ratio, (3) get_resolved_player_content device object includes orientation in all ~8 occurrences, (4) 3 portrait layout templates seeded.</done>
</task>

<task type="auto">
  <name>Task 2: Screen service and EditScreenModal orientation selector</name>
  <files>
    src/services/screenService.js
    src/pages/components/ScreensComponents.jsx
    src/pages/hooks/useScreensData.js
  </files>
  <action>
**screenService.js:**
Add `'orientation'` to the `allowedFields` array in `updateScreen()` (after `'display_language'`).

**ScreensComponents.jsx - EditScreenModal:**
1. Add state: `const [orientation, setOrientation] = useState(screen?.orientation || 'landscape');`
2. Add to the `useEffect` that syncs state from screen prop: `setOrientation(screen?.orientation || 'landscape');`
3. Add `orientation` to the `handleSubmit` data object (alongside `displayLanguage`).
4. Add a FormField for orientation AFTER the Display Language field:
```jsx
<FormField
  label={
    <Inline gap="xs" align="center">
      <Monitor size={14} className="text-blue-500" />
      <span>Screen Orientation</span>
    </Inline>
  }
  hint="Set to match how the screen is physically mounted"
>
  <Select value={orientation} onChange={(e) => setOrientation(e.target.value)}>
    <option value="landscape">Landscape (horizontal)</option>
    <option value="portrait">Portrait (vertical)</option>
  </Select>
</FormField>
```
Make sure `Monitor` is imported from `lucide-react` (check existing imports -- it may already be imported; if not, add it).

**useScreensData.js - handleUpdateScreen:**
Add `orientation: data.orientation` to the `updateScreen()` call object (after `display_language`).
Also update the optimistic state update in the `setScreens` callback to include: `orientation: data.orientation`.
  </action>
  <verify>
Run: `grep "orientation" src/services/screenService.js` confirms it's in allowedFields.
Run: `grep "orientation" src/pages/components/ScreensComponents.jsx` confirms state and FormField.
Run: `grep "orientation" src/pages/hooks/useScreensData.js` confirms it's wired through handleUpdateScreen.
Run: `npm run lint -- --no-warn-ignored src/services/screenService.js src/pages/components/ScreensComponents.jsx src/pages/hooks/useScreensData.js` passes.
  </verify>
  <done>EditScreenModal shows orientation dropdown (landscape/portrait), selection persists through updateScreen to tv_devices.orientation column, and optimistic UI updates reflect the change.</done>
</task>

</tasks>

<verification>
1. Migration 147 creates orientation column, updates both RPCs, and seeds 3 portrait templates
2. screenService allowedFields includes 'orientation'
3. EditScreenModal has orientation selector with landscape/portrait options
4. handleUpdateScreen passes orientation through to updateScreen
5. ESLint passes on all modified files
</verification>

<success_criteria>
- tv_devices table has orientation column with 'landscape' default and CHECK constraint
- get_resolved_player_content returns orientation in every device object
- get_layout_content returns aspect_ratio in layout object
- 3 portrait layout templates seeded in layout_templates (orientation='9_16', 1080x1920)
- User can select landscape/portrait orientation in EditScreenModal
- Selection persists to database via screenService.updateScreen
</success_criteria>

<output>
After completion, create `.planning/phases/61-portrait-mode/61-01-SUMMARY.md`
</output>
