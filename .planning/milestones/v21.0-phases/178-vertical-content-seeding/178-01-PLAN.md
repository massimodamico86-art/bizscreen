---
phase: 178
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/generateOrientation.test.js
  - tests/integration/approveBulk.test.js
  - tests/integration/rejectBulk.test.js
  - tests/integration/templateDraftsService.bulk.test.js
  - tests/integration/seedTopics.schema.test.js
  - tests/integration/promptLibraryParity.test.js
  - tests/e2e/admin-template-queue.spec.js
  - scripts/verify-178-counts.cjs
  - scripts/validate-templates.cjs
autonomous: true
requirements: [TCAT-01, TCAT-02, TCAT-03, TCAT-04, TVRT-01, TVRT-02, TVRT-03, TVRT-04, TVRT-05]
must_haves:
  truths:
    - "RED tests committed before any production code (Nyquist gate; per RESEARCH §Wave 0 Gaps)"
    - "Every Wave 1+ plan has a failing test that flips GREEN as production lands"
    - "Verification harness scripts exist as RED skeletons proving SC enforcement is wired"
    - "Topic-file schema invariants enforced before seed-script authoring (D-15)"
    - "Bulk-action service contract enforced before EF or UI lands (D-05/D-06)"
    - "Validator-CLI gap closed (RESEARCH Open Q1 — `scripts/validate-templates.cjs` confirmed missing on 2026-05-09)"
  artifacts:
    - path: "tests/unit/generateOrientation.test.js"
      provides: "RED unit test asserting orientation parameter swaps viewBox + appends portrait guidance (BL-178-01)"
    - path: "tests/integration/approveBulk.test.js"
      provides: "RED integration test for approve_bulk handler (BL-178-02)"
    - path: "tests/integration/rejectBulk.test.js"
      provides: "RED integration test for reject_bulk handler (BL-178-03)"
    - path: "tests/integration/templateDraftsService.bulk.test.js"
      provides: "RED integration test for bulkApproveDrafts/bulkRejectDrafts service helpers"
    - path: "tests/integration/seedTopics.schema.test.js"
      provides: "RED integration test enforcing seedTopics.js schema invariants (D-15)"
    - path: "tests/integration/promptLibraryParity.test.js"
      provides: "EXTENDED parity test asserting ≥18 entries + (template_type, vertical) uniqueness + templateTypesPerVertical cross-check (BL-178-04)"
    - path: "tests/e2e/admin-template-queue.spec.js"
      provides: "EXTENDED Playwright spec with bulk-select + filter chips + confirm modal assertions (BL-178-05/06)"
    - path: "scripts/verify-178-counts.cjs"
      provides: "RED verification harness for TVRT-01..04 + TCAT-01..04 SC counts"
    - path: "scripts/validate-templates.cjs"
      provides: "Wrapper around svgValidator with --vertical and --since flags (TCAT-03 closure)"
  key_links:
    - from: "tests/unit/generateOrientation.test.js"
      to: "supabase/functions/generate-svg-template/handlers/generate.ts"
      via: "imports generate(); asserts captured system prompt contents"
      pattern: "viewBox=\"0 0 1080 1920\""
    - from: "tests/integration/approveBulk.test.js"
      to: "supabase/functions/generate-svg-template/handlers/approve_bulk.ts"
      via: "fs.readFileSync source assertion"
      pattern: "import.*\\{\\s*approve\\s*\\}.*from.*\\./approve\\.ts"
    - from: "scripts/verify-178-counts.cjs"
      to: "svg_templates table"
      via: "Supabase service-role client SQL queries"
      pattern: "vertical='restaurants' AND is_active=TRUE"
---

<objective>
Wave 0 Nyquist gate. Commit failing tests + verification harness skeletons BEFORE any Phase 178 production code lands.

Purpose: Every requirement (TVRT-01..05, TCAT-01..04, BL-178-01..06) gets a failing test that flips GREEN as the corresponding plan ships. Closes RESEARCH §Validation Architecture Wave 0 Gaps and Open Q1 (validator-CLI presence verified missing on 2026-05-09).

Output:
- 5 new test files (4 vitest unit/integration + 1 Playwright extension)
- 1 extended parity test (assertions strengthened, deep-equal preserved)
- 2 new CLI scripts (validate-templates wrapper + verify-178-counts harness)
- All 6 RED targets enumerated in 178-VALIDATION.md §Wave 0 Requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/178-vertical-content-seeding/178-CONTEXT.md
@.planning/phases/178-vertical-content-seeding/178-RESEARCH.md
@.planning/phases/178-vertical-content-seeding/178-PATTERNS.md
@.planning/phases/178-vertical-content-seeding/178-VALIDATION.md
@.planning/phases/178-vertical-content-seeding/178-UI-SPEC.md

<interfaces>
<!-- Existing test analogs to copy verbatim. Executor uses these as paste-and-rename templates. -->

From tests/integration/approveAtomicity.test.js (Phase 177 file-source assertion pattern):
```javascript
import { describe, it, expect, vi } from 'vitest';
import fs from 'node:fs';
import path from 'node:path';

describe('Phase 177 gap-closure 177-08 — BL-02 + BL-06 + WR-09 (approve atomicity)', () => {
  it('Test 1: handler source no longer contains BL-02 vulnerable 2-step pattern; rpc("approve_draft_atomic") is the sole mutation site', () => {
    const handlerSrc = fs.readFileSync(
      path.resolve(__dirname, '../../supabase/functions/generate-svg-template/handlers/approve.ts'),
      'utf8'
    );
    expect(handlerSrc).not.toMatch(/from\(["']svg_templates["']\)\.insert\(/);
    expect(handlerSrc).toMatch(/rpc\(["']approve_draft_atomic["']/);
  });
});
```

From tests/unit/generateValidatorOrder.test.js (DI-seam mocked vitest pattern):
```javascript
const mockAnthropic = { messages: { create: async (args) => { /* capture args.system */ } } };
const mockSupa = { from: () => ({ insert: () => ({ select: () => ({ single: async () => ({ data: { id: 'fake-uuid' }, error: null }) }) }) }) };
const mockValidate = vi.fn(() => ({ ok: true, errors: [], warnings: [] }));
await generate({ ... }, mockSupa, { Anthropic: mockAnthropic, validateSvg: mockValidate });
```

From tests/integration/promptLibraryParity.test.js (existing Phase 177 baseline):
```javascript
describe('Phase 177 — prompt library parity (D-08)', () => {
  it('promptLibrary.js and prompts.json are deep-equal by content', () => {
    expect(promptLibrary).toEqual(promptsJson);
  });
  it('contains at least 6 entries (one per template_type)', () => {
    expect(promptLibrary.length).toBeGreaterThanOrEqual(6);
    const types = new Set(promptLibrary.map((p) => p.template_type));
    expect(types).toEqual(new Set(['menu', 'promo', 'announcement', 'reminder', 'wayfinding', 'health_tip']));
  });
});
```

From tests/e2e/admin-template-queue.spec.js (Phase 177 navigation helper):
```javascript
async function gotoAdminTemplateQueue(page) {
  await page.getByRole('heading', { name: /^Super Admin Dashboard$/i }).first().waitFor(...);
  await page.evaluate(() => {
    window.dispatchEvent(new CustomEvent('test:setCurrentPage', { detail: 'admin-template-queue' }));
  });
  await page.getByRole('heading', { name: /^Template Queue$/i }).first().waitFor(...);
}
const SKIP = !process.env.TEST_SUPERADMIN_EMAIL || !process.env.TEST_SUPERADMIN_PASSWORD;
```

From scripts/generate-template-thumbnails.cjs (service-role + dotenv pattern):
```javascript
try { require('dotenv').config({ path: '.env.local', override: true }); } catch {}
try { require('dotenv').config({ path: '.env', override: false }); } catch {}
const { createClient } = require('@supabase/supabase-js');
const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED test scaffolds — unit + integration tests for orientation, bulk handlers, service, topic schema, and parity extension</name>
  <files>tests/unit/generateOrientation.test.js, tests/integration/approveBulk.test.js, tests/integration/rejectBulk.test.js, tests/integration/templateDraftsService.bulk.test.js, tests/integration/seedTopics.schema.test.js, tests/integration/promptLibraryParity.test.js</files>
  <read_first>
    - tests/integration/approveAtomicity.test.js (file-source assertion pattern — verbatim skeleton for approveBulk + rejectBulk)
    - tests/integration/rejectIdempotency.test.js (race-guard assertion analog)
    - tests/unit/generateValidatorOrder.test.js (DI-seam mocked vitest pattern — verbatim skeleton for generateOrientation)
    - tests/integration/promptLibraryParity.test.js (existing Phase 177 deep-equal + ≥6 entries assertion to EXTEND, not replace)
    - supabase/functions/generate-svg-template/handlers/generate.ts (to confirm DI seam shape: `generate(body, supabase, { Anthropic, validateSvg })`)
    - supabase/functions/generate-svg-template/handlers/approve.ts (to confirm export shape `export async function approve(...)` for the import-assertion in approveBulk RED test)
    - supabase/functions/generate-svg-template/handlers/reject.ts (same — for rejectBulk)
    - src/services/aiTemplate/templateDraftsService.js (to confirm error-throwing contract for bulk service tests)
  </read_first>
  <behavior>
    - Test file 1: tests/unit/generateOrientation.test.js asserts that calling `generate({orientation:'portrait',...})` produces a system prompt containing `viewBox="0 0 1080 1920"` and the literal string `PORTRAIT-SPECIFIC GUIDANCE`, and DOES NOT contain `viewBox="0 0 1920 1080"`. A second case (`orientation:'landscape'` or omitted) preserves `viewBox="0 0 1920 1080"` and DOES NOT contain `PORTRAIT-SPECIFIC GUIDANCE`. This file is RED until Plan 03 ships.
    - Test file 2: tests/integration/approveBulk.test.js reads `supabase/functions/generate-svg-template/handlers/approve_bulk.ts` (file-source assertion). Asserts: imports `approve` from `./approve.ts`; defines `BULK_HARD_CAP = 50`; contains `await new Promise(r => setTimeout(r, 300))` between iterations; does NOT contain `Promise.all(`; defines an `interface ApproveBulkBody` with `draftIds: string[]`. RED until Plan 05 ships.
    - Test file 3: tests/integration/rejectBulk.test.js mirrors approveBulk shape against `reject_bulk.ts`. Asserts: imports `reject` from `./reject.ts`; contains 300ms throttle; does NOT contain `Promise.all(`; defines `reason?: string` shared field. RED until Plan 05 ships.
    - Test file 4: tests/integration/templateDraftsService.bulk.test.js mocks `supabase.functions.invoke` and asserts `bulkApproveDrafts(['a','b'])` invokes EF with body `{ action: 'approve_bulk', draftIds: ['a','b'] }`; throws when `error` is truthy. Mirrors for `bulkRejectDrafts(['a'], 'duplicate')`. RED until Plan 06 ships.
    - Test file 5: tests/integration/seedTopics.schema.test.js loads `scripts/seedTopics.js` and asserts: every record has all 11 fields from D-15 (`slug, name, description, tags, topic, palette, vibe, layout, vertical, template_type, orientation`); slugs are unique; `vertical ∈ {restaurants,retail,healthcare}` (NOT `cross-vertical`); `orientation ∈ {landscape,portrait}`; `template_type` for each row is in `templateTypesPerVertical[vertical]`. RED until Plan 07 ships.
    - Test file 6 (EXTENSION): tests/integration/promptLibraryParity.test.js — KEEP existing deep-equal assertion verbatim. ADD: (a) `expect(promptLibrary.length).toBeGreaterThanOrEqual(18)`; (b) every `(template_type, vertical)` combination is unique across the array (no duplicate keys); (c) every entry's `(template_type, vertical)` pair appears in `templateTypesPerVertical[vertical]` (or `templateTypesPerVertical.null` if vertical is null); (d) DELETE the existing exactly-6-types assertion (replace with `>=6` since the type set expands per D-11). Tests (a)-(c) are RED until Plan 04 ships.
  </behavior>
  <action>
    1. Create `tests/unit/generateOrientation.test.js` per RESEARCH §"tests/unit/generateOrientation.test.js (NEW)" lines 884-925. Use the DI-seam pattern from `generateValidatorOrder.test.js` lines 17-67. Mock `Anthropic.messages.create` to capture `args.system`; assert `capturedSystem.includes('viewBox="0 0 1080 1920"')` AND `capturedSystem.includes('PORTRAIT-SPECIFIC GUIDANCE')` AND `!capturedSystem.includes('viewBox="0 0 1920 1080"')` for `orientation:'portrait'`. Second test case asserts the inverse for `orientation:'landscape'`. Both tests will RED-fail because the EF handler doesn't accept the orientation parameter yet.

    2. Create `tests/integration/approveBulk.test.js`. Use the verbatim skeleton from `approveAtomicity.test.js` lines 17-30 (path resolution + fs.readFileSync). Path resolves to `../../supabase/functions/generate-svg-template/handlers/approve_bulk.ts`. Three assertions:
       - `expect(src).toMatch(/import\s+\{\s*approve\s*\}\s+from\s+["']\.\/approve\.ts["']/)`
       - `expect(src).toMatch(/BULK_HARD_CAP\s*=\s*50/)`
       - `expect(src).toMatch(/await\s+new\s+Promise\(\s*r\s*=>\s*setTimeout\(r,\s*300/)`
       - `expect(src).not.toMatch(/Promise\.all\s*\(/)`
       Will RED-fail with `ENOENT` because the handler file doesn't exist yet — wrap fs.readFileSync in `it()` body so the read happens at test-run, not module-load (vitest will report 1 failing test, not crash the suite).

    3. Create `tests/integration/rejectBulk.test.js` mirroring approveBulk. Path `handlers/reject_bulk.ts`. Assertions: imports `reject` from `./reject.ts`; contains 300ms throttle; no `Promise.all(`; `interface RejectBulkBody` includes `reason?: string`. Will RED-fail until Plan 05.

    4. Create `tests/integration/templateDraftsService.bulk.test.js`. Use `vi.mock('@supabase/supabase-js', ...)` to mock `supabase.functions.invoke`. Test 1: `await bulkApproveDrafts(['id1','id2']); expect(invokeSpy).toHaveBeenCalledWith('generate-svg-template', { body: { action: 'approve_bulk', draftIds: ['id1','id2'] } })`. Test 2: when invoke returns `{ data: null, error: new Error('boom') }`, `bulkApproveDrafts` throws. Tests 3+4 mirror for `bulkRejectDrafts(['id1'], 'duplicate')` with `body.action === 'reject_bulk'` and `body.reason === 'duplicate'`. Wrap `import { bulkApproveDrafts, bulkRejectDrafts } from '../../src/services/aiTemplate/templateDraftsService'` inside the test body via dynamic `await import(...)` to defer module-load past file existence. Will RED-fail until Plan 06.

    5. Create `tests/integration/seedTopics.schema.test.js`. Inside the test body, dynamic-import `../../scripts/seedTopics.js` (or its CJS export shape if `module.exports`). Assertions:
       - `Array.isArray(seedTopics)` AND `seedTopics.length > 0`
       - Every entry has all 11 D-15 keys exactly: `['slug','name','description','tags','topic','palette','vibe','layout','vertical','template_type','orientation']`
       - `tags` is `Array<string>` (length ≥1)
       - `slug` collisions: `expect(new Set(slugs).size).toBe(slugs.length)`
       - `vertical` ∈ `['restaurants','retail','healthcare']` (NOT 'cross-vertical')
       - `orientation` ∈ `['landscape','portrait']`
       - `template_type ∈ templateTypesPerVertical[vertical]` (cross-import from `src/services/aiTemplate/promptLibrary.js`)
       Will RED-fail until Plan 07.

    6. EXTEND `tests/integration/promptLibraryParity.test.js`. Read existing file first to preserve the deep-equal assertion verbatim. Then ADD inside the same `describe(...)` block:
       - it('contains at least 18 entries (per-vertical specialization per D-09)', () => { expect(promptLibrary.length).toBeGreaterThanOrEqual(18); });
       - it('every (template_type, vertical) combination is unique', () => { const keys = promptLibrary.map(p => `${p.template_type}|${p.vertical ?? 'null'}`); expect(new Set(keys).size).toBe(keys.length); });
       - it('every (template_type, vertical) pair is in templateTypesPerVertical', () => { for (const p of promptLibrary) { const allowed = templateTypesPerVertical[p.vertical ?? 'null']; expect(allowed).toContain(p.template_type); } });
       Import `templateTypesPerVertical` from `../../src/services/aiTemplate/promptLibrary.js` (will fail to import until Plan 04 — that's the RED state).
       Also REPLACE the existing exact-6-types assertion (`expect(types).toEqual(new Set([...]))`) with a SUPERSET assertion: `for (const t of ['menu','promo','announcement','reminder','wayfinding','health_tip']) expect(types).toContain(t);` — this stays GREEN at 6 entries (Phase 177) and stays GREEN at ≥18 (Phase 178 expansion).

    7. Run `npx vitest run tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js tests/integration/promptLibraryParity.test.js`. ALL except parity's existing-baseline assertions must FAIL. Capture the output; the failure mode for the new files must be ENOENT (handler file missing) or import-load failure (templateTypesPerVertical not exported), NOT syntax errors in the test files themselves. The deep-equal assertion in promptLibraryParity must still PASS at 6 entries.

    8. Threat-mitigation source-grep: assert that the new test files do NOT log secrets — `grep -nE 'console\.log\(.*SUPABASE_SERVICE_ROLE_KEY|console\.log\(.*ANTHROPIC_API_KEY' tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js | grep -v '^#' | wc -l` returns 0.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js tests/integration/promptLibraryParity.test.js 2>&1 | tee /tmp/178-01-task1-vitest.log; grep -E '(FAIL|PASS).*tests/' /tmp/178-01-task1-vitest.log | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/generateOrientation.test.js` exists and contains `viewBox="0 0 1080 1920"` literal: `grep -c 'viewBox="0 0 1080 1920"' tests/unit/generateOrientation.test.js` ≥1
    - File `tests/unit/generateOrientation.test.js` contains `PORTRAIT-SPECIFIC GUIDANCE` literal: `grep -c 'PORTRAIT-SPECIFIC GUIDANCE' tests/unit/generateOrientation.test.js` ≥1
    - File `tests/integration/approveBulk.test.js` exists and asserts no Promise.all: `grep -c 'Promise\.all' tests/integration/approveBulk.test.js` ≥1 (the not.toMatch assertion is the line)
    - File `tests/integration/approveBulk.test.js` asserts BULK_HARD_CAP=50: `grep -c 'BULK_HARD_CAP' tests/integration/approveBulk.test.js` ≥1
    - File `tests/integration/rejectBulk.test.js` exists: `test -f tests/integration/rejectBulk.test.js`
    - File `tests/integration/templateDraftsService.bulk.test.js` exists: `test -f tests/integration/templateDraftsService.bulk.test.js`
    - File `tests/integration/seedTopics.schema.test.js` exists and references all 11 D-15 fields: `grep -cE "'slug'|'name'|'description'|'tags'|'topic'|'palette'|'vibe'|'layout'|'vertical'|'template_type'|'orientation'" tests/integration/seedTopics.schema.test.js` ≥11
    - File `tests/integration/promptLibraryParity.test.js` keeps deep-equal assertion: `grep -c 'expect(promptLibrary)\.toEqual(promptsJson)' tests/integration/promptLibraryParity.test.js` ≥1
    - File `tests/integration/promptLibraryParity.test.js` adds ≥18 entry assertion: `grep -c 'toBeGreaterThanOrEqual(18)' tests/integration/promptLibraryParity.test.js` ≥1
    - File `tests/integration/promptLibraryParity.test.js` references `templateTypesPerVertical`: `grep -c 'templateTypesPerVertical' tests/integration/promptLibraryParity.test.js` ≥1
    - vitest run produces ≥5 FAILing test files (the 5 new RED tests + 3 of the 4 new parity assertions) and 1 PASS (existing deep-equal at 6 entries): `grep -cE 'FAIL.*tests/(unit|integration)/' /tmp/178-01-task1-vitest.log` ≥5
    - No secret literals logged: `grep -cE 'console\.log\(.*(SUPABASE_SERVICE_ROLE_KEY|ANTHROPIC_API_KEY)' tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js | grep -v '^#' | grep -vE ':0$' | wc -l` = 0
  </acceptance_criteria>
  <done>RED test scaffolds for orientation/approve_bulk/reject_bulk/service/seedTopics committed; promptLibraryParity extended (≥18 entries, uniqueness, templateTypesPerVertical cross-check); existing deep-equal preserved; vitest run shows ≥5 FAIL + 1 PASS as expected RED-pre-Wave-1 state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 Playwright E2E spec extension — bulk-select, filter chips, confirm modal (RED until Plan 06)</name>
  <files>tests/e2e/admin-template-queue.spec.js</files>
  <read_first>
    - tests/e2e/admin-template-queue.spec.js (Phase 177 baseline — must NOT regress; preserve existing 4 test cases)
    - .planning/phases/178-vertical-content-seeding/178-UI-SPEC.md §"data-testid Contract" (lines 658-679 — exhaustive test-id list)
    - .planning/phases/178-vertical-content-seeding/178-UI-SPEC.md §"Surface 1-4" (filter chips + bulk toolbar + confirm modal contract)
    - .planning/phases/178-vertical-content-seeding/178-PATTERNS.md §"tests/e2e/admin-template-queue.spec.js (MODIFIED)" (lines 930-961)
  </read_first>
  <behavior>
    - EXTEND existing spec with new `test.describe('Phase 178 — bulk + filter chips')` block. ADD ≥6 test cases hitting the data-testid contract from UI-SPEC §"data-testid Contract":
      - filter-vertical / filter-type / filter-status chip clicks narrow draft list (client-side, instant)
      - checkbox-select-all toggles all visible rows; transitions `indeterminate` on partial deselect
      - bulk-action-toolbar hidden at 0 selected; visible when ≥1
      - btn-bulk-approve click opens bulk-confirm-modal showing first-5 names + `…and N more`
      - btn-bulk-confirm advances to executing phase; bulk-exec-feed populates with ✓/✗ rows
      - Done phase shows btn-bulk-close; click closes modal + refreshes
    - Same skip-guard pattern as Phase 177 baseline: `test.skip(SKIP, 'TEST_SUPERADMIN_EMAIL/PASSWORD not configured')`
    - Spec MUST run (and pass the existing 4 cases) when bulk UI is absent — new cases MUST be marked `test.fixme()` until Plan 06 lands, per RED-without-suite-crash convention. Plan 06 flips fixme → it.
    - Tag new cases with `@phase178` for selective Playwright runs (per VALIDATION.md sampling rate)
  </behavior>
  <action>
    1. Read existing `tests/e2e/admin-template-queue.spec.js` in full. Preserve everything. Note the existing test.describe block name and the navigation helper `gotoAdminTemplateQueue(page)` at lines 59-81.

    2. APPEND a new test.describe block AFTER the existing one:
       ```javascript
       test.describe('Phase 178 — bulk + filter chips @phase178', () => {
         test.skip(SKIP, 'TEST_SUPERADMIN_EMAIL/PASSWORD not configured');
         // 6 test.fixme cases below — flipped to test() in Plan 06
       });
       ```

    3. Add 6 `test.fixme(...)` cases inside the new describe (use `test.fixme` to mark as "expected to fail until implementation" — Playwright treats them as not-yet-implemented and they don't fail the suite). Each case uses the navigation helper, then asserts the relevant data-testid behavior. Concrete cases (mirror UI-SPEC contract verbatim):

       a. `test.fixme('filter chips narrow visible drafts', async ({ page }) => { /* signIn + gotoAdminTemplateQueue; click filter-vertical chip "restaurants"; expect rows showing only data-vertical="restaurants" */ });`

       b. `test.fixme('select-all checkbox transitions checked → indeterminate → unchecked', async ({ page }) => { /* signIn + goto; click checkbox-select-all; expect all checkbox-draft-* checked; click one row off; expect select-all has aria-checked="mixed" or .indeterminate */ });`

       c. `test.fixme('bulk-action-toolbar visibility tied to selection count', async ({ page }) => { /* expect bulk-action-toolbar not visible at 0 selected; click checkbox-draft-{first}; expect toolbar visible with count=1 */ });`

       d. `test.fixme('btn-bulk-approve opens confirm modal with first-5 names and overflow text', async ({ page }) => { /* select 7 rows; click btn-bulk-approve; expect bulk-confirm-modal visible with 5 li elements + paragraph containing "and 2 more" */ });`

       e. `test.fixme('confirm phase → executing phase shows feed with checkmarks', async ({ page }) => { /* trigger approve flow; click btn-bulk-confirm; expect bulk-exec-feed receives ✓ or ✗ rows */ });`

       f. `test.fixme('done phase shows close button which refreshes drafts list', async ({ page }) => { /* wait for done phase; click btn-bulk-close; expect modal hidden + Pending tab refreshed */ });`

    4. Above the new describe block, add a comment header block:
       ```javascript
       // -------------------------------------------------------------------------
       // Phase 178 — bulk + filter chips extension (RED scaffolds; flipped GREEN
       // by Plan 06 once AdminTemplateQueuePage extensions land). data-testid
       // contract sourced from .planning/phases/178-vertical-content-seeding/178-UI-SPEC.md
       // §"data-testid Contract" (lines 658-679).
       // -------------------------------------------------------------------------
       ```

    5. Run `npx playwright test tests/e2e/admin-template-queue.spec.js --reporter=list 2>&1 | tee /tmp/178-01-task2-pw.log`. The existing 4 cases must still PASS or SKIP as they did pre-edit. The 6 new cases must report as "fixme" (not failed, not passed). Verify suite exit code is 0 (or 1 only if existing cases were already failing pre-edit — capture pre-edit baseline by running the suite once before adding new cases, log to `/tmp/178-01-task2-pw-baseline.log`).
  </action>
  <verify>
    <automated>grep -c 'test.fixme' tests/e2e/admin-template-queue.spec.js && grep -cE "'(filter-vertical|filter-type|filter-status|checkbox-select-all|bulk-action-toolbar|btn-bulk-approve|btn-bulk-confirm|bulk-exec-feed|btn-bulk-close|bulk-confirm-modal)'" tests/e2e/admin-template-queue.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/admin-template-queue.spec.js` contains ≥6 `test.fixme` calls in the Phase 178 describe block: `grep -c 'test.fixme' tests/e2e/admin-template-queue.spec.js` ≥6
    - File references all 10 Phase 178 data-testids: `grep -c 'filter-vertical' tests/e2e/admin-template-queue.spec.js` ≥1; same for `filter-type`, `filter-status`, `checkbox-select-all`, `bulk-action-toolbar`, `btn-bulk-approve`, `btn-bulk-confirm`, `bulk-exec-feed`, `btn-bulk-close`, `bulk-confirm-modal`
    - Phase 178 describe tagged `@phase178`: `grep -c '@phase178' tests/e2e/admin-template-queue.spec.js` ≥1
    - Phase 177 baseline tests preserved (existing test/test.describe count is non-decreasing): `grep -cE '^\s*(test|test\.describe)\(' tests/e2e/admin-template-queue.spec.js` ≥ pre-edit baseline + 1 new describe + 6 fixme
    - Comment header block names UI-SPEC: `grep -c "178-UI-SPEC.md" tests/e2e/admin-template-queue.spec.js` ≥1
    - Playwright run exits with code 0 (or matches pre-edit exit code if baseline was non-zero — fixme cases never raise the exit code): `npx playwright test tests/e2e/admin-template-queue.spec.js --reporter=list 2>&1 | tail -5 | grep -E 'passed|fixme|skipped'`
  </acceptance_criteria>
  <done>Playwright spec extended with 6 `test.fixme` Phase 178 scaffolds covering filter chips, select-all, toolbar, confirm modal, executing feed, and done close; data-testid contract from UI-SPEC fully referenced; existing Phase 177 cases preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 verification harnesses — verify-178-counts.cjs (RED) + validate-templates.cjs (validator wrapper, GREEN at write)</name>
  <files>scripts/verify-178-counts.cjs, scripts/validate-templates.cjs</files>
  <read_first>
    - scripts/generate-template-thumbnails.cjs (service-role + dotenv pattern — verbatim env-loading skeleton)
    - scripts/eval-prompt-library.cjs (CLI argv parser pattern + exit-code conventions)
    - src/services/svgValidator.js (validator API: `validateSvg(svgContent, opts) → { ok, errors, warnings }` — used by validate-templates.cjs wrapper)
    - .planning/phases/178-vertical-content-seeding/178-RESEARCH.md §"Verification + Wave Artifacts" (lines 562-588 — SQL queries to run)
    - .planning/phases/178-vertical-content-seeding/178-RESEARCH.md §"Open Q1" (lines 1080-1085 — confirms scripts/validate-templates.cjs is MISSING; Wave 0 must create it)
  </read_first>
  <behavior>
    - `scripts/verify-178-counts.cjs` runs all SC verification SQL via Supabase service-role client. Hard-fails (process.exit(2)) if any SC threshold is missed. Pre-Phase-178-execution invocation will RED-fail (counts below threshold). Post-Phase-178 execution will GREEN-pass.
    - `scripts/validate-templates.cjs` is a thin wrapper over `src/services/svgValidator.js` that loads svg_content rows from svg_templates filtered by `--vertical=<comma-list>` and `--since=<ISO timestamp>`, runs validateSvg per row, and reports aggregate pass/fail. Closes RESEARCH Open Q1 (validator CLI confirmed missing on 2026-05-09).
    - Both scripts MUST NOT log secret values. `console.log` of env vars allowed only via key name, never value.
  </behavior>
  <action>
    1. Create `scripts/verify-178-counts.cjs` (~150 LOC). Pattern:
       - dotenv loading: `try { require('dotenv').config({ path: '.env.local', override: true }); } catch {}` then same for `.env`
       - service-role Supabase client: `const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);`
       - CLI flags: `--exit-on-fail` (default true), `--verbose`. Parse via `process.argv.slice(2)` loop.
       - SC checks (run sequentially; collect pass/fail flags):
         * TVRT-01: `SELECT COUNT(*) FROM svg_templates WHERE vertical='restaurants' AND is_active=TRUE` ≥80; `SELECT COUNT(DISTINCT metadata->>'template_type') FROM ... WHERE vertical='restaurants' ...` ≥8
         * TVRT-02: same shape for `vertical='retail'`
         * TVRT-03: same shape for `vertical='healthcare'`
         * TCAT-01: `SELECT COUNT(*) FROM gallery_templates WHERE is_active=TRUE` ≥427
         * TCAT-02 portrait+landscape on hero types: `SELECT DISTINCT orientation FROM svg_templates WHERE vertical='restaurants' AND is_active=TRUE AND metadata->>'template_type' IN ('menu','daypart_menu')` returns BOTH `'landscape'` AND `'portrait'`. Same for retail (`promo`,`flash_sale`/`menu`) and healthcare (`reminder`,`waiting_room_ambient`).
         * TCAT-04 CHECK present: `SELECT COUNT(*) FROM information_schema.check_constraints WHERE constraint_name='chk_svg_templates_category_enum'` =1
         * TVRT-04 vertical tagging: `SELECT COUNT(*) FROM svg_templates WHERE created_at >= '2026-05-09' AND vertical IS NULL` =0 (no net-new untagged rows)
         * S3 thumbnail audit: `SELECT id, slug, thumbnail FROM svg_templates WHERE created_at >= '2026-05-09' AND vertical IS NOT NULL AND is_active=TRUE` → for each, `await fetch(thumbnail, { method: 'HEAD' })` returns status 200. (Skip if AWS_REGION/AWS bucket env not set; log skip reason; do NOT exit-fail on missing AWS creds — verification is read-only public S3 URL HEADs.)
       - Output: per-check tabular row `[OK]`/`[FAIL]` with actual vs expected counts
       - Final exit: 0 if all pass; 2 if any fail
    2. Use queries verbatim from RESEARCH §"SC verification queries" (lines 567-581). Use Supabase JS client `.rpc('exec_sql', { sql: ... })` if available; otherwise compose individual `supabase.from('svg_templates').select(...)` query builders. Prefer query builders to avoid raw SQL injection surface.
    3. Create `scripts/validate-templates.cjs` (~120 LOC):
       - Same dotenv + service-role client setup
       - CLI flags: `--vertical=<csv|all>` (default `restaurants,retail,healthcare`), `--since=<ISO timestamp>` (default — open-ended; ALL rows in selected verticals)
       - Use `import { validateSvg } from '../src/services/svgValidator.js'` (CJS via dynamic `require` if ESM doesn't load directly — match Phase 175 Plan 02 pattern; if validator is ESM-only, use `(await import('../src/services/svgValidator.js')).validateSvg`)
       - SELECT `id, slug, svg_content` from svg_templates filtered by vertical and created_at since; loop with 50ms throttle (lighter than the 300ms LLM throttle — pure CPU); tally `passed`, `failed`, `failureReasons[]`
       - Write summary report to stdout: total checked / total passed / first-N failures with rule names
       - Exit 0 if all pass; 2 if any fail
    4. Smoke-run `node scripts/verify-178-counts.cjs --verbose 2>&1 | tee /tmp/178-01-task3-verify.log`. EXPECTED: most SCs FAIL pre-Phase-178 because counts below threshold; exit code 2. Capture as RED-state evidence.
    5. Smoke-run `node scripts/validate-templates.cjs --vertical=restaurants 2>&1 | tee /tmp/178-01-task3-validate.log`. EXPECTED: existing-127 already-validated rows; PASS or 0-row scope (if no existing rows have vertical=restaurants pre-Plan-02). Either way, exit code 0 or 2 with a clear log line.
    6. Threat-mitigation source-grep: confirm scripts do not log secrets. `grep -nE 'console\.log\(.*(SUPABASE_SERVICE_ROLE_KEY|process\.env\.SUPABASE_SERVICE_ROLE_KEY)' scripts/verify-178-counts.cjs scripts/validate-templates.cjs | grep -v '^#' | wc -l` returns 0.
  </action>
  <verify>
    <automated>node -c scripts/verify-178-counts.cjs && node -c scripts/validate-templates.cjs && grep -c "vertical='restaurants'" scripts/verify-178-counts.cjs && grep -c "validateSvg" scripts/validate-templates.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/verify-178-counts.cjs` exists and parses: `node -c scripts/verify-178-counts.cjs` exits 0
    - File `scripts/verify-178-counts.cjs` references all 9 phase requirement IDs as comments or labels: `grep -cE 'TVRT-0[1-4]|TCAT-0[1-4]' scripts/verify-178-counts.cjs` ≥8
    - File `scripts/validate-templates.cjs` exists and parses: `node -c scripts/validate-templates.cjs` exits 0
    - File `scripts/validate-templates.cjs` calls validateSvg: `grep -c 'validateSvg' scripts/validate-templates.cjs` ≥1
    - Both scripts use the dotenv pattern: `grep -c "dotenv.*config" scripts/verify-178-counts.cjs scripts/validate-templates.cjs | grep -v ':0' | wc -l` ≥2 (one match per file)
    - Both scripts use service-role client (env var name only, no value): `grep -c 'SUPABASE_SERVICE_ROLE_KEY' scripts/verify-178-counts.cjs scripts/validate-templates.cjs | grep -v ':0' | wc -l` ≥2
    - Neither script logs secret values: `grep -cE "console\.log\(.*(process\.env\.SUPABASE_SERVICE_ROLE_KEY|process\.env\.ANTHROPIC_API_KEY)" scripts/verify-178-counts.cjs scripts/validate-templates.cjs | grep -v '^#' | grep -vE ':0$' | wc -l` =0
    - verify-178-counts.cjs exits with code 2 (RED-state pre-Phase-178): `node scripts/verify-178-counts.cjs 2>&1 >/tmp/v.log; echo "exit=$?"` shows `exit=2` (or `exit=0` only if Phase 178 already complete — accept both at Wave 0 time)
  </acceptance_criteria>
  <done>verify-178-counts.cjs runs all 9 SC checks via Supabase service-role client, exits 2 in RED-state pre-Phase-178; validate-templates.cjs wraps svgValidator with --vertical and --since flags closing RESEARCH Open Q1; neither script logs secret values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-runner → repo files | Tests read source files via fs.readFileSync — untrusted file content if repo is poisoned |
| CLI scripts → Supabase service-role | Scripts hold service-role key (full DB access); never logged or echoed |
| validate-templates.cjs → svg_templates.svg_content | Reads SVG strings from DB; passes to validateSvg (which may run DOMPurify); no execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-01-01 | Information disclosure | scripts/verify-178-counts.cjs | mitigate | grep gate: `grep -c "console\.log\(.*process\.env\.SUPABASE_SERVICE_ROLE_KEY" scripts/verify-178-counts.cjs` =0 — secrets are read by name only, never logged |
| T-178-01-02 | Information disclosure | scripts/validate-templates.cjs | mitigate | Same grep gate as T-178-01-01 |
| T-178-01-03 | Tampering | tests/integration/seedTopics.schema.test.js | mitigate | Schema test enforces template_type ∈ templateTypesPerVertical[vertical] AT TEST-TIME — prevents wrong-vertical bleed (RESEARCH §Security Domain row 7) before Wave 4 seed runs |
| T-178-01-04 | Spoofing | tests/integration/promptLibraryParity.test.js | mitigate | Uniqueness assertion on (template_type, vertical) prevents accidental duplicate keys that would silently override prior promptLibrary entries |
| T-178-01-05 | DoS | scripts/validate-templates.cjs | accept | Validator runs in-process per row (no Anthropic/S3 fan-out); 50ms throttle is conservative; no rate-limit risk |
</threat_model>

<verification>
- All 6 RED test files exist and produce expected RED-state output via `npx vitest run`
- Playwright spec extension does not regress Phase 177 baseline (exit code 0 or matches pre-edit code; new fixme cases never raise exit code)
- verify-178-counts.cjs exits 2 (RED-state) in pre-Phase-178 environment
- validate-templates.cjs runs with no errors against existing-127 (which were already validated by Phase 175)
- No secret values logged from any new script: grep gate returns 0
</verification>

<success_criteria>
- 6 RED test files committed (5 new + 1 extended): generateOrientation, approveBulk, rejectBulk, templateDraftsService.bulk, seedTopics.schema, promptLibraryParity
- 1 Playwright spec extended with 6 test.fixme Phase 178 cases tagged @phase178
- 2 verification scripts committed (verify-178-counts.cjs RED, validate-templates.cjs functional)
- Wave 0 frozen — no production code lands until these RED tests are in place
- VALIDATION.md §Wave 0 Requirements all 6 items addressed
</success_criteria>

<output>
After completion, create `.planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md` documenting:
- Pre-edit Playwright baseline exit code + post-edit exit code (must be unchanged)
- Pre-edit vitest baseline file count + post-edit count
- verify-178-counts.cjs exit code in RED state
- validate-templates.cjs exit code on existing-127 validation
- Any deviations from RESEARCH/UI-SPEC contract (none expected)
</output>
