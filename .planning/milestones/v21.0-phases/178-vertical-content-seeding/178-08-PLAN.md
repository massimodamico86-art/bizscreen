---
phase: 178
plan: 08
type: execute
wave: 5
depends_on: [178-02, 178-06, 178-07]
files_modified:
  - .planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md
  - .planning/phases/178-vertical-content-seeding/178-WAVE-retail-RUN.md
  - .planning/phases/178-vertical-content-seeding/178-WAVE-healthcare-RUN.md
  - .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [TCAT-01, TCAT-02, TCAT-03, TCAT-04, TVRT-01, TVRT-02, TVRT-03, TVRT-04, TVRT-05]
notes: |
  Plan 08 contains 6 tasks: 3 checkpoint:human-verify (Tasks 1-3 — per-vertical wave runs)
  + 3 auto (Tasks 4-6 — verification + VERIFICATION.md + ROADMAP closure). The 6-task
  scope-sanity warning from the plan checker is acceptable here because checkpoints do
  NOT share agent context with auto tasks: each checkpoint pauses and surfaces to the
  user, who confirms before the next checkpoint runs. The 3 auto tasks (4-6) run
  sequentially after all 3 wave checkpoints complete and consume context for verification
  + documentation only. Effective context-budget split: ~30% per wave checkpoint
  (operator drives the heavy lift) + ~50% across the 3 auto closure tasks. No single
  agent invocation processes more than 2 tasks at once.
must_haves:
  truths:
    - "Wave 1 (Restaurants) produces ~120 drafts (D-04 1.5× over-generation); admin reviews + bulk-approves ≥75% (≥80 published net-new); 178-WAVE-restaurants-RUN.md committed"
    - "Wave 2 (Retail) produces ~120 drafts; same admin review + bulk-approve flow; ≥80 published net-new; 178-WAVE-retail-RUN.md committed"
    - "Wave 3 (Healthcare) produces ~120 drafts; same flow; ≥80 published net-new; 178-WAVE-healthcare-RUN.md committed"
    - "Net-new live rows per vertical floor: ≥80 net-new (post-cull) per Restaurants/Retail/Healthcare per ROADMAP SC-1..3"
    - "Existing-127 backfill (Plan 02) + Wave-1/2/3 net-new approvals together exceed TVRT-01..03 thresholds: ≥80 active templates per vertical, ≥8 distinct template_types per vertical"
    - "TCAT-01: gallery_templates COUNT(*) WHERE is_active=TRUE returns ≥427 (existing 127 + ≥300 net-new)"
    - "TCAT-02: hero types per vertical have BOTH portrait and landscape live (verified by SELECT DISTINCT orientation per hero type per vertical)"
    - "TCAT-03: scripts/validate-templates.cjs --vertical=restaurants,retail,healthcare --since=<post-Plan-02-timestamp> reports zero validator failures across all net-new content"
    - "TCAT-03: every net-new svg_templates row has S3 thumbnail URL — HEAD probe returns 200 for each"
    - "TCAT-04: chk_svg_templates_category_enum CHECK constraint untouched (information_schema query returns 1 row); gallery_templates VIEW unchanged"
    - "TVRT-04: every net-new template carries a vertical tag (no NULL vertical for created_at >= Plan 02 timestamp)"
    - "TVRT-05: Playwright tests/e2e/template-gallery.spec.js + tests/e2e/template-gallery-100.spec.js pass at ≥90% green (no regression in v20.0 category filter, sort, search, URL-sync)"
    - "scripts/verify-178-counts.cjs (Plan 01) exits 0 — all 9 SC checks GREEN"
    - "178-VERIFICATION.md authored with full evidence trail (every requirement ID, every SC, every threat-register disposition closed)"
    - "ROADMAP.md Phase 178 marked [x] complete; v21.0 progress table updated"
  artifacts:
    - path: ".planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md"
      provides: "Wave 1 run artifact (timestamps, draft IDs, validator pass rate, cost, qualitative review notes)"
    - path: ".planning/phases/178-vertical-content-seeding/178-WAVE-retail-RUN.md"
      provides: "Wave 2 run artifact"
    - path: ".planning/phases/178-vertical-content-seeding/178-WAVE-healthcare-RUN.md"
      provides: "Wave 3 run artifact"
    - path: ".planning/phases/178-vertical-content-seeding/178-VERIFICATION.md"
      provides: "Phase-close verification trail (mirrors 177-VERIFICATION.md format)"
      min_lines: 200
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 178 row marked [x] complete with completion date"
  key_links:
    - from: "scripts/seed-vertical-templates.cjs (Plan 07)"
      to: "EF action=generate via supabase.functions.invoke"
      via: "300ms serial throttle outer loop"
      pattern: "supabase\\.functions\\.invoke\\('generate-svg-template'"
    - from: "AdminTemplateQueuePage bulk-approve (Plan 06)"
      to: "EF action=approve_bulk (Plan 05)"
      via: "templateDraftsService.bulkApproveDrafts"
      pattern: "bulkApproveDrafts"
    - from: "178-VERIFICATION.md"
      to: "All Phase 178 requirement IDs (TVRT-01..05, TCAT-01..04)"
      via: "row-by-row evidence table"
      pattern: "(TVRT-0[1-5]|TCAT-0[1-4]).*PASS"
---

<objective>
Final phase wave: execute Wave 1 (Restaurants), Wave 2 (Retail), Wave 3 (Healthcare) — each runs the seed script (≥120 attempts/vertical per Plan 07 revised D-04 1.5× over-generation), admin reviews+culls drafts via the bulk-approve UI (Plan 06), and commits a per-wave run artifact. After all three waves, run the verification harness (Plan 01) + validator script + Playwright regression to confirm all 9 SCs (TVRT-01..05, TCAT-01..04) pass. Author 178-VERIFICATION.md mirroring 177-VERIFICATION.md format. Update ROADMAP.md to mark Phase 178 complete.

Purpose: This is THE content phase — 3 sequential operator-driven waves produce the ≥240 net-new approved templates (≥80 per vertical post-cull from ~120 attempts) that close the v21.0 milestone goal. Plans 02-07 are infrastructure; Plan 08 is execution + sign-off.

Output:
- 3 wave artifacts (178-WAVE-{vertical}-RUN.md)
- 178-VERIFICATION.md
- ROADMAP.md updated
- ~$14.40 expected total Anthropic spend (~360 attempts × $0.04/attempt + retry overhead); hard cap $20 per wave
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/178-vertical-content-seeding/178-CONTEXT.md
@.planning/phases/178-vertical-content-seeding/178-RESEARCH.md
@.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VERIFICATION.md

<interfaces>
<!-- Wave artifact format -->
Each wave artifact (~150-300 LOC) follows the format produced by scripts/seed-vertical-templates.cjs:
```markdown
# Wave: {vertical} — Run Log

**Run:** <ISO timestamp>
**Attempts:** <N>
**Validator pass rate:** <%>
**Total cost (estimated):** $<X.XX>
**Time elapsed:** <min>

## Per-attempt detail

| # | Slug | template_type | orientation | draftId | status | attempts | cost_est | error |
| ... |

## Qualitative review notes

- (Operator: review the resulting drafts in AdminTemplateQueuePage Pending tab. Filter by vertical=...
```

<!-- 178-VERIFICATION.md skeleton -->
Section structure:
1. Phase summary (timeline, plan execution, headline metrics)
2. Requirement-by-requirement evidence (TVRT-01..05, TCAT-01..04)
3. Success criteria evidence (5 SCs from ROADMAP §Phase 178)
4. Threat register dispositions (T-178-XX-NN from each plan's threat_model)
5. Recommended follow-ups for Phase 179

<!-- ROADMAP.md update target -->
From .planning/ROADMAP.md L39:
- [ ] **Phase 178: Vertical Content Seeding** - ≥300 net-new templates...

becomes:
- [x] **Phase 178: Vertical Content Seeding** - {N} net-new templates published... (closed YYYY-MM-DD; 8 plans; ${cost} Anthropic spend; verifier evidence in 178-VERIFICATION.md)

Progress table:
| 178. Vertical Content Seeding | 8/8 | Complete | YYYY-MM-DD |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Wave 1 — Restaurants seed run + admin review + bulk-approve session</name>
  <files>.planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md</files>
  <read_first>
    - scripts/seed-vertical-templates.cjs (Plan 07 Task 2)
    - scripts/seedTopics.js (Plan 07 Task 1 — ≥120 Restaurants topics queued per D-04)
    - .planning/phases/178-vertical-content-seeding/178-CONTEXT.md L50 (D-03 — vertical-by-vertical waves; per-wave course-correction)
    - .planning/phases/178-vertical-content-seeding/178-CONTEXT.md L52 (D-04 — 1.5× over-generation; ~120 attempts per vertical)
    - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VERIFICATION.md
  </read_first>
  <what-built>
    Run the Restaurants wave (~124 attempts per Plan 07 revised distribution; expected validator pass rate ≥66% ≈ ~82 pending drafts; admin culls weak/duplicate drafts via bulk-reject; admin bulk-approves the keepers). Net-new approve target ≥80 (post-cull) to satisfy TVRT-01.
  </what-built>
  <how-to-verify>
    Claude executes the seed run autonomously, then surfaces the artifact + draft queue state for human-driven admin review.

    1. **Pre-conditions:** All earlier plans GREEN (Plans 01-07). Plan 07 Task 3 smoke approved.

    2. **Run Wave 1:**
       ```bash
       node scripts/seed-vertical-templates.cjs --vertical=restaurants --verbose 2>&1 | tee /tmp/178-08-task1-wave1.log
       ```
       Expected: ~124 EF calls × ~700ms = ~87s wall + 300ms throttle × 123 gaps = ~37s wall total ≈ ~125s wave run. Cost ~$4.96 (~124 × $0.04). Wave artifact written to `.planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md`. Exit code 0 (or 2 if cost-hard cap of $20 hit).

    3. **Capture wave-script result counts** from artifact header.

    4. **Trigger admin review:** Surface to user with action prompt:
       > "Wave 1 (Restaurants) seed complete. ACTION REQUIRED:
       >  1. Open AdminTemplateQueuePage → Pending tab; filter chips: vertical='Restaurants', status='Pending'
       >  2. Visually review the ~Y drafts. Use bulk-reject for visually weak/duplicate drafts; aim to keep ≥80% of pending drafts.
       >  3. Bulk-approve the keepers via the confirm modal (REAL chunking — selections >50 produce sequential 50-sized chunks per Plan 06 revised).
       >  4. Optional: edit promptLibrary.js entries between waves if Wave 1 quality is weak (D-03 wave-tuning).
       >  After admin review + bulk-approve completes, append qualitative review notes to 178-WAVE-restaurants-RUN.md."

    5. **Operator manually:** Reviews drafts. Bulk-rejects ≤30%. Bulk-approves keepers. Commits artifact + any prompt-library tweaks.

    6. **Post-review verification by Claude (after operator confirms via "approved"):**
       ```sql
       SELECT COUNT(*) AS net_new_restaurants
       FROM svg_templates
       WHERE vertical='restaurants' AND is_active=TRUE
         AND created_at >= '<Plan 02 apply timestamp>';
       -- Expect ≥80 net-new (TVRT-01 SC floor)

       SELECT DISTINCT metadata->>'template_type' AS tt
       FROM svg_templates
       WHERE vertical='restaurants' AND is_active=TRUE
         AND created_at >= '<Plan 02 apply timestamp>';
       -- Expect ≥6 distinct net-new types (combined with existing-127 backfill, ≥8 per TVRT-01)
       ```

    7. **Wave artifact final state:** verify file ends with operator-supplied qualitative review notes.

    8. **Commit wave artifact** + any promptLibrary tweaks.

    Type "approved" if Wave 1 produced ≥80 net-new approved restaurants templates, validator pass rate within expected range, admin review notes captured, and artifact committed.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2 (Retail), or describe specific issues if Wave 1 quality is below threshold.</resume-signal>
  <acceptance_criteria>
    - Seed script Wave 1 exit code 0 (or 2 only if cost-cap hit before reaching ≥80 attempts — flag for operator)
    - 178-WAVE-restaurants-RUN.md exists with all required sections (header, per-attempt table, qualitative notes)
    - Wave artifact has ≥1 row in per-attempt table per topic-file Restaurants record (i.e., ≥120 rows): `grep -c '^| [0-9]' .planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md` ≥120
    - Validator pass rate ≥60% (Phase 177 baseline; per Plan 07 smoke at ≥66%)
    - Post-review SQL probe confirms ≥80 net-new active restaurants templates: captured in SUMMARY.md (the SC floor for TVRT-01)
    - ≥6 distinct net-new template_types for restaurants: captured
    - Operator qualitative review notes appended to artifact
    - Artifact committed to git
    - User confirms Wave 1 outcome
  </acceptance_criteria>
  <done>Wave 1 (Restaurants) complete: ≥80 net-new approved templates; validator pass rate ≥60%; 178-WAVE-restaurants-RUN.md committed with operator notes; user approves before Wave 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Wave 2 — Retail seed run + admin review + bulk-approve session</name>
  <files>.planning/phases/178-vertical-content-seeding/178-WAVE-retail-RUN.md</files>
  <read_first>
    - .planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md (Task 1 output — apply Wave 1 lessons)
  </read_first>
  <what-built>
    Run the Retail wave (~120 attempts per Plan 07 revised distribution; same flow as Wave 1).
  </what-built>
  <how-to-verify>
    Same procedure as Task 1 with `--vertical=retail`. Expected ~$4.80 spend.

    1. Run: `node scripts/seed-vertical-templates.cjs --vertical=retail --verbose 2>&1 | tee /tmp/178-08-task2-wave2.log`
    2. Wave artifact written to `.planning/phases/178-vertical-content-seeding/178-WAVE-retail-RUN.md`
    3. Surface to user with same review prompt
    4. Operator review + bulk-reject + bulk-approve via AdminTemplateQueuePage
    5. Post-review SQL probe (≥80 net-new active retail; ≥8 distinct types when combined with existing-127 backfill)
    6. Commit artifact

    Type "approved" if Wave 2 produced ≥80 net-new approved retail templates with ≥6 distinct net-new template_types.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 3 (Healthcare), or describe specific issues.</resume-signal>
  <acceptance_criteria>
    - Same shape as Task 1, vertical=retail
    - ≥80 net-new active retail templates (TVRT-02 SC floor)
    - ≥6 distinct net-new template_types for retail (Plan-04-revised mapping has ≥8 retail types; the wave should hit ≥6 net-new types after cull)
    - Combined with existing-127 backfill, total ≥8 distinct types per TVRT-02 verified in Plan 08 Task 4
    - Wave artifact committed
    - User approves
  </acceptance_criteria>
  <done>Wave 2 (Retail) complete: ≥80 net-new approved templates; artifact committed; user approves before Wave 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Wave 3 — Healthcare seed run + admin review + bulk-approve session</name>
  <files>.planning/phases/178-vertical-content-seeding/178-WAVE-healthcare-RUN.md</files>
  <read_first>
    - .planning/phases/178-vertical-content-seeding/178-WAVE-retail-RUN.md (Task 2 output)
  </read_first>
  <what-built>
    Run the Healthcare wave (~120 attempts per Plan 07 revised distribution).
  </what-built>
  <how-to-verify>
    Same procedure with `--vertical=healthcare`. Expected ~$4.80 spend.

    1. Run: `node scripts/seed-vertical-templates.cjs --vertical=healthcare --verbose 2>&1 | tee /tmp/178-08-task3-wave3.log`
    2. Wave artifact at `.planning/phases/178-vertical-content-seeding/178-WAVE-healthcare-RUN.md`
    3. Operator review focuses on: waiting_room_ambient (calm), emergency_alert (high-contrast), reminder (calm clinic-blue palette adherence)
    4. Operator bulk-reject + bulk-approve
    5. Post-review SQL probe
    6. Commit artifact

    Type "approved" if Wave 3 produced ≥80 net-new approved healthcare templates with ≥7 distinct net-new types.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 4 (verification), or describe specific issues.</resume-signal>
  <acceptance_criteria>
    - Same shape as Task 1/2, vertical=healthcare
    - ≥80 net-new active healthcare templates (TVRT-03 SC floor)
    - ≥7 distinct net-new template_types for healthcare
    - Both orientations present for waiting_room_ambient AND reminder (TCAT-02 hero-type pairing)
    - Wave artifact committed
    - User approves
  </acceptance_criteria>
  <done>Wave 3 (Healthcare) complete: ≥80 net-new approved templates with hero-type both-orientation coverage; artifact committed; user approves before Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Run full SC verification — verify-178-counts.cjs + validate-templates.cjs + Playwright regression sweep</name>
  <files>(read-only invocations; updates 178-VERIFICATION.md draft sections)</files>
  <read_first>
    - scripts/verify-178-counts.cjs (Plan 01 Task 3)
    - scripts/validate-templates.cjs (Plan 01 Task 3)
    - tests/e2e/template-gallery.spec.js (existing v20.0 spec — TVRT-05 regression target)
    - tests/e2e/template-gallery-100.spec.js (companion spec)
    - .planning/phases/178-vertical-content-seeding/178-WAVE-restaurants-RUN.md (Wave 1 output)
    - .planning/phases/178-vertical-content-seeding/178-WAVE-retail-RUN.md (Wave 2 output)
    - .planning/phases/178-vertical-content-seeding/178-WAVE-healthcare-RUN.md (Wave 3 output)
    - /tmp/178-pre-seed-count.txt + /tmp/178-02-task4-postbackfill-counts.txt (Plan 02 baselines for diff)
  </read_first>
  <action>
    1. **Run verify-178-counts.cjs:**
       ```bash
       node scripts/verify-178-counts.cjs --verbose 2>&1 | tee /tmp/178-08-task4-verify.log
       echo "exit=$?"
       ```
       Expected: exit 0; all 9 SC checks GREEN (TVRT-01..04 + TCAT-01..04). TVRT-05 is a Playwright run.

    2. **Run validate-templates.cjs:** (TCAT-03 closure)
       ```bash
       node scripts/validate-templates.cjs --vertical=restaurants,retail,healthcare --since=<Plan 02 apply timestamp ISO> 2>&1 | tee /tmp/178-08-task4-validator.log
       echo "exit=$?"
       ```
       Expected: exit 0; "All <N> templates passed validateSvg" — zero failures.

    3. **S3 thumbnail audit:** Captured by verify-178-counts.cjs.

    4. **Playwright regression sweep — TVRT-05:**
       ```bash
       npx playwright test tests/e2e/template-gallery.spec.js tests/e2e/template-gallery-100.spec.js --reporter=list 2>&1 | tee /tmp/178-08-task4-pw.log
       ```
       Expected: ≥90% of cases pass.

    5. **Playwright admin queue regression — TADM and Phase 178 cases:**
       ```bash
       npx playwright test tests/e2e/admin-template-queue.spec.js --reporter=list 2>&1 | tee /tmp/178-08-task4-pw-admin.log
       ```

    6. **Diff against Plan 02 baselines:** Compare `total_active` post-Wave-3 to `/tmp/178-pre-seed-count.txt` (127 baseline) and `/tmp/178-02-task4-postbackfill-counts.txt` (127 post-backfill, vertical-tagged but no content add). The delta = net-new approved templates (target ≥240, ≥80 per vertical).

    7. **Capture all evidence in 178-08-SUMMARY.md.**

    8. **If ANY SC fails:** halt and surface to user; the failing SC must be addressed before Plan 08 Task 5 + Task 6 can proceed.
  </action>
  <verify>
    <automated>node scripts/verify-178-counts.cjs 2>&1 | tee /tmp/178-08-task4-verify.log; echo "verify_exit=$?"; node scripts/validate-templates.cjs --vertical=restaurants,retail,healthcare 2>&1 | tee /tmp/178-08-task4-validator.log; echo "validate_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - verify-178-counts.cjs exits 0: `node scripts/verify-178-counts.cjs; echo $?` =0
    - All 9 SC checks GREEN in verify-178-counts.cjs output: `grep -cE 'TVRT-0[1-4].*\[OK\]|TCAT-0[1-4].*\[OK\]' /tmp/178-08-task4-verify.log` ≥8
    - validate-templates.cjs exits 0: `node scripts/validate-templates.cjs --vertical=restaurants,retail,healthcare; echo $?` =0
    - validate-templates.cjs reports zero failures: `grep -cE '0 failures|all.*passed|All.*passed' /tmp/178-08-task4-validator.log` ≥1
    - S3 thumbnail audit reports OK: `grep -cE 'S3.*OK|thumbnail.*200' /tmp/178-08-task4-verify.log` ≥1
    - Net-new diff against Plan 02 baseline shows ≥240 (≥80 per vertical post-cull): captured by comparing pre/post-backfill files vs post-Wave-3 totals
    - Playwright tests/e2e/template-gallery*.spec.js: ≥90% green (or all skipped if test infra unavailable — document)
    - Playwright admin-template-queue.spec.js Phase 178 cases pass or skip: `grep -c 'failed' /tmp/178-08-task4-pw-admin.log` =0 (or only previously-known flakes)
    - All evidence captured in /tmp logs for downstream Task 5
  </acceptance_criteria>
  <done>verify-178-counts.cjs all 9 SCs GREEN; validator reports 0 failures; S3 audit OK; Playwright regression sweeps pass; net-new diff vs Plan 02 baseline ≥240; full evidence captured for VERIFICATION.md authoring.</done>
</task>

<task type="auto">
  <name>Task 5: Author 178-VERIFICATION.md — full evidence trail mirroring 177-VERIFICATION.md format</name>
  <files>.planning/phases/178-vertical-content-seeding/178-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VERIFICATION.md (full file — verbatim format precedent)
    - All 3 wave artifacts (Tasks 1-3 outputs)
    - /tmp/178-08-task4-verify.log + /tmp/178-08-task4-validator.log + /tmp/178-08-task4-pw.log + /tmp/178-08-task4-pw-admin.log
    - All 8 PLAN.md files (Plans 01-08; threat-register entries to consolidate)
  </read_first>
  <action>
    1. Create `.planning/phases/178-vertical-content-seeding/178-VERIFICATION.md`. Header includes:
       - Phase 178 (v21.0 Templates at Scale)
       - Closed: YYYY-MM-DD
       - Plans executed: 8/8
       - Spend: ~$<X.XX> Anthropic (3 waves × ~$4.80 + ~$0.12 smoke)

    2. **Headline Metrics** table (pre/post/delta) covering: gallery_templates active count, vertical-tagged total, per-vertical totals, distinct types per vertical, validator failures (0), S3 thumbnails, Playwright pass rate.

    3. Per-requirement evidence section — one ##### subsection per TVRT-01..05 + TCAT-01..04, each with SQL probe / command / file-path evidence and PASS verdict. Mirror 177-VERIFICATION.md format.

    4. Success criteria evidence (5 SCs from ROADMAP §Phase 178 — collated from requirement evidence above).

    5. Threat register dispositions (consolidate from all 8 plans' threat_model blocks — Plans 01-07 each contribute rows; Plan 08 contributes T-178-08-01..05).

    6. Recommended follow-ups for Phase 179 + v21.x: queue virtualization, cost-cap-per-admin, TCAT-F2 vertical filter chip in v22.0, two-phase generation (rejected in D-15), wave-tuning lessons from Wave 1/2/3 promptLibrary tweaks.

    7. Final closing sign-off line.

    8. Commit:
       ```bash
       git add .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md
       git commit -m "docs(178): VERIFICATION trail — all 9 requirements PASS, threat register closed"
       ```
  </action>
  <verify>
    <automated>test -f .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md && grep -cE 'TVRT-0[1-5].*PASS|TCAT-0[1-4].*PASS' .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md`
    - Contains all 9 requirement verdicts: `grep -cE 'TVRT-0[1-5].*PASS|TCAT-0[1-4].*PASS' .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md` ≥9
    - Headline metrics table present: `grep -c '^| Metric' .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md` ≥1
    - SQL probe blocks present (≥9 — one per requirement): `grep -c '```sql' .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md` ≥9
    - Threat register section present with rows from all 8 plans: `grep -cE 'T-178-(0[1-8])' .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md` ≥7
    - Follow-ups section present: `grep -cE '## Recommended Follow-Ups' .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md` ≥1
    - File committed to git: `git log -1 --name-only | grep -c '178-VERIFICATION.md'` ≥1
  </acceptance_criteria>
  <done>178-VERIFICATION.md authored with full per-requirement evidence trail, SC-by-SC mapping, consolidated threat register, follow-ups; committed to git.</done>
</task>

<task type="auto">
  <name>Task 6: Update ROADMAP.md — mark Phase 178 [x] complete + update Progress table + commit</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file)
    - .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md (Task 5 output)
  </read_first>
  <action>
    1. Update Phase 178 list entry — flip `[ ]` to `[x]` + closure summary text (date, plans=8, spend, evidence ref).

    2. Update Phase 178 detail block — append "**Plans**: 8 plans" + 8 plan rows with [x] checkboxes (per Phase 177 pattern).

    3. Update Progress table row: `| 178. Vertical Content Seeding | 8/8 | Complete | YYYY-MM-DD |`.

    4. Update v21.0 totals line to reflect 17/19 plans complete (Phase 176 + 177 + 178 closed; Phase 179 TBD).

    5. Update footer "Last updated" timestamp + Phase 178 CLOSED summary.

    6. Commit:
       ```bash
       git add .planning/ROADMAP.md
       git commit -m "docs(178): mark Phase 178 complete in ROADMAP — 8/8 plans, ~\$<X> Anthropic, zero validator failures, no v20.0 regression"
       ```
  </action>
  <verify>
    <automated>grep -c '^\- \[x\] \*\*Phase 178' .planning/ROADMAP.md && grep -c '8/8 \| Complete' .planning/ROADMAP.md && grep -c '178-VERIFICATION.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 178 list entry marked [x]: `grep -c '^\- \[x\] \*\*Phase 178' .planning/ROADMAP.md` ≥1
    - Progress table updated: `grep -c '178. Vertical Content Seeding | 8/8 | Complete' .planning/ROADMAP.md` ≥1
    - Phase 178 detail block has all 8 plan rows marked [x]: `grep -c '178-0[1-8]-PLAN.md' .planning/ROADMAP.md` ≥8
    - Footer "Last updated" timestamp updated: `grep -c "Phase 178 CLOSED" .planning/ROADMAP.md` ≥1
    - 178-VERIFICATION.md referenced: `grep -c '178-VERIFICATION.md' .planning/ROADMAP.md` ≥1
    - File committed to git: `git log -1 --name-only | grep -c 'ROADMAP.md'` ≥1
  </acceptance_criteria>
  <done>ROADMAP.md updated: Phase 178 [x] complete; 8/8 plans listed; Progress table reflects closure; v21.0 totals updated; footer timestamped; committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave runs → Anthropic + S3 + Supabase | All API calls flow through Phase 177 EF infrastructure with admin-gated service-role client; no new trust boundary in Plan 08 |
| Operator review (admin-only) → bulk-approve UI | RLS-gated; only admins can read template_drafts; bulk-action toolbar lives behind App.jsx adminToolPages allowlist |
| Wave artifact contents → repo | Slugs + draftIds + status + cost; no PII; admin-only context |
| 178-VERIFICATION.md → repo | Evidence trail; SQL probe outputs (counts, IDs); no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-08-01 | Repudiation | Wave run audit trail | mitigate | Each wave artifact records every attempt's slug+draftId+status+cost+elapsed; committed alongside the wave's git activity |
| T-178-08-02 | Tampering | Verification harness drift | mitigate | verify-178-counts.cjs is repo-versioned; re-runnable; exits 2 on any SC failure |
| T-178-08-03 | Information disclosure | 178-VERIFICATION.md | accept | SQL probe outputs include counts and at most slug strings; no secrets |
| T-178-08-04 | DoS | 3 sequential waves running back-to-back | mitigate | Wave-by-wave cadence with 300ms throttle within each wave; admin review session between waves naturally pauses sequence; cost-cap-per-wave bounds spend |
| T-178-08-05 | Tampering | ROADMAP.md update | accept | git commit with descriptive message; reverting only requires `git revert` |
</threat_model>

<verification>
- 3 wave artifacts produced and committed (Restaurants, Retail, Healthcare); each ~120 attempts
- Each wave's net-new contribution ≥80 active per vertical (post-cull from ≥120 attempts per D-04)
- ≥8 distinct template_types per vertical (combined existing + net-new)
- Hero types in BOTH portrait + landscape per vertical (TCAT-02)
- gallery_templates total ≥427 (TCAT-01)
- Zero validator failures on net-new (TCAT-03)
- All net-new have S3 thumbnails (TCAT-03)
- chk_svg_templates_category_enum CHECK untouched (TCAT-04)
- Every net-new has vertical tag (TVRT-04)
- Playwright v20.0 category-filter regression at ≥90% green (TVRT-05)
- 178-VERIFICATION.md authored with full evidence trail
- ROADMAP.md updated; git committed
</verification>

<success_criteria>
- All 9 phase requirements (TVRT-01..05 + TCAT-01..04) PASS per verify-178-counts.cjs and validate-templates.cjs and Playwright
- 178-VERIFICATION.md committed
- ROADMAP.md committed with Phase 178 marked [x] complete
- v21.0 milestone progress: Phase 178 closed; Phase 179 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/178-vertical-content-seeding/178-08-SUMMARY.md` documenting:
- Wave 1 / 2 / 3 attempt counts (~120 each), validator pass rates, costs (~$4.80 each), approve counts (≥80 each)
- verify-178-counts.cjs full per-SC output
- validate-templates.cjs result (net-new total + 0 failures confirmation)
- Playwright regression sweep results
- 178-VERIFICATION.md author timestamp + line count
- ROADMAP.md commit hash + diff line count
- Total v21.0 Phase 178 spend (~$<X.XX>)
- Total wall-clock time across 3 waves + verification + sign-off
</output>
</output>
