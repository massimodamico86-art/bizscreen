---
phase: 179-gallery-virtualization-launch-validation
plan: 06
type: execute
wave: 4
depends_on: [03, 04, 05]
files_modified:
  - tests/e2e/template-gallery-perf.spec.js
autonomous: true
requirements: [TVRZ-02]
tags: [virtualization, performance, playwright, cdp]

must_haves:
  truths:
    - "Playwright spec at `tests/e2e/template-gallery-perf.spec.js` runs against the rewired gallery and the SC-2 assertion `elapsed < 1000ms` evaluates correctly"
    - "CDP throttle rate is documented in the spec header (rate=1 = no throttle per OQ-2 literal interpretation)"
    - "Catalog-floor pre-flight gate (`rowcount * 4 >= 400`) prevents false-positive passes against an unseeded DB"
    - "Spec is listed by Playwright in its standard configuration (`npx playwright test … --list` shows exactly 1 test)"
    - "When the spec is RUN with `TEST_USER_EMAIL` set against a populated local DB, it succeeds (or fails with diagnostic output identifying the breakdown — DB size, elapsed ms, or `[role='grid']` not visible)"
  artifacts:
    - path: "tests/e2e/template-gallery-perf.spec.js"
      provides: "SC-2 — gallery first-paint <1s at ~500-template catalog under CDP rate=1 throttle"
      contains: "expect(elapsed).toBeLessThan(1000)"
  key_links:
    - from: "tests/e2e/template-gallery-perf.spec.js"
      to: "src/pages/TemplateGalleryPage.jsx (rewired by Plan 05)"
      via: "Playwright navigation to /templates"
      pattern: "page\\.locator\\('\\[role=\"grid\"\\]'\\)"
    - from: "tests/e2e/template-gallery-perf.spec.js"
      to: "src/components/template-gallery/VirtualizedTemplateGrid.jsx (Plan 04)"
      via: "VirtualizedTemplateGrid renders the [role='grid'] this test asserts"
      pattern: "aria-rowcount"
---

<objective>
The Plan 03 Wave 0 scaffold for `tests/e2e/template-gallery-perf.spec.js` already encodes the full SC-2 assertion (CDP throttle + performance.mark + <1s + catalog-floor pre-flight). This plan VERIFIES the spec greens against the rewired gallery, adds any required env-availability skip-guards refinement, and locks the rate=1 decision in the spec header.

Purpose: TVRZ-02 single-source verification gate. The scaffold from Plan 03 Task 2 is structurally complete — Plan 06 is the integration validation: with Plans 04 + 05 shipped, the `[role="grid"]` element now exists and the assertion can actually execute. If the elapsed time is above 1000ms on local hardware, this plan documents the breakdown and either tunes `estimateSize` in Plan 04 (loop back) or escalates (RESEARCH §Pitfall 1 mitigation path).

Output: The same `tests/e2e/template-gallery-perf.spec.js` file from Plan 03 — verified to execute correctly against the rewired gallery. If the scaffold needs adjustment (e.g., spec runs but cannot find the grid because of timing), this plan applies the minimal fix.

This plan depends on Plan 03 (scaffold), Plan 04 (component), and Plan 05 (page rewire — the grid must actually render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-PATTERNS.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md

@tests/e2e/template-gallery-perf.spec.js
@tests/e2e/template-gallery.spec.js
@tests/e2e/helpers.js
@src/pages/TemplateGalleryPage.jsx
@src/components/template-gallery/VirtualizedTemplateGrid.jsx

<interfaces>
<!-- The locked SC-2 contract: -->
//   1. CDP session opened BEFORE navigation (Pitfall 7 — throttle does not stack)
//   2. Emulation.setCPUThrottlingRate { rate: 1 } sent (rate=1 = no throttle; documents intent)
//   3. performance.mark('gallery-paint-start') via page.addInitScript
//   4. gotoTemplates(page) — sidebar click → gallery render
//   5. [role="grid"] waitFor visible (timeout 5000ms)
//   6. aria-rowcount * 4 >= 400 (catalog-floor pre-flight)
//   7. performance.mark('gallery-paint-end') + performance.measure
//   8. expect(elapsed).toBeLessThan(1000)

// Plan 03 Task 2 already wrote this contract verbatim. Plan 06 verifies it RUNS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SC-2 perf spec lists and lock the rate=1 decision in the header</name>
  <files>tests/e2e/template-gallery-perf.spec.js</files>
  <read_first>
    - tests/e2e/template-gallery-perf.spec.js (current state from Plan 03 Task 2)
    - tests/e2e/helpers.js — confirm `loginAndPrepare` + `waitForPageReady` are exported (they are imported at top of spec)
    - playwright.config.js (project root) — confirm Chromium project + base URL
    - .planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md §Open Questions OQ-2 (lines 749-754) — rate=1 rationale
    - .planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md §Pitfalls Pitfall 7 — CDP throttle does not stack across navigations
  </read_first>
  <action>
    1. Open `tests/e2e/template-gallery-perf.spec.js` (created in Plan 03 Task 2). Confirm the spec already contains the full assertion shape (the Plan 03 acceptance criteria locked this in).

    2. ENSURE the spec header JSDoc names the chosen CDP rate explicitly. The Plan 03 scaffold already includes:
       ```
       /**
        * ... rate=1 (literal interpretation of SC-2 "1x CPU throttling" per
        * 179-RESEARCH OQ-2; rate=1 = no throttle, used here to document intent
        * and provide a CI-stable spot to swap rates if hardware varies) ...
        */
       ```
       If this paragraph is missing or modified, restore it verbatim.

    3. RUN `npx playwright test tests/e2e/template-gallery-perf.spec.js --list` — output MUST list exactly 1 test entry. If 0 tests are listed, Plan 03 Task 2 left a syntax/import error — fix it (likely missing import from `./helpers.js` or a stray top-level expression).

    4. RUN `npx playwright test tests/e2e/template-gallery-perf.spec.js --reporter=line 2>&1 | tail -40`. Three possible outcomes:
       - **Skipped** because `TEST_USER_EMAIL` is unset in the local environment → acceptable; the skip-guard fires correctly. Document in SUMMARY that the spec runs RED→SKIP in this env.
       - **Failed** with `Timeout 5000ms exceeded` waiting for `[role="grid"]` → Plans 04 + 05 didn't actually mount the grid. RE-VERIFY by running `npm run dev` in another terminal and manually visiting `/templates` to inspect for `role="grid"` (out-of-scope for this autonomous task — escalate via SUMMARY).
       - **Failed** with `Expected: < 1000, Received: <N>` where N > 1000 → SC-2 budget exceeded. Document the elapsed ms in SUMMARY; do NOT tune `estimateSize` in this plan (that would be a Plan 04 loop-back). Note: rate=1 = no throttle, so any elapsed >1000ms on local hardware means the virtualizer is slower than expected — RESEARCH §Pitfall 1 mitigation path = bump `ESTIMATE_SIZE` in `VirtualizedTemplateGrid.jsx` closer to the actual measured row height.

    5. If the test actually PASSES locally (elapsed <1000ms), great — record the elapsed value in SUMMARY for v21.0 launch documentation. If the env-skip path fires (no `TEST_USER_EMAIL`), the test is correctly gated and will run GREEN in CI where creds are set; record skip in SUMMARY.

    No edits to assertions in this task — the assertion shape is locked. Only restore-the-header and verify-listing-works actions are in scope.

    Commit message (if any edits made): `test(179-06): finalize SC-2 perf spec header (rate=1 OQ-2 lock)`
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/template-gallery-perf.spec.js --list 2>&1 | grep -qE "(1 test in|template-gallery-perf)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/template-gallery-perf.spec.js` exists (Plan 03 produced it)
    - `grep -F "rate: 1" tests/e2e/template-gallery-perf.spec.js` exits 0
    - `grep -F "OQ-2" tests/e2e/template-gallery-perf.spec.js` exits 0 (decision documented in header)
    - `grep -F "expect(elapsed).toBeLessThan(1000)" tests/e2e/template-gallery-perf.spec.js` exits 0
    - `grep -F "rowcount * 4" tests/e2e/template-gallery-perf.spec.js` exits 0 (catalog-floor gate)
    - `npx playwright test tests/e2e/template-gallery-perf.spec.js --list` exits 0 and lists exactly 1 test
    - Spec is either GREEN (elapsed <1000ms recorded), SKIPPED (no TEST_USER_EMAIL), or RED with documented breakdown ms in SUMMARY
  </acceptance_criteria>
  <done>SC-2 perf spec verified end-to-end against the rewired gallery; rate=1 decision locked in the spec header; result (green/skip/elapsed-ms) recorded in SUMMARY.</done>
</task>

</tasks>

<verification>
After Task 1:

```bash
# Spec lists
npx playwright test tests/e2e/template-gallery-perf.spec.js --list

# Spec runs (one of GREEN / SKIPPED / RED-with-breakdown)
npx playwright test tests/e2e/template-gallery-perf.spec.js --reporter=line 2>&1 | tail -20

# rate=1 documented in header
grep -A3 "rate" tests/e2e/template-gallery-perf.spec.js | head -10

# Catalog-floor gate preserved
grep -F "rowcount * 4" tests/e2e/template-gallery-perf.spec.js
```
</verification>

<success_criteria>
- Playwright lists the perf spec as exactly 1 test
- Rate=1 decision (OQ-2 literal interpretation) is documented in the spec's file-level JSDoc
- Catalog-floor pre-flight gate is preserved (`rowcount * 4 >= 400`)
- Final result is one of: GREEN (elapsed <1000ms on populated DB), SKIPPED (no creds), or RED with diagnostic elapsed ms recorded for v21.0 launch documentation
</success_criteria>

<output>
After completion, create `.planning/phases/179-gallery-virtualization-launch-validation/179-06-SUMMARY.md` recording the spec run result (GREEN ms / SKIPPED / RED with breakdown ms). If RED with breakdown >1500ms, flag for a Plan 04 loop-back to retune `ESTIMATE_SIZE`.
</output>
</content>
</invoke>