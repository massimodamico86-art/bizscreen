---
phase: 179-gallery-virtualization-launch-validation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [TVRZ-01, TVRZ-05]
tags: [virtualization, dependency-install, supply-chain]

must_haves:
  truths:
    - "`@tanstack/react-virtual` is installed at exactly ^3.13.24 and importable from src/"
    - "`@axe-core/playwright` is installed at exactly ^4.11.3 as a devDependency and importable from tests/e2e/"
    - "No source file uses `dangerouslySetInnerHTML` inside `src/components/template-gallery/` (lint/grep gate)"
  artifacts:
    - path: "package.json"
      provides: "@tanstack/react-virtual ^3.13.24 in dependencies; @axe-core/playwright ^4.11.3 in devDependencies"
      contains: '"@tanstack/react-virtual": "^3.13.24"'
    - path: "package-lock.json"
      provides: "Reproducible install of both packages"
  key_links:
    - from: "package.json"
      to: "node_modules/@tanstack/react-virtual"
      via: "npm install"
      pattern: '"@tanstack/react-virtual":\s*"\^3\.13'
    - from: "package.json"
      to: "node_modules/@axe-core/playwright"
      via: "npm install --save-dev"
      pattern: '"@axe-core/playwright":\s*"\^4\.11'
---

<objective>
Install the two new packages that the entire phase depends on: `@tanstack/react-virtual` (runtime — TVRZ-01 mechanism) and `@axe-core/playwright` (devDependency — SC-5 axe scanner). Verify importability and pin versions. Lock the supply-chain threat model.

Purpose: Wave 0 BLOCKING install. Every subsequent test file is meaningless without these packages on disk; every component file is unrunnable without the virtualizer. This plan is the literal gate that unblocks Wave 0 plans 02 and 03 (which can run in parallel after this completes) and all of Wave 1.

Output: Two new package entries in `package.json` (alphabetically sorted per project convention), refreshed `package-lock.json`, and importability verified via Node resolution check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-CONTEXT.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md

<interfaces>
<!-- The two packages this plan installs. Pinned versions from 179-RESEARCH.md §Standard Stack -->
<!-- and §Environment Availability — both verified live via npm registry on 2026-05-10. -->

From RESEARCH.md (lines 91–101, 119–121, 632–636):
```text
@tanstack/react-virtual  ^3.13.24    (published 2026-04-17; VERIFIED via `npm view`)
@axe-core/playwright     ^4.11.3     (published 2026-04-30; VERIFIED via `npm view`)
```

Existing package.json structure (alphabetically sorted per block):
- dependencies block (lines 34–58): `@supabase/...` → INSERT `@tanstack/react-virtual` HERE → `date-fns`
- devDependencies block (lines 59–86): `@anthropic-ai/sdk` → INSERT `@axe-core/playwright` HERE → `@eslint/js`

(Pattern confirmed in 179-PATTERNS.md §package.json.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-virtual at ^3.13.24 + verify importability</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md §Standard Stack (lines 86–121) — pinned version + verification command
    - .planning/phases/179-gallery-virtualization-launch-validation/179-PATTERNS.md §package.json — alphabetical sort convention
    - package.json (current state — to see existing dependencies block ordering)
  </read_first>
  <action>
    Run exactly: `npm install @tanstack/react-virtual@^3.13.24`

    After install completes:
    1. Verify `package.json` `dependencies` block now contains the line `"@tanstack/react-virtual": "^3.13.24",` between `"@supabase/supabase-js"` and `"date-fns"` (npm install preserves alphabetical sort by default; manually reorder only if npm produced an out-of-place insertion).
    2. Verify importability via Node resolution: run `node -e "console.log(Object.keys(require('@tanstack/react-virtual')))"` — output MUST include `useVirtualizer` (the named export this phase consumes).
    3. Verify `package-lock.json` was updated and that `@tanstack/react-virtual` resolves to a 3.13.x version: run `node -e "console.log(require('@tanstack/react-virtual/package.json').version)"` — output MUST start with `3.13.`.

    Do NOT pin to a different version range. The `^3.13.24` carat allows patch updates only within 3.x.x per the supply-chain mitigation in the threat model below.
  </action>
  <verify>
    <automated>node -e "console.log(require('@tanstack/react-virtual/package.json').version.split('.')[0] + '.' + require('@tanstack/react-virtual/package.json').version.split('.')[1])" | grep -F "3.13"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '"@tanstack/react-virtual": "^3.13' package.json` exits 0
    - `node -e "require('@tanstack/react-virtual')"` exits 0 (no MODULE_NOT_FOUND)
    - `node -e "console.log(Object.keys(require('@tanstack/react-virtual')).filter(k => k === 'useVirtualizer').length)"` outputs `1`
    - `package-lock.json` exists and contains the string `"@tanstack/react-virtual"`
  </acceptance_criteria>
  <done>`@tanstack/react-virtual` ^3.13.x is installed, present in `dependencies`, and the `useVirtualizer` export is reachable from Node.</done>
</task>

<task type="auto">
  <name>Task 2: Install @axe-core/playwright at ^4.11.3 as devDependency + verify importability</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md §Standard Stack (lines 94–101) + §Environment Availability (lines 632–636)
    - .planning/phases/179-gallery-virtualization-launch-validation/179-PATTERNS.md §package.json — devDependencies alphabetical sort
    - package.json (after Task 1 — verify dependencies block landed cleanly before touching devDependencies)
  </read_first>
  <action>
    Run exactly: `npm install --save-dev @axe-core/playwright@^4.11.3`

    After install completes:
    1. Verify `package.json` `devDependencies` block now contains `"@axe-core/playwright": "^4.11.3",` between `"@anthropic-ai/sdk"` and `"@eslint/js"` (alphabetical order maintained by npm).
    2. Verify importability: run `node -e "console.log(typeof require('@axe-core/playwright').default)"` — output MUST be `function` (the `AxeBuilder` class — `@axe-core/playwright` ships a CJS default export per its package layout).
    3. Verify version: `node -e "console.log(require('@axe-core/playwright/package.json').version.split('.').slice(0,2).join('.'))"` MUST output `4.11`.

    Do NOT install as a plain dependency. It is test-only infrastructure — must be in `devDependencies`. Do NOT install a different package such as `axe-core` (raw engine, no Playwright wiring) or `axe-playwright` (third-party fork, not first-party).
  </action>
  <verify>
    <automated>node -e "console.log(require('@axe-core/playwright/package.json').version.split('.').slice(0,2).join('.'))" | grep -F "4.11"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '"@axe-core/playwright": "^4.11' package.json` exits 0
    - The match for `@axe-core/playwright` occurs INSIDE the `devDependencies` block (verify with: `node -e "const p=require('./package.json'); console.log(p.devDependencies['@axe-core/playwright'] ? 'OK' : 'MISSING')"` outputs `OK`)
    - `node -e "require('@axe-core/playwright')"` exits 0
    - `node -e "console.log(require('@axe-core/playwright/package.json').version)"` outputs a string starting with `4.11.`
  </acceptance_criteria>
  <done>`@axe-core/playwright` ^4.11.x is in devDependencies, importable, and the `AxeBuilder` default export is reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Lint/grep gate — dangerouslySetInnerHTML banned in src/components/template-gallery/</name>
  <files>(no file modifications — verification gate only)</files>
  <read_first>
    - .planning/phases/179-gallery-virtualization-launch-validation/179-RESEARCH.md §Security Domain (lines 689–712) — threat model entry "dangerouslySetInnerHTML regression in new component"
    - src/components/template-gallery/StarterPacksStrip.jsx (analog — confirm baseline is clean)
    - src/components/template-gallery/PackCard.jsx
    - src/components/template-gallery/PackPreviewModal.jsx
  </read_first>
  <action>
    Verify the supply-chain + DOM-safety threat model gates for this phase (per `<threat_model>` block below):

    1. Run: `grep -rn "dangerouslySetInnerHTML" src/components/template-gallery/ src/hooks/useContainerColumns.js 2>/dev/null` — MUST exit non-zero (no matches). The `2>/dev/null` swallows ENOENT for `useContainerColumns.js` which will not exist until Plan 02.
    2. Run: `grep -rn "dangerouslySetInnerHTML" src/pages/TemplateGalleryPage.jsx` — MUST exit non-zero (no matches; baseline is clean today, gate stays clean post-rewire).
    3. Confirm baseline count of `dangerouslySetInnerHTML` usages site-wide for context (informational only — DO NOT touch): `grep -rn --include="*.jsx" --include="*.js" "dangerouslySetInnerHTML" src/ | wc -l` — this number is recorded in the plan SUMMARY and Plans 04/05 MUST NOT increase it.

    No file edits in this task — it is the supply-chain + DOM-safety gate that locks the threat model for downstream plans.
  </action>
  <verify>
    <automated>! grep -rn "dangerouslySetInnerHTML" src/components/template-gallery/ src/pages/TemplateGalleryPage.jsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "dangerouslySetInnerHTML" src/components/template-gallery/` exits non-zero (no matches)
    - `grep -rn "dangerouslySetInnerHTML" src/pages/TemplateGalleryPage.jsx` exits non-zero (no matches)
    - Baseline site-wide count recorded in SUMMARY (informational, for Plans 04/05 to reference)
  </acceptance_criteria>
  <done>Baseline confirmed clean. Plans 04 and 05 inherit a "no `dangerouslySetInnerHTML` in new gallery code" contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supply chain → install tree | npm registry packages enter the application bundle; tampering possible if version pins are loose |
| Test runtime → DOM | `@axe-core/playwright` evaluates rendered DOM but does not modify it; scoped via `.include('[role="grid"]')` in Plan 08 |
| New component code → DOM | `VirtualizedTemplateGrid` (Plan 04) renders untrusted-looking strings (template names/descriptions) as React children only — NEVER via `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-01 | Tampering | `@tanstack/react-virtual` install tree | mitigate | Pin `^3.13.24` (carat allows patch-only within 3.x.x); Task 1 verifies installed version starts with `3.13.`; `package-lock.json` diff must be reviewed at PR time (visible in Task 1 output). |
| T-179-02 | Tampering | `@axe-core/playwright` install tree | mitigate | Pin `^4.11.3` (carat allows patch-only within 4.x.x); Task 2 verifies installed version starts with `4.11.`; devDependency only — never reaches production bundle. |
| T-179-03 | Tampering | New gallery code uses `dangerouslySetInnerHTML` | mitigate | Task 3 grep gate locks the contract: zero `dangerouslySetInnerHTML` in `src/components/template-gallery/` and `src/pages/TemplateGalleryPage.jsx`. Plans 04 and 05 inherit this constraint; their verification re-runs the same grep. |
| T-179-04 | Denial of Service (a11y) | `aria-rowcount` confusion attack — screen-reader gets a stale virtual count | accept | Low risk in user-facing gallery; mitigated by Plan 04 unit test asserting `aria-rowcount === ceil(filteredTemplates.length / cols)`. Documented in RESEARCH §Pitfall 4. |
| T-179-05 | Information Disclosure | Search query (`filters.q`) reflected in URL via `useSearchParams` | accept | No PII; existing v20.0 behavior; fuse.js never renders the query as HTML. RESEARCH §Security Domain V5 confirms "no XSS surface change." |
</threat_model>

<verification>
After all three tasks complete:

```bash
# Both packages reachable from Node
node -e "require('@tanstack/react-virtual'); require('@axe-core/playwright'); console.log('OK')"

# Versions pinned correctly
node -e "const a = require('@tanstack/react-virtual/package.json').version; const b = require('@axe-core/playwright/package.json').version; console.log({a, b})"

# DOM-safety gate clean
! grep -rn "dangerouslySetInnerHTML" src/components/template-gallery/ src/pages/TemplateGalleryPage.jsx
```

All three commands MUST exit 0 (the third is negated with `!`).
</verification>

<success_criteria>
- `@tanstack/react-virtual` ^3.13.x present in `dependencies` and importable
- `@axe-core/playwright` ^4.11.x present in `devDependencies` and importable
- Zero `dangerouslySetInnerHTML` matches in `src/components/template-gallery/` and `src/pages/TemplateGalleryPage.jsx`
- `package-lock.json` updated and committed (lockfile drift catches tampering attempts)
</success_criteria>

<output>
After completion, create `.planning/phases/179-gallery-virtualization-launch-validation/179-01-SUMMARY.md` recording:
- The installed patch versions of both packages
- The baseline site-wide `dangerouslySetInnerHTML` count (for Plans 04/05 to verify-against-delta)
- Confirmation that `useVirtualizer` and `AxeBuilder` exports are reachable
</output>
</content>
</invoke>