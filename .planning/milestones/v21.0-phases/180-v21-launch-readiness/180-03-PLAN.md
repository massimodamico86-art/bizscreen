---
phase: 180-v21-launch-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md
autonomous: true
requirements: [TCAT-01, TCAT-02, TCAT-03, TCAT-04, TVRT-01, TVRT-02, TVRT-03, TVRT-04, TVRT-05]
requirements_addressed: [TCAT-01, TCAT-02, TCAT-03, TCAT-04, TVRT-01, TVRT-02, TVRT-03, TVRT-04, TVRT-05]
gap_closure: true
tags: [phase-180, traceability, requirements, doc-correction, human-uat]

must_haves:
  truths:
    - "REQUIREMENTS.md Traceability table shows Status='Complete' for all 9 IDs (TCAT-01..04 + TVRT-01..05) with a cross-reference pointing to Phase 178 VERIFICATION + commit 04ed5938"
    - "REQUIREMENTS.md v1 checklist boxes for TCAT-01..04 + TVRT-01..05 are flipped from `[ ]` to `[x]`"
    - "REQUIREMENTS.md Coverage line at the bottom reflects accurate state — 24/24 v1 requirements mapped to phases AND 24/24 satisfied (the 9 Pending IDs are now Complete; 15 were already Complete or codebase-tier-Complete-pending-CI)"
    - "Phase 177 HUMAN-UAT.md item 4 reflects what was ACTUALLY verified during Phase 177 testing (E2E test:setCurrentPage CustomEvent path, NOT 'reachable only via the Admin Tools panel') AND notes that Phase 180 Plan 01 closes the nav-gap so the queue IS now reachable via the Admin Tools panel"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows flipped Pending → Complete for TCAT-01..04 + TVRT-01..05; v1 checklist boxes flipped [ ] → [x]; Coverage line refreshed"
      contains: "Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938"
    - path: ".planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md"
      provides: "Corrected item 4 reflecting the actual verification path (E2E test:setCurrentPage CustomEvent) + Phase 180 BLOCKER-1 closure note"
      contains: "Phase 180 Plan 01"
  key_links:
    - from: "REQUIREMENTS.md TCAT/TVRT Traceability rows"
      to: ".planning/phases/178-vertical-content-seeding/178-VERIFICATION.md"
      via: "Cross-reference string 'Phase 178 — 178-VERIFICATION.md commit 04ed5938'"
      pattern: "04ed5938"
    - from: "Phase 177 HUMAN-UAT.md item 4 (corrected)"
      to: "Phase 180 Plan 01 (nav-repair closure)"
      via: "Cross-reference note added during this plan"
      pattern: "Phase 180"
---

<objective>
Close two audit gaps in one atomic doc-only plan:

1. **SC-4 (Traceability flip):** Update `.planning/REQUIREMENTS.md` Traceability table to flip `Pending` → `Complete` for all 9 Phase 178 requirement IDs (TCAT-01..04 + TVRT-01..05). Each row gets a cross-reference to `178-VERIFICATION.md` (verified 2026-05-10) and the commit hash `04ed5938` (the 178-VERIFICATION.md commit). Also flip the 9 v1 checklist boxes from `[ ]` to `[x]` in the corresponding `## Catalog Scale-Up (TCAT)` and `## Vertical Content (TVRT)` sections. Refresh the Coverage block at the bottom so it reflects 24/24 satisfied.

2. **SC-5 (Phase 177 HUMAN-UAT.md item 4 correction):** Rewrite the item 4 result paragraph in `.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md`. The current wording claims the queue is "reachable only via the SuperAdminDashboard 'Admin Tools' panel which doesn't render for non-admin users" — that assertion was incorrect at the time of writing (Phase 177): the panel had no Template Queue tile; the actual verification path was the `test:setCurrentPage` CustomEvent (which only works under `VITE_E2E_TEST_MODE=1` — not the production admin path). The corrected wording must (a) accurately describe the actual verification mechanism that was used in Phase 177, and (b) note that as of Phase 180 Plan 01, the queue IS reachable via the Admin Tools panel (BLOCKER-1 closure).

Purpose: Two of v21.0's audit-flagged closeout items are pure traceability + doc-correction. Closing them in a single plan keeps the gap-closure phase atomic and minimizes the number of doc-only commits.

Output: Updated `.planning/REQUIREMENTS.md` with 9 row Status flips + 9 checklist flips + refreshed Coverage block. Updated `.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` with the rewritten item 4 result paragraph. Both files committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v21.0-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md
@.planning/phases/178-vertical-content-seeding/178-VERIFICATION.md
@.planning/phases/180-v21-launch-readiness/

<interfaces>
<!-- Concrete contracts the executor needs. -->

### Current REQUIREMENTS.md v1 checklist for TCAT (lines 14-19, verbatim)

```markdown
### Catalog Scale-Up (TCAT)

- [ ] **TCAT-01**: Active template catalog reaches ≥427 published templates (existing 127 + ≥300 net-new) by milestone close
- [ ] **TCAT-02**: Hero template types (menu boards, hero promo banners, drive-thru boards, full-screen sales banners, waiting-room ambient, daypart menus) ship in both portrait and landscape orientations
- [ ] **TCAT-03**: Every net-new template passes `svgValidator` (6-rule gate); a PNG thumbnail rasterized via `@resvg/resvg-js` is uploaded to `s3://bizscreen-media/thumbnails/system/` for each
- [ ] **TCAT-04**: `chk_svg_templates_category_enum` CHECK and the `gallery_templates` VIEW continue to enforce taxonomy with no fail-open paths through the new ingestion lanes
```

### Current REQUIREMENTS.md v1 checklist for TVRT (lines 21-27, verbatim)

```markdown
### Vertical Content (TVRT)

- [ ] **TVRT-01**: Restaurants & QSR vertical reaches ≥80 published templates spanning ≥8 distinct template types — full menu board, daypart menu, daily specials / LTO, new-item announcement, social proof / reviews, queue / wait-time display, seasonal campaign, hours + loyalty + drive-thru
- [ ] **TVRT-02**: Retail & e-commerce vertical reaches ≥80 published templates spanning ≥8 distinct template types — flash sale / promo banner, new arrivals, product spotlight, seasonal campaign, social proof / UGC, loyalty / rewards, wayfinding, hours / window display
- [ ] **TVRT-03**: Healthcare & wellness vertical reaches ≥80 published templates spanning ≥8 distinct template types — waiting-room ambient, wait time / queue status, health tip, appointment reminder, provider directory, vaccination / screening reminder, emergency / safety alert, clinic hours / pharmacy info
- [ ] **TVRT-04**: Every net-new template carries a `vertical` tag (`restaurants` | `retail` | `healthcare`) so downstream search and filter can target it
- [ ] **TVRT-05**: Existing 15 generic categories continue to filter correctly post-tagging (no regression in the v20.0 category filter UX)
```

All 9 boxes must be flipped from `[ ]` to `[x]`.

### Current REQUIREMENTS.md Traceability rows for TCAT + TVRT (lines 115-123, verbatim)

```markdown
| TCAT-01 | 178 | Pending |
| TCAT-02 | 178 | Pending |
| TCAT-03 | 178 | Pending |
| TCAT-04 | 178 | Pending |
| TVRT-01 | 178 | Pending |
| TVRT-02 | 178 | Pending |
| TVRT-03 | 178 | Pending |
| TVRT-04 | 178 | Pending |
| TVRT-05 | 178 | Pending |
```

All 9 Status cells must be flipped from `Pending` to `Complete` with a cross-reference.

### Current REQUIREMENTS.md Coverage block (lines 140-145, verbatim)

```markdown
**Coverage:**
- v1 requirements: 24 total (was 23 at initial definition; TGEN-06 added 2026-05-06)
- Mapped to phases: 24 (Phase 177: 10, Phase 178: 9, Phase 179: 5)
- Unmapped: 0 — 100% coverage achieved
```

This block should be refreshed to add an explicit "Satisfied" line reflecting 24/24 satisfied status post-flip.

### Current Phase 177 HUMAN-UAT.md item 4 (verbatim, lines 35-37 of 177-HUMAN-UAT.md)

```markdown
### 4. TADM-04 non-admin redirect
expected: Non-admin user does NOT see Template Queue in the nav. Direct navigation routes to dashboard. EF returns 403 if any direct fetch is attempted.
result: PASS — non-admin sidebar contained the same 9 standard items (Welcome / Dashboard / Media / Apps / Playlists / Templates / Schedules / Screens / Social Moderation), no admin-only entries. Template Queue is reachable only via the SuperAdminDashboard "Admin Tools" panel which doesn't render for non-admin users. Server-side defense-in-depth verified via curl with the non-admin JWT: `is_admin()` returns `false`, `is_super_admin()` returns `false`, EF call with `action=approve` returns HTTP 403, EF call with `action=save_edit` returns HTTP 403. (Test-mode `CustomEvent('test:setCurrentPage', ...)` did render the page UI for the non-admin, but that listener only registers when `VITE_E2E_TEST_MODE=1` is set in vite env — not present in production builds, so this is not a real attack vector.)
```

The result paragraph contains the incorrect "reachable only via the SuperAdminDashboard 'Admin Tools' panel" claim — at Phase 177 time, the Admin Tools panel had no Template Queue tile (BLOCKER-1; closed in Phase 180 Plan 01).

### Phase 178 verification commit (already verified)

- `git log --oneline 04ed5938` → `docs(178-08): 178-VERIFICATION.md — 9/9 SC GREEN, 357 net-new published` (confirmed; this is the canonical cross-reference for SC-4 flip)

### Audit's authoritative flip spec (verbatim from v21.0-MILESTONE-AUDIT.md lines 251-256 — tech debt list, Phase 178 cluster)

> "TCAT-01..04 + TVRT-01..05 still show 'Pending' in REQUIREMENTS.md traceability table — update to 'Complete' per 178-VERIFICATION"

### Audit's authoritative correction spec (verbatim from v21.0-MILESTONE-AUDIT.md lines 169-171)

> "Phase 177 HUMAN-UAT.md item 4 incorrectly claims the queue is reachable via SuperAdminDashboard's Admin Tools panel; the verification was done via E2E `test:setCurrentPage` CustomEvent, which only registers when `VITE_E2E_TEST_MODE=1` (not active in production builds)"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip 9 REQUIREMENTS.md Traceability rows + 9 v1 checklist boxes + refresh Coverage block (closes SC-4)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — 151 lines; confirms current layout, line numbers of the 9 checklist boxes and 9 traceability rows, and the Coverage block)
    - .planning/v21.0-MILESTONE-AUDIT.md lines 251-256 (audit's authoritative flip spec)
    - .planning/v21.0-MILESTONE-AUDIT.md lines 126-140 (Phase 178 requirements scoreboard — all 9 IDs satisfied at code-tier)
    - .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md (full file or first 50 lines — confirms verification frontmatter `status: passed` and `score: 9/9 SC checks GREEN`)
    - Verify commit hash with `git log --oneline 04ed5938` returns the docs(178-08) line — proves the cross-reference target exists
  </read_first>
  <behavior>
    - All 9 v1 checklist boxes for TCAT-01, TCAT-02, TCAT-03, TCAT-04, TVRT-01, TVRT-02, TVRT-03, TVRT-04, TVRT-05 are flipped from `- [ ] **TCAT-XX**` (or `**TVRT-XX**`) to `- [x] **TCAT-XX**` (or `**TVRT-XX**`)
    - All 9 Traceability table Status cells for the same IDs are flipped from `Pending` to `Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: ...)` with a 1-line per-ID rationale matching the audit's verified evidence
    - Coverage block at the bottom adds an explicit "Satisfied" line reflecting 24/24 satisfied (or equivalent wording — see action body for the exact replacement)
    - `grep -c "^- \[x\] \*\*TCAT-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns exactly 4
    - `grep -c "^- \[x\] \*\*TVRT-0[1-5]\*\*" .planning/REQUIREMENTS.md` returns exactly 5
    - `grep -c "^| TCAT-0[1-4] | 178 | Pending |" .planning/REQUIREMENTS.md` returns EXACTLY 0 (no remaining Pending rows in this set)
    - `grep -c "^| TVRT-0[1-5] | 178 | Pending |" .planning/REQUIREMENTS.md` returns EXACTLY 0
    - `grep -c "04ed5938" .planning/REQUIREMENTS.md` returns ≥ 9 (one cross-reference per flipped row)
  </behavior>
  <action>
    Make three sets of edits to `.planning/REQUIREMENTS.md`. Use the Edit tool with the verbatim old_string / new_string blocks below.

    **Edit 1 — Flip TCAT v1 checklist boxes (4 boxes).**

    Find (verbatim):
    ```markdown
    - [ ] **TCAT-01**: Active template catalog reaches ≥427 published templates (existing 127 + ≥300 net-new) by milestone close
    - [ ] **TCAT-02**: Hero template types (menu boards, hero promo banners, drive-thru boards, full-screen sales banners, waiting-room ambient, daypart menus) ship in both portrait and landscape orientations
    - [ ] **TCAT-03**: Every net-new template passes `svgValidator` (6-rule gate); a PNG thumbnail rasterized via `@resvg/resvg-js` is uploaded to `s3://bizscreen-media/thumbnails/system/` for each
    - [ ] **TCAT-04**: `chk_svg_templates_category_enum` CHECK and the `gallery_templates` VIEW continue to enforce taxonomy with no fail-open paths through the new ingestion lanes
    ```

    Replace with:
    ```markdown
    - [x] **TCAT-01**: Active template catalog reaches ≥427 published templates (existing 127 + ≥300 net-new) by milestone close
    - [x] **TCAT-02**: Hero template types (menu boards, hero promo banners, drive-thru boards, full-screen sales banners, waiting-room ambient, daypart menus) ship in both portrait and landscape orientations
    - [x] **TCAT-03**: Every net-new template passes `svgValidator` (6-rule gate); a PNG thumbnail rasterized via `@resvg/resvg-js` is uploaded to `s3://bizscreen-media/thumbnails/system/` for each
    - [x] **TCAT-04**: `chk_svg_templates_category_enum` CHECK and the `gallery_templates` VIEW continue to enforce taxonomy with no fail-open paths through the new ingestion lanes
    ```

    **Edit 2 — Flip TVRT v1 checklist boxes (5 boxes).**

    Find (verbatim):
    ```markdown
    - [ ] **TVRT-01**: Restaurants & QSR vertical reaches ≥80 published templates spanning ≥8 distinct template types — full menu board, daypart menu, daily specials / LTO, new-item announcement, social proof / reviews, queue / wait-time display, seasonal campaign, hours + loyalty + drive-thru
    - [ ] **TVRT-02**: Retail & e-commerce vertical reaches ≥80 published templates spanning ≥8 distinct template types — flash sale / promo banner, new arrivals, product spotlight, seasonal campaign, social proof / UGC, loyalty / rewards, wayfinding, hours / window display
    - [ ] **TVRT-03**: Healthcare & wellness vertical reaches ≥80 published templates spanning ≥8 distinct template types — waiting-room ambient, wait time / queue status, health tip, appointment reminder, provider directory, vaccination / screening reminder, emergency / safety alert, clinic hours / pharmacy info
    - [ ] **TVRT-04**: Every net-new template carries a `vertical` tag (`restaurants` | `retail` | `healthcare`) so downstream search and filter can target it
    - [ ] **TVRT-05**: Existing 15 generic categories continue to filter correctly post-tagging (no regression in the v20.0 category filter UX)
    ```

    Replace with (same 5 lines, only the `[ ]` → `[x]` per line):
    ```markdown
    - [x] **TVRT-01**: Restaurants & QSR vertical reaches ≥80 published templates spanning ≥8 distinct template types — full menu board, daypart menu, daily specials / LTO, new-item announcement, social proof / reviews, queue / wait-time display, seasonal campaign, hours + loyalty + drive-thru
    - [x] **TVRT-02**: Retail & e-commerce vertical reaches ≥80 published templates spanning ≥8 distinct template types — flash sale / promo banner, new arrivals, product spotlight, seasonal campaign, social proof / UGC, loyalty / rewards, wayfinding, hours / window display
    - [x] **TVRT-03**: Healthcare & wellness vertical reaches ≥80 published templates spanning ≥8 distinct template types — waiting-room ambient, wait time / queue status, health tip, appointment reminder, provider directory, vaccination / screening reminder, emergency / safety alert, clinic hours / pharmacy info
    - [x] **TVRT-04**: Every net-new template carries a `vertical` tag (`restaurants` | `retail` | `healthcare`) so downstream search and filter can target it
    - [x] **TVRT-05**: Existing 15 generic categories continue to filter correctly post-tagging (no regression in the v20.0 category filter UX)
    ```

    **Edit 3 — Flip 9 Traceability table rows.**

    Find (verbatim):
    ```markdown
    | TCAT-01 | 178 | Pending |
    | TCAT-02 | 178 | Pending |
    | TCAT-03 | 178 | Pending |
    | TCAT-04 | 178 | Pending |
    | TVRT-01 | 178 | Pending |
    | TVRT-02 | 178 | Pending |
    | TVRT-03 | 178 | Pending |
    | TVRT-04 | 178 | Pending |
    | TVRT-05 | 178 | Pending |
    ```

    Replace with (each row gets the literal cross-reference `Phase 178 — 178-VERIFICATION.md commit 04ed5938` plus the 1-line per-ID evidence from v21.0-MILESTONE-AUDIT.md lines 126-140):
    ```markdown
    | TCAT-01 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: gallery_templates COUNT=485, ≥427 floor; verified 2026-05-10) |
    | TCAT-02 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: all 6 hero types ship both portrait + landscape orientations live; verified 2026-05-10) |
    | TCAT-03 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: zero validator failures in net-new content; all 357 published rows passed validator + resvg rasterize + S3 PUT gate) |
    | TCAT-04 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: chk_svg_templates_category_enum constraint definition unchanged through ingestion; gallery_templates VIEW invariants preserved) |
    | TVRT-01 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: 138 active Restaurants templates spanning 12 distinct template types — floor ≥80 cleared, ≥6 type-distinct floor cleared) |
    | TVRT-02 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: 129 active Retail templates spanning 11 distinct template types) |
    | TVRT-03 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: 126 active Healthcare templates spanning 10 distinct template types) |
    | TVRT-04 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: zero null-vertical rows in net-new content; every published row carries restaurants \| retail \| healthcare tag) |
    | TVRT-05 | 178 | Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: 9/9 SC checks GREEN per scripts/verify-178-counts.cjs; v20.0 invariants preserved; programmatic data-shape invariants pass — live Playwright sweep delegated to Phase 180 Plan 05) |
    ```

    Notes:
    - Each Traceability row's Status cell now contains: `Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: <evidence>)`. The literal string `04ed5938` appears 9 times (once per row), satisfying the grep-gate `grep -c "04ed5938" .planning/REQUIREMENTS.md` returns ≥ 9.
    - The TVRT-04 row uses `restaurants \| retail \| healthcare` with escaped `\|` because the markdown table column delimiter is `|` and unescaped pipes would split the cell. This matches the audit's verified evidence wording.
    - The TVRT-05 row notes that the live Playwright sweep is delegated to Phase 180 Plan 05 (the deferred CI gate) — this preserves traceability and avoids overclaiming.

    **Edit 4 — Refresh Coverage block at the bottom of REQUIREMENTS.md.**

    Find (verbatim):
    ```markdown
    **Coverage:**
    - v1 requirements: 24 total (was 23 at initial definition; TGEN-06 added 2026-05-06)
    - Mapped to phases: 24 (Phase 177: 10, Phase 178: 9, Phase 179: 5)
    - Unmapped: 0 — 100% coverage achieved
    ```

    Replace with:
    ```markdown
    **Coverage:**
    - v1 requirements: 24 total (was 23 at initial definition; TGEN-06 added 2026-05-06)
    - Mapped to phases: 24 (Phase 177: 10, Phase 178: 9, Phase 179: 5)
    - Unmapped: 0 — 100% coverage achieved
    - **Satisfied: 24/24** (codebase tier; Phase 178 traceability flip applied 2026-05-11 per v21.0-MILESTONE-AUDIT.md SC-4; Phase 179 TVRZ-02/03/04/05 satisfied at codebase tier with empirical CI confirmation tracked in 179-HUMAN-UAT.md and exercised live in Phase 180 Plan 05)
    ```

    Notes:
    - The "Satisfied: 24/24" line is the explicit closure marker. It preserves the previous lines (no destructive overwrite) and adds the new satisfied tally.
    - The note about "Phase 178 traceability flip applied 2026-05-11" is the cross-reference to this very plan.
    - The note about "TVRZ-02/03/04/05 satisfied at codebase tier with empirical CI confirmation tracked in 179-HUMAN-UAT.md" reflects audit reality and doesn't overclaim (those rows are already accurately worded in their existing Traceability rows).
  </action>
  <verify>
    <automated>
      [ "$(grep -c '^- \[x\] \*\*TCAT-0[1-4]\*\*' .planning/REQUIREMENTS.md)" = "4" ] && \
      [ "$(grep -c '^- \[x\] \*\*TVRT-0[1-5]\*\*' .planning/REQUIREMENTS.md)" = "5" ] && \
      [ "$(grep -c '^| TCAT-0[1-4] | 178 | Pending |' .planning/REQUIREMENTS.md)" = "0" ] && \
      [ "$(grep -c '^| TVRT-0[1-5] | 178 | Pending |' .planning/REQUIREMENTS.md)" = "0" ] && \
      grep -c "04ed5938" .planning/REQUIREMENTS.md | awk '$1 >= 9 { exit 0 } { exit 1 }' && \
      grep -c "Satisfied: 24/24" .planning/REQUIREMENTS.md | grep -q '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*TCAT-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns EXACTLY 4 (all 4 TCAT boxes flipped to `[x]`)
    - `grep -c "^- \[x\] \*\*TVRT-0[1-5]\*\*" .planning/REQUIREMENTS.md` returns EXACTLY 5 (all 5 TVRT boxes flipped to `[x]`)
    - `grep -c "^- \[ \] \*\*TCAT-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns EXACTLY 0 (no remaining unchecked TCAT boxes)
    - `grep -c "^- \[ \] \*\*TVRT-0[1-5]\*\*" .planning/REQUIREMENTS.md` returns EXACTLY 0 (no remaining unchecked TVRT boxes)
    - `grep -c "^| TCAT-0[1-4] | 178 | Pending |" .planning/REQUIREMENTS.md` returns EXACTLY 0
    - `grep -c "^| TVRT-0[1-5] | 178 | Pending |" .planning/REQUIREMENTS.md` returns EXACTLY 0
    - `grep -c "| TCAT-0[1-4] | 178 | Complete" .planning/REQUIREMENTS.md` returns EXACTLY 4
    - `grep -c "| TVRT-0[1-5] | 178 | Complete" .planning/REQUIREMENTS.md` returns EXACTLY 5
    - `grep -c "04ed5938" .planning/REQUIREMENTS.md` returns ≥ 9 (one cross-reference per flipped row)
    - `grep -c "Satisfied: 24/24" .planning/REQUIREMENTS.md` returns ≥ 1 (Coverage block refreshed)
    - **Untouched-rows gate (TGEN/TADM/TVRZ rows must remain exactly as-is):** `git diff .planning/REQUIREMENTS.md | grep -E "^[+-]\| (TGEN|TADM|TVRZ)" | wc -l` returns EXACTLY 0 (no TGEN/TADM/TVRZ rows touched)
    - **Untouched-checklists gate:** `git diff .planning/REQUIREMENTS.md | grep -E "^[+-]- \[.\] \*\*(TGEN|TADM|TVRZ)-" | wc -l` returns EXACTLY 0 (no TGEN/TADM/TVRZ checklist boxes touched — those are governed by other phase verifications)
  </acceptance_criteria>
  <done>
    9 v1 checklist boxes flipped `[ ] → [x]`. 9 Traceability rows flipped `Pending → Complete (Phase 178 — 178-VERIFICATION.md commit 04ed5938: ...)`. Coverage block adds `Satisfied: 24/24` line. No TGEN/TADM/TVRZ rows or checklists modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Phase 177 HUMAN-UAT.md item 4 result paragraph (closes SC-5)</name>
  <files>.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md (full file — confirms current item 4 wording)
    - .planning/v21.0-MILESTONE-AUDIT.md lines 169-171 (audit's authoritative correction spec)
    - .planning/phases/180-v21-launch-readiness/180-01-PLAN.md (this phase's Plan 01 — confirms the BLOCKER-1 closure note ties back to a real plan in this phase)
    - src/App.jsx lines 177-189 (the test:setCurrentPage listener gate — confirms the VITE_E2E_TEST_MODE condition wording in the corrected paragraph is technically accurate)
  </read_first>
  <behavior>
    - Phase 177 HUMAN-UAT.md item 4 result paragraph is rewritten so it accurately describes what was verified
    - The corrected wording explicitly states: (a) the actual verification path used in Phase 177 was the E2E `test:setCurrentPage` CustomEvent (only active under `VITE_E2E_TEST_MODE=1`); (b) the server-side defense-in-depth checks (is_admin/is_super_admin = false, EF 403 on action=approve, EF 403 on action=save_edit) all PASS and that is what gates a non-admin from reaching the queue's data even if they bypass the nav layer; (c) at Phase 177 close, the queue had NO production navigation entry point (BLOCKER-1 surfaced by the v21.0 milestone audit); (d) as of Phase 180 Plan 01, the queue IS reachable via the SuperAdminDashboardPage "Admin Tools" panel (BLOCKER-1 closed); (e) the prior assertion "reachable only via the SuperAdminDashboard 'Admin Tools' panel" was incorrect at the time of writing and has been corrected
    - The PASS verdict on TADM-04 (non-admin redirect) is preserved — the server-side defense-in-depth gates verified in Phase 177 are still valid; only the assertion about the navigation surface needed correction
    - `grep -c "incorrect at the time of writing\|prior assertion was incorrect" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (correction is explicitly flagged for any future reader)
    - `grep -c "Phase 180" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (cross-references the closure plan)
    - `grep -c "reachable only via" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns EXACTLY 0 (the incorrect phrase is removed)
  </behavior>
  <action>
    Replace the item 4 result paragraph in `.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md`. Use the Edit tool with the verbatim old_string / new_string blocks below.

    **Find (verbatim, the full item 4 result line — note this is a single long line in the source file):**
    ```markdown
    result: PASS — non-admin sidebar contained the same 9 standard items (Welcome / Dashboard / Media / Apps / Playlists / Templates / Schedules / Screens / Social Moderation), no admin-only entries. Template Queue is reachable only via the SuperAdminDashboard "Admin Tools" panel which doesn't render for non-admin users. Server-side defense-in-depth verified via curl with the non-admin JWT: `is_admin()` returns `false`, `is_super_admin()` returns `false`, EF call with `action=approve` returns HTTP 403, EF call with `action=save_edit` returns HTTP 403. (Test-mode `CustomEvent('test:setCurrentPage', ...)` did render the page UI for the non-admin, but that listener only registers when `VITE_E2E_TEST_MODE=1` is set in vite env — not present in production builds, so this is not a real attack vector.)
    ```

    **Replace with (verbatim — a multi-line corrected result block):**
    ```markdown
    result: PASS — non-admin sidebar contained the same 9 standard items (Welcome / Dashboard / Media / Apps / Playlists / Templates / Schedules / Screens / Social Moderation), no admin-only entries. The actual verification path used at Phase 177 close was the E2E `test:setCurrentPage` CustomEvent — that listener only registers when `VITE_E2E_TEST_MODE=1` is set in vite env (src/App.jsx:177-189), not active in production builds. Server-side defense-in-depth verified via curl with the non-admin JWT: `is_admin()` returns `false`, `is_super_admin()` returns `false`, EF call with `action=approve` returns HTTP 403, EF call with `action=save_edit` returns HTTP 403 — these are the load-bearing gates that prevent a non-admin from reaching draft data even if they bypass the nav layer. The TADM-04 PASS verdict stands on the server-side gates. **Correction (2026-05-11, Phase 180 SC-5):** The prior assertion "reachable only via the SuperAdminDashboard 'Admin Tools' panel which doesn't render for non-admin users" was incorrect at the time of writing — at Phase 177 close, the Admin Tools panel had no Template Queue tile (v21.0-MILESTONE-AUDIT.md BLOCKER-1). The production-admin navigation entry point did not exist; the queue was only reachable from VITE_E2E_TEST_MODE-enabled builds via CustomEvent. **As of Phase 180 Plan 01, BLOCKER-1 is closed:** the SuperAdminDashboardPage Admin Tools panel now contains an "AI Template Queue" tile that routes super_admins to AdminTemplateQueuePage through the standard production navigation path (no CustomEvent dependency). Phase 180 Plan 01's Playwright spec (`tests/e2e/admin-template-queue-nav.spec.js`) exercises the corrected production path end-to-end.
    ```

    Notes for the executor:
    - The replacement is a single Edit operation. The old_string MUST match the source line VERBATIM (it's a single long markdown line in the file).
    - The replacement preserves the PASS verdict on TADM-04 because the server-side defense-in-depth gates verified in Phase 177 are still valid — only the assertion about the navigation surface was wrong.
    - The bold "**Correction (2026-05-11, Phase 180 SC-5):**" sentinel makes the correction easy to spot for any future reader.
    - The cross-reference to `Phase 180 Plan 01` and `tests/e2e/admin-template-queue-nav.spec.js` ties this correction back to the active closure work in this phase.
    - Do NOT modify any other line in the file (items 1, 2, 3, BL-NEW-02 verification, Summary, Gaps).
  </action>
  <verify>
    <automated>
      [ "$(grep -c 'reachable only via' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md)" = "0" ] && \
      grep -c 'Phase 180' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -q '^[1-9]' && \
      grep -c 'Correction (2026-05-11, Phase 180 SC-5):' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -q '^[1-9]' && \
      grep -c 'incorrect at the time of writing' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -q '^[1-9]' && \
      grep -c 'BLOCKER-1' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -q '^[1-9]' && \
      grep -c 'PASS — non-admin sidebar contained' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -q '^[1-9]'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reachable only via" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns EXACTLY 0 (the incorrect phrase has been removed)
    - `grep -c "Phase 180" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (cross-reference to the closure plan)
    - `grep -c "Correction (2026-05-11, Phase 180 SC-5):" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns EXACTLY 1 (correction sentinel for future readers)
    - `grep -c "incorrect at the time of writing" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (correction is explicitly flagged)
    - `grep -c "BLOCKER-1" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (audit reference present)
    - `grep -c "VITE_E2E_TEST_MODE" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (technical accuracy preserved)
    - **PASS verdict preserved:** `grep -c "PASS — non-admin sidebar contained" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns ≥ 1 (TADM-04 verdict stands)
    - **Untouched-items gate:** `git diff .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -cE "^[+-]### [123]\."` returns EXACTLY 0 (items 1, 2, 3 headings unchanged)
    - **Untouched-items gate:** `git diff .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md | grep -cE "^[+-]## (BL-NEW-02|Summary|Gaps)"` returns EXACTLY 0 (BL-NEW-02, Summary, Gaps sections unchanged)
    - **`tests/e2e/admin-template-queue-nav.spec.js` reference present:** `grep -c "admin-template-queue-nav.spec.js" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` returns EXACTLY 1 (links forward to Plan 01's spec)
  </acceptance_criteria>
  <done>
    Item 4 result paragraph rewritten. Incorrect "reachable only via" phrase eliminated. Correction sentinel + Phase 180 cross-reference + BLOCKER-1 reference all present. PASS verdict preserved. Items 1/2/3/BL-NEW-02/Summary/Gaps untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies markdown documentation only. No executable code paths, auth gates, schema files, or runtime services are touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-06 | (n/a) | Doc-only changes | accept | No executable code paths modified. Two markdown files modified: `.planning/REQUIREMENTS.md` (traceability flips) and `.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md` (item 4 wording correction). Accept — no production attack surface. |
| T-180-07 | Repudiation | Backdated correction obscures audit history | mitigate | The corrected HUMAN-UAT.md item 4 paragraph EXPLICITLY flags the correction with a bold "**Correction (2026-05-11, Phase 180 SC-5):**" sentinel and explains what was incorrect about the original wording. The original Phase 177 testing evidence (server-side gates, JWT-based curl probes) is preserved — only the misleading sentence about the navigation surface is corrected. Future readers see both what was wrong and why it was wrong. Acceptance criterion `grep -c "incorrect at the time of writing" ... returns >= 1` enforces this. **LOW severity** once gate is exercised. |
</threat_model>

<verification>
After Tasks 1 + 2 complete:

```bash
# SC-4 gates
[ "$(grep -c '^- \[x\] \*\*TCAT-0[1-4]\*\*' .planning/REQUIREMENTS.md)" = "4" ]
[ "$(grep -c '^- \[x\] \*\*TVRT-0[1-5]\*\*' .planning/REQUIREMENTS.md)" = "5" ]
[ "$(grep -c '^| TCAT-0[1-4] | 178 | Pending |' .planning/REQUIREMENTS.md)" = "0" ]
[ "$(grep -c '^| TVRT-0[1-5] | 178 | Pending |' .planning/REQUIREMENTS.md)" = "0" ]
grep -c "04ed5938" .planning/REQUIREMENTS.md         # >= 9
grep -c "Satisfied: 24/24" .planning/REQUIREMENTS.md  # >= 1

# SC-5 gates
[ "$(grep -c 'reachable only via' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md)" = "0" ]
grep -c "Correction (2026-05-11, Phase 180 SC-5):" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md   # == 1
grep -c "BLOCKER-1" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-HUMAN-UAT.md   # >= 1
```

All gates must pass before the plan is marked complete.
</verification>

<success_criteria>
- SC-4: 9 traceability rows + 9 v1 checklist boxes flipped; commit hash 04ed5938 referenced ≥9× in REQUIREMENTS.md; Coverage block adds Satisfied: 24/24 line
- SC-5: Phase 177 HUMAN-UAT.md item 4 paragraph rewritten with the bold Correction sentinel and Phase 180 cross-reference; the incorrect "reachable only via" phrase is eliminated; PASS verdict preserved
- No TGEN/TADM/TVRZ rows or other Phase 177 HUMAN-UAT items are touched
</success_criteria>

<output>
After completion, create `.planning/phases/180-v21-launch-readiness/180-03-SUMMARY.md` per the GSD summary template. Include:
- Verbatim diff snippets showing the 9 TCAT/TVRT traceability flips
- Verbatim diff snippet showing the v1 checklist box flips
- Verbatim diff snippet showing the Coverage block refresh
- Verbatim diff snippet showing the HUMAN-UAT item 4 rewrite
- Output of all acceptance_criteria grep gates
- Commit hash for the (single) commit covering both Tasks (this is a doc-atomic plan)
</output>
