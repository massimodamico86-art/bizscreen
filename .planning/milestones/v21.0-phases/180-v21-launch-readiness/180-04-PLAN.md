---
phase: 180-v21-launch-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md
  - .planning/phases/178-vertical-content-seeding/178-VALIDATION.md
  - .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md
autonomous: true
requirements: []
requirements_addressed: []
gap_closure: true
tags: [phase-180, validation, nyquist, frontmatter-refresh, wave-0, doc-correction]

must_haves:
  truths:
    - "Phase 177 VALIDATION.md frontmatter accurately reflects post-execution state: nyquist_compliant: true, wave_0_complete: true — verified by spot-checking 177-01-SUMMARY.md confirms the 5 RED test files + Docker/CLI prereqs were exercised at Wave 0"
    - "Phase 178 VALIDATION.md frontmatter accurately reflects post-execution state — verified by spot-checking 178-01-SUMMARY.md confirms the 5 RED test files + verify-178-counts.cjs + validate-templates.cjs were exercised at Wave 0"
    - "Phase 179 VALIDATION.md frontmatter accurately reflects post-execution state — verified by spot-checking 179-03-SUMMARY.md confirms the 3 RED scaffold files + axe-core install + tests-collect-pass were exercised at Wave 0"
    - "Each flip is anchored to a cross-reference in the SUMMARY.md file that proves Wave 0 was actually exercised — no blind flips"
  artifacts:
    - path: ".planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md"
      provides: "Refreshed frontmatter — nyquist_compliant: true, wave_0_complete: true; signed off by Phase 180 Plan 04 cross-referencing 177-01-SUMMARY.md Wave 0 evidence"
    - path: ".planning/phases/178-vertical-content-seeding/178-VALIDATION.md"
      provides: "Refreshed frontmatter — nyquist_compliant: true, wave_0_complete: true; signed off by Phase 180 Plan 04 cross-referencing 178-01-SUMMARY.md Wave 0 evidence"
    - path: ".planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md"
      provides: "Refreshed frontmatter — nyquist_compliant: true, wave_0_complete: true; signed off by Phase 180 Plan 04 cross-referencing 179-03-SUMMARY.md Wave 0 evidence"
  key_links:
    - from: "177-VALIDATION.md frontmatter (nyquist_compliant + wave_0_complete)"
      to: "177-01-SUMMARY.md (Wave 0 RED scaffold creation evidence)"
      via: "comment in VALIDATION.md citing specific SUMMARY.md sections"
      pattern: "177-01-SUMMARY.md"
    - from: "178-VALIDATION.md frontmatter (nyquist_compliant + wave_0_complete)"
      to: "178-01-SUMMARY.md (Wave 0 RED test creation evidence)"
      via: "comment in VALIDATION.md citing specific SUMMARY.md sections"
      pattern: "178-01-SUMMARY.md"
    - from: "179-VALIDATION.md frontmatter (nyquist_compliant + wave_0_complete)"
      to: "179-03-SUMMARY.md (Wave 0 RED scaffold creation evidence)"
      via: "comment in VALIDATION.md citing specific SUMMARY.md sections"
      pattern: "179-03-SUMMARY.md"
---

<objective>
Close SC-6 from ROADMAP.md line 168: refresh the `nyquist_compliant` and `wave_0_complete` frontmatter fields in `.planning/phases/177-*/177-VALIDATION.md`, `.planning/phases/178-*/178-VALIDATION.md`, and `.planning/phases/179-*/179-VALIDATION.md` — **only after each phase's Wave 0 evidence has been spot-checked against the existing SUMMARY.md files (don't flip blind)**.

Purpose: The v21.0 milestone audit's nyquist scoreboard (v21.0-MILESTONE-AUDIT.md lines 217-224) records all three phases as `nyquist_compliant: false` / `wave_0_complete: false` despite the audit explicitly noting that "Phase 177/178/179 all exercised Wave 0 RED gates per their plan SUMMARYs; VALIDATION.md frontmatter just never got the `nyquist_compliant: true / wave_0_complete: true` bump." This is a tracking-only gap — the SUMMARY.md files already prove Wave 0 was exercised; the frontmatter just never got refreshed post-execution.

Output: Three VALIDATION.md frontmatter blocks updated with `nyquist_compliant: true` + `wave_0_complete: true`, each anchored by an inline comment that cites the specific SUMMARY.md evidence section. The audit's "spot-check before flip" requirement is satisfied via the `<read_first>` mandate on each task — the executor MUST open each phase's Plan 01 (or Plan 03 for Phase 179) SUMMARY.md before flipping its VALIDATION.md frontmatter, so the flip is observably evidence-based.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v21.0-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/phases/180-v21-launch-readiness/

<interfaces>
<!-- Concrete contracts the executor needs. -->

### Current state of all three VALIDATION.md frontmatters (verbatim — all three are identical in shape)

**177-VALIDATION.md lines 1-8:**
```yaml
---
phase: 177
slug: ai-generation-pipeline-admin-queue-ui
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-06
---
```

**178-VALIDATION.md lines 1-8:**
```yaml
---
phase: 178
slug: vertical-content-seeding
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-09
---
```

**179-VALIDATION.md lines 1-8:**
```yaml
---
phase: 179
slug: gallery-virtualization-launch-validation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-10
---
```

### Wave 0 evidence cross-references (from existing SUMMARY.md files — these are the spot-check anchors)

**Phase 177 — 177-01-SUMMARY.md provides Wave 0 evidence:**
- `requirements-completed: [TGEN-01, TGEN-02, TGEN-03, TGEN-05, TADM-03]` (Wave 0 RED test contracts)
- `key-files: created:` lists `tests/integration/edgeFunctionSpike.test.js (47 LOC) — Wave 0 RED spike contract`, `tests/integration/generateSvgTemplate.test.js (67 LOC) — Wave 1 RED contract`, `tests/integration/approveDraftPipeline.test.js (76 LOC) — Wave 2 RED contract`, `tests/integration/promptLibraryParity.test.js (32 LOC) — Wave 1 RED contract`, `tests/unit/generateValidatorOrder.test.js (68 LOC) — Wave 1 RED contract`
- All 5 RED tests in Wave 0 Requirements (177-VALIDATION.md lines 63-68) shipped per 177-01-SUMMARY.md key-files block

**Phase 178 — 178-01-SUMMARY.md provides Wave 0 evidence:**
- `tags: [vitest, playwright, supabase, validator, red-tests, nyquist-gate]` (Wave 0 Nyquist gate exercised)
- `key-files: created:` lists 5 new RED test files + `scripts/verify-178-counts.cjs` + `scripts/validate-templates.cjs`
- All Wave 0 Requirements (178-VALIDATION.md lines 49-57) shipped per 178-01-SUMMARY.md key-files block

**Phase 179 — 179-03-SUMMARY.md provides Wave 0 evidence:**
- `tags: [virtualization, test-scaffold, nyquist-wave-0, vitest, playwright, axe-core, cdp-throttle]` (Wave 0 explicitly tagged)
- `key-files: created:` lists `tests/unit/components/VirtualizedTemplateGrid.test.jsx`, `tests/e2e/template-gallery-perf.spec.js`, `tests/e2e/template-gallery-axe.spec.js`
- The 5 Wave 0 items in 179-VALIDATION.md lines 62-67 (axe-core install via Plan 01 + 5 test scaffolds via Plan 03) shipped per 179-01-SUMMARY.md + 179-03-SUMMARY.md key-files blocks

### Audit's authoritative wording on this gap (verbatim from v21.0-MILESTONE-AUDIT.md lines 215-224)

> "Nyquist Compliance | Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
> | 176 | missing | n/a | n/a | MISSING (by design — schema-only, no Wave 0) |
> | 177 | exists | false (stale) | false (stale) | PARTIAL (Wave 0 RED gate executed per Plan 01 SUMMARY — 5 RED tests + Docker/CLI prereqs; frontmatter never refreshed post-execution) |
> | 178 | exists | false (stale) | false (stale) | PARTIAL (Wave 0 RED gate executed per Plan 01 SUMMARY — 5 RED test files + verify-178-counts.cjs + validate-templates.cjs; frontmatter never refreshed) |
> | 179 | exists | false (stale) | false (stale) | PARTIAL (Wave 0 Nyquist RED scaffolds executed per Plan 03 SUMMARY; frontmatter never refreshed) |
> **Tracking-only gap, not a real compliance failure.**"

### ROADMAP SC-6 wording (line 168)

> "VALIDATION.md frontmatter refresh: `nyquist_compliant: true` and `wave_0_complete: true` set in `.planning/phases/177-*/VALIDATION.md`, `.planning/phases/178-*/VALIDATION.md`, and `.planning/phases/179-*/VALIDATION.md` — only after each phase's Wave 0 evidence has been spot-checked against the existing SUMMARY.md files (don't flip blind)"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spot-check Phase 177 Wave 0 evidence + flip 177-VALIDATION.md frontmatter (SC-6 cluster A)</name>
  <files>.planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md</files>
  <read_first>
    - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md (full file — confirms current frontmatter + Wave 0 Requirements list at lines 60-68)
    - .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-01-SUMMARY.md (full file — the Wave 0 evidence anchor; confirm the 5 RED test files in key-files block + Wave 0 deliverable matches VALIDATION.md Wave 0 Requirements)
    - **Spot-check evidence:** verify `grep -c "edgeFunctionSpike\|generateSvgTemplate\|approveDraftPipeline\|promptLibraryParity\|generateValidatorOrder" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-01-SUMMARY.md` returns ≥ 5 (all 5 RED test files mentioned in the SUMMARY's key-files block)
    - **Spot-check evidence:** verify the 5 RED test files physically exist via `ls tests/integration/edgeFunctionSpike.test.js tests/integration/generateSvgTemplate.test.js tests/integration/approveDraftPipeline.test.js tests/integration/promptLibraryParity.test.js tests/unit/generateValidatorOrder.test.js` returns no errors
  </read_first>
  <behavior>
    - 177-VALIDATION.md frontmatter `nyquist_compliant` flipped from `false` to `true`
    - 177-VALIDATION.md frontmatter `wave_0_complete` flipped from `false` to `true`
    - 177-VALIDATION.md frontmatter `status` flipped from `draft` to `approved`
    - 177-VALIDATION.md frontmatter gains a new `wave_0_evidence_ref` field naming `177-01-SUMMARY.md` as the cross-reference (this is the anti-blind-flip safeguard required by ROADMAP SC-6)
    - 177-VALIDATION.md frontmatter gains a new `validation_signed_off: 2026-05-11` field (the closure date, matching the milestone audit date)
    - No body content of 177-VALIDATION.md is modified — only the frontmatter block (lines 1-8)
  </behavior>
  <action>
    First, perform the **mandatory spot-check** (per ROADMAP SC-6 "don't flip blind" rule). Run these commands and verify all pass before editing:

    ```bash
    # Spot-check 1: SUMMARY.md mentions all 5 RED test files
    grep -c "edgeFunctionSpike\|generateSvgTemplate\|approveDraftPipeline\|promptLibraryParity\|generateValidatorOrder" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-01-SUMMARY.md
    # Expected: >= 5

    # Spot-check 2: The 5 RED test files physically exist
    ls tests/integration/edgeFunctionSpike.test.js tests/integration/generateSvgTemplate.test.js tests/integration/approveDraftPipeline.test.js tests/integration/promptLibraryParity.test.js tests/unit/generateValidatorOrder.test.js
    # Expected: exit 0 (all 5 files exist)
    ```

    If either spot-check fails, ABORT the task and report the gap — the audit's "tracking-only" assertion may be incorrect for this phase. If both pass, proceed.

    Now replace the frontmatter block (lines 1-8) using the Edit tool.

    **Find (verbatim):**
    ```yaml
    ---
    phase: 177
    slug: ai-generation-pipeline-admin-queue-ui
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-05-06
    ---
    ```

    **Replace with:**
    ```yaml
    ---
    phase: 177
    slug: ai-generation-pipeline-admin-queue-ui
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-06
    validation_signed_off: 2026-05-11
    wave_0_evidence_ref: "177-01-SUMMARY.md key-files block — 5 RED test files created: tests/integration/edgeFunctionSpike.test.js (47 LOC), tests/integration/generateSvgTemplate.test.js (67 LOC), tests/integration/approveDraftPipeline.test.js (76 LOC), tests/integration/promptLibraryParity.test.js (32 LOC), tests/unit/generateValidatorOrder.test.js (68 LOC). All 5 RED tests committed before any production code per Plan 01 Wave 0 deliverable; downstream waves (Plan 02 onward) flipped them to GREEN. Cross-reference: Phase 180 Plan 04 spot-check 2026-05-11 confirms files physically present + 177-01-SUMMARY.md cites them in key-files."
    ---
    ```

    Notes:
    - The new `validation_signed_off` field uses ISO date (matches the milestone audit date 2026-05-11)
    - The new `wave_0_evidence_ref` field is a long-form string citing the specific SUMMARY.md evidence — this is the anti-blind-flip safeguard
    - `status: draft` → `status: approved` reflects the fact that Wave 0 sign-off is now applied (per the existing "Approval: pending" line at the bottom of the file at line 93 — the approval is now applied)
    - Do not modify any line below line 8 (the body of 177-VALIDATION.md including the optional "Approval: pending — flips to `approved YYYY-MM-DD` when Wave 0 RED tests are committed." line stays as historical reference; the frontmatter is the load-bearing source of truth per ROADMAP SC-6)
  </action>
  <verify>
    <automated>
      grep -c "edgeFunctionSpike\|generateSvgTemplate\|approveDraftPipeline\|promptLibraryParity\|generateValidatorOrder" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-01-SUMMARY.md | awk '$1 >= 5 { exit 0 } { exit 1 }' && \
      ls tests/integration/edgeFunctionSpike.test.js tests/integration/generateSvgTemplate.test.js tests/integration/approveDraftPipeline.test.js tests/integration/promptLibraryParity.test.js tests/unit/generateValidatorOrder.test.js > /dev/null 2>&1 && \
      grep -c "nyquist_compliant: true" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md | grep -q '^1$' && \
      grep -c "wave_0_complete: true" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md | grep -q '^1$' && \
      grep -c "wave_0_evidence_ref:" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md | grep -q '^1$' && \
      grep -c "validation_signed_off: 2026-05-11" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md | grep -q '^1$' && \
      [ "$(grep -c 'nyquist_compliant: false' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md)" = "0" ] && \
      [ "$(grep -c 'wave_0_complete: false' .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - **Spot-check gate (anti-blind-flip):** `grep -c "edgeFunctionSpike\|generateSvgTemplate\|approveDraftPipeline\|promptLibraryParity\|generateValidatorOrder" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-01-SUMMARY.md` returns ≥ 5
    - **Spot-check gate (file existence):** `ls tests/integration/edgeFunctionSpike.test.js tests/integration/generateSvgTemplate.test.js tests/integration/approveDraftPipeline.test.js tests/integration/promptLibraryParity.test.js tests/unit/generateValidatorOrder.test.js` exits 0
    - `grep -c "nyquist_compliant: true" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md` returns EXACTLY 1
    - `grep -c "wave_0_complete: true" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md` returns EXACTLY 1
    - `grep -c "wave_0_evidence_ref:" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md` returns EXACTLY 1
    - `grep -c "validation_signed_off: 2026-05-11" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md` returns EXACTLY 1
    - `grep -c "nyquist_compliant: false" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md` returns EXACTLY 0 (no stale false value)
    - `grep -c "wave_0_complete: false" .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md` returns EXACTLY 0 (no stale false value)
    - **Untouched-body gate:** `git diff .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md | grep -E "^[+-]" | grep -v "^[+-][+-][+-]" | wc -l` returns ≤ 20 (the +/- delta is bounded to the frontmatter block; rough budget: 5 lines removed + ~10 lines added)
    - **Untouched-body gate:** No `## ` headings should appear in the diff — `git diff .planning/phases/177-ai-generation-pipeline-admin-queue-ui/177-VALIDATION.md | grep -cE "^[+-]## "` returns EXACTLY 0
  </acceptance_criteria>
  <done>
    Spot-check passes (5 RED test files exist + SUMMARY.md cites them). 177-VALIDATION.md frontmatter shows nyquist_compliant: true, wave_0_complete: true, status: approved, validation_signed_off: 2026-05-11, wave_0_evidence_ref: <long-form citation>. No stale `false` values remain. Body content below line 8 is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spot-check Phase 178 Wave 0 evidence + flip 178-VALIDATION.md frontmatter (SC-6 cluster B)</name>
  <files>.planning/phases/178-vertical-content-seeding/178-VALIDATION.md</files>
  <read_first>
    - .planning/phases/178-vertical-content-seeding/178-VALIDATION.md (full file — confirms current frontmatter + Wave 0 Requirements list at lines 49-57)
    - .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md (full file — the Wave 0 evidence anchor; confirm the 5 RED test files in key-files block + verify-178-counts.cjs + validate-templates.cjs match VALIDATION.md Wave 0 Requirements)
    - **Spot-check evidence:** verify `grep -c "generateOrientation\|approveBulk\|rejectBulk\|templateDraftsService.bulk\|seedTopics.schema" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md` returns ≥ 5 (all 5 RED test files cited)
    - **Spot-check evidence:** verify `grep -c "verify-178-counts.cjs\|validate-templates.cjs" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md` returns ≥ 2 (the 2 CLI tools cited)
    - **Spot-check evidence:** verify file existence via `ls tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js scripts/verify-178-counts.cjs scripts/validate-templates.cjs` returns no errors
  </read_first>
  <behavior>
    - 178-VALIDATION.md frontmatter `nyquist_compliant` flipped from `false` to `true`
    - 178-VALIDATION.md frontmatter `wave_0_complete` flipped from `false` to `true`
    - 178-VALIDATION.md frontmatter `status` flipped from `draft` to `approved`
    - 178-VALIDATION.md frontmatter gains `wave_0_evidence_ref` field naming `178-01-SUMMARY.md` + the specific Wave 0 artifacts created
    - 178-VALIDATION.md frontmatter gains `validation_signed_off: 2026-05-11` field
    - No body content of 178-VALIDATION.md is modified — only the frontmatter block (lines 1-8)
  </behavior>
  <action>
    First, perform the **mandatory spot-check** (per ROADMAP SC-6 "don't flip blind" rule):

    ```bash
    # Spot-check 1: SUMMARY.md mentions all 5 RED test files
    grep -c "generateOrientation\|approveBulk\|rejectBulk\|templateDraftsService.bulk\|seedTopics.schema" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md
    # Expected: >= 5

    # Spot-check 2: SUMMARY.md mentions the 2 verification CLI tools
    grep -c "verify-178-counts.cjs\|validate-templates.cjs" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md
    # Expected: >= 2

    # Spot-check 3: All Wave 0 artifacts physically exist
    ls tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js scripts/verify-178-counts.cjs scripts/validate-templates.cjs
    # Expected: exit 0
    ```

    If any spot-check fails, ABORT and report. If all pass, proceed.

    **Find (verbatim):**
    ```yaml
    ---
    phase: 178
    slug: vertical-content-seeding
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-05-09
    ---
    ```

    **Replace with:**
    ```yaml
    ---
    phase: 178
    slug: vertical-content-seeding
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-09
    validation_signed_off: 2026-05-11
    wave_0_evidence_ref: "178-01-SUMMARY.md key-files block — 5 RED test files created: tests/unit/generateOrientation.test.js, tests/integration/approveBulk.test.js, tests/integration/rejectBulk.test.js, tests/integration/templateDraftsService.bulk.test.js, tests/integration/seedTopics.schema.test.js; plus 2 verification CLI tools: scripts/verify-178-counts.cjs (9 SC harness for TVRT-01..04 + TCAT-01..04) and scripts/validate-templates.cjs (svgValidator bulk CLI). All Wave 0 artifacts committed before any production code per Plan 01 Wave 0 deliverable; downstream waves (Plans 02-08) flipped RED tests to GREEN and consumed the verification tools. Cross-reference: Phase 180 Plan 04 spot-check 2026-05-11 confirms files physically present + 178-01-SUMMARY.md cites them in key-files."
    ---
    ```
  </action>
  <verify>
    <automated>
      grep -c "generateOrientation\|approveBulk\|rejectBulk\|templateDraftsService.bulk\|seedTopics.schema" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md | awk '$1 >= 5 { exit 0 } { exit 1 }' && \
      grep -c "verify-178-counts.cjs\|validate-templates.cjs" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md | awk '$1 >= 2 { exit 0 } { exit 1 }' && \
      ls tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js scripts/verify-178-counts.cjs scripts/validate-templates.cjs > /dev/null 2>&1 && \
      grep -c "nyquist_compliant: true" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md | grep -q '^1$' && \
      grep -c "wave_0_complete: true" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md | grep -q '^1$' && \
      grep -c "wave_0_evidence_ref:" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md | grep -q '^1$' && \
      grep -c "validation_signed_off: 2026-05-11" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md | grep -q '^1$' && \
      [ "$(grep -c 'nyquist_compliant: false' .planning/phases/178-vertical-content-seeding/178-VALIDATION.md)" = "0" ] && \
      [ "$(grep -c 'wave_0_complete: false' .planning/phases/178-vertical-content-seeding/178-VALIDATION.md)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - **Spot-check gate (anti-blind-flip):** `grep -c "generateOrientation\|approveBulk\|rejectBulk\|templateDraftsService.bulk\|seedTopics.schema" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md` returns ≥ 5
    - **Spot-check gate (CLI tools):** `grep -c "verify-178-counts.cjs\|validate-templates.cjs" .planning/phases/178-vertical-content-seeding/178-01-SUMMARY.md` returns ≥ 2
    - **Spot-check gate (file existence):** `ls tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js tests/integration/seedTopics.schema.test.js scripts/verify-178-counts.cjs scripts/validate-templates.cjs` exits 0
    - `grep -c "nyquist_compliant: true" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md` returns EXACTLY 1
    - `grep -c "wave_0_complete: true" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md` returns EXACTLY 1
    - `grep -c "wave_0_evidence_ref:" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md` returns EXACTLY 1
    - `grep -c "validation_signed_off: 2026-05-11" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md` returns EXACTLY 1
    - `grep -c "nyquist_compliant: false" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md` returns EXACTLY 0
    - `grep -c "wave_0_complete: false" .planning/phases/178-vertical-content-seeding/178-VALIDATION.md` returns EXACTLY 0
    - **Untouched-body gate:** `git diff .planning/phases/178-vertical-content-seeding/178-VALIDATION.md | grep -cE "^[+-]## "` returns EXACTLY 0
  </acceptance_criteria>
  <done>
    Spot-check passes. 178-VALIDATION.md frontmatter flipped with cross-reference to 178-01-SUMMARY.md key-files block. No stale `false` values. Body unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Spot-check Phase 179 Wave 0 evidence + flip 179-VALIDATION.md frontmatter (SC-6 cluster C)</name>
  <files>.planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md</files>
  <read_first>
    - .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md (full file — confirms current frontmatter + Wave 0 Requirements list at lines 62-67)
    - .planning/phases/179-gallery-virtualization-launch-validation/179-03-SUMMARY.md (full file — the Wave 0 evidence anchor; confirm the 3 RED scaffold files in key-files block; @axe-core/playwright install was a Plan 01 prerequisite cited via dependency_graph.requires)
    - **Spot-check evidence:** verify `grep -c "VirtualizedTemplateGrid.test.jsx\|template-gallery-perf.spec.js\|template-gallery-axe.spec.js" .planning/phases/179-gallery-virtualization-launch-validation/179-03-SUMMARY.md` returns ≥ 3 (all 3 RED scaffolds cited)
    - **Spot-check evidence:** verify `grep -c "@axe-core/playwright" .planning/phases/179-gallery-virtualization-launch-validation/179-01-SUMMARY.md` returns ≥ 1 (axe-core install cited in Plan 01's SUMMARY)
    - **Spot-check evidence:** verify file existence via `ls tests/unit/components/VirtualizedTemplateGrid.test.jsx tests/e2e/template-gallery-perf.spec.js tests/e2e/template-gallery-axe.spec.js` returns no errors
    - **Spot-check evidence:** verify package.json has `@axe-core/playwright` via `grep -c '"@axe-core/playwright"' package.json` returns ≥ 1
  </read_first>
  <behavior>
    - 179-VALIDATION.md frontmatter `nyquist_compliant` flipped from `false` to `true`
    - 179-VALIDATION.md frontmatter `wave_0_complete` flipped from `false` to `true`
    - 179-VALIDATION.md frontmatter `status` flipped from `draft` to `approved`
    - 179-VALIDATION.md frontmatter gains `wave_0_evidence_ref` field naming `179-03-SUMMARY.md` + `179-01-SUMMARY.md` (axe-core install)
    - 179-VALIDATION.md frontmatter gains `validation_signed_off: 2026-05-11` field
    - No body content of 179-VALIDATION.md is modified — only the frontmatter block (lines 1-8)
  </behavior>
  <action>
    First, perform the **mandatory spot-check** (per ROADMAP SC-6 "don't flip blind" rule):

    ```bash
    # Spot-check 1: 179-03-SUMMARY.md mentions all 3 RED scaffolds
    grep -c "VirtualizedTemplateGrid.test.jsx\|template-gallery-perf.spec.js\|template-gallery-axe.spec.js" .planning/phases/179-gallery-virtualization-launch-validation/179-03-SUMMARY.md
    # Expected: >= 3

    # Spot-check 2: 179-01-SUMMARY.md mentions axe-core install (the Wave 0 prerequisite)
    grep -c "@axe-core/playwright" .planning/phases/179-gallery-virtualization-launch-validation/179-01-SUMMARY.md
    # Expected: >= 1

    # Spot-check 3: All Wave 0 artifacts physically exist
    ls tests/unit/components/VirtualizedTemplateGrid.test.jsx tests/e2e/template-gallery-perf.spec.js tests/e2e/template-gallery-axe.spec.js
    # Expected: exit 0

    # Spot-check 4: @axe-core/playwright is in package.json
    grep -c '"@axe-core/playwright"' package.json
    # Expected: >= 1
    ```

    If any spot-check fails, ABORT and report. If all pass, proceed.

    **Find (verbatim):**
    ```yaml
    ---
    phase: 179
    slug: gallery-virtualization-launch-validation
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-05-10
    ---
    ```

    **Replace with:**
    ```yaml
    ---
    phase: 179
    slug: gallery-virtualization-launch-validation
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-10
    validation_signed_off: 2026-05-11
    wave_0_evidence_ref: "179-03-SUMMARY.md key-files block — 3 RED scaffold files created: tests/unit/components/VirtualizedTemplateGrid.test.jsx (SC-1 unit gate), tests/e2e/template-gallery-perf.spec.js (SC-2 perf gate with CDP throttle + catalog-floor pre-flight), tests/e2e/template-gallery-axe.spec.js (SC-5 axe-core zero-violations gate scoped to [role='grid']). Wave 0 prerequisite @axe-core/playwright install documented in 179-01-SUMMARY.md (package.json devDependency). All 3 RED scaffolds committed before production virtualizer code per Plan 03 deliverable; Plans 04/05/06/08 flipped them to GREEN. Cross-reference: Phase 180 Plan 04 spot-check 2026-05-11 confirms files physically present + 179-03-SUMMARY.md cites them in key-files + package.json has @axe-core/playwright."
    ---
    ```
  </action>
  <verify>
    <automated>
      grep -c "VirtualizedTemplateGrid.test.jsx\|template-gallery-perf.spec.js\|template-gallery-axe.spec.js" .planning/phases/179-gallery-virtualization-launch-validation/179-03-SUMMARY.md | awk '$1 >= 3 { exit 0 } { exit 1 }' && \
      grep -c "@axe-core/playwright" .planning/phases/179-gallery-virtualization-launch-validation/179-01-SUMMARY.md | awk '$1 >= 1 { exit 0 } { exit 1 }' && \
      ls tests/unit/components/VirtualizedTemplateGrid.test.jsx tests/e2e/template-gallery-perf.spec.js tests/e2e/template-gallery-axe.spec.js > /dev/null 2>&1 && \
      grep -c '"@axe-core/playwright"' package.json | awk '$1 >= 1 { exit 0 } { exit 1 }' && \
      grep -c "nyquist_compliant: true" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md | grep -q '^1$' && \
      grep -c "wave_0_complete: true" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md | grep -q '^1$' && \
      grep -c "wave_0_evidence_ref:" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md | grep -q '^1$' && \
      grep -c "validation_signed_off: 2026-05-11" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md | grep -q '^1$' && \
      [ "$(grep -c 'nyquist_compliant: false' .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md)" = "0" ] && \
      [ "$(grep -c 'wave_0_complete: false' .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - **Spot-check gate (RED scaffolds):** `grep -c "VirtualizedTemplateGrid.test.jsx\|template-gallery-perf.spec.js\|template-gallery-axe.spec.js" .planning/phases/179-gallery-virtualization-launch-validation/179-03-SUMMARY.md` returns ≥ 3
    - **Spot-check gate (axe-core install):** `grep -c "@axe-core/playwright" .planning/phases/179-gallery-virtualization-launch-validation/179-01-SUMMARY.md` returns ≥ 1
    - **Spot-check gate (file existence):** `ls tests/unit/components/VirtualizedTemplateGrid.test.jsx tests/e2e/template-gallery-perf.spec.js tests/e2e/template-gallery-axe.spec.js` exits 0
    - **Spot-check gate (package.json):** `grep -c '"@axe-core/playwright"' package.json` returns ≥ 1
    - `grep -c "nyquist_compliant: true" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md` returns EXACTLY 1
    - `grep -c "wave_0_complete: true" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md` returns EXACTLY 1
    - `grep -c "wave_0_evidence_ref:" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md` returns EXACTLY 1
    - `grep -c "validation_signed_off: 2026-05-11" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md` returns EXACTLY 1
    - `grep -c "nyquist_compliant: false" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md` returns EXACTLY 0
    - `grep -c "wave_0_complete: false" .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md` returns EXACTLY 0
    - **Untouched-body gate:** `git diff .planning/phases/179-gallery-virtualization-launch-validation/179-VALIDATION.md | grep -cE "^[+-]## "` returns EXACTLY 0
  </acceptance_criteria>
  <done>
    Spot-check passes (3 RED scaffolds + axe-core install all cited and physically present). 179-VALIDATION.md frontmatter flipped with cross-reference. No stale `false` values. Body unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies markdown frontmatter only. No executable code paths, runtime services, schemas, or auth gates are touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-08 | (n/a) | Doc-only changes | accept | No executable code paths modified. Three VALIDATION.md frontmatter blocks updated. No production attack surface. Accept. |
| T-180-09 | Repudiation | Frontmatter flip without spot-check evidence (audit trail integrity) | mitigate | Each task's `<read_first>` block MANDATES reading the corresponding SUMMARY.md before flipping the frontmatter. Each task's `<verify>` block runs spot-check grep gates that confirm (a) the SUMMARY.md cites the Wave 0 artifacts and (b) those artifacts physically exist on disk. The new `wave_0_evidence_ref` frontmatter field embeds a long-form citation so any future audit can trace the flip back to verifiable evidence. The ROADMAP SC-6 "don't flip blind" rule is enforced by the spot-check pattern: if either evidence gate fails, the task ABORTs and does NOT flip the frontmatter. **LOW severity** once gates are exercised. |
</threat_model>

<verification>
After Tasks 1+2+3 complete:

```bash
# All three frontmatters flipped to true
for phase in 177-ai-generation-pipeline-admin-queue-ui 178-vertical-content-seeding 179-gallery-virtualization-launch-validation; do
  echo "--- ${phase} ---"
  grep -c "nyquist_compliant: true" .planning/phases/${phase}/*-VALIDATION.md   # expect 1
  grep -c "wave_0_complete: true" .planning/phases/${phase}/*-VALIDATION.md     # expect 1
  grep -c "nyquist_compliant: false" .planning/phases/${phase}/*-VALIDATION.md  # expect 0
  grep -c "wave_0_complete: false" .planning/phases/${phase}/*-VALIDATION.md    # expect 0
  grep -c "wave_0_evidence_ref:" .planning/phases/${phase}/*-VALIDATION.md      # expect 1
done

# Spot-check anchors are real
ls tests/integration/edgeFunctionSpike.test.js tests/integration/generateSvgTemplate.test.js \
   tests/integration/approveDraftPipeline.test.js tests/integration/promptLibraryParity.test.js \
   tests/unit/generateValidatorOrder.test.js                                                        # Phase 177
ls tests/unit/generateOrientation.test.js tests/integration/approveBulk.test.js \
   tests/integration/rejectBulk.test.js tests/integration/templateDraftsService.bulk.test.js \
   tests/integration/seedTopics.schema.test.js scripts/verify-178-counts.cjs scripts/validate-templates.cjs  # Phase 178
ls tests/unit/components/VirtualizedTemplateGrid.test.jsx tests/e2e/template-gallery-perf.spec.js \
   tests/e2e/template-gallery-axe.spec.js                                                            # Phase 179
```

All gates must pass before the plan is marked complete.
</verification>

<success_criteria>
- SC-6: All three VALIDATION.md frontmatters flipped to `nyquist_compliant: true` + `wave_0_complete: true` + `status: approved` + `validation_signed_off: 2026-05-11` + `wave_0_evidence_ref: <long-form citation>`
- Each flip anchored to a spot-check that confirms the SUMMARY.md cites the Wave 0 artifacts AND those artifacts physically exist on disk (no blind flips)
- No body content modified in any of the three VALIDATION.md files
</success_criteria>

<output>
After completion, create `.planning/phases/180-v21-launch-readiness/180-04-SUMMARY.md` per the GSD summary template. Include:
- For each phase, the verbatim before/after diff of the frontmatter
- The output of all spot-check grep gates and `ls` commands
- Confirmation via `git diff` that only frontmatter (lines 1-8 + new field additions) was modified
- Commit hash for each task (one task = one commit, or one combined commit if the executor prefers atomic doc-cluster)
</output>
