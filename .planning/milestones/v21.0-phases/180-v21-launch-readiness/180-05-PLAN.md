---
phase: 180-v21-launch-readiness
plan: 05
type: execute
wave: 2
depends_on: ["180-01", "180-04"]
files_modified:
  - .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md
autonomous: false
requirements: [TVRZ-02, TVRZ-03, TVRZ-04, TVRZ-05]
requirements_addressed: [TVRZ-02, TVRZ-03, TVRZ-04, TVRZ-05]
gap_closure: true
tags: [phase-180, ci-empirical, playwright, perf-budget, axe-core, scroll-reset, skeleton-flash, seeded-env, super-admin]
user_setup:
  - service: local-supabase
    why: "Empirical CI-style gates require a locally-seeded gallery of ≥400 active templates AND TEST_USER_EMAIL / TEST_USER_PASSWORD provisioned. The 5 Phase 179 deferred gates cannot meaningfully run against an unseeded DB — the catalog-floor pre-flight in template-gallery-perf.spec.js will short-circuit and the axe scan would assert against an empty grid."
    env_vars:
      - name: TEST_USER_EMAIL
        source: "Provision a local test user via scripts/seed-ci-test-user.cjs OR use existing test creds; documented in tests/e2e/helpers.js"
      - name: TEST_USER_PASSWORD
        source: "Pair with TEST_USER_EMAIL"
      - name: SUPABASE_URL
        source: ".env.local — already provisioned for Phases 177/178/179"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Fetched via supabase CLI mgmt API per Phase 177 pattern"
    dashboard_config:
      - task: "Ensure local Supabase DB has ≥400 active gallery_templates rows (production cloud project gdxizdiltfqeugbsgtpx had 485 at Phase 178 close per 178-VERIFICATION.md; if testing against local, run scripts/seed-ci-test-user.cjs and seed via existing scripts/seed-vertical-templates.cjs OR test against the live cloud project — operator's choice)"
        location: "Local box .env.local; OR Supabase cloud dashboard project gdxizdiltfqeugbsgtpx"

must_haves:
  truths:
    - "Playwright spec tests/e2e/template-gallery-perf.spec.js runs against a populated environment, the catalog-floor pre-flight (rowcount * 4 >= 400) PASSes, and the measured first-paint duration is recorded in 180-VERIFICATION.md as actual milliseconds (PASS if <1000ms; FAIL with diagnostic if ≥1000ms)"
    - "Playwright spec tests/e2e/template-gallery-axe.spec.js runs against the populated environment, axe scan reports zero violations scoped to [role='grid'], and the violation count + aria-rowcount value are recorded in 180-VERIFICATION.md"
    - "Playwright test in tests/e2e/template-gallery.spec.js named 'search filter resets grid scroll + retains input focus + no blank viewport (SC-3)' runs and PASSes against the populated environment, with the post-search scrollTop value (expected 0) recorded in 180-VERIFICATION.md"
    - "Playwright test in tests/e2e/template-gallery.spec.js named 'URL-synced filters restore state (TDSC-04 + SC-4 skeleton-flash gate)' runs and PASSes against the populated environment with no flash of 'No templates match your search' during the ?category=Restaurant transition, recorded in 180-VERIFICATION.md"
    - "180-VERIFICATION.md is created with frontmatter (status, score, ci-empirical-runs section) and 4 per-SC test result blocks (SC-2 / SC-3 / SC-4 / SC-5) documenting actual measurements"
  artifacts:
    - path: ".planning/phases/180-v21-launch-readiness/180-VERIFICATION.md"
      provides: "Empirical evidence trail for SC-7/8/9/10 (179 SC-2/SC-3/SC-4/SC-5) with actual ms / violation counts / scrollTop values / skeleton-flash observations"
      contains: "TVRZ-02\\|TVRZ-03\\|TVRZ-04\\|TVRZ-05"
  key_links:
    - from: "tests/e2e/template-gallery-perf.spec.js (run)"
      to: "180-VERIFICATION.md SC-2 result block"
      via: "operator copy-pastes the [SC-2] gallery first-paint: Nms (budget 1000ms) console.log line into the verification file"
      pattern: "\\[SC-2\\] gallery first-paint"
    - from: "tests/e2e/template-gallery-axe.spec.js (run)"
      to: "180-VERIFICATION.md SC-5 result block"
      via: "operator records results.violations.length (expect 0) and aria-rowcount value into the verification file"
      pattern: "violations.*0"
---

<objective>
Close 4 of the 5 Phase 179 deferred CI-empirical gates against a populated local-or-cloud environment:

- **SC-7 (ROADMAP line 169 — TVRZ-02):** Run `npx playwright test tests/e2e/template-gallery-perf.spec.js` with the catalog-floor pre-flight (`rowcount * 4 >= 400`) PASSing and the first-paint budget under 1 second.
- **SC-8 (ROADMAP line 170 — TVRZ-03):** Run the SC-3 case in `tests/e2e/template-gallery.spec.js` ('search filter resets grid scroll + retains input focus + no blank viewport (SC-3)') and PASS the scroll-reset + focus retention checks on real Chromium.
- **SC-9 (ROADMAP line 171 — TVRZ-04 skeleton-flash clause):** Run the TDSC-04 case in `tests/e2e/template-gallery.spec.js` ('URL-synced filters restore state (TDSC-04 + SC-4 skeleton-flash gate)') and PASS the no-flash-of-empty-state assertion during the `?category=Restaurant` deep-link transition.
- **SC-10 (ROADMAP line 172 — TVRZ-05 axe clause):** Run `npx playwright test tests/e2e/template-gallery-axe.spec.js` with zero axe-core violations scoped to `[role='grid']` and aria-rowcount > 50.

Purpose: At Phase 179 close, all 5 launch-validation gates passed at the codebase tier (unit suite GREEN, build green, spec files structurally correct) but the empirical runs were deferred because the local Supabase migration ordering bug (migration 105 references `tenants` before it exists) blocked `db reset` and the operator opted to ship the structural correctness verification without spending time provisioning CI. Phase 180 closes the gap by running the specs against a populated environment — either local-with-manual-seed or the live cloud project (per `user_setup.dashboard_config`).

Output: New file `.planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` with:
- Frontmatter: `status: passed` / `partial` / `failed` based on the 4 runs; `score: N/4 SC checks GREEN`
- One result block per SC (SC-2/SC-3/SC-4/SC-5) with the actual command run, the captured console output (e.g., `[SC-2] gallery first-paint: 412ms (budget 1000ms)`), the Playwright pass/fail status, and any diagnostic info (e.g., aria-rowcount value, axe violation summary if any)
- A diagnostic section recording the chosen environment (local-seeded vs cloud) and the seeded template count
- Cross-reference to Plan 06 (which closes SC-11 — the v20.0 E2E regression delta gate)

This is the **first plan in Wave 2** — it depends on Plan 01 (BLOCKER-1 closure — the SC-1 nav tile must exist so the gallery is reachable through normal flows during the test runs) and Plan 04 (validation frontmatter refresh — gives the AI runs accurate phase-validation state to assert against in 180-VERIFICATION.md frontmatter). Plan 06 depends on this plan.

**autonomous: false** — this plan involves environment-touching commands (DB seeding, live Playwright runs that consume real auth credentials, optional cloud-project access). A checkpoint is required so the operator confirms the environment is ready before live runs are kicked off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v21.0-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-HUMAN-UAT.md
@.planning/phases/179-gallery-virtualization-launch-validation/179-VERIFICATION.md
@.planning/phases/180-v21-launch-readiness/180-01-PLAN.md
@.planning/phases/180-v21-launch-readiness/180-04-PLAN.md
@.planning/phases/180-v21-launch-readiness/

<interfaces>
<!-- Concrete contracts the executor needs. -->

### Spec files that this plan runs (these were created/extended in Phase 179)

**tests/e2e/template-gallery-perf.spec.js (Phase 179 Plan 03 + Plan 09 CR-01 fix):**
- Single test: `'gallery first-paint <1s at ~500-template catalog with 1x CPU throttle (SC-2)'`
- Skip-guard: `test.skip(() => !process.env.TEST_USER_EMAIL, 'Test credentials not configured')`
- CDP throttle: `Emulation.setCPUThrottlingRate { rate: 1 }` (rate=1 documented as "no throttle" — the spec is intentionally permissive on M1-class hardware)
- Catalog-floor pre-flight: `aria-rowcount * 4 >= 400` (fails if DB has fewer than ~100 rows)
- Measurement: `performance.mark('gallery-paint-start')` inside `page.evaluate()` (CR-01 fix); `performance.measure(...)` after grid renders; `expect(elapsed).toBeLessThan(1000)`
- Console output to capture: `[SC-2] gallery first-paint: Nms (budget 1000ms)` — operator copies the literal N value into 180-VERIFICATION.md

**tests/e2e/template-gallery-axe.spec.js (Phase 179 Plan 03):**
- Single test: `'virtualized gallery is axe-core clean at full catalog (SC-5)'`
- Skip-guard: same TEST_USER_EMAIL guard
- aria-rowcount sanity: `expect(Number(rowcountAttr)).toBeGreaterThan(50)` (~200 templates / 4 cols ≈ 50 rows)
- Axe scope: `new AxeBuilder({ page }).include('[role="grid"]').analyze()`
- Assertion: `expect(results.violations).toEqual([])` — operator records violations array length + any violation rule IDs

**tests/e2e/template-gallery.spec.js (Phase 179 Plan 07 + Plan 09):**
- SC-3 test name: `'search filter resets grid scroll + retains input focus + no blank viewport (SC-3)'` (lines 138-172)
- SC-3 behavior: scroll the internal grid container to scrollTop=800, type 'menu' into search, assert `expect.poll` returns scrollTop=0 within 2s, assert search input retains focus, assert `[role='grid']` remains visible
- TDSC-04 test name: `'URL-synced filters restore state (TDSC-04 + SC-4 skeleton-flash gate)'` (lines 92-126)
- TDSC-04 SC-4 behavior: navigate to `?category=Restaurant`, assert `getByText('No templates match your search')` has `toHaveCount(0)` during the load (no flash), assert `getByText(/Category:\\s*Restaurant/i)` becomes visible within 5s

### Phase 179 deferred items (verbatim from 179-HUMAN-UAT.md — these are the 5 deferred gates; Plan 06 closes #5)

```
### 1. SC-2 perf budget evaluates correctly against a populated DB (~500 templates)
### 2. SC-5 axe-core scan reports zero violations on the virtualized grid
### 3. SC-3 scroll-reset + focus-retention + no-blank-viewport behavior on real browser
### 4. SC-4 skeleton-flash gate verifies clean URL-driven category-filter transition
### 5. v20.0 gallery E2E regression suite ≥90% green delta gate (SC-5 second clause)  ← closed by Plan 06
```

### Environment preparation options (operator picks one — see checkpoint task)

**Option A — Test against live cloud Supabase project (production data, fast path).**
- The production cloud project (Supabase ref `gdxizdiltfqeugbsgtpx`) had 485 active templates at Phase 178 close (verified in 178-VERIFICATION.md).
- Requires test super_admin or regular test user with the production .env.local already wired.
- Fastest path; no local seeding needed.
- Risk: tests run against live data; the SC-3 search/scroll case modifies no DB state (read-only) but the run still uses production auth.

**Option B — Seed local Supabase with ≥400 templates.**
- Run `supabase db start` (requires Docker).
- If `supabase db reset` fails at migration 105 (the known pre-existing bug per 179-VERIFICATION.md / STATE.md), apply migrations selectively OR document the seed via direct SQL inserts.
- Then run `scripts/seed-ci-test-user.cjs` to provision the test user.
- Then run `scripts/seed-vertical-templates.cjs --vertical=restaurants --limit=200` (and similar for retail/healthcare) to bulk-generate or use a smaller manual SQL INSERT to land 400+ rows directly into svg_templates.
- Slower path; preserves test isolation from prod.

**Option C — Synthetic Playwright route intercept (no DB seeding).**
- Mock `fetchGalleryTemplates` at the Playwright fixture level to return 500 deterministic fixture rows.
- Pros: fully deterministic, no env coupling.
- Cons: tests the rendering layer but NOT the real `gallery_templates` VIEW + RLS data path. SC-7's spirit ("at ~500-template catalog") is closer to a real DB.
- Use only if Option A and B are both blocked.

### 180-VERIFICATION.md skeleton (the file this plan creates)

The frontmatter pattern mirrors the existing 178-VERIFICATION.md format. Key fields:
- `phase: 180-v21-launch-readiness`
- `verified: 2026-05-11T<HH:MM:SS>Z` (timestamp of the latest run)
- `status: passed` / `partial` / `failed` (depending on which SCs pass)
- `score: N/11 SC checks GREEN` (N = sum of {SC-1 via Plan 01 closure proof, SC-2 via Plan 02, SC-4 via Plan 03 traceability, SC-5 via Plan 03 HUMAN-UAT correction, SC-6 via Plan 04 nyquist flips, SC-7 via this plan's perf run, SC-8 via this plan's SC-3 run, SC-9 via this plan's SC-4 run, SC-10 via this plan's axe run, SC-11 via Plan 06}; SC-3 nav-flow is Plan 01)
- `plans_executed: 5/6` (or 6/6 if Plan 06 has also completed at file-write time)
- `human_verification`: list of items still pending if any SC failed and needs follow-up
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator confirms test environment is provisioned (DB seeded ≥400 active templates + TEST_USER_EMAIL/PASSWORD set)</name>
  <what-built>Plan 01 added the AI Template Queue nav tile and Plan 04 refreshed VALIDATION.md frontmatters. The 4 Playwright specs that Plan 05 will run already exist in tests/e2e/ (committed in Phase 179). What needs to be true before live runs start:

1. Either local Supabase has ≥400 active `gallery_templates` rows seeded OR the operator chooses to run against the cloud project (`gdxizdiltfqeugbsgtpx`) which has 485 active rows.
2. `TEST_USER_EMAIL` and `TEST_USER_PASSWORD` are set in the shell environment (or in `.env.local` — `playwright.config.js` loads dotenv).
3. The `npx playwright install --with-deps chromium` step has been run at least once in this checkout (so Chromium binary is available).
4. The Vite dev server (`npm run dev`) is running on the port `playwright.config.js` expects (typically the dev server is auto-started by Playwright's webServer config — verify).</what-built>
  <how-to-verify>
    Operator runs each of these checks in their shell (NOT in the Playwright runner):

    1. **Catalog-floor check:**
       - **If testing against cloud:** `node -e "const c=require('@supabase/supabase-js').createClient(process.env.SUPABASE_URL,process.env.SUPABASE_ANON_KEY);c.from('gallery_templates').select('id',{count:'exact',head:true}).then(r=>console.log('rows:',r.count))"` — expect `rows: 485` (or close to it).
       - **If testing against local:** Run the same query against local Supabase URL. If count < 400, seed first (see Option B in `<interfaces>`).

    2. **Credentials check:** `echo "TEST_USER_EMAIL=$TEST_USER_EMAIL"` and `echo "TEST_USER_PASSWORD set: $([ -n "$TEST_USER_PASSWORD" ] && echo yes || echo no)"` — both must show non-empty.

    3. **Playwright Chromium check:** `npx playwright test --list tests/e2e/template-gallery-perf.spec.js` — expect output `Total: 1 test in 1 file` (collection works).

    4. **Dev server check:** Either confirm `npm run dev` is running on the expected port OR confirm `playwright.config.js` has `webServer` config that auto-starts it (read the file to confirm).

    Operator declares which environment they chose:
    - "Option A — cloud project gdxizdiltfqeugbsgtpx"
    - "Option B — local Supabase, seeded to ${N} templates"
    - "Option C — Playwright route intercept (no DB seed)"
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `environment-ready: option-A` (cloud, 485 templates) — proceed to Task 2
    - `environment-ready: option-B, seeded=${N}` (local, N templates) — proceed to Task 2
    - `environment-ready: option-C` (intercept) — proceed to Task 2 with the route-intercept variant
    - `environment-blocked: <reason>` — abort plan, report blocker, file 180-VERIFICATION.md with status: blocked + diagnostic

    Each subsequent task assumes the environment is ready in the chosen mode.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run SC-7 perf gate + SC-10 axe gate + SC-8 SC-3 case + SC-9 SC-4 case; capture results in 180-VERIFICATION.md</name>
  <files>.planning/phases/180-v21-launch-readiness/180-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/179-gallery-virtualization-launch-validation/179-HUMAN-UAT.md (the 5 deferred items — confirms what this plan is closing)
    - .planning/phases/179-gallery-virtualization-launch-validation/179-VERIFICATION.md (full file — confirms the 'partial' status that this plan flips to 'passed' for items 1-4)
    - .planning/phases/178-vertical-content-seeding/178-VERIFICATION.md frontmatter (lines 1-26 — confirms the YAML schema pattern that 180-VERIFICATION.md will mirror)
    - tests/e2e/template-gallery-perf.spec.js (the spec being run — confirms expected console output `[SC-2] gallery first-paint: Nms (budget 1000ms)`)
    - tests/e2e/template-gallery-axe.spec.js (the spec being run — confirms expected assertions)
    - tests/e2e/template-gallery.spec.js lines 92-172 (the SC-3 and TDSC-04 cases — confirms expected behavior)
    - playwright.config.js (full file — confirms webServer config + base URL + projects)
  </read_first>
  <behavior>
    - Run `npx playwright test tests/e2e/template-gallery-perf.spec.js` against the prepared environment; capture the `[SC-2] gallery first-paint: Nms (budget 1000ms)` console output and the overall pass/fail status
    - Run `npx playwright test tests/e2e/template-gallery-axe.spec.js` against the prepared environment; capture the violations array (expect empty `[]`) and the aria-rowcount value from the sanity check
    - Run `npx playwright test tests/e2e/template-gallery.spec.js -g "SC-3"` against the prepared environment; capture the pass/fail status and the recorded scrollTop value
    - Run `npx playwright test tests/e2e/template-gallery.spec.js -g "TDSC-04"` against the prepared environment; capture the pass/fail status (the SC-4 skeleton-flash assertion is embedded in this test — `expect(getByText('No templates match your search')).toHaveCount(0)` during the `?category=Restaurant` load)
    - Create `.planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` with frontmatter + 4 SC result blocks matching the pattern of the existing 178-VERIFICATION.md
    - The created file uses the verbatim text from the test console outputs — no paraphrasing
    - If any of the 4 runs fails, the frontmatter `status: partial` and the failure is documented with diagnostic output (full Playwright stderr tail)
    - If all 4 pass, frontmatter `status: passed` and `score: 4/4 Phase 179 deferred items closed`
  </behavior>
  <action>
    Execute the 4 Playwright runs in sequence (NOT parallel — Playwright already runs internally parallel; running specs serially at the CLI prevents log-interleaving from confusing the result-capture step). For each run, capture full stdout + stderr to a temp file, extract the relevant signal, and write the result block into 180-VERIFICATION.md.

    **Run 1 — SC-7 perf gate:**
    ```bash
    npx playwright test tests/e2e/template-gallery-perf.spec.js --reporter=line 2>&1 | tee /tmp/180-sc7-perf.log
    # Capture the [SC-2] gallery first-paint: Nms line:
    grep -oE '\[SC-2\] gallery first-paint: [0-9.]+ms \(budget 1000ms\)' /tmp/180-sc7-perf.log | head -1
    # Capture overall pass/fail:
    grep -E "passed|failed" /tmp/180-sc7-perf.log | tail -3
    ```
    Expected: pass + `[SC-2] gallery first-paint: <N>ms` with N < 1000.
    If pass: record N (literal milliseconds) in 180-VERIFICATION.md SC-2 block.
    If fail: record full stderr tail in SC-2 block diagnostic; set frontmatter status: partial.

    **Run 2 — SC-10 axe gate:**
    ```bash
    npx playwright test tests/e2e/template-gallery-axe.spec.js --reporter=line 2>&1 | tee /tmp/180-sc10-axe.log
    # The spec asserts results.violations === []; if it fails, the diff shows the rule IDs.
    grep -E "passed|failed|violations" /tmp/180-sc10-axe.log | tail -10
    ```
    Expected: pass.
    Record `violations: 0` + `aria-rowcount: <value>` (extract from the spec's pre-axe sanity check OR from the page DOM during the run; if not captured live, the operator can re-run with `--debug` to inspect) in SC-5 block. If fail: list each rule ID + impact level in the diagnostic.

    **Run 3 — SC-8 SC-3 case (scroll reset + focus retention):**
    ```bash
    npx playwright test tests/e2e/template-gallery.spec.js -g "SC-3" --reporter=line 2>&1 | tee /tmp/180-sc8-sc3.log
    grep -E "passed|failed" /tmp/180-sc8-sc3.log | tail -3
    ```
    Expected: pass. The spec's three sub-assertions (scrollTop=0 + focus retained + grid visible) all run inside one test block — pass/fail is atomic.
    Record `scroll-reset: PASS, scrollTop=0 within 2s, focus retained, grid visible` in SC-3 block (or the failure diagnostic).

    **Run 4 — SC-9 SC-4 case (skeleton-flash gate):**
    ```bash
    npx playwright test tests/e2e/template-gallery.spec.js -g "TDSC-04" --reporter=line 2>&1 | tee /tmp/180-sc9-tdsc04.log
    grep -E "passed|failed" /tmp/180-sc9-tdsc04.log | tail -3
    ```
    Expected: pass. The `expect(getByText('No templates match your search')).toHaveCount(0)` assertion is the SC-4 skeleton-flash gate. If `?category=Restaurant` loaded would show the empty-state at any poll interval, this fails. The category chip visibility check (`Category: Restaurant` within 5s) is the second clause of the same test.
    Record `skeleton-flash: PASS, no empty-state heading observed during ?category=Restaurant transition, Category: Restaurant chip visible within 5s` in SC-4 block (or failure diagnostic).

    **Now create 180-VERIFICATION.md** with the captured results. Use this exact structure (mirrors 178-VERIFICATION.md frontmatter pattern):

    ```markdown
    ---
    phase: 180-v21-launch-readiness
    verified: 2026-05-11T<HH:MM:SS>Z
    status: <passed|partial|failed>
    score: <N>/11 ROADMAP SC checks GREEN (Plan 01 SC-1+SC-3 nav; Plan 02 SC-2 EF header; Plan 03 SC-4+SC-5 traceability+UAT; Plan 04 SC-6 nyquist; this plan SC-7+SC-8+SC-9+SC-10; Plan 06 SC-11)
    plans_executed: 5/6
    environment: <option-A | option-B-seeded-N | option-C-intercept>
    catalog_template_count: <observed-count>
    human_verification:
      - test: <if-any-failed>
        expected: <expected behavior>
        actual: <observed behavior>
        why_human: <reason a human needs to look>
    ---

    # Phase 180: v21.0 Launch Readiness — Verification Report

    **Phase Goal (from ROADMAP.md):** v21.0 is fully shippable — the AI generation queue is reachable through the standard admin navigation surface, audit-flagged doc drift is repaired, REQUIREMENTS.md traceability reflects actual codebase state, and the 5 Phase 179 empirical gates deferred to CI have been exercised end-to-end against a seeded local environment.

    **Verified:** 2026-05-11T<HH:MM:SS>Z
    **Status:** <PASSED | PARTIAL | FAILED>
    **Environment:** <chosen-option-from-Task-1>

    ---

    ## Empirical CI Gates — Phase 179 deferred items closure

    ### SC-7 (TVRZ-02) — Gallery first-paint budget <1s with catalog-floor pre-flight

    **Command:** `npx playwright test tests/e2e/template-gallery-perf.spec.js`

    **Console output:** `[SC-2] gallery first-paint: <N>ms (budget 1000ms)`

    **Catalog-floor pre-flight:** `aria-rowcount=<R>` → `<R> * 4 = <R*4>` (floor 400) → <PASS | FAIL>

    **Playwright verdict:** <PASS | FAIL>

    **Diagnostic (if FAIL):** <Playwright stderr tail or "n/a — passed">

    ---

    ### SC-10 (TVRZ-05) — axe-core zero violations scoped to [role='grid']

    **Command:** `npx playwright test tests/e2e/template-gallery-axe.spec.js`

    **aria-rowcount sanity:** <value> (floor 50)

    **Violation count:** <N>

    **Violation rule IDs (if any):** <list or "none">

    **Playwright verdict:** <PASS | FAIL>

    **Diagnostic (if FAIL):** <Playwright stderr tail or "n/a — passed">

    ---

    ### SC-8 (TVRZ-03) — Scroll reset + focus retention + no blank viewport on real Chromium

    **Command:** `npx playwright test tests/e2e/template-gallery.spec.js -g "SC-3"`

    **Sub-assertions:**
    - scrollTop returned to 0 within 2s after typing search: <PASS | FAIL>
    - Search input retained focus: <PASS | FAIL>
    - [role='grid'] remained visible (no blank viewport): <PASS | FAIL>

    **Playwright verdict:** <PASS | FAIL>

    **Diagnostic (if FAIL):** <Playwright stderr tail or "n/a — passed">

    ---

    ### SC-9 (TVRZ-04 skeleton-flash clause) — ?category=Restaurant deep-link skeleton-flash gate

    **Command:** `npx playwright test tests/e2e/template-gallery.spec.js -g "TDSC-04"`

    **Sub-assertions:**
    - "No templates match your search" heading NEVER appeared during the transition: <PASS | FAIL>
    - "Category: Restaurant" chip became visible within 5s: <PASS | FAIL>

    **Playwright verdict:** <PASS | FAIL>

    **Diagnostic (if FAIL):** <Playwright stderr tail or "n/a — passed">

    ---

    ## Cross-references to other Plan-180 closures

    | SC | Requirement | Plan | Status | Evidence |
    |----|-------------|------|--------|----------|
    | SC-1 | BLOCKER-1 nav repair | 180-01 | <see Plan 01 SUMMARY> | grep "admin-template-queue" src/pages/SuperAdminDashboardPage.jsx ≥1 |
    | SC-2 | EF header refresh | 180-02 | <see Plan 02 SUMMARY> | grep -c "501 stub" supabase/functions/generate-svg-template/index.ts == 0 |
    | SC-3 | FLOW-1 closure (E2E) | 180-01 | <see Plan 01 SUMMARY> | tests/e2e/admin-template-queue-nav.spec.js exists; 0 dispatchEvent references |
    | SC-4 | Traceability flip | 180-03 | <see Plan 03 SUMMARY> | 9 REQUIREMENTS.md rows flipped Pending → Complete with commit 04ed5938 |
    | SC-5 | Phase 177 HUMAN-UAT item 4 | 180-03 | <see Plan 03 SUMMARY> | Correction sentinel + Phase 180 reference present |
    | SC-6 | VALIDATION.md frontmatter | 180-04 | <see Plan 04 SUMMARY> | 3 phases nyquist_compliant: true + wave_0_evidence_ref cited |
    | SC-7 | Perf budget empirical | 180-05 (this) | <as above> | (above) |
    | SC-8 | Scroll-reset empirical | 180-05 (this) | <as above> | (above) |
    | SC-9 | Skeleton-flash empirical | 180-05 (this) | <as above> | (above) |
    | SC-10 | axe zero-violations empirical | 180-05 (this) | <as above> | (above) |
    | SC-11 | v20.0 E2E ≥90% delta | 180-06 | <see Plan 06 SUMMARY> | (pending Plan 06) |

    ---

    ## Closure for v21.0

    On all-PASS, this verification + Plan 06 (SC-11) closure together satisfy the 11 numbered ROADMAP success criteria. v21.0 then proceeds to `/gsd-verify-work 180` → `/gsd-audit-milestone v21.0` → `/gsd-complete-milestone v21.0`.

    ---

    *Verified by: Phase 180 Plan 05 (this file)*
    *Cross-references: 180-01-PLAN.md, 180-02-PLAN.md, 180-03-PLAN.md, 180-04-PLAN.md, 180-06-PLAN.md*
    ```

    Replace every `<placeholder>` with the actual captured values from the 4 runs.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md && \
      grep -c "phase: 180-v21-launch-readiness" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^1$' && \
      grep -c "SC-7\|SC-8\|SC-9\|SC-10" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | awk '$1 >= 4 { exit 0 } { exit 1 }' && \
      grep -c "\\[SC-2\\] gallery first-paint:" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^[1-9]' && \
      grep -cE "aria-rowcount" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^[1-9]' && \
      grep -cE "Violation count" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^[1-9]' && \
      grep -cE "scrollTop\|Scroll reset\|scroll-reset" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^[1-9]' && \
      grep -cE "skeleton-flash\|TDSC-04" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^[1-9]' && \
      grep -cE "status: (passed|partial|failed)" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | grep -q '^1$'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` exists
    - Frontmatter has `phase: 180-v21-launch-readiness` (exactly once)
    - Frontmatter has a `status:` field with value `passed`, `partial`, or `failed`
    - Frontmatter has a `score:` field referencing all 11 ROADMAP SC checks
    - Frontmatter has an `environment:` field naming the chosen option (A/B/C)
    - Frontmatter has a `catalog_template_count:` field with a number (the observed count from the catalog-floor pre-flight)
    - All 4 SC result blocks are present: SC-7 (perf), SC-10 (axe), SC-8 (SC-3 scroll/focus), SC-9 (SC-4 skeleton-flash)
    - **SC-7 evidence:** `grep -c "\\[SC-2\\] gallery first-paint:" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 1 (the literal console output line is captured)
    - **SC-7 evidence:** A numeric milliseconds value appears in the SC-7 block — extracted via `grep -oE 'first-paint: [0-9.]+ms' .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | head -1` returns a non-empty match
    - **SC-10 evidence:** `grep -c "Violation count" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 1; the recorded count is `0` (PASS) or a non-zero number + listed rule IDs (FAIL)
    - **SC-10 evidence:** `grep -c "aria-rowcount" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 1
    - **SC-8 evidence:** `grep -c "scrollTop\|Scroll reset\|scroll-reset" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 1
    - **SC-9 evidence:** `grep -c "skeleton-flash\|TDSC-04" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 1
    - **Cross-reference table:** `grep -c "180-01\|180-02\|180-03\|180-04\|180-06" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 5 (all sibling plans referenced in the closure table)
    - **Each SC has a Playwright verdict:** `grep -cE "Playwright verdict: (PASS|FAIL)" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns ≥ 4
    - If status is `partial` or `failed`, at least one `human_verification:` entry MUST appear in the frontmatter naming the failing item + why a human needs to look at it
    - **Defense-in-depth gate:** If SC-7's first-paint exceeds 1000ms, the file MUST NOT claim status: passed — the script enforces `grep -c "status: passed" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` returns 0 OR `grep -oE 'first-paint: [0-9.]+ms' .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md` extracts a value < 1000
  </acceptance_criteria>
  <done>
    All 4 Playwright runs executed against the prepared environment. Console outputs captured. 180-VERIFICATION.md created with frontmatter + 4 SC result blocks + cross-reference table. Status accurately reflects per-SC outcomes. If any SC failed, frontmatter status is partial/failed and human_verification entries name the failures. If all PASSed, status: passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local box → Supabase auth | Playwright runs use `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` to log in via the real auth flow. If running against cloud project, this consumes real auth tokens. |
| Local box → Supabase data | Read-only queries against `gallery_templates` (the SC-3 search/scroll case modifies no DB state; SC-4 deep-link is also read-only). |
| axe-core scan → DOM | The scan runs entirely in-browser via the Playwright fixture; no external service touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-10 | Information Disclosure | Test credentials leak into 180-VERIFICATION.md | mitigate | The captured console outputs from Playwright are filtered: `grep` extracts only specific signal lines (`[SC-2] gallery first-paint:`, violation counts, scrollTop values). The grep-extracted lines do NOT contain auth tokens. If the executor accidentally pastes raw `npx playwright test --reporter=line` output containing JWT fragments into 180-VERIFICATION.md, the file would leak partial secrets — operator MUST review the diff before committing. **MEDIUM severity** — mitigation is operator review at commit time. The acceptance criteria use grep-shape filters, not raw-paste, so the surface is bounded. |
| T-180-11 | Tampering | Live runs modify production data | accept | All 4 specs are read-only against `gallery_templates`. SC-3 case scrolls the grid then types in the search input — no mutation. SC-4 case navigates to `?category=Restaurant` — read-only URL filter. The axe scan reads DOM only. No INSERT/UPDATE/DELETE issued. **LOW severity**, accept. |
| T-180-12 | Denial of Service | Test runs against production saturate API quotas | mitigate | The 4 spec runs each make ~5-20 HTTP requests during their lifetime (login + dashboard fetch + gallery fetch + search debounce). Total well under any rate-limit ceiling. The cloud project (Option A) has been operational at Phase 178 close (~$15 Anthropic spend; thousands of EF calls survived). **LOW severity**, accept. |
| T-180-13 | Elevation of Privilege | Test user gains super_admin rights via reused JWT | mitigate | The 4 specs run as the regular TEST_USER role, not super_admin (Plan 01's nav-tile spec is the one that uses super_admin, and that's a separate file run in Plan 01 / not part of this plan). The 4 gallery specs only need a logged-in user — gallery is reachable from all authenticated roles. **LOW severity**, accept. |
</threat_model>

<verification>
After Task 2 completes:

```bash
# File exists and structurally correct
test -f .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md
grep -c "phase: 180-v21-launch-readiness" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md  # == 1
grep -c "SC-7\|SC-8\|SC-9\|SC-10" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md         # >= 4
grep -c "\\[SC-2\\] gallery first-paint:" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md # >= 1 (literal captured)
grep -c "Playwright verdict: (PASS|FAIL)" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md  # >= 4

# Internal consistency
status=$(grep "^status:" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md | awk '{print $2}')
if [ "$status" = "passed" ]; then
  # All 4 verdicts must be PASS
  fail_count=$(grep -cE "Playwright verdict: FAIL" .planning/phases/180-v21-launch-readiness/180-VERIFICATION.md)
  [ "$fail_count" = "0" ] || { echo "INCONSISTENT: status=passed but $fail_count FAIL verdicts"; exit 1; }
fi
```

All gates must pass before the plan is marked complete.
</verification>

<success_criteria>
- SC-7 closed: perf spec runs against populated DB, first-paint < 1000ms (or status: partial with diagnostic)
- SC-8 closed: SC-3 scroll-reset + focus retention case passes on real Chromium
- SC-9 closed: SC-4 skeleton-flash gate passes — no flash of empty-state during `?category=Restaurant` transition
- SC-10 closed: axe-core scan reports 0 violations scoped to [role='grid']
- 180-VERIFICATION.md created with frontmatter + 4 SC result blocks + cross-reference table to sibling plans
- Status accurately reflects per-SC outcomes
- If any failed, the failure is documented as `human_verification:` entries with clear next steps for the operator
</success_criteria>

<output>
After completion, create `.planning/phases/180-v21-launch-readiness/180-05-SUMMARY.md` per the GSD summary template. Include:
- Verbatim console outputs from all 4 Playwright runs (filtered to the relevant signal lines)
- The full text of 180-VERIFICATION.md (so the SUMMARY is independently auditable without opening another file)
- Decision log: which environment option was chosen (A/B/C) and why
- Cost/duration: total minutes spent on Playwright runs + any DB seeding cost
- Diagnostic notes: any flakes observed, retry counts, or partial-pass conditions
- Commit hash for the 180-VERIFICATION.md creation
</output>
