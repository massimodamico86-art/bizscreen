---
phase: 176
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: true
requirements: []
tags:
  - migration
  - supabase
  - schema-push
  - blocking
must_haves:
  truths:
    - "Migration 176 has been applied to the live Supabase project — `template_drafts` table is queryable, `svg_templates.vertical` column exists, `gallery_templates` VIEW exposes the new `vertical` column"
    - "The embedded DO $$ ASSERT $$ block in migration 176 ran without raising — proving all 3 ROADMAP success criteria are satisfied at the database level"
    - "Live SQL smoke SELECTs confirm: SELECT FROM template_drafts returns empty result + correct columns; SELECT vertical FROM svg_templates LIMIT 1 returns 1 row with NULL value (or any of 3 enum values); SELECT vertical FROM gallery_templates LIMIT 1 returns 1 row"
    - "Existing gallery E2E smoke (tests/e2e/template-gallery.spec.js) still passes — zero regressions from the VIEW replacement"
  artifacts:
    - path: ".planning/phases/176-schema-foundation/176-02-SUMMARY.md"
      provides: "Apply log + smoke SELECT outputs + gallery E2E run result"
      contains: "All 3 ROADMAP success criteria PASS"
  key_links:
    - from: "Mgmt API push"
      to: "live Supabase project gdxizdiltfqeugbsgtpx"
      via: "POST /v1/projects/{id}/database/query with Bearer ${SUPABASE_ACCESS_TOKEN}"
      pattern: "supabase.com/v1/projects/.*/database/query"
    - from: "DO $$ ASSERT $$ block"
      to: "atomic apply"
      via: "Postgres ASSERT raises EXCEPTION on mismatch — aborts the migration"
      pattern: "ASSERT v_view_columns = 23"
---

<objective>
[BLOCKING] Apply migration `supabase/migrations/176_template_drafts_and_vertical.sql` to the live Supabase database, run live SQL smoke SELECTs to confirm all 3 ROADMAP success criteria are satisfied, and re-run the existing gallery E2E smoke test (`tests/e2e/template-gallery.spec.js`) to confirm zero regressions from the VIEW replacement.

Purpose: Phase 176's deliverables are inert without a live DB apply — Phases 177/178/179 cannot read `template_drafts` or write `svg_templates.vertical` until the schema is physically in place. Build/typecheck pass without this step (no TypeScript types involved), creating a false-positive verification state if skipped — hence [BLOCKING].

Output: Live DB has migration 176 applied. Plan 03's negative-path RLS / CHECK tests can run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/176-schema-foundation/176-01-PLAN.md
@.planning/milestones/v20.0-phases/175-new-template-content-quality-pass/175-06-SUMMARY.md
@supabase/migrations/176_template_drafts_and_vertical.sql
@tests/e2e/template-gallery.spec.js

<interfaces>
<!-- Apply path: Supabase Management API direct, NOT `supabase db push`. -->
<!-- This matches the established v20.0 pattern (Phase 175 Plan 06). -->

From .planning/milestones/v20.0-phases/175-new-template-content-quality-pass/175-06-SUMMARY.md (Apply path — VERIFIED):
```
Tool: Direct Supabase Management API call (POST /v1/projects/{id}/database/query
       with Bearer ${SUPABASE_ACCESS_TOKEN}).
Rationale: large migrations exceed practical context budget for round-tripping
           through the MCP `apply_migration` tool inside an interactive
           orchestrator. The Mgmt API endpoint is the same backend the MCP
           server proxies; it accepts the SQL via JSON body without truncation.
Auth: Postgres superuser via management API (Pitfall 2 — superuser inserts
      ignore WITH CHECK predicates so seeding works).
Project ID: gdxizdiltfqeugbsgtpx
Result on success: HTTP 201, empty result array `[]` (DDL returns no rows).
```

For Phase 176, the migration is small (~250 lines, ~10KB), so the MCP `mcp__supabase__apply_migration` tool is also viable (and is the project-preferred path for small migrations). Either path is acceptable. Decision: prefer MCP `apply_migration` first (cleaner, uses the same Mgmt API under the hood, lower friction for small files); fall back to direct curl + Bearer token if MCP is unavailable.

Smoke SELECT path: use MCP `mcp__supabase__execute_sql` (Postgres superuser, RLS bypassed — fine for schema verification, NOT for RLS testing which is Plan 03's job).

From tests/e2e/template-gallery.spec.js header (VERIFIED):
- Spec is skip-guarded — entire suite skips when `TEST_USER_EMAIL` is unset
- Uses `loginAndPrepare` from `./helpers.js`
- Assertions are STRUCTURAL ONLY — no exact template-count checks (per TQAL-05)
- Run command: `npx playwright test tests/e2e/template-gallery.spec.js --project=chromium --reporter=list`
- Expected outcome on a healthy `gallery_templates` VIEW: PASS (or SKIPPED if TEST_USER_EMAIL unset). FAIL would indicate the VIEW replacement broke a column the gallery service consumes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 176-02-01: [BLOCKING] Apply migration 176 to live DB via MCP apply_migration (fallback: Mgmt API curl)</name>
  <files>(none — database-only side effect)</files>
  <read_first>
    - supabase/migrations/176_template_drafts_and_vertical.sql (must exist from Plan 01 — applying this exact file content)
    - .planning/milestones/v20.0-phases/175-new-template-content-quality-pass/175-06-SUMMARY.md (project apply pattern: direct Mgmt API. MCP apply_migration is the preferred wrapper for files of this size.)
  </read_first>
  <action>
  **[BLOCKING]** This task applies migration 176 to the live Supabase project. It MUST succeed before Task 176-02-02 (smoke SELECTs) and before Plan 03 begins.

  ### Step 1: Confirm the migration file exists and is the version Plan 01 produced
  ```bash
  test -f supabase/migrations/176_template_drafts_and_vertical.sql || { echo "FAIL: migration file missing — Plan 01 did not run"; exit 1; }
  wc -l supabase/migrations/176_template_drafts_and_vertical.sql
  ```
  Expected: file exists; line count is approximately 200-260 (sanity ceiling — if it's 0 or > 500 something is wrong).

  ### Step 2: Apply the migration via MCP `mcp__supabase__apply_migration` (PREFERRED PATH)
  Read the entire file content and pass it to the MCP tool:

  ```
  mcp__supabase__apply_migration(
    name='176_template_drafts_and_vertical',
    query=<full file content>
  )
  ```

  Expected on success: HTTP 201 / empty array result `[]`. The embedded DO $$ ASSERT $$ block in the migration runs as part of the apply — if any of the 6 assertions fail, the entire migration aborts and the tool returns an error string containing the ASSERT failure message (e.g., `expected 8 columns on template_drafts, got 7`).

  **If the ASSERT block fails:** Do NOT proceed. The migration apply is atomic — Postgres rolls everything back. Report the exact ASSERT message; the failure mode is a Plan 01 bug (file content does not match acceptance criteria), not an apply infrastructure issue. Re-open Plan 01 with the specific failure.

  ### Step 3 (FALLBACK ONLY — if MCP `apply_migration` is unavailable or errors with infrastructure)
  Use direct Mgmt API per Phase 175 Plan 06 pattern:

  ```bash
  test -n "$SUPABASE_ACCESS_TOKEN" || { echo "FAIL: SUPABASE_ACCESS_TOKEN not set"; exit 1; }
  PROJECT_ID="gdxizdiltfqeugbsgtpx"
  SQL=$(cat supabase/migrations/176_template_drafts_and_vertical.sql)
  curl -sS -X POST "https://api.supabase.com/v1/projects/${PROJECT_ID}/database/query" \
    -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
    -H "Content-Type: application/json" \
    -d "$(jq -nc --arg q "$SQL" '{query: $q}')"
  ```
  Expected: HTTP 201, body `[]`. Same atomicity guarantees as the MCP path (since MCP proxies this same endpoint).

  ### Step 4: Post-apply schema verification (REQUIRED — this is the [BLOCKING] gate)

  After the apply call returns success, the executor MUST run the following SQL via `mcp__supabase__execute_sql` (or via a Mgmt API curl to the same `/database/query` endpoint with the SAME Bearer token used in Step 2/3) and assert the exact expected counts. Any mismatch FAILS this task — do NOT proceed to Task 176-02-02.

  Run all four checks in a single MCP `execute_sql` call (one round-trip):
  ```sql
  SELECT
    (SELECT count(*) FROM information_schema.columns
       WHERE table_schema='public' AND table_name='template_drafts')                    AS drafts_cols,
    (SELECT count(*) FROM information_schema.columns
       WHERE table_schema='public' AND table_name='gallery_templates')                  AS view_cols,
    (SELECT relrowsecurity FROM pg_class
       WHERE relname='template_drafts' AND relnamespace='public'::regnamespace)         AS drafts_rls,
    (SELECT count(*) FROM pg_policies
       WHERE tablename='template_drafts' AND polname='template_drafts_admin_only')      AS admin_policy_count;
  ```

  Expected exact values:
  - `drafts_cols` = **8**
  - `view_cols` = **23**
  - `drafts_rls` = **true**
  - `admin_policy_count` = **1**

  If ANY value is not exactly the expected value, this task is FAILED — capture the actual values in the SUMMARY, do not run Task 176-02-02, and report a Plan 01 / Plan 02 bug.

  ### Step 5: Capture the apply outcome
  Record in scratch notes (will go into the SUMMARY in Plan 02 final task):
  - Tool path used (MCP apply_migration vs Mgmt API curl)
  - HTTP status and result body
  - Apply timestamp
  - Step 4 verification output (drafts_cols, view_cols, drafts_rls, admin_policy_count) — must match expected values exactly
  - Any DO ASSERT message (only present on failure)

  Do NOT proceed to Task 176-02-02 until this task returns a successful apply AND the Step 4 verification matches all four expected values.
  </action>
  <verify>
    <automated>
      # The executor MUST run this verification via mcp__supabase__execute_sql (or Mgmt API curl with $SUPABASE_ACCESS_TOKEN to /database/query) — NOT via a no-op.
      # The four expected values are: drafts_cols=8, view_cols=23, drafts_rls=true, admin_policy_count=1.
      # If any value is not exactly the expected, exit non-zero.
      #
      # SQL (single round-trip, run via MCP execute_sql or curl to /database/query):
      #   SELECT
      #     (SELECT count(*) FROM information_schema.columns
      #        WHERE table_schema='public' AND table_name='template_drafts')               AS drafts_cols,
      #     (SELECT count(*) FROM information_schema.columns
      #        WHERE table_schema='public' AND table_name='gallery_templates')             AS view_cols,
      #     (SELECT relrowsecurity FROM pg_class
      #        WHERE relname='template_drafts' AND relnamespace='public'::regnamespace)    AS drafts_rls,
      #     (SELECT count(*) FROM pg_policies
      #        WHERE tablename='template_drafts' AND polname='template_drafts_admin_only') AS admin_policy_count;
      #
      # The executor MUST assert each value matches exactly. Below is a Mgmt-API curl harness fallback that
      # the executor's bash environment can run when SUPABASE_ACCESS_TOKEN is exported and jq is available;
      # otherwise the executor should run the SQL via MCP execute_sql and perform the assertion in-tool.
      bash -c '
        set -euo pipefail
        if [ -z "${SUPABASE_ACCESS_TOKEN:-}" ]; then
          echo "WARN: SUPABASE_ACCESS_TOKEN not set in shell — executor must run the verification SELECT via mcp__supabase__execute_sql instead and assert drafts_cols=8, view_cols=23, drafts_rls=true, admin_policy_count=1."
          exit 1
        fi
        PROJECT_ID="gdxizdiltfqeugbsgtpx"
        SQL="SELECT (SELECT count(*) FROM information_schema.columns WHERE table_schema='"'"'public'"'"' AND table_name='"'"'template_drafts'"'"') AS drafts_cols, (SELECT count(*) FROM information_schema.columns WHERE table_schema='"'"'public'"'"' AND table_name='"'"'gallery_templates'"'"') AS view_cols, (SELECT relrowsecurity FROM pg_class WHERE relname='"'"'template_drafts'"'"' AND relnamespace='"'"'public'"'"'::regnamespace) AS drafts_rls, (SELECT count(*) FROM pg_policies WHERE tablename='"'"'template_drafts'"'"' AND polname='"'"'template_drafts_admin_only'"'"') AS admin_policy_count;"
        BODY=$(jq -nc --arg q "$SQL" '"'"'{query: $q}'"'"')
        RESP=$(curl -sS -X POST "https://api.supabase.com/v1/projects/${PROJECT_ID}/database/query" \
                 -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
                 -H "Content-Type: application/json" \
                 -d "$BODY")
        DC=$(echo "$RESP" | jq -r ".[0].drafts_cols")
        VC=$(echo "$RESP" | jq -r ".[0].view_cols")
        DR=$(echo "$RESP" | jq -r ".[0].drafts_rls")
        AP=$(echo "$RESP" | jq -r ".[0].admin_policy_count")
        echo "drafts_cols=$DC view_cols=$VC drafts_rls=$DR admin_policy_count=$AP"
        [ "$DC" = "8" ] || { echo "FAIL: drafts_cols expected 8, got $DC"; exit 1; }
        [ "$VC" = "23" ] || { echo "FAIL: view_cols expected 23, got $VC"; exit 1; }
        [ "$DR" = "true" ] || { echo "FAIL: drafts_rls expected true, got $DR"; exit 1; }
        [ "$AP" = "1" ] || { echo "FAIL: admin_policy_count expected 1, got $AP"; exit 1; }
        echo OK
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - Migration apply call returns success (HTTP 201 / empty result, OR MCP tool returns success without an ASSERT exception)
    - The MCP `mcp__supabase__list_tables` (or equivalent introspection) lists `template_drafts` in the public schema after the apply
    - Apply timestamp captured
    - Post-apply verification SELECT returns exactly: `drafts_cols=8`, `view_cols=23`, `drafts_rls=true`, `admin_policy_count=1` — any mismatch is a hard FAIL
    - If the apply failed: this task is reported as FAILED with the exact ASSERT / Postgres error message and Plan 03 is NOT started
  </acceptance_criteria>
  <done>Migration 176 is applied atomically to the live Supabase project. The embedded DO ASSERT block did not raise — meaning the schema state matches all 6 assertions (8 columns on template_drafts, RLS enabled, admin policy present, svg_templates.vertical exists, gallery_templates VIEW has vertical, gallery_templates has 23 columns). Step 4 post-apply SELECT independently confirms drafts_cols=8 / view_cols=23 / drafts_rls=true / admin_policy_count=1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 176-02-02: Live smoke SELECTs — confirm all 3 ROADMAP success criteria via MCP execute_sql</name>
  <files>(none — read-only DB queries)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 176 success criteria — the 3 SCs we are confirming live)
    - supabase/migrations/176_template_drafts_and_vertical.sql (so the executor knows the expected schema shape)
  </read_first>
  <action>
  Run the following 6 smoke SELECTs against the live Supabase project via `mcp__supabase__execute_sql`. ALL 6 must succeed; ANY error or unexpected row count is a hard fail and Plan 03 must not start.

  The executor MUST issue these via `mcp__supabase__execute_sql` (preferred) OR via curl to `https://api.supabase.com/v1/projects/${PROJECT_ID}/database/query` with `Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}`. The `<automated>` block below provides a curl-based harness that asserts each smoke result and exits non-zero on any mismatch — the executor must run it (or run the equivalent assertions via MCP and exit non-zero on failure).

  ### Smoke 1 — SC-1.a: template_drafts table exists, is empty, and has the 8 expected columns
  ```sql
  SELECT column_name, data_type, is_nullable
  FROM information_schema.columns
  WHERE table_schema = 'public' AND table_name = 'template_drafts'
  ORDER BY ordinal_position;
  ```
  Expected: 8 rows. Column names (in any order — sort by ordinal_position is hint, not constraint): `id`, `svg_content`, `prompt`, `source`, `status`, `vertical`, `metadata`, `created_at`. `id` is uuid NOT NULL; `svg_content` is text NOT NULL; `created_at` is timestamp with time zone NOT NULL.

  ### Smoke 2 — SC-1.a: SELECT * FROM template_drafts returns empty result, no error
  ```sql
  SELECT COUNT(*) AS row_count FROM template_drafts;
  ```
  Expected: row_count = 0.

  ### Smoke 3 — SC-1.b: RLS enabled + admin-only policy exists
  ```sql
  SELECT
    (SELECT relrowsecurity FROM pg_class WHERE relname = 'template_drafts' AND relnamespace = 'public'::regnamespace) AS rls_enabled,
    (SELECT polname FROM pg_policies WHERE tablename = 'template_drafts') AS policy_name;
  ```
  Expected: rls_enabled = true; policy_name = `template_drafts_admin_only`.

  ### Smoke 4 — SC-2: svg_templates.vertical column + CHECK exist
  ```sql
  SELECT vertical FROM svg_templates LIMIT 1;
  ```
  Expected: 1 row, value either NULL or one of `restaurants`/`retail`/`healthcare`. No error.

  ```sql
  SELECT conname, pg_get_constraintdef(oid)
  FROM pg_constraint
  WHERE conrelid = 'public.svg_templates'::regclass
    AND conname = 'chk_svg_templates_vertical_enum';
  ```
  Expected: 1 row; pg_get_constraintdef contains both `'restaurants'` and `'healthcare'`.

  ### Smoke 5 — SC-3.a: gallery_templates VIEW has `vertical` column and is queryable
  ```sql
  SELECT vertical FROM gallery_templates LIMIT 1;
  ```
  Expected: 1 row (since svg_templates has 127 active rows from Phase 175). Value is NULL (because Phase 178 hasn't backfilled yet — this is correct).

  ### Smoke 6 — SC-3.b: gallery_templates VIEW has exactly 23 columns
  ```sql
  SELECT COUNT(*) AS view_column_count
  FROM information_schema.columns
  WHERE table_schema = 'public' AND table_name = 'gallery_templates';
  ```
  Expected: view_column_count = 23.

  Capture all 6 outputs verbatim for the SUMMARY.
  </action>
  <verify>
    <automated>
      # The executor MUST run all 6 smokes via mcp__supabase__execute_sql (preferred) OR via curl to
      # https://api.supabase.com/v1/projects/$PROJECT_ID/database/query with $SUPABASE_ACCESS_TOKEN.
      # The harness below runs the smokes via curl when SUPABASE_ACCESS_TOKEN is exported in the shell;
      # otherwise the executor MUST execute the equivalent assertions in-MCP and exit non-zero on any mismatch.
      bash -c '
        set -euo pipefail
        if [ -z "${SUPABASE_ACCESS_TOKEN:-}" ]; then
          echo "WARN: SUPABASE_ACCESS_TOKEN not set in shell — executor must run the 6 smokes via mcp__supabase__execute_sql and assert: drafts_col_count=8, drafts_row_count=0, rls_enabled=true, policy_name=template_drafts_admin_only, vertical_check_def_contains restaurants+healthcare+retail, gallery_view_col_count=23."
          exit 1
        fi
        PROJECT_ID="gdxizdiltfqeugbsgtpx"
        run_sql() {
          local SQL="$1"
          local BODY=$(jq -nc --arg q "$SQL" '"'"'{query: $q}'"'"')
          curl -sS -X POST "https://api.supabase.com/v1/projects/${PROJECT_ID}/database/query" \
            -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$BODY"
        }
        # Smoke 1: template_drafts has 8 columns
        S1=$(run_sql "SELECT count(*) AS c FROM information_schema.columns WHERE table_schema='"'"'public'"'"' AND table_name='"'"'template_drafts'"'"'")
        S1V=$(echo "$S1" | jq -r ".[0].c")
        [ "$S1V" = "8" ] || { echo "FAIL Smoke 1: drafts cols expected 8 got $S1V"; exit 1; }
        # Smoke 2: template_drafts is empty
        S2=$(run_sql "SELECT count(*) AS c FROM template_drafts")
        S2V=$(echo "$S2" | jq -r ".[0].c")
        [ "$S2V" = "0" ] || { echo "FAIL Smoke 2: drafts row count expected 0 got $S2V"; exit 1; }
        # Smoke 3: RLS enabled + admin policy exists
        S3=$(run_sql "SELECT (SELECT relrowsecurity FROM pg_class WHERE relname='"'"'template_drafts'"'"' AND relnamespace='"'"'public'"'"'::regnamespace) AS rls, (SELECT polname FROM pg_policies WHERE tablename='"'"'template_drafts'"'"' LIMIT 1) AS pname")
        S3R=$(echo "$S3" | jq -r ".[0].rls")
        S3P=$(echo "$S3" | jq -r ".[0].pname")
        [ "$S3R" = "true" ] || { echo "FAIL Smoke 3a: rls expected true got $S3R"; exit 1; }
        [ "$S3P" = "template_drafts_admin_only" ] || { echo "FAIL Smoke 3b: policy expected template_drafts_admin_only got $S3P"; exit 1; }
        # Smoke 4: svg_templates.vertical column exists + CHECK contains 3 enum values
        S4=$(run_sql "SELECT pg_get_constraintdef(oid) AS def FROM pg_constraint WHERE conrelid='"'"'public.svg_templates'"'"'::regclass AND conname='"'"'chk_svg_templates_vertical_enum'"'"'")
        S4D=$(echo "$S4" | jq -r ".[0].def")
        echo "$S4D" | grep -q "restaurants" || { echo "FAIL Smoke 4: CHECK def missing restaurants: $S4D"; exit 1; }
        echo "$S4D" | grep -q "retail" || { echo "FAIL Smoke 4: CHECK def missing retail: $S4D"; exit 1; }
        echo "$S4D" | grep -q "healthcare" || { echo "FAIL Smoke 4: CHECK def missing healthcare: $S4D"; exit 1; }
        # Smoke 5: gallery_templates VIEW has vertical column queryable
        S5=$(run_sql "SELECT count(*) AS c FROM information_schema.columns WHERE table_schema='"'"'public'"'"' AND table_name='"'"'gallery_templates'"'"' AND column_name='"'"'vertical'"'"'")
        S5V=$(echo "$S5" | jq -r ".[0].c")
        [ "$S5V" = "1" ] || { echo "FAIL Smoke 5: VIEW vertical col expected 1 got $S5V"; exit 1; }
        # Smoke 6: gallery_templates VIEW has 23 columns total
        S6=$(run_sql "SELECT count(*) AS c FROM information_schema.columns WHERE table_schema='"'"'public'"'"' AND table_name='"'"'gallery_templates'"'"'")
        S6V=$(echo "$S6" | jq -r ".[0].c")
        [ "$S6V" = "23" ] || { echo "FAIL Smoke 6: view col count expected 23 got $S6V"; exit 1; }
        echo OK
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - Smoke 1 returns exactly 8 column rows for `template_drafts`; column names exactly match the set {id, svg_content, prompt, source, status, vertical, metadata, created_at}
    - Smoke 2 returns row_count = 0 (template_drafts is empty post-apply)
    - Smoke 3 returns rls_enabled = true AND policy_name = `template_drafts_admin_only`
    - Smoke 4 part 1 returns 1 row with value NULL (or one of the 3 enum values; NULL expected on first apply)
    - Smoke 4 part 2 returns 1 row; pg_get_constraintdef output contains substring `'restaurants'` AND substring `'healthcare'` AND substring `'retail'`
    - Smoke 5 returns 1 row from gallery_templates VIEW with `vertical` column accessible (NULL value is acceptable)
    - Smoke 6 returns view_column_count = 23 (exact)
    - All 6 smokes complete without raising a Postgres error
  </acceptance_criteria>
  <done>Live DB state matches every assertion the migration's DO ASSERT block made. SC-1, SC-2, and SC-3 are confirmed at the database layer. Plan 03 (negative-path tests) is unblocked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 176-02-03: Re-run gallery E2E smoke (template-gallery.spec.js) — confirm zero regressions from VIEW replacement</name>
  <files>(none — test run only)</files>
  <read_first>
    - tests/e2e/template-gallery.spec.js (the spec being re-run; structural-only assertions, skip-guarded on TEST_USER_EMAIL)
  </read_first>
  <action>
  Re-run the existing gallery E2E smoke against the live DB (now carrying migration 176). The spec is skip-guarded; on machines without `TEST_USER_EMAIL` it skips cleanly, which is acceptable. On machines with credentials, it MUST pass — a failure here means the VIEW replacement broke a column the gallery service consumes.

  ### Run command
  ```bash
  npx playwright test tests/e2e/template-gallery.spec.js --project=chromium --reporter=list
  ```

  ### Outcome handling
  - **Exit 0 + tests PASSED:** Zero regression. Phase 176 SC-3 second clause confirmed.
  - **Exit 0 + tests SKIPPED (TEST_USER_EMAIL unset):** Acceptable per project pattern. Note in SUMMARY that the structural-grep verification on the VIEW (Smoke 5/6 in Task 176-02-02) is the de-facto check.
  - **Exit non-zero (any test FAILED):** This is a hard fail. Capture the failing test name and Playwright error output. Do NOT proceed. The VIEW replacement is regressing the gallery service contract — likely a column name typo, wrong cast, or missing column. Reopen Plan 01 (the migration file) and re-deliver.

  Capture run outcome (PASS / SKIPPED / FAILED) and any failure detail for the SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/template-gallery.spec.js --project=chromium --reporter=list 2>&1; PW_EXIT=$?; [ $PW_EXIT -eq 0 ] && echo OK || echo "playwright exit=$PW_EXIT"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/template-gallery.spec.js --project=chromium --reporter=list` exits 0
    - Test outcome is either PASSED or SKIPPED (never FAILED)
    - If SKIPPED: the output explicitly contains a skip reason (typically `TEST_USER_EMAIL` env var unset) — this is the project-standard skip guard from Phase 171 onward
    - If any test FAILED: this task is reported as FAILED with the exact test title and error message
  </acceptance_criteria>
  <done>Gallery E2E smoke confirms zero regression — migration 176's gallery_templates VIEW replacement does not break any existing structural assertion in the test suite. Together with Smoke 5/6 from the previous task, SC-3 is fully verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local executor → Supabase Mgmt API | Bearer SUPABASE_ACCESS_TOKEN; same posture as Phase 175 Plan 06 |
| Live DB schema → application code | Migration is atomic — either fully applied (with DO ASSERT passing) or fully rolled back (no partial state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-02-01 | Tampering | Partial migration apply (e.g., template_drafts created but RLS not enabled) leaves DB in unsafe state | mitigate | Migration is atomic in a single Postgres transaction; the DO ASSERT block at the END of the file runs in the same transaction — any failed assertion rolls back the entire migration including the table creation. |
| T-176-02-02 | Information Disclosure | SUPABASE_ACCESS_TOKEN logged or persisted | mitigate | Use Bearer header only; do NOT echo the token. The Mgmt API curl path uses `-H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}"` — bash variable, not literal. |
| T-176-02-03 | Repudiation | No record of the apply for audit | mitigate | SUMMARY in Plan 02 captures: tool path, HTTP status, apply timestamp, full smoke SELECT outputs, gallery E2E result. |
| T-176-02-04 | Denial of Service | Apply blocks live traffic (locks svg_templates) | accept | ALTER TABLE ADD COLUMN with no default, ALTER TABLE ADD CONSTRAINT (without NOT VALID), CREATE OR REPLACE VIEW are all fast metadata-only operations on Postgres. svg_templates has only 127 rows. Lock duration negligible. |
</threat_model>

<verification>
- Apply outcome (Task 176-02-01): MCP tool / Mgmt API HTTP 201 + DO ASSERT block did not raise + post-apply SELECT confirms drafts_cols=8, view_cols=23, drafts_rls=true, admin_policy_count=1
- Smoke SELECT outcomes (Task 176-02-02): all 6 smokes pass; gallery_templates VIEW has 23 columns post-migration
- Gallery E2E outcome (Task 176-02-03): PASS or SKIPPED, never FAIL
</verification>

<success_criteria>
- Migration 176 is applied to live DB
- All 6 live smoke SELECTs return expected results matching ROADMAP SC-1, SC-2, SC-3 (gallery_templates VIEW has exactly 23 columns)
- `tests/e2e/template-gallery.spec.js` exits 0 (PASSED or SKIPPED)
- All 3 ROADMAP success criteria for Phase 176 are confirmed at the database layer (modulo Plan 03's negative-path tests for the rejection clauses of SC-1 and SC-2)
</success_criteria>

<output>
After completion, create `.planning/phases/176-schema-foundation/176-02-SUMMARY.md` with:
- Apply tool path used (MCP `apply_migration` or Mgmt API curl)
- Apply HTTP status + result body + timestamp
- Post-apply Step 4 verification output (drafts_cols=8, view_cols=23, drafts_rls=true, admin_policy_count=1)
- Full output of all 6 smoke SELECTs (verbatim)
- Gallery E2E outcome (PASS / SKIPPED / FAILED) and any detail
- Verdict: SC-1 a-clauses, SC-2 a-clauses, SC-3 PASS (with note that SC-1.b "non-admin INSERT rejection" and SC-2.b "invalid vertical CHECK rejection" are Plan 03's job)
- Hand-off note to Plan 03: "Negative-path tests (admin RLS rejection + CHECK violation) are next."
</output>
</output>
