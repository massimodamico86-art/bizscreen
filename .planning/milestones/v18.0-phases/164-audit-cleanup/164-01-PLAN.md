---
phase: 164-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/scenes.spec.js
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "scenes.spec.js loads without module errors"
    - "scenes.spec.js does not import assertAppReady"
    - "scenes.spec.js still imports all other helpers it uses"
  artifacts:
    - path: "tests/e2e/scenes.spec.js"
      provides: "Scene E2E tests with clean imports"
      contains: "import { loginAndPrepare, waitForPageReady, navigateToSection, dismissAnyModals } from './helpers/index.js'"
  key_links:
    - from: "tests/e2e/scenes.spec.js"
      to: "tests/e2e/helpers/index.js"
      via: "ES module import"
      pattern: "import.*loginAndPrepare.*from.*helpers/index"
---

<objective>
Remove the unused `assertAppReady` import from `scenes.spec.js` to close the single low-severity integration gap identified in the v18.0 milestone audit.

Purpose: Clean up an orphaned import that was added during Phase 161 import fixes but is never called in this file.
Output: A clean `scenes.spec.js` with no unused imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/scenes.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused assertAppReady import from scenes.spec.js</name>
  <files>tests/e2e/scenes.spec.js</files>
  <read_first>tests/e2e/scenes.spec.js</read_first>
  <action>
On line 11 of tests/e2e/scenes.spec.js, change:

```javascript
import { loginAndPrepare, waitForPageReady, navigateToSection, dismissAnyModals, assertAppReady } from './helpers/index.js';
```

to:

```javascript
import { loginAndPrepare, waitForPageReady, navigateToSection, dismissAnyModals } from './helpers/index.js';
```

Remove ONLY the `, assertAppReady` portion. Keep all other imports intact. Do not modify any other lines in the file.

Verify the file still parses correctly by running Playwright's list command on it.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -c "assertAppReady" tests/e2e/scenes.spec.js | grep -q "^0$" && echo "PASS: no assertAppReady references" && npx playwright test tests/e2e/scenes.spec.js --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "assertAppReady" tests/e2e/scenes.spec.js` returns zero matches
    - Line 11 reads exactly: `import { loginAndPrepare, waitForPageReady, navigateToSection, dismissAnyModals } from './helpers/index.js';`
    - `npx playwright test tests/e2e/scenes.spec.js --list` completes without parse errors
    - No other lines in the file are modified
  </acceptance_criteria>
  <done>scenes.spec.js has no unused assertAppReady import, file parses without errors, all other imports and test code unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This change removes an unused import from a test file; no production code is modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | N/A | tests/e2e/scenes.spec.js | accept | Test-only file, no security impact. Removing an unused import has zero effect on application security posture. |
</threat_model>

<verification>
1. `grep "assertAppReady" tests/e2e/scenes.spec.js` returns no matches
2. `npx playwright test tests/e2e/scenes.spec.js --list` parses without errors
3. Line 11 contains exactly four helper imports (loginAndPrepare, waitForPageReady, navigateToSection, dismissAnyModals)
</verification>

<success_criteria>
- scenes.spec.js no longer imports assertAppReady
- File parses correctly with Playwright
- CONT-01 integration gap from v18.0 audit is closed
</success_criteria>

<output>
After completion, create `.planning/phases/164-audit-cleanup/164-01-SUMMARY.md`
</output>
