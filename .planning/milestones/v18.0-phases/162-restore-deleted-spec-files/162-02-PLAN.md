---
phase: 162-restore-deleted-spec-files
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - tests/e2e/playlists.spec.js
  - tests/e2e/template-marketplace.spec.js
autonomous: true
requirements:
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-07
  - CONT-08
  - CONT-09
  - CONT-10
  - CONT-11
  - CONT-12

must_haves:
  truths:
    - "playlists.spec.js contains full test coverage for CONT-02, CONT-03, CONT-04, CONT-08, CONT-09, CONT-10"
    - "template-marketplace.spec.js contains full test coverage for CONT-07, CONT-11, CONT-12"
    - "All 29 restored/repaired spec files parse without Playwright errors"
    - "All 77 requirement IDs have runnable spec coverage across the 29 files"
  artifacts:
    - path: "tests/e2e/playlists.spec.js"
      provides: "Full playlist test coverage (CONT-02..04, CONT-08..10)"
      min_lines: 600
    - path: "tests/e2e/template-marketplace.spec.js"
      provides: "Full template test coverage (CONT-07, CONT-11, CONT-12)"
      min_lines: 450
  key_links:
    - from: "tests/e2e/playlists.spec.js"
      to: "CONT-02"
      via: "test.describe('Playlist Editing (CONT-02)')"
      pattern: "CONT-02"
    - from: "tests/e2e/template-marketplace.spec.js"
      to: "CONT-11"
      via: "test('can apply a template via hover overlay (CONT-11)')"
      pattern: "CONT-11"
---

<objective>
Restore the 2 overwritten spec files (playlists.spec.js and template-marketplace.spec.js) to their full pre-deletion content and validate that all 29 restored/repaired files parse correctly with Playwright.

Purpose: The current playlists.spec.js (209 lines) and template-marketplace.spec.js (384 lines) were truncated by the worktree merge. The pre-deletion versions (619 and 484 lines respectively) contain the full CONT-02..04/07..12 test coverage.
Output: 2 overwritten files restored to full content + full parse validation of all 29 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-restore-deleted-spec-files/162-RESEARCH.md
@.planning/phases/162-restore-deleted-spec-files/162-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore overwritten playlists.spec.js and template-marketplace.spec.js</name>
  <files>tests/e2e/playlists.spec.js, tests/e2e/template-marketplace.spec.js</files>
  <read_first>
    - tests/e2e/playlists.spec.js (current state: 209 lines, truncated — missing CONT-02/03/04/08/09/10 test groups)
    - tests/e2e/template-marketplace.spec.js (current state: 384 lines, truncated — may be missing CONT-11/12)
    - .planning/phases/162-restore-deleted-spec-files/162-RESEARCH.md (Patterns 2 and 3)
  </read_first>
  <action>
Restore both files from git history to their full pre-deletion versions:

```bash
git checkout 75371133~1 -- tests/e2e/playlists.spec.js tests/e2e/template-marketplace.spec.js
```

After restore, verify the pre-deletion content is in place:

For playlists.spec.js (should be ~619 lines):
```bash
wc -l tests/e2e/playlists.spec.js
grep -c "test.describe" tests/e2e/playlists.spec.js
grep "CONT-" tests/e2e/playlists.spec.js
```

Expected: File contains `test.describe('Playlist Editing (CONT-02)')`, `test.describe('Nested Playlists (CONT-03)')`, `test.describe('Background Audio (CONT-04)')`, `test.describe('Drag-Drop Reorder (CONT-08)')`, `test.describe('Playlist Empty State (CONT-09 partial)')`, `test.describe('Playlist Validation (CONT-10 partial)')`.

For template-marketplace.spec.js (should be ~484 lines):
```bash
wc -l tests/e2e/template-marketplace.spec.js
grep "CONT-" tests/e2e/template-marketplace.spec.js
```

Expected: File contains `CONT-11` (apply via hover overlay) and `CONT-12` (template customization).
  </action>
  <verify>
    <automated>wc -l < tests/e2e/playlists.spec.js | awk '$1 >= 600 {print "playlists.spec.js line count OK: "$1; exit 0} $1 < 600 {print "FAIL: playlists.spec.js only "$1" lines (expected >= 600)"; exit 1}' && grep -q "CONT-02" tests/e2e/playlists.spec.js && grep -q "CONT-08" tests/e2e/playlists.spec.js && wc -l < tests/e2e/template-marketplace.spec.js | awk '$1 >= 450 {print "template-marketplace.spec.js line count OK: "$1; exit 0} $1 < 450 {print "FAIL: template-marketplace.spec.js only "$1" lines (expected >= 450)"; exit 1}' && grep -q "CONT-11" tests/e2e/template-marketplace.spec.js && echo "Both overwritten specs restored"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/playlists.spec.js has >= 600 lines (pre-deletion was 619)
    - tests/e2e/playlists.spec.js contains `CONT-02`
    - tests/e2e/playlists.spec.js contains `CONT-03`
    - tests/e2e/playlists.spec.js contains `CONT-04`
    - tests/e2e/playlists.spec.js contains `CONT-08`
    - tests/e2e/playlists.spec.js contains `CONT-09`
    - tests/e2e/playlists.spec.js contains `CONT-10`
    - tests/e2e/template-marketplace.spec.js has >= 450 lines (pre-deletion was 484)
    - tests/e2e/template-marketplace.spec.js contains `CONT-11`
    - tests/e2e/template-marketplace.spec.js contains `CONT-12`
  </acceptance_criteria>
  <done>Both overwritten spec files restored to their full pre-deletion content with complete CONT requirement coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Validate all 29 restored/repaired spec files parse with Playwright</name>
  <files></files>
  <read_first>
    - .planning/phases/162-restore-deleted-spec-files/162-RESEARCH.md (Validation Architecture section)
  </read_first>
  <action>
Run Playwright's list mode to verify all 29 restored/repaired spec files parse without errors:

```bash
npx playwright test --list 2>&1
```

Check the output for:
1. No lines containing "Error", "Cannot find", "does not provide an export named", or "SyntaxError"
2. All 29 spec files appear in the test list (26 restored + 2 overwritten-then-restored + layouts-screenshots.spec.js from Phase 161)

Verify specific file discovery:
```bash
npx playwright test --list 2>&1 | grep -c "auth-login-logout\|auth-signup-reset-invite" 
# Expected: >= 2 (at least 1 test from each auth file)

npx playwright test --list 2>&1 | grep -c "nav-route-loading\|nav-error-handling\|nav-responsive\|nav-accessibility" 
# Expected: >= 4

npx playwright test --list 2>&1 | grep -c "screen-management\|screen-groups"
# Expected: >= 2

npx playwright test --list 2>&1 | grep -c "scheduling\|campaigns"
# Expected: >= 2

npx playwright test --list 2>&1 | grep -c "widgets-basic\|widgets-data\|widgets-embeds\|widgets-rendering\|edge-functions"
# Expected: >= 5

npx playwright test --list 2>&1 | grep -c "menu-boards\|multi-language"
# Expected: >= 2

npx playwright test --list 2>&1 | grep -c "admin-settings"
# Expected: >= 3

npx playwright test --list 2>&1 | grep -c "enterprise-sso\|enterprise-api\|enterprise-analytics"
# Expected: >= 3

npx playwright test --list 2>&1 | grep -c "player-rendering\|player-offline\|player-telemetry"
# Expected: >= 3

npx playwright test --list 2>&1 | grep -c "playlists\|template-marketplace"
# Expected: >= 2
```

If ANY import errors appear, diagnose and fix before proceeding. Common fixes documented in RESEARCH.md Common Pitfalls section.

Final count check:
```bash
npx playwright test --list 2>&1 | grep -cE "Error|Cannot find|does not provide|SyntaxError"
# Expected: 0
```
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | grep -cE "Error|Cannot find|does not provide|SyntaxError" | awk '$1 == 0 {print "No parse errors"; exit 0} $1 > 0 {print "FAIL: "$1" parse errors found"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list` exits without error
    - Output contains 0 lines matching "Error", "Cannot find", "does not provide", or "SyntaxError"
    - Output lists tests from auth-login-logout.spec.js
    - Output lists tests from nav-route-loading.spec.js
    - Output lists tests from screen-management.spec.js
    - Output lists tests from scheduling.spec.js
    - Output lists tests from widgets-basic.spec.js
    - Output lists tests from menu-boards.spec.js
    - Output lists tests from admin-settings-team.spec.js
    - Output lists tests from enterprise-sso.spec.js
    - Output lists tests from player-rendering.spec.js
    - Output lists tests from playlists.spec.js (with CONT-02 test groups)
    - Output lists tests from template-marketplace.spec.js (with CONT-11 test groups)
    - Output lists tests from layouts-screenshots.spec.js (Phase 161, not broken by fixtures merge)
  </acceptance_criteria>
  <done>All 29 restored/repaired spec files parse correctly with Playwright. Zero import or syntax errors. All 77 requirement IDs have runnable spec coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> working tree | Restoring overwritten files from git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-03 | Tampering | Overwritten spec restore | accept | Same local git history source as Plan 01. No external input. |
| T-162-04 | Denial of Service | Playwright --list parse validation | accept | Parse-only mode (--list), no browser launch, completes in ~5 seconds |
</threat_model>

<verification>
After Task 1: `wc -l tests/e2e/playlists.spec.js` shows >= 600 lines AND `grep -q "CONT-02" tests/e2e/playlists.spec.js`

After Task 2: `npx playwright test --list 2>&1 | grep -cE "Error|Cannot find|SyntaxError"` outputs 0
</verification>

<success_criteria>
- playlists.spec.js restored to >= 600 lines with CONT-02/03/04/08/09/10 test describe blocks
- template-marketplace.spec.js restored to >= 450 lines with CONT-11/12 test groups
- `npx playwright test --list` shows zero parse errors across all spec files
- layouts-screenshots.spec.js (Phase 161) still parses correctly (fixtures/index.js merge preserved constants)
</success_criteria>

<output>
After completion, create `.planning/phases/162-restore-deleted-spec-files/162-02-SUMMARY.md`
</output>
