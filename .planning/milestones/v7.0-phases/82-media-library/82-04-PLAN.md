---
phase: 82-media-library
plan: 04
type: execute
wave: 2
depends_on:
  - "82-01"
  - "82-02"
  - "82-03"
files_modified:
  - src/pages/MediaLibraryPage.jsx
autonomous: false
requirements:
  - MEDIA-04

must_haves:
  truths:
    - "Type filter buttons (All, Images, Videos, Documents) correctly filter the grid"
    - "Search results update as the user types (live/debounced), no Enter required"
    - "Empty state message appears when search or filter returns no results"
    - "Applying a type filter AND searching further narrows results correctly (combined state works)"
    - "Clear filter/search reverts to showing all items"
  artifacts:
    - path: "src/pages/MediaLibraryPage.jsx"
      provides: "Filter panel with type buttons and search bar wired to state"
  key_links:
    - from: "src/pages/MediaLibraryPage.jsx"
      to: "setTypeFilter"
      via: "filter button onClick calls setTypeFilter(option.id)"
      pattern: "setTypeFilter.*option\\.id"
    - from: "src/pages/MediaLibraryPage.jsx"
      to: "setSearch"
      via: "search input onChange calls setSearch(e.target.value)"
      pattern: "setSearch.*e\\.target\\.value"
---

<objective>
Verify and fix the filter and search functionality: type filter buttons, live search behavior, empty state messages, and combined filter+search mode.

Purpose: MEDIA-04 — user can filter the library by type and search by filename, with results updating correctly.
Output: All filter/search interactions verified correct; any gaps fixed; human checkpoint confirms behavior end-to-end.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-media-library/82-CONTEXT.md
@.planning/phases/82-media-library/82-01-SUMMARY.md
@.planning/phases/82-media-library/82-02-SUMMARY.md
@.planning/phases/82-media-library/82-03-SUMMARY.md

Key files to read before implementing:
@src/pages/MediaLibraryPage.jsx
@src/pages/hooks/useMediaLibrary.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix filter/search wiring and empty states</name>
  <files>src/pages/MediaLibraryPage.jsx</files>
  <action>
Read src/pages/MediaLibraryPage.jsx fully before editing. Also read the relevant sections of src/pages/hooks/useMediaLibrary.js for how `typeFilter`, `setTypeFilter`, `search`, `setSearch`, and `filteredAssets` are computed.

Audit the following and fix any gaps:

**Type filter buttons:**
- Filter panel is shown/hidden by `showFilters` toggle (Filter icon in header). The TYPE_FILTER_OPTIONS are rendered with `onClick={() => setTypeFilter(option.id)}`. Confirm this is correct.
- Verify that `filteredAssets` in the hook actually filters by `typeFilter`. Read the filtering logic in useMediaLibrary.js.
- If filtering is client-side (filtering `mediaAssets` array), confirm the filter condition is `asset.type === typeFilter` (or null = all). If filtering is server-side (re-fetches with type param), confirm the fetch call includes the filter.

**Search behavior:**
- Search input has `onChange={(e) => setSearch(e.target.value)}` — this is live/debounced. Confirm in the hook that `filteredAssets` re-filters when `search` changes (or that the hook debounces and re-fetches).
- If debounce is implemented, confirm it triggers within ~300ms of typing (not requiring Enter).

**Empty state when search/filter returns nothing:**
- There is already an empty state for search: `filteredAssets.length === 0 && folders.length === 0 && search`. Confirm it renders the "No media found" EmptyState with the search query shown.
- If the type filter alone (no search text) returns zero results, verify an appropriate empty state is shown. Check if `filteredAssets.length === 0 && typeFilter` also has an EmptyState — if not, add one like: "No {type} files found. Try a different filter."

**Combined filter + search:**
- Verify in useMediaLibrary.js that both `typeFilter` AND `search` are applied together when both are set (e.g., `filteredAssets` checks both conditions simultaneously, not one overriding the other).

**Filter panel discoverability:**
- The filter panel requires clicking the Filter icon in the header. Per CONTEXT.md, there is a TYPE_FILTER_OPTIONS list. Verify that clicking the Filter icon does show/hide the panel with `setShowFilters(!showFilters)`. This is confirmed correct in the current code — no change needed if wired.

Make targeted fixes only where actual gaps exist. Do not restructure working code.

ESLint: no-console (error). Unused variables prefixed with _.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npm run build 2>&1 | tail -5</automated>
    <manual>Run dev server (npm run dev), navigate to media library, verify: (1) filter panel shows when Filter icon clicked, (2) clicking Images filters to images only, (3) typing in search updates results live, (4) search + filter combined narrows correctly, (5) empty state message appears when no results</manual>
  </verify>
  <done>Type filter buttons correctly filter filteredAssets; search updates results live; empty state messages appear for both empty-search and empty-type-filter cases; combined filter+search narrows results correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verification: full media library lifecycle</name>
  <what-built>
    Complete media library interaction suite:
    - Plan 01: Upload progress display + upload error toasts
    - Plan 02: Detail modal preview, rename validation, delete confirmation
    - Plan 03: Bulk select (fixed X import, fixed list-view checkboxes), bulk delete confirmation
    - Plan 04: Filter panel, live search, empty states, combined filter+search
  </what-built>
  <how-to-verify>
Run `npm run dev` and navigate to the Media Library page (http://localhost:5173, then Media section).

**Upload (MEDIA-01):**
1. Click "Add Media" — modal opens
2. Try uploading a file — confirm progress indicator appears during upload (spinner or %)
3. Try uploading a file with a disallowed type (e.g., .exe) — confirm error toast appears
4. Try multi-file upload (hold Ctrl/Shift, select multiple) — confirm all files upload

**Preview + rename + delete (MEDIA-02):**
5. Double-click a media item — detail modal opens, actual image/video renders (not a placeholder)
6. Clear the Name field and click Save — confirm error message appears ("Name cannot be empty")
7. Click Delete — confirm a confirmation dialog/overlay appears
8. Click Cancel in confirmation — item is NOT deleted
9. Click Delete again, then confirm — item is deleted and disappears from grid

**Bulk select + bulk delete (MEDIA-03):**
10. Hover over a grid card — checkbox appears
11. Click the checkbox — BulkActionBar appears at bottom showing "1 selected"
12. Click "Select All" — all visible items selected
13. Click the X (close) button on BulkActionBar — selection cleared, bar disappears (no crash)
14. Select some items, click Delete in BulkActionBar — confirmation dialog appears
15. Cancel the confirmation — items not deleted

**Filter + search (MEDIA-04):**
16. Click the Filter icon in the header — filter panel appears
17. Click "Images" filter — only image items show in grid
18. Type in the search box — results narrow live (no Enter needed)
19. With Images filter active, type a search — results are both images AND match the search
20. Clear both — all items return
21. Search for something that doesn't exist — empty state message appears (not a blank grid)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
  <action>Human verifies all 21 steps listed in how-to-verify against the running dev server. No automated action — this is a manual verification gate.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; verification is performed manually per the how-to-verify steps above</automated>
    <manual>All 21 verification steps pass without errors</manual>
  </verify>
  <done>User types "approved" confirming all upload, preview/rename/delete, bulk selection, and filter/search behaviors are correct end-to-end</done>
</task>

</tasks>

<verification>
After human verification approved:
1. All 4 requirements are met: MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04
2. No JavaScript console errors during normal use
3. Build passes: `npm run build` exits 0
</verification>

<success_criteria>
- Upload: progress visible, errors surfaced, multi-file works
- Preview/rename/delete: modal renders media, empty name rejected, delete requires confirmation
- Bulk: checkboxes work in both grid and list view, BulkActionBar renders (X fixed), bulk delete requires confirmation, post-delete clears selection
- Filter/search: type filters correct results, search is live, empty states appear, combined filter+search works
- Human verification: all 21 steps above pass
</success_criteria>

<output>
After completion, create `.planning/phases/82-media-library/82-04-SUMMARY.md` with:
- Audit findings for filter/search
- Human verification result (approved/issues)
- Final build status
- Any remaining gaps discovered (for gap closure if needed)
</output>
