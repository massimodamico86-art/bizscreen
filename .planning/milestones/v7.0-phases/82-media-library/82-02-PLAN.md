---
phase: 82-media-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/media/MediaDetailModal.jsx
autonomous: true
requirements:
  - MEDIA-02

must_haves:
  truths:
    - "User can open the detail modal and the actual media (image or video) renders in the preview area"
    - "User can close the modal with the X button"
    - "User can rename a media item (edit the Name field and save)"
    - "Empty name is rejected — saving with a blank name shows an error, not a silent save"
    - "Pressing Escape or Cancel while editing reverts to the original name"
    - "Delete confirmation dialog appears before deletion — confirming deletes the item"
    - "Canceling the delete confirmation does NOT delete the item"
    - "After deletion the modal closes and the item is gone from the grid"
  artifacts:
    - path: "src/components/media/MediaDetailModal.jsx"
      provides: "Detail modal with working preview, rename validation, and delete confirmation"
      min_lines: 200
  key_links:
    - from: "src/components/media/MediaDetailModal.jsx"
      to: "onUpdate prop"
      via: "handleSave calling onUpdate with editName"
      pattern: "onUpdate.*editName|editName.*onUpdate"
    - from: "src/components/media/MediaDetailModal.jsx"
      to: "showDeleteConfirm state"
      via: "Delete button sets showDeleteConfirm=true; confirmation overlay renders"
      pattern: "showDeleteConfirm"
---

<objective>
Verify and fix the media detail modal so preview renders correctly, inline rename validates (empty name rejected), escape/cancel reverts edits, and the delete confirmation overlay functions correctly.

Purpose: MEDIA-02 — user can preview, rename, and delete media items with correct behavior.
Output: MediaDetailModal fully functional with validation and confirmation guards.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-media-library/82-CONTEXT.md

Key files to read before implementing:
@src/components/media/MediaDetailModal.jsx
@src/pages/MediaLibraryPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix MediaDetailModal rename validation and escape behavior</name>
  <files>src/components/media/MediaDetailModal.jsx</files>
  <action>
Read src/components/media/MediaDetailModal.jsx fully before editing.

Audit the following and fix any gaps:

**Preview rendering:**
- Confirm the modal renders `<img>` for image types and `<video>` for video types with correct src from asset.url. If the preview section is missing or uses a placeholder, add the correct media element. Images should have `object-contain` styling and a reasonable max-height (e.g., max-h-64). Videos should have `controls` attribute.

**Close button:**
- Confirm an X button exists in the header that calls `onClose`. If missing, add it (import X from lucide-react if not already imported — check existing imports first).

**Rename validation (empty name):**
- Find `handleSave`. Currently it calls `onUpdate?.(asset.id, { name: editName, ... })` without validating that `editName` is non-empty.
- Add validation before the try block: `if (!editName.trim()) { showToast?.('Name cannot be empty', 'error'); return; }`
- This prevents silent save of empty names.

**Escape/Cancel revert:**
- Confirm the Cancel button (or close without saving) does NOT persist edits. The `useEffect` that sets `editName(asset.name)` when `asset` changes should handle revert-on-close (since modal re-mounts on next open with fresh asset). Verify this is the case.
- If there is a Cancel button in the edit form that is distinct from close, ensure it resets `editName` to `asset.name || ''`.

**Delete confirmation overlay:**
- Confirm `showDeleteConfirm` state gates the overlay correctly — `setShowDeleteConfirm(true)` must be called when the Delete button is clicked (not directly calling handleDelete).
- Confirm Cancel button in the overlay calls `setShowDeleteConfirm(false)` without deleting.
- Confirm the destructive Delete button in the overlay calls `handleDelete()` which calls `onDelete?.(asset.id)` then `onClose()`.

Document each finding. Only modify code where there is an actual gap.

ESLint: no-console (error), unused-imports (error). Do not add console.log. Prefix unused variables with _.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npm run build 2>&1 | tail -5</automated>
    <manual>Confirm: (1) empty name save shows error toast, (2) delete button shows confirmation overlay before deleting, (3) cancel in overlay does not delete</manual>
  </verify>
  <done>MediaDetailModal rejects empty name save with toast error; delete requires confirmation overlay; preview renders actual media; cancel/escape reverts edits; build passes</done>
</task>

</tasks>

<verification>
After task completes:
1. `cd /Users/massimodamico/bizscreen && npm run build 2>&1 | tail -5` — exit 0, no ESLint errors
2. Grep confirms empty-name validation: `grep -n "cannot be empty\|trim()" src/components/media/MediaDetailModal.jsx` — must show validation line
3. Grep confirms delete guard: `grep -n "showDeleteConfirm" src/components/media/MediaDetailModal.jsx` — must appear both as state setter and as JSX condition
</verification>

<success_criteria>
- Preview modal shows actual image/video content, not a placeholder
- Saving with empty name is blocked with a user-visible error
- Delete button shows confirmation overlay; Cancel cancels; Confirm deletes + closes modal
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/82-media-library/82-02-SUMMARY.md` with:
- Audit findings: what was already correct vs what was fixed
- Exact lines changed in MediaDetailModal.jsx
- Build result
</output>
