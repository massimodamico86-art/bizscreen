---
phase: 82-media-library
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/media/BulkActionBar.jsx
  - src/pages/components/MediaLibraryComponents.jsx
autonomous: true
requirements:
  - MEDIA-03

must_haves:
  truths:
    - "Each media grid card has a visible checkbox that can be clicked to select it"
    - "Selecting one or more items causes the BulkActionBar to appear"
    - "BulkActionBar shows the count of selected items and a Delete action"
    - "The X (close/deselect) button in BulkActionBar renders without crashing"
    - "A select-all option in BulkActionBar selects all visible items"
    - "Bulk delete shows a confirmation dialog before executing"
    - "After bulk delete, deleted items disappear and selection state clears"
  artifacts:
    - path: "src/components/media/BulkActionBar.jsx"
      provides: "Bulk action bar with X icon import fixed"
      contains: "import.*X.*lucide-react"
    - path: "src/pages/components/MediaLibraryComponents.jsx"
      provides: "Media grid card with working checkbox selection"
  key_links:
    - from: "src/components/media/BulkActionBar.jsx"
      to: "onDeselectAll"
      via: "X button click handler"
      pattern: "onClick.*onDeselectAll"
    - from: "src/pages/components/MediaLibraryComponents.jsx"
      to: "onToggleSelect"
      via: "checkbox onChange calls onToggleSelect(asset.id)"
      pattern: "onToggleSelect.*asset\\.id"
---

<objective>
Fix the missing X icon import in BulkActionBar (currently crashes render), verify media grid card checkboxes are wired to the selection handler, and confirm bulk delete includes a confirmation step.

Purpose: MEDIA-03 — user can bulk-select and bulk-delete media items.
Output: BulkActionBar renders correctly; checkboxes wire selection; bulk delete requires confirmation.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-media-library/82-CONTEXT.md

Key files to read before implementing:
@src/components/media/BulkActionBar.jsx
@src/pages/components/MediaLibraryComponents.jsx
@src/pages/hooks/useMediaLibrary.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix missing X icon import in BulkActionBar</name>
  <files>src/components/media/BulkActionBar.jsx</files>
  <action>
Read src/components/media/BulkActionBar.jsx fully before editing.

The file uses `<X size={18} />` on line ~158 (the close/deselect button) but `X` is not in the import list from lucide-react. The current import is:

```
import {
  CheckSquare,
  Download,
  FolderInput,
  ListPlus,
  Loader2,
  Square,
  Tag,
  Trash2,
} from 'lucide-react';
```

Fix: Add `X` to the lucide-react import list. That is the only change needed in this file.

After fixing, also confirm:
- The `selectedCount === 0` guard at the top returns null (correct behavior — bar is hidden when nothing selected)
- The "selected" count display uses `selectedCount` correctly
- The Delete button calls `onDelete` (not a hardcoded action)
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -n "^import" src/components/media/BulkActionBar.jsx | head -5 && grep -n "X," src/components/media/BulkActionBar.jsx | head -3</automated>
    <manual>X is now in the lucide-react import list</manual>
  </verify>
  <done>X icon imported from lucide-react in BulkActionBar.jsx; close button renders without crash</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix grid card checkbox wiring and list row checkbox wiring</name>
  <files>src/pages/components/MediaLibraryComponents.jsx</files>
  <action>
Read src/pages/components/MediaLibraryComponents.jsx fully before editing.

**MediaGridCard checkbox (grid view):**
Read the checkbox implementation. It should have:
```jsx
<input
  type="checkbox"
  checked={isBulkSelected}
  onChange={() => { e.stopPropagation(); onToggleSelect?.(asset.id); }}
  ...
/>
```
Verify the onChange calls `onToggleSelect(asset.id)`. The event object `e` must be received in the handler to call `e.stopPropagation()` so clicking the checkbox doesn't trigger the card's onClick. If `e.stopPropagation()` is missing, add it.

**MediaListRow checkbox (list view):**
The table list view has a static `<input type="checkbox" className="rounded border-gray-300" />` (around line 119) with NO onChange handler and NO checked state. This means clicking a row's checkbox in list view does nothing.

Fix MediaListRow: The component receives `isSelected` and possibly needs bulk selection props too. Check what props MediaListRow receives. If `isBulkSelected` and `onToggleSelect` are NOT passed to MediaListRow from MediaLibraryPage.jsx, you will need to add them:

1. In MediaLibraryComponents.jsx, update `MediaListRow` to accept `isBulkSelected` and `onToggleSelect` props.
2. Wire the checkbox: `<input type="checkbox" checked={isBulkSelected || false} onChange={(e) => { e.stopPropagation(); onToggleSelect?.(asset.id); }} className="rounded border-gray-300" />`
3. In MediaLibraryPage.jsx (`src/pages/MediaLibraryPage.jsx`), update the `<MediaListRow>` usage (around line 572) to pass `isBulkSelected={selectedIds.has(asset.id)}` and `onToggleSelect={toggleSelection}`.

If `isBulkSelected` and `onToggleSelect` are ALREADY passed to MediaListRow, just fix the static checkbox to use them.

ESLint: no-console (error). Do not add console.log. Unused variables prefixed with _.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -n "isBulkSelected\|onToggleSelect" src/pages/components/MediaLibraryComponents.jsx | head -10</automated>
    <manual>List view checkboxes in MediaListRow are wired to isBulkSelected and onToggleSelect</manual>
  </verify>
  <done>MediaListRow checkbox has checked={isBulkSelected} and onChange calls onToggleSelect(asset.id); grid card checkbox calls stopPropagation to prevent card click when selecting</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /Users/massimodamico/bizscreen && npm run build 2>&1 | tail -5` — exit 0, no ESLint errors
2. `grep -n "X," src/components/media/BulkActionBar.jsx` — X must appear in import list
3. `grep -n "isBulkSelected\|onToggleSelect" src/pages/components/MediaLibraryComponents.jsx | wc -l` — must be > 4 (used in both grid and list components)
4. Note: bulk delete confirmation uses `window.confirm()` in useMediaLibrary.js (line 656) — this is a valid browser confirmation step per CONTEXT.md ("verify a confirmation step exists"). No change needed here.
</verification>

<success_criteria>
- BulkActionBar renders without crash (X icon imported)
- Grid card checkboxes enter selection mode and show BulkActionBar
- List view checkboxes are wired to selection state (were previously static/broken)
- Bulk delete triggers window.confirm before executing
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/82-media-library/82-03-SUMMARY.md` with:
- Exact fix for missing X import
- List view checkbox fix details
- Whether MediaLibraryPage.jsx also required changes
- Build result
</output>
