---
phase: 088-analytics-alerts
plan: 03
type: gap_closure
wave: 1
depends_on: [088-02]
files_modified:
  - src/pages/AlertsCenterPage.jsx
  - src/pages/ContentPerformancePage.jsx
autonomous: true
requirements:
  - ANLYT-01
  - ANLYT-02
  - ANLYT-03
  - ANLYT-04
  - ALRT-01
  - ALRT-02

must_haves:
  truths:
    - "AlertsCenterPage AlertDetailModal renders when an alert is selected — Modal receives open={true} instead of isOpen={true}"
    - "ContentPerformancePage scene detail Modal renders when a scene is clicked — Modal receives open={true} instead of isOpen={true}"
    - "No other props on the Modal components are changed — only isOpen is renamed to open"
    - "Both pages still function correctly: alert acknowledge/resolve and scene detail display work as before"
  artifacts:
    - path: "src/pages/AlertsCenterPage.jsx"
      provides: "AlertDetailModal with correct Modal open prop"
      contains: "open={true}"
    - path: "src/pages/ContentPerformancePage.jsx"
      provides: "Scene detail Modal with correct Modal open prop"
      contains: "open={true}"
  key_links:
    - from: "src/pages/AlertsCenterPage.jsx"
      to: "src/design-system/components/Modal.jsx"
      via: "Modal component with open prop (not isOpen)"
      pattern: "<Modal open="
    - from: "src/pages/ContentPerformancePage.jsx"
      to: "src/design-system/components/Modal.jsx"
      via: "Modal component with open prop (not isOpen)"
      pattern: "<Modal"
---

<objective>
Fix two Modal prop mismatches found during 088-VERIFICATION.md gap analysis. Both AlertsCenterPage.jsx and ContentPerformancePage.jsx pass `isOpen={true}` to the design-system Modal, but Modal only accepts `open` (defaulting to false). The `isOpen` prop is silently spread as an unknown HTML attribute, and `open` stays false, so modal content never renders.

The fix is a single-prop rename in each file: `isOpen` → `open`. No other changes.

Purpose: Close the two BLOCKER gaps from 088-VERIFICATION.md, completing ALRT-01 (alert detail modal must render) and ANLYT-02 (content performance scene detail modal must render).
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/088-analytics-alerts/088-VERIFICATION.md
@src/pages/AlertsCenterPage.jsx
@src/pages/ContentPerformancePage.jsx
@src/design-system/components/Modal.jsx

<interfaces>
<!-- Design-system Modal component -->
From src/design-system/components/Modal.jsx (line 32-42):
- Modal: accepts { open, onClose, children, size, closeOnOverlay, closeOnEscape, showCloseButton, className, ...props }
- The prop is `open` (boolean, defaults to false), NOT `isOpen`
- When `open` is false (the default), the modal renders nothing — `{open && ...}` evaluates to false
- Unknown props like `isOpen` are silently spread onto the wrapper div as HTML attributes

<!-- Current bug locations -->
AlertsCenterPage.jsx line 688:
  `<Modal isOpen={true} onClose={onClose} title={alert.title} size="lg">`
  → Should be: `<Modal open={true} onClose={onClose} title={alert.title} size="lg">`

ContentPerformancePage.jsx line 484:
  `isOpen={true}`
  → Should be: `open={true}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Modal prop in AlertsCenterPage and ContentPerformancePage</name>
  <files>src/pages/AlertsCenterPage.jsx, src/pages/ContentPerformancePage.jsx</files>
  <action>
Two single-line prop renames:

**File 1: src/pages/AlertsCenterPage.jsx (line 688)**
Find:
```jsx
<Modal isOpen={true} onClose={onClose} title={alert.title} size="lg">
```
Replace with:
```jsx
<Modal open={true} onClose={onClose} title={alert.title} size="lg">
```

**File 2: src/pages/ContentPerformancePage.jsx (line 484)**
Find:
```jsx
          isOpen={true}
```
Replace with:
```jsx
          open={true}
```

No other changes. The fix is exactly `isOpen` → `open` in both files.

**Why this works:** The design-system Modal component (src/design-system/components/Modal.jsx line 34) destructures `open = false` from props. When `open` is truthy, it renders the modal overlay and content. When `isOpen` was passed instead, it fell through to `...props` (line 42) and was spread as an unknown HTML attribute on the wrapper div, while `open` defaulted to `false` and the modal content was never rendered.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && node -e "
      const fs = require('fs');
      // Check AlertsCenterPage
      const alerts = fs.readFileSync('src/pages/AlertsCenterPage.jsx', 'utf8');
      if (alerts.includes('isOpen={true}')) throw new Error('AlertsCenterPage still has isOpen={true}');
      if (!alerts.includes('open={true}')) throw new Error('AlertsCenterPage missing open={true}');
      // Check ContentPerformancePage
      const content = fs.readFileSync('src/pages/ContentPerformancePage.jsx', 'utf8');
      if (content.includes('isOpen={true}')) throw new Error('ContentPerformancePage still has isOpen={true}');
      if (!content.includes('open={true}')) throw new Error('ContentPerformancePage missing open={true}');
      console.log('PASS: Both Modal prop mismatches fixed (isOpen → open)');
    "
    </automated>
  </verify>
  <done>Both Modal isOpen→open prop fixes applied; AlertsCenterPage AlertDetailModal and ContentPerformancePage scene detail Modal will now render correctly</done>
</task>

</tasks>

<verification>
1. Read AlertsCenterPage.jsx line 688 — confirm `open={true}` replaces `isOpen={true}`
2. Read ContentPerformancePage.jsx line 484 — confirm `open={true}` replaces `isOpen={true}`
3. Run automated node script to verify no remaining `isOpen={true}` in either file and `open={true}` is present
4. No other lines in either file should be changed
</verification>

<success_criteria>
- AlertsCenterPage AlertDetailModal renders when an alert is selected (Modal receives `open={true}`)
- ContentPerformancePage scene detail Modal renders when a scene is clicked (Modal receives `open={true}`)
- No other changes to either file — this is a prop-name-only fix
- All 6 phase requirements (ANLYT-01 through ANLYT-04, ALRT-01, ALRT-02) are now fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/088-analytics-alerts/088-03-SUMMARY.md`
</output>
