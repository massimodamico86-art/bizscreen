---
phase: 81-authentication-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DashboardPage.jsx
  - src/pages/dashboard/DashboardSections.jsx
  - src/pages/dashboard/OnboardingCards.jsx
  - src/components/dashboard/QuickActionsBar.jsx
  - src/components/dashboard/HealthBanner.jsx
  - src/components/dashboard/ActiveContentGrid.jsx
  - src/components/dashboard/TimelineActivity.jsx
  - src/components/dashboard/PendingApprovalsWidget.jsx
  - src/components/dashboard/ScreenPairingReminderCard.jsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Dashboard loads all sections (stats grid, screens overview, quick actions, timeline activity, alerts widget) without crashing"
    - "No JavaScript errors appear in the browser console when the dashboard is loaded"
    - "Quick action buttons (Add Screen, Create Playlist, Upload Media, Create App) navigate to the correct pages"
    - "Header quick actions bar buttons (Add Screen, Upload, Analytics) navigate correctly"
    - "Stats grid displays without layout breakage even when stats are null or zero"
    - "Screens overview shows empty state with Add Screen button when no screens exist"
    - "Timeline activity and alerts widget show loading spinners then resolve without crashing"
  artifacts:
    - path: "src/pages/DashboardPage.jsx"
      provides: "Main dashboard orchestrator with data fetching, error state, and all widget layout"
    - path: "src/pages/dashboard/DashboardSections.jsx"
      provides: "StatsGrid, DashboardErrorState, ScreenRow, QuickActionButton, AlertsWidget components"
    - path: "src/components/dashboard/QuickActionsBar.jsx"
      provides: "Header quick actions (Add Screen, Upload, Analytics)"
  key_links:
    - from: "src/pages/DashboardPage.jsx"
      to: "src/services/dashboardService"
      via: "getDashboardStats, getTopScreens, getRecentActivity, getAlertSummary"
      pattern: "getDashboardStats|getTopScreens|getRecentActivity|getAlertSummary"
    - from: "src/components/dashboard/QuickActionsBar.jsx"
      to: "onNavigate prop"
      via: "onNavigate?.('screens'), onNavigate?.('media-all'), onNavigate?.('analytics')"
      pattern: "onNavigate\\?\\."
    - from: "src/pages/dashboard/DashboardSections.jsx"
      to: "setCurrentPage prop"
      via: "QuickActionButton onClick callbacks"
      pattern: "setCurrentPage\\("
---

<objective>
Verify the dashboard loads all widgets and sections without errors and all quick-action navigation targets work correctly. Fix any issues found.

Purpose: The dashboard is the first screen authenticated users see. It must be fully functional before any other phase verification can rely on navigation from the dashboard.
Output: Dashboard page that loads cleanly with all sections functional and all quick actions navigating correctly.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key source files for this plan -->
@src/pages/DashboardPage.jsx
@src/pages/dashboard/DashboardSections.jsx
@src/pages/dashboard/OnboardingCards.jsx
@src/components/dashboard/QuickActionsBar.jsx
@src/components/dashboard/HealthBanner.jsx
@src/components/dashboard/ActiveContentGrid.jsx
@src/components/dashboard/TimelineActivity.jsx
@src/components/dashboard/PendingApprovalsWidget.jsx
@src/components/dashboard/ScreenPairingReminderCard.jsx
@src/services/dashboardService.js
@src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit dashboard loading, widgets, and quick action navigation</name>
  <files>
    src/pages/DashboardPage.jsx
    src/pages/dashboard/DashboardSections.jsx
    src/pages/dashboard/OnboardingCards.jsx
    src/components/dashboard/QuickActionsBar.jsx
    src/components/dashboard/HealthBanner.jsx
    src/components/dashboard/ActiveContentGrid.jsx
    src/components/dashboard/TimelineActivity.jsx
    src/components/dashboard/PendingApprovalsWidget.jsx
    src/components/dashboard/ScreenPairingReminderCard.jsx
  </files>
  <action>
Systematically audit the dashboard and fix all issues found. Follow this checklist:

**1. DashboardPage.jsx — Data fetching and layout:**
- Confirm `getDashboardStats()`, `getTopScreens(5)`, `getRecentActivity(5)`, `getAlertSummary()` are all imported from dashboardService and called correctly in `fetchData()`
- Confirm the `loading` state gate prevents rendering before data arrives
- Confirm `error && !stats` shows `DashboardErrorState` with a retry button that calls `fetchData`
- Confirm the `setInterval(fetchData, 30000)` has `clearInterval` cleanup in the useEffect return
- Confirm the `UnifiedOnboardingController` is wrapped in a conditional that checks `showUnifiedOnboarding`
- Confirm `setCurrentPage` is passed down to all widgets that need navigation:
  - `HealthBanner` receives `onNavigate={setCurrentPage}`
  - `ScreenPairingReminderCard` receives `onNavigate={setCurrentPage}`
  - `StatsGrid` receives `setCurrentPage={setCurrentPage}`
  - `PendingApprovalsWidget` receives `onNavigate={setCurrentPage}`
  - `ActiveContentGrid` receives `onNavigate={setCurrentPage}`
  - `QuickActionsBar` receives `onNavigate={setCurrentPage}` (in both desktop header and mobile card)
  - `TimelineActivity` receives `onNavigate={setCurrentPage}`
  - `AlertsWidget` receives `setCurrentPage={setCurrentPage}`

**2. DashboardSections.jsx — Component verification:**
- Confirm `StatsGrid` handles `stats={null}` without crashing (guards before accessing `stats?.screens?.total` etc.)
- Confirm `AlertsWidget` handles `alertSummary={null}` and `loading={true}` without crashing
- Confirm `QuickActionButton` renders correctly with all four actions: screens, playlists, media-all, apps
- Confirm `ScreenRow` renders without crashing when given a screen object from `getTopScreens`

**3. QuickActionsBar.jsx — Navigation targets:**
- Confirm "Add Screen" button calls `onNavigate?.('screens')`
- Confirm "Upload" button calls `onNavigate?.('media-all')`
- Confirm "Analytics" button calls `onNavigate?.('analytics')`
- Check these page IDs are valid keys in App.jsx's pages map — `screens`, `media-all`, and `analytics` should all be valid

**4. QuickActionButton in DashboardSections.jsx — Navigation targets:**
- "Add Screen" → `setCurrentPage('screens')` ✓
- "Create Playlist" → `setCurrentPage('playlists')` ✓
- "Upload Media" → `setCurrentPage('media-all')` ✓
- "Create App" → `setCurrentPage('apps')` ✓
- Verify all four page IDs are valid keys in App.jsx pages map

**5. ActiveContentGrid, HealthBanner, TimelineActivity, PendingApprovalsWidget, ScreenPairingReminderCard:**
- Read each component briefly. For each one: confirm it handles `loading` prop or empty data state without a JS crash.
- Specifically check that none of these components call methods on `undefined` values without null-guarding.

**Fix any issues found.** Common patterns to watch for:
- Missing null checks: `data.map(...)` where data could be undefined — fix with `(data || []).map(...)`
- Missing prop type guards: `onNavigate('foo')` without `?.` when onNavigate could be undefined
- Import issues: any component using design-system components that are not imported
- Navigation target mismatches: if a component navigates to a page ID that doesn't exist in App.jsx's pages map, update it to the correct ID

After fixing, verify the dashboard service functions exist and are correctly exported from `src/services/dashboardService.js`. Read the file briefly to confirm `getDashboardStats`, `getTopScreens`, `getRecentActivity`, `getAlertSummary`, and `formatLastSeen` are all exported.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/ --grep "dashboard" --project=chromium 2>&1 | tail -20</automated>
    <manual>
      After running the dev server (`npm run dev`), log in as the test user and observe:
      1. Dashboard loads without spinner hanging indefinitely
      2. Stats grid section is visible (even if all zeros)
      3. Screens overview section is visible
      4. Quick Actions card is visible with all four action buttons
      5. Timeline Activity section is visible (even if empty)
      6. Alerts widget is visible (even if no alerts)
      7. Browser console shows NO JavaScript errors
    </manual>
  </verify>
  <done>
    - All dashboard widgets render without JS errors for a user with or without screens/content
    - setCurrentPage is passed to all sub-components that require navigation
    - All quick action navigation targets are valid App.jsx page IDs
    - Components handle null/empty data states gracefully without crashing
    - dashboardService exports are confirmed complete
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify dashboard loads and navigation works</name>
  <what-built>
    Audited all dashboard components — DashboardPage, StatsGrid, QuickActionsBar, QuickActionButtons, HealthBanner, ActiveContentGrid, TimelineActivity, PendingApprovalsWidget, ScreenPairingReminderCard, AlertsWidget — and fixed any issues found.
  </what-built>
  <how-to-verify>
    Log in to the app (http://localhost:5173) and verify the dashboard:

    1. **Load check**: Dashboard renders all sections — stats, screens overview, quick actions, activity, alerts. No white screen, no infinite spinner.

    2. **Console check**: Open DevTools → Console. Confirm zero JavaScript errors on dashboard load.

    3. **Quick action navigation (header bar)**:
       - Click "Add Screen" in the header bar → navigates to Screens page
       - Click "Upload" in the header bar → navigates to Media Library
       - Click "Analytics" in the header bar → navigates to Analytics page
       - Navigate back to Dashboard after each

    4. **Quick action navigation (card in main content)**:
       - Click "Add Screen" card → Screens page
       - Click "Create Playlist" card → Playlists page
       - Click "Upload Media" card → Media Library
       - Click "Create App" card → Apps page

    5. **Screens overview**: "View all" link navigates to Screens page. If no screens, empty state with "Add Screen" button is visible.

    6. **Edge case**: If stats show zeros everywhere, the page does NOT crash.

    Type "approved" if all checks pass, or describe any issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Dashboard verification is primarily manual (visual + console check) since there are no dedicated dashboard E2E tests at the time of this plan.

Run: `npx playwright test tests/e2e/ --project=chromium 2>&1 | tail -30` — overall test suite should not regress.
</verification>

<success_criteria>
- Dashboard loads all sections without JS errors for authenticated users
- Stats grid, screens overview, quick actions, timeline activity, and alerts widget all render
- All four quick action buttons in the main content card navigate to the correct pages (screens, playlists, media-all, apps)
- All three header quick action buttons navigate correctly (screens, media-all, analytics)
- Null/empty data states are handled gracefully in all widgets
- No JavaScript console errors on dashboard load
</success_criteria>

<output>
After completion, create `.planning/phases/81-authentication-dashboard/81-02-SUMMARY.md` summarizing:
- What was audited across all dashboard components
- What issues were found and fixed (files and changes)
- Confirmation of navigation target correctness
- Any edge cases or concerns noted for future phases
</output>
