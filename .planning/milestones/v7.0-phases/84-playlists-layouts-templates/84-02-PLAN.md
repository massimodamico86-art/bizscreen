---
phase: 84-playlists-layouts-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LayoutEditorPage.jsx
  - src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx
  - src/components/layout-editor/LayoutPropertiesPanel.jsx
  - src/components/layout-editor/LayoutEditorCanvas.jsx
  - src/components/layout-editor/LeftSidebar.jsx
  - src/pages/LayoutTemplates/LayoutTemplatesPage.jsx
autonomous: false
requirements:
  - LAYT-01
  - LAYT-02
  - LAYT-03
  - LAYT-04

must_haves:
  truths:
    - "User can create a new layout and it appears in the layout list"
    - "User can edit an existing layout's name and zones"
    - "User can delete a layout with usage-aware confirmation"
    - "Layout editor canvas renders zones that can be resized and repositioned"
    - "Selecting a zone opens the properties panel with correct element-type controls"
    - "All 12 widget types show their configuration controls in the layout properties panel"
    - "Layout templates page loads templates from the database and displays them in a browsable gallery"
    - "Clicking 'Use Template' on a layout template creates a new layout from the template"
  artifacts:
    - path: "src/pages/LayoutEditorPage.jsx"
      provides: "Legacy layout editor with zone creation, resize, configuration"
      min_lines: 100
    - path: "src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx"
      provides: "Modern layout editor with canvas, sidebar, properties panel"
      min_lines: 100
    - path: "src/components/layout-editor/LayoutPropertiesPanel.jsx"
      provides: "Widget-type-specific property controls for all 12 types"
      min_lines: 100
    - path: "src/pages/LayoutTemplates/LayoutTemplatesPage.jsx"
      provides: "Layout template gallery with browse, filter, and apply"
      min_lines: 100
  key_links:
    - from: "src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx"
      to: "src/hooks/useLayout.js"
      via: "useLayout(layoutId)"
      pattern: "useLayout"
    - from: "src/components/layout-editor/LayoutPropertiesPanel.jsx"
      to: "src/widgets/registry.js"
      via: "getWidgetTypes, getWidgetDefaults"
      pattern: "getWidgetTypes|getWidgetDefaults"
    - from: "src/pages/LayoutTemplates/LayoutTemplatesPage.jsx"
      to: "src/hooks/useLayoutTemplates.js"
      via: "useLayoutTemplates hook"
      pattern: "useLayoutTemplates"
---

<objective>
Verify and fix the layout editor (zone creation, resize, element configuration for all 12 widget types), layout CRUD operations, and layout template gallery (browse, preview, apply).

Purpose: Layouts define multi-zone screen arrangements — each zone can hold playlists, media, or widgets. Every zone's configuration controls must work for all 12 widget types.
Output: Fully functional layout editor, zone configuration for all widget types, and layout template browsing/apply.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-playlists-layouts-templates/84-CONTEXT.md

@src/pages/LayoutEditorPage.jsx
@src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx
@src/components/layout-editor/LayoutPropertiesPanel.jsx
@src/components/layout-editor/LayoutEditorCanvas.jsx
@src/components/layout-editor/LeftSidebar.jsx
@src/pages/LayoutTemplates/LayoutTemplatesPage.jsx
@src/hooks/useLayout.js
@src/hooks/useLayoutTemplates.js
@src/services/layoutService.js
@src/services/layoutTemplateService.js
@src/widgets/registry.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix layout CRUD and editor zone operations</name>
  <files>
    src/pages/LayoutEditorPage.jsx
    src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx
    src/components/layout-editor/LayoutEditorCanvas.jsx
    src/components/layout-editor/LeftSidebar.jsx
  </files>
  <action>
Launch the app and navigate to the layout-related pages. Determine which editor is the primary one (LayoutEditorPage.jsx vs YodeckLayoutEditorPage.jsx) by checking the router/App.jsx navigation pattern for `layout-editor-{id}`.

**Layout CRUD (LAYT-01):**
1. Find where layouts are listed — check if it's LayoutsPage (the design template gallery) or LayoutTemplatesPage or a "Your Templates/Designs" section
2. Verify creating a new layout: look for a "Create Layout" or "New Layout" or the CreateLayoutModal in the design-system components. If the creation flow requires navigating to the layout editor (e.g., from LayoutTemplatesPage "Your Layouts" tab), verify that works
3. Verify editing a layout name: check if the layout editor header has an editable name field. If not, check LayoutEditorPage's layout settings section
4. Verify deleting a layout: check if delete is available from the layouts list or from within the editor. Verify usage-aware confirmation (deleteLayoutSafely from layoutService)

**Layout Editor Zones (LAYT-02):**
1. Open a layout in the editor
2. Verify the canvas renders with existing zones displayed as rectangles
3. Add a new zone: use the "Add Zone" button or the preset layouts (Full Screen, Two Columns, etc. from PRESET_LAYOUTS in LayoutEditorPage). Verify the zone appears on the canvas
4. Select a zone on the canvas — verify it shows selection handles
5. Resize a zone by dragging its handles — verify the zone dimensions update and persist (auto-save)
6. Reposition a zone by dragging it — verify the new position persists
7. Delete a zone — verify removal from canvas and persistence

**Widget Configuration (LAYT-03):**
Systematically test all 12 widget types in the LayoutPropertiesPanel:
1. Select a zone, change its content type to "widget"
2. For each widget type in the registry (clock, date, clock-date, weather, qr-code, data-table, rss-ticker, rss-card, social-feed, countdown, menu-board, and any 12th type), select it and verify:
   - The correct widget-specific controls render (imported from scene-editor: QRCodeWidgetControls, DataTableWidgetControls, RssWidgetControls, SocialFeedWidgetControls, CountdownWidgetControls, MenuBoardWidgetControls)
   - Changing a control value updates the element props
   - For basic widgets (clock, date, weather): verify size, timezone, format controls
3. If any widget type's controls fail to render or crash, fix the import/adapter wiring in LayoutPropertiesPanel

Fix all broken interactions. For external-dependency widgets (weather needing API key, RSS needing URL), verify the controls render correctly even if data preview isn't available.
  </action>
  <verify>
    <automated>npx vite build --mode development 2>&1 | tail -5</automated>
    <manual>Open layout editor, add zones, resize, configure each widget type. All 12 widget controls render without errors.</manual>
  </verify>
  <done>
    - Layout CRUD (create, edit name, delete) works without errors
    - Zones can be added, resized, repositioned, and deleted on the canvas
    - All 12 widget types show their configuration controls in the properties panel
    - Widget control changes update element props correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix layout templates browse, preview, and apply</name>
  <files>
    src/pages/LayoutTemplates/LayoutTemplatesPage.jsx
    src/pages/LayoutEditorPage.jsx
  </files>
  <action>
Navigate to the Layout Templates page and audit the full template lifecycle:

**Layout Templates Browse (LAYT-04):**
1. Navigate to the layout templates page — verify it loads templates from the database via useLayoutTemplates hook
2. Verify template cards display: thumbnail, name, orientation badge, zone count
3. Verify search: type in the search field and confirm templates filter by name
4. Verify orientation filter: click landscape/portrait/square filters and confirm templates filter correctly
5. Verify category filter: if categories exist, test switching between them

**Layout Template Preview:**
1. Hover over a template card — verify a preview appears (hover state or popover)
2. If there's a preview modal, click to open it and verify template details display

**Layout Template Apply:**
1. Click "Use Template" (or equivalent) on a template — verify it clones the template into a new user layout
2. After applying, verify the user is navigated to the layout editor with the new layout loaded
3. Verify the cloned layout has the correct zones from the template
4. Check the "Your Layouts" tab in LayoutTemplatesPage — verify user's layouts display with pagination (already implemented)

**Also verify the LayoutEditorPage "Save as Template" flow:**
1. In the layout editor, check if there's a "Save as Template" button (createTemplateFromLayout from layoutTemplateService)
2. If present, verify clicking it opens the save-as-template modal and saving creates a new template

Fix all broken template interactions. If template data is empty in the database, the page should show an appropriate empty state (not crash).
  </action>
  <verify>
    <automated>npx vite build --mode development 2>&1 | tail -5</automated>
    <manual>Navigate to Layout Templates, browse templates, filter by orientation, click Use Template, verify new layout opens in editor.</manual>
  </verify>
  <done>
    - Layout templates page loads and displays templates from database
    - Search and orientation filters work correctly
    - "Use Template" creates a new layout and navigates to the editor
    - Empty states render correctly when no templates match
    - Save as Template from editor creates a new template (if feature exists)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of layout editor and templates</name>
  <files>src/pages/LayoutEditor/YodeckLayoutEditorPage.jsx</files>
  <action>
Human verifies the complete layout editor with zone CRUD, all 12 widget configuration controls, and layout template gallery with browse/filter/apply.

Steps:
1. Navigate to Layout Templates page — verify templates load and display
2. Filter by orientation (landscape/portrait) — verify filtering works
3. Click "Use Template" on any template — verify new layout opens in editor
4. In the editor, add a zone and resize it — verify it persists
5. Select a zone and change its widget type to "clock" — verify clock controls appear
6. Switch through 3-4 different widget types — verify each shows appropriate controls
7. Delete a zone — verify removal
8. Navigate back and delete the test layout — verify deletion
9. Check browser console for any JavaScript errors during all actions
  </action>
  <verify>Human approves all layout editor and template flows work correctly</verify>
  <done>All layout CRUD, zone operations, widget controls, and template apply verified by human</done>
</task>

</tasks>

<verification>
- Layout CRUD (create, edit, delete) works without errors
- Zone operations (add, resize, reposition, delete) work on the canvas
- All 12 widget types render their property controls in the properties panel
- Layout templates page loads, filters, and the "Use Template" apply flow works
- No JavaScript console errors during any layout/template operation
</verification>

<success_criteria>
- LAYT-01: Create, edit, delete layouts all functional
- LAYT-02: Zone creation, resize, configuration works end-to-end
- LAYT-03: All 12 widget types configurable in zone properties panel
- LAYT-04: Layout templates browse, preview, apply flow works
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/84-playlists-layouts-templates/84-02-SUMMARY.md`
</output>
