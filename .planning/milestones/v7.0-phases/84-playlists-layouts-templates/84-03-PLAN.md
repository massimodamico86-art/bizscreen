---
phase: 84-playlists-layouts-templates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/TemplatesPage.jsx
  - src/components/templates/TemplateGrid.jsx
  - src/components/templates/TemplateCustomizeModal.jsx
  - src/components/templates/TemplateLivePreview.jsx
  - src/components/templates/TemplatePreviewPopover.jsx
  - src/components/templates/TemplateCustomizationWizard.jsx
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02

must_haves:
  truths:
    - "Template marketplace page loads templates from the database in a browsable grid"
    - "User can search templates by keyword and results update"
    - "User can filter templates by category from the sidebar"
    - "Hovering over a template card shows a preview popover or highlight"
    - "Clicking a template opens a preview or apply modal"
    - "One-click apply creates content from the template and navigates to the editor"
    - "Customization wizard (if present) allows modifying template fields before applying"
  artifacts:
    - path: "src/pages/TemplatesPage.jsx"
      provides: "Template marketplace with search, filter, browse, apply"
      min_lines: 100
    - path: "src/components/templates/TemplateGrid.jsx"
      provides: "Grid display of template cards with hover preview"
      min_lines: 30
    - path: "src/components/templates/TemplateCustomizeModal.jsx"
      provides: "Template customization before applying"
      min_lines: 30
    - path: "src/components/templates/TemplatePreviewPopover.jsx"
      provides: "Hover preview for template cards"
      min_lines: 20
  key_links:
    - from: "src/pages/TemplatesPage.jsx"
      to: "src/services/templateService.js"
      via: "fetchTemplates, applyTemplate, fetchTemplateCategories"
      pattern: "fetchTemplates|applyTemplate"
    - from: "src/pages/TemplatesPage.jsx"
      to: "src/components/templates"
      via: "TemplateGrid, TemplatePreviewPopover, TemplateCustomizeModal imports"
      pattern: "import.*from.*components/templates"
---

<objective>
Verify and fix the template marketplace — browsing, search, category filtering, template preview, one-click apply to editor, and the customization wizard flow.

Purpose: The template marketplace is the primary discovery surface for pre-made content. Users need to browse, preview, and apply templates to quickly create content.
Output: Fully functional TemplatesPage with search, filter, preview, apply, and customization wizard all working.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-playlists-layouts-templates/84-CONTEXT.md

@src/pages/TemplatesPage.jsx
@src/components/templates/TemplateGrid.jsx
@src/components/templates/TemplateCustomizeModal.jsx
@src/components/templates/TemplateLivePreview.jsx
@src/components/templates/TemplatePreviewPopover.jsx
@src/components/templates/TemplateCustomizationWizard.jsx
@src/components/templates/StarterPackCard.jsx
@src/components/templates/StarterPacksRow.jsx
@src/services/templateService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix template marketplace browse, search, filter, and preview</name>
  <files>
    src/pages/TemplatesPage.jsx
    src/components/templates/TemplateGrid.jsx
    src/components/templates/TemplatePreviewPopover.jsx
    src/components/templates/TemplateLivePreview.jsx
  </files>
  <action>
Launch the app and navigate to the Templates page. Perform a systematic audit:

**Template Browse (TMPL-01):**
1. Navigate to the Templates page — verify it loads without errors and templates appear in a grid/gallery layout
2. Verify the sidebar categories load via fetchTemplateCategories. Check that category buttons in the sidebar are clickable and filter the template grid
3. If the page shows sections (Featured, Recently Used, Starter Packs, category rows), verify each section renders with correct data
4. Verify empty states: if no templates exist in a category, an appropriate empty state message displays

**Search:**
1. Type a search term in the search bar — verify templates filter by name/description
2. Clear the search — verify all templates return
3. Try a search that matches nothing — verify an appropriate "no results" empty state

**Category Filter:**
1. Click different categories in the sidebar — verify the grid updates to show only templates in that category
2. If there's a "Favorites" section, verify it shows favorited templates (requires favoriteTemplateIds logic working)
3. If there's a "Recently Used" section, verify it shows recently applied templates

**Preview:**
1. Hover over a template card — verify the TemplatePreviewPopover renders with:
   - Template thumbnail/preview image
   - Template name and description
   - A "Use" or "Apply" action button
2. If TemplateLivePreview is used instead, verify it shows a larger preview of the template
3. Check that preview doesn't crash if template has no thumbnail_url

Fix all broken interactions. Common issues to check:
- Missing imports in TemplatesPage
- Null guard failures on template.category or template.thumbnail_url
- fetchTemplateCategories returning unexpected format
- TemplatePreviewPopover positioning issues
  </action>
  <verify>
    <automated>npx vite build --mode development 2>&1 | tail -5</automated>
    <manual>Navigate to Templates page, browse grid, search by keyword, filter by category, hover over cards for preview. All interactions work without errors.</manual>
  </verify>
  <done>
    - Templates page loads with template grid/gallery
    - Search filters templates by keyword
    - Category sidebar filters templates by category
    - Hover preview displays template details
    - Empty states render for no-results cases
    - No JavaScript errors during browsing
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix one-click apply and customization wizard</name>
  <files>
    src/pages/TemplatesPage.jsx
    src/components/templates/TemplateCustomizeModal.jsx
    src/components/templates/TemplateCustomizationWizard.jsx
  </files>
  <action>
Test the template apply and customization flows:

**One-Click Apply (TMPL-02):**
1. Click a template card or the "Use Template" button in the preview — trace the handler to see what happens:
   - Does it call applyTemplate directly? Does it open a customization modal first?
   - Check the TemplatesPage handler (likely handleApplyTemplate or similar)
2. Verify the apply flow:
   - applyTemplate from templateService is called with the template slug/id
   - After successful apply, the user is navigated to the appropriate editor (playlist editor for playlist templates, layout editor for layout templates, or the Polotno design editor)
   - A success toast is shown
3. Verify error handling: if applyTemplate fails (e.g., template not found), a meaningful error toast appears

**Customization Wizard:**
1. Check if TemplateCustomizeModal is wired into the TemplatesPage flow (imported and rendered)
2. If present, verify the wizard flow:
   - Opens with template fields pre-populated (name, colors, text placeholders if applicable)
   - User can modify fields (name at minimum)
   - "Apply" button creates the content from the template with customizations
   - The result navigates to the editor
3. If TemplateCustomizationWizard is a separate multi-step component, verify each step:
   - Step navigation (Next/Back) works
   - Final step applies the template
4. If the customization wizard is NOT currently wired in (TemplateCustomizeModal exists but isn't rendered in TemplatesPage), add the wiring:
   - When user clicks "Customize" or the primary apply button, open TemplateCustomizeModal with the selected template
   - On modal submit, call applyTemplate with customization overrides
   - On success, navigate to editor

**Also verify starter packs:**
1. If StarterPacksRow/StarterPackCard are rendered, verify clicking a pack applies all templates in the pack via applyPack
2. Verify the pack card shows template count, description, and an "Install" button

Fix all broken apply and customization flows. Ensure the full path from "click template" to "content created and editor opens" completes without errors.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/template-marketplace.spec.js --timeout 30000 2>&1 | tail -20</automated>
    <manual>Click "Use Template" on a template, verify content is created and editor opens. If customization modal exists, verify fields are editable and apply works.</manual>
  </verify>
  <done>
    - One-click apply creates content from template and navigates to editor
    - Customization wizard (if present) allows field modification before applying
    - Error handling shows meaningful toast on failure
    - Starter packs install correctly (if feature exists)
    - Full template-to-editor flow completes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of template marketplace</name>
  <files>src/pages/TemplatesPage.jsx</files>
  <action>
Human verifies the complete template marketplace with search, filter, preview, one-click apply, and customization wizard.

Steps:
1. Navigate to Templates page — verify templates load in a grid
2. Search for a template by keyword — verify results filter
3. Click a category in the sidebar — verify the grid updates
4. Hover over a template card — verify preview popover appears
5. Click "Use Template" on a template — verify content is created
6. Verify you're navigated to the editor with the template content loaded
7. If a customization modal appeared, verify fields were editable
8. Check browser console for any JavaScript errors during all actions
  </action>
  <verify>Human approves all template marketplace flows work correctly</verify>
  <done>All template browse, search, filter, preview, and apply flows verified by human</done>
</task>

</tasks>

<verification>
- Template marketplace loads and displays templates
- Search and category filter produce correct results
- Hover preview renders template details
- One-click apply creates content and navigates to editor
- Customization wizard allows field modification (if present)
- No JavaScript console errors during any template operation
</verification>

<success_criteria>
- TMPL-01: Browse, search, preview, filter all functional
- TMPL-02: One-click apply and customization wizard both work
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/84-playlists-layouts-templates/84-03-SUMMARY.md`
</output>
