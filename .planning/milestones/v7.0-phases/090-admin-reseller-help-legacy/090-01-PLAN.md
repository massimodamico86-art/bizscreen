---
phase: 090-admin-reseller-help-legacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/OpsConsolePage.jsx
  - src/pages/Admin/AdminTenantDetailPage.jsx
  - src/pages/AdminDashboardPage.jsx
  - src/pages/FeatureFlagsPage.jsx
  - src/pages/TenantAdminPage.jsx
  - src/pages/SuperAdminDashboardPage.jsx
  - src/pages/ServiceQualityPage.jsx
  - src/pages/UsageDashboardPage.jsx
  - src/pages/TranslationDashboardPage.jsx
  - src/components/AuditLogTable.jsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "OpsConsolePage renders tabs with Badge notification counts from design-system (not lucide-react icon), Refresh button uses Button from design-system with variant='secondary'"
    - "AdminTenantDetailPage loads tenant data via useTenantDetail hook and renders Overview, Users, Screens, Billing tabs with feature flag toggles"
    - "FeatureFlagsPage enables/disables flags per tenant via useFeatureFlags hook with tab-based UI"
    - "TenantAdminPage search, filter, sort, and tenant lifecycle actions (suspend, reactivate, reset trial) work via billingService"
    - "ServiceQualityPage loads SLA metrics and displays charts via slaService and metricsService"
    - "UsageDashboardPage and TranslationDashboardPage render data without JS errors"
    - "AuditLogTable loads and displays chronological events via auditService"
  artifacts:
    - path: "src/pages/OpsConsolePage.jsx"
      provides: "Operations console with tenant management and system monitoring"
      contains: "import.*Badge.*Button.*from.*design-system|import.*Button.*Badge.*from.*design-system"
    - path: "src/pages/Admin/AdminTenantDetailPage.jsx"
      provides: "Admin tenant detail with tabs and feature flag toggles"
      contains: "useTenantDetail"
    - path: "src/pages/FeatureFlagsPage.jsx"
      provides: "Feature flags management page with tab UI"
      contains: "useFeatureFlags"
    - path: "src/pages/TenantAdminPage.jsx"
      provides: "Tenant admin list with search, filter, sort, and lifecycle actions"
      contains: "billingService"
  key_links:
    - from: "src/pages/OpsConsolePage.jsx"
      to: "src/design-system/index.js"
      via: "import { Badge, Button } from '../design-system'"
      pattern: "import.*Badge.*from.*design-system|import.*Button.*from.*design-system"
    - from: "src/pages/TenantAdminPage.jsx"
      to: "src/services/billingService.js"
      via: "suspendTenant, reactivateTenant, resetTrial"
      pattern: "suspendTenant|reactivateTenant|resetTrial"
    - from: "src/pages/ServiceQualityPage.jsx"
      to: "src/services/slaService.js"
      via: "fetchSLAMetrics"
      pattern: "slaService|metricsService"
---

<objective>
Audit and fix admin pages and ops tooling — fix OpsConsolePage Badge collision bug, missing Button import, and invalid variant; verify all other admin/ops pages render with correct data and working actions.

Purpose: ADMIN-01 requires admin tenant list and detail pages load correctly. ADMIN-02 requires feature flags management works. ADMIN-03 requires usage and translation dashboards display correctly. ADMIN-04 requires ops console, service quality, audit logs work. OpsConsolePage has three critical bugs: Badge imported from lucide-react (collision with design-system Badge component), Button used but never imported, and variant="outline" (invalid).

Output: OpsConsolePage with correct design-system imports and zero variant="outline"; all 10 admin/ops pages verified functional.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/OpsConsolePage.jsx
@src/pages/Admin/AdminTenantDetailPage.jsx
@src/pages/AdminDashboardPage.jsx
@src/pages/FeatureFlagsPage.jsx
@src/pages/TenantAdminPage.jsx
@src/pages/SuperAdminDashboardPage.jsx
@src/pages/ServiceQualityPage.jsx
@src/pages/UsageDashboardPage.jsx
@src/pages/TranslationDashboardPage.jsx
@src/components/AuditLogTable.jsx
@src/design-system/index.js

<interfaces>
<!-- Design-system Button valid variants: primary, secondary, ghost, danger -->
<!-- NO "outline" variant exists — use "secondary" instead -->
<!-- Known project decisions:
     - Phase 86: Button variant='outline' replaced with variant='secondary'
     - Phase 85: Badge collision fix — remove Badge from lucide-react to avoid shadowing design-system Badge
-->

From src/design-system/index.js (relevant exports):
```javascript
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from './components/Card';
export { Button, ButtonGroup, IconButton } from './components/Button';
export { Badge, StatusBadge } from './components/Badge';
export { Alert, Banner } from './components/Alert';
```

OpsConsolePage current imports (BROKEN — 3 bugs):
```javascript
import {
  Activity,
  AlertTriangle,
  Badge,          // BUG 1: lucide-react Badge ICON shadows design-system Badge COMPONENT
  Building2,
  CheckCircle2,
  ExternalLink,
  Key,
  Monitor,
  RefreshCw,
  Search,
  Wifi,
  WifiOff,
  XCircle,
} from 'lucide-react';
// BUG 2: Button is NEVER imported but used on lines 243-251 and 393-401
// BUG 3: variant="outline" on line 246 — invalid design-system variant
```

OpsConsolePage uses:
- `<Badge variant="danger" className="ml-1">` on line 273 — this is design-system Badge, NOT lucide icon
- `<Button variant="outline" ...>` on line 243-251 — needs design-system Button import + variant fix
- `<Button size="sm" variant="ghost" ...>` on line 393-401 — needs design-system Button import

All other admin/ops pages have been pre-audited and found to have correct imports:
- AdminTenantDetailPage: Uses raw HTML (no design-system), self-contained, functional
- AdminDashboardPage: Has correct design-system imports (PageLayout, PageContent, PageHeader, Alert, StatCard, Card, Badge, Button, Modal)
- FeatureFlagsPage: Has correct design-system imports, uses useFeatureFlags hook
- TenantAdminPage: Has correct design-system imports including Modal components
- SuperAdminDashboardPage: Uses ErrorBoundary + raw HTML, self-contained
- ServiceQualityPage: Has extensive design-system imports (Card, PageLayout, Button, Badge, etc.)
- UsageDashboardPage: Self-contained with raw HTML, no design-system needed
- TranslationDashboardPage: Has correct design-system imports
- AuditLogTable: Self-contained table component with auditService
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OpsConsolePage Badge collision, missing Button import, and variant="outline"</name>
  <files>src/pages/OpsConsolePage.jsx</files>
  <action>
1. Open `src/pages/OpsConsolePage.jsx`

2. Fix the Badge collision (critical bug — same pattern as Phase 85 CampaignEditorPage fix):
   - REMOVE `Badge` from the lucide-react import block (line 9). The lucide-react Badge icon is NOT used anywhere — all Badge usage is design-system `<Badge variant="danger">` style on line 273.
   - After removing Badge, the lucide-react import should have: Activity, AlertTriangle, Building2, CheckCircle2, ExternalLink, Key, Monitor, RefreshCw, Search, Wifi, WifiOff, XCircle

3. Add design-system imports after the lucide-react import block (after line 20):
```javascript
import { Badge, Button } from '../design-system';
```

4. Replace the single instance of `variant="outline"` with `variant="secondary"` on line 246 (Refresh data button).

5. Save the file.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -c 'variant="outline"' src/pages/OpsConsolePage.jsx | grep -q '^0$' && echo "PASS: No outline variants" || echo "FAIL: outline variants remain"; grep "from.*lucide-react" src/pages/OpsConsolePage.jsx | grep -qv "Badge" && echo "PASS: Badge not in lucide import" || echo "FAIL: Badge still in lucide import"; grep -q "import.*Badge.*from.*design-system\|import.*Button.*from.*design-system" src/pages/OpsConsolePage.jsx && echo "PASS: Badge and Button from design-system" || echo "FAIL: design-system imports missing"</automated>
  </verify>
  <done>OpsConsolePage has Badge and Button imported from design-system (not lucide-react), zero variant="outline" instances. Tabs render Badge notification counts correctly and Refresh button works.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all other admin/ops pages render with correct imports and wiring</name>
  <files>src/pages/Admin/AdminTenantDetailPage.jsx, src/pages/AdminDashboardPage.jsx, src/pages/FeatureFlagsPage.jsx, src/pages/TenantAdminPage.jsx, src/pages/SuperAdminDashboardPage.jsx, src/pages/ServiceQualityPage.jsx, src/pages/UsageDashboardPage.jsx, src/pages/TranslationDashboardPage.jsx, src/components/AuditLogTable.jsx</files>
  <action>
READ-ONLY AUDIT of all 9 remaining admin/ops files. Verify each file for:
- Design-system imports are correct (no missing imports for used components)
- No `variant="outline"` (invalid variant)
- No Badge collision (lucide-react Badge shadowing design-system Badge)
- Service function calls are correctly wired

Specific checks per file:

1. `src/pages/Admin/AdminTenantDetailPage.jsx`:
   - Verify `useTenantDetail` hook is imported and used for data loading
   - Verify `adminService` is imported for admin actions
   - Verify feature flag toggle uses `onOverrideFeature` callback
   - Uses raw HTML (Tailwind) — no design-system imports needed, which is acceptable
   - NO changes expected

2. `src/pages/AdminDashboardPage.jsx`:
   - Verify design-system imports present: PageLayout, PageContent, PageHeader, Alert, StatCard, Card, Badge, Button, Modal
   - Verify client loading via Supabase `from('profiles')`
   - Verify modals (Add Property, Import Guests, Generate Report) have open/close state
   - NO changes expected

3. `src/pages/FeatureFlagsPage.jsx`:
   - Verify `useFeatureFlags` hook is imported and provides flag data
   - Verify tab components are imported (FeatureFlagsTab, ExperimentsTab, FeedbackTab, AnnouncementsTab, FeatureFlagsDebug)
   - Verify Modal components are imported from design-system
   - NO changes expected

4. `src/pages/TenantAdminPage.jsx`:
   - Verify design-system imports include Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter
   - Verify billingService functions imported for tenant lifecycle
   - Verify search, filter, sort state management
   - NO changes expected

5. `src/pages/SuperAdminDashboardPage.jsx`:
   - Verify ErrorBoundary import present
   - Verify admin tools quick links use `onNavigate` prop
   - Uses raw HTML — no design-system imports needed, acceptable
   - NO changes expected

6. `src/pages/ServiceQualityPage.jsx`:
   - Verify design-system imports present: Card, CardContent, CardHeader, CardTitle, PageLayout, PageHeader, PageContent, Button, Badge, Stack, Inline, Banner
   - Verify slaService and metricsService imports
   - NO changes expected

7. `src/pages/UsageDashboardPage.jsx`:
   - Verify usageService import present
   - Self-contained with raw HTML — no design-system imports needed
   - NO changes expected

8. `src/pages/TranslationDashboardPage.jsx`:
   - Verify design-system imports present: PageLayout, PageContent, PageHeader, Card, Button, EmptyState, Checkbox
   - Verify translationService and languageService imports
   - NO changes expected

9. `src/components/AuditLogTable.jsx`:
   - Verify auditService import present
   - Verify date-fns imports for date formatting
   - Self-contained table component — no design-system imports needed
   - NO changes expected

If ANY file has a bug not caught during pre-audit (missing import, variant="outline", Badge collision), fix it.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && echo "=== variant=outline check ===" && grep -rn 'variant="outline"' src/pages/Admin/AdminTenantDetailPage.jsx src/pages/AdminDashboardPage.jsx src/pages/FeatureFlagsPage.jsx src/pages/TenantAdminPage.jsx src/pages/SuperAdminDashboardPage.jsx src/pages/ServiceQualityPage.jsx src/pages/UsageDashboardPage.jsx src/pages/TranslationDashboardPage.jsx src/components/AuditLogTable.jsx 2>/dev/null && echo "FAIL: outline variants found" || echo "PASS: No outline variants"; echo "=== key imports check ===" && grep -q "useTenantDetail" src/pages/Admin/AdminTenantDetailPage.jsx && echo "PASS: useTenantDetail present" || echo "FAIL"; grep -q "useFeatureFlags" src/pages/FeatureFlagsPage.jsx && echo "PASS: useFeatureFlags present" || echo "FAIL"; grep -q "slaService\|metricsService" src/pages/ServiceQualityPage.jsx && echo "PASS: slaService present" || echo "FAIL"; grep -q "auditService" src/components/AuditLogTable.jsx && echo "PASS: auditService present" || echo "FAIL"</automated>
  </verify>
  <done>All 9 admin/ops pages verified: correct imports, no variant="outline", no Badge collision, service wiring intact. AdminTenantDetailPage loads via useTenantDetail, FeatureFlagsPage uses useFeatureFlags, ServiceQualityPage uses slaService/metricsService, AuditLogTable uses auditService.</done>
</task>

</tasks>

<verification>
1. `grep -c 'variant="outline"' src/pages/OpsConsolePage.jsx` returns 0
2. `grep 'Badge' src/pages/OpsConsolePage.jsx | grep lucide` returns no matches
3. `grep 'Badge.*design-system\|design-system.*Badge' src/pages/OpsConsolePage.jsx` returns a match
4. `grep 'Button.*design-system\|design-system.*Button' src/pages/OpsConsolePage.jsx` returns a match
5. `grep -rn 'variant="outline"' src/pages/Admin/ src/pages/AdminDashboardPage.jsx src/pages/FeatureFlagsPage.jsx src/pages/TenantAdminPage.jsx src/pages/SuperAdminDashboardPage.jsx src/pages/ServiceQualityPage.jsx src/pages/UsageDashboardPage.jsx src/pages/TranslationDashboardPage.jsx src/components/AuditLogTable.jsx` returns no matches
6. `npm run build 2>&1 | tail -5` completes without errors for these files
</verification>

<success_criteria>
- OpsConsolePage renders tabs with Badge notification counts (design-system component, not lucide icon) and Refresh/View buttons work
- AdminTenantDetailPage loads tenant data with working feature flag toggles
- FeatureFlagsPage enables/disables flags per tenant via useFeatureFlags hook
- TenantAdminPage search, filter, sort, and lifecycle actions work
- ServiceQualityPage displays SLA metrics and charts
- UsageDashboardPage and TranslationDashboardPage render data without errors
- AuditLogTable displays chronological audit events
- Zero instances of variant="outline" across all admin/ops files
</success_criteria>

<output>
After completion, create `.planning/phases/090-admin-reseller-help-legacy/090-01-SUMMARY.md`
</output>
