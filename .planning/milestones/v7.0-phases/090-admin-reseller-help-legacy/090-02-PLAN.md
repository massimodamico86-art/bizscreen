---
phase: 090-admin-reseller-help-legacy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ClientsPage.jsx
  - src/pages/ResellerDashboardPage.jsx
  - src/pages/ResellerBillingPage.jsx
  - src/pages/HelpCenterPage.jsx
  - src/legacy/pages/FAQsPage.jsx
  - src/legacy/pages/ReferPage.jsx
  - src/legacy/pages/SetupPage.jsx
  - src/legacy/pages/SubscriptionPage.jsx
  - src/legacy/pages/UsersPage.jsx
  - src/legacy/components/listings/BackgroundVideoSelector.jsx
  - src/legacy/components/listings/BackgroundMediaManager.jsx
autonomous: true
requirements: [RESELL-01, RESELL-02, RESELL-03, HELP-01, HELP-02, LEGC-01]

must_haves:
  truths:
    - "ClientsPage renders CreateClientModal and EditClientModal close buttons with X icon from lucide-react"
    - "ResellerDashboardPage loads client list with metrics via resellerService and displays license generation modal"
    - "ResellerBillingPage displays revenue charts, commission events, and payout settings"
    - "HelpCenterPage navigates categories, searches topics, and displays topic detail via helpService"
    - "All 5 legacy pages (FAQs, Refer, Setup, Subscription, Users) render without JS errors — all referenced components are imported"
    - "Legacy pages have Badge imported from design-system (not lucide-react icon) where Badge component is used"
    - "Legacy pages and components have zero instances of variant='outline' — all replaced with variant='secondary'"
  artifacts:
    - path: "src/pages/ClientsPage.jsx"
      provides: "Client management console with create/edit modals and X close buttons"
      contains: "X,"
    - path: "src/legacy/pages/FAQsPage.jsx"
      provides: "FAQ listing with category badges and search"
      contains: "import.*Badge.*from.*design-system|import.*Card.*Badge.*from.*design-system"
    - path: "src/legacy/pages/UsersPage.jsx"
      provides: "Team members table with invite modal and role badges"
      contains: "variant=\"secondary\""
    - path: "src/legacy/pages/SubscriptionPage.jsx"
      provides: "Subscription billing page with plan cards and billing history"
      contains: "variant=\"secondary\""
    - path: "src/legacy/pages/ReferPage.jsx"
      provides: "Referral page with social share buttons and stats"
      contains: "variant=\"secondary\""
  key_links:
    - from: "src/pages/ClientsPage.jsx"
      to: "src/services/clientService.js"
      via: "fetchClientsWithStats, createClient, updateClient"
      pattern: "fetchClientsWithStats|createClient|updateClient"
    - from: "src/pages/ResellerDashboardPage.jsx"
      to: "src/services/resellerService.js"
      via: "fetchResellerDashboard, generateLicense"
      pattern: "resellerService|licenseService"
    - from: "src/pages/HelpCenterPage.jsx"
      to: "src/services/helpService.js"
      via: "fetchCategories, fetchTopics, searchTopics"
      pattern: "helpService"
---

<objective>
Audit and fix reseller pages, help center, and all legacy pages — fix ClientsPage missing X import, fix legacy pages missing imports and Badge collision bugs, replace all variant="outline" with variant="secondary", and verify reseller/help pages render correctly.

Purpose: RESELL-01/02/03 require reseller dashboard, billing, and client management work. HELP-01/02 require help center navigation and search work. LEGC-01 requires all 5 legacy pages render without JS errors. ClientsPage has a missing X import. All 5 legacy pages use Card, Button, Badge, StatCard, and Modal components without importing them, and 3 legacy pages have Badge imported from lucide-react (collision with design-system Badge component). Legacy pages also have variant="outline" which is invalid.

Output: ClientsPage with X import fixed; all 5 legacy pages with correct imports and zero variant="outline"; all reseller/help pages verified functional.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/ClientsPage.jsx
@src/pages/ResellerDashboardPage.jsx
@src/pages/ResellerBillingPage.jsx
@src/pages/HelpCenterPage.jsx
@src/legacy/pages/FAQsPage.jsx
@src/legacy/pages/ReferPage.jsx
@src/legacy/pages/SetupPage.jsx
@src/legacy/pages/SubscriptionPage.jsx
@src/legacy/pages/UsersPage.jsx
@src/legacy/components/listings/BackgroundVideoSelector.jsx
@src/legacy/components/listings/BackgroundMediaManager.jsx
@src/design-system/index.js

<interfaces>
<!-- Design-system Button valid variants: primary, secondary, ghost, danger -->
<!-- NO "outline" variant exists — use "secondary" instead -->
<!-- Known project decisions:
     - Phase 86: Button variant='outline' replaced with variant='secondary'
     - Phase 85: Badge collision fix — remove Badge from lucide-react to avoid shadowing design-system Badge
-->
<!-- Legacy wrapper: src/components/Button.jsx maps variant="outline" to "secondary" automatically.
     However, legacy pages do NOT import the wrapper either — they use Button/Card/Badge/Modal/StatCard
     without any import statements at all. These must be added. -->

From src/design-system/index.js (relevant exports):
```javascript
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from './components/Card';
export { Button, ButtonGroup, IconButton } from './components/Button';
export { Badge, StatusBadge } from './components/Badge';
export { Alert, Banner } from './components/Alert';
export { EmptyState, Placeholder } from './components/EmptyState';
export { Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter } from './components/Modal';
```

From src/components/StatCard.jsx (wrapper component):
```javascript
export default StatCard;  // default export
```

ClientsPage bug (line 24-41 imports):
```javascript
import {
  AlertCircle, Building2, CheckCircle, CreditCard, Edit2, Image,
  Lock, Mail, Monitor, MoreVertical, Plus, RefreshCw, Search,
  User, UserCheck, Users,
} from 'lucide-react';
// MISSING: X — used on line 432 (<X className="w-5 h-5 text-gray-500" />) and line 615
```

Legacy page bugs summary:
- FAQsPage.jsx: Uses Card, Badge. Imports Badge from lucide-react (COLLISION). Missing: Card from design-system, Badge from design-system (remove from lucide-react).
- ReferPage.jsx: Uses Card, Button, StatCard. Missing ALL three imports. Has 3x variant="outline".
- SetupPage.jsx: Uses Card, Button. Missing both imports.
- SubscriptionPage.jsx: Uses Card, Button, Badge. Imports Badge from lucide-react (COLLISION). Missing: Card, Button from design-system, Badge from design-system. Has 1x variant="outline" (conditional, line 45).
- UsersPage.jsx: Uses Card, Button, Badge, Modal. Imports Badge from lucide-react (COLLISION). Missing: Card, Button, Modal from design-system, Badge from design-system. Has 2x variant="outline".

Legacy component bugs:
- BackgroundVideoSelector.jsx: Uses Modal, Button. Missing both imports. Has 1x variant="outline".
- BackgroundMediaManager.jsx: Uses Button, X. Has ZERO imports at all. Has 3x variant="outline".

Reseller/Help pages (pre-audited, no bugs found):
- ResellerDashboardPage: Has correct design-system imports, uses resellerService and licenseService
- ResellerBillingPage: Has correct design-system imports
- HelpCenterPage: Has correct design-system imports, uses helpService
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ClientsPage missing X import and verify reseller/help pages</name>
  <files>src/pages/ClientsPage.jsx, src/pages/ResellerDashboardPage.jsx, src/pages/ResellerBillingPage.jsx, src/pages/HelpCenterPage.jsx</files>
  <action>
1. Open `src/pages/ClientsPage.jsx`

2. Add `X` to the lucide-react import block (between `Users` and the closing brace, or alphabetically). The current import (lines 24-41) has: AlertCircle, Building2, CheckCircle, CreditCard, Edit2, Image, Lock, Mail, Monitor, MoreVertical, Plus, RefreshCw, Search, User, UserCheck, Users. Add `X` after `Users`:
```javascript
import {
  AlertCircle,
  Building2,
  CheckCircle,
  CreditCard,
  Edit2,
  Image,
  Lock,
  Mail,
  Monitor,
  MoreVertical,
  Plus,
  RefreshCw,
  Search,
  User,
  UserCheck,
  Users,
  X,
} from 'lucide-react';
```

3. Save the file.

4. READ-ONLY AUDIT of reseller and help pages:

   `src/pages/ResellerDashboardPage.jsx`:
   - Verify design-system imports are present and correct
   - Verify resellerService and licenseService imports for data loading
   - Verify license generation modal has open/close state management
   - Verify add tenant modal has open/close state management
   - NO changes expected

   `src/pages/ResellerBillingPage.jsx`:
   - Verify design-system imports are present and correct
   - Verify revenue charts, commission events table, and payout settings sections render
   - NO changes expected

   `src/pages/HelpCenterPage.jsx`:
   - Verify design-system imports present: PageLayout, PageHeader, PageContent, Button, Card, EmptyState
   - Verify helpService import for fetchCategories, fetchTopics, searchTopics
   - Verify category browsing, search, and topic detail navigation
   - NO changes expected

If ANY file has a bug not caught during pre-audit, fix it.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -q "X," src/pages/ClientsPage.jsx && echo "PASS: X imported in ClientsPage" || echo "FAIL: X missing"; grep -q "resellerService\|licenseService" src/pages/ResellerDashboardPage.jsx && echo "PASS: resellerService present" || echo "FAIL"; grep -q "helpService" src/pages/HelpCenterPage.jsx && echo "PASS: helpService present" || echo "FAIL"</automated>
  </verify>
  <done>ClientsPage has X imported from lucide-react — CreateClientModal and EditClientModal close buttons render without errors. ResellerDashboardPage loads clients and metrics via resellerService. ResellerBillingPage displays revenue/commission data. HelpCenterPage navigates categories and searches topics via helpService.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all legacy pages and components — add missing imports, fix Badge collision, replace variant="outline"</name>
  <files>src/legacy/pages/FAQsPage.jsx, src/legacy/pages/ReferPage.jsx, src/legacy/pages/SetupPage.jsx, src/legacy/pages/SubscriptionPage.jsx, src/legacy/pages/UsersPage.jsx, src/legacy/components/listings/BackgroundVideoSelector.jsx, src/legacy/components/listings/BackgroundMediaManager.jsx</files>
  <action>
Fix all 7 legacy files. Each file needs missing design-system imports and variant="outline" replacements.

**1. `src/legacy/pages/FAQsPage.jsx`:**
- Current imports: `import { Badge, Search } from 'lucide-react';`
- Uses: Card (lines 43, 49, 59), Badge with variant="info" (line 52), Search icon (line 25)
- Fix: REMOVE `Badge` from lucide-react import (only keep Search). ADD design-system imports:
```javascript
import { Search } from 'lucide-react';
import { Card, Badge } from '../../design-system';
```
- No variant="outline" to fix.

**2. `src/legacy/pages/ReferPage.jsx`:**
- Current imports: `import { DollarSign, Share2, Target, Users } from 'lucide-react';`
- Uses: Card (lines 28, 40), Button (lines 32, 43, 46, 49), StatCard (lines 23-25)
- Fix: ADD imports after lucide-react:
```javascript
import { Card, Button } from '../../design-system';
import StatCard from '../../components/StatCard';
```
- Replace 3x `variant="outline"` with `variant="secondary"` on lines 43, 46, 49.

**3. `src/legacy/pages/SetupPage.jsx`:**
- Current imports: `import { Check } from 'lucide-react';`
- Uses: Card (line 34), Button (line 83)
- Fix: ADD imports after lucide-react:
```javascript
import { Card, Button } from '../../design-system';
```
- No variant="outline" to fix.

**4. `src/legacy/pages/SubscriptionPage.jsx`:**
- Current imports: `import { Badge, CreditCard, Download } from 'lucide-react';`
- Uses: Card (lines 20, 36, 55), Button (lines 27, 43, 65), Badge with variant="success" (line 41)
- Fix: REMOVE `Badge` from lucide-react (keep CreditCard, Download). ADD design-system imports:
```javascript
import { CreditCard, Download } from 'lucide-react';
import { Card, Button, Badge } from '../../design-system';
```
- Replace 1x `variant="outline"` with `variant="secondary"` on line 65 (Invoice button).
- Line 45 has a conditional: `variant={currentPlan === plan.id ? 'outline' : 'primary'}` — replace `'outline'` with `'secondary'`.

**5. `src/legacy/pages/UsersPage.jsx`:**
- Current imports: `import { Badge, Plus } from 'lucide-react';`
- Uses: Card (line 21), Button (lines 15, 50, 77, 78), Badge with variant="info"/"success" (lines 43, 47), Modal (line 62)
- Fix: REMOVE `Badge` from lucide-react (keep Plus). ADD design-system imports:
```javascript
import { Plus } from 'lucide-react';
import { Card, Button, Badge, Modal } from '../../design-system';
```
- IMPORTANT: The Modal on line 62 uses legacy API: `<Modal isOpen={true} onClose={...} title="Invite Team Member" size="small">`. The design-system Modal uses `open` prop instead of `isOpen` and different size format. Since these legacy pages are meant to "render without JS errors", either:
  (a) Import the legacy Modal wrapper: `import Modal from '../../components/Modal';` (supports `isOpen` and `size="small"`)
  (b) Or update the JSX to use design-system Modal API.

  Use option (a) — import the legacy Modal wrapper from `../../components/Modal` which maps `isOpen` to `open` and `size="small"` to `size="sm"`. Keep design-system imports for Card, Button, Badge only:
```javascript
import { Plus } from 'lucide-react';
import { Card, Button, Badge } from '../../design-system';
import Modal from '../../components/Modal';
```
- Replace 2x `variant="outline"` with `variant="secondary"` on lines 50 and 77.

**6. `src/legacy/components/listings/BackgroundVideoSelector.jsx`:**
- Current imports: `import { useState, useRef } from 'react'; import { Plus } from 'lucide-react';`
- Uses: Modal (line 38, uses `isOpen` and `size="large"` legacy API), Button (lines 98-99)
- Fix: ADD imports after existing:
```javascript
import { Button } from '../../design-system';
import Modal from '../../components/Modal';
```
- IMPORTANT: Uses legacy Modal API (`isOpen`, `size="large"`), so import legacy wrapper.
- Replace 1x `variant="outline"` with `variant="secondary"` on line 98.

**7. `src/legacy/components/listings/BackgroundMediaManager.jsx`:**
- Current: NO imports at all (line 1 is blank, line 2 starts with `export const`)
- Uses: Button (lines 66, 99, 144), X icon (lines 62, 95, 134)
- Fix: ADD imports at the top of the file:
```javascript
import { X } from 'lucide-react';
import { Button } from '../../design-system';
```
- Replace 3x `variant="outline"` with `variant="secondary"` on lines 68, 101, 146.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && echo "=== variant=outline check ===" && grep -rn 'variant="outline"' src/legacy/ 2>/dev/null && echo "FAIL: outline variants remain" || echo "PASS: No outline variants in legacy"; echo "=== Badge collision check ===" && for f in src/legacy/pages/FAQsPage.jsx src/legacy/pages/SubscriptionPage.jsx src/legacy/pages/UsersPage.jsx; do grep "from.*lucide-react" "$f" | grep -q "Badge" && echo "FAIL: Badge still in lucide import in $f" || echo "PASS: No Badge in lucide import in $f"; done; echo "=== design-system imports check ===" && grep -q "from.*design-system" src/legacy/pages/FAQsPage.jsx && echo "PASS: FAQs has design-system" || echo "FAIL"; grep -q "from.*design-system" src/legacy/pages/ReferPage.jsx && echo "PASS: Refer has design-system" || echo "FAIL"; grep -q "from.*design-system" src/legacy/pages/SetupPage.jsx && echo "PASS: Setup has design-system" || echo "FAIL"; grep -q "from.*design-system" src/legacy/pages/SubscriptionPage.jsx && echo "PASS: Subscription has design-system" || echo "FAIL"; grep -q "from.*design-system" src/legacy/pages/UsersPage.jsx && echo "PASS: Users has design-system" || echo "FAIL"; grep -q "import.*X.*from\|from.*lucide-react" src/legacy/components/listings/BackgroundMediaManager.jsx && echo "PASS: BackgroundMediaManager has imports" || echo "FAIL"</automated>
  </verify>
  <done>All 7 legacy files fixed: FAQsPage, ReferPage, SetupPage, SubscriptionPage, UsersPage have correct design-system imports (Card, Button, Badge) and legacy Modal wrapper where needed. BackgroundVideoSelector and BackgroundMediaManager have correct imports. Zero variant="outline" instances across all legacy files. Zero Badge collision bugs (Badge removed from lucide-react imports where design-system Badge is used).</done>
</task>

</tasks>

<verification>
1. `grep -q "X," src/pages/ClientsPage.jsx` — X is in lucide-react import
2. `grep -rn 'variant="outline"' src/legacy/` returns no matches
3. `grep -rn 'variant="outline"' src/pages/ClientsPage.jsx src/pages/ResellerDashboardPage.jsx src/pages/ResellerBillingPage.jsx src/pages/HelpCenterPage.jsx` returns no matches
4. For each of FAQsPage, SubscriptionPage, UsersPage: `grep 'Badge' file | grep lucide` returns no matches
5. For each of the 5 legacy pages: `grep 'design-system' file` returns a match
6. `grep -q "import" src/legacy/components/listings/BackgroundMediaManager.jsx` returns a match
7. `npm run build 2>&1 | tail -5` completes without errors
</verification>

<success_criteria>
- ClientsPage CreateClientModal and EditClientModal render X close buttons without errors
- ResellerDashboardPage loads client list with metrics and license generation modal works
- ResellerBillingPage displays subscription info with revenue charts
- ClientsPage management actions (add, edit) work with proper modals
- HelpCenterPage navigation and search return results
- All 5 legacy pages (FAQs, Refer, Setup, Subscription, Users) render without JS errors
- All legacy pages have correct design-system imports (Card, Button, Badge where used)
- Zero Badge collision bugs in legacy pages
- Zero variant="outline" instances across all legacy files and components
</success_criteria>

<output>
After completion, create `.planning/phases/090-admin-reseller-help-legacy/090-02-SUMMARY.md`
</output>
