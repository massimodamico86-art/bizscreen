---
phase: 85-scheduling-campaigns
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/CampaignEditorPage.jsx
  - src/pages/CampaignsPage.jsx
  - src/pages/components/CampaignEditorComponents.jsx
autonomous: true
requirements:
  - CAMP-01
  - CAMP-02
  - CAMP-03

must_haves:
  truths:
    - "CampaignsPage renders without JS errors — campaign list, stats cards, search, status filter, and action menus all functional"
    - "CampaignEditorPage renders without JS errors — details form, targets section, content section, summary sidebar, and analytics card all visible"
    - "Campaign CRUD (create, edit, delete) and status actions (activate, pause) complete without errors"
    - "Campaign rotation controls, priority slider, and seasonal date picker are interactive"
    - "CampaignAnalyticsCard renders with date range filter in the editor sidebar"
  artifacts:
    - path: "src/pages/CampaignEditorPage.jsx"
      provides: "Campaign editor with all sub-components imported and working"
      contains: "import.*Button.*Card.*Badge.*Modal.*from.*design-system"
    - path: "src/pages/CampaignsPage.jsx"
      provides: "Campaign list page with CRUD operations"
      contains: "CampaignsPage"
    - path: "src/pages/components/CampaignEditorComponents.jsx"
      provides: "TargetPickerModal, ContentPickerModal, ApprovalRequestModal, PreviewLinksModal"
      contains: "export function TargetPickerModal"
  key_links:
    - from: "src/pages/CampaignEditorPage.jsx"
      to: "src/pages/components/CampaignEditorComponents.jsx"
      via: "named import"
      pattern: "import.*TargetPickerModal.*ContentPickerModal.*ApprovalRequestModal.*PreviewLinksModal.*from.*CampaignEditorComponents"
    - from: "src/pages/CampaignEditorPage.jsx"
      to: "src/components/campaigns/RotationControls.jsx"
      via: "default or named import"
      pattern: "import.*RotationControls.*from.*campaigns"
    - from: "src/pages/CampaignEditorPage.jsx"
      to: "src/components/analytics/CampaignAnalyticsCard.jsx"
      via: "default import"
      pattern: "import.*CampaignAnalyticsCard.*from.*analytics"
---

<objective>
Fix all import errors and render bugs in CampaignEditorPage, CampaignsPage, and CampaignEditorComponents so all campaign pages load without crashes, and verify that campaign CRUD, rotation, priority, seasonal dates, and analytics display are functional.

Purpose: CampaignEditorPage has ~13 missing imports (Button, Card, Badge, Modal/ModalHeader/ModalContent/ModalFooter/ModalTitle from design-system; X from lucide-react; SeasonalDatePicker, FrequencyLimitControls, RotationControls from campaigns; CampaignAnalyticsCard; TargetPickerModal, ContentPickerModal, ApprovalRequestModal, PreviewLinksModal from CampaignEditorComponents). CampaignEditorComponents.jsx also uses Button, Card, X without importing them. CampaignsPage has a minor unused `Icon` import. This plan fixes all issues and verifies campaign lifecycle.

Output: All campaign pages render and function correctly.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/CampaignEditorPage.jsx
@src/pages/CampaignsPage.jsx
@src/pages/components/CampaignEditorComponents.jsx
@src/components/campaigns/index.js
@src/design-system/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CampaignEditorPage and CampaignEditorComponents missing imports</name>
  <files>src/pages/CampaignEditorPage.jsx, src/pages/components/CampaignEditorComponents.jsx, src/pages/CampaignsPage.jsx</files>
  <action>
    **CampaignEditorPage.jsx — Critical missing imports (page will crash without these):**

    1. **Badge collision**: Line 16 imports `Badge` from `lucide-react` (it's an icon, not a component). Line 211 uses `<Badge color={statusBadge.color}>` which expects the design-system Badge. Remove `Badge` from the lucide-react import.

    2. **Missing design-system imports**: Add `import { Button, Card, Badge, Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter } from '../design-system';`
       - `Button` is used on ~15 lines (233, 239, etc.)
       - `Card` is used on lines 289, 387, 441, 546, 598, 637
       - `Badge` for line 211 status badge
       - `Modal`, `ModalHeader`, `ModalTitle`, `ModalContent`, `ModalFooter` for the Save as Template modal (lines 709-761)

    3. **Missing lucide-react icon**: `X` is used on line 430 (remove target button) but not in the lucide-react import. Add `X` to the existing lucide-react import.

    4. **Missing campaign sub-component imports**:
       - `SeasonalDatePicker` (line 376): `import SeasonalDatePicker from '../components/campaigns/SeasonalDatePicker';`
       - `FrequencyLimitControls` (line 509): `import FrequencyLimitControls from '../components/campaigns/FrequencyLimitControls';`
       - `RotationControls` (line 527): `import RotationControls from '../components/campaigns/RotationControls';`

    5. **Missing analytics import**:
       - `CampaignAnalyticsCard` (line 588): `import CampaignAnalyticsCard from '../components/analytics/CampaignAnalyticsCard';`

    6. **Missing modal component imports from CampaignEditorComponents**:
       - `TargetPickerModal` (line 654), `ContentPickerModal` (line 669), `ApprovalRequestModal` (line 682), `PreviewLinksModal` (line 693)
       - Add: `import { TargetPickerModal, ContentPickerModal, ApprovalRequestModal, PreviewLinksModal } from './components/CampaignEditorComponents';`

    **CampaignEditorComponents.jsx — Missing imports (these components use Button, Card, X without importing):**

    1. Add `import { Button, Card } from '../../design-system';` (Button used in ApprovalRequestModal, PreviewLinksModal, TargetPickerModal; Card used in all modals)
    2. Add `X` to the existing lucide-react import (used in ApprovalRequestModal line 54, PreviewLinksModal line 142, TargetPickerModal line 302, ContentPickerModal line 455)

    **CampaignsPage.jsx — Minor cleanup:**

    1. Remove unused `Icon` import from lucide-react (line 12: `Icon` is imported but never used — the page defines local `Icon` via `const Icon = STATUS_CONFIG[status].icon`)

    After fixing all imports, run the dev server and verify both pages render without errors.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && node -e "
      const fs = require('fs');
      const files = [
        'src/pages/CampaignEditorPage.jsx',
        'src/pages/CampaignsPage.jsx',
        'src/pages/components/CampaignEditorComponents.jsx'
      ];
      let ok = true;
      for (const f of files) {
        const code = fs.readFileSync(f, 'utf8');
        // Check that Button is imported somewhere in the file
        if (code.includes('<Button') && !code.includes(\"import\") ) { ok = false; console.log(f + ': missing Button import'); }
      }
      // Check CampaignEditorPage specifically for critical imports
      const editor = fs.readFileSync('src/pages/CampaignEditorPage.jsx', 'utf8');
      const required = ['RotationControls', 'SeasonalDatePicker', 'FrequencyLimitControls', 'CampaignAnalyticsCard', 'TargetPickerModal', 'ContentPickerModal'];
      for (const r of required) {
        if (!editor.includes('import') || !editor.match(new RegExp('import.*' + r))) {
          console.log('CampaignEditorPage missing import: ' + r);
          ok = false;
        }
      }
      console.log(ok ? 'PASS: All critical imports present' : 'FAIL: Missing imports');
      process.exit(ok ? 0 : 1);
    "
    </automated>
    <manual>Open http://localhost:5173 in browser, navigate to Campaigns page, verify it loads. Click into a campaign or create new one, verify the editor renders all sections.</manual>
  </verify>
  <done>CampaignEditorPage renders without crashes — all 13+ missing imports resolved. CampaignEditorComponents modals (TargetPicker, ContentPicker, Approval, Preview) render with Button/Card/X. CampaignsPage renders cleanly with no unused imports.</done>
</task>

<task type="auto">
  <name>Task 2: Verify campaign lifecycle, rotation controls, and analytics display via Playwright</name>
  <files>src/pages/CampaignEditorPage.jsx, src/pages/CampaignsPage.jsx</files>
  <action>
    With imports fixed, use Playwright MCP browser to verify the following success criteria:

    1. **CAMP-01 — Campaign CRUD**: Navigate to Campaigns page. Verify the page renders with: header, search bar, status filter dropdown, quick stats cards (Active, Scheduled, Drafts, Paused), and campaign table. Click "New Campaign" dropdown, select "Blank Campaign" to navigate to `/app/campaigns/new`. Verify the campaign editor loads with: name input, description, start/end date-time pickers, priority slider (50-200 range), Save button. Fill in a name and save. Verify navigation back to campaigns list shows the new campaign. Test delete via the action menu.

    2. **CAMP-02 — Rotation, priority, and seasonal controls**: In a campaign with 2+ content items, verify:
       - `RotationControls` renders below the content list with mode selection
       - Priority slider is visible and adjustable (50-200 range)
       - `SeasonalDatePicker` renders in the details section for existing campaigns
       - `FrequencyLimitControls` renders when expanding a content item's settings (gear icon)

    3. **CAMP-03 — Campaign analytics display**: In the campaign editor sidebar for an existing campaign, verify `CampaignAnalyticsCard` renders with a date range selector and analytics data (or empty state if no data).

    4. **Additional modal verification**: Verify TargetPickerModal opens when clicking "Add Target", ContentPickerModal opens when clicking "Add Content", and Save as Template modal opens from the header button.

    Fix any additional runtime errors encountered during verification. If components crash due to missing props or undefined references, add guard clauses.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && node -e "
      const fs = require('fs');
      const editor = fs.readFileSync('src/pages/CampaignEditorPage.jsx', 'utf8');
      // Verify no remaining undefined component references
      const used = ['Button', 'Card', 'Badge', 'Modal', 'ModalHeader', 'ModalTitle', 'ModalContent', 'ModalFooter', 'RotationControls', 'SeasonalDatePicker', 'FrequencyLimitControls', 'CampaignAnalyticsCard', 'TargetPickerModal', 'ContentPickerModal', 'ApprovalRequestModal', 'PreviewLinksModal'];
      let ok = true;
      for (const u of used) {
        if (editor.includes('<' + u) && !editor.match(new RegExp('import[^;]*' + u))) {
          console.log('Used but not imported: ' + u);
          ok = false;
        }
      }
      console.log(ok ? 'PASS: All components imported' : 'FAIL');
      process.exit(ok ? 0 : 1);
    "
    </automated>
    <manual>In campaign editor: details form editable, targets section shows Add Target button that opens picker, content section shows Add Content button, rotation controls visible with 2+ items, analytics card in sidebar, save/delete/activate/pause buttons work</manual>
  </verify>
  <done>Campaign CRUD works end-to-end — create, edit, delete, activate, pause. Rotation controls, priority slider, and seasonal date picker are interactive. CampaignAnalyticsCard displays in sidebar with date range filter. All 3 CAMP requirements pass.</done>
</task>

</tasks>

<verification>
1. CampaignsPage loads without JS errors — list renders, stats cards show counts, search filters, status filter works, action menus functional
2. CampaignEditorPage loads without JS errors — all sections render (details, targets, content, summary, analytics, validation)
3. All campaign sub-components render: RotationControls, SeasonalDatePicker, FrequencyLimitControls, CampaignAnalyticsCard
4. All campaign modals render: TargetPickerModal, ContentPickerModal, ApprovalRequestModal, PreviewLinksModal, Save as Template modal
5. Campaign lifecycle actions work: create, save, activate, pause, delete
</verification>

<success_criteria>
- CampaignsPage and CampaignEditorPage render without crashes
- Campaign CRUD (create, edit, delete) and status actions (activate, pause) complete without errors
- Rotation controls, priority slider, and seasonal date picker are visible and interactive
- CampaignAnalyticsCard renders in sidebar with date range filter
- All modals (Target, Content, Approval, Preview, Template) open and close correctly
</success_criteria>

<output>
After completion, create `.planning/phases/85-scheduling-campaigns/85-02-SUMMARY.md`
</output>
