---
phase: 87-data-sources-apps-moderation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ModerationPage.jsx
  - src/pages/ReviewInboxPage.jsx
  - src/services/approvalService.js
autonomous: false
requirements:
  - MODQ-01
  - MODQ-02

must_haves:
  truths:
    - "ModerationPage loads social feed posts with account filter and status filter tabs"
    - "User can approve or reject a social feed post and the status updates"
    - "ModerationPage shows empty state with link to connect social accounts when no posts exist"
    - "ReviewInboxPage loads review requests with search, status filter, and type filter"
    - "User can click a review row to open the detail drawer with comments and approve/reject actions"
    - "Approve and reject actions in ReviewInboxPage update the review status and refresh the list"
  artifacts:
    - path: "src/pages/ModerationPage.jsx"
      provides: "Social feed moderation queue with approve/reject actions, account filter, status tabs"
    - path: "src/pages/ReviewInboxPage.jsx"
      provides: "Content review inbox with table, detail drawer, comments, approve/reject decision modal"
    - path: "src/services/approvalService.js"
      provides: "fetchReviews, fetchReview, approveReview, rejectReview, addReviewComment"
  key_links:
    - from: "src/pages/ModerationPage.jsx"
      to: "src/services/socialFeedSyncService.js"
      via: "getModerationQueue, moderatePost, getConnectedAccounts"
      pattern: "import.*from.*socialFeedSyncService"
    - from: "src/pages/ModerationPage.jsx"
      to: "src/services/social"
      via: "PROVIDER_LABELS, PROVIDER_COLORS"
      pattern: "import.*from.*services/social"
    - from: "src/pages/ReviewInboxPage.jsx"
      to: "src/services/approvalService.js"
      via: "fetchReviews, fetchReview, approveReview, rejectReview, addReviewComment"
      pattern: "import.*from.*approvalService"
    - from: "src/pages/ReviewInboxPage.jsx"
      to: "src/design-system"
      via: "PageLayout, PageContent, PageHeader, Card, Button, Badge, EmptyState, Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter"
      pattern: "import.*from.*design-system"
---

<objective>
Audit and verify ModerationPage (social feed moderation queue with approve/reject) and ReviewInboxPage (content approval inbox with detail drawer and decision modals).

Purpose: Ensure social feed moderation queue displays pending posts with working approve/reject actions (MODQ-01), and review inbox shows pending content approvals with functional approve/reject workflow (MODQ-02).
Output: Verified ModerationPage and ReviewInboxPage with all imports correct, service calls wired, and action handlers functional.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/pages/ModerationPage.jsx
@src/pages/ReviewInboxPage.jsx
@src/services/approvalService.js
@src/services/socialFeedSyncService.js
@src/services/social/index.js

<interfaces>
<!-- ModerationPage service imports -->
From src/services/socialFeedSyncService.js:
- getModerationQueue(accountId?) → Array of social feed posts with moderation records
- moderatePost(feedId, approved, notes?) → Moderation record
- getConnectedAccounts(tenantId?) → Array of connected social accounts

From src/services/social/index.js:
- PROVIDER_LABELS: { instagram: "Instagram", facebook: "Facebook", tiktok: "TikTok", google: "Google Reviews" }
- PROVIDER_COLORS: { [provider]: { light: "bg-... text-...", dark: "..." } }

<!-- ReviewInboxPage service imports -->
From src/services/approvalService.js:
- fetchReviews({ status?, resourceType? }) → Array of review requests
- fetchReview(reviewId) → Review with comments
- approveReview(reviewId, { comment? }) → Updated review
- rejectReview(reviewId, { comment }) → Updated review (comment required)
- addReviewComment(reviewId, message) → Created comment

Design-system components used by ReviewInboxPage:
- PageLayout, PageContent, PageHeader, Card, Button, Badge, EmptyState
- Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter

Note: ReviewInboxPage imports unused `Icon` from lucide-react (line 8) — remove it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModerationPage and ReviewInboxPage import audit and functional wiring verification</name>
  <files>src/pages/ModerationPage.jsx, src/pages/ReviewInboxPage.jsx</files>
  <action>
**ModerationPage audit (~307 lines):**

1. **Import verification:**
   - Verify lucide-react imports: CheckCircle, XCircle, Loader2, Heart, MessageCircle, AlertCircle. All used in rendering.
   - Verify design-system imports: PageLayout, PageContent, PageHeader, Card, EmptyState.
   - Verify socialFeedSyncService imports: getModerationQueue, moderatePost, getConnectedAccounts.
   - Verify social/index imports: PROVIDER_LABELS, PROVIDER_COLORS.

2. **Account filter wiring:**
   - Confirm `useEffect` on mount calls `getConnectedAccounts()` and populates `accounts` state.
   - Confirm `accountId` state change triggers `loadPosts()` which calls `getModerationQueue(accountId || null)`.

3. **Status filter wiring:**
   - Confirm `statusFilter` state controls client-side filtering via `filteredPosts`.
   - Confirm status tab buttons update `statusFilter` and show counts.

4. **Approve/reject action wiring:**
   - Confirm `handleModerate(postId, true)` calls `moderatePost(postId, true)` for approval.
   - Confirm `handleModerate(postId, false)` calls `moderatePost(postId, false)` for rejection.
   - Confirm `actioning` state disables buttons during API call.
   - Confirm posts list refreshes after action via `loadPosts()`.

5. **Empty state:**
   - Confirm empty state shows "Connect Social Account" button that calls `onNavigate?.('social-accounts')`.

**ReviewInboxPage audit (~706 lines):**

6. **Import verification:**
   - Remove unused `Icon` import from lucide-react (line 8). It is NOT used anywhere — the `Icon` on line 514 is a local variable (`const Icon = RESOURCE_ICONS[...]`), not the imported one.
   - Verify remaining lucide-react imports are used: CheckCircle, ChevronRight, Clock, Film, Filter, Layout, ListVideo, Loader2, MessageSquare, RefreshCw, Search, Send, X, XCircle, Zap.
   - Verify design-system imports: PageLayout, PageContent, PageHeader, Card, Button, Badge, EmptyState, Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter.
   - Verify approvalService imports: fetchReviews, fetchReview, approveReview, rejectReview, addReviewComment.
   - Verify useAuth and useTranslation imports.

7. **Review list wiring:**
   - Confirm `loadReviews()` calls `fetchReviews({ status: statusFilter, resourceType: typeFilter })`.
   - Confirm status filter and type filter `<select>` elements update state and trigger `useEffect` re-fetch.
   - Confirm `search` state filters client-side on title, resource_name, and requested_by_name.

8. **Detail drawer wiring:**
   - Confirm `handleRowClick(review)` sets `selectedReview` and calls `loadReviewDetail(review.id)`.
   - Confirm `loadReviewDetail` calls `fetchReview(reviewId)` and sets `reviewDetail`.
   - Confirm drawer close resets `selectedReview`, `reviewDetail`, and `newComment`.

9. **Comments wiring:**
   - Confirm `handleSubmitComment()` validates non-empty, calls `addReviewComment(selectedReview.id, newComment.trim())`.
   - Confirm successful comment is appended to local `reviewDetail.comments` array.

10. **Approve/reject decision modal wiring:**
    - Confirm `showDecisionModal` state ('approve' | 'reject' | null) controls the modal.
    - Confirm `handleApprove()` calls `approveReview(selectedReview.id, { comment: decisionComment })`.
    - Confirm `handleReject()` validates `decisionComment.trim()` is non-empty, then calls `rejectReview(selectedReview.id, { comment: decisionComment })`.
    - Confirm both handlers close drawer, clear state, and call `loadReviews()` to refresh.
    - Confirm `submittingDecision` state disables buttons during API call.

11. **Navigate to resource:**
    - Confirm `handleNavigateToResource` maps resource types to page routes correctly.

Fix the dead `Icon` import in ReviewInboxPage. Fix any other broken imports or wiring issues found.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx vite build --mode development 2>&1 | head -50</automated>
  </verify>
  <done>ModerationPage and ReviewInboxPage have all imports verified, unused `Icon` removed from ReviewInboxPage, all service calls correctly wired (getModerationQueue, moderatePost, fetchReviews, fetchReview, approveReview, rejectReview, addReviewComment), and both pages build without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify ModerationPage and ReviewInboxPage functional behavior in browser</name>
  <action>
    Human verification of ModerationPage and ReviewInboxPage in the browser:
    1. Navigate to Content Moderation page (sidebar -> Content Moderation)
    2. Verify the page loads without console errors
    3. Verify account dropdown populates (if social accounts are connected)
    4. Verify status filter tabs (All, Pending, Approved, Rejected) are clickable and show counts
    5. If posts exist: verify Approve and Reject buttons are visible and not disabled
    6. Navigate to Reviews page (sidebar -> Reviews)
    7. Verify the page loads with the reviews table (or empty state)
    8. Verify status filter dropdown and type filter dropdown work
    9. If reviews exist: click a row -> verify the detail drawer slides in from the right
    10. In the drawer: verify Approve and Reject buttons appear for open reviews
    11. Verify the comment input and Send button are present for open reviews
    12. Verify no console errors on either page
  </action>
  <verify>Human confirms both pages load and all interactive elements are present and functional</verify>
  <done>ModerationPage shows moderation queue with working filters and approve/reject actions; ReviewInboxPage shows reviews table with working detail drawer, comments, and decision modal</done>
</task>

</tasks>

<verification>
- ModerationPage and ReviewInboxPage build without import errors
- ReviewInboxPage no longer imports unused `Icon` from lucide-react
- All service imports match exported function signatures
- Approve/reject handlers call the correct service functions with correct arguments
</verification>

<success_criteria>
ModerationPage loads social feed posts with account filter and status tabs, approve/reject actions work. ReviewInboxPage loads reviews with search/filter, detail drawer opens on row click, approve/reject decision modal appears with comment support.
</success_criteria>

<output>
After completion, create `.planning/phases/87-data-sources-apps-moderation/87-03-SUMMARY.md`
</output>
