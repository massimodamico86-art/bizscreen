---
phase: 166.1-marketplace-nav-and-seed
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
autonomous: false
requirements:
  - TBD-166.1-UAT
must_haves:
  truths:
    - "A visible sidebar item labeled 'Template Marketplace' renders for authenticated client users"
    - "Clicking the sidebar item invokes setCurrentPage('template-marketplace') and the TemplateMarketplacePage renders in the main content area"
    - "No React fiber-level state injection is required to reach the Template Marketplace page — a normal mouse click is sufficient"
  artifacts:
    - path: "src/App.jsx"
      provides: "Sidebar navigation entry for template-marketplace"
      contains: "'template-marketplace'"
  key_links:
    - from: "src/App.jsx navigation array (lines 457-479)"
      to: "pages['template-marketplace'] (line 556)"
      via: "setCurrentPage('template-marketplace') handler attached to sidebar button at line 826"
      pattern: "id: 'template-marketplace'"
---

<objective>
Add a visible sidebar navigation entry for the Template Marketplace so users can reach the page that is already registered in `src/App.jsx` (line 556) without React fiber-level state injection.

Purpose: Phase 166 UAT confirmed the Template Marketplace page is wired in the page router at `src/App.jsx:556` but no button, sidebar item, or dashboard card invokes `setCurrentPage('template-marketplace')` from the UI. This plan closes that gap by following the exact pattern used by existing flat (non-expandable) sidebar items (e.g., `templates`, `playlists`, `screens`).

Output: Updated `src/App.jsx` with one new entry in the `navigation` array. No new component, no new route, no new feature flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166.1-marketplace-nav-and-seed/README.md
@.planning/phases/166-template-quick-customize/166-HUMAN-UAT.md
@.planning/phases/166-template-quick-customize/166-02-SUMMARY.md
@src/App.jsx

<interfaces>
<!-- Key patterns the executor needs from src/App.jsx. Executor should match these exactly — do NOT introduce a new navigation primitive. -->

**The `navigation` array** (src/App.jsx lines 457-479) — a flat list of objects with this shape for non-expandable items:
```js
{ id: '<page-id>', label: t('nav.<key>', '<fallback label>'), icon: <LucideIconComponent> }
```

**Examples of existing flat navigation items (lines 474-478):**
```js
{ id: 'apps', label: t('nav.apps', 'Apps'), icon: Grid3X3 },
{ id: 'playlists', label: t('nav.playlists', 'Playlists'), icon: ListVideo },
{ id: 'templates', label: t('nav.templates', 'Templates'), icon: LayoutTemplate },
{ id: 'schedules', label: t('nav.schedules', 'Schedules'), icon: Calendar },
{ id: 'screens', label: t('nav.screens', 'Screens'), icon: Monitor },
```

**How the click handler is wired** (src/App.jsx lines 823-826, automatic):
```jsx
<button
  key={item.id}
  onClick={() => canAccess ? setCurrentPage(item.id) : null}
  ...
```
The `navigation.map()` renderer already calls `setCurrentPage(item.id)` on click — simply adding an entry to the array is sufficient; no additional click handler code is required.

**Page registration already exists** (src/App.jsx:556):
```jsx
'template-marketplace': <Suspense fallback={<PageLoader />}><TemplateMarketplacePage showToast={showToast} onNavigate={setCurrentPage} /></Suspense>,
```

**Icon import already exists** (src/App.jsx:45): `Store` from `lucide-react` — reuse this, do not add a new import.

**Access control:** Since the `item.feature` key is omitted, `isFeatureEnabled` is `true` by default (see line 773: `const isFeatureEnabled = !item.feature || featureFlags[item.feature];`). The marketplace page itself enforces any license gating server-side via `can_access_template()`. No plan-tier gating needed in the nav entry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Template Marketplace entry to sidebar navigation array</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (MUST read lines 457-479 to see the current navigation array shape; lines 44-48 to confirm `Store` icon is already imported from lucide-react; lines 823-829 to confirm the click handler is automatic via `navigation.map()`)
    - .planning/phases/166-template-quick-customize/166-HUMAN-UAT.md (reference — explains why this entry point is needed)
  </read_first>
  <behavior>
    - After the change, the sidebar (rendered by `navigation.map()` at src/App.jsx:767) shows a new item between "Templates" and "Schedules" labeled "Template Marketplace".
    - Clicking the new sidebar item calls the existing handler at line 826, which invokes `setCurrentPage('template-marketplace')`.
    - The `TemplateMarketplacePage` renders in place of the current page (already registered at src/App.jsx:556 — no router change needed).
    - The new item does NOT require `item.feature` — it is always visible to authenticated client users.
  </behavior>
  <action>
    Open `src/App.jsx`. Locate the `navigation` array which starts at line 457 and ends at line 479. Insert exactly ONE new entry immediately AFTER the existing `templates` entry (currently line 476) and BEFORE the `schedules` entry (currently line 477). The inserted line must read EXACTLY:

    ```js
        { id: 'template-marketplace', label: t('nav.templateMarketplace', 'Template Marketplace'), icon: Store },
    ```

    Use the `Store` icon already imported from `lucide-react` at line 45 — do NOT add a new import. Do NOT add a `feature:` key (the marketplace should be reachable by all authenticated client users; license gating happens server-side via `can_access_template()`). Do NOT add a `subItems` array — this is a flat entry following the pattern of `apps`, `playlists`, `templates`, `schedules`, `screens`. Do NOT modify any other line. Do NOT modify the `pages` object at line 556 — it already contains the `'template-marketplace'` key.

    After the edit, the navigation array around the insertion point must look like:
    ```js
        { id: 'playlists', label: t('nav.playlists', 'Playlists'), icon: ListVideo },
        { id: 'templates', label: t('nav.templates', 'Templates'), icon: LayoutTemplate },
        { id: 'template-marketplace', label: t('nav.templateMarketplace', 'Template Marketplace'), icon: Store },
        { id: 'schedules', label: t('nav.schedules', 'Schedules'), icon: Calendar },
        { id: 'screens', label: t('nav.screens', 'Screens'), icon: Monitor },
      ];
    ```
  </action>
  <verify>
    <automated>grep -n "id: 'template-marketplace', label: t('nav.templateMarketplace', 'Template Marketplace'), icon: Store" src/App.jsx &amp;&amp; grep -c "'template-marketplace'" src/App.jsx &amp;&amp; npx eslint src/App.jsx &amp;&amp; npx vitest run --reporter=dot 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'template-marketplace'" src/App.jsx` returns exactly `2` (the new nav entry + the existing pages router key at line 556).
    - `grep -n "id: 'template-marketplace', label: t('nav.templateMarketplace', 'Template Marketplace'), icon: Store" src/App.jsx` returns a match on exactly one line (the new line).
    - `grep -c "^  Store,$" src/App.jsx` returns exactly `1` — proves `Store` appears on exactly one line of the multi-line `lucide-react` import block (i.e., the existing import at line 45 was reused and no second import was added).
    - `npx eslint src/App.jsx` exits 0 (no new lint errors).
    - `npx vitest run --reporter=dot` passes all existing unit tests (no regression).
    - The inserted entry position is between `templates` and `schedules` — verified by: `grep -nE "id: 'templates'|id: 'template-marketplace'|id: 'schedules'" src/App.jsx | head -3` shows `templates` first, `template-marketplace` second, `schedules` third (line numbers increasing).
  </acceptance_criteria>
  <done>
    One new line added to the `navigation` array in `src/App.jsx` between the `templates` and `schedules` entries. No other file modified. Lint passes. Unit tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of marketplace navigation</name>
  <files>(none — verification only; no file changes in this task)</files>
  <read_first>
    - src/App.jsx (to confirm the nav entry added in Task 1 is still present before starting the dev server)
  </read_first>
  <what-built>
    A sidebar entry "Template Marketplace" (using the `Store` lucide icon) positioned between "Templates" and "Schedules" in the left sidebar. Clicking it navigates to the existing TemplateMarketplacePage at src/App.jsx:556.
  </what-built>
  <action>
    Pause autonomous execution. The human operator MUST manually execute the verification steps below in a real browser. The executor's role in this task is limited to: (1) starting the dev server with `npm run dev` in a background process (or instructing the user to start it), (2) presenting the verification steps from `how-to-verify` to the human, and (3) waiting for the human's resume-signal before proceeding. Do NOT mark this task complete based on automated checks alone — the success criterion explicitly requires a human visual confirmation that the sidebar entry is visible and clickable.
  </action>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173 in a browser.
    2. Log in as a client user (any seeded test account).
    3. Confirm a sidebar item labeled "Template Marketplace" with a store icon appears between "Templates" and "Schedules".
    4. Click the "Template Marketplace" sidebar item.
    5. Confirm the page content changes to the Template Marketplace page (heading / template cards / filters should be visible — note: the page may be empty if plan 02 has not yet been executed; that is acceptable for THIS checkpoint — we are only verifying that navigation works, NOT that templates display).
    6. Confirm the navigation did NOT require React DevTools fiber state injection.
    7. Click another sidebar item (e.g., "Templates"), then click "Template Marketplace" again — confirm navigation is consistent across re-entries.
  </how-to-verify>
  <verify>
    <automated>grep -n "id: 'template-marketplace', label: t('nav.templateMarketplace', 'Template Marketplace'), icon: Store" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar displays "Template Marketplace" between "Templates" and "Schedules".
    - Clicking the item changes the main content area to TemplateMarketplacePage without errors.
    - No console errors on click.
    - Navigation survives round-trip (click away, click back).
    - Human operator explicitly types "approved" as resume-signal.
  </acceptance_criteria>
  <done>
    Human operator has confirmed (via resume-signal "approved") that the sidebar entry appears, is clickable, and routes to the Template Marketplace page. If any of the 7 verification steps failed, the human must describe the failure and the executor must address it before marking done.
  </done>
  <resume-signal>Type "approved" if the sidebar entry appears and navigation works. If it does not, describe what you see (no sidebar item? click does nothing? error on page load?).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -&gt; React page router | User click triggers client-side state change; no new network surface |
| React page router -&gt; TemplateMarketplacePage | Page already enforces its own data-loading authz via Supabase RLS and `can_access_template()` — no new authz boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166.1-01 | Elevation of Privilege | navigation entry | accept | The new nav item does NOT bypass any authz: `TemplateMarketplacePage` and its underlying RPCs (`get_marketplace_templates`, `clone_template_to_scene`) already enforce authentication (`auth.uid()`) and license gating (`can_access_template()`) server-side via RLS. Adding a UI shortcut exposes nothing a logged-in client could not already reach by manipulating page state. |
| T-166.1-02 | Information Disclosure | i18n label | accept | The label string "Template Marketplace" is non-sensitive public product branding. No PII or secrets in the literal. |
| T-166.1-03 | Tampering | lucide icon import | mitigate | Reuse the existing `Store` import at src/App.jsx:45 — do not introduce a new dependency or icon library. Adding a new import would widen the supply-chain surface unnecessarily. |
</threat_model>

<verification>
- `grep -c "'template-marketplace'" src/App.jsx` returns `2`.
- `npx eslint src/App.jsx` exits 0.
- `npx vitest run` passes (no regression in existing unit tests).
- Human checkpoint passed: sidebar item renders and navigation works.
</verification>

<success_criteria>
- Success criterion #1 from ROADMAP: A visible UI entry point invokes `setCurrentPage('template-marketplace')` from the main app shell. Verified by grep that the entry exists in the navigation array, and by human click-through that it routes to the page.
- Success criterion #3 (partial): The first prereq (no nav entry point) is resolved. Criterion #3 fully closes after plan 02 also seeds `template_library`.
</success_criteria>

<output>
After completion, create `.planning/phases/166.1-marketplace-nav-and-seed/166.1-01-SUMMARY.md`.
</output>
