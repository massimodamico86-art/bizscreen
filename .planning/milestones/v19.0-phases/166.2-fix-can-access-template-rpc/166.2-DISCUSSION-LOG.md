# Phase 166.2: Fix `can_access_template` RPC - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 166.2-fix-can-access-template-rpc
**Areas discussed:** Fix strategy, Fix scope, Migration convention, Verification, Enterprise gating, Next-step boundary

---

## Fix Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Remove gating — treat pro as free | Simplest: drop the plan_tier lookup entirely. pro → accessible to all authenticated users. Unblocks UAT immediately. All 9 seeded templates are free so no current impact. | ✓ |
| Rewire via subscriptions → plans | Faithful to original intent: join subscriptions.plan_id to plans.slug, map slug to access. Preserves gating semantics. More SQL; no UI exists to manage plans today. | |
| Stub gating — pro blocked for all | Set v_user_plan := NULL. Only free accessible to regular users; pro/enterprise denied unless super_admin. Safe deny-by-default until a real plan system ships. | |
| Defer decision — fix Option (A) now, file follow-up | Ship Option A to unblock UAT, capture backlog phase to revisit when subscription UI exists. | |

**User's choice:** Remove gating — treat pro as free
**Notes:** See deferred idea in CONTEXT.md — plan-tier gating can be reintroduced via subscriptions/plans tables if/when billing UI exists.

---

## Fix Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Fix both functions in this migration | Redefine can_access_template AND clone_template_to_scene in migration 110. Prevents a second 42703 the moment a user clicks Install on a pro template. | ✓ |
| Fix can_access_template only (README scope) | Stay strictly within the README's stated blocker. Leave clone_template_to_scene broken (only fires on non-free templates; seeded rows are all free). | |

**User's choice:** Fix both functions in this migration
**Notes:** Same plan_tier bug exists in clone_template_to_scene at supabase/migrations/080_template_marketplace.sql:208 — would block Phase 166's "Apply & Create" UAT if not fixed here.

---

## Migration Convention

| Option | Description | Selected |
|--------|-------------|----------|
| New migration (e.g. 110_fix_can_access_template.sql) | Matches README's Implementation hint and Phase 166.1's pattern (seed was 109_*). Additive — preserves history, safe for already-migrated environments. Uses CREATE OR REPLACE FUNCTION. | ✓ |
| Edit 080_template_marketplace.sql in place | Fix at the definition site for a cleaner mental model. Risky in Supabase where 080 is already applied — edits don't re-run. | |

**User's choice:** New migration (e.g. 110_fix_can_access_template.sql)

---

## Verification

| Option | Description | Selected |
|--------|-------------|----------|
| SQL: call get_marketplace_templates() as auth user, assert 9 rows | Hit the RPC directly with an authenticated role. Fastest, deterministic, catches the exact error class. Success Criterion 2 from ROADMAP. | ✓ |
| SQL: call can_access_template(<uuid>) for free + super_admin paths | Unit-level check on the fixed function itself. Confirms it compiles and returns expected booleans. Success Criterion 1. | |
| Playwright smoke: Template Marketplace renders 9 cards | End-to-end: reuse the Phase 166.1 UI smoke harness that surfaced the bug. Higher signal but slower. | |
| Re-run `/gsd-verify-work 166` manual UAT | Resume the four paused Phase 166 checkpoints. Confirms the real goal. | |

**User's choice:** SQL: call get_marketplace_templates() as auth user, assert 9 rows (single-select answer from a multi-select question — treated as primary verification)
**Notes:** Planner may add the per-function smoke (option 2) as a cheap supplement — it's already implied by Success Criterion 1. Playwright smoke and Phase 166 UAT resumption are explicitly out of 166.2 scope (see "Next step" answer below).

---

## Enterprise Gating (follow-up)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep enterprise allow-list gating | Pro → accessible to all. Enterprise → still requires entry in template_enterprise_access (or super_admin). Preserves the only remaining access control. | ✓ |
| Remove all gating — everything accessible | Both pro and enterprise become accessible to all. Simplest function body. Loses the allow-list mechanism. | |

**User's choice:** Keep enterprise allow-list gating
**Notes:** Raised as a follow-up because "Remove gating" in the first question was phrased around `pro`; enterprise uses a separate mechanism (allow-list table, no plan_tier). Explicit retention documented in CONTEXT.md D-02.

---

## Next-Step Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 166.2 ends at SQL verification | Success Criterion 2 (9 rows returned) is the phase boundary. Resuming Phase 166 UAT is a manual follow-up via `/gsd-verify-work 166`. Keeps scope tight. | ✓ |
| Include Phase 166 UAT resumption in this phase | Expand scope: after migration applies, auto-advance back to Phase 166's four paused checkpoints. More deliverable per phase, but blurs the boundary. | |

**User's choice:** Phase 166.2 ends at SQL verification

---

## Claude's Discretion

- Exact SQL body of the rewritten functions (signatures and behavior are locked by decisions above).
- Whether to drop `v_user_plan` variable entirely vs. leave a NULL stub.
- Whether to re-issue `GRANT EXECUTE` after `CREATE OR REPLACE` (safe either way).
- File-level plan concerns (commit message conventions, whether `supabase db push` runs in execute-phase).

## Deferred Ideas

- Restore plan-tier gating for `pro` templates if/when a subscription/billing UI ships — route via `/gsd-add-backlog`.
- Second-pass simplification of `clone_template_to_scene` to call `can_access_template` internally (out of scope to keep diff surgical).
