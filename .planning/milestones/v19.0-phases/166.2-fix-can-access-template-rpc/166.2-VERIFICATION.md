---
phase: 166.2-fix-can-access-template-rpc
verified: 2026-04-12T00:00:00Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Load Template Marketplace page in browser as authenticated non-super-admin user"
    expected: "Page lists 9 templates (the seeded dev-166.1 rows), no 'HTTP 400' or '0 templates found' error displayed"
    why_human: "Cannot drive a browser session programmatically in this environment; PostgREST → Supabase auth header chain cannot be fully simulated without a live browser/Playwright session"
  - test: "Attempt Apply & Create from the Template Marketplace page for any seeded template"
    expected: "clone_template_to_scene() succeeds, new scene is created, user is redirected to scene editor — no 'Access denied' or HTTP 400 error"
    why_human: "Requires authenticated browser session performing a mutating RPC call; cannot verify the full PostgREST round-trip without running the app"
---

# Phase 166.2: Fix can_access_template RPC — Verification Report

**Phase Goal:** `get_marketplace_templates` RPC returns rows for authenticated client users so the Template Marketplace page actually lists seeded templates and Phase 166 UAT can proceed.
**Verified:** 2026-04-12
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Migration 110 file exists at supabase/migrations/110_fix_can_access_template.sql | VERIFIED | File present; committed in `60ee5c26` |
| 2 | Migration 110 has been applied to the local Supabase database | VERIFIED | `pg_proc` shows both functions with updated bodies; live DB query returns 9 rows |
| 3 | An authenticated non-super-admin session can call get_marketplace_templates() without SQL error | VERIFIED | Live D-05 re-run: exit 0, no 42703 raised, `row_count = 9` |
| 4 | get_marketplace_templates() returns exactly 9 rows for an authenticated non-super-admin | VERIFIED | `SELECT count(*) FROM public.get_marketplace_templates()` = 9 |
| 5 | Every row returned by get_marketplace_templates() has can_access = true | VERIFIED | `SELECT bool_and(can_access) FROM public.get_marketplace_templates()` = t |
| 6 | Enterprise gating path is preserved: can_access_template body still references template_enterprise_access | VERIFIED | Migration file contains `template_enterprise_access` in 3 locations; live DB function body confirmed no plan_tier (0 matches) |
| 7 | Super-admin bypass is preserved: can_access_template body still references role = 'super_admin' | VERIFIED | Migration file contains `super_admin` in 4 locations; live DB `pg_proc` body confirmed |
| 8 | SECURITY DEFINER + SET search_path = public attributes are preserved on both redefined functions | VERIFIED | `pg_proc` query: `prosecdef = t`, `proconfig = {search_path=public}` for both functions |
| 9 | Neither redefined function references profiles.plan_tier or the v_user_plan variable | VERIFIED | `grep -c "plan_tier"` = 0 in migration file; `grep -c "v_user_plan"` = 0 in migration file; live DB body grep returns 0 for both functions |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/110_fix_can_access_template.sql` | Additive migration redefining both functions without plan_tier reference | VERIFIED | File exists, 196 lines, all required patterns present, all forbidden patterns absent |

**Content checks (supabase/migrations/110_fix_can_access_template.sql):**

| Pattern | Required | Found |
|---------|----------|-------|
| `CREATE OR REPLACE FUNCTION public.can_access_template` | must contain | 1 match |
| `CREATE OR REPLACE FUNCTION public.clone_template_to_scene` | must contain | 1 match |
| `SECURITY DEFINER` | must contain | 2 matches |
| `SET search_path = public` | must contain | 2 matches |
| `template_enterprise_access` | must contain | 3 matches |
| `super_admin` | must contain | 4 matches |
| `GRANT EXECUTE ON FUNCTION public.can_access_template(uuid) TO authenticated` | must contain | 1 match |
| `GRANT EXECUTE ON FUNCTION public.clone_template_to_scene(uuid, text) TO authenticated` | must contain | 1 match |
| `plan_tier` | must NOT contain | 0 matches (PASS) |
| `v_user_plan` | must NOT contain | 0 matches (PASS) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `080_template_marketplace.sql` (get_marketplace_templates body) | `public.can_access_template(uuid)` (redefined by migration 110) | `can_access_template(tl.id) AS can_access` at line 399 | WIRED | Live call confirmed: 9 rows returned with `can_access = true`; call site unchanged in 080 (git diff clean) |
| `src/pages/TemplateMarketplacePage.jsx` / `src/services/marketplaceService.js` | PostgREST RPC `/rest/v1/rpc/get_marketplace_templates` | `supabase.rpc('get_marketplace_templates', ...)` | WIRED | `marketplaceService.js:62` contains the RPC call; `TemplateMarketplacePage.jsx` imports from `marketplaceService` |
| `can_access_template` body (enterprise branch) | `public.template_enterprise_access` | `EXISTS (SELECT 1 FROM template_enterprise_access WHERE template_id = ... AND tenant_id = ...)` | WIRED | Pattern present in migration file (3 occurrences); same pattern confirmed in live DB function body |

### Data-Flow Trace (Level 4)

This phase is database-layer only (SQL migration). No React component renders dynamic data from this phase's artifacts directly — `TemplateMarketplacePage.jsx` already existed and wired to `marketplaceService.js` prior to this phase. The fix restores the data pipeline that was broken by the 42703 error; data flow is verified end-to-end at the SQL layer by the live D-05 re-run.

| Layer | Status | Evidence |
|-------|--------|----------|
| DB function → query result | FLOWING | `count(*) = 9`, `bool_and(can_access) = true` from live DB |
| Service → RPC | WIRED | `marketplaceService.js:62` calls `supabase.rpc('get_marketplace_templates')` |
| Page → Service | WIRED | `TemplateMarketplacePage.jsx` imports and uses `marketplaceService` |
| Browser render | NEEDS HUMAN | Cannot drive browser without Playwright/live session |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| get_marketplace_templates() returns 9 rows as authenticated user | `docker exec psql ... SELECT count(*) FROM public.get_marketplace_templates()` (with JWT claim set) | `9` | PASS |
| All returned rows have can_access = true | `docker exec psql ... SELECT bool_and(can_access) FROM public.get_marketplace_templates()` | `t` | PASS |
| Live DB: can_access_template has SECURITY DEFINER | `pg_proc` query: `prosecdef` column | `t` for both functions | PASS |
| Live DB: search_path preserved | `pg_proc` query: `proconfig` column | `{search_path=public}` for both functions | PASS |
| Live DB: plan_tier absent from function bodies | `grep` on `pg_proc.prosrc` | `0` matches | PASS |
| EXECUTE grant active for authenticated role | `information_schema.routine_privileges` query | Both functions: `authenticated / EXECUTE` confirmed | PASS |
| Migration 080 unmodified | `git diff --stat supabase/migrations/080_template_marketplace.sql` | Empty (no changes) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| `phase-166.2-uat-blocker` | 166.2-01-PLAN.md | Phase-internal tracking ID for the UAT blocker routed from Phase 166.1. Not a v19.0 REQUIREMENTS.md entry. | SATISFIED | The UAT blocker (HTTP 400 from `get_marketplace_templates`) is resolved: live DB returns 9 rows with no error. The requirement is not in REQUIREMENTS.md because it is an infrastructure prerequisite, not a user-facing v19.0 requirement. CONT-01 (the upstream user requirement) remains mapped to Phase 166 and is unaffected. |
| `CONT-01` (upstream, Phase 166) | Phase 166 (parent) | User can quick-customize a template without entering the full editor | UNBLOCKED by this phase — not satisfied here | Phase 166.2 removes the DB-level blocker; CONT-01 satisfaction still requires Phase 166 UAT completion |

**Note on requirement traceability:** The PLAN frontmatter declares `requirements: [phase-166.2-uat-blocker]`. This ID does not appear in `REQUIREMENTS.md` because it is a phase-internal UAT blocker label, not a v19.0 milestone requirement. `REQUIREMENTS.md` contains no orphaned requirement IDs assigned to Phase 166.2. This is consistent and correct — Phase 166.2 is a sub-phase infrastructure fix, not a new user requirement.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `supabase/migrations/110_fix_can_access_template.sql:143` | 143 | Error message `'Access denied: insufficient plan tier for this template'` references "plan tier" conceptually (legacy string, not a column reference) | Info | No functional impact — this is a user-facing exception message, not a schema reference. The `plan_tier` column is not referenced. Cosmetic cleanup could rename to "insufficient access" in a later pass. |

No blocker or warning anti-patterns found.

### Human Verification Required

#### 1. Template Marketplace Page Renders 9 Templates

**Test:** Log in as a non-super-admin user and navigate to `/templates` (or equivalent Template Marketplace route).
**Expected:** Page displays 9 template cards — Campus Event Announcement, Clinic Hours Display, Conference Keynote Banner, Corporate Welcome, Daily Menu Board, Empty Template (Fallback Test), Guest Welcome Board, Gym Class Schedule, Retail Sale Promo. No "0 templates found" message, no HTTP 400 error in the browser console.
**Why human:** Requires an authenticated browser session through the full PostgREST → Supabase auth JWT pipeline. The SQL layer is verified; the browser rendering of the service response cannot be confirmed without running the app.

#### 2. Apply & Create (clone_template_to_scene) Succeeds

**Test:** From the Template Marketplace page, select any seeded template and click "Apply & Create" (or equivalent action that calls `clone_template_to_scene`).
**Expected:** A new scene is created successfully, user is redirected to the scene editor, no error toast or HTTP error in console.
**Why human:** `clone_template_to_scene` is a mutating RPC requiring an authenticated browser session. The function body is verified at the SQL layer; the full round-trip through the UI requires Playwright or manual testing.

---

### Gaps Summary

No gaps. All 9 must-have truths are verified against the codebase and live database. The two human verification items above are standard UAT confirmations of the browser/UI layer — the SQL infrastructure is solid.

---

_Verified: 2026-04-12_
_Verifier: Claude (gsd-verifier)_
