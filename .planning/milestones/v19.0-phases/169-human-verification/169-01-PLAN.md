---
phase: 169-human-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/nav-accessibility-onboarding.spec.js
  - tests/e2e/admin-settings-branding-security.spec.js
  - tests/e2e/enterprise-sso.spec.js
  - tests/e2e/enterprise-api.spec.js
  - tests/e2e/enterprise-analytics.spec.js
  - tests/e2e/player-rendering.spec.js
  - tests/e2e/player-offline-selfheal.spec.js
  - tests/e2e/player-telemetry.spec.js
  - .planning/phases/169-human-verification/169-BASELINE.md
autonomous: true
requirements:
  - HVER-01
  - HVER-02
  - HVER-03
  - HVER-04
  - HVER-05
requirements_addressed:
  - HVER-01
  - HVER-02
  - HVER-03
  - HVER-04
  - HVER-05

must_haves:
  truths:
    - "All 8 spec files required by HVER-01..HVER-05 exist on disk (restored from commit d0028db6)"
    - "Playwright can parse all 8 restored spec files without syntax or import errors"
    - "A BASELINE.md captures current pass/fail/skip status for each restored spec and the pre-existing enterprise.spec.js"
    - "Downstream Wave 2 plans (169-02, 169-03) have the target spec files available on disk to verify against"
  artifacts:
    - path: "tests/e2e/nav-accessibility-onboarding.spec.js"
      provides: "NAVX-08/09/10 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 390
    - path: "tests/e2e/admin-settings-branding-security.spec.js"
      provides: "ADMN-02/03/06 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 140
    - path: "tests/e2e/enterprise-sso.spec.js"
      provides: "ENTR-01 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 65
    - path: "tests/e2e/enterprise-api.spec.js"
      provides: "ENTR-02/03 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 80
    - path: "tests/e2e/enterprise-analytics.spec.js"
      provides: "ENTR-04/05 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 105
    - path: "tests/e2e/player-rendering.spec.js"
      provides: "PLYR-01/02 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 270
    - path: "tests/e2e/player-offline-selfheal.spec.js"
      provides: "PLYR-03/04 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 225
    - path: "tests/e2e/player-telemetry.spec.js"
      provides: "PLYR-05/06 E2E coverage (restored verbatim from d0028db6)"
      min_lines: 230
    - path: ".planning/phases/169-human-verification/169-BASELINE.md"
      provides: "Baseline pass/fail/skip results per spec, recording the starting state before Wave 2 verification work"
      contains: "## Baseline Test Results"
  key_links:
    - from: "tests/e2e/nav-accessibility-onboarding.spec.js"
      to: "tests/e2e/helpers.js AND tests/e2e/fixtures/index.js"
      via: "import loginAndPrepare + test/expect"
      pattern: "from './helpers"
    - from: "tests/e2e/enterprise-sso.spec.js"
      to: "process.env.TEST_ENTERPRISE_EMAIL"
      via: "test.skip guard"
      pattern: "TEST_ENTERPRISE_EMAIL"
---

<objective>
Restore the 8 E2E spec files that Wave 2 HVER verification plans need but which were deleted from main by commit 05a7f89d (Phase 165 worktree merge data loss) and never fully restored. These are the canonical specs for NAVX-09, ADMN-02/03, ENTR-01..05, and PLYR-01..06 that Phase 162 originally brought back and Phase 168 Wave 1 only partially re-restored.

After restoration, run each spec once to capture a baseline pass/fail/skip status in `169-BASELINE.md`. This baseline is the evidence Wave 2 plans use to decide: (a) the test already passes — just document and move on, (b) the test fails and needs investigation, or (c) the test is blocked on credentials/infrastructure — wire .env or document the gap.

This plan is strictly restore + baseline. It does NOT modify spec contents, does NOT rewrite assertions, does NOT fix flakiness. Any real HVER verification/fix work happens in Wave 2.

Purpose: Unblock the downstream verification plans. Without the spec files on disk, HVER-01..HVER-05 are literally unverifiable.
Output: 8 restored spec files (all content byte-identical to commit d0028db6) + 169-BASELINE.md documenting initial status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v19.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v18.0-phases/162-restore-deleted-spec-files/162-VERIFICATION.md

# Phase 168 Wave 1 demonstrated this exact restore pattern for 6 files.
# We replicate that pattern for 8 more files here.
@.planning/phases/168-test-doc-quality-cleanup/168-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore 8 deleted E2E spec files from commit d0028db6</name>
  <files>
    tests/e2e/nav-accessibility-onboarding.spec.js,
    tests/e2e/admin-settings-branding-security.spec.js,
    tests/e2e/enterprise-sso.spec.js,
    tests/e2e/enterprise-api.spec.js,
    tests/e2e/enterprise-analytics.spec.js,
    tests/e2e/player-rendering.spec.js,
    tests/e2e/player-offline-selfheal.spec.js,
    tests/e2e/player-telemetry.spec.js
  </files>
  <read_first>
    - .planning/v19.0-MILESTONE-AUDIT.md (context: 5 HVER items pending, Phase 169 not yet created)
    - .planning/phases/168-test-doc-quality-cleanup/168-01-PLAN.md (pattern: same restore-from-SHA approach)
    - .planning/milestones/v18.0-phases/162-restore-deleted-spec-files/162-VERIFICATION.md (source: all 8 files originally restored in commit d0028db6 with these exact filenames and line counts)
    - Run: `git log --oneline -- tests/e2e/nav-accessibility-onboarding.spec.js` to confirm d0028db6 is the canonical restore commit
  </read_first>
  <action>
    Restore each file from commit d0028db6 (feat(162-01): batch restore) using git checkout. This is verbatim byte-for-byte restoration — do NOT edit, refactor, modernize, or "improve" any spec content. The goal is to get the Phase 162 artifact back on disk exactly as it was validated in 162-VERIFICATION.md.

    Run each command individually so any single-file failure is easy to diagnose:

    ```
    git checkout d0028db6 -- tests/e2e/nav-accessibility-onboarding.spec.js
    git checkout d0028db6 -- tests/e2e/admin-settings-branding-security.spec.js
    git checkout d0028db6 -- tests/e2e/enterprise-sso.spec.js
    git checkout d0028db6 -- tests/e2e/enterprise-api.spec.js
    git checkout d0028db6 -- tests/e2e/enterprise-analytics.spec.js
    git checkout d0028db6 -- tests/e2e/player-rendering.spec.js
    git checkout d0028db6 -- tests/e2e/player-offline-selfheal.spec.js
    git checkout d0028db6 -- tests/e2e/player-telemetry.spec.js
    ```

    After restoration, confirm Playwright can parse all 8 files with no import errors. The helpers/fixtures these specs depend on (helpers.js, helpers/index.js, fixtures/index.js, helpers/screenshots.js) were already restored by Phase 168 Plan 168-01 and are still on disk — verify this first with `ls tests/e2e/helpers.js tests/e2e/fixtures/index.js tests/e2e/helpers/index.js tests/e2e/helpers/screenshots.js` so any missing dependency surfaces before the Playwright parse run.

    Commit as a single atomic commit with message: `feat(169-01): restore 8 E2E spec files from d0028db6 (HVER unblocker)` — do not mix this commit with the baseline doc in Task 2.

    Do NOT attempt to run the full suite or fix test logic in this task. Pure file restoration + parse check only.
  </action>
  <verify>
    <automated>
      test -f tests/e2e/nav-accessibility-onboarding.spec.js &&
      test -f tests/e2e/admin-settings-branding-security.spec.js &&
      test -f tests/e2e/enterprise-sso.spec.js &&
      test -f tests/e2e/enterprise-api.spec.js &&
      test -f tests/e2e/enterprise-analytics.spec.js &&
      test -f tests/e2e/player-rendering.spec.js &&
      test -f tests/e2e/player-offline-selfheal.spec.js &&
      test -f tests/e2e/player-telemetry.spec.js &&
      npx playwright test --list tests/e2e/nav-accessibility-onboarding.spec.js tests/e2e/admin-settings-branding-security.spec.js tests/e2e/enterprise-sso.spec.js tests/e2e/enterprise-api.spec.js tests/e2e/enterprise-analytics.spec.js tests/e2e/player-rendering.spec.js tests/e2e/player-offline-selfheal.spec.js tests/e2e/player-telemetry.spec.js 2>&1 | grep -iE "SyntaxError|Cannot find module|does not provide an export|Unexpected token|ReferenceError" ; [ $? -ne 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist at the paths listed in <files>
    - `wc -l tests/e2e/nav-accessibility-onboarding.spec.js` reports >= 390 lines
    - `wc -l tests/e2e/admin-settings-branding-security.spec.js` reports >= 140 lines
    - `wc -l tests/e2e/enterprise-sso.spec.js` reports >= 65 lines
    - `wc -l tests/e2e/enterprise-api.spec.js` reports >= 80 lines
    - `wc -l tests/e2e/enterprise-analytics.spec.js` reports >= 105 lines
    - `wc -l tests/e2e/player-rendering.spec.js` reports >= 270 lines
    - `wc -l tests/e2e/player-offline-selfheal.spec.js` reports >= 225 lines
    - `wc -l tests/e2e/player-telemetry.spec.js` reports >= 230 lines
    - `npx playwright test --list <the 8 files>` produces zero lines matching `SyntaxError|Cannot find module|does not provide an export|Unexpected token|ReferenceError` (W4: narrowed from generic "Error" to avoid matching benign `describe('error state')` strings in the specs themselves)
    - Git diff for this commit shows only additions (A) for the 8 files, no modifications to helpers.js/fixtures/index.js/etc.
  </acceptance_criteria>
  <done>
    All 8 spec files are on disk, byte-identical to their content at commit d0028db6, and Playwright can enumerate their tests with `--list` without parse errors. A single atomic commit `feat(169-01): restore 8 E2E spec files from d0028db6 (HVER unblocker)` exists on main.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture baseline pass/fail/skip status per spec</name>
  <files>
    .planning/phases/169-human-verification/169-BASELINE.md
  </files>
  <read_first>
    - tests/e2e/nav-accessibility-onboarding.spec.js (just restored — to see describe block names and which NAVX IDs are inside)
    - tests/e2e/admin-settings-branding-security.spec.js (to see which ADMN IDs are inside and what credentials each requires — NOTE: the restored spec at d0028db6 gates every describe block on `!process.env.TEST_USER_EMAIL`, not TEST_CLIENT_EMAIL or TEST_ADMIN_EMAIL)
    - tests/e2e/enterprise-sso.spec.js, enterprise-api.spec.js, enterprise-analytics.spec.js (to see TEST_ENTERPRISE_EMAIL usage pattern)
    - tests/e2e/player-rendering.spec.js, player-offline-selfheal.spec.js, player-telemetry.spec.js (to see player/pair page routes used)
    - tests/e2e/enterprise.spec.js (pre-existing file that already uses TEST_ENTERPRISE_EMAIL — relevant to HVER-04 baseline too)
    - playwright.config.js (to confirm chromium-only project; retries=0 for local)
  </read_first>
  <action>
    Run each restored spec once on chromium locally with default env (NO TEST_ENTERPRISE_EMAIL set) and capture the outcome. Also run the pre-existing `tests/e2e/enterprise.spec.js` since it is the spec HVER-04 is ultimately validating (the restored `enterprise-*` specs complement but do not replace it).

    Add a per-spec `--timeout=30000` flag (I9) so a hung spec cannot block the whole baseline run.

    Commands (run them one at a time; capture stdout+stderr for each; record per-test outcome counts):

    ```
    npx playwright test tests/e2e/nav-accessibility-onboarding.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/enterprise-sso.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/enterprise-api.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/enterprise-analytics.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/enterprise.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/player-rendering.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/player-offline-selfheal.spec.js --project=chromium --reporter=list --timeout=30000
    npx playwright test tests/e2e/player-telemetry.spec.js --project=chromium --reporter=list --timeout=30000
    ```

    Dev server must be running (`npm run dev` per playwright.config.js webServer — `reuseExistingServer: true` is already set). If a spec consistently errors before a single test runs (e.g., dev server not reachable, Supabase down, missing migration), record that as `BLOCKED-INFRA` in the baseline and move on — do not attempt infra fixes here; surface them so Wave 2 can decide.

    Write the results to `.planning/phases/169-human-verification/169-BASELINE.md` with this exact structure:

    ```markdown
    ---
    phase: 169
    plan: 01
    purpose: Baseline HVER test state before Wave 2 verification
    captured_at: <ISO-8601 timestamp>
    env_snapshot:
      TEST_USER_EMAIL: <present|absent>   # the canonical guard used by the restored admin-settings spec
      TEST_CLIENT_EMAIL: <present|absent>
      TEST_ADMIN_EMAIL: <present|absent>
      TEST_SUPERADMIN_EMAIL: <present|absent>
      TEST_ENTERPRISE_EMAIL: absent (expected — HVER-04 scope)
    ---

    # Phase 169 Baseline Test Results

    ## Baseline Test Results

    | HVER | Spec | Tests | Passed | Failed | Skipped | Notes |
    |------|------|-------|--------|--------|---------|-------|
    | HVER-01 | nav-accessibility-onboarding.spec.js | N | P | F | S | <e.g. "NAVX-09 mobile-menu-button test FAILED: aria-expanded not found"> |
    | HVER-02 | admin-settings-branding-security.spec.js (ADMN-02 subset) | ... | ... | ... | ... | Skip guard is `!process.env.TEST_USER_EMAIL` |
    | HVER-03 | admin-settings-branding-security.spec.js (ADMN-03 subset) | ... | ... | ... | ... | Skip guard is `!process.env.TEST_USER_EMAIL`; NOTE: restored spec asserts VISIBILITY only, not persistence |
    | HVER-04 | enterprise.spec.js | ... | ... | ... | ... | All tests expected to SKIP because TEST_ENTERPRISE_EMAIL unset |
    | HVER-04 | enterprise-sso.spec.js | ... | ... | ... | ... | ... |
    | HVER-04 | enterprise-api.spec.js | ... | ... | ... | ... | ... |
    | HVER-04 | enterprise-analytics.spec.js | ... | ... | ... | ... | ... |
    | HVER-05 | player-rendering.spec.js | ... | ... | ... | ... | ... |
    | HVER-05 | player-offline-selfheal.spec.js | ... | ... | ... | ... | ... |
    | HVER-05 | player-telemetry.spec.js | ... | ... | ... | ... | ... |

    ## Per-spec verbatim output

    ### nav-accessibility-onboarding.spec.js
    ```
    <last ~40 lines of playwright stdout for this spec>
    ```

    <repeat for each spec>

    ## Wave 2 Handoff Notes

    - HVER-01 next step: <one of: "test passes — document only" | "test fails — investigate NAVX-09 assertion" | "blocked: <reason>">
    - HVER-02 next step: ...
    - HVER-03 next step: ...
    - HVER-04 next step: "wire TEST_ENTERPRISE_EMAIL in .env.example + confirm with real creds"
    - HVER-05 next step: <based on observed stability>
    ```

    Do NOT fix failing tests in this task. Record truthfully. A failed or skipped test here is valid input, not a defect.

    Commit as: `docs(169-01): baseline HVER test state (pre-verification snapshot)`.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "^## Baseline Test Results" .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "HVER-01" .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "HVER-02" .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "HVER-03" .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "HVER-04" .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "HVER-05" .planning/phases/169-human-verification/169-BASELINE.md &&
      grep -q "Wave 2 Handoff Notes" .planning/phases/169-human-verification/169-BASELINE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/169-human-verification/169-BASELINE.md` exists
    - The file contains a `## Baseline Test Results` table with exactly 10 rows (one per HVER × spec pairing as listed in the action)
    - Each row has integer values for `Tests / Passed / Failed / Skipped` columns (or explicit `BLOCKED-INFRA` with reason)
    - The file contains a `## Per-spec verbatim output` section with a fenced code block per spec containing the tail of the Playwright run
    - The file contains a `## Wave 2 Handoff Notes` section with one bullet per HVER-01..HVER-05
    - `grep -c "HVER-0" .planning/phases/169-human-verification/169-BASELINE.md` returns >= 10 (the 5 IDs appear multiple times across table + handoff)
  </acceptance_criteria>
  <done>
    169-BASELINE.md exists, contains the results table for all 10 spec runs (including pre-existing enterprise.spec.js), verbatim output tails, and Wave 2 handoff guidance. Committed as `docs(169-01): baseline HVER test state (pre-verification snapshot)`. Wave 2 plans can read this file and know exactly what each HVER item's starting state is.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — no new production surface | This plan only restores test spec files (read-only at runtime in CI/local) and writes a planning doc. No API endpoints, no user input paths, no auth boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-01-01 | Information Disclosure | Restored specs could contain hardcoded test credentials | accept | All 8 specs use `process.env.TEST_*_EMAIL` guards — no hardcoded secrets. Verified by grep before commit. |
| T-169-01-02 | Tampering | Someone could alter spec contents during restore | mitigate | Use `git checkout d0028db6 -- <file>` (verbatim from trusted SHA), not manual reconstruction. Verify via `git diff d0028db6 -- <file>` = empty before committing. |
</threat_model>

<verification>
After both tasks complete:
1. `git log --oneline -2` shows the two commits in order (restore first, baseline second)
2. `git diff d0028db6 HEAD -- tests/e2e/nav-accessibility-onboarding.spec.js` is empty (byte-identical restore)
3. `ls tests/e2e/*.spec.js | wc -l` is >= (previous count + 8)
4. `.planning/phases/169-human-verification/169-BASELINE.md` exists and passes the acceptance_criteria grep checks for Task 2
</verification>

<success_criteria>
1. All 8 spec files required by HVER-01..HVER-05 are present on main and byte-identical to their d0028db6 versions
2. Playwright can parse all 8 restored specs without import or syntax errors
3. 169-BASELINE.md provides Wave 2 with a table of current pass/fail/skip counts per spec and per-HVER handoff notes
4. No existing helper or fixture file is modified (all dependencies already on disk from Phase 168 Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/169-human-verification/169-01-SUMMARY.md` summarizing:
- Which files were restored and at what SHA
- Baseline results table (copy from 169-BASELINE.md)
- Any infra blockers discovered that Wave 2 needs to know about
- Explicit confirmation that no spec content was modified during restore
</output>
</output>
