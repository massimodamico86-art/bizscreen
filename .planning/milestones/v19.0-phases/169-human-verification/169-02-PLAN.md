---
phase: 169-human-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 169-01
files_modified:
  - tests/e2e/nav-accessibility-onboarding.spec.js
  - tests/e2e/admin-settings-branding-security.spec.js
  - .planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md
  - .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md
autonomous: false
requirements:
  - HVER-01
  - HVER-02
  - HVER-03
requirements_addressed:
  - HVER-01
  - HVER-02
  - HVER-03

must_haves:
  truths:
    - "NAVX-09 mobile nav ARIA expectations are documented against actual browser behavior in 169-NAVX-09-ARIA-FINDINGS.md"
    - "nav-accessibility-onboarding.spec.js contains an NAVX-09 test whose assertions match the documented ARIA findings (no aspirational assertions left in code)"
    - "admin-settings-branding-security.spec.js ADMN-02 describe block (VISIBILITY ONLY per the restored spec at d0028db6: 'brand themes section visible', 'import brand button visible', 'brand importer modal opens') passes locally on chromium with TEST_USER_EMAIL set — 3 consecutive runs, at least 1 test executes (NOT all-skipped)"
    - "admin-settings-branding-security.spec.js ADMN-03 describe block (VISIBILITY ONLY per the restored spec at d0028db6: 'account security section visible', 'session management visible', 'login history visible') passes locally on chromium with TEST_USER_EMAIL set — 3 consecutive runs, at least 1 test executes (NOT all-skipped)"
    - "Persistence behavior for HVER-02 (branding) and HVER-03 (security) is proven by the HUMAN round-trip in Task 3, because the restored automated spec at d0028db6 asserts visibility only — NOT persistence. This distinction is documented in 169-ADMN-PERSISTENCE-EVIDENCE.md so future auditors do not mistake the automated runs for persistence proof."
    - "169-ADMN-PERSISTENCE-EVIDENCE.md records three successive pass runs for ADMN-02 and ADMN-03 (with executed-test counts from JSON reporter proving skip-all cannot masquerade as pass) plus human-confirmation bullets from the checkpoint"
  artifacts:
    - path: ".planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md"
      provides: "Documented mobile nav ARIA expectations vs. observed browser behavior (the 'documented in the spec' half of HVER-01)"
      contains: "## Expected ARIA Behavior"
    - path: ".planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md"
      provides: "3-run evidence log proving ADMN-02 + ADMN-03 visibility assertions pass deterministically, PLUS human round-trip proving actual persistence (HVER-02, HVER-03)"
      contains: "## Run 1"
    - path: "tests/e2e/nav-accessibility-onboarding.spec.js"
      provides: "NAVX-09 test assertions aligned to documented findings"
      contains: "aria-expanded"
    - path: "tests/e2e/admin-settings-branding-security.spec.js"
      provides: "ADMN-02 + ADMN-03 visibility tests passing without manual intervention"
      contains: "ADMN-02"
  key_links:
    - from: ".planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md"
      to: "tests/e2e/nav-accessibility-onboarding.spec.js"
      via: "Findings doc cites exact line numbers of NAVX-09 assertions, and spec lines reference the findings doc in a comment"
      pattern: "169-NAVX-09-ARIA-FINDINGS"
---

<objective>
Close HVER-01 (NAVX-09 mobile nav ARIA), HVER-02 (ADMN-02 branding persistence), and HVER-03 (ADMN-03 security persistence). All three are "run the restored spec against real browser behavior, reconcile the assertions to observed behavior, and record evidence." This plan does exactly that — it does NOT add new product features and does NOT rewrite the admin/branding UI.

**IMPORTANT SCOPE NOTE (from checker revision B2):** The restored `admin-settings-branding-security.spec.js` at d0028db6 was inspected line-by-line. Both ADMN-02 and ADMN-03 describe blocks assert **visibility only**:
- ADMN-02 (lines 16-46): "brand themes section visible", "import brand button visible", "can open brand importer modal" — NO upload/reload/persistence assertion.
- ADMN-03 (lines 48-75): "account security section visible", "session management visible", "login history visible" — NO toggle/reload/persistence assertion.
- (ADMN-06, notifications, IS a real persistence test — but it is out of scope here; HVER-02/03 are the branding and security requirements specifically.)

Therefore: the automated runs in Task 2 are **visibility regression proof**, and **persistence for HVER-02 and HVER-03 is proven only by the human round-trip in Task 3**. This distinction is called out explicitly in the evidence doc so no one mistakes 3 green runs for persistence evidence.

Scope boundary:
- If a test fails because the product has a genuine bug, this plan DOCUMENTS the bug, marks the assertion with `test.fixme()`, and escalates — it does NOT fix the product bug (that would be a new feature-build phase).
- If a test fails because the spec's assertion was aspirational (assumed ARIA attribute that the app doesn't actually emit), this plan UPDATES the assertion to match observed reality and records the decision in 169-NAVX-09-ARIA-FINDINGS.md.

Purpose: Prove NAVX-09/ADMN-02/ADMN-03 are confirmed and documented.
Output: Findings doc + persistence evidence doc (with explicit visibility-vs-persistence call-out) + spec assertions that match observed behavior + human verification of the actual UI round-trips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v19.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/phases/169-human-verification/169-BASELINE.md

# Prior ADMN-02/03 spec work
@.planning/milestones/v18.0-phases/162-restore-deleted-spec-files/162-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HVER-01 — Verify NAVX-09 mobile nav ARIA and document findings</name>
  <files>
    tests/e2e/nav-accessibility-onboarding.spec.js,
    .planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md
  </files>
  <read_first>
    - tests/e2e/nav-accessibility-onboarding.spec.js (the NAVX-09 describe block — review every assertion)
    - .planning/phases/169-human-verification/169-BASELINE.md (Wave 2 Handoff Notes for HVER-01 — tells you whether the test currently passes or fails)
    - src/components/Navigation.jsx, src/components/Sidebar.jsx, or whichever component renders the mobile hamburger (grep `src/` for `aria-expanded` and `aria-label` within navigation components)
    - Run: `grep -rn "aria-expanded\|aria-controls\|aria-label" src/components/ | grep -i "nav\|menu\|sidebar\|drawer"` to locate the actual ARIA attributes the app emits
  </read_first>
  <action>
    Step 0 (W6 pre-check) — before any ARIA assertion work, confirm a mobile nav / hamburger component actually exists in the codebase:
    ```
    ls src/components/ 2>/dev/null | grep -iE "mobile|hamburger|drawer"
    grep -rl "aria-expanded" src/ 2>/dev/null | head
    grep -rl "hamburger\|mobile.*nav\|MobileNav" src/ 2>/dev/null | head
    ```
    If NOTHING matches (no mobile nav component exists in the codebase), NAVX-09 is aspirational. Hard-route to the escalation branch below: mark the NAVX-09 tests with `test.fixme(true, 'NAVX-09 gap: no mobile nav component present in codebase; aria-* expectations cannot be tested')`, record the gap in 169-NAVX-09-ARIA-FINDINGS.md under `## Gaps Escalated`, and skip Step 1 reconciliation — there is nothing to reconcile against.

    Step 1 — Reconcile spec to reality (only if Step 0 found a component):
    Run `npx playwright test tests/e2e/nav-accessibility-onboarding.spec.js --project=chromium --reporter=list --grep="NAVX-09"` at 375px viewport (mobile). For each failing assertion in the NAVX-09 block, determine the cause:
      (a) Spec asserts `aria-expanded` but the app emits `aria-pressed` or has no attribute at all → update the assertion to match what the app emits. Record the observation in the findings doc (see Step 2).
      (b) App genuinely does not meet accessibility requirements (e.g., no accessible name on the hamburger button) → keep the assertion as-is, wrap the test in `test.fixme(true, 'NAVX-09 gap: <description>. Tracked as <issue-ref or backlog entry>')`, and record the gap in the findings doc.
      (c) Test is flaky (viewport-size race) → add a `await page.setViewportSize({ width: 375, height: 667 }); await page.waitForTimeout(100);` guard before the assertion — but do NOT lengthen timeouts beyond 2 seconds (per HVER-05 stability culture).

    Step 2 — Write `.planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md` with this structure:

    ```markdown
    ---
    phase: 169
    plan: 02
    hver: HVER-01
    requirement: NAVX-09
    captured_at: <ISO-8601>
    viewport_tested: 375x667 (mobile)
    browser: chromium
    ---

    # NAVX-09 Mobile Nav ARIA Findings

    ## Expected ARIA Behavior
    Per WAI-ARIA Authoring Practices for disclosure buttons (e.g., hamburger menus):
    - The trigger button MUST have a `role="button"` (implicit via `<button>` is fine)
    - The trigger button MUST have an accessible name (text content, `aria-label`, or `aria-labelledby`)
    - The trigger button MUST expose `aria-expanded="true|false"` reflecting disclosure state
    - The trigger button SHOULD expose `aria-controls` pointing to the id of the disclosed region
    - The disclosed region SHOULD be `role="navigation"` with an `aria-label` (e.g., "Main")

    ## Observed Behavior
    Component under test: `<path/to/component.jsx>` (e.g., `src/components/MobileNav.jsx` — fill in actual path found via grep, OR write "NOT FOUND — no mobile nav component in codebase" if Step 0 pre-check was empty)

    | Attribute | Expected | Observed | Match? |
    |-----------|----------|----------|--------|
    | Button role | button | <observed> | yes/no |
    | Accessible name | non-empty | <observed text or aria-label value> | yes/no |
    | aria-expanded | "true"/"false" | <observed or "absent"> | yes/no |
    | aria-controls | id reference | <observed or "absent"> | yes/no |
    | Region role | navigation | <observed> | yes/no |
    | Region aria-label | present | <observed or "absent"> | yes/no |

    ## Reconciliation
    <One paragraph per row where observed != expected, explaining what the spec now asserts and why.>

    ## Gaps Escalated
    <List any assertions that were marked test.fixme with the rationale and suggested follow-up phase. If no gaps, write "None — all expected attributes present.">

    ## Spec Assertion Map
    | Test name (line in spec) | Asserts | Source of truth |
    |--------------------------|---------|-----------------|
    | <e.g. "mobile menu button has aria-expanded" (line 142)> | <assertion> | <WAI-ARIA APG section or observed app behavior> |
    ```

    Step 3 — Re-run the NAVX-09 subset one final time to prove the updated assertions pass (or that `test.fixme` markers are honored):
    ```
    npx playwright test tests/e2e/nav-accessibility-onboarding.spec.js --project=chromium --grep="NAVX-09" --reporter=list
    ```

    Commit as: `fix(169-02): align NAVX-09 assertions to observed browser ARIA + document findings`.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md &&
      grep -q "^## Expected ARIA Behavior" .planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md &&
      grep -q "^## Observed Behavior" .planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md &&
      grep -q "^## Reconciliation" .planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md &&
      grep -q "aria-expanded" tests/e2e/nav-accessibility-onboarding.spec.js &&
      grep -q "NAVX-09" tests/e2e/nav-accessibility-onboarding.spec.js &&
      npx playwright test tests/e2e/nav-accessibility-onboarding.spec.js --project=chromium --grep="NAVX-09" --reporter=list
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/169-human-verification/169-NAVX-09-ARIA-FINDINGS.md` contains all four headings: `## Expected ARIA Behavior`, `## Observed Behavior`, `## Reconciliation`, `## Gaps Escalated`, `## Spec Assertion Map`
    - The findings doc names the actual component file path under test (OR explicitly writes "NOT FOUND — no mobile nav component in codebase" if the Step 0 pre-check found no component; W6)
    - `tests/e2e/nav-accessibility-onboarding.spec.js` still contains the string `aria-expanded` and the string `NAVX-09`
    - `npx playwright test tests/e2e/nav-accessibility-onboarding.spec.js --project=chromium --grep="NAVX-09"` exits 0 (either all assertions pass or documented `test.fixme` markers are honored — Playwright treats `test.fixme` as expected-failed which is exit 0 in chromium-only mode)
    - Any `test.fixme` introduced carries a comment string matching the pattern `NAVX-09 gap:` so grep can find them later
  </acceptance_criteria>
  <done>
    NAVX-09 test suite passes on chromium at 375px viewport, all assertions reflect the actual ARIA attributes the app emits, and 169-NAVX-09-ARIA-FINDINGS.md documents the expected/observed comparison plus any escalated gaps. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: HVER-02 + HVER-03 — Confirm ADMN-02 and ADMN-03 visibility assertions pass deterministically</name>
  <files>
    tests/e2e/admin-settings-branding-security.spec.js,
    .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md
  </files>
  <read_first>
    - tests/e2e/admin-settings-branding-security.spec.js (restored from d0028db6 in Plan 01). **Critical observation from checker B1/B2:** both ADMN-02 (lines 16-46) and ADMN-03 (lines 48-75) describe blocks use `test.skip(() => !process.env.TEST_USER_EMAIL, 'Test credentials not configured')` as their skip guard. Neither block tests persistence — they assert visibility only. `loginAndPrepare(page)` in helpers.js defaults to `TEST_USER_EMAIL`.
    - tests/e2e/brand-theme.spec.js (pre-existing — already has a `brand theme data persists across page reloads` test at line 210. Note: this is complementary coverage of the persistence concern; the restored d0028db6 admin-settings spec does NOT cover it)
    - .planning/phases/169-human-verification/169-BASELINE.md (see Wave 2 Handoff for HVER-02 and HVER-03)
    - .env or .env.local to confirm **TEST_USER_EMAIL** is set (required for the restored ADMN-02/03 blocks to unskip)
    - src/pages/SettingsPage.jsx (to confirm the branding and security tabs exist and what roles they require)
  </read_first>
  <action>
    Step 1 — Precondition check (W8 fix + B1 fix). The restored spec gates every describe on `!process.env.TEST_USER_EMAIL`, NOT `TEST_CLIENT_EMAIL` or `TEST_ADMIN_EMAIL`. Source `.env` safely with POSIX-safe loader so values containing spaces/`#`/quotes are not mangled:
    ```
    set -a
    source .env
    set +a
    env | grep -E "^TEST_USER_EMAIL="
    ```
    `TEST_USER_EMAIL` must be set (present in .env.example at line 188 mapped to `client@bizscreen.test`). If missing, the spec will skip EVERY test in ADMN-02 and ADMN-03 and Playwright will still exit 0 — that is the anti-pattern the checker B1 called out. Flag this explicitly in 169-ADMN-PERSISTENCE-EVIDENCE.md as a precondition blocker and do NOT claim HVER-02/03 closed.

    Step 2 — Run ADMN-02 three times consecutively using the JSON reporter so we can verify tests actually EXECUTED (not skipped) (B1 fix):
    ```
    mkdir -p /tmp/169-admn
    for i in 1 2 3; do
      PLAYWRIGHT_JSON_OUTPUT_FILE=/tmp/169-admn/admn02-run$i.json npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --grep="ADMN-02" --reporter=json > /tmp/169-admn/admn02-run$i.log 2>&1
      EXIT=$?
      # Also capture a human-readable list-reporter run for the evidence tail
      npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --grep="ADMN-02" --reporter=list > /tmp/169-admn/admn02-run$i.list.txt 2>&1
      echo "run $i exit=$EXIT"
    done
    ```

    For EACH run, extract from the JSON (using `jq` or `node -e`):
    - `stats.expected` — tests that ran and passed (must be >= 1)
    - `stats.skipped` — tests that were skipped (must be 0 for the grep-matched subset)
    - `stats.unexpected` — tests that failed (must be 0)

    If `stats.expected < 1` or `stats.skipped > 0` for tests matching the `--grep="ADMN-02"` filter → the run did NOT actually execute the assertions and CANNOT count as a green run. This is the "skip-all exit 0" anti-pattern — treat it as a precondition blocker, not a pass.

    Step 3 — Same three-run protocol for ADMN-03:
    ```
    for i in 1 2 3; do
      PLAYWRIGHT_JSON_OUTPUT_FILE=/tmp/169-admn/admn03-run$i.json npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --grep="ADMN-03" --reporter=json > /tmp/169-admn/admn03-run$i.log 2>&1
      npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --grep="ADMN-03" --reporter=list > /tmp/169-admn/admn03-run$i.list.txt 2>&1
    done
    ```

    Step 4 — Write `.planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md`:

    ```markdown
    ---
    phase: 169
    plan: 02
    hver: [HVER-02, HVER-03]
    requirements: [ADMN-02, ADMN-03]
    captured_at: <ISO-8601>
    env:
      TEST_USER_EMAIL: present   # the canonical guard used by the restored spec (checker B1)
      TEST_CLIENT_EMAIL: <present|absent>   # informational; not used by the d0028db6 spec
      TEST_ADMIN_EMAIL: <present|absent>    # informational; not used by the d0028db6 spec
    ---

    # ADMN-02 + ADMN-03 Evidence

    ## Scope Note (READ BEFORE INTERPRETING RESULTS)
    The restored `admin-settings-branding-security.spec.js` at d0028db6 asserts **visibility only** for both ADMN-02 and ADMN-03. It does NOT upload a logo, toggle a security setting, reload, or assert persistence. Therefore:
    - The 3 green runs below prove the Settings > Branding tab and Settings > Security tab **render their expected sections** deterministically.
    - **Persistence for HVER-02 (branding) and HVER-03 (security) is proven only by the HUMAN round-trip in Task 3 of this plan.** Do not mistake these automated runs for persistence evidence.

    ## Summary
    <One paragraph: did the visibility tests pass deterministically? Any flake observed? Any assertion adjusted? Confirm TEST_USER_EMAIL was set and at least 1 test executed per run.>

    ## Run 1 — ADMN-02 Visibility
    Command: `npx playwright test ... --grep="ADMN-02" --reporter=json`
    Exit code: <0|non-0>
    JSON stats: expected=<N>/skipped=<N>/unexpected=<N>   ← expected MUST be >= 1, skipped MUST be 0
    Duration: <seconds>
    ```
    <tail of run1 list-reporter output>
    ```

    ## Run 2 — ADMN-02
    <same structure>

    ## Run 3 — ADMN-02
    <same structure>

    ## Run 1 — ADMN-03 Visibility
    <same structure>

    ## Run 2 — ADMN-03
    ## Run 3 — ADMN-03

    ## Assertion Adjustments
    <Any spec edits made to match observed product behavior. If none, write "None".>

    ## Escalated Gaps
    <Any product bugs discovered. If none, write "None".>

    ## Persistence Coverage Delegation
    HVER-02 persistence: PROVEN BY — Task 3 human round-trip (Branding tab change → save → reload → confirm)
    HVER-03 persistence: PROVEN BY — Task 3 human round-trip (Security tab change → save → reload → confirm)
    Automated coverage gap acknowledged: the restored spec at d0028db6 does not automate these round-trips. Complementary automated persistence coverage exists in `tests/e2e/brand-theme.spec.js` for brand themes specifically (line 210), but that is a complementary signal — not a substitute for the human round-trip that IS the canonical HVER-02/03 proof per this plan's scope.
    ```

    Commit spec edits (if any) and the evidence doc as: `test(169-02): verify ADMN-02/03 visibility across 3 consecutive runs + document persistence delegation to human round-trip`.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md &&
      grep -cE "^## Run [123] — ADMN-02" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md | grep -q "^3$" &&
      grep -cE "^## Run [123] — ADMN-03" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md | grep -q "^3$" &&
      grep -q "^## Scope Note" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md &&
      grep -q "^## Persistence Coverage Delegation" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md &&
      grep -qE "expected=[1-9]" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md &&
      grep -qE "skipped=0" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md &&
      PLAYWRIGHT_JSON_OUTPUT_FILE=/tmp/169-admn-verify.json npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --grep="ADMN-02" --reporter=json > /dev/null 2>&1 &&
      node -e "const s=require('/tmp/169-admn-verify.json').stats; if (s.expected<1 || s.skipped>0 || s.unexpected>0) process.exit(1)" &&
      PLAYWRIGHT_JSON_OUTPUT_FILE=/tmp/169-admn-verify.json npx playwright test tests/e2e/admin-settings-branding-security.spec.js --project=chromium --grep="ADMN-03" --reporter=json > /dev/null 2>&1 &&
      node -e "const s=require('/tmp/169-admn-verify.json').stats; if (s.expected<1 || s.skipped>0 || s.unexpected>0) process.exit(1)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md` has three `## Run N — ADMN-02` sections and three `## Run N — ADMN-03` sections
    - Each Run section contains: command, exit code, JSON stats line (`expected=N/skipped=N/unexpected=N`), duration, and a verbatim tail of list-reporter stdout
    - Each run's exit code in the evidence file is 0 AND each run's recorded JSON stats satisfy `expected >= 1 AND skipped == 0 AND unexpected == 0` (B1 fix: exit 0 alone is insufficient proof; must prove tests actually executed)
    - The file contains a `## Scope Note` section explicitly stating the d0028db6 spec tests visibility only, not persistence (B2 fix)
    - The file contains a `## Persistence Coverage Delegation` section mapping HVER-02 and HVER-03 persistence proof to Task 3's human round-trip (B2 fix)
    - The `env:` block in frontmatter lists `TEST_USER_EMAIL: present` (B1 fix — correct guard var)
    - A fresh 4th invocation (verifier's own) of `--grep="ADMN-02"` and `--grep="ADMN-03"` each produce JSON where `stats.expected >= 1 AND stats.skipped == 0 AND stats.unexpected == 0` (B1 strengthened acceptance: exit 0 via test-skip does NOT count)
    - The `## Summary` section explicitly states whether any assertion was adjusted
    - `## Escalated Gaps` is either "None" or lists specific product-side issues with suggested follow-up phase
  </acceptance_criteria>
  <done>
    ADMN-02 and ADMN-03 visibility assertions each passed 3 consecutive times with at least 1 test actually executed per run (JSON stats prove skip-all cannot masquerade as pass). Evidence doc exists with all 6 run records, explicit scope note, and persistence delegation map. Committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify ADMN-02 + ADMN-03 round-trip in the actual UI (canonical persistence proof for HVER-02/03)</name>
  <files>
    .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md
  </files>
  <read_first>
    - .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md (what the automated runs produced — note that those runs are VISIBILITY only; this checkpoint is the canonical persistence proof per B2)
    - src/pages/SettingsPage.jsx (which tab controls branding vs security)
  </read_first>
  <what-built>
    Task 2 proved the ADMN-02 and ADMN-03 Playwright specs pass deterministically across 3 runs each — but per the checker revision (B2), those restored specs at d0028db6 assert visibility only, not persistence. This checkpoint is therefore the **canonical persistence proof** for HVER-02 and HVER-03: a human round-trip of Settings > Branding (change color → save → hard-reload → confirm persisted) and Settings > Security (change setting → save → hard-reload → confirm persisted).
  </what-built>
  <how-to-verify>
    1. Start dev server if not running: `npm run dev` (http://localhost:5173)
    2. Log in as the client admin (TEST_ADMIN_EMAIL / TEST_ADMIN_PASSWORD from .env, or TEST_USER_EMAIL if admin-tier features not required for Branding/Security tabs)
    3. Navigate to /app/settings
    4. Click the **Branding** tab (HVER-02 / ADMN-02) — this is the canonical persistence proof because the automated test does NOT exercise this round-trip:
       - Change the primary brand color (any value different from current)
       - Save the change (button text likely "Save" or "Apply")
       - Confirm a success indicator appears (toast, inline confirmation)
       - Hard-refresh the browser (Cmd+Shift+R)
       - Click the Branding tab again
       - Confirm the new color is still selected (not reverted to previous)
    5. Click the **Security** tab (HVER-03 / ADMN-03) — canonical persistence proof:
       - Toggle one security setting (e.g., session timeout, password-min-length) to a new value
       - Save
       - Confirm success indicator
       - Hard-refresh
       - Click Security tab
       - Confirm the new value persists
    6. Append a bullet list to `169-ADMN-PERSISTENCE-EVIDENCE.md` under a new `## Human Round-Trip Confirmation` section:
       - [ ] Branding: color changed from <before> to <after>, saved, persisted after reload — PASS/FAIL
       - [ ] Security: setting <name> changed from <before> to <after>, saved, persisted after reload — PASS/FAIL
       - [ ] Any UX issues observed (toast wording, delay, stale UI): <list or "none">

    If either flow fails end-to-end despite the automated visibility test passing, that is a product-side persistence gap (not a test-validity gap — the automated test wasn't asserting persistence to begin with). Record it explicitly and escalate as a new bug — do NOT retroactively claim HVER-02 or HVER-03 are closed.
  </how-to-verify>
  <action>
    The executor awaits human confirmation. Before pausing, confirm:
    1. Task 2 completed successfully (169-ADMN-PERSISTENCE-EVIDENCE.md exists with 6 run records and the scope note).
    2. The dev server is running at http://localhost:5173 (or offer to start it: `npm run dev`).
    3. Open the evidence doc in the editor so the human can append their findings inline.

    Then pause and wait for the resume signal. When the human types "approved" or "issues: <desc>":
    - Append the human findings to `169-ADMN-PERSISTENCE-EVIDENCE.md` under `## Human Round-Trip Confirmation` (or confirm the human already added them).
    - Commit as: `docs(169-02): append human round-trip confirmation for ADMN-02/03 (canonical persistence proof)`.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md &&
      grep -q "^## Human Round-Trip Confirmation" .planning/phases/169-human-verification/169-ADMN-PERSISTENCE-EVIDENCE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `169-ADMN-PERSISTENCE-EVIDENCE.md` contains a `## Human Round-Trip Confirmation` section
    - The section has two bullet items (Branding, Security) each with an explicit PASS or FAIL and the before/after values
    - User explicitly typed "approved" or "approved with notes: <notes>" to resume
  </acceptance_criteria>
  <done>
    Human round-trip confirmation captured in the evidence doc and committed. HVER-02 and HVER-03 closed with both automated visibility regression proof + canonical human persistence sign-off.
  </done>
  <resume-signal>Type "approved" if both Branding and Security persist correctly, or "issues: <description>" to capture UX gaps for follow-up</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → local dev server | Tests authenticate with TEST_USER_EMAIL credentials — same trust posture as Phase 168 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-02-01 | Information Disclosure | Evidence doc could leak config values from Settings UI | mitigate | Action specifies capturing only pass/fail/skip + command output. Forbid copying actual tenant config values (hex colors are fine, password policy numerics are fine, anything resembling a secret is NOT copied). |
| T-169-02-02 | Tampering | `test.fixme` added to hide real product regressions | mitigate | Every `test.fixme` MUST carry a comment matching `NAVX-09 gap:` or `ADMN-0X gap:` and appear in the findings/evidence docs with escalation. Grep check in acceptance_criteria enforces this pattern. |
| T-169-02-03 | Denial of Service | Persistence writes corrupt tenant state | accept | Tests run against local Supabase with seed data; no production impact. |
| T-169-02-04 | Tampering | Playwright skip-all exit 0 masquerading as pass (B1) | mitigate | Every run in the evidence doc records JSON reporter `stats.expected >= 1` AND `stats.skipped == 0` for the grep-matched subset. Verifier re-runs and parses stats before accepting closure. |
</threat_model>

<verification>
1. All three tasks complete in order; human checkpoint (Task 3) is blocking and cannot be skipped
2. HVER-01 closure evidence: 169-NAVX-09-ARIA-FINDINGS.md + green NAVX-09 test run
3. HVER-02 closure evidence: 3 green ADMN-02 runs (with JSON stats proving actual execution, not skip) + human-confirmed branding round-trip (canonical persistence proof)
4. HVER-03 closure evidence: 3 green ADMN-03 runs (with JSON stats proving actual execution, not skip) + human-confirmed security round-trip (canonical persistence proof)
5. Any `test.fixme` markers introduced have tracked gap entries in the corresponding findings doc
</verification>

<success_criteria>
1. NAVX-09 spec assertions match observed browser ARIA behavior; any gaps are documented and escalated, not silently accepted
2. ADMN-02 visibility confirmed by 3 consecutive green test runs with JSON-reporter stats proving tests executed (expected>=1, skipped=0), AND ADMN-02 persistence confirmed by human round-trip
3. ADMN-03 visibility confirmed by 3 consecutive green test runs with JSON-reporter stats proving tests executed, AND ADMN-03 persistence confirmed by human round-trip
4. Evidence doc explicitly distinguishes automated visibility coverage from canonical human persistence proof (scope note + persistence delegation section)
5. No product code modified in this plan — scope stays strictly within verification and test-side reconciliation
</success_criteria>

<output>
After completion, create `.planning/phases/169-human-verification/169-02-SUMMARY.md` summarizing:
- HVER-01 disposition (passed-as-written, passed-after-assertion-update, or gap-escalated)
- HVER-02 and HVER-03 run counts (with JSON stats) and human-confirmation outcome
- Explicit note that automated visibility runs are NOT persistence proof; human round-trip IS
- Any `test.fixme` markers introduced and where they are tracked
- Explicit statement that no product code was modified in this plan
</output>
</output>
