---
phase: 168
plan: 02
type: execute
wave: 2
depends_on:
  - 168-00
  - 168-01
files_modified:
  - tests/e2e/layouts-screenshots.spec.js
  - tests/e2e/fixtures/index.js
  - tests/e2e/playlists.spec.js
  - .planning/ROADMAP.md
  - .planning/milestones/v18.0-ROADMAP.md
autonomous: true
requirements:
  - TQAL-01
  - TQAL-02
  - TQAL-03
  - TQAL-04
must_haves:
  truths:
    - "TQAL-01: layouts-screenshots.spec.js no longer imports TEST_LAYOUT_PREFIX"
    - "TQAL-01 SC1: npx eslint exits 0 on the three touched spec files"
    - "TQAL-02: line 120 of both ROADMAP.md files no longer contains 'language variants'"
    - "TQAL-03: fixtures/index.js JSDoc references loginAndPrepare() and no longer references 'storage state'"
    - "TQAL-04: zero occurrences of `partial` in playlists.spec.js (both describe labels cleaned)"
    - "Each TQAL fix is its own atomic commit for audit clarity (4 commits total, per researcher recommendation)"
  artifacts:
    - path: "tests/e2e/layouts-screenshots.spec.js"
      provides: "Lint-clean spec with TEST_LAYOUT_PREFIX import removed"
      contains: "LAYOUT_PRESETS, WIDGET_TYPES"
    - path: "tests/e2e/fixtures/index.js"
      provides: "Fixture barrel with loginAndPrepare-accurate JSDoc"
      contains: "loginAndPrepare"
    - path: "tests/e2e/playlists.spec.js"
      provides: "Playlist spec with CONT-09/CONT-10 labels marked as full coverage"
      contains: "(CONT-09)"
    - path: ".planning/ROADMAP.md"
      provides: "v19.0 ROADMAP with corrected Phase 155 line-120 summary wording"
    - path: ".planning/milestones/v18.0-ROADMAP.md"
      provides: "v18.0 milestone roadmap with corrected Phase 155 line-120 summary wording"
  key_links:
    - from: "tests/e2e/layouts-screenshots.spec.js"
      to: "tests/e2e/fixtures/index.js"
      via: "import { LAYOUT_PRESETS, WIDGET_TYPES } from './fixtures/index.js'"
      pattern: "LAYOUT_PRESETS, WIDGET_TYPES"
---

<objective>
Apply all four TQAL fixes on the freshly restored code and roadmap documents. Each TQAL item becomes its own atomic commit (4 commits total) for audit clarity per researcher recommendation. Verify TQAL-01 with ESLint (unblocked by Wave 0's eslint.config.js).

Purpose: Close SC1–SC4 for Phase 168 with surgical edits to known-good restored content.

Output: Four atomic commits, each addressing one TQAL item with grep-verifiable before/after text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v18.0-ROADMAP.md
@.planning/phases/168-test-doc-quality-cleanup/168-CONTEXT.md
@.planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md
@.planning/phases/168-test-doc-quality-cleanup/168-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TQAL-01 — remove unused TEST_LAYOUT_PREFIX import</name>
  <files>tests/e2e/layouts-screenshots.spec.js</files>
  <read_first>
    - tests/e2e/layouts-screenshots.spec.js (restored in Wave 1; confirm line 10 matches)
    - .planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md (Example 2 and "TQAL-01 Target" sections)
    - .planning/REQUIREMENTS.md line 26 (TQAL-01 spec)
  </read_first>
  <action>
Edit exactly one line in `tests/e2e/layouts-screenshots.spec.js`. Per D-03 and 168-RESEARCH.md "TQAL-01 Target": target is line 10.

BEFORE (exact text on line 10):
```
import { LAYOUT_PRESETS, WIDGET_TYPES, TEST_LAYOUT_PREFIX } from './fixtures/index.js';
```

AFTER (exact replacement text):
```
import { LAYOUT_PRESETS, WIDGET_TYPES } from './fixtures/index.js';
```

Change: drop `, TEST_LAYOUT_PREFIX` from the named-import list. Keep `LAYOUT_PRESETS` and `WIDGET_TYPES` in the exact order shown. Do NOT reorder, reformat, or touch any other line.

Steps:

1. Verify current line 10 matches the BEFORE text:
   ```bash
   sed -n '10p' tests/e2e/layouts-screenshots.spec.js
   # expect exact: import { LAYOUT_PRESETS, WIDGET_TYPES, TEST_LAYOUT_PREFIX } from './fixtures/index.js';
   ```
   If it does not match, STOP — restore may have drifted; re-verify Wave 1 output.

2. Apply the edit (use the Edit tool / sed, whichever matches GSD norms):
   Replace exactly `import { LAYOUT_PRESETS, WIDGET_TYPES, TEST_LAYOUT_PREFIX } from './fixtures/index.js';`
   with        `import { LAYOUT_PRESETS, WIDGET_TYPES } from './fixtures/index.js';`

3. Verify the edit:
   ```bash
   ! grep -q "TEST_LAYOUT_PREFIX" tests/e2e/layouts-screenshots.spec.js && echo "TQAL-01 removed"
   grep -q "LAYOUT_PRESETS, WIDGET_TYPES" tests/e2e/layouts-screenshots.spec.js && echo "other imports intact"
   ```

4. Run D-09-scoped ESLint (now that Wave 0 has eslint.config.js):
   ```bash
   npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js
   echo "exit=$?"
   # expect exit=0 with zero errors/warnings on the three files
   ```
   If lint errors emerge on the phase-scoped files (not pre-existing project-wide), surface them — may require small additional fixes within this plan per D-08 spirit (test-side drift).

5. Commit as atomic TQAL-01 commit:
   ```bash
   git add tests/e2e/layouts-screenshots.spec.js
   git commit -m "chore(168-02): TQAL-01 remove unused TEST_LAYOUT_PREFIX import from layouts-screenshots.spec.js"
   ```

Rationale: Per D-03, the import is unused in the 197-line body (grep confirmed zero body references). Removing it satisfies TQAL-01 SC1 and makes `npx eslint` pass clean on the file.
  </action>
  <verify>
    <automated>! grep -q "TEST_LAYOUT_PREFIX" tests/e2e/layouts-screenshots.spec.js && grep -q "LAYOUT_PRESETS, WIDGET_TYPES" tests/e2e/layouts-screenshots.spec.js && npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c TEST_LAYOUT_PREFIX tests/e2e/layouts-screenshots.spec.js` equals 0
    - `grep -q "import { LAYOUT_PRESETS, WIDGET_TYPES } from './fixtures/index.js';" tests/e2e/layouts-screenshots.spec.js` exits 0
    - `npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js` exits 0 with no errors/warnings
    - `git log -1 --format=%s` matches regex `^chore\(168-02\): TQAL-01`
    - Commit touches exactly `tests/e2e/layouts-screenshots.spec.js` (verify: `git diff HEAD~1 --name-only` returns exactly that path)
  </acceptance_criteria>
  <done>TEST_LAYOUT_PREFIX removed from the import on line 10; ESLint clean across the three phase-scoped files; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: TQAL-02 — fix line-120 Phase 155 summary in both ROADMAP files</name>
  <files>.planning/ROADMAP.md, .planning/milestones/v18.0-ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (confirm current line 120 text)
    - .planning/milestones/v18.0-ROADMAP.md (confirm current line 120 text)
    - .planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md ("TQAL-02 Target Ambiguity" — Candidate B, lines 329-337)
    - .planning/phases/168-test-doc-quality-cleanup/168-CONTEXT.md (D-04 and planning_context decision #3)
  </read_first>
  <action>
Per user decision #3 in planning_context: "The literal SC3 text in both roadmaps is already correct. The real remaining staleness is the Phase 155 1-line summary on line 120 of both `.planning/ROADMAP.md` and `.planning/milestones/v18.0-ROADMAP.md`... removes 'language variants' in favor of 'locale preference' wording consistent with the rest of the Phase 155 definition."

Target line 120 of each file has identical current text. Apply the same one-line edit to both, as ONE combined atomic commit (two-file edit).

BEFORE (exact text on line 120 of both files — verified by grep):
```
- [x] **Phase 155: Menu Board & Multi-Language Tests** - E2E tests for menu board CRUD, drag-drop, dietary tags, rendering, language variants, and fallback (completed 2026-04-09)
```

AFTER (exact replacement text — matches SC3 wording model per 168-RESEARCH.md line 334):
```
- [x] **Phase 155: Menu Board & Multi-Language Tests** - E2E tests for menu board CRUD, drag-drop, dietary tags, rendering, locale preference, and language fallback chain (completed 2026-04-09)
```

Change: replace the phrase `rendering, language variants, and fallback` with `rendering, locale preference, and language fallback chain`. Preserve every other character of the line, including the `- [x]` prefix, the bold `**Phase 155: Menu Board & Multi-Language Tests**` span, the ` - E2E tests for menu board CRUD, drag-drop, dietary tags, ` prefix, and the ` (completed 2026-04-09)` suffix.

Steps:

1. Confirm both files currently have the stale text on line 120:
   ```bash
   sed -n '120p' .planning/ROADMAP.md
   sed -n '120p' .planning/milestones/v18.0-ROADMAP.md
   # both expected to print: - [x] **Phase 155: Menu Board & Multi-Language Tests** - E2E tests for menu board CRUD, drag-drop, dietary tags, rendering, language variants, and fallback (completed 2026-04-09)
   ```

2. Apply the edit to each file (Edit tool or sed). Replace exactly:
   `rendering, language variants, and fallback`
   with
   `rendering, locale preference, and language fallback chain`

3. Verify:
   ```bash
   ! grep -q "language variants" .planning/ROADMAP.md
   ! grep -q "language variants" .planning/milestones/v18.0-ROADMAP.md
   grep -q "rendering, locale preference, and language fallback chain" .planning/ROADMAP.md
   grep -q "rendering, locale preference, and language fallback chain" .planning/milestones/v18.0-ROADMAP.md
   ```

4. Sanity: confirm SC3 in v18.0-ROADMAP.md (line 239 area) was NOT touched — per Pitfall 4:
   ```bash
   grep -q "switch locale preference via Settings, language preference persists across reload" .planning/milestones/v18.0-ROADMAP.md
   # expect exit 0 — SC3 text intact, locale-preference wording preserved
   ```

5. Commit as atomic TQAL-02 commit (combined edit):
   ```bash
   git add .planning/ROADMAP.md .planning/milestones/v18.0-ROADMAP.md
   git commit -m "docs(168-02): TQAL-02 update Phase 155 summary to locale-preference wording in both ROADMAPs (line 120)"
   ```

Rationale: Candidate B from 168-RESEARCH.md line 329-337. Both files share identical line-120 text, so a single logical edit spanning both files is cleaner than two identical sub-commits. CONTEXT #3 explicitly permits "one combined edit" at planner's discretion.
  </action>
  <verify>
    <automated>! grep -q "language variants" .planning/ROADMAP.md && ! grep -q "language variants" .planning/milestones/v18.0-ROADMAP.md && grep -q "rendering, locale preference, and language fallback chain" .planning/ROADMAP.md && grep -q "rendering, locale preference, and language fallback chain" .planning/milestones/v18.0-ROADMAP.md && grep -q "switch locale preference via Settings" .planning/milestones/v18.0-ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "language variants" .planning/ROADMAP.md` equals 0
    - `grep -c "language variants" .planning/milestones/v18.0-ROADMAP.md` equals 0
    - `grep -q "rendering, locale preference, and language fallback chain" .planning/ROADMAP.md` exits 0
    - `grep -q "rendering, locale preference, and language fallback chain" .planning/milestones/v18.0-ROADMAP.md` exits 0
    - `sed -n '120p' .planning/ROADMAP.md | grep -q "locale preference"` exits 0
    - `sed -n '120p' .planning/milestones/v18.0-ROADMAP.md | grep -q "locale preference"` exits 0
    - `grep -q "switch locale preference via Settings, language preference persists across reload" .planning/milestones/v18.0-ROADMAP.md` exits 0 (SC3 NOT regressed — Pitfall 4 avoided)
    - `git log -1 --format=%s` matches regex `^docs\(168-02\): TQAL-02`
    - `git diff HEAD~1 --name-only` returns exactly the two ROADMAP paths
  </acceptance_criteria>
  <done>Both ROADMAP files have the corrected Phase 155 summary on line 120; SC3 text in v18.0-ROADMAP.md remains intact; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: TQAL-03 — update fixtures/index.js JSDoc to reference loginAndPrepare()</name>
  <files>tests/e2e/fixtures/index.js</files>
  <read_first>
    - tests/e2e/fixtures/index.js (restored in Wave 1; confirm three target strings present)
    - .planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md (Example 3 and "TQAL-03 Target" sections, lines 228-346)
    - .planning/REQUIREMENTS.md line 28 (TQAL-03 spec)
  </read_first>
  <action>
Per D-05 and 168-RESEARCH.md Example 3: three docstring-only edits in `tests/e2e/fixtures/index.js`. No runtime behavior changes.

**Edit 1 — USAGE PATTERNS comment (~line 18):**

BEFORE (exact text):
```
 *    test('my authenticated test', async ({ page }) => {
 *      // page already has auth from storage state (project config)
 *    });
```

AFTER (exact replacement):
```
 *    test('my authenticated test', async ({ authenticatedPage }) => {
 *      // authenticatedPage already called loginAndPrepare() before the test body
 *    });
```

**Edit 2 — FIXTURES PROVIDED bullet (~line 50):**

BEFORE (exact text):
```
 * - authenticatedPage: Uses existing storage state and prepares the page
```

AFTER (exact replacement):
```
 * - authenticatedPage: Logs in via loginAndPrepare() and prepares the page
```

**Edit 3 — authenticatedPage fixture JSDoc (~lines 65-73):**

BEFORE (exact block):
```
  /**
   * Authenticated page fixture
   *
   * Uses the page from context (which already has storage state from project config)
   * and calls loginAndPrepare to ensure the page is ready for testing.
   *
   * @example
   * test('authenticated test', async ({ authenticatedPage }) => {
   *   await authenticatedPage.goto('/app/dashboard');
   * });
   */
```

AFTER (exact replacement block):
```
  /**
   * Authenticated page fixture
   *
   * Calls loginAndPrepare() on the per-test page (no storageState in project config)
   * so the page is logged in and ready for testing. Credentials come from
   * TEST_USER_EMAIL / TEST_USER_PASSWORD env vars.
   *
   * @example
   * test('authenticated test', async ({ authenticatedPage }) => {
   *   await authenticatedPage.goto('/app/dashboard');
   * });
   */
```

Steps:

1. Confirm all three BEFORE texts currently exist in the restored file (they will, per Wave 1 restore):
   ```bash
   grep -q "page already has auth from storage state (project config)" tests/e2e/fixtures/index.js
   grep -q "authenticatedPage: Uses existing storage state and prepares the page" tests/e2e/fixtures/index.js
   grep -q "Uses the page from context (which already has storage state from project config)" tests/e2e/fixtures/index.js
   # all three expected to exit 0 before editing
   ```

2. Apply all three edits using the Edit tool (or three discrete sed commands). Use exact literal strings.

3. Verify BEFORE strings are gone and AFTER strings are present:
   ```bash
   ! grep -q "storage state" tests/e2e/fixtures/index.js
   grep -q "loginAndPrepare()" tests/e2e/fixtures/index.js
   grep -q "authenticatedPage: Logs in via loginAndPrepare()" tests/e2e/fixtures/index.js
   grep -q "Calls loginAndPrepare() on the per-test page" tests/e2e/fixtures/index.js
   grep -q "authenticatedPage already called loginAndPrepare" tests/e2e/fixtures/index.js
   ```

4. Confirm runtime (the actual fixture body) was not altered — line count deltas should be very small (each edit is same-line count; the fixture function body code is untouched):
   ```bash
   wc -l tests/e2e/fixtures/index.js   # should still be ~109 ±2
   ```

5. Re-run ESLint on the three phase-scoped files to confirm no regressions:
   ```bash
   npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js
   echo "exit=$?"
   ```

6. Commit as atomic TQAL-03 commit:
   ```bash
   git add tests/e2e/fixtures/index.js
   git commit -m "docs(168-02): TQAL-03 update fixtures/index.js JSDoc to reference loginAndPrepare (not storageState)"
   ```
  </action>
  <verify>
    <automated>! grep -q "storage state" tests/e2e/fixtures/index.js && grep -q "loginAndPrepare()" tests/e2e/fixtures/index.js && grep -q "authenticatedPage: Logs in via loginAndPrepare()" tests/e2e/fixtures/index.js && grep -q "Calls loginAndPrepare() on the per-test page" tests/e2e/fixtures/index.js && npx eslint tests/e2e/fixtures/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storage state" tests/e2e/fixtures/index.js` equals 0
    - `grep -q "loginAndPrepare()" tests/e2e/fixtures/index.js` exits 0
    - `grep -q "authenticatedPage: Logs in via loginAndPrepare() and prepares the page" tests/e2e/fixtures/index.js` exits 0
    - `grep -q "Calls loginAndPrepare() on the per-test page (no storageState in project config)" tests/e2e/fixtures/index.js` exits 0
    - `grep -q "authenticatedPage already called loginAndPrepare() before the test body" tests/e2e/fixtures/index.js` exits 0
    - `[ "$(wc -l < tests/e2e/fixtures/index.js)" -ge 100 ]` AND `[ "$(wc -l < tests/e2e/fixtures/index.js)" -le 115 ]` (runtime body unchanged)
    - `npx eslint tests/e2e/fixtures/index.js` exits 0
    - `git log -1 --format=%s` matches regex `^docs\(168-02\): TQAL-03`
    - `git diff HEAD~1 --name-only` returns exactly `tests/e2e/fixtures/index.js`
  </acceptance_criteria>
  <done>All three storage-state JSDoc references rewritten to reflect loginAndPrepare(); runtime behavior unchanged; atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: TQAL-04 — strip "partial" from CONT-09/CONT-10 describe labels</name>
  <files>tests/e2e/playlists.spec.js</files>
  <read_first>
    - tests/e2e/playlists.spec.js (restored to 619 lines in Wave 1; confirm `partial` count = 2)
    - .planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md (Example 4 and "TQAL-04 Target" sections, lines 286-357)
    - .planning/REQUIREMENTS.md line 29 (TQAL-04 spec)
  </read_first>
  <action>
Per D-06 and 168-RESEARCH.md Example 4: two describe-label edits in `tests/e2e/playlists.spec.js`. Target lines 277 and 316 (as restored from 05a7f89d~1).

**Edit 1 — line 277:**

BEFORE (exact text):
```
test.describe('Playlist Validation (CONT-10 partial)', () => {
```

AFTER (exact replacement):
```
test.describe('Playlist Validation (CONT-10)', () => {
```

**Edit 2 — line 316:**

BEFORE (exact text):
```
test.describe('Playlist Empty State (CONT-09 partial)', () => {
```

AFTER (exact replacement):
```
test.describe('Playlist Empty State (CONT-09)', () => {
```

Change: drop ` partial` (space + word) from inside the parens on each describe label. Preserve the `CONT-10` / `CONT-09` requirement IDs and every other character.

Steps:

1. Confirm the current count is exactly 2 (as restored):
   ```bash
   [ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 2 ] && echo "baseline OK"
   grep -n partial tests/e2e/playlists.spec.js
   # expect two lines matching the describe labels above; no other `partial` occurrences
   ```

2. Apply the two edits (Edit tool or sed). Use exact literal strings so no other similar text is affected.

3. Verify:
   ```bash
   [ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 0 ] && echo "TQAL-04 done"
   grep -q "Playlist Validation (CONT-10)" tests/e2e/playlists.spec.js
   grep -q "Playlist Empty State (CONT-09)" tests/e2e/playlists.spec.js
   # confirm the CONT-IDs are still present; we only stripped ` partial`
   ```

4. Confirm spec still parses under Playwright (no structural damage):
   ```bash
   npx playwright test tests/e2e/playlists.spec.js --list > /tmp/168-02-4-list.txt
   echo "exit=$?"
   # expect exit 0; /tmp/168-02-4-list.txt should list the two renamed describes
   grep -q "Playlist Validation (CONT-10)" /tmp/168-02-4-list.txt && echo "describe label reflects new text in --list output"
   grep -q "Playlist Empty State (CONT-09)" /tmp/168-02-4-list.txt && echo "describe label reflects new text in --list output"
   ```

5. Re-run ESLint on the three phase-scoped files to confirm no regressions:
   ```bash
   npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js
   ```

6. Commit as atomic TQAL-04 commit:
   ```bash
   git add tests/e2e/playlists.spec.js
   git commit -m "chore(168-02): TQAL-04 strip 'partial' qualifier from CONT-09/CONT-10 describe labels (full coverage)"
   ```

Rationale: Per D-06, CONT-09 and CONT-10 coverage is full, not partial — the labels lagged. This is the final TQAL item; after this commit, all four TQAL SCs are closed on HEAD.
  </action>
  <verify>
    <automated>[ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 0 ] && grep -q "Playlist Validation (CONT-10)" tests/e2e/playlists.spec.js && grep -q "Playlist Empty State (CONT-09)" tests/e2e/playlists.spec.js && npx playwright test tests/e2e/playlists.spec.js --list > /dev/null && npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c partial tests/e2e/playlists.spec.js` equals 0
    - `grep -q "test.describe('Playlist Validation (CONT-10)', () => {" tests/e2e/playlists.spec.js` exits 0
    - `grep -q "test.describe('Playlist Empty State (CONT-09)', () => {" tests/e2e/playlists.spec.js` exits 0
    - `npx playwright test tests/e2e/playlists.spec.js --list` exits 0 (spec still valid)
    - `npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js` exits 0 (all three files lint-clean)
    - `git log -1 --format=%s` matches regex `^chore\(168-02\): TQAL-04`
    - `git diff HEAD~1 --name-only` returns exactly `tests/e2e/playlists.spec.js`
    - Total commit count in Wave 2: exactly 4 (one per TQAL) — verify `git log --oneline HEAD~4..HEAD | wc -l` equals 4 and each commit message matches TQAL-01, TQAL-02, TQAL-03, TQAL-04 in order
  </acceptance_criteria>
  <done>Both CONT-09/CONT-10 describe labels cleaned; Playwright still discovers the spec; ESLint clean across all three phase-scoped files; atomic commit landed — Wave 2 complete.</done>
</task>

</tasks>

<threat_model>
No runtime code paths modified. Changes are limited to:
- Test specs (no production behavior)
- Test fixtures/helpers (no production behavior)
- ESLint config (dev-time tooling only)
- Planning/roadmap markdown (documentation only)

No threats in scope for this phase.
</threat_model>

<verification>
Post-plan sanity (after all 4 tasks commit):

```bash
# Four atomic commits, one per TQAL
git log --oneline HEAD~4..HEAD
# expect (top-most first):
#   <sha> chore(168-02): TQAL-04 strip 'partial' qualifier ...
#   <sha> docs(168-02): TQAL-03 update fixtures/index.js JSDoc ...
#   <sha> docs(168-02): TQAL-02 update Phase 155 summary ...
#   <sha> chore(168-02): TQAL-01 remove unused TEST_LAYOUT_PREFIX ...

# All four SCs grep-verifiable
! grep -q "TEST_LAYOUT_PREFIX" tests/e2e/layouts-screenshots.spec.js       # SC1
! grep -q "language variants" .planning/ROADMAP.md                          # SC2
! grep -q "language variants" .planning/milestones/v18.0-ROADMAP.md         # SC2
! grep -q "storage state" tests/e2e/fixtures/index.js && \
  grep -q "loginAndPrepare" tests/e2e/fixtures/index.js                     # SC3
[ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 0 ]                  # SC4

# ESLint clean across the three phase-scoped files
npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js
```

Per 168-VALIDATION.md Sampling Rate: Post-Wave 2 — re-run all four grep checks + spec `--list`. Plan 03 (Wave 3) handles the full live Playwright run.
</verification>

<success_criteria>
- Four atomic commits land on HEAD, one per TQAL item (researcher recommendation: all-separate for audit clarity)
- Commit sequence: TQAL-01 → TQAL-02 → TQAL-03 → TQAL-04 (order chosen: smallest-blast-radius first)
- All four TQAL requirement success criteria grep-verifiable on HEAD
- `npx eslint tests/e2e/layouts-screenshots.spec.js tests/e2e/fixtures/index.js tests/e2e/playlists.spec.js` exits 0
- Playwright `--list` still works on the edited specs (no structural damage)
- SC3 in v18.0-ROADMAP.md ("switch locale preference via Settings ...") is NOT touched (Pitfall 4 avoided)
- Runtime test behavior unchanged (all edits are imports / JSDoc / labels / doc text)
</success_criteria>

<output>
After completion, create `.planning/phases/168-test-doc-quality-cleanup/168-02-SUMMARY.md` with:
- Commit grid: 4 commits × (SHA, TQAL-id, files touched, grep confirmation)
- ESLint output on the three files (should be zero errors/warnings)
- Any unexpected lint warnings surfaced (and whether they were in scope per D-08/D-09)
- Note confirming SC3 text in v18.0-ROADMAP.md is intact
</output>
