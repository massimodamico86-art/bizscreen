---
phase: 168
plan: 01
type: execute
wave: 1
depends_on:
  - 168-00
files_modified:
  - tests/e2e/layouts-screenshots.spec.js
  - tests/e2e/playlists.spec.js
  - tests/e2e/fixtures/index.js
  - tests/e2e/helpers/index.js
  - tests/e2e/helpers/screenshots.js
  - tests/e2e/helpers.js
autonomous: true
requirements:
  - TQAL-01
  - TQAL-03
  - TQAL-04
must_haves:
  truths:
    - "All 6 artifacts deleted or truncated by commit 05a7f89d are restored to their 05a7f89d~1 state"
    - "tests/e2e/layouts-screenshots.spec.js exists and is 197 lines"
    - "tests/e2e/playlists.spec.js exists and is 619 lines"
    - "tests/e2e/fixtures/index.js exists and is 109 lines"
    - "tests/e2e/helpers/index.js exists and is 21 lines"
    - "tests/e2e/helpers/screenshots.js exists and is 110 lines"
    - "tests/e2e/helpers.js exports assertAppReady and imports expect from @playwright/test"
    - "Restore is a single atomic commit framed as regression recovery of v18 Phase 162 work"
  artifacts:
    - path: "tests/e2e/layouts-screenshots.spec.js"
      provides: "Layout screenshot visual-regression E2E suite (restored)"
      min_lines: 195
    - path: "tests/e2e/playlists.spec.js"
      provides: "Playlist CRUD + empty state + validation E2E suite (restored to full 619 lines)"
      min_lines: 600
    - path: "tests/e2e/fixtures/index.js"
      provides: "Playwright fixture barrel with authenticatedPage, LAYOUT_PRESETS, WIDGET_TYPES, TEST_LAYOUT_PREFIX"
      min_lines: 100
    - path: "tests/e2e/helpers/index.js"
      provides: "Helpers barrel re-exporting screenshots.js + helpers.js utilities incl assertAppReady"
      min_lines: 18
    - path: "tests/e2e/helpers/screenshots.js"
      provides: "Screenshot utilities (VIEWPORTS, screenshotStep)"
      min_lines: 100
    - path: "tests/e2e/helpers.js"
      provides: "Shared E2E helpers: loginAndPrepare, assertAppReady, etc."
      min_lines: 220
  key_links:
    - from: "tests/e2e/layouts-screenshots.spec.js"
      to: "tests/e2e/helpers/index.js AND tests/e2e/fixtures/index.js"
      via: "import statements at lines 9-10"
      pattern: "from './helpers/index.js'"
    - from: "tests/e2e/helpers/index.js"
      to: "tests/e2e/helpers.js (assertAppReady)"
      via: "re-export"
      pattern: "assertAppReady"
    - from: "tests/e2e/fixtures/index.js"
      to: "tests/e2e/helpers.js (loginAndPrepare)"
      via: "import"
      pattern: "loginAndPrepare"
---

<objective>
Restore all 6 artifacts deleted or truncated by commit `05a7f89d feat(165-01)` in a single atomic Wave 1 commit. This recovers v18 Phase 162's restore work and establishes the targets for TQAL-01/03/04 edits in Wave 2.

Purpose: Phase 162 previously restored playlists.spec.js and helpers state; commit 05a7f89d silently regressed 716 lines of test infrastructure. This plan undoes that regression by checking out the files from `05a7f89d~1` (the known-good snapshot immediately before the deletion).

Output: 6 files restored on disk, one atomic commit naming 05a7f89d as the regression source and Phase 162 as the origin of the restored work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/168-test-doc-quality-cleanup/168-CONTEXT.md
@.planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md
@.planning/phases/168-test-doc-quality-cleanup/168-VALIDATION.md
@.planning/milestones/v18.0-phases/162-restore-deleted-spec-files/162-VERIFICATION.md
</context>

<interfaces>
<!-- Key artifacts and their roles in the restored state (from 168-RESEARCH.md). -->
<!-- All content comes from `git show 05a7f89d~1:<path>` — executor does NOT reconstruct from memory. -->

tests/e2e/helpers.js exports (227 lines, restored from 05a7f89d~1):
  - loginAndPrepare(page, email, password)
  - dismissAnyModals(page)
  - waitForPageReady(page)
  - navigateToSection(page, section)
  - generateTestName(prefix)
  - assertAppReady(page)            ← currently MISSING on main; must be present after restore
  - (top of file) `import { expect } from '@playwright/test';`   ← currently MISSING on main

tests/e2e/helpers/index.js (21 lines, barrel):
  - re-exports from ./screenshots.js (VIEWPORTS, screenshotStep, etc.)
  - re-exports assertAppReady from ../helpers.js

tests/e2e/helpers/screenshots.js (110 lines):
  - VIEWPORTS constant
  - screenshotStep(page, name) utility

tests/e2e/fixtures/index.js (109 lines):
  - extends Playwright base test with authenticatedPage + freshPage fixtures
  - exports LAYOUT_PRESETS, WIDGET_TYPES, TEST_LAYOUT_PREFIX constants
  - re-exports expect from @playwright/test

tests/e2e/layouts-screenshots.spec.js (197 lines):
  - imports from ./helpers/index.js (line 9)
  - imports from ./fixtures/index.js (line 10) — includes TEST_LAYOUT_PREFIX (unused; target of TQAL-01 in Wave 2)

tests/e2e/playlists.spec.js (619 lines):
  - line 277: test.describe('Playlist Validation (CONT-10 partial)', ...) — target of TQAL-04
  - line 316: test.describe('Playlist Empty State (CONT-09 partial)', ...) — target of TQAL-04
  - exactly 2 occurrences of the word `partial`, both on describe labels
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-restore safety diff of tests/e2e/helpers.js (partial-restore check)</name>
  <files>(no file modifications; read-only diagnostic)</files>
  <read_first>
    - tests/e2e/helpers.js (current state on main, 190 lines, truncated)
    - Output of `git show 05a7f89d~1:tests/e2e/helpers.js` (227 lines, target state)
    - .planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md (Pitfall 2 and Assumption A2 sections)
  </read_first>
  <action>
Per CONTEXT user decision: "for `tests/e2e/helpers.js` (partial file restore; merge with current helpers.js content, do NOT overwrite the current file if it has other post-05a7f89d additions; researcher confirmed no such additions, but executor must diff first)".

Execute:

1. Capture current state:
   ```bash
   cp tests/e2e/helpers.js /tmp/168-helpers-current.js
   wc -l /tmp/168-helpers-current.js   # expect 190
   ```

2. Capture target state:
   ```bash
   git show 05a7f89d~1:tests/e2e/helpers.js > /tmp/168-helpers-target.js
   wc -l /tmp/168-helpers-target.js   # expect 227
   ```

3. Diff them to confirm the ONLY substantive differences are additions in the target (no divergent post-05a7f89d edits on main):
   ```bash
   diff /tmp/168-helpers-current.js /tmp/168-helpers-target.js > /tmp/168-helpers-diff.txt
   # Inspect the diff: every hunk should show `>` lines (additions in target) or
   # lines that exist in target but are absent in current. There should be NO `<`-only
   # hunks that represent content the current file has and the target doesn't (i.e.
   # no divergent additions on main since 05a7f89d).
   ```

4. Also confirm with git log that no commits since 05a7f89d modified this file (Assumption A2):
   ```bash
   git log 05a7f89d..HEAD --oneline -- tests/e2e/helpers.js
   # expect: empty output (0 commits)
   ```

5. Decision gate:
   - If step 4 returns 0 commits AND step 3 shows only target-side additions → safe to overwrite with target file (handled in Task 2). Record this conclusion in commit-ready notes.
   - If step 4 returns ≥1 commit OR step 3 shows current-file-only content → STOP. Escalate: user decision requires merging, not overwriting. Do not proceed with Task 2 until decision is documented.

6. Write a short status file `/tmp/168-01-helpers-decision.txt` with one line: either `OVERWRITE_SAFE` or `MERGE_REQUIRED: <reason>`.
  </action>
  <verify>
    <automated>[ -f /tmp/168-01-helpers-decision.txt ] && grep -qE "^(OVERWRITE_SAFE|MERGE_REQUIRED)" /tmp/168-01-helpers-decision.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/168-helpers-target.js` exists and `wc -l` ≥ 220
    - `/tmp/168-helpers-current.js` exists (snapshot of pre-restore helpers.js)
    - `git log 05a7f89d..HEAD --oneline -- tests/e2e/helpers.js` output is empty (0 commits — confirms Assumption A2)
    - `/tmp/168-01-helpers-decision.txt` exists and contains `OVERWRITE_SAFE` OR `MERGE_REQUIRED:` (with reason)
    - If `MERGE_REQUIRED`: Task 2 must STOP and escalate before proceeding
  </acceptance_criteria>
  <done>Safety diff completed. Decision recorded. If `OVERWRITE_SAFE`, Task 2 may proceed with full file overwrite. If `MERGE_REQUIRED`, this plan halts and escalates per CONTEXT instruction.</done>
</task>

<task type="auto">
  <name>Task 2: Restore all 6 artifacts from 05a7f89d~1 in a single atomic commit</name>
  <files>
    tests/e2e/layouts-screenshots.spec.js,
    tests/e2e/playlists.spec.js,
    tests/e2e/fixtures/index.js,
    tests/e2e/helpers/index.js,
    tests/e2e/helpers/screenshots.js,
    tests/e2e/helpers.js
  </files>
  <read_first>
    - /tmp/168-01-helpers-decision.txt (result from Task 1 — must read OVERWRITE_SAFE)
    - .planning/phases/168-test-doc-quality-cleanup/168-RESEARCH.md (Example 1 — exact restoration commands)
    - .planning/phases/168-test-doc-quality-cleanup/168-CONTEXT.md (D-01, D-02 — restoration decisions)
  </read_first>
  <action>
Gate: read `/tmp/168-01-helpers-decision.txt`. If it does NOT start with `OVERWRITE_SAFE`, abort this task and escalate.

Per D-01 + D-02 + user decision in planning_context: restore all 6 artifacts from `05a7f89d~1` in ONE atomic commit framed as regression recovery of v18 Phase 162's work, naming 05a7f89d as the regression source.

Execute exactly (do NOT substitute or reorder):

1. Create parent directories:
   ```bash
   mkdir -p tests/e2e/helpers tests/e2e/fixtures
   ```

2. Restore all 6 files (exact commands from 168-RESEARCH.md Example 1):
   ```bash
   git show '05a7f89d~1:tests/e2e/layouts-screenshots.spec.js' > tests/e2e/layouts-screenshots.spec.js
   git show '05a7f89d~1:tests/e2e/fixtures/index.js'           > tests/e2e/fixtures/index.js
   git show '05a7f89d~1:tests/e2e/playlists.spec.js'           > tests/e2e/playlists.spec.js
   git show '05a7f89d~1:tests/e2e/helpers/index.js'            > tests/e2e/helpers/index.js
   git show '05a7f89d~1:tests/e2e/helpers/screenshots.js'      > tests/e2e/helpers/screenshots.js
   git show '05a7f89d~1:tests/e2e/helpers.js'                  > tests/e2e/helpers.js
   ```

3. Verify line counts match the research-documented baseline:
   ```bash
   wc -l tests/e2e/layouts-screenshots.spec.js  # expect 197
   wc -l tests/e2e/fixtures/index.js            # expect 109
   wc -l tests/e2e/playlists.spec.js            # expect 619
   wc -l tests/e2e/helpers/index.js             # expect 21
   wc -l tests/e2e/helpers/screenshots.js       # expect 110
   wc -l tests/e2e/helpers.js                   # expect 227
   ```

4. Spot-check the critical import that caused Pitfall 2 is now present:
   ```bash
   grep -n "from '@playwright/test'" tests/e2e/helpers.js | head -1   # expect a match including `expect`
   grep -n "assertAppReady" tests/e2e/helpers.js                       # expect ≥1 match (function definition)
   ```

5. Spot-check TQAL-01/03/04 targets exist in the restored state (so Wave 2 has work to do):
   ```bash
   grep -q "TEST_LAYOUT_PREFIX" tests/e2e/layouts-screenshots.spec.js && echo "TQAL-01 target present"
   grep -q "storage state" tests/e2e/fixtures/index.js && echo "TQAL-03 target present"
   [ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 2 ] && echo "TQAL-04 targets present (exactly 2)"
   ```

6. Stage and commit as a single atomic commit (D-02):
   ```bash
   git add tests/e2e/layouts-screenshots.spec.js \
           tests/e2e/fixtures/index.js \
           tests/e2e/playlists.spec.js \
           tests/e2e/helpers/index.js \
           tests/e2e/helpers/screenshots.js \
           tests/e2e/helpers.js
   git commit -m "$(cat <<'EOF'
revert(168-01): restore 6 test artifacts silently deleted by 05a7f89d

Recovers v18 Phase 162's restore work. Commit 05a7f89d feat(165-01) dayparting
silently deleted 716 lines of test infrastructure across:
- tests/e2e/layouts-screenshots.spec.js (DELETED, restored 197 lines)
- tests/e2e/fixtures/index.js          (DELETED, restored 109 lines)
- tests/e2e/playlists.spec.js          (TRUNCATED 619→209, restored to 619)
- tests/e2e/helpers/index.js           (DELETED, restored 21 lines)
- tests/e2e/helpers/screenshots.js     (DELETED, restored 110 lines)
- tests/e2e/helpers.js                 (TRUNCATED 227→190 — assertAppReady +
                                        expect import stripped, restored 227)

All 6 files checked out verbatim from 05a7f89d~1 (the known-good snapshot
immediately before the regression). This is the restore-only commit; TQAL-01,
TQAL-03, and TQAL-04 edits land as separate atomic commits in Wave 2.

Source: D-01 / D-02 of .planning/phases/168-test-doc-quality-cleanup/168-CONTEXT.md
EOF
)"
   ```

7. Post-commit sanity:
   ```bash
   git log -1 --stat
   git diff HEAD~1 --stat tests/e2e/   # show the restored line deltas
   ```

Do NOT make any content changes. This commit is restoration only — identical bytes to `05a7f89d~1`. Any edits must happen in Wave 2.
  </action>
  <verify>
    <automated>test -f tests/e2e/layouts-screenshots.spec.js && test -f tests/e2e/fixtures/index.js && test -f tests/e2e/playlists.spec.js && test -f tests/e2e/helpers/index.js && test -f tests/e2e/helpers/screenshots.js && grep -q "assertAppReady" tests/e2e/helpers.js && grep -q "from '@playwright/test'" tests/e2e/helpers.js && [ "$(wc -l < tests/e2e/playlists.spec.js)" -ge 600 ] && [ "$(wc -l < tests/e2e/layouts-screenshots.spec.js)" -ge 195 ] && [ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/e2e/layouts-screenshots.spec.js` exits 0
    - `test -f tests/e2e/fixtures/index.js` exits 0
    - `test -f tests/e2e/playlists.spec.js` exits 0
    - `test -f tests/e2e/helpers/index.js` exits 0
    - `test -f tests/e2e/helpers/screenshots.js` exits 0
    - `[ "$(wc -l < tests/e2e/layouts-screenshots.spec.js)" -ge 195 ]` (research baseline 197 ±2)
    - `[ "$(wc -l < tests/e2e/playlists.spec.js)" -ge 600 ]` (Phase 162 baseline, research says 619)
    - `[ "$(wc -l < tests/e2e/fixtures/index.js)" -ge 100 ]` (research baseline 109)
    - `[ "$(wc -l < tests/e2e/helpers/index.js)" -ge 18 ]` (research baseline 21)
    - `[ "$(wc -l < tests/e2e/helpers/screenshots.js)" -ge 100 ]` (research baseline 110)
    - `[ "$(wc -l < tests/e2e/helpers.js)" -ge 220 ]` (research baseline 227)
    - `grep -q "assertAppReady" tests/e2e/helpers.js` exits 0
    - `grep -q "from '@playwright/test'" tests/e2e/helpers.js` exits 0 (confirms Pitfall 2 fix — expect import restored)
    - `grep -q "TEST_LAYOUT_PREFIX" tests/e2e/layouts-screenshots.spec.js` exits 0 (TQAL-01 target present, to be removed in Wave 2)
    - `grep -q "storage state" tests/e2e/fixtures/index.js` exits 0 (TQAL-03 target present, to be rewritten in Wave 2)
    - `[ "$(grep -c partial tests/e2e/playlists.spec.js)" -eq 2 ]` (TQAL-04 targets present, to be stripped in Wave 2)
    - `grep -q "loginAndPrepare" tests/e2e/fixtures/index.js` exits 0 (imports from helpers.js wire up correctly)
    - `git log -1 --format=%s` matches regex `^revert\(168-01\): restore 6 test artifacts`
    - `git log -1 --format=%B | grep -q "05a7f89d"` exits 0 (commit message names the regression source)
    - `git log -1 --format=%B | grep -q "Phase 162"` exits 0 (commit message credits Phase 162)
  </acceptance_criteria>
  <done>All 6 artifacts are restored verbatim from 05a7f89d~1 in a single atomic commit. File byte-equivalence to 05a7f89d~1 confirmed via line counts and critical grep checks. No content edits applied — Wave 2 owns TQAL fixes.</done>
</task>

</tasks>

<threat_model>
No runtime code paths modified. Changes are limited to:
- Test specs (no production behavior)
- Test fixtures/helpers (no production behavior)
- ESLint config (dev-time tooling only)
- Planning/roadmap markdown (documentation only)

No threats in scope for this phase.
</threat_model>

<verification>
Post-plan sanity (run after Task 2 commits):

```bash
# All 6 files present with restored content
ls -la tests/e2e/layouts-screenshots.spec.js tests/e2e/playlists.spec.js \
       tests/e2e/fixtures/index.js tests/e2e/helpers/index.js \
       tests/e2e/helpers/screenshots.js tests/e2e/helpers.js

# Playwright can at least parse and list the restored specs (no import errors)
npx playwright test tests/e2e/layouts-screenshots.spec.js tests/e2e/playlists.spec.js --list
# expect: exit 0, some tests listed

# Atomic commit — single commit touched exactly these 6 files
git diff HEAD~1 --name-only
```

Per 168-VALIDATION.md Sampling Rate: Post-Wave 1 run
`npx playwright test tests/e2e/layouts-screenshots.spec.js tests/e2e/playlists.spec.js --reporter=list`
to confirm restored files load without import errors. Failures at this stage indicate incomplete restore, not test-side drift (that's Wave 3's problem).
</verification>

<success_criteria>
- All 6 artifacts restored to 05a7f89d~1 state (byte-equivalent content)
- Line counts match research baselines (197, 109, 619, 21, 110, 227 ±2)
- Single atomic commit with message naming `05a7f89d` and `Phase 162`
- `assertAppReady` function + `expect` import present in restored helpers.js
- TQAL-01/03/04 targets (TEST_LAYOUT_PREFIX, "storage state" JSDoc, `partial` describe labels) all present in restored content, ready for Wave 2 edits
- Playwright `--list` succeeds on restored specs (import resolution works)
- No content edits applied in this wave (restoration-only)
</success_criteria>

<output>
After completion, create `.planning/phases/168-test-doc-quality-cleanup/168-01-SUMMARY.md` with:
- Confirmation of OVERWRITE_SAFE decision from Task 1
- Line-count delta table (pre vs post restore)
- Single commit SHA
- Playwright `--list` output snippet confirming test discovery works
</output>
