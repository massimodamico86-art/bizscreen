---
phase: 260414-qc4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/pages/TemplateMarketplacePage.jsx
  - src/components/TemplatePreviewModal.jsx
  - src/components/QuickCustomizePanel.jsx
  - src/components/TemplatePickerModal.jsx
  - tests/e2e/template-marketplace.spec.js
autonomous: true
requirements:
  - QC4-01-remove-marketplace-nav-and-route
  - QC4-02-delete-marketplace-only-frontend-files
  - QC4-03-redirect-old-marketplace-id-to-templates
must_haves:
  truths:
    - "Sidebar no longer shows a 'Template Marketplace' entry."
    - "Selecting (or programmatic navigation to) page-id 'template-marketplace' lands on the SVG Templates gallery, not a blank page or 404."
    - "The 'Templates' sidebar entry still works and renders SvgTemplateGalleryPage."
    - "App builds (vite build) and dev server starts with no unresolved imports."
    - "Admin template tooling (AdminTemplatesPage, AdminEditTemplatePage, BulkTemplateUpload) still functions because marketplaceService.js is preserved."
  artifacts:
    - path: "src/App.jsx"
      provides: "Nav config + pageMap with marketplace removed and aliased to templates"
      contains: "'templates'"
    - path: "src/pages/TemplateMarketplacePage.jsx"
      provides: "DELETED"
    - path: "src/components/TemplatePreviewModal.jsx"
      provides: "DELETED (only consumer was TemplateMarketplacePage)"
    - path: "src/components/QuickCustomizePanel.jsx"
      provides: "DELETED (only consumer was TemplatePreviewModal)"
    - path: "src/components/TemplatePickerModal.jsx"
      provides: "DELETED (orphan — no importers in src/)"
  key_links:
    - from: "src/App.jsx nav array"
      to: "pageMap"
      via: "page-id keys"
      pattern: "no 'template-marketplace' nav entry; pageMap aliases it to 'templates' content"
    - from: "Admin pages and BulkTemplateUpload"
      to: "src/services/marketplaceService.js"
      via: "imports of fetchMarketplaceTemplates, createTemplate, etc."
      pattern: "marketplaceService.js MUST remain on disk and unchanged"
---

<objective>
Remove the Template Marketplace feature from the BizScreen frontend per CONTEXT.md decisions:
- Frontend-only removal (backend RPCs, tables, migrations stay intact)
- Replace the page-id with a sensible alias to the SVG Templates gallery so old in-app navigation lands somewhere useful
- Preserve `src/services/marketplaceService.js` and its unit tests because admin tooling (AdminTemplatesPage, AdminEditTemplatePage, BulkTemplateUpload) still imports it

Purpose: Eliminate the dual-templates UX confusion users hit by exposing two competing nav entries.
Output: Smaller bundle, single "Templates" entry in the sidebar, marketplace-only files gone, app builds clean.
</objective>

<context>
@.planning/quick/260414-qc4-remove-the-template-marketplace/260414-qc4-CONTEXT.md
@src/App.jsx
@src/pages/TemplateMarketplacePage.jsx

<routing_reality>
Reality differs from the original scope hint about `<Navigate to="/app/templates" replace />`:
this codebase does NOT use React Router URL paths for app pages. Navigation is driven by a
`currentPage` string state and a `pageMap` object in `src/App.jsx`. There is no
`/app/template-marketplace` URL route to redirect — only a page-id `'template-marketplace'`
in the nav array (line 478) and in the pageMap (line 560).

The CONTEXT.md intent ("old in-app links land somewhere sensible") is honored by aliasing
the page-id to the existing `'templates'` content in pageMap, rather than by a Router
`<Navigate>` element.
</routing_reality>

<dependency_audit>
Verified before planning (do NOT re-audit; act on these results):

DELETE (marketplace-only, zero non-marketplace consumers):
- src/pages/TemplateMarketplacePage.jsx — only imported in src/App.jsx (lines 120, 560)
- src/components/TemplatePreviewModal.jsx — only imported by TemplateMarketplacePage
- src/components/QuickCustomizePanel.jsx — only imported by TemplatePreviewModal
- src/components/TemplatePickerModal.jsx — orphan; the modal used by PlaylistsPage.jsx is a
  locally-defined component with the same name, NOT this file (PlaylistsPage.jsx:419)
- tests/e2e/template-marketplace.spec.js — entire test file targets the deleted page

KEEP (shared with admin tooling per CONTEXT.md "preserve any shared utilities"):
- src/services/marketplaceService.js — imported by:
  - src/pages/Admin/AdminTemplatesPage.jsx
  - src/pages/Admin/AdminEditTemplatePage.jsx
  - src/components/Admin/BulkTemplateUpload.jsx (via createTemplate)
- tests/unit/services/marketplaceService.test.js — covers the kept service
- src/services/svgCustomizeService.js — would become orphan after QuickCustomizePanel goes;
  KEEP for now per "delete only if provably unused" → it's a thin SVG utility that future
  code may need. Leaving it avoids an over-eager delete.

NO-OP (verified absent):
- No `nav.templateMarketplace` key in any src/i18n/locales/*.json (App.jsx uses inline t()
  fallback string `'Template Marketplace'`, so nothing to remove from locale files).
- No marketplace-specific telemetry hooks found in frontend (`trackMarketplace`,
  `marketplaceTelemetry` searches returned zero src/ results).
- Backend files (supabase/migrations/080_template_marketplace.sql, RPCs, table) are out of
  scope per CONTEXT.md and remain untouched.
</dependency_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove marketplace from App.jsx nav, pageMap, and lazy import</name>
  <files>src/App.jsx</files>
  <action>
Make exactly these edits in src/App.jsx (and ONLY these):

1. Delete the lazy import on line 120:
   `const TemplateMarketplacePage = lazy(() => import('./pages/TemplateMarketplacePage'));`
   Remove the entire line.

2. In the nav array, delete line 478:
   `    { id: 'template-marketplace', label: t('nav.templateMarketplace', 'Template Marketplace'), icon: Store },`
   Remove the entire line. Keep the 'templates' entry on line 477 untouched.

3. Check if `Store` from `lucide-react` (line 45 in the import block) is used anywhere
   else in App.jsx after step 2. If `grep -n "\\bStore\\b" src/App.jsx` shows no
   remaining references besides the import, remove `Store,` from the lucide-react
   import block. If `Store` is still used elsewhere, leave the import as-is.

4. In the `pageMap` object, replace the marketplace entry on line 560 with a defensive
   alias so any lingering `setCurrentPage('template-marketplace')` calls render the SVG
   Templates gallery instead of nothing. Replace:
   `    'template-marketplace': <Suspense fallback={<PageLoader />}><TemplateMarketplacePage showToast={showToast} onNavigate={setCurrentPage} /></Suspense>,`
   with (use the EXACT element JSX from the existing 'templates' entry on line 534, do
   not invent new props):
   `    // Legacy alias: 'template-marketplace' was removed in qc4 (260414); route to the SVG Templates gallery so old in-app navigation calls land somewhere sensible.`
   `    'template-marketplace': <Suspense fallback={<PageLoader />}><SvgTemplateGalleryPage showToast={showToast} onNavigate={setCurrentPage} /></Suspense>,`

   Rationale per CONTEXT.md: "Replace the route with a redirect to /app/templates ...
   old bookmarks and any in-app links land somewhere sensible without adding a 404."
   Since this codebase uses page-id state instead of URL routes, the equivalent is
   aliasing the page-id key in pageMap. Do NOT remove the key entirely — that would
   regress the "no 404" guarantee from CONTEXT.md.

DO NOT touch: line 477 ('templates' nav entry), line 534 ('templates' pageMap entry),
line 565 ('svg-templates' pageMap entry — unrelated), or any other lazy imports.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen &amp;&amp; node -e "const s = require('fs').readFileSync('src/App.jsx','utf8'); const checks = { noLazyImport: !s.includes(\"lazy(() => import('./pages/TemplateMarketplacePage'))\"), noNavEntry: !/id:\\s*'template-marketplace'/.test(s), hasAlias: /'template-marketplace':\\s*&lt;Suspense[\\s\\S]*?SvgTemplateGalleryPage/.test(s), templatesEntryIntact: /id:\\s*'templates'/.test(s) &amp;&amp; /'templates':\\s*&lt;Suspense/.test(s) }; console.log(JSON.stringify(checks, null, 2)); if (Object.values(checks).some(v =&gt; !v)) process.exit(1);"</automated>
  </verify>
  <done>
src/App.jsx no longer lazy-imports TemplateMarketplacePage, no longer lists a
'template-marketplace' nav entry, and pageMap maps 'template-marketplace' to
SvgTemplateGalleryPage. The 'templates' nav entry and pageMap binding are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete marketplace-only frontend files and their e2e suite</name>
  <files>
src/pages/TemplateMarketplacePage.jsx,
src/components/TemplatePreviewModal.jsx,
src/components/QuickCustomizePanel.jsx,
src/components/TemplatePickerModal.jsx,
tests/e2e/template-marketplace.spec.js
  </files>
  <action>
Delete these five files. The dependency audit in &lt;context&gt; above proves each is either
marketplace-only or an orphan; do NOT re-run grep to "double check" — act on the audit.

```bash
git rm src/pages/TemplateMarketplacePage.jsx
git rm src/components/TemplatePreviewModal.jsx
git rm src/components/QuickCustomizePanel.jsx
git rm src/components/TemplatePickerModal.jsx
git rm tests/e2e/template-marketplace.spec.js
```

DO NOT delete:
- src/services/marketplaceService.js (admin tooling depends on it)
- tests/unit/services/marketplaceService.test.js (covers the kept service)
- src/services/svgCustomizeService.js (kept per CONTEXT.md "delete only if provably
  unused"; future SVG features may use it)
- Any backend file (migrations, RPCs, tables — out of scope per CONTEXT.md)
- src/pages/Admin/AdminTemplatesPage.jsx or AdminEditTemplatePage.jsx (admin tooling)

If `git rm` fails because a file does not exist (e.g. it was already deleted), continue
with the remaining files and note it in the commit message.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen &amp;&amp; test ! -e src/pages/TemplateMarketplacePage.jsx &amp;&amp; test ! -e src/components/TemplatePreviewModal.jsx &amp;&amp; test ! -e src/components/QuickCustomizePanel.jsx &amp;&amp; test ! -e src/components/TemplatePickerModal.jsx &amp;&amp; test ! -e tests/e2e/template-marketplace.spec.js &amp;&amp; test -e src/services/marketplaceService.js &amp;&amp; test -e tests/unit/services/marketplaceService.test.js &amp;&amp; test -e src/pages/SvgTemplateGalleryPage.jsx &amp;&amp; echo "deletion-shape OK"</automated>
  </verify>
  <done>
The five marketplace-only files are gone from the working tree. marketplaceService.js,
its unit test, and SvgTemplateGalleryPage.jsx are still present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + import-graph verification (the "land sensibly, no 404" guarantee)</name>
  <files>(none — verification only)</files>
  <action>
Run the full verification pass. This is the gate that proves the rip didn't break the
app. Do these in order; stop and fix the prior task if any step fails.

1. Static import check — confirm nothing in src/ still references the deleted files or
   the removed page-id symbol (excluding the legitimate alias entry in App.jsx and the
   marketplaceService imports from admin tooling, which are expected):

   ```bash
   ! grep -rn "from.*TemplateMarketplacePage\|from.*['\"]\\./TemplatePreviewModal\|from.*['\"]\\.\\./components/TemplatePreviewModal\|from.*QuickCustomizePanel\|from.*['\"]\\.\\./components/TemplatePickerModal\|from.*['\"]\\./TemplatePickerModal" src/
   ```

   Expected: zero matches → command exits 0 because of the leading `!`.

2. Vite build — must succeed with no unresolved imports:

   ```bash
   npm run build
   ```

   Watch for "Could not resolve" errors in the output. If present, fix the offending
   import in the file the build complains about.

3. Unit tests — marketplaceService tests must still pass (proves we kept the service
   correctly):

   ```bash
   npm test -- tests/unit/services/marketplaceService.test.js --run
   ```

   (Adjust runner flags to match whatever vitest/jest config the repo uses; the goal is
   "this single suite passes".)

4. Page-id alias smoke check — confirm the pageMap alias actually renders something:

   ```bash
   node -e "const s = require('fs').readFileSync('src/App.jsx','utf8'); const m = s.match(/'template-marketplace':\\s*(&lt;[^,]+)/); if (!m || !m[1].includes('SvgTemplateGalleryPage')) { console.error('alias missing'); process.exit(1); } console.log('alias OK:', m[1].slice(0,80));"
   ```

If all four checks pass, the rip is complete and the must_haves are satisfied.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen &amp;&amp; ! grep -rn "from.*TemplateMarketplacePage\|from.*['\"]\\./TemplatePreviewModal\|from.*['\"]\\.\\./components/TemplatePreviewModal\|from.*QuickCustomizePanel\|from.*['\"]\\.\\./components/TemplatePickerModal\|from.*['\"]\\./TemplatePickerModal" src/ &amp;&amp; npm run build 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
- No src/ file imports any of the deleted modules.
- `npm run build` exits 0 with no "Could not resolve" errors.
- marketplaceService.test.js suite still passes.
- pageMap['template-marketplace'] resolves to SvgTemplateGalleryPage (no 404 / blank
  screen for legacy in-app navigation).
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after all three tasks):

1. `npm run build` succeeds with zero "Could not resolve" warnings.
2. `npm run dev` starts and the sidebar shows exactly one templates-related entry
   ("Templates"), pointing at the SVG gallery.
3. Manually navigating in code via `setCurrentPage('template-marketplace')` (e.g. from
   browser devtools or a temporary button) renders the SVG Templates gallery, not a
   blank screen.
4. `git diff --stat` shows: src/App.jsx modified; 5 files deleted (TemplateMarketplacePage,
   TemplatePreviewModal, QuickCustomizePanel, TemplatePickerModal, template-marketplace.spec.js);
   no other files touched.
5. `grep -rn "marketplaceService" src/` still returns the admin tooling imports
   (AdminTemplatesPage, AdminEditTemplatePage, BulkTemplateUpload) — proving we
   correctly preserved the shared service.
</verification>

<success_criteria>
- Sidebar contains a single "Templates" entry; "Template Marketplace" is gone.
- The five marketplace-only files are deleted; the shared service and its tests remain.
- App builds clean; no unresolved imports.
- Page-id `'template-marketplace'` is aliased in pageMap to SvgTemplateGalleryPage so
  any legacy in-app navigation lands on the templates gallery (CONTEXT.md "no 404"
  guarantee, adapted to this codebase's page-id routing model).
- Backend (RPCs, tables, migrations, telemetry events table) is untouched per CONTEXT.md.
- Admin template tooling continues to function (marketplaceService.js intact).
</success_criteria>

<output>
After completion, create
`.planning/quick/260414-qc4-remove-the-template-marketplace/260414-qc4-SUMMARY.md`
documenting:
- Files deleted (with brief justification per file)
- Files preserved despite "marketplace" name (marketplaceService.js — shared with admin)
- The page-id alias decision and why it differs from the original `<Navigate>` hint
- Build verification result
</output>
