---
phase: 82-media-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/media/YodeckAddMediaModal.jsx
  - src/pages/hooks/useMediaLibrary.js
autonomous: true
requirements:
  - MEDIA-01

must_haves:
  truths:
    - "User can trigger file upload from the Add Media modal (click or drop)"
    - "Multi-file upload selects all chosen files, not just the first"
    - "Upload progress is visible to the user during upload (spinner, progress bar, or status text)"
    - "Uploading a wrong file type shows an error message, not a silent failure"
    - "Uploading an oversized file shows an error message, not a silent failure"
  artifacts:
    - path: "src/components/media/YodeckAddMediaModal.jsx"
      provides: "Upload modal with progress feedback rendered"
      contains: "uploading"
    - path: "src/pages/hooks/useMediaLibrary.js"
      provides: "Upload error surfacing to user via showToast"
      contains: "errors"
  key_links:
    - from: "src/hooks/useS3Upload.jsx"
      to: "src/pages/hooks/useMediaLibrary.js"
      via: "errors array and onError callback"
      pattern: "errors|onError"
    - from: "src/pages/hooks/useMediaLibrary.js"
      to: "src/components/media/YodeckAddMediaModal.jsx"
      via: "uploading and uploadProgress props"
      pattern: "uploading|uploadProgress"
---

<objective>
Verify and fix the upload flow so multi-file upload works, progress feedback is visible during upload, and error states (wrong type, oversized file) surface to the user rather than silently failing.

Purpose: MEDIA-01 — user can upload media files with progress feedback.
Output: Upload modal renders progress, upload errors are shown via toast.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-media-library/82-CONTEXT.md

Key files to read before implementing:
@src/hooks/useS3Upload.jsx
@src/pages/hooks/useMediaLibrary.js
@src/components/media/YodeckAddMediaModal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface upload errors to user via toast</name>
  <files>src/pages/hooks/useMediaLibrary.js</files>
  <action>
Read src/pages/hooks/useMediaLibrary.js fully before editing.

The hook calls useS3Upload with onSuccess and onError callbacks. The onError callback currently only logs; it does NOT call showToast. Fix this:

1. Find the useS3Upload call (around line 85-100). The onError callback should call `showToast?.('Upload failed: ' + error.message, 'error')`.

2. Verify that the `multiple: true` option is already being passed (it should be at line 93). Confirm multi-file upload is configured.

3. After the useS3Upload call result is destructured (uploading, progress, openFilePicker, renderFileInput), confirm `errors` is also destructured if available. If useS3Upload returns an `errors` array, check if any errors need to be shown on mount/update — if errors accumulate without being shown to the user, add a useEffect that calls `showToast` for each new error entry.

The goal: any file that fails validation (wrong type, too large) must trigger a visible toast message, not just a console log.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -n "showToast" src/pages/hooks/useMediaLibrary.js | grep -i "error\|fail\|upload" | head -10</automated>
    <manual>Confirm onError now calls showToast with the error message</manual>
  </verify>
  <done>useMediaLibrary onError callback calls showToast with a user-visible error message for upload failures including wrong-type and oversized-file validation errors</done>
</task>

<task type="auto">
  <name>Task 2: Add upload progress feedback to YodeckAddMediaModal</name>
  <files>src/components/media/YodeckAddMediaModal.jsx</files>
  <action>
Read src/components/media/YodeckAddMediaModal.jsx fully before editing.

The modal receives `uploading` (boolean) and `uploadProgress` (0-100 number) props in its signature. However, inspecting the component: the current signature is `{ open, onClose, _onUpload, _onAddWebPage, onMediaAdded, openFilePicker, showToast }` — the `uploading` and `uploadProgress` props are NOT in the destructured parameters at all.

Fix:
1. Add `uploading = false` and `uploadProgress = 0` to the component's destructured props (remove leading underscore prefix if currently present).
2. In the modal footer (where the Upload button is rendered), when `uploading` is true:
   - Disable the Upload button
   - Show a loading indicator inline: replace button text with `<Loader2 size={16} className="animate-spin mr-2" />` + `Uploading... {uploadProgress}%`
3. Optionally, also show progress in the drop zone area of the Upload tab content — a simple text line `"Uploading... {uploadProgress}%" ` when `uploading` is true is sufficient.

Do NOT restructure the component — make minimal targeted changes to add these props and render the progress feedback inline. Loader2 is already imported.

ESLint rule: no-console is error — do not add any console.log calls.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && node -e "const fs = require('fs'); const code = fs.readFileSync('src/components/media/YodeckAddMediaModal.jsx', 'utf8'); const hasUploading = code.includes('uploading') && !code.includes('_uploading'); const hasProgress = code.includes('uploadProgress') && !code.includes('_uploadProgress'); console.log('uploading prop used:', hasUploading, '| uploadProgress prop used:', hasProgress);"</automated>
    <manual>Confirm the footer Upload button shows spinner + percentage when uploading prop is true</manual>
  </verify>
  <done>YodeckAddMediaModal renders upload progress (spinner + percentage) in the footer Upload button when uploading=true, and the uploadProgress value is displayed</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /Users/massimodamico/bizscreen && npm run build 2>&1 | tail -5` — must exit 0 with no errors
2. Check that useMediaLibrary onError calls showToast: `grep -n "showToast" src/pages/hooks/useMediaLibrary.js | grep -c "error\|fail"` — must be > 0
3. Check that YodeckAddMediaModal uses uploading prop: `grep -n "uploading" src/components/media/YodeckAddMediaModal.jsx | grep -v "_uploading"` — must appear in destructured params and in JSX render
</verification>

<success_criteria>
- Upload modal renders a spinner + progress percentage during active upload
- Wrong-type or oversized-file uploads trigger a toast error message visible to the user
- Multi-file upload is confirmed enabled (multiple: true already set, no regression)
- Build passes with no ESLint errors
</success_criteria>

<output>
After completion, create `.planning/phases/82-media-library/82-01-SUMMARY.md` with:
- What was verified (already working) vs what was fixed
- Exact files modified and what changed
- Build result
</output>
