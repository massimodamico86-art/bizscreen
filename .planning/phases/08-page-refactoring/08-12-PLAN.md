---
phase: 08-page-refactoring
plan: 12
type: execute
wave: 2
depends_on: ["08-07", "08-08", "08-09", "08-10", "08-11"]
files_modified: []
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 5 pages meet their line count targets"
    - "All builds pass without errors"
    - "All tests pass"
    - "Page functionality is preserved"
  artifacts:
    - path: "src/pages/FeatureFlagsPage.jsx"
      provides: "Main page under 600 lines"
      max_lines: 600
    - path: "src/pages/MediaLibraryPage.jsx"
      provides: "Main page under 800 lines"
      max_lines: 800
    - path: "src/pages/ScreensPage.jsx"
      provides: "Main page under 700 lines"
      max_lines: 700
    - path: "src/pages/PlaylistEditorPage.jsx"
      provides: "Main page under 700 lines"
      max_lines: 700
    - path: "src/pages/CampaignEditorPage.jsx"
      provides: "Main page under 600 lines"
      max_lines: 600
  key_links:
    - from: "All 5 page files"
      to: "Corresponding component files"
      via: "import statements"
      pattern: "from.*Components"
---

<objective>
Verify all gap closure plans achieved their line count targets and functionality is preserved.

Purpose: After plans 08-07 through 08-11 execute, this plan verifies all 5 pages meet their line count targets and the application still functions correctly.

Output: Verification report confirming all success criteria met
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-page-refactoring/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify line count targets</name>
  <files></files>
  <action>
1. Check all 5 page files meet their line count targets:

```bash
wc -l src/pages/FeatureFlagsPage.jsx   # Target: < 600
wc -l src/pages/MediaLibraryPage.jsx   # Target: < 800
wc -l src/pages/ScreensPage.jsx        # Target: < 700
wc -l src/pages/PlaylistEditorPage.jsx # Target: < 700
wc -l src/pages/CampaignEditorPage.jsx # Target: < 600
```

2. Record results in verification report.

3. If any page exceeds target:
   - Note the deviation
   - Identify what additional extraction could be done
   - Flag for future work if necessary
  </action>
  <verify>
    - FeatureFlagsPage.jsx < 600 lines
    - MediaLibraryPage.jsx < 800 lines
    - ScreensPage.jsx < 700 lines
    - PlaylistEditorPage.jsx < 700 lines
    - CampaignEditorPage.jsx < 600 lines
  </verify>
  <done>All 5 pages verified against line count targets</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and tests</name>
  <files></files>
  <action>
1. Run full build:
   - `npm run build`
   - Check for any errors or warnings related to the modified pages

2. Run all tests:
   - `npm test -- --run`
   - Verify no new test failures introduced

3. Verify all component imports resolve:
   - Check for "module not found" errors
   - Check for undefined component warnings

4. Generate verification summary with:
   - Line counts for all 5 pages
   - Line counts for all component files
   - Build status
   - Test status
  </action>
  <verify>
    - `npm run build` succeeds
    - `npm test -- --run` passes (no new failures)
  </verify>
  <done>Build and tests verified, gap closure complete</done>
</task>

</tasks>

<verification>
- All 5 pages meet line count targets
- npm run build succeeds
- npm test passes
- No regressions in functionality
</verification>

<success_criteria>
1. FeatureFlagsPage.jsx is under 600 lines
2. MediaLibraryPage.jsx is under 800 lines
3. ScreensPage.jsx is under 700 lines
4. PlaylistEditorPage.jsx is under 700 lines
5. CampaignEditorPage.jsx is under 600 lines
6. All builds pass
7. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-page-refactoring/08-12-SUMMARY.md`

Update 08-VERIFICATION.md to reflect gap closure results.
</output>
