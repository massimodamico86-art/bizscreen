---
phase: 44-eslint-zero-warnings
plan: 04
type: execute
wave: 3
depends_on: ["44-03"]
files_modified:
  - "81 files across src/ with react-hooks/exhaustive-deps warnings"
autonomous: true

must_haves:
  truths:
    - "All 125 react-hooks/exhaustive-deps warnings are resolved"
    - "No infinite render loops introduced by adding dependencies"
    - "Hook behavior preserved (no unnecessary re-executions)"
  artifacts:
    - path: "src/**/*.{js,jsx}"
      provides: "81 files with correct hook dependency arrays"
  key_links:
    - from: "useEffect/useCallback dependency arrays"
      to: "referenced variables in hook body"
      via: "ESLint exhaustive-deps validation"
      pattern: "\\[(.*logger.*|.*load\\w+.*)\\]"
---

<objective>
Fix all 125 react-hooks/exhaustive-deps warnings across 81 files.

Purpose: Correct hook dependency arrays so all referenced variables are properly listed as dependencies. Most warnings (majority) are missing `logger` in dependency arrays -- logger is stable (created once via createLogger) so adding it to deps is safe and correct.

Output: 81 files updated, 0 react-hooks/exhaustive-deps warnings remaining.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 125 react-hooks/exhaustive-deps warnings across 81 files</name>
  <files>81 files across src/ -- use eslint output to identify each file and line</files>
  <action>
  Process all 125 react-hooks/exhaustive-deps warnings. The warning message tells you exactly which dependency is missing.

  **Step 1:** Get the full list of warnings:
  ```bash
  npx eslint . --format json 2>/dev/null | node -e "
    const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));
    for(const f of d){
      const ms=f.messages.filter(m=>m.ruleId==='react-hooks/exhaustive-deps');
      if(ms.length>0){
        const s=f.filePath;
        for(const m of ms) console.log(JSON.stringify({file:s,line:m.line,msg:m.message}));
      }
    }
  "
  ```

  **Step 2:** For each warning, apply the appropriate fix based on the warning type:

  **Type A -- "missing dependency: 'logger'" (most common):**
  - Add `logger` to the dependency array: `[dep1, dep2]` -> `[dep1, dep2, logger]`
  - This is SAFE because `logger` is created via `createLogger('ComponentName')` at module/component level and is stable (same reference across renders).
  - Do NOT wrap in useCallback or useMemo -- just add to deps.

  **Type B -- "missing dependency: 'loadXxx'" (function reference):**
  - If `loadXxx` is defined inside the component with useCallback, add it to deps.
  - If `loadXxx` is defined as a plain function inside the component, wrap it in useCallback first, then add to the useEffect deps. OR move the function body inside the useEffect if it is only called from that one effect.
  - If adding the dependency would cause an infinite loop (function recreated every render), use the pattern: move the function definition inside the useEffect, or wrap it in useCallback with its own proper deps.

  **Type C -- "missing dependencies: 'formData', 'logger', 'setFormData', and 'showToast'" (multiple missing):**
  - Add all listed dependencies to the array.
  - For state setters like `setFormData`, these are stable (React guarantees) so adding them is safe.
  - For values like `formData` that change on every state update, adding them may cause the effect to re-run. If the effect is meant to run only on mount, add an `// eslint-disable-next-line react-hooks/exhaustive-deps` comment with a reason explaining why deps are intentionally excluded. Use this sparingly -- prefer fixing the dependency.

  **Type D -- "logical expression could make dependencies change on every render":**
  - These are cases like `const blocks = props.blocks || []` where the `|| []` creates a new array each render.
  - Fix by using `useMemo`: `const blocks = useMemo(() => props.blocks || [], [props.blocks]);`
  - Then add `blocks` to the hook deps as normal.

  **Step 3:** Process files in batches (~20 at a time). Verify periodically.

  **Step 4:** After all files, run full ESLint to verify 0 exhaustive-deps warnings.

  **CRITICAL:** After adding dependencies, do NOT just blindly add and move on. Consider:
  - Will this cause the effect to run more often? If so, is that correct?
  - For mount-only effects (empty `[]`), adding deps changes behavior. If truly mount-only, use eslint-disable with a comment.
  - For callbacks, adding stable refs (logger, setState, dispatch) is always safe.
  </action>
  <verify>Run `npx eslint . --format json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));let c=0;for(const f of d)for(const m of f.messages)if(m.ruleId==='react-hooks/exhaustive-deps')c++;console.log('exhaustive-deps remaining:',c);"` should output 0.</verify>
  <done>All 125 react-hooks/exhaustive-deps warnings eliminated across 81 files. Dependencies correctly added without introducing infinite loops or unnecessary re-renders.</done>
</task>

</tasks>

<verification>
- `npx eslint . --format json` shows 0 react-hooks/exhaustive-deps warnings
- `npm run build` succeeds
- `npm test` passes
</verification>

<success_criteria>
Zero react-hooks/exhaustive-deps warnings. All hook dependency arrays are correct. No infinite render loops introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/44-eslint-zero-warnings/44-04-SUMMARY.md`
</output>
