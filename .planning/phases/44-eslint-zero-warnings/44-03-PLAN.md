---
phase: 44-eslint-zero-warnings
plan: 03
type: execute
wave: 2
depends_on: ["44-01", "44-02"]
files_modified:
  - "172 files across src/ (see action for approach)"
autonomous: true

must_haves:
  truths:
    - "All 356 unused-imports/no-unused-vars warnings are resolved"
    - "No functional regressions -- prefixed vars with _ or removed truly dead assignments"
    - "No new ESLint errors introduced"
  artifacts:
    - path: "src/**/*.{js,jsx}"
      provides: "172 files with unused variables fixed"
  key_links:
    - from: "unused variable fixes"
      to: "existing functionality"
      via: "prefix with _ (safe) or remove dead assignment (verify unused)"
      pattern: "^_"
---

<objective>
Fix all 356 unused-imports/no-unused-vars warnings across 172 files.

Purpose: Eliminate all unused variable warnings so the rule can be promoted to error level. These are 164 unused function arguments and 192 unused assigned variables.

Output: 172 files updated, 0 unused-imports/no-unused-vars warnings remaining.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 356 unused variable warnings across 172 files</name>
  <files>172 files across src/ -- use eslint output to identify each file and line</files>
  <action>
  Process all 356 unused-imports/no-unused-vars warnings systematically. Use the following approach:

  **Step 1:** Get the full list of warnings with file paths and line numbers:
  ```bash
  npx eslint . --format json 2>/dev/null | node -e "
    const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));
    for(const f of d){
      const ms=f.messages.filter(m=>m.ruleId==='unused-imports/no-unused-vars');
      if(ms.length>0){
        const s=f.filePath;
        for(const m of ms) console.log(JSON.stringify({file:s,line:m.line,col:m.column,msg:m.message}));
      }
    }
  "
  ```

  **Step 2:** For each warning, apply the appropriate fix:

  **For "defined but never used" (function arguments, 164 occurrences):**
  - Prefix the argument name with `_` (e.g., `err` -> `_err`, `data` -> `_data`)
  - This preserves the function signature while signaling intentional non-use
  - For destructured args that are unused, prefix with `_` (e.g., `{ plan }` -> `{ plan: _plan }` or just prefix `_plan`)

  **For "assigned a value but never used" (variable assignments, 192 occurrences):**
  - If the variable is assigned from a destructuring and other parts ARE used, prefix with `_`
    Example: `const { data, error } = await fetch(...)` where `data` is unused -> `const { data: _data, error } = ...`
  - If the variable is a standalone `const x = someCall()` and `x` is never read, either:
    - Remove the variable and just call the function: `someCall();` (if the call has side effects)
    - Remove the entire line if the call result and side effects are not needed
  - If the variable is `const [x, setX] = useState(...)` and `x` is unused, prefix: `const [_x, setX] = useState(...)`
  - Common pattern: `const { data, error } = await supabase...` where only `error` is checked -- prefix data with `_`

  **Step 3:** Process files in batches. For efficiency, read multiple files, make fixes, and verify periodically (every ~30 files) with:
  ```bash
  npx eslint {file1} {file2} ... --format json
  ```

  **Step 4:** After all files are fixed, run full ESLint scan to verify 0 remaining unused-vars warnings.

  **Important:** The eslint config has `varsIgnorePattern: '^_'` and `argsIgnorePattern: '^_'` so prefixing with `_` is the canonical fix. Also `ignoreRestSiblings: true` means rest patterns are already handled.

  Do NOT delete function arguments or change function signatures beyond prefixing -- this could break callers or callback contracts.
  </action>
  <verify>Run `npx eslint . --format json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));let c=0;for(const f of d)for(const m of f.messages)if(m.ruleId==='unused-imports/no-unused-vars')c++;console.log('unused-vars remaining:',c);"` should output 0.</verify>
  <done>All 356 unused-imports/no-unused-vars warnings eliminated. Each fix uses _ prefix (safe, non-breaking) or removes genuinely dead assignments. No functional regressions.</done>
</task>

</tasks>

<verification>
- `npx eslint . --format json` shows 0 unused-imports/no-unused-vars warnings
- `npm run build` succeeds
- `npm test` passes
</verification>

<success_criteria>
Zero unused-imports/no-unused-vars warnings across entire codebase. All 172 files updated with safe fixes.
</success_criteria>

<output>
After completion, create `.planning/phases/44-eslint-zero-warnings/44-03-SUMMARY.md`
</output>
