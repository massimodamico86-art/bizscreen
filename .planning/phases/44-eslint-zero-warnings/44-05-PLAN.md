---
phase: 44-eslint-zero-warnings
plan: 05
type: execute
wave: 4
depends_on: ["44-04"]
files_modified:
  - eslint.config.js
autonomous: true

must_haves:
  truths:
    - "User can run npx eslint . and see zero warnings AND zero errors"
    - "User can inspect eslint.config.js and confirm all previously-warn rules are now error (except disabled ones)"
    - "User can attempt a commit with a lint violation and see it blocked by pre-commit hook"
  artifacts:
    - path: "eslint.config.js"
      provides: "All warn-level rules promoted to error"
      contains: "'no-undef': 'error'"
  key_links:
    - from: "eslint.config.js"
      to: ".husky/pre-commit"
      via: "lint-staged runs eslint --fix which now enforces error-level rules"
      pattern: "lint-staged"
---

<objective>
Promote all remaining warn-level rules to error in ESLint config and verify the entire codebase achieves zero warnings and zero errors.

Purpose: This is the final step of Phase 44. With all warnings fixed in Plans 01-04, promoting rules to error ensures future violations are caught at commit time via pre-commit hooks. This completes requirements LINT-01, LINT-02, and LINT-03.

Output: eslint.config.js with all rules at error level, verified zero-warning/zero-error codebase.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote all warn-level rules to error and verify zero warnings</name>
  <files>eslint.config.js</files>
  <action>
  In eslint.config.js, promote every remaining `'warn'` rule to `'error'`:

  1. `'no-console': ['warn', { allow: ['warn', 'error'] }]` -> `'no-console': ['error', { allow: ['warn', 'error'] }]`
  2. `'unused-imports/no-unused-vars': ['warn', {...}]` -> `'unused-imports/no-unused-vars': ['error', {...}]`
  3. `'no-case-declarations': 'warn'` -> `'no-case-declarations': 'error'`
  4. `'no-useless-catch': 'warn'` -> `'no-useless-catch': 'error'`
  5. `'no-useless-escape': 'warn'` -> `'no-useless-escape': 'error'`
  6. `'no-undef': 'warn'` -> `'no-undef': 'error'`

  Note: `react/prop-types`, `jsdoc/*`, and `react-refresh/only-export-components` are already set to `'off'` from Plan 01 -- leave them as off.

  Also update the TODO comments to reflect completion:
  - Remove "TODO: Upgrade to error after console migration to loggingService" -- done now
  - Remove "TODO: Consider upgrading to error after cleanup sprint" -- done now
  - Remove "TODO: Fix these in dedicated cleanup tasks" -- done now
  - Remove "TODO: Phase 28-02 should address these systematically" -- done now
  - Update comments to reflect current state (e.g., "All rules enforced at error level")

  After updating config, run full ESLint verification:
  ```bash
  npx eslint . 2>&1 | tail -5
  ```
  This must show 0 errors and 0 warnings.

  Then verify pre-commit enforcement works:
  1. Create a temporary test file with a violation:
     ```bash
     echo "const unused = 42;" > /tmp/test-lint.js
     ```
  2. Verify eslint catches it:
     ```bash
     npx eslint /tmp/test-lint.js
     ```
     Should show error (not warning).
  3. Clean up: `rm /tmp/test-lint.js`

  Verify the existing pre-commit hook (`cat .husky/pre-commit`) runs `npx lint-staged`, and `lint-staged` config in package.json runs `eslint --fix` on staged files. Since all rules are now `error`, any violation will cause `eslint --fix` to exit non-zero (for non-fixable errors), blocking the commit.
  </action>
  <verify>
  Run these three checks:
  1. `npx eslint . 2>&1 | tail -1` -- must show "0 problems" or no output (clean)
  2. `node -e "const c=require('fs').readFileSync('eslint.config.js','utf8');const warns=c.match(/: 'warn'/g);console.log('warn rules remaining:',warns?warns.length:0);"` -- must show 0
  3. `npm run build` -- must succeed
  </verify>
  <done>All previously-warn ESLint rules promoted to error. `npx eslint .` reports 0 warnings and 0 errors. Pre-commit hook blocks lint violations. LINT-01, LINT-02, and LINT-03 requirements met.</done>
</task>

</tasks>

<verification>
- `npx eslint .` shows 0 warnings and 0 errors across entire codebase (LINT-01)
- eslint.config.js has no 'warn' rules -- all are 'error' or 'off' (LINT-02)
- .husky/pre-commit + lint-staged configuration enforces error-level rules on commit (LINT-03)
- `npm run build` succeeds
- `npm test` passes
</verification>

<success_criteria>
Phase 44 complete: ESLint runs clean with zero warnings and zero errors. All rules enforce at error level. Pre-commit hooks block violations.
</success_criteria>

<output>
After completion, create `.planning/phases/44-eslint-zero-warnings/44-05-SUMMARY.md`
</output>
