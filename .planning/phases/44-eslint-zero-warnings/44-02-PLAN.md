---
phase: 44-eslint-zero-warnings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout-editor/PixieEditorModal.jsx
  - src/components/scene-editor/AiSuggestionsPanel.jsx
  - src/pages/Admin/AdminEditTemplatePage.jsx
  - src/pages/DataSourcesPage.jsx
  - src/pages/PlaylistsPage.jsx
  - src/pages/SchedulesPage.jsx
  - src/pages/ScreenGroupsPage.jsx
  - src/player/offlineService.js
  - src/services/sessionService.js
autonomous: true

must_haves:
  truths:
    - "All 34 no-undef warnings are resolved -- zero undefined variable references remain"
    - "No functional regressions introduced (variables properly declared or dead code removed)"
  artifacts:
    - path: "src/pages/PlaylistsPage.jsx"
      provides: "Fixed 6 undefined variable references"
    - path: "src/pages/ScreenGroupsPage.jsx"
      provides: "Fixed 10 undefined variable references"
    - path: "src/pages/SchedulesPage.jsx"
      provides: "Fixed 5 undefined variable references"
  key_links:
    - from: "src/player/offlineService.js"
      to: "syncPendingHeartbeats function"
      via: "function must be defined or imported before use"
      pattern: "syncPendingHeartbeats"
    - from: "src/services/sessionService.js"
      to: "sessionId variable"
      via: "variable must be declared before use"
      pattern: "sessionId"
---

<objective>
Fix all 34 no-undef warnings -- undefined variable references that represent real bugs in 9 source files.

Purpose: These are genuine code defects where variables are referenced but never declared. Some are unreachable dead code (remnants of refactoring), some are missing imports, some are variables that should reference existing state/props. Each must be inspected individually.

Output: 9 files fixed, 0 no-undef warnings remaining.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 34 no-undef warnings across 9 files</name>
  <files>
    src/components/layout-editor/PixieEditorModal.jsx
    src/components/scene-editor/AiSuggestionsPanel.jsx
    src/pages/Admin/AdminEditTemplatePage.jsx
    src/pages/DataSourcesPage.jsx
    src/pages/PlaylistsPage.jsx
    src/pages/SchedulesPage.jsx
    src/pages/ScreenGroupsPage.jsx
    src/player/offlineService.js
    src/services/sessionService.js
  </files>
  <action>
  Read each file and fix undefined variable references. For each file, determine whether the undefined variable is:
  (a) A missing import (add the import)
  (b) A reference to existing state/prop under a different name (fix the reference)
  (c) Dead/unreachable code from a past refactor (remove the dead code block)

  **Specific files and their undefined variables:**

  1. `src/components/layout-editor/PixieEditorModal.jsx` line 327: `logger` not defined
     - Check if loggingService is imported. If not, add `import { createLogger } from '../../services/loggingService';` and `const logger = createLogger('PixieEditorModal');`. If the logger call is in dead code, remove it.

  2. `src/components/scene-editor/AiSuggestionsPanel.jsx` line 403: `logger` not defined, `polishAction` not defined
     - Add logger import if missing. For `polishAction`, check if this is a renamed function or dead code -- find the correct reference or remove the dead block.

  3. `src/pages/Admin/AdminEditTemplatePage.jsx` lines 194, 196: `result` not defined; line 399: `accessId` not defined
     - These are likely references to variables from a prior refactor. Read surrounding code to determine: should they reference existing variables, or is the code block dead?

  4. `src/pages/DataSourcesPage.jsx` line 455: `campaign` not defined
     - Check context: probably should reference an existing variable like `selectedCampaign` or `dataSource`, or is dead code.

  5. `src/pages/PlaylistsPage.jsx` lines 812 (`selectedTemplate`, `newPlaylist`), 850 (`newPlaylistName`), 897/909/931/988 (`playlistId`), 988 (`screenId`)
     - These 8 references likely come from a refactor that moved state management. Check the component's state declarations and find the correct current variable names. Common pattern: state was moved to a custom hook or renamed.

  6. `src/pages/SchedulesPage.jsx` lines 125 (`newScheduleName`), 140/151/170 (`scheduleId`), 170 (`updates`)
     - Similar to PlaylistsPage -- find correct state variable references.

  7. `src/pages/ScreenGroupsPage.jsx` lines 124/532/550/568 (`groupId`), 364/393 (`formData`), 393 (`editingGroup`), 532/550/568 (`logger`), 550/568 (`selectedScreenIds`)
     - 10 warnings. Add logger import, and fix state variable references (`groupId`, `formData`, `editingGroup`, `selectedScreenIds`) by finding their correct current names in component state.

  8. `src/player/offlineService.js` line 125: `syncPendingHeartbeats` not defined
     - This function is likely defined elsewhere in the file or needs to be imported. If it is a forward reference, move the function definition before its use or add the function. If the function does not exist, the calling code is dead -- remove it.

  9. `src/services/sessionService.js` line 103: `sessionId` not defined
     - Check if this should reference a different variable or if the code block is dead.

  **For each fix:** Read the surrounding code (50+ lines of context around the flagged line) to understand the intent. Do NOT just delete lines without understanding if the code path is reachable. If a code block is unreachable (e.g., inside a function that is never called, or after a return statement), remove the entire unreachable block.

  After all fixes, run `npx eslint . --format json` and verify 0 no-undef warnings.
  </action>
  <verify>Run `npx eslint . --format json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));let c=0;for(const f of d)for(const m of f.messages)if(m.ruleId==='no-undef')c++;console.log('no-undef remaining:',c);"` should output 0. Also run `npm run build` to confirm no build errors introduced.</verify>
  <done>All 34 no-undef warnings eliminated across 9 files. Each fix is contextually correct (missing imports added, dead code removed, variable references corrected). Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx eslint . --format json` shows 0 no-undef warnings
- `npm run build` succeeds
- `npm test` passes (no regressions from variable fixes)
</verification>

<success_criteria>
Zero no-undef warnings. All 9 files have correct variable references. No functional regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/44-eslint-zero-warnings/44-02-SUMMARY.md`
</output>
