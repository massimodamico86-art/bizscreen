---
phase: 44-eslint-zero-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - src/hooks/useAdmin.js
  - src/components/security/PasswordStrengthIndicator.jsx
  - src/components/LanguageSwitcher.jsx
  - src/components/modals/InsertContentModal.jsx
  - src/components/scene-editor/EditorCanvas.jsx
  - src/components/svg-editor/AnimatePanel.jsx
  - src/components/svg-editor/FabricSvgEditor.jsx
  - src/services/complianceService.js
  - src/services/sceneDesignService.js
  - src/config/env.js
  - src/pages/DesignEditorPage.jsx
  - src/pages/LayoutsPage.jsx
  - src/pages/SvgEditorPage.jsx
  - src/supabase.js
autonomous: true

must_haves:
  truths:
    - "react/prop-types, jsdoc/*, and react-refresh/only-export-components rules are disabled (OFF) in ESLint config"
    - "no-case-declarations, no-useless-catch, no-useless-escape, no-console, and stale eslint-disable warnings are all fixed to zero"
    - "ESLint reports exactly 515 warnings remaining (356 unused-vars + 125 exhaustive-deps + 34 no-undef)"
  artifacts:
    - path: "eslint.config.js"
      provides: "Updated ESLint configuration with prop-types/jsdoc/react-refresh disabled"
      contains: "'react/prop-types': 'off'"
    - path: "src/hooks/useAdmin.js"
      provides: "All 7 no-useless-catch warnings fixed"
    - path: "src/components/svg-editor/FabricSvgEditor.jsx"
      provides: "Case declarations wrapped in blocks"
  key_links:
    - from: "eslint.config.js"
      to: "all source files"
      via: "ESLint rule configuration"
      pattern: "prop-types.*off"
---

<objective>
Overhaul ESLint config to disable impractical rules and fix all small-count warning categories.

Purpose: Eliminate 6,817 of 7,332 warnings by disabling rules that add no value (prop-types in a JS codebase without PropTypes, JSDoc enforcement on all exports, react-refresh for non-component exports) and manually fixing 42 straightforward code warnings.

Output: eslint.config.js updated, 13 source files fixed, warning count reduced from 7,332 to 515.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable impractical ESLint rules in config</name>
  <files>eslint.config.js</files>
  <action>
  In eslint.config.js, change these rules in the base config (files: ['**/*.{js,jsx}']):

  1. `'react/prop-types': 'warn'` -> `'react/prop-types': 'off'`
     Reason: PropTypes are deprecated in React 19+, this JS codebase has no PropTypes, adding them to 256 files would be busywork.

  2. `'jsdoc/require-jsdoc': ['warn', {...}]` -> `'jsdoc/require-jsdoc': 'off'`
  3. `'jsdoc/require-param': 'warn'` -> `'jsdoc/require-param': 'off'`
  4. `'jsdoc/require-returns': 'warn'` -> `'jsdoc/require-returns': 'off'`
     Reason: JSDoc enforcement on all public functions is aspirational but impractical for this codebase size.

  5. `'react-refresh/only-export-components': ['warn', { allowConstantExport: true }]` -> `'react-refresh/only-export-components': 'off'`
     Reason: Many files legitimately export constants alongside components (contexts, hooks, feature flags). Restructuring 23 files for HMR purity is not worth the churn.

  Also remove the now-unnecessary jsdoc rule overrides in the unit test and E2E test config blocks (the ones that set jsdoc rules to 'off'), since the base rules are already 'off'. Keep the prop-types 'off' overrides in test blocks for clarity even though base is now off.

  Run `npx eslint . --format json 2>/dev/null | node -e "..."` to verify prop-types/jsdoc/react-refresh warnings are now 0.
  </action>
  <verify>Run `npx eslint . 2>&1 | tail -1` -- should report ~515 warnings (down from 7,332). Specifically: `npx eslint . --format json` piped through node should show 0 warnings for react/prop-types, jsdoc/*, and react-refresh/only-export-components.</verify>
  <done>ESLint config has prop-types, jsdoc/*, and react-refresh/only-export-components set to 'off'. Running ESLint shows ~515 remaining warnings (no prop-types, jsdoc, or react-refresh warnings).</done>
</task>

<task type="auto">
  <name>Task 2: Fix all small-count warning categories</name>
  <files>
    src/hooks/useAdmin.js
    src/components/security/PasswordStrengthIndicator.jsx
    src/components/LanguageSwitcher.jsx
    src/components/modals/InsertContentModal.jsx
    src/components/scene-editor/EditorCanvas.jsx
    src/components/svg-editor/AnimatePanel.jsx
    src/components/svg-editor/FabricSvgEditor.jsx
    src/services/complianceService.js
    src/services/sceneDesignService.js
    src/config/env.js
    src/pages/DesignEditorPage.jsx
    src/pages/LayoutsPage.jsx
    src/pages/SvgEditorPage.jsx
    src/supabase.js
  </files>
  <action>
  Fix 42 warnings across 5 rules in 14 files:

  **no-useless-catch (7 warnings, 1 file):**
  - `src/hooks/useAdmin.js` lines 153, 167, 180, 193, 205, 219, 232: Remove the try/catch blocks entirely (they just rethrow). Keep the try body code inline.

  **no-useless-escape (1 warning, 1 file):**
  - `src/components/security/PasswordStrengthIndicator.jsx` line 94: Remove unnecessary backslash before `[`.

  **stale eslint-disable (1 warning, 1 file):**
  - `src/components/LanguageSwitcher.jsx` line 9: Remove the `// eslint-disable-next-line unused-imports/no-unused-imports` directive since no warning is being suppressed.

  **no-case-declarations (23 warnings, 6 files):**
  Wrap each case block containing `const`/`let` declarations in curly braces `{ }`:
  - `src/components/modals/InsertContentModal.jsx` lines 293, 297, 301, 305 (4 declarations)
  - `src/components/scene-editor/EditorCanvas.jsx` line 560 (1 declaration)
  - `src/components/svg-editor/AnimatePanel.jsx` lines 76, 93, 108, 109, 110 (5 declarations)
  - `src/components/svg-editor/FabricSvgEditor.jsx` lines 930-1024 (8 declarations across multiple cases)
  - `src/services/complianceService.js` lines 205, 214, 223, 232 (4 declarations)
  - `src/services/sceneDesignService.js` line 868 (1 declaration)

  For each case with declarations, wrap like:
  ```
  case 'value': {
    const x = ...;
    // existing code
    break;
  }
  ```

  **no-console (10 warnings, 5 files):**
  Replace `console.log` with `logger.info` or `logger.debug`. Each file should already import or have access to a logger. If not, add `import { createLogger } from '../services/loggingService';` and `const logger = createLogger('ModuleName');` at the top.
  - `src/config/env.js` line 228: Replace console.log with console.warn (env validation is appropriate for console.warn, which is allowed)
  - `src/pages/DesignEditorPage.jsx` lines 103, 122: Replace console.log with logger calls
  - `src/pages/LayoutsPage.jsx` lines 250, 277: Replace console.log with logger calls
  - `src/pages/SvgEditorPage.jsx` lines 73, 78, 99, 130: Replace console.log with logger calls
  - `src/supabase.js` line 55: Replace console.log with console.warn (Supabase init is appropriate for console.warn)

  Run `npx eslint . --format json` to verify these 5 rules now show 0 warnings.
  </action>
  <verify>Run `npx eslint . --format json 2>/dev/null | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));const rules=['no-case-declarations','no-useless-catch','no-useless-escape','no-console',null];let c=0;for(const f of d)for(const m of f.messages)if(rules.includes(m.ruleId))c++;console.log('Small rules remaining:',c);"` should output 0.</verify>
  <done>All 42 small-category warnings eliminated: no-case-declarations (0), no-useless-catch (0), no-useless-escape (0), no-console (0), stale eslint-disable (0). Total ESLint warnings reduced to ~515.</done>
</task>

</tasks>

<verification>
- `npx eslint . 2>&1 | tail -1` shows ~515 warnings remaining
- Zero warnings for: react/prop-types, jsdoc/*, react-refresh/only-export-components, no-case-declarations, no-useless-catch, no-useless-escape, no-console, stale eslint-disable
- Only remaining warning categories: unused-imports/no-unused-vars (356), react-hooks/exhaustive-deps (125), no-undef (34)
- `npm run build` still succeeds
</verification>

<success_criteria>
Warning count reduced from 7,332 to ~515. All impractical rules disabled. All small fixable rules at zero warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/44-eslint-zero-warnings/44-01-SUMMARY.md`
</output>
