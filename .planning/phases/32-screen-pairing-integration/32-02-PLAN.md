---
phase: 32-screen-pairing-integration
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/components/dashboard/ScreenPairingReminderCard.jsx
  - src/components/dashboard/index.js
  - src/pages/DashboardPage.jsx
autonomous: true

must_haves:
  truths:
    - "Skip creates dashboard card prompting user to return and complete pairing"
    - "Reminder card only shows when screen_pairing_completed_at is null but step was skipped/passed"
    - "Clicking reminder card navigates user to pair their screen"
  artifacts:
    - path: "src/components/dashboard/ScreenPairingReminderCard.jsx"
      provides: "Dashboard reminder for users who skipped screen pairing"
      min_lines: 40
  key_links:
    - from: "src/pages/DashboardPage.jsx"
      to: "ScreenPairingReminderCard"
      via: "import and conditional render"
      pattern: "<ScreenPairingReminderCard"
    - from: "src/components/dashboard/ScreenPairingReminderCard.jsx"
      to: "onboarding_progress table"
      via: "supabase query"
      pattern: "screen_pairing_completed_at"
---

<objective>
Create dashboard reminder card for users who skipped screen pairing during onboarding

Purpose: Ensure users who skip pairing have a clear path back to complete the activation metric

Output: ScreenPairingReminderCard component integrated into DashboardPage
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-screen-pairing-integration/32-01-SUMMARY.md
@src/pages/DashboardPage.jsx
@src/components/dashboard/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScreenPairingReminderCard component</name>
  <files>
    src/components/dashboard/ScreenPairingReminderCard.jsx
    src/components/dashboard/index.js
  </files>
  <action>
Create a new ScreenPairingReminderCard component:

1. **Data fetching logic:**
   - Query `onboarding_progress` table for current user
   - Check: `screen_pairing_completed_at IS NULL` AND `is_complete = true OR skipped_at IS NOT NULL`
   - This means: user completed/skipped onboarding but never paired a screen
   - Also check if user has ANY paired screens: `tv_devices WHERE is_paired = true`
   - If user has paired screens, don't show reminder

2. **Component structure:**
   ```jsx
   import { useState, useEffect } from 'react';
   import { Monitor, ArrowRight, X } from 'lucide-react';
   import { useNavigate } from 'react-router-dom';
   import { supabase } from '../../supabase';
   import { Button, Card } from '../../design-system';
   ```

3. **UI design (matches OnboardingBanner pattern):**
   - Card with gradient left border (teal)
   - Monitor icon on left
   - Heading: "Connect Your First Screen"
   - Description: "Pair a TV or display to start showing your content"
   - CTA button: "Set Up Screen" -> navigates to /screens (or opens pairing modal)
   - Dismiss button (X) in top-right corner

4. **Dismiss behavior:**
   - Store dismissed state in localStorage: `bizscreen_pairing_reminder_dismissed`
   - Don't show card if dismissed
   - Could reset dismissal after 7 days (optional, use timestamp)

5. **Navigation:**
   - "Set Up Screen" button navigates to `/screens`
   - Alternatively, could trigger AddScreenModal directly (simpler: just navigate)

6. **Export from index:**
   - Add export to `src/components/dashboard/index.js`
  </action>
  <verify>
File exists at src/components/dashboard/ScreenPairingReminderCard.jsx
Component exported from src/components/dashboard/index.js
npm run build succeeds
  </verify>
  <done>
ScreenPairingReminderCard component created with proper data fetching, UI, and dismiss behavior
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ScreenPairingReminderCard into DashboardPage</name>
  <files>
    src/pages/DashboardPage.jsx
  </files>
  <action>
Add ScreenPairingReminderCard to DashboardPage:

1. **Import the component:**
   ```javascript
   import { ScreenPairingReminderCard } from '../components/dashboard';
   ```

2. **Position in layout:**
   - Place above the main dashboard grid
   - After OnboardingBanner (if present)
   - Before ActivityLogCard and other widgets

3. **No additional state needed:**
   - ScreenPairingReminderCard handles its own visibility logic internally
   - Just render it unconditionally and let it self-determine visibility

4. **Code placement:**
   ```jsx
   {/* Screen pairing reminder for users who skipped */}
   <ScreenPairingReminderCard />

   {/* Existing dashboard content */}
   <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
     ...
   </div>
   ```

5. **Ensure no duplicate prompts:**
   - OnboardingBanner handles unified onboarding resume
   - ScreenPairingReminderCard only shows for completed/skipped onboarding without paired screen
   - These are mutually exclusive scenarios
  </action>
  <verify>
npm run build succeeds
Grep for 'ScreenPairingReminderCard' in DashboardPage.jsx returns match
  </verify>
  <done>
ScreenPairingReminderCard renders on dashboard for users who skipped screen pairing
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds without errors
2. ScreenPairingReminderCard.jsx exists and is exported from dashboard/index.js
3. DashboardPage.jsx imports and renders ScreenPairingReminderCard
4. Card only shows when: onboarding skipped/complete AND screen_pairing_completed_at is null AND no paired screens
5. Dismiss button hides card via localStorage
6. "Set Up Screen" button navigates to /screens
</verification>

<success_criteria>
- Reminder card appears on dashboard for users who skipped screen pairing
- Card has clear CTA to set up a screen
- Card is dismissible (stored in localStorage)
- Card does not appear for users who have paired screens
- Card does not appear for users actively in onboarding
</success_criteria>

<output>
After completion, create `.planning/phases/32-screen-pairing-integration/32-02-SUMMARY.md`
</output>
