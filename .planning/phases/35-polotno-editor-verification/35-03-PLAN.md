---
phase: 35-polotno-editor-verification
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - src/components/EditorModal.jsx
  - src/components/PostSaveDialog.jsx
autonomous: true

must_haves:
  truths:
    - "After successful save, user sees dialog asking 'Keep editing or view your template?'"
    - "Keep Editing closes dialog and returns to editor"
    - "View Template closes modal and navigates to media library"
    - "User controls post-save navigation rather than auto-close"
  artifacts:
    - path: "src/components/PostSaveDialog.jsx"
      provides: "Dialog shown after save with Keep Editing / View Template options"
      min_lines: 40
    - path: "src/components/EditorModal.jsx"
      provides: "Shows PostSaveDialog after successful save"
      contains: "PostSaveDialog"
  key_links:
    - from: "src/components/EditorModal.jsx"
      to: "src/components/PostSaveDialog.jsx"
      via: "State management"
      pattern: "showPostSaveDialog"
---

<objective>
Implement post-save user choice dialog per CONTEXT.md: "After successful save, ask user: 'Keep editing or view your template?'"

Purpose: CONTEXT.md specifies "User controls post-save navigation rather than auto-close" - some users want to continue editing, others want to see their saved work.

Output: PostSaveDialog component, EditorModal integration showing dialog after save success.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-polotno-editor-verification/35-CONTEXT.md
@.planning/phases/35-polotno-editor-verification/35-RESEARCH.md
@.planning/phases/35-polotno-editor-verification/35-01-SUMMARY.md

Key files:
@src/components/EditorModal.jsx
@src/design-system/components/Modal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostSaveDialog component</name>
  <files>src/components/PostSaveDialog.jsx</files>
  <action>
Create PostSaveDialog.jsx - shown after successful save:

1. Import Modal from design-system/components/Modal
2. Accept props: open, onKeepEditing, onViewTemplate, savedDesignName
3. Modal content:
   - Success icon (CheckCircle from lucide-react, green)
   - Title: "Design Saved!"
   - Description: "Your design '{savedDesignName}' has been saved to the media library."
   - If savedDesignName is not provided, use generic: "Your design has been saved..."
4. Two buttons:
   - "Keep Editing" (primary, teal) - calls onKeepEditing
   - "View My Template" (secondary, gray outline) - calls onViewTemplate
5. Use size="sm" for the modal

Button layout: Stack vertically with Keep Editing first (most common action)

Style notes:
- Green checkmark icon in a light green circle background
- Centered text alignment
- Buttons full width, stacked with gap-2
  </action>
  <verify>
File exists: src/components/PostSaveDialog.jsx
grep -n "onKeepEditing\|onViewTemplate" src/components/PostSaveDialog.jsx
  </verify>
  <done>
PostSaveDialog.jsx exists with success message and two action buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate PostSaveDialog into EditorModal</name>
  <files>src/components/EditorModal.jsx</files>
  <action>
Update EditorModal.jsx to show PostSaveDialog after successful save:

1. Import PostSaveDialog from './PostSaveDialog'
2. Add state:
   - const [showPostSaveDialog, setShowPostSaveDialog] = useState(false)
   - const [savedDesignName, setSavedDesignName] = useState(null)
3. Update handleSave (or create if not exists):
   - Call the actual save operation (via onSave prop or inline)
   - On success:
     - Set savedDesignName to the design name
     - Set showPostSaveDialog to true
     - Clear dirty state if tracking (from Plan 02)
   - On failure:
     - Show error toast
     - Keep modal open for retry
4. Add handlers:
   - handleKeepEditing: setShowPostSaveDialog(false), stay in editor
   - handleViewTemplate: setShowPostSaveDialog(false), call onClose, navigate to media library
5. Pass onNavigate prop or callback to handle navigation to media library

Render PostSaveDialog:
```jsx
<PostSaveDialog
  open={showPostSaveDialog}
  onKeepEditing={handleKeepEditing}
  onViewTemplate={handleViewTemplate}
  savedDesignName={savedDesignName}
/>
```

The "View My Template" should navigate to media-images page where saved designs appear.
Use onNavigate prop: onNavigate?.('media-images') or window.location.href = '/app#media-images'
  </action>
  <verify>
grep -n "PostSaveDialog\|showPostSaveDialog" src/components/EditorModal.jsx
grep -n "handleKeepEditing\|handleViewTemplate" src/components/EditorModal.jsx
  </verify>
  <done>
EditorModal shows PostSaveDialog after successful save with Keep Editing and View Template options.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run lint` - no new errors
2. Run `npm run build` - builds successfully
3. Manual test: Open editor, make changes, save
4. Verify PostSaveDialog appears with success message
5. Test "Keep Editing" - dialog closes, editor still open
6. Test "View My Template" - modal closes, navigates to media library
</verification>

<success_criteria>
- PostSaveDialog.jsx exists with proper UI
- EditorModal shows dialog after successful save
- Keep Editing returns to editor
- View Template navigates to media library
- User chooses their post-save destination
</success_criteria>

<output>
After completion, create `.planning/phases/35-polotno-editor-verification/35-03-SUMMARY.md`
</output>
