---
phase: 35-polotno-editor-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PolotnoEditor.jsx
  - src/components/EditorModal.jsx
  - src/pages/LayoutsPage.jsx
autonomous: true

must_haves:
  truths:
    - "Editor opens in modal overlay instead of full-page navigation"
    - "Loading state shows within 10 seconds before timeout error"
    - "Error state shows retry button and Design Studio fallback link"
    - "Template click from LayoutsPage opens modal without page navigation"
  artifacts:
    - path: "src/components/EditorModal.jsx"
      provides: "Modal wrapper for Polotno editor with loading/error states"
      min_lines: 100
    - path: "src/components/PolotnoEditor.jsx"
      provides: "Updated timeout from 30s to 10s, receives retry callback"
      contains: "10000"
  key_links:
    - from: "src/pages/LayoutsPage.jsx"
      to: "src/components/EditorModal.jsx"
      via: "Modal state and template data"
      pattern: "EditorModal.*isOpen"
    - from: "src/components/EditorModal.jsx"
      to: "src/components/PolotnoEditor.jsx"
      via: "Props passing"
      pattern: "PolotnoEditor"
---

<objective>
Create modal overlay wrapper for Polotno editor with proper loading timeout (10s) and error recovery states.

Purpose: CONTEXT.md specifies "Editor opens in modal overlay (not full-page navigation)" to maintain user context. Users can return to where they were without losing their place.

Output: EditorModal.jsx component wrapping PolotnoEditor, updated LayoutsPage to use modal instead of navigation, 10-second timeout with retry/fallback UI.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-polotno-editor-verification/35-CONTEXT.md
@.planning/phases/35-polotno-editor-verification/35-RESEARCH.md

Key files:
@src/components/PolotnoEditor.jsx
@src/pages/LayoutsPage.jsx
@src/design-system/components/Modal.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditorModal wrapper component</name>
  <files>src/components/EditorModal.jsx</files>
  <action>
Create EditorModal.jsx that wraps PolotnoEditor in the design system Modal component:

1. Import Modal from design-system/components/Modal.jsx
2. Accept props: isOpen, onClose, templateData, onSave, showToast
3. Use size="full" for the Modal to maximize editor space
4. Set closeOnOverlay={false} to prevent accidental close (per Claude's discretion)
5. Set closeOnEscape={false} initially (will add confirm dialog in Plan 02)
6. Render PolotnoEditor inside the modal with all necessary props
7. Track loading/error state at this level
8. Handle close via a handleClose function that calls onClose (will add dirty check in Plan 02)

Loading state overlay (shown while isLoading=true):
- Center spinner with "Loading design editor..." text
- Use Loader2 from lucide-react (project standard)
- Template name visible if provided (Claude's discretion: show template info during load)

Error state (when error is set):
- Red error icon (AlertCircle from lucide-react)
- Error title and message
- Primary: "Try Again" button that triggers retry
- Secondary: "Open Design Studio" link to /layouts (per CONTEXT.md fallback)
- Tertiary: "Contact Support" link (per CONTEXT.md)

Pass onRetry callback to PolotnoEditor so it can reset and reload.
  </action>
  <verify>
File exists at src/components/EditorModal.jsx with:
- Modal import from design-system
- LoadingOverlay component or inline loading UI
- ErrorState with retry button and Design Studio link
- Props interface matching requirements
  </verify>
  <done>
EditorModal.jsx exists with Modal wrapper, loading overlay, error state with retry/fallback, and PolotnoEditor rendering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PolotnoEditor with 10s timeout and retry support</name>
  <files>src/components/PolotnoEditor.jsx</files>
  <action>
Modify PolotnoEditor.jsx to:

1. Change timeout from 30000 (30s) to 10000 (10s) per CONTEXT.md decision
2. Add onError callback prop to notify parent of timeout/errors
3. Add onReady callback prop to notify parent when editor is ready
4. Add optional onRetry callback prop - when called:
   - Reset error state
   - Reset loading state to true
   - Force iframe reload by updating src with cache-busting param

Keep the existing error UI as fallback but prefer the parent handling errors via onError callback.

The component should:
- Call onError({ type: 'timeout', message: '...' }) on timeout
- Call onReady() when 'ready' message received from iframe
- Support onRetry being called to reload

Update the useEffect timeout to use the new 10000ms value.
  </action>
  <verify>
grep -n "10000" src/components/PolotnoEditor.jsx shows timeout is 10 seconds
grep -n "onError\|onReady\|onRetry" src/components/PolotnoEditor.jsx shows new props exist
  </verify>
  <done>
PolotnoEditor.jsx has 10-second timeout and supports onError, onReady, onRetry callbacks.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update LayoutsPage to use EditorModal instead of navigation</name>
  <files>src/pages/LayoutsPage.jsx</files>
  <action>
Modify LayoutsPage.jsx to open EditorModal instead of navigating:

1. Import EditorModal from '../components/EditorModal'
2. Add state: const [editorModal, setEditorModal] = useState({ isOpen: false, templateData: null })
3. Modify handleTemplateClick to:
   - Instead of calling onNavigate(`design-editor?template=...`)
   - Set editorModal state: { isOpen: true, templateData: { id, name, thumbnail, orientation, width, height } }
   - Keep the existing template data preparation logic

4. Add handleEditorClose function:
   - Set editorModal.isOpen to false
   - Clear templateData

5. Add handleEditorSave function:
   - Call the save logic (similar to DesignEditorPage.handleSave)
   - Or pass through to a shared save function
   - Show toast on success
   - Keep modal open (Plan 03 will add post-save choice)

6. Render EditorModal at the bottom of the component:
   ```jsx
   <EditorModal
     isOpen={editorModal.isOpen}
     onClose={handleEditorClose}
     templateData={editorModal.templateData}
     onSave={handleEditorSave}
     showToast={showToast}
   />
   ```

Keep the "New Design" button behavior - it can still navigate to design-editor for blank designs, or optionally also use the modal with no templateData.
  </action>
  <verify>
grep -n "EditorModal" src/pages/LayoutsPage.jsx shows import and usage
grep -n "editorModal" src/pages/LayoutsPage.jsx shows state management
Clicking a template in LayoutsPage should open modal, not navigate (manual verification)
  </verify>
  <done>
LayoutsPage.jsx opens EditorModal when clicking templates instead of navigating to a new page.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run lint` - no new errors in modified files
2. Run `npm run build` - builds successfully
3. Manual test: Navigate to Layouts page, click a template, verify modal opens (not page navigation)
4. Manual test: Wait 10+ seconds with editor loading - verify timeout error appears
5. Manual test: Click "Try Again" in error state - verify reload attempt
6. Manual test: Verify "Open Design Studio" link in error state navigates correctly
</verification>

<success_criteria>
- EditorModal.jsx exists and renders PolotnoEditor in a full-screen modal
- PolotnoEditor timeout is 10 seconds (not 30)
- LayoutsPage template click opens modal instead of navigating
- Error state shows retry button and Design Studio fallback
- All modified files pass lint and build
</success_criteria>

<output>
After completion, create `.planning/phases/35-polotno-editor-verification/35-01-SUMMARY.md`
</output>
