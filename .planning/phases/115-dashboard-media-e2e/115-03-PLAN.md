---
phase: 115-dashboard-media-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/media-advanced-screenshots.spec.js
autonomous: true
requirements:
  - MEDIA-07
  - MEDIA-08
  - MEDIA-09
  - MEDIA-10

must_haves:
  truths:
    - "Running media-advanced-screenshots.spec.js produces a screenshot of bulk selection mode with checkboxes"
    - "Running media-advanced-screenshots.spec.js produces a screenshot of the folder creation modal"
    - "Running media-advanced-screenshots.spec.js produces a screenshot showing the storage usage bar with quota info"
    - "Running media-advanced-screenshots.spec.js produces screenshots of all 5 media sub-pages (Images, Videos, Audio, Documents, Web Pages)"
  artifacts:
    - path: "tests/e2e/media-advanced-screenshots.spec.js"
      provides: "Media library advanced E2E screenshot test suite"
      min_lines: 100
  key_links:
    - from: "tests/e2e/media-advanced-screenshots.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import { loginAndPrepare, navigateToSection, waitForPageReady }"
      pattern: "import.*helpers"
---

<objective>
Create screenshot-verified E2E tests for media library advanced operations: bulk select/delete, folder creation, storage usage bar, and all 5 media sub-pages.

Purpose: Complete the media library E2E coverage with bulk operations, organization, and sub-page verification.
Output: tests/e2e/media-advanced-screenshots.spec.js covering MEDIA-07 through MEDIA-10.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@tests/e2e/helpers.js
@tests/e2e/media.spec.js
@playwright.config.js
</context>

<interfaces>
<!-- Existing helpers and media page patterns -->
From tests/e2e/helpers.js:
```javascript
export async function loginAndPrepare(page, options = {})
export async function navigateToSection(page, section) // 'media' -> expands sidebar > "All Media"
export async function waitForPageReady(page)
```

Media page DOM patterns (from existing media.spec.js):
- Media heading in main: `main` > heading /all media/i
- Add Media button: `button:has-text("Add Media")`
- Search: `getByPlaceholder(/search media/i)`
- Grid items: `.aspect-video`
- View toggle: `.flex.border.rounded-lg`
- Storage bar component: `StorageUsageBar` renders a progress bar with usage text
- Folder creation: look for "Create Folder" or "New Folder" button
- Bulk select: look for checkbox inputs on media cards, or a "Select" toggle button
- Sub-pages in sidebar under Media: "All Media", "Images", "Videos", "Audio", "Documents", "Web Pages"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create media advanced screenshot E2E spec</name>
  <files>tests/e2e/media-advanced-screenshots.spec.js</files>
  <action>
Create `tests/e2e/media-advanced-screenshots.spec.js`. Use `chromium` project only, skip if `TEST_USER_EMAIL` not set.

Use `beforeEach` to `loginAndPrepare` with client credentials.

Create these test cases:

**MEDIA-07: Bulk select and bulk delete**
- Navigate to media via `navigateToSection(page, 'media')`
- Look for a "Select" or "Bulk Select" toggle button, or checkboxes that appear on hover/click
- If media items exist: try to enter selection mode (click select button or checkbox on first item)
- Screenshot showing selection checkboxes: `screenshots/115-12-media-bulk-select.png`
- If a bulk action bar appears (with "Delete Selected" or similar), screenshot it: `screenshots/115-12-media-bulk-actions-bar.png`
- If no media or no bulk select available: skip with message, screenshot empty state

**MEDIA-08: Folder creation**
- Navigate to media
- Look for "Create Folder" or "New Folder" button (may be in action bar, toolbar, or alongside Add Media)
- Click it to open folder creation modal/dialog
- Screenshot the modal: `screenshots/115-13-media-create-folder-modal.png`
- If the button is in a dropdown/menu, screenshot the menu first, then the modal
- Close the modal without creating
- If no folder creation button found: look for it in the "Add Media" modal or bottom action bar, or check if there is a folder icon button. Skip with message if truly not available.

**MEDIA-09: Storage usage bar**
- Navigate to media
- Look for the storage usage bar component -- it shows a progress bar with text like "X MB / Y MB" or "X% used"
- The StorageUsageBar component renders at the bottom of the media sidebar or within the media page
- Screenshot the storage bar area: `screenshots/115-14-media-storage-bar.png`
- If storage bar is not visible on the page, check if it appears in the sidebar under the media navigation section
- Take a full-page screenshot if needed to capture it: `screenshots/115-14-media-storage-fullpage.png`

**MEDIA-10: All 5 media sub-pages**
- From the media section, navigate to each sub-page by expanding the Media sidebar section and clicking each sub-item
- For each sub-page: Images, Videos, Audio, Documents, Web Pages:
  1. Click the corresponding sidebar button
  2. Wait for the page heading to update
  3. Verify the heading matches the sub-page name
  4. Screenshot: `screenshots/115-15-media-subpage-{name}.png` (lowercase name)
- This confirms that type filtering works correctly by showing filtered content on each sub-page

For sub-page navigation: The Media sidebar section has an expandable menu. After expanding, the sub-items should be: "All Media", "Images", "Videos", "Audio", "Documents", "Web Pages". Click each one. If the sidebar structure uses different text, inspect with `page.locator('aside button').allTextContents()` and adapt.

Mark `test.slow()` on the sub-pages test since it navigates 5 pages.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/media-advanced-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 media advanced tests pass (or skip gracefully), producing screenshot files 115-12 through 115-15</done>
</task>

<task type="auto">
  <name>Task 2: Verify all media advanced screenshots and run full suite</name>
  <files>tests/e2e/media-advanced-screenshots.spec.js</files>
  <action>
Run the full media advanced spec and verify:

1. Run `npx playwright test tests/e2e/media-advanced-screenshots.spec.js --project=chromium`
2. Verify screenshot files exist:
   - screenshots/115-12-media-bulk-select.png (or skip screenshot)
   - screenshots/115-13-media-create-folder-modal.png (or skip)
   - screenshots/115-14-media-storage-bar.png
   - screenshots/115-15-media-subpage-images.png
   - screenshots/115-15-media-subpage-videos.png
   - screenshots/115-15-media-subpage-audio.png
   - screenshots/115-15-media-subpage-documents.png
   - screenshots/115-15-media-subpage-web-pages.png (or "webpages")

3. Then run ALL three phase 115 specs together to confirm no conflicts:
   ```
   npx playwright test tests/e2e/dashboard-screenshots.spec.js tests/e2e/media-screenshots.spec.js tests/e2e/media-advanced-screenshots.spec.js --project=chromium
   ```

4. Fix any failures. Common issues:
   - Sub-page buttons may have slightly different text -- use `page.locator('aside').locator('button').allTextContents()` to discover exact text
   - Storage bar might be in a collapsible section
   - Bulk select may require a long-press or special mode toggle

All three specs should pass together without interference.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/dashboard-screenshots.spec.js tests/e2e/media-screenshots.spec.js tests/e2e/media-advanced-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -5</automated>
  </verify>
  <done>All three Phase 115 spec files pass together, at least 20 total screenshot files exist in screenshots/115-* pattern</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/*-screenshots.spec.js --project=chromium` all pass
- `ls screenshots/115-*.png | wc -l` shows at least 20 files
- All 15 requirements (DASH-01 to DASH-05, MEDIA-01 to MEDIA-10) have screenshot evidence
</verification>

<success_criteria>
- 4 MEDIA requirements (07-10) have passing E2E tests with screenshots
- All 5 media sub-pages navigated and screenshotted
- Combined run of all 3 phase specs passes without conflicts
- No modifications to existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/115-dashboard-media-e2e/115-03-SUMMARY.md`
</output>
