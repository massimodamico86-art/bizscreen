---
phase: 118-templates-schedules-campaigns-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/schedules-screenshots.spec.js
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06

must_haves:
  truths:
    - "Running the schedules E2E spec produces screenshots of schedule list with create/delete actions"
    - "Running the schedules E2E spec captures schedule creation with time range and day selection"
    - "Running the schedules E2E spec captures schedule editor with playlist/layout assignment"
    - "Running the schedules E2E spec captures conflict detection warning"
    - "Running the schedules E2E spec captures dayparting preset"
    - "Running the schedules E2E spec captures recurring schedule entry"
  artifacts:
    - path: "tests/e2e/schedules-screenshots.spec.js"
      provides: "Schedule screenshot E2E tests"
      min_lines: 120
    - path: "screenshots/118/"
      provides: "Screenshot evidence for SCHED-01 through SCHED-06"
  key_links:
    - from: "tests/e2e/schedules-screenshots.spec.js"
      to: "SchedulesPage"
      via: "sidebar navigation to 'schedules' page"
      pattern: "navigateToSection.*schedules"
    - from: "tests/e2e/schedules-screenshots.spec.js"
      to: "ScheduleEditorPage"
      via: "__setCurrentPage('schedule-editor-{id}')"
      pattern: "__setCurrentPage.*schedule-editor"
---

<objective>
Create Playwright E2E screenshot tests for schedule management covering all 6 SCHED requirements: list CRUD, time/day creation, playlist/layout assignment, conflict detection, dayparting, and recurring entries.

Purpose: Provide screenshot evidence that schedule management workflows work end-to-end.
Output: tests/e2e/schedules-screenshots.spec.js with 6 test cases producing screenshots in screenshots/118/
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/e2e/helpers.js
@tests/e2e/helpers/screenshots.js
@tests/e2e/schedules.spec.js
@src/pages/SchedulesPage.jsx
@src/pages/ScheduleEditorPage.jsx

<interfaces>
<!-- Navigation patterns -->
- Schedules page: sidebar nav item 'schedules' -> SchedulesPage
- Schedule editor: window.__setCurrentPage('schedule-editor-{uuid}') -> ScheduleEditorPage
- SchedulesPage has: search, "Add Schedule" button, create modal (name + description), action menu (edit, duplicate, delete)
- ScheduleEditorPage has: WeekPreview, FillerContentPicker, AssignScreensModal, ConflictWarning, DaypartPicker, DateDurationPicker, PriorityBadge, CampaignPicker
- REPEAT_OPTIONS: none, daily, weekday, weekly, monthly, yearly, custom
- Schedule creation may show limit modal if plan limit reached
- Use screenshotStep(page, '118', 'NN-description') for consistent naming
- Use page.route() to mock Supabase APIs if needed (pattern from phase 117)
- Handle backend-unavailable gracefully
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schedules screenshot E2E spec</name>
  <files>tests/e2e/schedules-screenshots.spec.js</files>
  <action>
Create tests/e2e/schedules-screenshots.spec.js with 6 test cases covering SCHED-01 through SCHED-06.

Structure:
- Import { test, expect } from '@playwright/test'
- Import { loginAndPrepare, navigateToSection, waitForPageReady, dismissAnyModals } from './helpers.js'
- Import { screenshotStep } from './helpers/screenshots.js'
- test.describe('Schedules Screenshots') with beforeEach that skips non-chromium and logs in

Test cases:
1. SCHED-01 (list page with create/delete): Navigate to 'schedules' via sidebar. Wait for page heading. Screenshot the schedules list page (may show empty state or list). If "Add Schedule" button visible, click it to open create modal. Screenshot the modal. Close modal. If any schedule has an action menu (MoreVertical icon), click it and screenshot showing delete option.

2. SCHED-02 (creation with time range and day selection): On schedules page, click "Add Schedule" button. Fill in name field in the create modal (scope to [role="dialog"] to avoid search bar ambiguity). Use dispatchEvent if modal overlay intercepts clicks. Screenshot the filled create form. If form has time/day fields, fill them. Otherwise, submit to create and navigate to editor where time/day fields exist. Screenshot time range and day selection UI.

3. SCHED-03 (editor with playlist/layout assignment): Navigate to schedule editor. If no schedule exists, try creating one first. Use page.route() to mock the Supabase REST API for schedules if backend unavailable (return mock schedule data). In the editor, look for playlist/layout assignment section or "Assign" button. Screenshot the editor page showing assignment options.

4. SCHED-04 (conflict detection warning): In the schedule editor, look for ConflictWarning component or overlapping time entries. If visible, screenshot. If not visible (no conflicts exist), create overlapping entries or screenshot the editor noting conflict detection is inactive. This may require mocking schedule entries with overlapping times via page.route().

5. SCHED-05 (dayparting preset): In the schedule editor, look for DaypartPicker component or daypart-related UI (breakfast, lunch, dinner presets). If visible, click to select a daypart and screenshot. If not immediately visible, look for an "Add Entry" or time slot creation UI that includes daypart options.

6. SCHED-06 (recurring schedule entry): In the schedule editor, look for repeat/recurrence options (the REPEAT_OPTIONS dropdown: daily, weekly, monthly, etc.). If an entry exists, click its repeat dropdown. Screenshot showing the recurrence options. If no entry exists, look for "Add Entry" button to create one showing repeat options.

Key patterns:
- Use screenshotStep(page, '118', '10-schedules-list') naming with 10-15 range to avoid collision with template screenshots (01-08)
- Handle missing backend gracefully with page.route() mocking if needed
- Use dispatchEvent for modal button clicks that get intercepted by overlay (pattern from phase 117)
- Scope modal inputs to [role="dialog"] to avoid ambiguity
- For schedule editor navigation: page.evaluate((id) => window.__setCurrentPage('schedule-editor-' + id), 'new')
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/schedules-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 SCHED test cases pass (or skip gracefully with screenshots) producing screenshot files in screenshots/118/</done>
</task>

<task type="auto">
  <name>Task 2: Verify all schedule screenshots exist</name>
  <files>screenshots/118/</files>
  <action>
Run the schedules screenshot spec and verify that screenshot files are produced in screenshots/118/ directory. List the files to confirm at least 6 new screenshots exist for schedule tests (118-10 through 118-15 range). If any test failed in Task 1, review the error and fix the spec.

Verification: ls screenshots/118/118-1* | wc -l should show >= 6 files.
  </action>
  <verify>
    <automated>ls /Users/massimodamico/bizscreen/screenshots/118/118-1* 2>/dev/null | wc -l | grep -E '^[6-9]|^[1-9][0-9]'</automated>
  </verify>
  <done>At least 6 screenshot files exist in screenshots/118/ covering SCHED-01 through SCHED-06</done>
</task>

</tasks>

<verification>
- All 6 SCHED requirements have corresponding test cases in schedules-screenshots.spec.js
- Tests pass on chromium project without crashes
- Screenshots are saved to screenshots/118/ with consistent naming (10+ range)
- Tests handle backend-unavailable gracefully
</verification>

<success_criteria>
- schedules-screenshots.spec.js contains 6 test cases (SCHED-01 through SCHED-06)
- All tests pass or skip gracefully
- At least 6 screenshot files exist in screenshots/118/
</success_criteria>

<output>
After completion, create `.planning/phases/118-templates-schedules-campaigns-e2e/118-02-SUMMARY.md`
</output>
