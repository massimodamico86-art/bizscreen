---
phase: 87-data-sources-apps-moderation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AppsPage.jsx
  - src/components/apps/AppDetailModal.jsx
  - src/pages/MenuBoardsPage.jsx
  - src/components/menu-boards/MenuBoardEditorModal.jsx
autonomous: false
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "AppsPage loads the app gallery with category filter, search, and sort controls"
    - "User can select a catalog app, see the detail modal, and open the configuration modal via Use App"
    - "All six app type config modals (Clock, WebPage, Weather, RssTicker, DataTable, GenericEmbed) open and accept configuration"
    - "User can edit an existing app instance and the edit modal pre-populates with saved config values"
    - "User can delete an app instance with confirmation"
    - "MenuBoardsPage loads a card grid of menu boards with create, edit, duplicate, and delete actions"
    - "MenuBoardEditorModal supports category and item CRUD with drag-and-drop reordering"
  artifacts:
    - path: "src/pages/AppsPage.jsx"
      provides: "App gallery, category browsing, search, 6 config modals, user app CRUD"
    - path: "src/components/apps/AppDetailModal.jsx"
      provides: "App preview modal with Use App action"
    - path: "src/pages/MenuBoardsPage.jsx"
      provides: "Menu board list with card grid, create/edit/duplicate/delete"
    - path: "src/components/menu-boards/MenuBoardEditorModal.jsx"
      provides: "Full CRUD editor for menu board categories and items with DnD"
  key_links:
    - from: "src/pages/AppsPage.jsx"
      to: "src/services/mediaService.js"
      via: "fetchApps, createClockApp, createWebPageApp, createWeatherApp, createRssTickerApp, createDataTableApp, deleteApp, updateAppConfig"
      pattern: "import.*from.*mediaService"
    - from: "src/pages/AppsPage.jsx"
      to: "src/config/appCatalog.js"
      via: "APP_CATEGORIES, FEATURED_APPS, INDUSTRY_POPULAR, getAppsByCategory, searchApps, sortAppsAlphabetically, getAppById"
      pattern: "import.*from.*appCatalog"
    - from: "src/pages/AppsPage.jsx"
      to: "src/components/apps"
      via: "AppCard, AppDetailModal, WeatherWallConfigModal"
      pattern: "import.*from.*components/apps"
    - from: "src/pages/MenuBoardsPage.jsx"
      to: "src/services/menuBoardService.js"
      via: "fetchMenuBoards, deleteMenuBoard, getMenuBoard, createMenuBoard, createCategory, createMenuItem"
      pattern: "import.*from.*menuBoardService"
    - from: "src/components/menu-boards/MenuBoardEditorModal.jsx"
      to: "src/services/menuBoardService.js"
      via: "getMenuBoard, createMenuBoard, updateMenuBoard, createCategory, updateCategory, deleteCategory, createMenuItem, updateMenuItem, deleteMenuItem, toggleItemAvailability, reorderCategories, reorderItems"
      pattern: "import.*from.*menuBoardService"
---

<objective>
Audit and verify AppsPage (app gallery + 6 config modals + edit with pre-populated values) and MenuBoardsPage (card grid + MenuBoardEditorModal with category/item CRUD and drag-and-drop reordering).

Purpose: Ensure all six app types can be added and edited with pre-populated modals (DATA-02), and menu board categories/items support CRUD with drag-and-drop reordering (DATA-03).
Output: Verified AppsPage and MenuBoardsPage with all imports correct, CRUD operations wired, and edit modals pre-populating existing values.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/pages/AppsPage.jsx
@src/components/apps/AppDetailModal.jsx
@src/components/apps/index.js
@src/pages/MenuBoardsPage.jsx
@src/components/menu-boards/MenuBoardEditorModal.jsx
@src/services/menuBoardService.js
@src/config/appCatalog.js

<interfaces>
<!-- AppsPage service imports -->
From src/services/mediaService.js:
- fetchApps() → Array of user-created app instances
- createClockApp(config) → Created app
- createWebPageApp(config) → Created app
- createWeatherApp(config) → Created app
- createRssTickerApp(config) → Created app
- createDataTableApp(config) → Created app
- deleteApp(id) → void
- updateAppConfig(id, config) → Updated app
- APP_TYPE_KEYS: { CLOCK, WEB_PAGE, WEATHER, RSS_TICKER, DATA_TABLE }

From src/config/appCatalog.js:
- APP_CATEGORIES: Array of { id, label, icon }
- FEATURED_APPS: Array of app catalog entries
- INDUSTRY_POPULAR: { [industry]: { label, appIds } }
- getAppsByCategory(categoryId) → Array
- searchApps(query) → Array
- sortAppsAlphabetically(apps) → Array
- getAppById(id) → app | null

From src/components/apps:
- AppCard, AppCardSkeleton, AppDetailModal, WeatherWallConfigModal

<!-- MenuBoardsPage service imports -->
From src/services/menuBoardService.js:
- fetchMenuBoards() → Array
- deleteMenuBoard(id) → void
- getMenuBoard(id) → Board with categories and items
- createMenuBoard(config) → Board
- createCategory(boardId, { name, description }) → Category
- createMenuItem(categoryId, boardId, config) → Item
- updateMenuBoard, updateCategory, deleteCategory
- updateMenuItem, deleteMenuItem, toggleItemAvailability
- reorderCategories(boardId, orderedIds) → void
- reorderItems(categoryId, orderedIds) → void

Design-system Button: variant="secondary" (no "outline"). Badge from design-system.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppsPage and MenuBoardsPage import audit and functional wiring verification</name>
  <files>src/pages/AppsPage.jsx, src/components/apps/AppDetailModal.jsx, src/pages/MenuBoardsPage.jsx, src/components/menu-boards/MenuBoardEditorModal.jsx</files>
  <action>
**AppsPage audit (~680 lines + 5 inline modal components):**

1. **Import verification:**
   - Verify lucide-react imports: Clock, CloudSun, Edit, Globe, Grid3X3, LinkIcon, Loader2, MoreVertical, Rss, Search, Table, Trash2, X. Remove any unused.
   - Verify design-system imports: Card, Button. Confirm these are the only design-system components used (AppsPage uses raw HTML for most layout).
   - Verify components/apps imports: AppCard, AppDetailModal, WeatherWallConfigModal.
   - Verify mediaService imports: fetchApps, createClockApp, createWebPageApp, createWeatherApp, createRssTickerApp, createDataTableApp, deleteApp, updateAppConfig, APP_TYPE_KEYS.
   - Verify appCatalog imports: APP_CATEGORIES, FEATURED_APPS, INDUSTRY_POPULAR, getAppsByCategory, searchApps, sortAppsAlphabetically, getAppById.
   - Note: `Icon` import from lucide-react in AppDetailModal.jsx (line 12) is dead code (shadowed by `const Icon = app.icon` on line 41). Remove the unused `Icon` import from lucide-react.

2. **App gallery wiring:**
   - Confirm `filteredApps` uses `searchApps(search)` when search is active, else `getAppsByCategory(activeCategory)`.
   - Confirm `handleAppSelect` sets `selectedApp` and opens `showDetailModal`.
   - Confirm `handleUseApp` closes detail modal and routes to correct config modal based on `app.configType`.

3. **User app editing wiring (critical for DATA-02 — pre-populated modals):**
   - Confirm `handleEditApp(app)` sets `editingApp` state and opens the correct modal based on `app.config_json.appType`.
   - Confirm each config modal receives `initialValues={editingApp?.config_json}` prop.
   - Confirm each modal's `onCreate` handler checks `if (editingApp)` and calls `handleSaveApp(config)` for updates instead of creating new.
   - Verify `handleSaveApp` calls `updateAppConfig(editingApp.id, config)` and updates local state.
   - Verify all 6 modals (ClockAppModal, WebPageAppModal, WeatherWallConfigModal, RssTickerAppModal, DataTableAppModal, GenericEmbedModal) use `initialValues` to pre-populate form fields.

4. **Delete wiring:**
   - Confirm `handleDeleteApp(id)` shows confirm prompt, calls `deleteApp(id)`, and removes from local state.

**MenuBoardsPage audit (~295 lines):**

5. **Import verification:**
   - Verify lucide-react imports: UtensilsCrossed, Plus, Pencil, Trash2, Copy, Loader2.
   - Verify design-system imports: PageLayout, Button.
   - Verify menuBoardService imports: fetchMenuBoards, deleteMenuBoard, getMenuBoard, createMenuBoard, createCategory, createMenuItem.
   - Verify MenuBoardEditorModal import.

6. **CRUD wiring:**
   - Confirm `handleCreate` opens editor with `editingBoard=null`.
   - Confirm `handleEdit(board)` opens editor with the board object.
   - Confirm `handleDelete(board)` shows confirm prompt, calls `deleteMenuBoard(board.id)`, removes from local state.
   - Confirm `handleDuplicate(board)` fetches full board via `getMenuBoard`, creates a copy via `createMenuBoard`, recreates categories and items, and refreshes the list.

**MenuBoardEditorModal audit (first 100 lines already read, verify key wiring):**

7. **DnD and CRUD verification:**
   - Verify @dnd-kit/core and @dnd-kit/sortable imports resolve (DndContext, closestCenter, PointerSensor, useSensor, useSensors, SortableContext, verticalListSortingStrategy, arrayMove).
   - Verify design-system Modal imports: Modal, ModalHeader, ModalTitle, ModalDescription, ModalContent, ModalFooter.
   - Verify menuBoardService imports for full CRUD: getMenuBoard, createMenuBoard, updateMenuBoard, createCategory, updateCategory, deleteCategory, createMenuItem, updateMenuItem, deleteMenuItem, toggleItemAvailability, reorderCategories, reorderItems.
   - Verify CategorySection import from same directory.

Fix any broken imports. Follow existing patterns:
- Button variant="secondary" (not "outline") — design-system Button has no "outline" variant.
- Badge from design-system if used (not lucide-react).
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx vite build --mode development 2>&1 | head -50</automated>
  </verify>
  <done>All AppsPage and MenuBoardsPage imports resolve, all 6 app config modals accept initialValues for pre-population, edit flow routes to correct modal, menu board CRUD and DnD imports are valid, and both pages build without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AppsPage and MenuBoardsPage functional behavior in browser</name>
  <action>
    Human verification of AppsPage and MenuBoardsPage in the browser:
    1. Navigate to Apps page (sidebar -> Apps)
    2. Verify the app gallery loads with categories, search bar, and sort control
    3. Click an app card -> confirm detail modal opens with preview and "Use App" button
    4. Click "Use App" -> confirm the config modal opens for that app type
    5. If existing user apps appear in "Recently Used": click the three-dot menu -> Edit -> confirm the modal opens pre-populated with saved values
    6. Navigate to Menu Boards page (sidebar -> Menu Boards)
    7. Verify the page loads (empty state or card grid)
    8. Click "New Menu Board" -> confirm the editor modal opens
    9. If boards exist: click Edit on a card -> confirm editor opens with board data loaded
    10. Verify no console errors on either page
  </action>
  <verify>Human confirms both pages load and all interactive elements are present and functional</verify>
  <done>AppsPage shows gallery with working search/filter, config modals open, edit pre-populates values; MenuBoardsPage shows card grid with working create/edit/duplicate/delete</done>
</task>

</tasks>

<verification>
- AppsPage and MenuBoardsPage build without import errors
- All 6 app config modals receive initialValues prop for edit pre-population
- MenuBoardEditorModal DnD imports (@dnd-kit/core, @dnd-kit/sortable) resolve
- Menu board CRUD calls the correct service functions
</verification>

<success_criteria>
AppsPage loads app gallery with working search/filter/sort, all 6 app types can be configured via their modals, editing pre-populates values, and MenuBoardsPage loads with working create/edit/duplicate/delete and DnD reordering in the editor modal.
</success_criteria>

<output>
After completion, create `.planning/phases/87-data-sources-apps-moderation/87-02-SUMMARY.md`
</output>
