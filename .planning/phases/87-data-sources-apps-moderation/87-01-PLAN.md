---
phase: 87-data-sources-apps-moderation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DataSourcesPage.jsx
  - src/components/data-sources/DataPreviewTable.jsx
  - src/components/data-sources/ColumnPicker.jsx
  - src/services/dataSourceService.js
autonomous: false
requirements:
  - DATA-01

must_haves:
  truths:
    - "DataSourcesPage loads and lists all data sources without errors"
    - "User can create a new internal-table data source via the create modal"
    - "User can create a new CSV-imported data source with auto-field detection"
    - "User can link an existing data source to Google Sheets and sync data"
    - "User can add, edit, and delete fields and rows inline in the editor view"
    - "User can delete a data source with confirmation"
  artifacts:
    - path: "src/pages/DataSourcesPage.jsx"
      provides: "Data sources list, create modal, inline editor, Google Sheets linking"
    - path: "src/services/dataSourceService.js"
      provides: "CRUD operations for data sources, fields, and rows"
    - path: "src/components/data-sources/DataPreviewTable.jsx"
      provides: "Tabular preview of data source rows and columns"
    - path: "src/components/data-sources/ColumnPicker.jsx"
      provides: "Field/column visibility and type picker"
  key_links:
    - from: "src/pages/DataSourcesPage.jsx"
      to: "src/services/dataSourceService.js"
      via: "fetchDataSources, createDataSource, deleteDataSource, createField, updateField, deleteField, createRow, updateRow, deleteRow, createDataSourceFromCSV, linkToGoogleSheet, unlinkIntegration"
      pattern: "import.*from.*dataSourceService"
    - from: "src/pages/DataSourcesPage.jsx"
      to: "src/services/googleSheetsService.js"
      via: "syncDataSourceFromSheet, parseSheetId"
      pattern: "import.*from.*googleSheetsService"
    - from: "src/pages/DataSourcesPage.jsx"
      to: "src/design-system"
      via: "Card, PageLayout, PageHeader, PageContent, Button, IconButton, Badge, Stack, Inline, EmptyState, Modal, Input, Select, Alert, FormField"
      pattern: "import.*from.*design-system"
---

<objective>
Audit and verify DataSourcesPage — data source listing, create modal (internal table + CSV import), inline field/row editing, Google Sheets linking, and delete with confirmation.

Purpose: Ensure data source configuration (Google Sheets, CSV, internal table) works end-to-end with correct field mapping — requirement DATA-01.
Output: Verified DataSourcesPage with all import paths correct, all CRUD operations wired, and any broken interactions fixed.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/pages/DataSourcesPage.jsx
@src/services/dataSourceService.js
@src/services/googleSheetsService.js
@src/components/data-sources/DataPreviewTable.jsx
@src/components/data-sources/ColumnPicker.jsx

<interfaces>
<!-- Key imports DataSourcesPage needs from design-system -->
From src/design-system (verified exports):
- Card, CardContent, CardHeader, CardTitle
- PageLayout, PageHeader, PageContent
- Button, IconButton, Badge
- Stack, Inline, EmptyState
- Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter
- Input, Select, Alert, FormField

From src/services/dataSourceService.js:
- fetchDataSources() → Array of data sources with field/row counts
- getDataSource(id) → Data source with fields and rows
- createDataSource({ name, type }) → Created data source
- deleteDataSource(id) → void
- createField(dataSourceId, { name, dataType, formatOptions }) → Field
- updateField(fieldId, updates) → Updated field
- deleteField(fieldId) → void
- createRow(dataSourceId, values) → Row
- updateRow(rowId, values) → Updated row
- deleteRow(rowId) → void
- createDataSourceFromCSV(file, name) → Data source with parsed fields/rows
- linkToGoogleSheet(dataSourceId, { sheetId, range, pollIntervalMinutes }) → Integration config
- unlinkIntegration(dataSourceId) → void
- getSyncHistory(dataSourceId) → Array of sync events
- DATA_SOURCE_TYPES, FIELD_DATA_TYPES, INTEGRATION_TYPES, SYNC_STATUS

From src/services/googleSheetsService.js:
- syncDataSourceFromSheet(dataSourceId) → Sync result
- parseSheetId(urlOrId) → string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataSourcesPage import audit and functional wiring verification</name>
  <files>src/pages/DataSourcesPage.jsx, src/components/data-sources/DataPreviewTable.jsx, src/components/data-sources/ColumnPicker.jsx</files>
  <action>
Audit the DataSourcesPage (~800+ lines) for import correctness and functional wiring:

1. **Import verification:**
   - Verify all lucide-react icon imports are used (AlertTriangle, ArrowDown, ArrowUp, Check, Clock, Database, Edit, Eye, EyeOff, FileSpreadsheet, GripVertical, Link2, Loader2, MoreVertical, Plus, RefreshCw, Search, Table2, Trash2, Unlink, X). Remove any unused imports.
   - Verify all design-system imports resolve: Card, CardContent, CardHeader, CardTitle, PageLayout, PageHeader, PageContent, Button, IconButton, Badge, Stack, Inline, EmptyState, Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter, Input, Select, Alert, FormField.
   - Verify dataSourceService imports: fetchDataSources, getDataSource, createDataSource, deleteDataSource, createField, updateField, deleteField, createRow, updateRow, deleteRow, createDataSourceFromCSV, linkToGoogleSheet, unlinkIntegration, getSyncHistory, DATA_SOURCE_TYPES, FIELD_DATA_TYPES, INTEGRATION_TYPES, SYNC_STATUS.
   - Verify googleSheetsService imports: syncDataSourceFromSheet, parseSheetId.
   - Verify DataPreviewTable and ColumnPicker component imports.

2. **Create modal wiring:**
   - Confirm the create modal (`showCreateModal` state) opens on "New Data Source" button click.
   - Confirm type selection (internal_table vs csv_import) routes to the correct creation flow.
   - Confirm CSV file upload calls `createDataSourceFromCSV` with the File object and name.
   - Confirm internal table creation calls `createDataSource` with name and type.

3. **Inline editing wiring:**
   - Confirm selecting a data source (`selectedSource`) loads full details via `getDataSource(id)`.
   - Confirm field CRUD: adding a field calls `createField`, editing calls `updateField`, deleting calls `deleteField`.
   - Confirm row CRUD: adding a row calls `createRow`, editing calls `updateRow`, deleting calls `deleteRow`.

4. **Google Sheets integration:**
   - Confirm `linkToGoogleSheet` is called with parsed sheetId, range, and pollIntervalMinutes.
   - Confirm `parseSheetId` is used to validate the Google Sheets URL before linking.
   - Confirm `syncDataSourceFromSheet` triggers a manual sync.
   - Confirm `unlinkIntegration` disconnects the sheet integration.

5. **Delete flow:**
   - Confirm delete shows confirmation prompt and calls `deleteDataSource(id)`.
   - Confirm the data sources list refreshes after successful deletion.

6. **DataPreviewTable and ColumnPicker:**
   - Open DataPreviewTable.jsx and ColumnPicker.jsx and verify they have no missing imports.
   - Verify props passed from DataSourcesPage match what these components expect.

Fix any broken imports, missing function calls, or incorrect prop passing. Follow existing patterns:
- Button variant="secondary" (not "outline") per design-system constraint.
- Badge imported from design-system, not lucide-react.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx vite build --mode development 2>&1 | head -50</automated>
  </verify>
  <done>All DataSourcesPage imports resolve correctly, all CRUD operations are properly wired to dataSourceService functions, Google Sheets integration handlers call the correct service functions, and the page builds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DataSourcesPage functional behavior in browser</name>
  <action>
    Human verification of DataSourcesPage in the browser:
    1. Navigate to the Data Sources page (sidebar -> Data Sources)
    2. Verify the page loads without console errors
    3. Click "New Data Source" — confirm the create modal opens with type selection
    4. If data sources exist: click one to open the editor view, verify fields and rows display
    5. Test the Google Sheets section visibility (should show link/unlink controls)
    6. Verify delete button shows confirmation before removing
  </action>
  <verify>Human confirms page loads and all interactive elements are present</verify>
  <done>DataSourcesPage loads without errors, create modal opens, editor view works, and Google Sheets controls are visible</done>
</task>

</tasks>

<verification>
- DataSourcesPage builds without import errors
- All service function imports match exported function signatures
- Design-system component imports all resolve
- DataPreviewTable and ColumnPicker have valid imports
</verification>

<success_criteria>
DataSourcesPage loads without errors, create modal opens with type selection, data source editing shows fields/rows, Google Sheets linking/syncing controls are present, and delete shows confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/87-data-sources-apps-moderation/87-01-SUMMARY.md`
</output>
