---
phase: 43-e2e-test-triage
plan: 04
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - src/__fixtures__/screens.js
  - src/__fixtures__/playlists.js
  - src/__fixtures__/schedules.js
  - tests/unit/services/screenService.test.js
  - tests/unit/services/playlistService.test.js
  - tests/unit/services/schedulerService.test.js
autonomous: true

must_haves:
  truths:
    - "User can find tests that use src/__fixtures__/ for shared test data instead of inline setup"
  artifacts:
    - path: "src/__fixtures__/screens.js"
      provides: "Screen mock data factories"
      contains: "createMockScreen"
    - path: "src/__fixtures__/playlists.js"
      provides: "Playlist mock data factories"
      contains: "createMockPlaylist"
    - path: "src/__fixtures__/schedules.js"
      provides: "Schedule mock data factories"
      contains: "createMockSchedule"
  key_links:
    - from: "tests/unit/services/screenService.test.js"
      to: "src/__fixtures__/screens.js"
      via: "import { createMockScreen }"
      pattern: "from.*__fixtures__"
    - from: "tests/unit/services/playlistService.test.js"
      to: "src/__fixtures__/playlists.js"
      via: "import { createMockPlaylist }"
      pattern: "from.*__fixtures__"
---

<objective>
Adopt the src/__fixtures__/ shared test data pattern in 3 representative unit test files, replacing inline mock data with imported fixture factories.

Purpose: Demonstrate the __fixtures__/ pattern so future test writers follow it. The fixtures infrastructure (screens.js, playlists.js, schedules.js) already exists but zero tests use it. This plan creates the first adopters.
Output: 3 unit test files updated to import from src/__fixtures__/ instead of defining inline mock data. Fixture files enhanced only if needed to match test requirements.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/__fixtures__/index.js
@src/__fixtures__/screens.js
@src/__fixtures__/playlists.js
@src/__fixtures__/schedules.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt fixture factories in 3 service unit tests</name>
  <files>
    src/__fixtures__/screens.js
    src/__fixtures__/playlists.js
    src/__fixtures__/schedules.js
    tests/unit/services/screenService.test.js
    tests/unit/services/playlistService.test.js
    tests/unit/services/schedulerService.test.js
  </files>
  <action>
    **Part A: Review existing fixture files.**

    The 3 existing fixture files already have factory functions (createMockScreen, createMockPlaylist, createMockSchedule with overrides parameter). Only add fields if a test being converted needs a field that is missing. Do NOT add fields speculatively.

    **Part B: Convert 3 service unit test files.**

    For each of these 3 test files:
    1. Read the file and identify inline mock data definitions (e.g., `const mockScreen = { id: '...', ... }`)
    2. Replace with import from __fixtures__: e.g., `import { mockScreen, createMockScreen } from '../../../src/__fixtures__/screens.js';`
    3. If the test uses custom overrides, use the factory: `const customScreen = createMockScreen({ name: 'Custom' });`
    4. If the inline mock has fields not in the fixture, add those fields to the fixture factory
    5. Run the test to verify it still passes: `npx vitest run <test-file>`

    If any of these 3 specific test files do not exist or do not contain inline mock data for the matching entity, search for an alternative test file in tests/unit/ that defines `const mockScreen`, `const mockPlaylist`, or `const mockSchedule` inline.

    **Rules:**
    - Only convert tests where the inline mock data closely matches what's in __fixtures__
    - Do NOT force-fit: if a test needs a very different shape of data, leave it inline
    - Keep the import path relative from the test file location
    - Run each test file after conversion to verify no breakage
  </action>
  <verify>
    - grep -r '__fixtures__' tests/unit/ shows 3 files importing from fixtures
    - npx vitest run for each converted test file passes
    - npm run test:unit passes overall
  </verify>
  <done>
    3 unit test files import mock data from src/__fixtures__/ instead of defining it inline. All converted tests pass. The __fixtures__/ pattern has demonstrable adopters in the codebase.
  </done>
</task>

</tasks>

<verification>
- At least 3 test files in tests/unit/ import from src/__fixtures__/
- All unit tests pass: npm run test:unit
- Fixture files in src/__fixtures__/ are unchanged or enhanced (not broken)
</verification>

<success_criteria>
User can grep for `__fixtures__` in tests/unit/ and find 3 files demonstrating the shared fixture pattern. All tests pass after conversion.
</success_criteria>

<output>
After completion, create `.planning/phases/43-e2e-test-triage/43-04-SUMMARY.md`
</output>
