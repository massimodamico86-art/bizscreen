---
phase: 43-e2e-test-triage
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - tests/e2e/audit.spec.js
  - tests/e2e/admin.spec.js
  - tests/e2e/onboarding.spec.js
  - tests/e2e/screens.spec.js
  - tests/e2e/media.spec.js
  - tests/e2e/settings.spec.js
  - tests/e2e/social.spec.js
  - tests/e2e/seo.spec.js
  - tests/e2e/alerts-center.spec.js
  - tests/e2e/industry-wizards.spec.js
  - tests/e2e/playlist-screen-persistence.spec.js
  - tests/e2e/content-performance.spec.js
  - tests/e2e/smoke.spec.js
  - tests/e2e/polotno-editor.spec.js
autonomous: true

must_haves:
  truths:
    - "User can run the E2E suite and see previously-fixable tests now passing (selector updates, timing fixes applied)"
    - "User can inspect every remaining test.skip/test.fixme and find a comment explaining why it is skipped"
  artifacts:
    - path: "tests/e2e/audit.spec.js"
      provides: "Fixed or documented audit test skips"
      contains: "test.fixme\\|test.skip"
    - path: "tests/e2e/admin.spec.js"
      provides: "Documented admin test skips"
      contains: "SKIP REASON:"
  key_links:
    - from: "tests/e2e/*.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers for login and navigation"
      pattern: "import.*helpers"
---

<objective>
Fix fixable skipped tests (selector updates, auth pattern fixes) and add clear documentation comments to every remaining skip that cannot be fixed.

Purpose: Reduce the number of unnecessarily-skipped tests by fixing those with simple selector or pattern issues, and ensure every remaining skip has a comment explaining WHY it is skipped so future developers understand the reason.
Output: Updated test files with fixes applied and skip documentation comments added.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-e2e-test-triage/43-01-SUMMARY.md
@.planning/phases/38-e2e-test-coverage-gate/COVERAGE-REPORT.md
@tests/e2e/fixtures/index.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fixable test.fixme and test.skip tests</name>
  <files>
    tests/e2e/audit.spec.js
    tests/e2e/admin.spec.js
    tests/e2e/screens.spec.js
    tests/e2e/media.spec.js
    tests/e2e/seo.spec.js
    tests/e2e/alerts-center.spec.js
    tests/e2e/industry-wizards.spec.js
    tests/e2e/content-performance.spec.js
    tests/e2e/smoke.spec.js
    tests/e2e/polotno-editor.spec.js
  </files>
  <action>
    For each file, read the current test code, inspect the skip reason, and determine if the fix is straightforward. Apply fixes where possible, convert unfixable skips to have proper documentation.

    **audit.spec.js -- 8 test.fixme tests for selector mismatches:**
    Read the actual audit page UI (check for the AuditLogsPage component in src/) to find current button names/selectors. If the refresh/filter buttons exist with different names or roles, update the selectors. If the UI genuinely lacks these buttons, convert test.fixme to test.skip with a clear comment: `// SKIP REASON: Audit page does not have refresh/filter buttons in current UI (Phase 43 triage)`

    **admin.spec.js -- 6 test.skip() calls with no reason:**
    Lines 41, 53, 86, 195, 204, 226, 231 have bare `test.skip()` with no documentation. For each: check if "Tenant Management" button exists in the current superadmin sidebar. If not, add comment: `// SKIP REASON: Tenant Management button not present in superadmin sidebar navigation`. If the button exists but with different text, fix the selector.

    **screens.spec.js -- close button selector mismatch:**
    Read the current Modal component to find the actual close button markup. Update the selector to match. If the close button uses a different aria-label or button text, fix it.

    **media.spec.js -- 1 test.skip for upload (Cloudinary external dependency):**
    This is correctly skipped. Add comment: `// SKIP REASON: Cloudinary upload widget is an external service that cannot be automated in E2E tests`

    **seo.spec.js -- 4 test.skip for missing SEO features:**
    These test features not yet implemented (noindex directive, meta tags, meaningful link text, skip-to-content). Convert each from bare `test.skip('name', async...)` to have a leading comment: `// SKIP REASON: [specific feature] not yet implemented -- see Phase 43 audit`

    **alerts-center.spec.js -- 9 bare test.skip() inside test bodies:**
    These skip when the alerts nav button is not visible. Add comment before each: `// SKIP REASON: Alerts navigation button not visible in current admin sidebar UI`

    **industry-wizards.spec.js -- 7+ test.skip(true, 'Service not available in test context'):**
    These are testing service imports that don't work in E2E context. Add comment: `// SKIP REASON: Service functions cannot be imported directly in Playwright E2E tests`
    For the 6 test.skip('name', ...) tests testing wizard UI: Check if the wizard button exists in the current scene editor. If not, add: `// SKIP REASON: Industry wizard button not present in current scene editor toolbar`

    **content-performance.spec.js -- 7 test.skip(true, 'Content Performance page not accessible'):**
    These correctly handle feature-gated content. Add comment: `// SKIP REASON: Content Performance page is feature-gated and not accessible with test user's plan`

    **smoke.spec.js -- 1 bare test.skip() at line 426:**
    Read surrounding context. Add appropriate SKIP REASON comment.

    **polotno-editor.spec.js -- 3 bare test.skip() calls:**
    Lines 90, 96, 264, 281, 322, 329: Add SKIP REASON comments based on the surrounding context (likely selector mismatches or dependency on earlier skipped tests).

    For the `playlist-screen-persistence.spec.js` bare `test.skip()` at line 90: Add SKIP REASON comment.

    **Important rules:**
    - DO NOT remove `test.skip` for project-specific skips (`testInfo.project.name !== 'chromium'`). These are correct.
    - DO NOT remove credential-check skips (`!process.env.TEST_USER_EMAIL`). These are correct.
    - Every bare `test.skip()` (with no arguments or no string reason) MUST get a `// SKIP REASON:` comment.
    - Every `test.fixme` MUST either be fixed (selector update) or get a `// SKIP REASON:` comment.
    - Every `test.describe.skip` block MUST have a `// SKIP REASON:` comment on the line above.
    - Use the existing pattern from Phase 38 where applicable (e.g., `// SKIPPED: reason` comments already exist on some files -- be consistent).
  </action>
  <verify>
    - grep for bare `test.skip()` with no comment above: count should be 0
    - grep for `test.fixme` with no comment: count should be 0
    - grep for `test.describe.skip` with no comment: count should be 0
    - `npx playwright test --list` still shows the same test count (no accidental test removal)
  </verify>
  <done>
    Every test.skip, test.fixme, and describe.skip across all E2E files has a clear SKIP REASON comment. Selector-fixable tests have been updated where the current UI supports them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document remaining skips in describe.skip blocks</name>
  <files>
    tests/e2e/brand-theme.spec.js
    tests/e2e/billing.spec.js
    tests/e2e/template-marketplace.spec.js
    tests/e2e/scenes.spec.js
    tests/e2e/scene-editor.spec.js
    tests/e2e/usage.spec.js
    tests/e2e/reseller.spec.js
    tests/e2e/enterprise.spec.js
    tests/e2e/alert-notification-flow.spec.js
    tests/e2e/onboarding.spec.js
    tests/e2e/settings.spec.js
    tests/e2e/social.spec.js
    tests/e2e/playlist-screen-persistence.spec.js
  </files>
  <action>
    Ensure every file with describe.skip or credential-based skips has consistent documentation. For each file:

    1. Check if a `// SKIP REASON:` or `// SKIPPED:` comment already exists above/near the skip. If yes, leave it. If no, add one.

    2. Specific files to check and document:

    **brand-theme.spec.js** (2 describe.skip blocks):
    - Already has `// SKIPPED: Branding tab not present in settings UI - needs UI implementation`
    - Verify both blocks have this documentation. If second block lacks it, add matching comment.

    **billing.spec.js** (1 describe.skip):
    - Already has `// SKIPPED: "Plan & Limits" sidebar button not found in current UI`
    - Verify comment exists.

    **template-marketplace.spec.js** (3 describe.skip blocks):
    - Ensure each block has a SKIP REASON comment explaining the specific selector mismatch.

    **scenes.spec.js** (1 describe.skip):
    - Add or verify: `// SKIP REASON: Scenes feature not accessible via sidebar navigation`

    **scene-editor.spec.js** (1 describe.skip):
    - Add or verify: `// SKIP REASON: Scene editor depends on Scenes page which is not accessible via sidebar navigation`

    **usage.spec.js** (2 describe.skip blocks):
    - Already has inline comment. Ensure it's in SKIP REASON format.

    **reseller.spec.js** (credential skip):
    - Add: `// SKIP REASON: Requires TEST_RESELLER_EMAIL environment variable (reseller account not provisioned in test env)`

    **enterprise.spec.js** (credential skip):
    - Add: `// SKIP REASON: Requires TEST_ENTERPRISE_EMAIL environment variable (enterprise account not provisioned in test env)`

    **alert-notification-flow.spec.js** (describe.skip):
    - Already has comment about CI/database state. Verify it's clear.

    **onboarding.spec.js, settings.spec.js, social.spec.js, playlist-screen-persistence.spec.js**:
    - Check for any undocumented skips and add SKIP REASON comments.

    **Consistency rule:** Use `// SKIP REASON:` prefix for new comments. Do not change existing `// SKIPPED:` comments that are already clear.
  </action>
  <verify>
    - Every test.describe.skip in tests/e2e/ has a comment on the line before or as inline comment explaining the reason
    - Every credential-based skip (process.env.TEST_*_EMAIL) has a comment explaining what credentials are needed
    - grep -B1 'describe.skip' tests/e2e/*.spec.js shows a comment line before each describe.skip
  </verify>
  <done>
    Every describe.skip, credential skip, and remaining test.skip/test.fixme across all 35 remaining E2E spec files has a clear documentation comment explaining why it is skipped.
  </done>
</task>

</tasks>

<verification>
- Every test.skip() (bare, no args) has a SKIP REASON comment above it
- Every test.fixme has either been fixed or has a SKIP REASON comment
- Every test.describe.skip has a SKIP REASON comment
- No test files were accidentally broken (npx playwright test --list succeeds)
- Pass rate has not decreased from 92.7% baseline
</verification>

<success_criteria>
User can inspect any test.skip, test.fixme, or describe.skip in the E2E test suite and immediately understand why it is skipped. Any fixable selector mismatches have been updated to match the current UI.
</success_criteria>

<output>
After completion, create `.planning/phases/43-e2e-test-triage/43-02-SUMMARY.md`
</output>
