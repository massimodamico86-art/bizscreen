---
phase: 43-e2e-test-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-e2e-test-triage/E2E-AUDIT-REPORT.md
  - tests/e2e/debug.spec.js
  - tests/e2e/feature-diagnostic.spec.js
  - tests/e2e/location-diagnostic.spec.js
autonomous: true

must_haves:
  truths:
    - "User can view an audit report that categorizes every previously-skipped test by reason (fixable, obsolete, blocked)"
    - "User can search for deleted test files and confirm they covered features that no longer exist"
  artifacts:
    - path: ".planning/phases/43-e2e-test-triage/E2E-AUDIT-REPORT.md"
      provides: "Categorized audit of all 917 skipped E2E tests"
      contains: "## Category"
  key_links:
    - from: "E2E-AUDIT-REPORT.md"
      to: "tests/e2e/*.spec.js"
      via: "references to actual test files and line numbers"
      pattern: "tests/e2e/"
---

<objective>
Create a comprehensive audit report categorizing all 917 skipped E2E tests, and delete obsolete diagnostic/debug test files that exist only as development helpers.

Purpose: Establish a clear picture of the entire skipped test landscape so that fixable tests can be triaged in Plan 43-02 and remaining skips properly documented in 43-03. Also remove test files that serve no ongoing test value (debug helpers, one-off diagnostics).
Output: E2E-AUDIT-REPORT.md with every skipped test categorized; 3 obsolete test files deleted.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-e2e-test-coverage-gate/COVERAGE-REPORT.md
@.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
@playwright.config.js
@tests/e2e/fixtures/index.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E audit report and delete obsolete test files</name>
  <files>
    .planning/phases/43-e2e-test-triage/E2E-AUDIT-REPORT.md
    tests/e2e/debug.spec.js
    tests/e2e/feature-diagnostic.spec.js
    tests/e2e/location-diagnostic.spec.js
  </files>
  <action>
    **Part A: Audit every skipped test across all 38 spec files.**

    Scan every file in tests/e2e/*.spec.js and categorize each skip into one of these buckets:

    1. **Intentional project-specific skips** (~800): `test.skip(testInfo.project.name !== 'chromium')` etc. These are CORRECT behavior -- tests designed for one Playwright project but running across 3 projects (chromium, chromium-admin, chromium-superadmin). Count them but mark as "intentional - no action needed".

    2. **Fixable -- selector mismatch** (~15-20): Tests with `test.fixme`, `test.skip(true, 'reason')` where the skip reason indicates a selector or button name doesn't match current UI. For each, note the file, test name, and current skip reason. These become Plan 43-02 work.

    3. **Fixable -- auth pattern** (~5): Tests using manual login when storage state is available. Note file, test name, skip reason.

    4. **Obsolete -- feature not implemented** (~45): `test.describe.skip` blocks for features not in the current UI (brand-theme branding tab, billing plan & limits button, template marketplace headings). For each describe block, note the file, block name, test count, and reason the feature doesn't exist.

    5. **Obsolete -- diagnostic/debug** (~8): debug.spec.js (manual debug helper, no assertions), feature-diagnostic.spec.js (one-off diagnostic tool), location-diagnostic.spec.js (one-off diagnostic). These have no ongoing test value.

    6. **Blocked -- feature not accessible** (~30): scenes.spec.js, scene-editor.spec.js where the feature page exists but is not exposed in sidebar navigation.

    7. **Blocked -- missing features** (~4): SEO meta tags, skip-to-content link -- features not yet implemented.

    8. **Blocked -- external dependencies** (~1): Cloudinary widget in media upload.

    9. **Blocked -- credentials/environment** (~22): Enterprise features requiring TEST_ENTERPRISE_EMAIL, reseller features requiring TEST_RESELLER_EMAIL, usage dashboard requiring route wiring.

    Write E2E-AUDIT-REPORT.md with:
    - Summary table (category, count, action)
    - Detailed section per category listing every test with file:line, test name, skip mechanism, and recommended action
    - Action summary: what Plan 43-02 will fix, what Plan 43-03 will document, what gets deleted now

    **Part B: Delete 3 obsolete test files.**

    Delete these files (they are one-off development helpers, not real tests):
    - `tests/e2e/debug.spec.js` -- Manual debug test with no assertions, comment says "To use, temporarily change test.skip to test and run manually"
    - `tests/e2e/feature-diagnostic.spec.js` -- One-off diagnostic tool that logs console output, uses hardcoded credentials, incompatible with storage state pattern
    - `tests/e2e/location-diagnostic.spec.js` -- One-off diagnostic for a specific page error, uses legacy login pattern, incompatible with storage state

    These 3 files contain 9 tests total, all permanently skipped with documented reasons.
  </action>
  <verify>
    - E2E-AUDIT-REPORT.md exists and contains categorized entries for all skip types
    - grep -r "debug.spec.js\|feature-diagnostic.spec.js\|location-diagnostic.spec.js" tests/e2e/ returns no results (files deleted)
    - The sum of all categorized tests in the report approximates 917
  </verify>
  <done>
    E2E-AUDIT-REPORT.md contains a complete categorization of all 917 skipped tests with clear recommended actions per category. Three obsolete diagnostic/debug test files are deleted.
  </done>
</task>

</tasks>

<verification>
- E2E-AUDIT-REPORT.md exists in .planning/phases/43-e2e-test-triage/
- Report contains sections for all 9 categories
- Report lists every describe.skip block and test.fixme individually with file references
- 3 obsolete test files deleted (debug.spec.js, feature-diagnostic.spec.js, location-diagnostic.spec.js)
- Remaining E2E tests still pass (no accidental breakage from deletion)
</verification>

<success_criteria>
User can open E2E-AUDIT-REPORT.md and see every previously-skipped test categorized by reason with clear action items. User can confirm deleted test files covered only debug/diagnostic features with no ongoing value.
</success_criteria>

<output>
After completion, create `.planning/phases/43-e2e-test-triage/43-01-SUMMARY.md`
</output>
