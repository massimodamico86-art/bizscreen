---
phase: 34-cleanup-and-deprecation
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/pages/DashboardPage.jsx
  - tests/unit/pages/DashboardPage.test.jsx
  - tests/e2e/helpers.js
autonomous: true

must_haves:
  truths:
    - "DashboardPage has no imports for deleted components"
    - "DashboardPage has no state variables for legacy onboarding"
    - "DashboardPage has no JSX rendering deleted components"
    - "Unit tests pass without mocking deleted components"
    - "E2E tests pass without setting deleted storage keys"
    - "Full test suite passes after all cleanup"
  artifacts:
    - path: "src/pages/DashboardPage.jsx"
      provides: "Simplified dashboard with unified onboarding only"
      not_contains: ["OnboardingWizard", "OnboardingBanner", "WelcomeModal", "showWelcomeModal", "showOnboardingWizard", "showOnboardingBanner"]
    - path: "tests/unit/pages/DashboardPage.test.jsx"
      provides: "Updated unit tests without deleted component mocks"
      not_contains: ["OnboardingWizard", "OnboardingBanner", "WelcomeModal"]
    - path: "tests/e2e/helpers.js"
      provides: "Updated E2E helpers without obsolete storage keys"
      not_contains: "bizscreen_welcome_modal_shown"
  key_links:
    - from: "DashboardPage.jsx"
      to: "UnifiedOnboardingController"
      via: "import and render"
      pattern: "UnifiedOnboardingController"
    - from: "post-cleanup tests"
      to: "passing suite"
      via: "npm run test:e2e"
      pattern: "all tests pass"
---

<objective>
Clean up DashboardPage, update tests, and verify all tests pass

Purpose: Remove all imports, state variables, handlers, and JSX related to deleted components from DashboardPage. Update unit test mocks and E2E helpers. Verify the entire test suite passes after cleanup.

Output: Simplified DashboardPage, passing unit and E2E tests, cleanup complete
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cleanup-and-deprecation/34-CONTEXT.md
@.planning/phases/34-cleanup-and-deprecation/34-RESEARCH.md
@.planning/phases/34-cleanup-and-deprecation/34-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up DashboardPage imports, state, and JSX</name>
  <files>src/pages/DashboardPage.jsx</files>
  <action>
Remove all references to deleted components from DashboardPage.jsx.

**Step 1: Remove imports**
Delete these import lines:
- `import OnboardingWizard from '../components/OnboardingWizard';`
- Any import of `WelcomeModal` from `./dashboard/WelcomeModal` or `./dashboard`
- Any import of `OnboardingBanner` or `isBannerDismissed` from `../components/onboarding`

Also evaluate and potentially remove if no longer used:
- `WelcomeTour`, `IndustrySelectionModal`, `StarterPackOnboarding` imports (if only used by legacy flow)
- `Sparkles` icon if only used by deleted components

**Keep these imports:**
- `UnifiedOnboardingController` from `../components/onboarding`
- `config` from `../config/env` (still needed for feature flag check)

**Step 2: Remove state variables**
Delete these useState hooks (identified in research):
- `showWelcomeModal`, `setShowWelcomeModal`
- `showOnboardingWizard`, `setShowOnboardingWizard`
- `welcomeStep`, `setWelcomeStep`
- `selectedBusinessType`, `setSelectedBusinessType`
- `applyingPack`, `setApplyingPack`
- `packResult`, `setPackResult`
- `packError`, `setPackError`
- `showWelcomeTour`, `setShowWelcomeTour`
- `showIndustryModal`, `setShowIndustryModal`
- `showStarterPackModal`, `setShowStarterPackModal`
- `selectedIndustry`, `setSelectedIndustry`
- `showOnboardingBanner`, `setShowOnboardingBanner`
- `creatingDemo`, `setCreatingDemo`
- `demoResult`, `setDemoResult`

Also evaluate:
- `onboardingNeeded` - remove if only used by legacy flow
- `isFirstRun` - KEEP if used for stats empty state hint

**Keep:**
- `showUnifiedOnboarding`, `setShowUnifiedOnboarding` (unified flow)
- Core dashboard state (stats, screens, loading, errors, etc.)

**Step 3: Remove handlers and effects**
Remove any useEffect, handler functions, or callbacks that:
- Reference deleted state variables
- Handle legacy onboarding flow logic
- Set storage keys for deleted components

**Step 4: Remove JSX**
Delete any JSX blocks that render:
- `<OnboardingWizard ... />`
- `<WelcomeModal ... />`
- `<OnboardingBanner ... />`
- `<WelcomeTour ... />` (if only in legacy conditional)
- `<IndustrySelectionModal ... />` (if only in legacy conditional)
- `<StarterPackOnboarding ... />` (if only in legacy conditional)

Look for patterns like:
```jsx
{!config().useUnifiedOnboarding && showWelcomeModal && (
  <WelcomeModal ... />
)}
```
Delete entire conditional blocks for legacy components.

**Keep:**
```jsx
{config().useUnifiedOnboarding && showUnifiedOnboarding && (
  <UnifiedOnboardingController onComplete={...} />
)}
```

**Step 5: Clean up unused variables**
After removals, run ESLint or visually inspect for any remaining unused imports or variables.
  </action>
  <verify>
- `grep "OnboardingWizard" src/pages/DashboardPage.jsx` returns no matches
- `grep "OnboardingBanner" src/pages/DashboardPage.jsx` returns no matches
- `grep "WelcomeModal" src/pages/DashboardPage.jsx` returns no matches
- `grep "showWelcomeModal" src/pages/DashboardPage.jsx` returns no matches
- `grep "showOnboardingWizard" src/pages/DashboardPage.jsx` returns no matches
- `npm run build` succeeds with no import errors
  </verify>
  <done>DashboardPage cleaned of all legacy onboarding references, only unified flow remains</done>
</task>

<task type="auto">
  <name>Task 2: Update test mocks and E2E helpers</name>
  <files>
tests/unit/pages/DashboardPage.test.jsx
tests/e2e/helpers.js
  </files>
  <action>
Update test files to remove references to deleted components and storage keys.

**Step 1: Update unit test mocks in DashboardPage.test.jsx**

Remove these vi.mock blocks entirely:
```javascript
vi.mock('../../../src/components/OnboardingWizard', () => ({
  default: () => null,
}));

vi.mock('../../../src/components/onboarding/OnboardingBanner', () => ({
  OnboardingBanner: () => null,
  isBannerDismissed: vi.fn().mockReturnValue(false),
}));

vi.mock('../../../src/pages/dashboard/WelcomeModal', () => ({
  WelcomeModal: () => null,
}));
```

Also check for and remove:
- Any mock of `WelcomeTour`, `IndustrySelectionModal`, `StarterPackOnboarding` if only used for legacy tests
- Any test cases that specifically test legacy onboarding behavior
- Any assertions that check for deleted component rendering

**Keep mocks for:**
- `UnifiedOnboardingController` (if mocked)
- Other non-onboarding mocks

**Step 2: Update E2E helpers**

In `tests/e2e/helpers.js`, find and remove:
```javascript
await page.evaluate(() => {
  localStorage.setItem('bizscreen_welcome_modal_shown', 'true');
});
```

Or any similar pattern that sets:
- `bizscreen_welcome_modal_shown`
- `onboarding_banner_dismissed`

If there's a helper function that sets up onboarding state, update it to use the unified onboarding approach (or remove if no longer needed).

**Step 3: Run unit tests to verify**
Run `npm run test:unit` or equivalent to ensure unit tests pass after mock updates.
  </action>
  <verify>
- `grep "OnboardingWizard" tests/unit/pages/DashboardPage.test.jsx` returns no matches (or file pattern)
- `grep "OnboardingBanner" tests/unit/pages/DashboardPage.test.jsx` returns no matches
- `grep "WelcomeModal" tests/unit/pages/DashboardPage.test.jsx` returns no matches
- `grep "bizscreen_welcome_modal_shown" tests/e2e/helpers.js` returns no matches
- `npm run test:unit` passes
  </verify>
  <done>Test mocks and E2E helpers updated, unit tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify cleanup complete</name>
  <files>No files modified - verification only</files>
  <action>
Run the complete test suite to verify all cleanup is successful.

**Step 1: Run E2E tests**
Run `npm run test:e2e` (full Playwright suite)

All tests must pass. This is the user's hard requirement from CONTEXT.md:
"All E2E tests must pass after cleanup - no exceptions"

**Step 2: If any test fails**
1. Identify the failure cause
2. Fix immediately - do NOT leave broken tests
3. Re-run until all pass

**Step 3: Document final state**
Compare to baseline from Plan 01:
- Same number of tests should pass
- No new failures introduced
- Cleanup successful

**Step 4: Final codebase verification**
Run these checks:
```bash
grep -r "OnboardingWizard" src/ --include="*.jsx" --include="*.js"
grep -r "OnboardingBanner" src/ --include="*.jsx" --include="*.js"
grep -r "WelcomeModal" src/ --include="*.jsx" --include="*.js"
grep -r "bizscreen_welcome_modal_shown" src/
grep -r "onboarding_banner_dismissed" src/
```

All should return empty (no matches in source code).
  </action>
  <verify>
- `npm run test:e2e` shows all tests passing
- `npm run test:unit` shows all tests passing
- No grep matches for deleted component names in src/
- No grep matches for deleted storage keys in src/
  </verify>
  <done>Full test suite passes, cleanup verified complete</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. DashboardPage simplified: No legacy onboarding imports, state, or JSX
2. Unit tests pass: No mocks for deleted components
3. E2E tests pass: Same as baseline, no regressions
4. Codebase clean: No references to deleted components or storage keys in source
5. Build succeeds: `npm run build` works without errors
</verification>

<success_criteria>
- DashboardPage.jsx reduced by ~100-200 lines (state/import/JSX removal)
- All unit tests pass
- All E2E tests pass (matching baseline from Plan 01)
- Zero grep matches for deleted components in src/
- Zero grep matches for deleted storage keys in src/
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-cleanup-and-deprecation/34-02-SUMMARY.md`
</output>
