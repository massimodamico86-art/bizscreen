---
phase: 34-cleanup-and-deprecation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/OnboardingWizard.jsx
  - src/components/onboarding/OnboardingBanner.jsx
  - src/pages/dashboard/WelcomeModal.jsx
  - src/components/onboarding/index.js
  - src/pages/dashboard/index.js
autonomous: true

must_haves:
  truths:
    - "E2E test suite passes before any deletions (baseline captured)"
    - "OnboardingWizard.jsx no longer exists in codebase"
    - "OnboardingBanner.jsx no longer exists in codebase"
    - "WelcomeModal.jsx no longer exists in codebase"
    - "Barrel exports no longer reference deleted components"
  artifacts:
    - path: "src/components/OnboardingWizard.jsx"
      provides: "DELETED - broken wizard component"
      exists: false
    - path: "src/components/onboarding/OnboardingBanner.jsx"
      provides: "DELETED - replaced by unified flow"
      exists: false
    - path: "src/pages/dashboard/WelcomeModal.jsx"
      provides: "DELETED - replaced by unified flow"
      exists: false
    - path: "src/components/onboarding/index.js"
      provides: "Updated barrel exports"
      not_contains: "OnboardingBanner"
    - path: "src/pages/dashboard/index.js"
      provides: "Updated barrel exports"
      not_contains: "WelcomeModal"
  key_links:
    - from: "E2E test run"
      to: "known-good baseline"
      via: "pre-deletion verification"
      pattern: "test results captured before deletions"
---

<objective>
Establish E2E baseline and delete legacy onboarding components

Purpose: Before any code changes, capture a passing E2E baseline. Then delete the three legacy onboarding components (OnboardingWizard, OnboardingBanner, WelcomeModal) that are replaced by the unified flow from Phases 30-33.

Output: Three component files deleted, barrel exports updated, E2E baseline documented
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cleanup-and-deprecation/34-CONTEXT.md
@.planning/phases/34-cleanup-and-deprecation/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run E2E baseline before any deletions</name>
  <files>No files modified - verification only</files>
  <action>
Run the full E2E test suite to establish a baseline BEFORE making any changes.

1. Run: `npm run test:e2e` (or equivalent Playwright command)
2. Capture the results - all tests should pass
3. If any tests fail, stop and report. Do NOT proceed with deletions if baseline fails.
4. Document the baseline result (number of tests, pass rate)

This is a BLOCKING gate. The user decision in CONTEXT.md states: "Run full E2E suite BEFORE starting cleanup to establish baseline" and "All E2E tests must pass after cleanup - no exceptions"
  </action>
  <verify>E2E test output shows all tests passing. Document test count and status.</verify>
  <done>E2E baseline captured showing all tests pass before any code changes</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy component files and update barrel exports</name>
  <files>
src/components/OnboardingWizard.jsx
src/components/onboarding/OnboardingBanner.jsx
src/pages/dashboard/WelcomeModal.jsx
src/components/onboarding/index.js
src/pages/dashboard/index.js
  </files>
  <action>
Delete three legacy component files and update their barrel exports.

**Step 1: Delete component files**
- Delete `src/components/OnboardingWizard.jsx` (539 lines - broken wizard, never properly wired)
- Delete `src/components/onboarding/OnboardingBanner.jsx` (102 lines - replaced by unified flow)
- Delete `src/pages/dashboard/WelcomeModal.jsx` (359 lines - replaced by unified flow)

**Step 2: Update barrel exports**

In `src/components/onboarding/index.js`:
- Remove any export referencing `OnboardingBanner`
- Keep all other exports (UnifiedOnboardingController, WelcomeTour, etc.)

In `src/pages/dashboard/index.js`:
- Remove any export referencing `WelcomeModal`
- Keep all other exports

**Important:** OnboardingWizard has no index.js barrel export (it's a default export imported directly).
  </action>
  <verify>
- `ls src/components/OnboardingWizard.jsx` returns "No such file"
- `ls src/components/onboarding/OnboardingBanner.jsx` returns "No such file"
- `ls src/pages/dashboard/WelcomeModal.jsx` returns "No such file"
- `grep -r "OnboardingBanner" src/components/onboarding/index.js` returns no matches
- `grep -r "WelcomeModal" src/pages/dashboard/index.js` returns no matches
  </verify>
  <done>Three component files deleted, barrel exports no longer reference deleted components</done>
</task>

<task type="auto">
  <name>Task 3: Remove storage key references from deleted component areas</name>
  <files>No new files - verification only</files>
  <action>
Verify that storage key references are removed along with the deleted components.

The two storage keys targeted for removal are:
- `bizscreen_welcome_modal_shown` (localStorage) - used in WelcomeModal
- `onboarding_banner_dismissed` (sessionStorage) - used in OnboardingBanner

Since both keys were only used in the deleted components, deleting those files should remove the references.

**Verify no remaining references:**
1. Search codebase for `bizscreen_welcome_modal_shown` - should only appear in test files
2. Search codebase for `onboarding_banner_dismissed` - should not appear anywhere

If references remain outside deleted files, identify where and document for Task 2 plan.

Note: Do NOT modify E2E helpers in this task - that's Plan 02.
  </action>
  <verify>
- `grep -r "bizscreen_welcome_modal_shown" src/` returns no matches (test files are OK)
- `grep -r "onboarding_banner_dismissed" src/` returns no matches
  </verify>
  <done>Storage key references removed from source code with component deletion</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Components deleted: 3 files removed (OnboardingWizard, OnboardingBanner, WelcomeModal)
2. Barrel exports updated: No references to deleted components
3. Storage keys: No source code references (test files addressed in Plan 02)
4. Build still works: `npm run build` should succeed (imports not yet cleaned from DashboardPage)

Note: Build may have warnings about unused imports in DashboardPage. This is expected and addressed in Plan 02.
</verification>

<success_criteria>
- E2E baseline documented showing all tests pass
- OnboardingWizard.jsx deleted
- OnboardingBanner.jsx deleted
- WelcomeModal.jsx deleted
- src/components/onboarding/index.js updated
- src/pages/dashboard/index.js updated
- No storage key references in src/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/34-cleanup-and-deprecation/34-01-SUMMARY.md`
</output>
