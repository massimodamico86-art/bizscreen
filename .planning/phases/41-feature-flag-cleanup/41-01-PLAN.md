---
phase: 41-feature-flag-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.js
  - src/pages/DashboardPage.jsx
  - src/App.jsx
  - .env.local
  - tests/e2e/helpers.js
autonomous: true

must_haves:
  truths:
    - "VITE_USE_UNIFIED_ONBOARDING has zero references in source code"
    - "Unified onboarding renders unconditionally (no feature flag gate)"
    - "ScreenPairingReminderCard renders unconditionally on dashboard"
    - "AutoBuildOnboardingModal is fully de-wired from App.jsx (dead code removed)"
    - "E2E tests pass after legacy code removal"
    - "Unit tests pass after legacy code removal"
    - "Build succeeds with no errors"
  artifacts:
    - path: "src/config/env.js"
      provides: "Config without useUnifiedOnboarding property"
      contains: "enableAI"
    - path: "src/pages/DashboardPage.jsx"
      provides: "Dashboard without feature flag conditionals"
      contains: "showUnifiedOnboarding &&"
    - path: "src/App.jsx"
      provides: "App without AutoBuild dead code"
    - path: ".env.local"
      provides: "Env file without VITE_USE_UNIFIED_ONBOARDING"
  key_links:
    - from: "src/pages/DashboardPage.jsx"
      to: "src/components/onboarding/UnifiedOnboardingController"
      via: "direct render gated by showUnifiedOnboarding state (not feature flag)"
      pattern: "showUnifiedOnboarding && "
    - from: "src/pages/DashboardPage.jsx"
      to: "src/components/dashboard/ScreenPairingReminderCard"
      via: "unconditional render (no feature flag gate)"
      pattern: "<ScreenPairingReminderCard"
---

<objective>
Remove the VITE_USE_UNIFIED_ONBOARDING feature flag from the entire codebase and clean up all dead code it was guarding.

Purpose: The unified onboarding is the only path (legacy components deleted in Phase 34). The feature flag gates are dead conditionals that always evaluate to true. Removing them eliminates dead code and simplifies the codebase for the final phase of v2.3.

Output: Clean codebase with zero references to VITE_USE_UNIFIED_ONBOARDING, no AutoBuild dead code in App.jsx, and all tests passing.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-feature-flag-cleanup/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove feature flag from env config, DashboardPage, env file, and test comment</name>
  <files>src/config/env.js, src/pages/DashboardPage.jsx, .env.local, tests/e2e/helpers.js</files>
  <action>
Remove all traces of VITE_USE_UNIFIED_ONBOARDING from env config, DashboardPage, .env.local, and a stale test comment.

**src/config/env.js:**
- Remove the VITE_USE_UNIFIED_ONBOARDING entry from `envSchema.optional` (lines 112-116, the object with description/default/sensitive)
- Remove the `useUnifiedOnboarding: import.meta.env.VITE_USE_UNIFIED_ONBOARDING === 'true'` line from the getConfig() return object (line 214)
- Do NOT touch VITE_ENABLE_AI or VITE_ERROR_TRACKING_PROVIDER entries

**src/pages/DashboardPage.jsx:**
- Line 139: Change `if (config().useUnifiedOnboarding && !loading)` to `if (!loading)` -- keep the loading guard, remove the flag check
- Line 195: Change `{config().useUnifiedOnboarding && showUnifiedOnboarding && (` to `{showUnifiedOnboarding && (` -- keep showUnifiedOnboarding guard
- Line 216: Change `{config().useUnifiedOnboarding && (` to just render `<ScreenPairingReminderCard onNavigate={setCurrentPage} />` unconditionally (remove the wrapping conditional entirely)
- Line 57: Remove `import { config } from '../config/env';` -- after the above changes, config() is no longer used anywhere in this file. Verify by searching the file for any other `config(` usage before removing.
- Remove the comment on line 137 referencing "feature flag - Phase 31" -- update to just say "Check for unified onboarding state"

CRITICAL: Do NOT remove or modify `useUnifiedOnboarding` hook imports, `UnifiedOnboardingController`, `getUnifiedOnboardingState`, or `ScreenPairingReminderCard`. These are ACTIVE code.

**.env.local:**
- Remove line 9: `VITE_USE_UNIFIED_ONBOARDING=true`
- Remove the blank line after it if it results in double blank lines

**tests/e2e/helpers.js:**
- Line 85: Change comment from `This handles the Welcome Modal, OnboardingWizard, and any other dialogs.` to `This handles any modal dialogs that may appear on page load.`
  </action>
  <verify>
Run these commands to verify:
1. `grep -r "VITE_USE_UNIFIED_ONBOARDING" src/ .env.local` -- should return 0 matches
2. `grep -r "useUnifiedOnboarding" src/config/` -- should return 0 matches (removed from config)
3. `grep "useUnifiedOnboarding" src/hooks/useUnifiedOnboarding.js` -- should still exist (active hook, untouched)
4. `grep "config()" src/pages/DashboardPage.jsx` -- should return 0 matches
5. `npx vitest run tests/unit/pages/DashboardPage.test.jsx` -- tests pass
6. `npx vite build 2>&1 | tail -5` -- build succeeds
  </verify>
  <done>
VITE_USE_UNIFIED_ONBOARDING is fully removed from env schema, config export, DashboardPage conditionals, .env.local, and the stale test comment. The config() import is removed from DashboardPage. UnifiedOnboardingController renders gated only by showUnifiedOnboarding state. ScreenPairingReminderCard renders unconditionally. Active onboarding code (useUnifiedOnboarding hook, UnifiedOnboardingController) is untouched. DashboardPage unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead AutoBuild onboarding code from App.jsx</name>
  <files>src/App.jsx</files>
  <action>
Remove the entire AutoBuildOnboardingModal integration from App.jsx. This code is dead because the feature flag guard (`if (config().useUnifiedOnboarding) return;`) always triggers, meaning the AutoBuild path never executes. With the flag removed in Task 1, this must be cleaned up to avoid the AutoBuild modal incorrectly showing.

**Remove from App.jsx:**

1. **Import** (line 40): Remove `import AutoBuildOnboardingModal from './components/onboarding/AutoBuildOnboardingModal';`

2. **State** (line 158): Remove `const [showAutoBuildModal, setShowAutoBuildModal] = useState(false);`

3. **Entire useEffect** (lines 203-226): Remove the complete `checkAutoBuildOnboarding` effect block:
   ```
   // Check if user should see AI autobuild onboarding
   useEffect(() => {
     const checkAutoBuildOnboarding = async () => { ... };
     checkAutoBuildOnboarding();
   }, [authUserProfile?.id, authUserProfile?.role, authUserProfile?.has_completed_onboarding]);
   ```

4. **Props passed to ClientUILayout** (lines 715-716): Remove the two prop lines:
   ```
   showAutoBuildModal={showAutoBuildModal}
   setShowAutoBuildModal={setShowAutoBuildModal}
   ```

5. **ClientUILayout destructured params** (lines 748-749): Remove from the function parameter destructuring:
   ```
   showAutoBuildModal,
   setShowAutoBuildModal,
   ```

6. **AutoBuildOnboardingModal JSX** (lines 1106-1116): Remove the entire block:
   ```
   {/* AI Autobuild Onboarding Modal */}
   <AutoBuildOnboardingModal
     isOpen={showAutoBuildModal}
     onClose={() => setShowAutoBuildModal(false)}
     onComplete={() => { ... }}
     user={user}
     userProfile={authUserProfile}
   />
   ```

7. **config import check**: After removing the useEffect, verify whether `config` is imported and still used elsewhere in App.jsx. Line 207 was the only `config()` usage. If no other usage exists, remove the `import { config } from './config/env';` line as well. Search for `config(` in the file to confirm.

CRITICAL: Do NOT delete the file `src/components/onboarding/AutoBuildOnboardingModal.jsx` itself. Only de-wire it from App.jsx. The file becomes unused but deleting component files is out of scope for this flag cleanup. Also do NOT remove the `fetchScenesForTenant` import if it is used elsewhere in App.jsx (it likely is not -- verify before removing).
  </action>
  <verify>
Run these commands to verify:
1. `grep -c "AutoBuildOnboardingModal\|showAutoBuildModal\|checkAutoBuildOnboarding" src/App.jsx` -- should return 0
2. `grep "config()" src/App.jsx` -- should return 0 matches (config import removed if unused)
3. `grep -r "VITE_USE_UNIFIED_ONBOARDING" src/` -- should return 0 matches across entire src/
4. `npx vite build 2>&1 | tail -5` -- build succeeds with no errors
5. `npx vitest run --reporter=verbose 2>&1 | tail -20` -- all unit tests pass
  </verify>
  <done>
AutoBuildOnboardingModal is fully de-wired from App.jsx: import removed, state removed, useEffect removed, props removed from ClientUILayout, JSX removed. The config() import is removed if no longer used. Zero references to VITE_USE_UNIFIED_ONBOARDING remain in src/. Build succeeds and all unit tests pass.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run comprehensive checks:

1. **Zero flag references:** `grep -r "VITE_USE_UNIFIED_ONBOARDING" --include="*.js" --include="*.jsx" --include="*.ts" --include="*.env*" .` returns 0 matches (excluding .planning/ docs)
2. **Zero config().useUnifiedOnboarding:** `grep -r "useUnifiedOnboarding" src/config/` returns 0 matches
3. **Active onboarding intact:** `grep -r "useUnifiedOnboarding" src/hooks/` returns matches (hook still exists)
4. **Build passes:** `npx vite build` completes without errors
5. **Unit tests pass:** `npx vitest run` all pass
6. **E2E smoke test:** `npx playwright test tests/e2e/dashboard.spec.js --project=chromium` passes (dashboard renders correctly without flag)
</verification>

<success_criteria>
1. Zero references to VITE_USE_UNIFIED_ONBOARDING in source code and env files
2. Zero references to AutoBuildOnboardingModal in App.jsx
3. UnifiedOnboardingController still renders (gated by showUnifiedOnboarding state, not feature flag)
4. ScreenPairingReminderCard renders unconditionally on dashboard
5. useUnifiedOnboarding hook and UnifiedOnboardingController are untouched (active code)
6. Build succeeds, unit tests pass, E2E dashboard tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/41-feature-flag-cleanup/41-01-SUMMARY.md`
</output>
