---
phase: 37-e2e-test-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/auth.spec.js
  - tests/e2e/helpers.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Auth tests pass 5 consecutive runs locally"
    - "Auth tests contain zero waitForTimeout calls"
    - "Helpers use proper auto-waiting patterns"
  artifacts:
    - path: "tests/e2e/auth.spec.js"
      provides: "Stabilized auth tests"
      contains: "expect.*toBeVisible"
    - path: "tests/e2e/helpers.js"
      provides: "Stabilized helper functions"
      contains: "waitFor.*state"
    - path: ".planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md"
      provides: "Tracking document for skipped tests"
  key_links:
    - from: "tests/e2e/auth.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import { loginAndPrepare }"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 1: Core Auth & Navigation tests by removing all waitForTimeout calls and applying proper Playwright auto-waiting patterns.

Purpose: Auth tests are the foundation - they validate the authentication flow used by all other tests. Stabilizing these first ensures the base is solid.
Output: auth.spec.js and helpers.js with zero waitForTimeout calls, passing 5 consecutive runs.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md

@tests/e2e/auth.spec.js
@tests/e2e/helpers.js
@tests/e2e/fixtures/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize helpers.js</name>
  <files>tests/e2e/helpers.js</files>
  <action>
Replace all flaky patterns in helpers.js with proper auto-waiting:

1. In `dismissAnyModals()`:
   - Replace `.isVisible({ timeout: 100 }).catch(() => false)` with proper locator checks
   - Use `expect(dialog).not.toBeVisible()` instead of `waitFor({ state: 'hidden', timeout: 1000 }).catch(() => {})`
   - Remove `.catch(() => {})` swallowing - let failures surface

2. In `waitForPageReady()`:
   - Replace `waitForLoadState('networkidle', { timeout: 10000 }).catch(() => {})` with element-based waits
   - Use `expect(locator).not.toBeVisible()` for loading indicators instead of `.catch(() => {})`

3. Remove ALL `.catch(() => false)` and `.catch(() => {})` patterns - these mask real failures

Patterns to apply (from RESEARCH.md):
- `await expect(element).toBeVisible()` instead of `isVisible().catch(() => false)`
- `await element.waitFor({ state: 'hidden' })` for modal close
- Element-based waits instead of networkidle
  </action>
  <verify>
Run: `grep -c "catch.*=>.*false\|catch.*=>.*{}" tests/e2e/helpers.js`
Result should be 0 (no catch swallowing patterns)
  </verify>
  <done>helpers.js has zero .catch(() => false) or .catch(() => {}) patterns</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize auth.spec.js</name>
  <files>tests/e2e/auth.spec.js</files>
  <action>
Remove all waitForTimeout calls from auth.spec.js:

1. Line 240: `await page.waitForTimeout(1000);` in reset password test
   - Replace with: `await expect(page.getByRole('heading', { name: /check your email|reset.*password/i })).toBeVisible()`

2. Line 324: `await page.waitForTimeout(300);` in logout test
   - Replace with: Wait for menu to be visible after click
   - Use: `await expect(page.locator('[role="menu"]')).toBeVisible()` or appropriate selector

3. Review `.catch(() => false)` patterns in auth.spec.js:
   - Lines using `.isVisible().catch(() => false)` - evaluate if masking real issues
   - Replace with proper assertions where appropriate

4. Ensure all assertions use `await expect()` pattern (web-first assertions)

5. Check for any remaining `isVisible()` used without expect() wrapper - these don't auto-retry
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/auth.spec.js`
Result should be 0
  </verify>
  <done>auth.spec.js has zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Verify with 5 consecutive runs and create SKIPPED-TESTS.md</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
1. Run auth tests 5 consecutive times locally:
```bash
for i in {1..5}; do
  echo "=== Run $i of 5 ==="
  npx playwright test tests/e2e/auth.spec.js --reporter=line
  if [ $? -ne 0 ]; then
    echo "FAILED on run $i"
    exit 1
  fi
done
echo "All 5 runs passed"
```

2. If any test fails:
   - Investigate root cause (not just the symptom)
   - Fix the underlying issue
   - Restart the 5-run count

3. If a test is unfixable after reasonable effort:
   - Add `test.skip(true, 'Reason - see SKIPPED-TESTS.md');` as first line
   - Document in SKIPPED-TESTS.md

4. Create SKIPPED-TESTS.md with format:
```markdown
# Skipped E2E Tests

## Summary
- Total skipped: 0
- Unfixable timing issues: 0
- External dependencies: 0

## Skipped Tests

(None in Category 1)
```

Update the summary counts if any tests are skipped.
  </action>
  <verify>
Run: `npx playwright test tests/e2e/auth.spec.js --reporter=line`
All tests pass (skipped tests don't count as failures)
  </verify>
  <done>Auth tests pass 5 consecutive runs, SKIPPED-TESTS.md exists</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout" tests/e2e/auth.spec.js tests/e2e/helpers.js` returns 0 for both files
2. `grep -c "catch.*=>.*false" tests/e2e/helpers.js` returns 0
3. Auth tests pass: `npx playwright test tests/e2e/auth.spec.js --reporter=line`
4. SKIPPED-TESTS.md exists in phase directory
</verification>

<success_criteria>
- Zero waitForTimeout calls in auth.spec.js and helpers.js
- Zero .catch(() => false) patterns in helpers.js
- Auth tests pass 5 consecutive local runs
- SKIPPED-TESTS.md created and tracking any skipped tests
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-01-SUMMARY.md`
</output>
