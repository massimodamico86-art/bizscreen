---
phase: 37-e2e-test-stabilization
plan: 05
type: execute
wave: 5
depends_on: ["37-04"]
files_modified:
  - tests/e2e/content-performance.spec.js
  - tests/e2e/template-marketplace.spec.js
  - tests/e2e/template-packs.spec.js
  - tests/e2e/playlist-template.spec.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Content and template tests pass 5 consecutive runs"
    - "All four files contain zero waitForTimeout calls"
  artifacts:
    - path: "tests/e2e/content-performance.spec.js"
      provides: "Stabilized content performance tests"
    - path: "tests/e2e/template-marketplace.spec.js"
      provides: "Stabilized template marketplace tests"
    - path: "tests/e2e/template-packs.spec.js"
      provides: "Stabilized template packs tests"
    - path: "tests/e2e/playlist-template.spec.js"
      provides: "Stabilized playlist template tests"
  key_links:
    - from: "tests/e2e/template-marketplace.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 5: Content & Templates tests by removing all waitForTimeout calls (43 total across 4 files).

Purpose: These files have high waitForTimeout density (11-15 each) testing content loading and template operations.
Output: content-performance.spec.js (15), template-marketplace.spec.js (11), template-packs.spec.js (13), playlist-template.spec.js (4) with zero waitForTimeout calls.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md
@.planning/phases/37-e2e-test-stabilization/37-04-SUMMARY.md

@tests/e2e/content-performance.spec.js
@tests/e2e/template-marketplace.spec.js
@tests/e2e/template-packs.spec.js
@tests/e2e/playlist-template.spec.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize content-performance.spec.js (15 calls)</name>
  <files>tests/e2e/content-performance.spec.js</files>
  <action>
Content performance tests measure load times and rendering, which explains the many waits.

Common patterns to replace:
- Waiting for content to load: use `expect(contentElement).toBeVisible()`
- Waiting for images: `await page.waitForLoadState('domcontentloaded')` or specific image checks
- Waiting for performance metrics: may need response waits or custom logic

For performance-specific tests:
- Some may legitimately need timing but should use `page.waitForEvent` or response timing
- NOT arbitrary waitForTimeout

Be especially careful with performance tests - they may have intentional timing logic that needs to be preserved differently.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/content-performance.spec.js`
Should return 0
  </verify>
  <done>content-performance.spec.js has zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize template tests (28 calls total)</name>
  <files>tests/e2e/template-marketplace.spec.js, tests/e2e/template-packs.spec.js, tests/e2e/playlist-template.spec.js</files>
  <action>
**template-marketplace.spec.js (11 calls):**
Marketplace tests wait for:
- Template grid loading
- Template preview modal
- Template install/purchase actions

Replace with:
- Grid item visibility checks
- Modal visibility waits
- API response waits for actions

**template-packs.spec.js (13 calls):**
Pack tests wait for:
- Pack content loading
- Pack install operations
- Content synchronization

Similar patterns - use element and response waits.

**playlist-template.spec.js (4 calls):**
Playlist template tests wait for:
- Template application to playlist
- Preview updates

Replace with appropriate waits.

Work through each file systematically.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/template-marketplace.spec.js tests/e2e/template-packs.spec.js tests/e2e/playlist-template.spec.js`
All should return 0
  </verify>
  <done>All template test files have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Run 5-consecutive verification for Category 5</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
Run 5-consecutive verification:

```bash
for file in content-performance template-marketplace template-packs playlist-template; do
  echo "=== Testing $file.spec.js ==="
  for i in {1..5}; do
    echo "Run $i of 5"
    npx playwright test tests/e2e/$file.spec.js --reporter=line
    if [ $? -ne 0 ]; then
      echo "FAILED $file on run $i"
      break
    fi
  done
done
```

Content tests may have more failures due to:
- Heavy API dependencies
- Image/media loading variability
- Performance test timing sensitivity

Be prepared to skip more tests in this category with good documentation.
  </action>
  <verify>
Run: `npx playwright test tests/e2e/content-performance.spec.js tests/e2e/template-marketplace.spec.js tests/e2e/template-packs.spec.js tests/e2e/playlist-template.spec.js --reporter=line`
  </verify>
  <done>All Category 5 tests pass 5 consecutive runs</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout"` returns 0 for all 4 files
2. All test files pass
3. SKIPPED-TESTS.md updated
</verification>

<success_criteria>
- Zero waitForTimeout calls across all 4 files (43 removed)
- All 4 test files pass 5 consecutive local runs
- SKIPPED-TESTS.md updated with Category 5 section if any skips
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-05-SUMMARY.md`
</output>
