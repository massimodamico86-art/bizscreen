---
phase: 37-e2e-test-stabilization
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - tests/e2e/smoke.spec.js
  - tests/e2e/smoke-test-client.spec.js
  - tests/e2e/client-interactions.spec.js
  - tests/e2e/client-flows.spec.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Smoke tests pass 5 consecutive runs"
    - "Client tests pass 5 consecutive runs"
    - "All four files contain zero waitForTimeout calls"
  artifacts:
    - path: "tests/e2e/smoke.spec.js"
      provides: "Stabilized smoke tests"
    - path: "tests/e2e/smoke-test-client.spec.js"
      provides: "Stabilized client smoke tests"
    - path: "tests/e2e/client-interactions.spec.js"
      provides: "Stabilized client interaction tests"
    - path: "tests/e2e/client-flows.spec.js"
      provides: "Stabilized client flow tests"
  key_links:
    - from: "tests/e2e/smoke.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 3: Complex Interactions tests by removing all waitForTimeout calls (39 total across 4 files).

Purpose: These are the highest-density files with 14-15 waitForTimeout calls each. They test complex multi-step interactions that are most prone to flakiness.
Output: smoke.spec.js (14), smoke-test-client.spec.js (15), client-interactions.spec.js (4), client-flows.spec.js (6) with zero waitForTimeout calls.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md
@.planning/phases/37-e2e-test-stabilization/37-02-SUMMARY.md

@tests/e2e/smoke.spec.js
@tests/e2e/smoke-test-client.spec.js
@tests/e2e/client-interactions.spec.js
@tests/e2e/client-flows.spec.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize smoke.spec.js (14 calls)</name>
  <files>tests/e2e/smoke.spec.js</files>
  <action>
This file has the most waitForTimeout calls. Read it carefully and for each:

1. Find each `waitForTimeout` with grep and line numbers
2. Read context around each to understand what it's waiting for
3. Replace with appropriate pattern:

Common smoke test patterns:
- After navigation: `await page.waitForURL(/pattern/)` or `await expect(heading).toBeVisible()`
- After modal actions: `await dialog.waitFor({ state: 'hidden' })`
- After form submit: capture `waitForResponse` BEFORE click
- After tab/section click: `await expect(content).toBeVisible()`

Also clean up excessive `.catch(() => false)` patterns - these hide real failures in smoke tests which defeats their purpose.

Work systematically through the file top to bottom.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/smoke.spec.js`
Should return 0
  </verify>
  <done>smoke.spec.js has zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize client tests (25 calls total)</name>
  <files>tests/e2e/smoke-test-client.spec.js, tests/e2e/client-interactions.spec.js, tests/e2e/client-flows.spec.js</files>
  <action>
**smoke-test-client.spec.js (15 calls):**
Client tests often wait for:
- Screen preview to load
- Content to render
- Playlist transitions
- API responses for content fetch

For each waitForTimeout, replace with element or response-based wait.

**client-interactions.spec.js (4 calls):**
Interaction tests typically wait for:
- Touch/click response
- UI state changes
- Animation completion

Replace with element state waits: `expect(element).toBeVisible()` or `toHaveClass()`

**client-flows.spec.js (6 calls):**
Flow tests wait for:
- Navigation between screens
- Playlist progression
- State persistence

Replace with URL waits or element visibility checks.

For all three files:
1. Grep for waitForTimeout with line numbers
2. Replace each systematically
3. Remove .catch() masking patterns
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/smoke-test-client.spec.js tests/e2e/client-interactions.spec.js tests/e2e/client-flows.spec.js`
All should return 0
  </verify>
  <done>All client test files have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Run 5-consecutive verification for Category 3</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
Run 5-consecutive verification:

```bash
for file in smoke smoke-test-client client-interactions client-flows; do
  echo "=== Testing $file.spec.js ==="
  for i in {1..5}; do
    echo "Run $i of 5"
    npx playwright test tests/e2e/$file.spec.js --reporter=line
    if [ $? -ne 0 ]; then
      echo "FAILED $file on run $i"
      break
    fi
  done
done
```

These tests are complex and likely to have more failures. For each failure:
1. Analyze the failure (not just the timeout but the root cause)
2. Apply appropriate fix
3. Restart 5-run count

If a test is truly unfixable (external timing dependency, third-party API):
- Add test.skip with reason
- Document in SKIPPED-TESTS.md

Expect some tests may need skipping in this category.
  </action>
  <verify>
Run: `npx playwright test tests/e2e/smoke.spec.js tests/e2e/smoke-test-client.spec.js tests/e2e/client-interactions.spec.js tests/e2e/client-flows.spec.js --reporter=line`
  </verify>
  <done>All Category 3 tests pass 5 consecutive runs (or are properly skipped)</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout" tests/e2e/smoke.spec.js tests/e2e/smoke-test-client.spec.js tests/e2e/client-interactions.spec.js tests/e2e/client-flows.spec.js` returns 0 for all
2. All four test files pass or have properly documented skips
3. SKIPPED-TESTS.md updated with Category 3 section
</verification>

<success_criteria>
- Zero waitForTimeout calls across all 4 files (39 removed)
- All 4 test files pass 5 consecutive local runs
- Any skipped tests documented with reason, what was tried, suggested fix
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-03-SUMMARY.md`
</output>
