---
phase: 37-e2e-test-stabilization
plan: 06
type: execute
wave: 6
depends_on: ["37-05"]
files_modified:
  - tests/e2e/scenes.spec.js
  - tests/e2e/scene-editor.spec.js
  - tests/e2e/polotno-editor.spec.js
  - tests/e2e/screen-assignments.spec.js
  - tests/e2e/playlist-screen-persistence.spec.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Advanced feature tests pass 5 consecutive runs"
    - "All files contain zero waitForTimeout calls"
  artifacts:
    - path: "tests/e2e/scenes.spec.js"
      provides: "Stabilized scenes tests"
    - path: "tests/e2e/scene-editor.spec.js"
      provides: "Stabilized scene editor tests"
    - path: "tests/e2e/polotno-editor.spec.js"
      provides: "Stabilized polotno editor tests"
    - path: "tests/e2e/screen-assignments.spec.js"
      provides: "Stabilized screen assignments tests"
    - path: "tests/e2e/playlist-screen-persistence.spec.js"
      provides: "Stabilized persistence tests"
  key_links:
    - from: "tests/e2e/scene-editor.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 6: Advanced Features tests by removing all waitForTimeout calls (30 total across 5 files).

Purpose: These test complex editor functionality and data persistence - the most challenging category.
Output: scenes.spec.js (8), scene-editor.spec.js (9), polotno-editor.spec.js (1), screen-assignments.spec.js (6), playlist-screen-persistence.spec.js (6) with zero waitForTimeout calls.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md
@.planning/phases/37-e2e-test-stabilization/37-05-SUMMARY.md

@tests/e2e/scenes.spec.js
@tests/e2e/scene-editor.spec.js
@tests/e2e/polotno-editor.spec.js
@tests/e2e/screen-assignments.spec.js
@tests/e2e/playlist-screen-persistence.spec.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize scenes.spec.js and scene-editor.spec.js</name>
  <files>tests/e2e/scenes.spec.js, tests/e2e/scene-editor.spec.js</files>
  <action>
NOTE: Research indicates scenes.spec.js and scene-editor.spec.js may already be skipped. Check first:
`grep -n "test.describe.skip\|test.skip" tests/e2e/scenes.spec.js tests/e2e/scene-editor.spec.js`

If entirely skipped - document in SKIPPED-TESTS.md and move on.

If not skipped, these editor tests are complex:

**scenes.spec.js (8 calls):**
- Scene creation/deletion
- Scene preview
- Scene transitions

**scene-editor.spec.js (9 calls):**
- Canvas interactions
- Layer management
- Asset placement

Editor tests often wait for:
- Canvas render completion
- Asset loading
- Undo/redo state updates

These may require:
- Canvas-specific element checks
- Animation frame completion (harder to detect)
- Third-party library state (Polotno)

May need to skip some tests that depend on third-party canvas timing.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/scenes.spec.js tests/e2e/scene-editor.spec.js`
Should return 0 (or files are fully skipped)
  </verify>
  <done>scenes.spec.js and scene-editor.spec.js have zero waitForTimeout calls or are properly skipped</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize remaining advanced feature tests</name>
  <files>tests/e2e/polotno-editor.spec.js, tests/e2e/screen-assignments.spec.js, tests/e2e/playlist-screen-persistence.spec.js</files>
  <action>
**polotno-editor.spec.js (1 call):**
Just one waitForTimeout - should be straightforward to fix.
Polotno is the third-party canvas library.

**screen-assignments.spec.js (6 calls):**
Screen assignment tests wait for:
- Assignment modal
- Screen selection
- Assignment confirmation

Replace with element and response waits.

**playlist-screen-persistence.spec.js (6 calls):**
Persistence tests wait for:
- Data save operations
- State reload verification
- Cross-session persistence

Use response waits for saves, element checks for reload.

Work through each systematically.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/polotno-editor.spec.js tests/e2e/screen-assignments.spec.js tests/e2e/playlist-screen-persistence.spec.js`
All should return 0
  </verify>
  <done>All remaining advanced feature tests have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Run 5-consecutive verification for Category 6</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
Run 5-consecutive verification for non-skipped files:

```bash
for file in scenes scene-editor polotno-editor screen-assignments playlist-screen-persistence; do
  echo "=== Testing $file.spec.js ==="
  for i in {1..5}; do
    echo "Run $i of 5"
    npx playwright test tests/e2e/$file.spec.js --reporter=line
    if [ $? -ne 0 ]; then
      echo "FAILED $file on run $i"
      break
    fi
  done
done
```

Editor tests are highest risk for unfixable issues:
- Canvas timing is hard to control
- Third-party library (Polotno) internal state
- Complex async rendering

Be liberal with skip documentation for editor-specific issues.
  </action>
  <verify>
Run passing test files to confirm stability
  </verify>
  <done>All Category 6 tests pass 5 consecutive runs (or properly skipped)</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout"` returns 0 for all files
2. All test files pass (or are properly skipped with documentation)
3. SKIPPED-TESTS.md updated with Category 6 section
</verification>

<success_criteria>
- Zero waitForTimeout calls across all 5 files
- All test files pass 5 consecutive local runs (or documented skips)
- SKIPPED-TESTS.md comprehensive for editor tests
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-06-SUMMARY.md`
</output>
