---
phase: 37-e2e-test-stabilization
plan: 04
type: execute
wave: 4
depends_on: ["37-03"]
files_modified:
  - tests/e2e/schedules.spec.js
  - tests/e2e/settings.spec.js
  - tests/e2e/admin.spec.js
  - tests/e2e/brand-theme.spec.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Feature page tests pass 5 consecutive runs"
    - "All four files contain zero waitForTimeout calls"
  artifacts:
    - path: "tests/e2e/schedules.spec.js"
      provides: "Stabilized schedules tests"
    - path: "tests/e2e/settings.spec.js"
      provides: "Stabilized settings tests"
    - path: "tests/e2e/admin.spec.js"
      provides: "Stabilized admin tests"
    - path: "tests/e2e/brand-theme.spec.js"
      provides: "Stabilized brand theme tests"
  key_links:
    - from: "tests/e2e/schedules.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 4: Feature-Specific Pages tests by removing all waitForTimeout calls (13 total across 4 files).

Purpose: These pages have specific functionality (scheduling, settings, admin, theming) with moderate complexity.
Output: schedules.spec.js (6), settings.spec.js (1), admin.spec.js (2), brand-theme.spec.js (4) with zero waitForTimeout calls.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md
@.planning/phases/37-e2e-test-stabilization/37-03-SUMMARY.md

@tests/e2e/schedules.spec.js
@tests/e2e/settings.spec.js
@tests/e2e/admin.spec.js
@tests/e2e/brand-theme.spec.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize schedules.spec.js and brand-theme.spec.js</name>
  <files>tests/e2e/schedules.spec.js, tests/e2e/brand-theme.spec.js</files>
  <action>
**schedules.spec.js (6 waitForTimeout calls):**

Schedule tests typically wait for:
- Date/time picker interactions
- Schedule save confirmation
- Calendar view updates
- Modal open/close

For date pickers: wait for the picker element to be visible
For saves: use waitForResponse on the API call
For calendar updates: wait for the updated content to appear

**brand-theme.spec.js (4 waitForTimeout calls):**

Theme tests wait for:
- Color picker interactions
- Preview updates
- Theme save confirmation

Replace with:
- Element visibility checks for pickers
- Response waits for save operations
- Visual state checks for preview updates

Work through each file systematically.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/schedules.spec.js tests/e2e/brand-theme.spec.js`
Both should return 0
  </verify>
  <done>schedules.spec.js and brand-theme.spec.js have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize settings.spec.js and admin.spec.js</name>
  <files>tests/e2e/settings.spec.js, tests/e2e/admin.spec.js</files>
  <action>
**settings.spec.js (1 waitForTimeout call):**

Settings page is simpler - likely just one wait for:
- Settings save or
- Navigation/tab switch

Identify and replace with appropriate pattern.

**admin.spec.js (2 waitForTimeout calls):**

Admin tests wait for:
- Admin panel load
- User management actions
- Permission changes

Replace with element or response waits.

Both files should be straightforward with few calls.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/settings.spec.js tests/e2e/admin.spec.js`
Both should return 0
  </verify>
  <done>settings.spec.js and admin.spec.js have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Run 5-consecutive verification for Category 4</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
Run 5-consecutive verification:

```bash
for file in schedules settings admin brand-theme; do
  echo "=== Testing $file.spec.js ==="
  for i in {1..5}; do
    echo "Run $i of 5"
    npx playwright test tests/e2e/$file.spec.js --reporter=line
    if [ $? -ne 0 ]; then
      echo "FAILED $file on run $i"
      break
    fi
  done
done
```

Handle failures as before:
1. Investigate root cause
2. Fix and restart count
3. Skip unfixable tests with documentation

Update SKIPPED-TESTS.md with any Category 4 entries.
  </action>
  <verify>
Run: `npx playwright test tests/e2e/schedules.spec.js tests/e2e/settings.spec.js tests/e2e/admin.spec.js tests/e2e/brand-theme.spec.js --reporter=line`
  </verify>
  <done>All Category 4 tests pass 5 consecutive runs</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout"` returns 0 for all 4 files
2. All test files pass
3. SKIPPED-TESTS.md updated
</verification>

<success_criteria>
- Zero waitForTimeout calls across all 4 files (13 removed)
- All 4 test files pass 5 consecutive local runs
- SKIPPED-TESTS.md updated with Category 4 section if any skips
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-04-SUMMARY.md`
</output>
