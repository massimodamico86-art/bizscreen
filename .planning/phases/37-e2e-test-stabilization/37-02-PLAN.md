---
phase: 37-e2e-test-stabilization
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - tests/e2e/dashboard.spec.js
  - tests/e2e/screens.spec.js
  - tests/e2e/playlists.spec.js
  - tests/e2e/media.spec.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Dashboard tests pass 5 consecutive runs"
    - "Screens tests pass 5 consecutive runs"
    - "Playlists tests pass 5 consecutive runs"
    - "Media tests pass 5 consecutive runs"
    - "All four files contain zero waitForTimeout calls"
  artifacts:
    - path: "tests/e2e/dashboard.spec.js"
      provides: "Stabilized dashboard tests"
    - path: "tests/e2e/screens.spec.js"
      provides: "Stabilized screens tests"
    - path: "tests/e2e/playlists.spec.js"
      provides: "Stabilized playlists tests"
    - path: "tests/e2e/media.spec.js"
      provides: "Stabilized media tests"
  key_links:
    - from: "tests/e2e/dashboard.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 2: Dashboard & Basic Pages tests by removing all waitForTimeout calls (13 total across 4 files).

Purpose: These are high-traffic pages that many other tests navigate through. Stabilizing them reduces cascading failures.
Output: dashboard.spec.js (3), screens.spec.js (5), playlists.spec.js (3), media.spec.js (2) with zero waitForTimeout calls.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md
@.planning/phases/37-e2e-test-stabilization/37-01-SUMMARY.md

@tests/e2e/dashboard.spec.js
@tests/e2e/screens.spec.js
@tests/e2e/playlists.spec.js
@tests/e2e/media.spec.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize dashboard.spec.js and screens.spec.js</name>
  <files>tests/e2e/dashboard.spec.js, tests/e2e/screens.spec.js</files>
  <action>
**dashboard.spec.js (3 waitForTimeout calls):**

For each waitForTimeout, identify what it's waiting for and replace:
- If waiting for element: `await expect(element).toBeVisible()`
- If waiting for modal close: `await element.waitFor({ state: 'hidden' })`
- If waiting for navigation: `await page.waitForURL(/pattern/)`
- If waiting for API: Capture response promise BEFORE action, await after

Also clean up any `.catch(() => false)` patterns that mask failures.

**screens.spec.js (5 waitForTimeout calls):**

Apply same patterns. Common scenarios:
- After clicking Add Screen button -> wait for modal with `expect(dialog).toBeVisible()`
- After form submission -> wait for API with `waitForResponse`
- After modal close -> wait for dialog hidden with `waitFor({ state: 'hidden' })`

For both files:
1. Read file to find all waitForTimeout locations
2. Understand context of each (what is the wait actually for?)
3. Replace with appropriate auto-waiting pattern from RESEARCH.md
4. Remove excessive .catch() patterns
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/dashboard.spec.js tests/e2e/screens.spec.js`
Both should return 0
  </verify>
  <done>dashboard.spec.js and screens.spec.js have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize playlists.spec.js and media.spec.js</name>
  <files>tests/e2e/playlists.spec.js, tests/e2e/media.spec.js</files>
  <action>
**playlists.spec.js (3 waitForTimeout calls):**

Common patterns in playlist tests:
- After creating playlist -> wait for success toast or redirect
- After adding media to playlist -> wait for list update
- After saving -> wait for API response

Replace each with appropriate pattern.

**media.spec.js (2 waitForTimeout calls):**

Common patterns:
- After uploading media -> wait for upload completion indicator
- After filtering -> wait for filtered results to appear

For both files:
1. Identify each waitForTimeout context
2. Replace with proper wait (element, URL, response, or state)
3. Clean up any .catch(() => false) masking
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/playlists.spec.js tests/e2e/media.spec.js`
Both should return 0
  </verify>
  <done>playlists.spec.js and media.spec.js have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Run 5-consecutive verification for all Category 2 tests</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
Run 5-consecutive verification for each file:

```bash
# Test each file 5 times
for file in dashboard screens playlists media; do
  echo "=== Testing $file.spec.js ==="
  for i in {1..5}; do
    echo "Run $i of 5"
    npx playwright test tests/e2e/$file.spec.js --reporter=line
    if [ $? -ne 0 ]; then
      echo "FAILED $file on run $i"
      # Continue to next file to see full picture
      break
    fi
  done
done
```

For any failing tests:
1. Investigate root cause
2. Fix and restart 5-run count for that file
3. If unfixable: skip with documentation

Update SKIPPED-TESTS.md with any Category 2 skipped tests:
- Add section for each file if needed
- Include reason, what was tried, suggested fix
  </action>
  <verify>
Run each test file once and verify pass:
```bash
npx playwright test tests/e2e/dashboard.spec.js tests/e2e/screens.spec.js tests/e2e/playlists.spec.js tests/e2e/media.spec.js --reporter=line
```
  </verify>
  <done>All Category 2 tests pass 5 consecutive runs, SKIPPED-TESTS.md updated</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout" tests/e2e/dashboard.spec.js tests/e2e/screens.spec.js tests/e2e/playlists.spec.js tests/e2e/media.spec.js` returns 0 for all
2. All four test files pass: `npx playwright test tests/e2e/dashboard.spec.js tests/e2e/screens.spec.js tests/e2e/playlists.spec.js tests/e2e/media.spec.js`
3. SKIPPED-TESTS.md updated with any Category 2 skips
</verification>

<success_criteria>
- Zero waitForTimeout calls across all 4 files
- All 4 test files pass 5 consecutive local runs
- SKIPPED-TESTS.md updated with Category 2 section if any skips
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-02-SUMMARY.md`
</output>
