---
phase: 37-e2e-test-stabilization
plan: 07
type: execute
wave: 7
depends_on: ["37-06"]
files_modified:
  - tests/e2e/alerts-center.spec.js
  - tests/e2e/alert-notification-flow.spec.js
  - tests/e2e/feature-diagnostic.spec.js
  - tests/e2e/location-diagnostic.spec.js
  - .planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md
autonomous: true

must_haves:
  truths:
    - "Alert and diagnostic tests pass 5 consecutive runs"
    - "All four files contain zero waitForTimeout calls"
  artifacts:
    - path: "tests/e2e/alerts-center.spec.js"
      provides: "Stabilized alerts center tests"
    - path: "tests/e2e/alert-notification-flow.spec.js"
      provides: "Stabilized alert notification tests"
    - path: "tests/e2e/feature-diagnostic.spec.js"
      provides: "Stabilized feature diagnostic tests"
    - path: "tests/e2e/location-diagnostic.spec.js"
      provides: "Stabilized location diagnostic tests"
  key_links:
    - from: "tests/e2e/alerts-center.spec.js"
      to: "tests/e2e/helpers.js"
      via: "import helpers"
      pattern: "import.*helpers"
---

<objective>
Stabilize Category 7: Alerts & Diagnostics tests by removing all waitForTimeout calls (13 total across 4 files).

Purpose: These tests verify alerting and diagnostic features, important for production monitoring.
Output: alerts-center.spec.js (1), alert-notification-flow.spec.js (3), feature-diagnostic.spec.js (8), location-diagnostic.spec.js (1) with zero waitForTimeout calls.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-e2e-test-stabilization/37-CONTEXT.md
@.planning/phases/37-e2e-test-stabilization/37-RESEARCH.md
@.planning/phases/37-e2e-test-stabilization/37-06-SUMMARY.md

@tests/e2e/alerts-center.spec.js
@tests/e2e/alert-notification-flow.spec.js
@tests/e2e/feature-diagnostic.spec.js
@tests/e2e/location-diagnostic.spec.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize alerts tests</name>
  <files>tests/e2e/alerts-center.spec.js, tests/e2e/alert-notification-flow.spec.js</files>
  <action>
**alerts-center.spec.js (1 call):**
Just one waitForTimeout - identify what it's waiting for and replace.

**alert-notification-flow.spec.js (3 calls):**
Notification flow tests wait for:
- Alert to appear (real-time events)
- Notification toast
- Alert acknowledgment

For real-time alerts:
- Use `waitForResponse` if triggered by API
- Use `expect(alertElement).toBeVisible()` for UI appearance
- For toast notifications: `await expect(toast).toBeVisible()`

May need longer timeouts for actual notification delivery, but use element waits not arbitrary delays.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/alerts-center.spec.js tests/e2e/alert-notification-flow.spec.js`
Both should return 0
  </verify>
  <done>alerts-center.spec.js and alert-notification-flow.spec.js have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize diagnostic tests</name>
  <files>tests/e2e/feature-diagnostic.spec.js, tests/e2e/location-diagnostic.spec.js</files>
  <action>
**feature-diagnostic.spec.js (8 calls):**
Diagnostic tests run checks and wait for:
- Diagnostic results to complete
- Status indicators to update
- Check completion states

Replace with:
- Result element visibility
- Status text/class changes
- Completion indicators

**location-diagnostic.spec.js (1 call - 5 seconds!):**
Research noted this has a 5-second waitForTimeout - likely waiting for location services or geolocation API.

This may need:
- Permission mocking for geolocation
- Longer explicit timeout on element wait
- Or skip if truly geolocation-dependent

Investigate the context before replacing.
  </action>
  <verify>
Run: `grep -c "waitForTimeout" tests/e2e/feature-diagnostic.spec.js tests/e2e/location-diagnostic.spec.js`
Both should return 0
  </verify>
  <done>feature-diagnostic.spec.js and location-diagnostic.spec.js have zero waitForTimeout calls</done>
</task>

<task type="auto">
  <name>Task 3: Run 5-consecutive verification for Category 7</name>
  <files>.planning/phases/37-e2e-test-stabilization/SKIPPED-TESTS.md</files>
  <action>
Run 5-consecutive verification:

```bash
for file in alerts-center alert-notification-flow feature-diagnostic location-diagnostic; do
  echo "=== Testing $file.spec.js ==="
  for i in {1..5}; do
    echo "Run $i of 5"
    npx playwright test tests/e2e/$file.spec.js --reporter=line
    if [ $? -ne 0 ]; then
      echo "FAILED $file on run $i"
      break
    fi
  done
done
```

Diagnostic tests may have external dependencies (network checks, location services) that cause flakiness. Document appropriately.
  </action>
  <verify>
Run: `npx playwright test tests/e2e/alerts-center.spec.js tests/e2e/alert-notification-flow.spec.js tests/e2e/feature-diagnostic.spec.js tests/e2e/location-diagnostic.spec.js --reporter=line`
  </verify>
  <done>All Category 7 tests pass 5 consecutive runs</done>
</task>

</tasks>

<verification>
1. `grep -c "waitForTimeout"` returns 0 for all 4 files
2. All test files pass
3. SKIPPED-TESTS.md updated
</verification>

<success_criteria>
- Zero waitForTimeout calls across all 4 files (13 removed)
- All 4 test files pass 5 consecutive local runs
- SKIPPED-TESTS.md updated with Category 7 section if any skips
</success_criteria>

<output>
After completion, create `.planning/phases/37-e2e-test-stabilization/37-07-SUMMARY.md`
</output>
