---
phase: 84-playlists-layouts-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PlaylistsPage.jsx
  - src/pages/PlaylistEditorPage.jsx
  - src/pages/components/PlaylistEditorComponents.jsx
autonomous: false
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03

must_haves:
  truths:
    - "User can create a playlist with a name, description, and default duration"
    - "User can rename a playlist from within the playlist editor"
    - "User can delete a playlist from the playlists list with usage-aware confirmation"
    - "Playlist editor supports adding items from the media library via click or drag-and-drop"
    - "Playlist editor supports reordering items via drag-and-drop AND up/down arrow buttons"
    - "Playlist editor supports removing individual items from the timeline"
    - "Per-item duration is editable and persists on save"
    - "Default duration and transition effect settings save correctly"
  artifacts:
    - path: "src/pages/PlaylistsPage.jsx"
      provides: "Playlist CRUD (create, delete, duplicate, search)"
      min_lines: 100
    - path: "src/pages/PlaylistEditorPage.jsx"
      provides: "Playlist editor with media library, timeline, reordering"
      min_lines: 100
    - path: "src/pages/components/PlaylistEditorComponents.jsx"
      provides: "PlaylistStripItem, LibraryMediaItem, modals"
      min_lines: 50
  key_links:
    - from: "src/pages/PlaylistsPage.jsx"
      to: "supabase playlists table"
      via: "supabase.from('playlists').insert/delete"
      pattern: "supabase.*from.*playlists"
    - from: "src/pages/PlaylistEditorPage.jsx"
      to: "usePlaylistEditor hook"
      via: "usePlaylistEditor(playlistId)"
      pattern: "usePlaylistEditor"
---

<objective>
Verify and fix playlist CRUD operations and the playlist editor, ensuring create/rename/delete, item add/reorder/remove via drag-and-drop AND arrow buttons, and settings (duration, transitions) all work correctly.

Purpose: Users must be able to build and manage playlists — the primary content sequencing mechanism for digital signage screens.
Output: Fully functional PlaylistsPage and PlaylistEditorPage with all interactions verified.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-playlists-layouts-templates/84-CONTEXT.md

@src/pages/PlaylistsPage.jsx
@src/pages/PlaylistEditorPage.jsx
@src/pages/components/PlaylistEditorComponents.jsx
@src/pages/hooks (usePlaylistEditor hook)
@src/services/playlistService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix playlist CRUD and editor reorder/settings</name>
  <files>
    src/pages/PlaylistsPage.jsx
    src/pages/PlaylistEditorPage.jsx
    src/pages/components/PlaylistEditorComponents.jsx
  </files>
  <action>
Launch the app in the browser and navigate to the Playlists page. Perform a systematic audit:

**Playlists Page (PLAY-01):**
1. Click "Add Playlist" — verify the Create Playlist modal opens with name (required), description, default duration fields
2. Submit with a valid name — verify playlist appears in the list and user is navigated to the editor
3. Submit with empty name — verify validation error appears
4. Try the template creation flow: if the ChoiceModal path is still used, verify both "Blank Playlist" and "Start from Template" options work
5. Delete a playlist: verify the usage check loads, the confirmation modal shows in-use details if applicable, and deletion succeeds
6. Duplicate a playlist: verify the copy appears with " (Copy)" suffix and its items are duplicated
7. Bulk select playlists and verify bulk delete and bulk duplicate work
8. Search: type in the search field and verify playlists filter by name in real-time

**Playlist Editor (PLAY-02):**
1. Navigate to a playlist editor — verify the editor loads with header showing playlist name and save status
2. Add items from the library panel: click an item and verify it appears in the timeline strip
3. Drag an item from the library panel to the timeline strip — verify it inserts at the correct position
4. Reorder items in the timeline via drag-and-drop: drag an item from one position to another, verify the new order persists
5. **CRITICAL (per user decision):** Verify up/down arrow buttons exist on each timeline item for accessibility reorder. If missing, add them:
   - In PlaylistStripItem component (PlaylistEditorComponents.jsx), add small up/down arrow buttons that call a new `onMoveUp`/`onMoveDown` handler
   - In usePlaylistEditor hook, add `handleMoveItemUp(index)` and `handleMoveItemDown(index)` functions that swap items and persist
6. Remove an item: click the remove/X button on a timeline item and verify it disappears
7. Click a timeline item — verify the preview area shows the correct media preview

**Playlist Settings (PLAY-03):**
1. Verify per-item duration is editable: click on the duration display on a timeline item, change it, and verify it saves (auto-save pattern)
2. Verify default_duration from playlist creation is used for items without custom duration
3. Check if transition effect settings exist in the editor or playlist creation modal. If transition_effect is in the playlist schema but not exposed in the editor UI, add a settings dropdown/section in the playlist editor header or a settings panel:
   - Options: none, fade, slide, dissolve (or whatever values the schema supports)
   - Wire it to update the playlist's transition_effect field via the usePlaylistEditor hook

Fix all broken interactions found during the audit. Log any issues that are cosmetic-only for later.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/playlists.spec.js --timeout 30000 2>&1 | tail -20</automated>
    <manual>Navigate to /app, go to Playlists, create a playlist, add items, reorder with drag AND arrow buttons, change duration, delete. All actions complete without console errors.</manual>
  </verify>
  <done>
    - Create playlist modal validates name and creates playlist on submit
    - Delete playlist shows usage confirmation and removes playlist
    - Playlist editor adds/removes items from timeline
    - Items can be reordered via both drag-and-drop AND up/down arrow buttons
    - Per-item duration is editable and auto-saves
    - Transition effect setting is exposed and saves to playlist record
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of playlist flows</name>
  <files>src/pages/PlaylistsPage.jsx</files>
  <action>
Human verifies the complete playlist CRUD and editor functionality including drag-and-drop reordering, arrow button reordering, duration editing, and transition settings.

Steps:
1. Navigate to Playlists page
2. Create a new playlist — verify modal fields and successful creation
3. In the editor, add 3+ items from the media library
4. Drag items to reorder — verify new positions hold
5. Use up/down arrow buttons to reorder — verify they swap items correctly
6. Edit duration on one item — verify the change persists (check save indicator)
7. Change the transition effect setting — verify it saves
8. Go back to Playlists list, delete the test playlist — verify confirmation and removal
9. Check browser console for any JavaScript errors during all actions
  </action>
  <verify>Human approves all playlist flows work correctly</verify>
  <done>All playlist CRUD and editor interactions verified by human</done>
</task>

</tasks>

<verification>
- All playlist CRUD operations (create, rename, delete, duplicate) work without errors
- Playlist editor supports drag-and-drop AND arrow-button reordering (per user decision)
- Per-item duration editing auto-saves
- Transition effect setting is visible and saves
- No JavaScript console errors during any playlist operation
</verification>

<success_criteria>
- PLAY-01: Create, rename, delete playlists all functional
- PLAY-02: Add, reorder (drag + arrows), remove items all functional
- PLAY-03: Duration and transition settings save correctly
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/84-playlists-layouts-templates/84-01-SUMMARY.md`
</output>
