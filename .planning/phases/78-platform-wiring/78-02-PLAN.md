---
phase: 78-platform-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AppsPage.jsx
autonomous: true
requirements:
  - FEAT-07

must_haves:
  truths:
    - "User can click Edit on an installed app from the Recently Used section and see its current configuration pre-populated in the config modal"
    - "User can modify configuration fields and click Save to persist changes"
    - "Saved changes are reflected when the app card is re-rendered (updated name visible)"
  artifacts:
    - path: "src/pages/AppsPage.jsx"
      provides: "Edit flow for installed apps with pre-populated config modals and save-to-updateAppConfig wiring"
      contains: "handleEditApp"
  key_links:
    - from: "src/pages/AppsPage.jsx"
      to: "src/services/mediaService.js"
      via: "handleEditApp populates modal state from app.config_json, save calls updateAppConfig"
      pattern: "updateAppConfig"
---

<objective>
Wire app configuration editing so users can open an installed app's configuration from the apps page, modify settings, and save.

Purpose: Currently the "Edit" button in the Recently Used section on AppsPage shows a toast "Edit coming soon". The `updateAppConfig` service function already exists in mediaService.js. The creation modals (ClockAppModal, WebPageAppModal, WeatherWallConfigModal, RssTickerAppModal, DataTableAppModal, GenericEmbedModal) already render all configuration fields. This plan wires the Edit button to open the correct modal pre-populated with existing config and saves changes via updateAppConfig.

Output: Updated AppsPage.jsx where clicking Edit on a user app opens the appropriate config modal pre-filled with current values, and saving calls updateAppConfig to persist changes.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/AppsPage.jsx
@src/services/mediaService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Edit button to open pre-populated config modal and save via updateAppConfig</name>
  <files>src/pages/AppsPage.jsx</files>
  <action>
    **Import updateAppConfig:**
    Add `updateAppConfig` to the existing import from `'../services/mediaService'`.

    **Add editing state:**
    Add `const [editingApp, setEditingApp] = useState(null);` alongside existing state declarations. This holds the user app being edited (the full media_assets row with config_json).

    **Create handleEditApp function:**
    ```
    const handleEditApp = (app) => {
      setEditingApp(app);
      setOpenMenuId(null);
      // Route to the correct modal based on appType from config_json
      const appType = app.config_json?.appType;
      switch (appType) {
        case APP_TYPE_KEYS.CLOCK:
          setShowClockModal(true);
          break;
        case APP_TYPE_KEYS.WEB_PAGE:
          setShowWebPageModal(true);
          break;
        case APP_TYPE_KEYS.WEATHER:
          setShowWeatherModal(true);
          break;
        case APP_TYPE_KEYS.RSS_TICKER:
          setShowRssTickerModal(true);
          break;
        case APP_TYPE_KEYS.DATA_TABLE:
          setShowDataTableModal(true);
          break;
        default:
          setShowGenericEmbedModal(true);
      }
    };
    ```

    **Create handleSaveApp function:**
    This is a reusable async function that updates an existing app's config:
    ```
    const handleSaveApp = async (config) => {
      if (!editingApp) return;
      setCreating(true);
      try {
        const updated = await updateAppConfig(editingApp.id, config);
        setUserApps(userApps.map(a => a.id === editingApp.id ? { ...a, ...updated, name: config.name || a.name } : a));
        showToast?.('App updated successfully');
      } catch (error) {
        logger.error('Error updating app:', error);
        showToast?.('Error updating app: ' + error.message, 'error');
      } finally {
        setCreating(false);
        setEditingApp(null);
      }
    };
    ```

    **Wire Edit button (line ~300-304):**
    Replace the placeholder toast handler:
    ```
    onClick={() => {
      showToast?.('Edit coming soon');
      setOpenMenuId(null);
    }}
    ```
    With:
    ```
    onClick={() => handleEditApp(app)}
    ```

    **Update each modal's onCreate to handle both create and edit modes:**

    For each modal (ClockAppModal, WebPageAppModal, WeatherWallConfigModal, RssTickerAppModal, DataTableAppModal, GenericEmbedModal), the pattern is:
    1. Pass `editingApp` as a new `editData` prop to identify edit mode
    2. If `editData` is set, call `handleSaveApp(config)` instead of the create flow

    The simplest approach: wrap each modal's onCreate handler in the parent to check `editingApp`:

    For **ClockAppModal** (around line 427-444), change the `onCreate` prop to:
    ```
    onCreate={async (config) => {
      if (editingApp) {
        await handleSaveApp(config);
        setShowClockModal(false);
        return;
      }
      // ... existing create logic stays as-is
    }}
    ```
    Also pass `initialValues={editingApp?.config_json}` prop.

    Apply the same pattern to **WebPageAppModal** (~line 454-471), **WeatherWallConfigModal** (~line 474-508), **RssTickerAppModal** (~line 511-535), **DataTableAppModal** (~line 538-562), and **GenericEmbedModal** (~line 565-589).

    For each modal, also add `onClose` cleanup to clear editingApp:
    In each modal's onClose callback, add `setEditingApp(null);` alongside the existing `setSelectedApp(null);`.

    **Update modal components to accept initialValues prop:**

    For each inline modal component (ClockAppModal, WebPageAppModal, RssTickerAppModal, DataTableAppModal, GenericEmbedModal), update the component signature to accept `initialValues` prop and use it for default state values.

    Example for **ClockAppModal** (line ~598):
    Change signature to: `function ClockAppModal({ app, onClose, onCreate, creating, initialValues })`
    Update defaults:
    - `const [name, setName] = useState(initialValues?.name || app?.name || 'Clock');`
    - `const [format, setFormat] = useState(initialValues?.format || 'HH:mm');`
    - `const [showSeconds, setShowSeconds] = useState(initialValues?.showSeconds || false);`
    - `const [showDate, setShowDate] = useState(initialValues?.showDate !== false);`
    - `const [dateFormat, setDateFormat] = useState(initialValues?.dateFormat || 'MMMM D, YYYY');`
    - `const [timezone, setTimezone] = useState(initialValues?.timezone || 'device');`
    Also update the submit button text: `{creating ? (initialValues ? 'Saving...' : 'Creating...') : (initialValues ? 'Save' : 'Create')}`

    Apply same pattern to:
    - **WebPageAppModal**: initialValues for name, url, refreshSeconds
    - **RssTickerAppModal**: initialValues for name, feedUrl, maxItems, scrollSpeed
    - **DataTableAppModal**: initialValues for name, dataUrl, format, theme
    - **GenericEmbedModal**: initialValues for name, url, refreshSeconds

    For **WeatherWallConfigModal** (external component in `src/components/apps/WeatherWallConfigModal.jsx`): pass `initialValues={editingApp?.config_json}` prop. Check if WeatherWallConfigModal already accepts an `initialValues` or `defaultValues` prop. If not, it already receives `app` prop — we can pass the config data through a different mechanism. The simplest approach: pass a synthetic `app` object that includes the config_json fields when editing. When calling WeatherWallConfigModal in edit mode, pass `app={editingApp ? { ...selectedApp, ...editingApp.config_json } : selectedApp}`. This way the existing field defaults from `app` will pick up the saved config values.
  </action>
  <verify>
    Run `npx eslint src/pages/AppsPage.jsx` — no errors.
    Grep for `handleEditApp` in AppsPage.jsx — function exists.
    Grep for `handleSaveApp` in AppsPage.jsx — function exists and calls updateAppConfig.
    Grep for `editingApp` in AppsPage.jsx — state variable exists and is used in modal handlers.
    Grep for `'Edit coming soon'` in AppsPage.jsx — should NOT be found (removed).
    Grep for `initialValues` in AppsPage.jsx — passed to modal components.
  </verify>
  <done>
    Clicking Edit on an installed app in the Recently Used section opens the appropriate config modal pre-populated with the app's current config_json values. User can modify fields and click Save. Save calls updateAppConfig which persists the merged config to Supabase. The userApps list updates optimistically to reflect the new name/config. "Edit coming soon" toast is completely removed.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/pages/AppsPage.jsx` passes with no errors
2. No occurrence of "Edit coming soon" toast in AppsPage.jsx
3. `handleEditApp` function routes to correct modal based on app.config_json.appType
4. `handleSaveApp` function calls `updateAppConfig` from mediaService
5. Each modal component accepts `initialValues` prop for pre-populating fields in edit mode
6. Modal submit button shows "Save" instead of "Create" in edit mode
7. `editingApp` state is cleared on modal close
</verification>

<success_criteria>
- User can click Edit on any installed app from the apps page
- Config modal opens with current configuration pre-populated
- User can modify any field and click Save
- Changes are persisted via updateAppConfig service call
- Updated app name/config reflected in the UI after save
- Success/error toasts shown appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/78-platform-wiring/78-02-SUMMARY.md`
</output>
