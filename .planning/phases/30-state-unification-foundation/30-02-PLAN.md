---
phase: 30-state-unification-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-state-unification-foundation/30-LOCALSTORAGE-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "Audit document lists all localStorage keys used in onboarding flows"
    - "Audit document identifies consumer files for each key"
    - "Audit document provides removal recommendation for Phase 34"
  artifacts:
    - path: ".planning/phases/30-state-unification-foundation/30-LOCALSTORAGE-AUDIT.md"
      provides: "Complete localStorage key inventory for Phase 34 cleanup"
      min_lines: 30
  key_links: []
---

<objective>
Audit all localStorage and sessionStorage keys used in onboarding-related flows to document what needs removal in Phase 34.

Purpose: Create inventory of client-side storage that duplicates database state, enabling clean removal after unified flow is validated.

Output: Audit document listing all keys, their consumers, and removal recommendations.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Known localStorage keys (from research)
# - bizscreen_welcome_modal_shown (localStorage) - DashboardPage.jsx
# - onboarding_banner_dismissed (sessionStorage) - OnboardingBanner.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search for all localStorage and sessionStorage usage</name>
  <files>N/A (search only)</files>
  <action>
Search the src/ directory for all localStorage and sessionStorage usage patterns:
- `localStorage.getItem`
- `localStorage.setItem`
- `localStorage.removeItem`
- `sessionStorage.getItem`
- `sessionStorage.setItem`
- `sessionStorage.removeItem`

For each key found, identify:
1. Key name (the string constant or variable value)
2. File(s) where it's used
3. Purpose (what state it tracks)
4. Whether it's onboarding-related
  </action>
  <verify>
Run: `grep -r "localStorage\|sessionStorage" src/ --include="*.js" --include="*.jsx" | head -50`
  </verify>
  <done>
List of all storage keys and their locations collected for analysis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create localStorage audit document</name>
  <files>.planning/phases/30-state-unification-foundation/30-LOCALSTORAGE-AUDIT.md</files>
  <action>
Create audit document with the following structure:

```markdown
# localStorage/sessionStorage Audit for Onboarding

**Purpose:** Document all client-side storage keys related to onboarding for Phase 34 cleanup.

## Onboarding-Related Keys

### localStorage Keys

| Key | File(s) | Purpose | Database Equivalent | Removal Status |
|-----|---------|---------|---------------------|----------------|
| `bizscreen_welcome_modal_shown` | src/pages/DashboardPage.jsx | Tracks if WelcomeModal has been shown | `onboarding_progress.starter_pack_applied` or `current_unified_step` | REMOVE in Phase 34 |
| ... | ... | ... | ... | ... |

### sessionStorage Keys

| Key | File(s) | Purpose | Database Equivalent | Removal Status |
|-----|---------|---------|---------------------|----------------|
| `onboarding_banner_dismissed` | src/components/onboarding/OnboardingBanner.jsx | Tracks banner dismissal per session | N/A (session-only intentionally) | KEEP (per-session is correct) |
| ... | ... | ... | ... | ... |

## Non-Onboarding Keys (Reference)

List any other localStorage keys found for context, but not targeted for removal.

## Removal Recommendations

### Phase 34 Actions

1. **Remove `bizscreen_welcome_modal_shown`:**
   - Delete constant and all usages in DashboardPage.jsx
   - Replace with check from `getUnifiedOnboardingState()`
   - ...

### Keys to Keep

- `onboarding_banner_dismissed` - Session-only is intentional behavior
- ...

## Migration Path

For users with existing localStorage state:
1. On first load after v2.2 deployment...
2. ...
```

Include all keys found in Task 1 and categorize appropriately.
  </action>
  <verify>
File exists and contains at least:
- Table of onboarding-related localStorage keys
- Table of sessionStorage keys
- Removal recommendations for Phase 34
  </verify>
  <done>
Audit document exists with complete inventory of storage keys and removal plan.
  </done>
</task>

</tasks>

<verification>
1. File exists: `ls .planning/phases/30-state-unification-foundation/30-LOCALSTORAGE-AUDIT.md`
2. Contains tables: `grep -c "|" .planning/phases/30-state-unification-foundation/30-LOCALSTORAGE-AUDIT.md` should be > 5
3. Contains key names: File mentions `bizscreen_welcome_modal_shown` and `onboarding_banner_dismissed`
4. Contains removal recommendations section
</verification>

<success_criteria>
- Audit document exists with structured format
- All onboarding-related localStorage keys documented with file locations
- All onboarding-related sessionStorage keys documented with file locations
- Clear removal recommendations for Phase 34 cleanup
- Migration path documented for existing user state
</success_criteria>

<output>
After completion, create `.planning/phases/30-state-unification-foundation/30-02-SUMMARY.md`
</output>
