---
phase: 083-scene-editor-ai-designer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/svg-editor/FabricSvgEditor.jsx
  - src/components/svg-editor/EditorToolbar.jsx
  - src/components/svg-editor/LayersPanel.jsx
  - src/components/svg-editor/ContextMenu.jsx
  - src/components/svg-editor/PropertiesPanel.jsx
autonomous: false
requirements:
  - SCEN-02
  - SCEN-03

must_haves:
  truths:
    - "User can add text, shapes (rect/circle/triangle), and images to the canvas from the toolbar"
    - "The Layers panel toggle button shows/hides the layers list and clicking a layer selects that object"
    - "Position, style, effects, and hyperlink panels render without errors when an object is selected"
    - "Crop mode starts and cancels cleanly from the TopToolbar for image objects"
    - "Context menu appears on right-click and all its actions execute without JS errors"
  artifacts:
    - path: "src/components/svg-editor/FabricSvgEditor.jsx"
      provides: "Full SVG editor with all tools and panels wired"
    - path: "src/components/svg-editor/LayersPanel.jsx"
      provides: "Layers panel showing canvas objects with select/reorder"
    - path: "src/components/svg-editor/ContextMenu.jsx"
      provides: "Right-click context menu with copy/paste/delete/layer actions"
  key_links:
    - from: "src/components/svg-editor/FabricSvgEditor.jsx"
      to: "src/components/svg-editor/LayersPanel.jsx"
      via: "showLayersPanel state and canvasObjects prop"
      pattern: "showLayersPanel|canvasObjects"
    - from: "src/components/svg-editor/FabricSvgEditor.jsx"
      to: "src/components/svg-editor/ContextMenu.jsx"
      via: "contextMenu state and mouse:down event"
      pattern: "contextMenu|mouse:down"
---

<objective>
Audit and fix all SVG editor tools (SCEN-02) and property panels (SCEN-03) so every interactive element works correctly.

Purpose: The SVG editor (FabricSvgEditor) was built in phases 73-74 and polished in phase 80. This phase does a final verification audit — read the actual code to find disconnected callbacks, missing imports, or broken handlers, and fix them.

Output: All editor tools add elements to canvas; LayersPanel, ContextMenu, EffectsPanel, PositionPanel, HyperlinkModal, AnimatePanel, and crop/replace flows work without JS errors. Human checkpoint confirms visual correctness.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-svg-editor-integration-polish/80-01-SUMMARY.md
@src/components/svg-editor/FabricSvgEditor.jsx
@src/components/svg-editor/EditorToolbar.jsx
@src/components/svg-editor/LayersPanel.jsx
@src/components/svg-editor/ContextMenu.jsx
@src/components/svg-editor/PropertiesPanel.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit SVG editor tool handlers and fix any disconnected callbacks</name>
  <files>src/components/svg-editor/FabricSvgEditor.jsx
src/components/svg-editor/EditorToolbar.jsx
src/components/svg-editor/LayersPanel.jsx
src/components/svg-editor/ContextMenu.jsx</files>
  <action>
**Step 1: Read all four files fully before making any changes.**

**Step 2: Audit and fix FabricSvgEditor.jsx tool wiring:**

Check that EditorToolbar receives all required callback props:
- `onAddText` → calls `handleAddText` (adds an i-text object to fabricCanvasRef)
- `onAddShape` → calls `handleAddShape` (adds rect/circle/triangle based on shape arg)
- `onAddImage` → wired to `fileInputRef.current.click()` (triggers file input)
- `onUndo` / `onRedo` → calls `handleUndo` / `handleRedo`
- `canUndo` / `canRedo` → derived from `historyIndex`

If any of these are not passed or the handlers are missing/broken, add the missing pieces.

**Step 3: Audit LayersPanel wiring in FabricSvgEditor.jsx:**

Verify `showLayersPanel` state is toggled by the Layers button in FabricSvgEditor's toolbar (likely in the right-side header controls). Confirm `canvasObjects` is passed as a prop. Confirm that clicking a layer item in LayersPanel calls `fabricCanvasRef.current.setActiveObject(obj)` and `fabricCanvasRef.current.renderAll()`.

Read LayersPanel.jsx to confirm the `onSelectObject` prop is called correctly. If the `onSelectObject` callback is missing or the object reference passed to it doesn't match what FabricSvgEditor expects, fix the wiring.

**Step 4: Audit ContextMenu.jsx:**

Read ContextMenu.jsx. Verify all menu item onClick handlers (copy, paste, delete, bring to front, send to back, etc.) are defined and passed as props from FabricSvgEditor. If any action prop is `undefined` (passed but not provided in FabricSvgEditor's usage of ContextMenu), add the missing handler.

**Step 5: Fix any issues found.** Common patterns to check:
- Props declared in component signature but never passed by parent → add to parent's JSX
- Handler references that are `undefined` (e.g., `onPaste` if clipboard is empty) → guard with `if (clipboard) { ... }`
- Missing `syncCanvasObjects()` call after layer reorder actions in LayersPanel

After all fixes, run ESLint on changed files.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx eslint src/components/svg-editor/FabricSvgEditor.jsx src/components/svg-editor/EditorToolbar.jsx src/components/svg-editor/LayersPanel.jsx src/components/svg-editor/ContextMenu.jsx --max-warnings=0 2>&1 | tail -10</automated>
    <manual>Navigate to SVG editor (/svg-editor), add a text element, add a shape, open layers panel — confirm no console errors.</manual>
  </verify>
  <done>
    - All EditorToolbar callback props are wired to FabricSvgEditor handlers
    - LayersPanel toggle works and clicking a layer selects the object on canvas
    - ContextMenu right-click menu items all have connected handlers
    - ESLint passes with no errors on all four files
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit property panels (PositionPanel, EffectsPanel, AnimatePanel, HyperlinkModal) for disconnected props</name>
  <files>src/components/svg-editor/FabricSvgEditor.jsx
src/components/svg-editor/PropertiesPanel.jsx</files>
  <action>
**Step 1: Read FabricSvgEditor.jsx focusing on how the side panels are rendered.**

Look for the `activePanel` state and how PositionPanel, EffectsPanel, AnimatePanel are conditionally rendered. Check that:

1. **PositionPanel** receives: `selectedObject`, `onUpdate` (updates position/size on fabric canvas), `onClose`
2. **EffectsPanel** receives: `selectedObject`, `onUpdate`, `onClose`
3. **AnimatePanel** receives: `selectedObject`, `onUpdate`, `onClose`
4. **HyperlinkModal** receives: `open` (showHyperlinkModal state), `onClose`, `onSave` (handleSaveHyperlink callback), `initialUrl`, `initialOpenInNewTab`

Phase 80 fixed `HyperlinkModal` openInNewTab wiring. Confirm those fixes are still intact (openInNewTab passed to onSave callback and handleSaveHyperlink sets hyperlinkTarget on the fabric object).

**Step 2: Check image replace flow.**

In FabricSvgEditor, the `replaceImageRef` flag and file input (`fileInputRef`) handle replace image. Confirm the TopToolbar's `onReplaceImage` prop triggers `replaceImageRef.current = true` then calls `fileInputRef.current.click()`. If this flow is broken or missing, fix it.

**Step 3: Check PropertiesPanel.jsx.**

Read PropertiesPanel.jsx. This is the simpler property panel used in SvgEditorPage (not SceneEditorPage). Confirm it renders correctly when `selectedObject` is an image vs text. If any props are missing from its parent usage in SvgEditorPage, fix them.

**Step 4: Fix any issues found in FabricSvgEditor.jsx.**

Only modify what is actually broken — do not rewrite sections that work correctly.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx eslint src/components/svg-editor/FabricSvgEditor.jsx src/components/svg-editor/PropertiesPanel.jsx --max-warnings=0 2>&1 | tail -10</automated>
    <manual>In SVG editor: select a text element, click Effects button — verify EffectsPanel opens. Click Animate button — verify AnimatePanel opens. Click the hyperlink icon — verify HyperlinkModal opens. Select an image, click Replace — verify file picker opens.</manual>
  </verify>
  <done>
    - PositionPanel, EffectsPanel, AnimatePanel open correctly from the TopToolbar
    - HyperlinkModal opens for selected objects and saves hyperlink + openInNewTab correctly
    - Image replace flow triggers file picker with replaceImageRef flag set
    - ESLint passes on modified files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify all SVG editor tools and property panels</name>
  <files>none</files>
  <action>Human verification checkpoint — no code changes.</action>
  <what-built>
    SVG editor full tool and panel audit: EditorToolbar add-text/add-shape/add-image wired; LayersPanel toggle and object selection; ContextMenu right-click menu; EffectsPanel/AnimatePanel/PositionPanel panels; HyperlinkModal; image crop and replace.
  </what-built>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Navigate to SVG Editor (from Templates page or direct route /svg-editor)
    3. Toolbar tools: click "Add Text" → text appears on canvas. Click "Rectangle" → rectangle appears. Click "Circle" → circle appears. Click "Add Image" → file picker opens.
    4. Select a text element → TopToolbar shows font/size/color controls. Change font size → canvas updates.
    5. Click the Layers icon → LayersPanel slides open showing canvas objects. Click a layer item → that object is selected on canvas.
    6. Right-click on a canvas object → context menu appears. Click "Duplicate" → copy appears. Click "Delete" → object removed.
    7. Select a text element → click Effects button → EffectsPanel opens with shadow/opacity controls.
    8. Select a text element → click Position button → PositionPanel opens with X/Y/W/H inputs.
    9. Select an object → click Link icon → HyperlinkModal opens → enter a URL → save → object has hyperlink set.
    10. Select an image → click Crop button → crop handles appear → click Cancel Crop → returns to normal.
    11. Check browser console for any JavaScript errors throughout.
  </how-to-verify>
  <verify>
    <automated>MISSING — human checkpoint, no automated verification</automated>
  </verify>
  <done>Human approves all SVG editor tools and property panels work without JS errors.</done>
  <resume-signal>Type "approved" if all tools work, or describe which specific tools/panels have issues</resume-signal>
</task>

</tasks>

<verification>
- Run: cd /Users/massimodamico/bizscreen && npx eslint src/components/svg-editor/ --max-warnings=0
- Human checkpoint confirms all tools and panels work interactively
</verification>

<success_criteria>
- All EditorToolbar buttons (text, shapes, image) add elements to the Fabric.js canvas
- LayersPanel toggle works; clicking a layer selects the canvas object
- ContextMenu right-click appears with working actions (duplicate, delete, layer order)
- EffectsPanel, AnimatePanel, PositionPanel open from TopToolbar with no JS errors
- HyperlinkModal opens, accepts URL input, and saves hyperlink data to the fabric object
- Image crop starts/cancels cleanly; image replace triggers file input
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/083-scene-editor-ai-designer/083-02-SUMMARY.md`
</output>
