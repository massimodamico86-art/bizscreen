---
phase: 86-screen-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PairDevicePage.jsx
  - src/pages/ScreensPage.jsx
  - src/pages/hooks/useScreensData.js
  - src/pages/components/ScreensComponents.jsx
  - src/components/ScreenDetailDrawer.jsx
autonomous: true
requirements:
  - SCRN-01
  - SCRN-02
  - SCRN-05

must_haves:
  truths:
    - "Screens list loads with correct online/offline status badges, search filters results, and bulk schedule assignment executes"
    - "Screen pairing flow (OTP code creation via AddScreenModal) completes without errors and the new screen appears in the list"
    - "PairDevicePage (QR-based pairing) renders without import errors and can pair a device to a screen"
    - "Remote commands (reboot, reload, clear cache, reset, kiosk mode) trigger from the action menu and produce visible toast feedback"
    - "Screen detail drawer shows device health metrics, screenshot section, and content source chain without errors"
  artifacts:
    - path: "src/pages/PairDevicePage.jsx"
      provides: "QR-based device pairing page with correct design-system imports"
      contains: "CardHeader"
    - path: "src/pages/ScreensPage.jsx"
      provides: "Screen listing with search, filter, bulk actions"
      min_lines: 100
    - path: "src/pages/components/ScreensComponents.jsx"
      provides: "ScreenRow, AddScreenModal, ScreenActionMenu, AnalyticsModal, EditScreenModal, KioskModeModal"
      min_lines: 200
    - path: "src/components/ScreenDetailDrawer.jsx"
      provides: "Device health metrics, screenshot section, content source diagnostics"
      min_lines: 100
  key_links:
    - from: "src/pages/ScreensPage.jsx"
      to: "src/pages/hooks/useScreensData.js"
      via: "useScreensData hook"
      pattern: "useScreensData"
    - from: "src/pages/hooks/useScreensData.js"
      to: "src/services/screenService.js"
      via: "service function calls"
      pattern: "rebootDevice|reloadDeviceContent|clearDeviceCache|resetDevice"
    - from: "src/components/ScreenDetailDrawer.jsx"
      to: "src/services/screenDiagnosticsService.js"
      via: "getScreenDiagnostics call"
      pattern: "getScreenDiagnostics"
---

<objective>
Audit and fix the screens list page, pairing flow, remote commands, and detail drawer so they render without errors and all interactive elements work end-to-end.

Purpose: The ScreensPage is the primary screen management hub. Pairing (OTP + QR), device commands, and the detail drawer must all work reliably.
Output: All screen list, pairing, command, and detail flows functional without runtime errors.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/ScreensPage.jsx
@src/pages/hooks/useScreensData.js
@src/pages/components/ScreensComponents.jsx
@src/components/ScreenDetailDrawer.jsx
@src/pages/PairDevicePage.jsx
@src/services/screenService.js
@src/services/screenDiagnosticsService.js
@src/services/deviceScreenshotService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PairDevicePage missing imports and audit ScreensPage imports</name>
  <files>
    src/pages/PairDevicePage.jsx
    src/pages/ScreensPage.jsx
    src/pages/hooks/useScreensData.js
    src/pages/components/ScreensComponents.jsx
    src/components/ScreenDetailDrawer.jsx
  </files>
  <action>
    **PairDevicePage.jsx — CRITICAL missing import fix:**
    The file uses `CardHeader`, `CardTitle`, and `CardDescription` JSX elements (lines 265-270, 349-354, 402-410) but does NOT import them. The design-system Card.jsx exports these components. Add them to the existing design-system import:
    - Current import: `Card, CardContent, Button, Stack, Alert, EmptyState, Input`
    - Change to: `Card, CardContent, CardHeader, CardTitle, CardDescription, Button, Stack, Alert, EmptyState, Input`

    Verify `CardDescription` is actually exported from the design-system. If not (some design systems don't have it), replace `CardDescription` usages with a `<p className="text-sm text-gray-500">` wrapper matching the pattern used elsewhere in the codebase.

    **ScreensPage.jsx — import verification:**
    - Verify all lucide-react icons are actually used (Search, Plus, RefreshCw, Calendar, Loader2, MapPin, Users, Key). Check no unused imports.
    - Verify design-system imports (PageLayout, PageHeader, PageContent, Stack, Inline, Button, Card) are complete.
    - Verify `isScreenOnline` and `setMasterKioskPin`, `getMasterPinStatus` from screenService are valid exports.

    **ScreensComponents.jsx — import verification:**
    - Verify all ~25 lucide-react icons are actually used in the file. Remove any unused imports.
    - Verify `SUPPORTED_LOCALES` import from i18nConfig exists.
    - Verify `formatLimitDisplay` from limitsService exists.
    - Verify `formatDuration`, `getUptimeColor`, `DATE_RANGES` from analyticsService exist.
    - Verify `YodeckEmptyState` component exists at the import path.
    - Verify `PlayerStatusBadge` and `getPlayerStatus` exist at the import path.
    - Verify `OrientationMismatchWarning` exists at the import path.
    - Verify all design-system imports (Stack, Grid, Inline, Button, Card/CardHeader/CardTitle/CardContent, Badge, FormField/Input/Select/Switch, Modal/ModalHeader/ModalTitle/ModalDescription/ModalContent/ModalFooter, Alert/Banner) are valid.

    **useScreensData.js — import verification:**
    - Verify all service imports exist: `fetchScreens`, `createScreen`, `updateScreen`, `deleteScreen`, `assignPlaylistToScreen`, `assignLayoutToScreen`, `assignScheduleToScreen`, `isScreenOnline`, `rebootDevice`, `reloadDeviceContent`, `clearDeviceCache`, `resetDevice`, `setDeviceKioskMode` from screenService.
    - Verify `fetchPlaylists` from playlistService, `fetchLayouts` from layoutService, `fetchSchedules` and `bulkAssignScheduleToDevices` from scheduleService, `getEffectiveLimits` from limitsService, `fetchLocations` and `assignScreenToLocation` from locationService, `getScreenAnalytics` from analyticsService, `fetchScreenGroups` from screenGroupService all exist as exports.

    **ScreenDetailDrawer.jsx — import verification:**
    - Verify `Badge` and `Button` from design-system are imported.
    - Verify all diagnostics service imports: `getScreenDiagnostics`, `getResolutionPathInfo`, `getOnlineStatusInfo`, `formatLastSeen`, `formatUptime`, `getUptimeColor`, `getPreviewInfo`, `getMetricStatus`, `formatMetricValue`, `getJsHeapPercent`, `forceDeviceSync`.
    - Verify `requestDeviceScreenshot` from deviceScreenshotService.
    - Verify `Button` variant="outline" is valid in the design-system (or should be variant="secondary"/"ghost"). Check `src/design-system/components/Button.jsx` for valid variants and fix if needed.

    For each file, run `npx vite build --mode development 2>&1 | head -50` (or use `grep` on the design-system index to verify exports) to catch any remaining import errors.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx vite build 2>&1 | tail -20</automated>
    <manual>Check that no "is not defined" or "is not exported" errors appear for screen management files</manual>
  </verify>
  <done>PairDevicePage imports CardHeader/CardTitle/CardDescription correctly. All screen management files have verified, correct imports with no missing symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix screen list, pairing, commands, and detail drawer wiring</name>
  <files>
    src/pages/ScreensPage.jsx
    src/pages/hooks/useScreensData.js
    src/pages/components/ScreensComponents.jsx
    src/components/ScreenDetailDrawer.jsx
    src/pages/PairDevicePage.jsx
  </files>
  <action>
    **Screen list (SCRN-01) verification:**
    - Verify `filteredScreens` correctly applies search + locationFilter + groupFilter (it does based on code review, but confirm all three filters combine properly)
    - Verify `onlineCount`/`offlineCount` display in PageHeader description
    - Verify bulk selection: `toggleSelectAll`, `toggleScreenSelection`, `clearSelection`, `handleBulkAssignSchedule` are wired correctly
    - Verify `ScreenRow` renders PlayerStatusBadge, device name, content display, and action menu

    **Pairing (SCRN-02) verification:**
    - In `AddScreenModal`: verify the OTP code display and copy-to-clipboard work after screen creation
    - In `PairDevicePage`: after fixing imports (Task 1), verify the existing/unpaired screen selection flow and create-and-pair flow are correctly wired
    - Verify `handleCopyOTP` in `useScreensData` copies to clipboard and shows toast
    - Verify `DemoPairingBanner` renders for demo screens with OTP and copy button

    **Remote commands (SCRN-05) verification:**
    - Verify `ScreenActionMenu` passes correct command strings ('reload', 'reboot', 'clear_cache', 'reset') to `onDeviceCommand`
    - Verify `handleDeviceCommand` in `useScreensData` dispatches to correct service function for each command type
    - Verify `commandingDevice` state provides loading feedback (spinner icons in action menu)
    - Verify kiosk mode toggle works via `KioskModeModal` -> `handleSetKioskMode`

    **Detail drawer (partial SCRN-04) verification:**
    - Verify `ScreenDetailDrawer` opens when `detailScreen` is set (from action menu "View Details")
    - Verify diagnostics load via `getScreenDiagnostics` and display MetricCard components
    - Verify screenshot request button calls `requestDeviceScreenshot` and shows loading state
    - Verify "Capture Now" button and screenshot display section work
    - Verify the "Force Sync" button on content mismatch warning works

    **Fix any wiring issues found.** Common patterns from prior phases:
    - Missing guard clauses on optional data (e.g., `data?.field`)
    - Incorrect prop names passed between parent and child components
    - Missing error handling in async operations
    - State not being reset when modals close

    If any functional gaps are found (e.g., a handler calls a service function that doesn't exist, or a prop is expected but not passed), fix them inline.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx vite build 2>&1 | tail -20</automated>
    <manual>Launch dev server, navigate to Screens page. Verify: list loads, search filters, bulk select checkbox works, action menu opens with all commands, detail drawer opens from action menu</manual>
  </verify>
  <done>Screen list loads with status badges, search/filter work, pairing OTP flow is functional, all 5 remote commands dispatch correctly with feedback, detail drawer shows diagnostics and screenshot section.</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors for all screen management files
2. ScreensPage renders screen list with PlayerStatusBadge for each screen
3. Search, location filter, and group filter narrow the visible screen list
4. Bulk selection selects/deselects screens and bulk schedule assignment dropdown is functional
5. AddScreenModal creates a screen and displays OTP pairing code
6. PairDevicePage renders without import errors (CardHeader, CardTitle, CardDescription)
7. ScreenActionMenu shows all commands (Reload, Reboot, Clear Cache, Kiosk Mode, Reset, Delete)
8. Each command shows loading spinner while executing and toast on completion
9. ScreenDetailDrawer opens and displays device health metrics, screenshot section, content source chain
10. Screenshot "Capture Now" button triggers request and shows loading state
</verification>

<success_criteria>
- All screen management pages build without errors
- Screen listing shows correct status, search filters results
- OTP pairing flow and QR pairing page both render correctly
- All 5 device commands execute from action menu with feedback
- Detail drawer displays diagnostics and screenshot sections
</success_criteria>

<output>
After completion, create `.planning/phases/86-screen-management/86-01-SUMMARY.md`
</output>
