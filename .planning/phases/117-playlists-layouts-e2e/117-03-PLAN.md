---
phase: 117-playlists-layouts-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/playlists-screenshots.spec.js
autonomous: true
gap_closure: true
requirements:
  - PLAY-01
  - PLAY-03
  - PLAY-04
  - PLAY-05
  - PLAY-06
  - PLAY-07
  - PLAY-08

must_haves:
  truths:
    - "Running PLAY-01 produces a screenshot showing the playlists page (list with items OR empty state with create button -- NOT the error state alone)"
    - "Running PLAY-03 produces a screenshot of the playlist editor page with the library panel visible"
    - "Running PLAY-04 produces a screenshot showing the timeline strip area"
    - "Running PLAY-05 produces a screenshot of the settings dropdown with Transition Effect options"
    - "Running PLAY-06 produces a screenshot of the library panel with the Playlists filter tab"
    - "Running PLAY-07 produces a screenshot of the Background Audio section in settings"
    - "Running PLAY-08 produces a screenshot of the player preview area"
  artifacts:
    - path: "tests/e2e/playlists-screenshots.spec.js"
      provides: "Fixed playlist E2E tests that reach the editor"
      min_lines: 400
  key_links:
    - from: "tests/e2e/playlists-screenshots.spec.js"
      to: "src/pages/PlaylistEditorPage.jsx"
      via: "navigates using __setCurrentPage('playlist-editor-{id}')"
      pattern: "__setCurrentPage.*playlist-editor-"
---

<objective>
Fix playlists E2E screenshot tests so PLAY-03 through PLAY-08 actually reach the playlist editor instead of producing identical error-page screenshots.

Purpose: The current playlists spec never reaches the playlist editor because the Supabase query fails and shows "Unable to load playlists." Tests need to create a playlist via the UI modal (which works -- PLAY-02 proved this) and then navigate to the editor using `__setCurrentPage('playlist-editor-{id}')`.

Output: Updated playlists-screenshots.spec.js that produces real screenshots of the playlist editor UI.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-playlists-layouts-e2e/117-01-SUMMARY.md
@.planning/phases/117-playlists-layouts-e2e/117-VERIFICATION.md

<interfaces>
<!-- Key navigation patterns the executor needs -->

From src/App.jsx:
- Playlist editor route: `currentPage.startsWith('playlist-editor-')` renders PlaylistEditorPage with `playlistId={currentPage.replace('playlist-editor-', '')}`
- Navigate via: `window.__setCurrentPage('playlist-editor-{uuid}')`

From src/pages/PlaylistsPage.jsx:
- Create playlist API: supabase.from('playlists').insert({...})
- After creation, navigates to: `onNavigate('playlist-editor-{data.id}')`
- Error state shows: "Unable to load playlists" with AlertCircle icon
- Empty state shows: "You don't have any Playlists."
- Table row click navigates to: `onNavigate('playlist-editor-{playlist.id}')`

From src/pages/PlaylistEditorPage.jsx:
- Props: `playlistId`, `showToast`, `onNavigate`
- Loading state: spinner
- Not found state: "Playlist not found" + Back to Playlists button
- Editor has: library panel (280px left), TV preview (center), timeline strip (bottom)
- Settings dropdown: Transition Effect select + Background Audio section
- Filter tabs: All, Images, Videos, Audio, Docs, Web, Apps, My Designs, Playlists
- Settings button has title="Playlist settings"
- "All changes saved" status badge in header
- "Select an item to preview" placeholder in TV preview
- Timeline strip shows item count and total duration

From tests/e2e/helpers/index.js:
- screenshotStep(page, area, stepName) - saves to screenshots/{area}/
- loginAndPrepare(page) - authenticates and navigates to /app
- waitForPageReady(page) - waits for network idle
- dismissAnyModals(page) - closes any open dialogs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite navigateToPlaylistEditor to create a playlist and use __setCurrentPage</name>
  <files>tests/e2e/playlists-screenshots.spec.js</files>
  <action>
Rewrite the `navigateToPlaylistEditor` function and update PLAY-01 and PLAY-03 through PLAY-08 tests:

**Strategy:** The Supabase query may fail during E2E tests, showing the error state. Instead of trying to click on existing playlists (which may not exist), the tests should:

1. **Update `navigateToPlaylistEditor`** to:
   a. First try the happy path: navigate to playlists page, look for existing playlist rows, click one to navigate
   b. If no rows exist (empty state), create a playlist via the create modal (this works per PLAY-02 evidence)
   c. After creating, extract the playlist ID from the URL/page state. Use `page.evaluate(() => window.__currentPage || '')` to get the current page value. If it starts with 'playlist-editor-', extract the ID.
   d. If creation did not auto-navigate to editor, try: `page.evaluate((id) => window.__setCurrentPage('playlist-editor-' + id), playlistId)`
   e. If error state shows ("Unable to load playlists"), screenshot it as evidence, then try to create a playlist directly by clicking the create button (which appears even in error state per the component code -- look for "Try Again" button first, click it, wait, then try the create flow)
   f. Wait for editor indicators: "All changes saved" text, "Search library..." input, timeline strip, or "Select an item to preview"

2. **Update PLAY-01** to handle error state vs empty state vs list state as three distinct paths, screenshotting each. If error state, click "Try Again" once and re-screenshot.

3. **For PLAY-03 through PLAY-08:** After `navigateToPlaylistEditor` succeeds:
   - PLAY-03: Screenshot the editor showing the library panel with media grid. The library panel is the 280px left panel with search input and filter tabs.
   - PLAY-04: Screenshot the timeline strip at the bottom. Even if empty, capture the drop zone (dashed border div with Plus icon).
   - PLAY-05: Click the Settings button (title="Playlist settings"), wait for the dropdown to appear (contains "Transition Effect" heading), screenshot it.
   - PLAY-06: In the library panel, click the "Playlists" filter tab button (one of the rounded-full filter buttons), screenshot.
   - PLAY-07: Click Settings button, look for "Background Audio" heading in the dropdown, screenshot.
   - PLAY-08: Screenshot the TV preview area (the aspect-video div with dark background). If items exist, click one to show preview; if empty, capture "Select an item to preview" placeholder.

**Key fix for the playlist creation flow:**
The existing PLAY-02 test proves the create modal works. After filling the name and clicking Create Playlist via dispatchEvent, the app navigates to `playlist-editor-{id}`. To capture this:
```javascript
// After create modal submit, wait for page change
await page.waitForTimeout(3000);
const currentPage = await page.evaluate(() => {
  // The app stores currentPage in React state accessible via the exposed setter
  // We can read it from the DOM or URL patterns
  return document.querySelector('[data-current-page]')?.dataset?.currentPage || '';
});
```

Alternative approach if the above doesn't work: After creating a playlist, the page should auto-navigate. Check for editor indicators directly. If not navigated, use the __setCurrentPage with a generated UUID:
```javascript
// Create a fake/test playlist ID to reach the editor page
// The editor will show "Playlist not found" but the UI chrome (library panel, timeline) should still render
await page.evaluate(() => window.__setCurrentPage('playlist-editor-test-e2e'));
```

**IMPORTANT:** Check the PlaylistEditorPage source -- it shows "Playlist not found" when !playlist (after loading completes). So navigating with a fake ID will NOT show the editor chrome. The test MUST either:
1. Create a real playlist via the create modal and let the app auto-navigate
2. OR find an existing playlist ID from the list page

The safest approach: In `navigateToPlaylistEditor`, always try to create a playlist via the modal. The modal submit triggers `onNavigate('playlist-editor-{data.id}')` internally. After submitting, wait for editor indicators.

**Do NOT use try/catch fallbacks that silently screenshot error pages.** If the editor cannot be reached, the test should screenshot whatever is visible and use descriptive names like "editor-not-accessible" to make the failure obvious.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/playlists-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>
All 8 PLAY tests pass. PLAY-03 through PLAY-08 produce screenshots of actual editor UI (library panel, timeline, settings dropdown, filter tabs, audio section, preview area) rather than identical error-page screenshots. Screenshots in screenshots/117/ show distinct content for each test.
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/e2e/playlists-screenshots.spec.js --project=chromium` -- all tests pass
2. Screenshots in screenshots/117/ for steps 03-08 show distinct content (not identical error pages)
3. At minimum, screenshots 03-08 should differ from the error-page md5 (bce90886)
</verification>

<success_criteria>
- PLAY-03 through PLAY-08 screenshots show real playlist editor content (library, timeline, settings, filters, audio, preview)
- No more than 2 screenshots share the same md5 hash
- All 8 tests pass without crashes
</success_criteria>

<output>
After completion, create `.planning/phases/117-playlists-layouts-e2e/117-03-SUMMARY.md`
</output>
