---
phase: 117-playlists-layouts-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/playlists-screenshots.spec.js
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
  - PLAY-05
  - PLAY-06
  - PLAY-07
  - PLAY-08

must_haves:
  truths:
    - "Running the playlists E2E spec produces screenshots of playlist list page"
    - "Running the playlists E2E spec produces screenshots of playlist creation modal"
    - "Running the playlists E2E spec produces screenshots of playlist editor with items"
    - "Running the playlists E2E spec produces screenshots of drag reorder indicators"
    - "Running the playlists E2E spec produces screenshots of duration and transition settings"
    - "Running the playlists E2E spec produces screenshots of nested playlist insertion"
    - "Running the playlists E2E spec produces screenshots of background audio controls"
    - "Running the playlists E2E spec produces screenshots of player preview mode"
  artifacts:
    - path: "tests/e2e/playlists-screenshots.spec.js"
      provides: "Playlists E2E screenshot test spec"
      min_lines: 200
    - path: "screenshots/117/"
      provides: "Screenshot evidence directory"
  key_links:
    - from: "tests/e2e/playlists-screenshots.spec.js"
      to: "tests/e2e/helpers/index.js"
      via: "import screenshotStep, loginAndPrepare, etc."
      pattern: "import.*screenshotStep.*from.*helpers"
    - from: "tests/e2e/playlists-screenshots.spec.js"
      to: "src/pages/PlaylistsPage.jsx"
      via: "navigates to Playlists sidebar link"
      pattern: "click.*Playlists"
---

<objective>
Create E2E screenshot tests for the Playlists feature, covering list CRUD operations, creation modal, editor interactions (item addition, drag reorder, duration/transition settings, nested playlists, background audio), and player preview.

Purpose: Provide screenshot evidence for all 8 playlist requirements (PLAY-01 through PLAY-08).
Output: tests/e2e/playlists-screenshots.spec.js with 8 test cases producing screenshots in screenshots/117/.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/helpers/index.js
@tests/e2e/helpers/screenshots.js
@tests/e2e/scenes-screenshots.spec.js (pattern reference)
@src/pages/PlaylistsPage.jsx
@src/pages/PlaylistEditorPage.jsx
</context>

<interfaces>
<!-- Key patterns the executor needs from existing E2E infrastructure -->

From tests/e2e/helpers/index.js:
```javascript
export { screenshotStep, VIEWPORTS, cleanScreenshots, detectViewport } from './screenshots.js';
export { loginAndPrepare, dismissAnyModals, waitForPageReady, navigateToSection, generateTestName, assertAppReady } from '../helpers.js';
```

From tests/e2e/helpers/screenshots.js:
```javascript
// screenshotStep(page, area, step, options) -> Promise<string>
// area = '117' for this phase
// step = descriptive name like '01-playlists-list'
```

From tests/e2e/fixtures/index.js:
```javascript
import { test } from './fixtures/index.js';
```

Key PlaylistsPage UI elements:
- Sidebar nav link: text "Playlists"
- Page heading: "Playlists" (h1 via PageHeader)
- Create button: "New Playlist" or Plus icon button
- Create modal: CreatePlaylistModal with name input
- Table with playlist rows showing name, items count, created date
- Delete confirmation modal: DeleteConfirmModal
- Duplicate action in table row

Key PlaylistEditorPage UI elements:
- Editor header with playlist name (editable), save status badge
- Settings button (gear icon) opens transition effect dropdown and Background Audio section
- Timeline strip at bottom with PlaylistStripItem components (drag-reorderable)
- Library panel on left with media grid, filter tabs (All, Images, Videos, Audio, etc.)
- TV Preview area in center with Play/Pause/Skip controls
- Filter tabs include "Playlists" tab for nested playlist insertion
- Volume slider (range input) for background audio
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create playlists E2E screenshot spec</name>
  <files>tests/e2e/playlists-screenshots.spec.js</files>
  <action>
Create a Playwright E2E test file following the exact pattern from scenes-screenshots.spec.js. Use area '117' for all screenshotStep calls. The file must contain 8 test cases:

**PLAY-01: Playlist list page with create, duplicate, delete actions**
- Navigate via sidebar click on "Playlists"
- Wait for page heading or empty state ("No playlists yet" or similar)
- Screenshot the list page: screenshotStep(page, '117', '01-playlists-list')
- If playlists exist, hover a row to reveal action buttons, screenshot: '01-playlists-actions'

**PLAY-02: Playlist creation modal with name input**
- Click the "New Playlist" / Plus button to open create modal
- Wait for modal to appear (look for input with placeholder containing "name" or modal title)
- Screenshot empty modal: '02-create-modal-empty'
- Fill in name "Test Playlist E2E", screenshot: '02-create-modal-filled'
- Close modal without creating (press Escape or click close)

**PLAY-03: Playlist editor with item addition from media library**
- Navigate to playlist editor. If playlists exist on the list page, click the first one to open editor. If no playlists exist, create one first via the modal then navigate to it.
- The editor page uses PlaylistEditorPage component with a library panel on the left.
- Wait for editor to load (look for "All changes saved" badge or playlist name heading)
- Screenshot the editor: '03-editor-loaded'
- In the library panel, if media items exist, click one to add it to the timeline
- Screenshot after adding: '03-editor-with-item'
- If no media, screenshot the empty library state

**PLAY-04: Playlist item drag-and-drop reordering**
- In the editor, if 2+ items exist in the timeline strip, screenshot the timeline strip area
- Since actual drag-and-drop is hard in Playwright, focus on capturing the timeline with multiple items and the drop zone indicator
- Screenshot: '04-timeline-items'
- Hover over a timeline item to show move up/down buttons (onMoveUp/onMoveDown handlers exist)
- Screenshot: '04-reorder-controls'

**PLAY-05: Playlist item duration and transition settings**
- In the editor, click the Settings gear button in the header to open the settings dropdown
- Wait for the dropdown to appear (look for "Transition Effect" heading)
- Screenshot the settings panel with transition dropdown: '05-transition-settings'
- Change the transition dropdown to "Fade" to capture the selection
- Screenshot: '05-transition-fade-selected'

**PLAY-06: Nested playlist insertion with depth indicator**
- In the editor library panel, click the "Playlists" filter tab
- Wait for the tab to become active (orange-500 background)
- Screenshot the library showing playlists filter: '06-nested-playlists-filter'
- If playlist items appear, screenshot one: '06-nested-playlist-available'

**PLAY-07: Background audio toggle and volume control**
- In the editor, open Settings (gear icon) if not already open
- Scroll down to the "Background Audio" section in the settings dropdown
- Screenshot the audio section: '07-background-audio-section'
- If an audio track is set, screenshot the volume slider: '07-audio-volume-control'
- If no audio set, screenshot the audio track selector dropdown

**PLAY-08: Playlist preview in player mode**
- In the editor, the center area is the TV Preview with Play/Pause controls
- If items exist in the timeline, click a timeline item to select it for preview
- Screenshot the preview area showing the selected item: '08-player-preview'
- Click the Play button (orange circle button) to start playback
- Screenshot the playing state (look for "PLAYING" indicator and "NOW" badge): '08-player-playing'

**Important patterns to follow:**
- Import from `./fixtures/index.js` for test, from `./helpers/index.js` for helpers
- Use `test.beforeEach` with `loginAndPrepare(page)` and `assertAppReady(page, test)`
- Skip non-chromium projects: `test.skip(testInfo.project.name !== 'chromium')`
- Skip without credentials: `test.skip(!process.env.TEST_USER_EMAIL)`
- Use `test.slow()` for tests that take longer
- Use `waitForPageReady(page)` after navigation
- Wrap operations in try/catch with fallback screenshots on failure
- Use `.catch(() => null)` on waitFor calls that might not resolve
- Navigate to Playlists via sidebar: `page.getByRole('link', { name: /playlists/i })` or `navigateToSection(page, 'playlists')`
- To enter playlist editor, click on a playlist row/name to navigate, or use `window.__setCurrentPage('playlist-editor-{id}')` if direct navigation is needed
  </action>
  <verify>
    <automated>ls tests/e2e/playlists-screenshots.spec.js && node -c "import('file:///$(pwd)/tests/e2e/playlists-screenshots.spec.js').catch(() => 'syntax ok')" 2>&1 | head -5; echo "File exists and is valid JS"</automated>
  </verify>
  <done>tests/e2e/playlists-screenshots.spec.js exists with 8 test cases (PLAY-01 through PLAY-08), each using screenshotStep with area '117'</done>
</task>

<task type="auto">
  <name>Task 2: Run playlists E2E tests and capture screenshots</name>
  <files>screenshots/117/</files>
  <action>
Run the playlists screenshot spec and verify screenshots are produced.

1. Run: `npx playwright test tests/e2e/playlists-screenshots.spec.js --project=chromium --reporter=list`
2. If tests fail due to selector issues, fix the selectors in the spec file based on error messages. Common fixes:
   - If sidebar "Playlists" link not found, try `page.locator('a').filter({ hasText: 'Playlists' })` or use `navigateToSection(page, 'playlists')`
   - If create modal doesn't appear, check for limit-reached modal blocking and dismiss it
   - If editor doesn't load, ensure playlist navigation works (may need `window.__setCurrentPage`)
   - If timeline items not found, adjust locators for the timeline strip area
3. After tests pass (or pass with expected graceful skips for missing data), verify screenshots exist: `ls screenshots/117/`
4. Each test should produce at least 1-2 screenshots. Total should be 10+ screenshot files in screenshots/117/.

If TEST_USER_EMAIL is not set, tests will skip gracefully -- that is acceptable. The spec file must be syntactically correct and structurally complete regardless.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/playlists-screenshots.spec.js --project=chromium --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>Playlists E2E spec runs without crashes. Screenshots captured in screenshots/117/ for each test that can execute with available data.</done>
</task>

</tasks>

<verification>
1. `tests/e2e/playlists-screenshots.spec.js` exists with 8 named test cases matching PLAY-01 through PLAY-08
2. Each test uses `screenshotStep(page, '117', ...)` for consistent screenshot naming
3. Screenshots are saved to `screenshots/117/` directory
4. Test follows established patterns from scenes-screenshots.spec.js
</verification>

<success_criteria>
- 8 test cases exist covering all PLAY requirements
- Tests run without crashes (may skip gracefully if no test credentials)
- Screenshot files produced in screenshots/117/ for each passing test
- Code follows project E2E patterns (fixtures, helpers, screenshotStep)
</success_criteria>

<output>
After completion, create `.planning/phases/117-playlists-layouts-e2e/117-01-SUMMARY.md`
</output>
