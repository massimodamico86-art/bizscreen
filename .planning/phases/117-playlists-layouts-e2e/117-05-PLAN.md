---
phase: 117-playlists-layouts-e2e
plan: 05
type: execute
wave: 1
depends_on: ["117-04"]
files_modified:
  - tests/e2e/layouts-screenshots.spec.js
autonomous: true
requirements: [PLAY-01, PLAY-02, PLAY-03, PLAY-04, PLAY-05, PLAY-06, PLAY-07, PLAY-08, LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, LAYOUT-05, LAYOUT-06, LAYOUT-07, LAYOUT-08]
gap_closure: true

must_haves:
  truths:
    - "LAYOUT-02: Layout editor screenshot shows preset buttons and zone canvas (not 'Layout not found')"
    - "LAYOUT-03: Layout editor screenshot shows zone selection with property panel visible"
    - "LAYOUT-04: Layout editor screenshot shows content assignment modal with tabs"
  artifacts:
    - path: "tests/e2e/layouts-screenshots.spec.js"
      provides: "Layout E2E spec with page.route() API mocking"
      contains: "setupLayoutMocking"
    - path: "screenshots/117/117-11-layout-editor-presets-desktop.png"
      provides: "Layout editor with preset buttons visible"
    - path: "screenshots/117/117-12-zone-selected-properties-desktop.png"
      provides: "Zone selection with property panel"
    - path: "screenshots/117/117-13-widget-type-selector-desktop.png"
      provides: "Content assignment modal"
  key_links:
    - from: "tests/e2e/layouts-screenshots.spec.js"
      to: "LayoutEditorPage.jsx"
      via: "page.route() mocking intercepts fetchLayoutWithZones Supabase call"
      pattern: "page\\.route\\('\\*\\*/rest/v1/layouts"
---

<objective>
Close the 3 remaining layout editor gaps (LAYOUT-02, LAYOUT-03, LAYOUT-04) by adding page.route() API mocking for the layouts and layout_zones Supabase endpoints.

Purpose: The layout editor tests currently show "Layout not found" because the Supabase dynamic import fails in Playwright's evaluate context. The proven fix (from 117-03 playlists) is to intercept REST API calls via page.route() and return mock data.

Output: Updated layouts-screenshots.spec.js with setupLayoutMocking() that returns a Two Columns layout with zones, enabling all 3 editor tests to render full UI.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-playlists-layouts-e2e/117-03-SUMMARY.md
@.planning/phases/117-playlists-layouts-e2e/117-04-SUMMARY.md
@.planning/phases/117-playlists-layouts-e2e/117-VERIFICATION.md

@tests/e2e/layouts-screenshots.spec.js
@tests/e2e/playlists-screenshots.spec.js

<interfaces>
<!-- The layout editor fetches data via fetchLayoutWithZones which calls:
     supabase.from('layouts').select('*, layout_zones(id, zone_name, x_percent, y_percent, width_percent, height_percent, z_index, assigned_playlist_id, assigned_media_id, playlists:assigned_playlist_id(id, name), media:assigned_media_id(id, name, type, thumbnail_url))').eq('id', layoutId).single()

     PostgREST translates this to a GET request like:
     /rest/v1/layouts?select=*,layout_zones(...)&id=eq.{layoutId}
     with Accept: application/vnd.pgrst.object+json header for .single()

     The response is a single JSON object (not array) with nested layout_zones array.

     LayoutEditorPage.jsx (line 165-167):
       const data = await fetchLayoutWithZones(layoutId);
       setLayout(data);
       setZones(data.layout_zones || []);

     When layout is null/undefined after loading, line 497 renders:
       <p className="text-gray-500">Layout not found</p>
-->

<!-- From playlists-screenshots.spec.js - the proven mocking pattern:
     setupPlaylistMocking(page) intercepts **/rest/v1/playlists?* and returns mock objects.
     Same approach needed for **/rest/v1/layouts?* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setupLayoutMocking and fix LAYOUT-02/03/04 tests</name>
  <files>tests/e2e/layouts-screenshots.spec.js</files>
  <action>
Add a `setupLayoutMocking(page)` function to `layouts-screenshots.spec.js` following the exact pattern from `setupPlaylistMocking` in `playlists-screenshots.spec.js`. This function must:

1. **Define mock constants** at module level (before the test.describe):
   - `MOCK_LAYOUT_ID = 'e2e-test-layout-001'`
   - `MOCK_LAYOUT` object with: id, name ("E2E Test Layout"), description ("Two column layout for testing"), owner_id ("e2e-test-user"), orientation ("landscape"), resolution ("1920x1080"), created_at, updated_at, and a `layout_zones` array with two zones:
     - Zone 1: id "zone-001", zone_name "Left", x_percent 0, y_percent 0, width_percent 50, height_percent 100, z_index 0, assigned_playlist_id null, assigned_media_id null, playlists null, media null
     - Zone 2: id "zone-002", zone_name "Right", x_percent 50, y_percent 0, width_percent 50, height_percent 100, z_index 1, assigned_playlist_id null, assigned_media_id null, playlists null, media null

2. **Create `setupLayoutMocking(page)` function** that intercepts these routes:
   - `**/rest/v1/layouts?*` GET: Return MOCK_LAYOUT as a single object (PostgREST .single() returns object, not array). For list queries (no `id=eq.` filter), return `[MOCK_LAYOUT]` array. Handle POST/PATCH similarly to playlists mock.
   - `**/rest/v1/layout_zones?*` GET: Return `MOCK_LAYOUT.layout_zones` array. This covers any separate zone queries.
   - `**/rest/v1/playlists?*` GET: Return `[{id: 'playlist-001', name: 'Test Playlist'}]` (for the assign content modal's playlist tab).
   - `**/rest/v1/media_assets?*` GET: Return mock media array similar to playlists spec (for the assign content modal's media tab).
   - For all non-GET methods on these routes, call `route.continue()`.

3. **Simplify `navigateToLayoutEditor(page)` function**:
   - Remove the entire Supabase evaluate block (lines 58-94). It always fails.
   - Replace with a single call: `page.evaluate(() => window.__setCurrentPage('layout-editor-e2e-test-layout-001'))` using the MOCK_LAYOUT_ID constant.
   - Keep the `waitForPageReady` and `waitForTimeout(1500)` at the end.

4. **Update LAYOUT-02 test** (line ~204):
   - Add `await setupLayoutMocking(page);` as the first line inside the test, BEFORE `navigateToLayoutEditor`.
   - The existing waits for `presetButton` and screenshot capture should now work because the layout loads with zones.

5. **Update LAYOUT-03 test** (line ~248):
   - Add `await setupLayoutMocking(page);` as the first line.
   - The zone locator `.absolute.border-2.cursor-pointer` should now find 2 zones (Left and Right).
   - If that CSS selector does not match rendered zones, try alternative: `page.locator('[data-zone-id]')` or look for clickable zone divs within the layout preview area.

6. **Update LAYOUT-04 test** (line ~286):
   - Add `await setupLayoutMocking(page);` as the first line.
   - After zones are found and clicked, look for "Assign Content" button. The button text may be "Assign Content", "Assign", or may appear in the zone properties panel.
   - If no assign modal appears after clicking the button, try looking for content tabs within the zone properties panel itself (some editors inline the content picker rather than using a modal).

7. **DO NOT modify** LAYOUT-01, LAYOUT-05, LAYOUT-06, LAYOUT-07, or LAYOUT-08 tests -- they already pass.

Important implementation notes:
- The Supabase PostgREST `.single()` modifier adds `Accept: application/vnd.pgrst.object+json` header and expects a single object response (not wrapped in array). The page.route handler must return a plain object for single-fetch requests, or PostgREST client will error.
- The `select` query parameter in the URL will contain the embedded `layout_zones(...)` relation. The mock response should include the nested `layout_zones` array directly in the object -- PostgREST returns embedded relations as nested arrays.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/layouts-screenshots.spec.js --project=chromium --grep="LAYOUT-02|LAYOUT-03|LAYOUT-04" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - LAYOUT-02 screenshot shows layout editor with preset buttons or zone canvas (not "Layout not found")
    - LAYOUT-03 screenshot shows zone selection with property panel visible
    - LAYOUT-04 screenshot shows content assignment area (modal or inline picker)
    - All 3 tests pass without errors
    - Screenshots in screenshots/117/ have distinct content (not identical md5 hashes)
  </done>
</task>

</tasks>

<verification>
Run the full layouts spec to confirm no regressions in LAYOUT-01/05/06/07/08:
```bash
npx playwright test tests/e2e/layouts-screenshots.spec.js --project=chromium 2>&1 | tail -30
```

Verify screenshots are distinct (not all same hash):
```bash
md5 screenshots/117/117-11-layout-editor-presets-desktop.png screenshots/117/117-12-zone-*-desktop.png screenshots/117/117-13-*-desktop.png 2>/dev/null
```
</verification>

<success_criteria>
- All 8 LAYOUT tests pass (LAYOUT-01 through LAYOUT-08)
- LAYOUT-02 screenshot shows editor chrome with preset buttons or zone canvas
- LAYOUT-03 screenshot shows zone selected with property panel
- LAYOUT-04 screenshot shows content assignment UI
- No regressions in existing passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/117-playlists-layouts-e2e/117-05-SUMMARY.md`
</output>
