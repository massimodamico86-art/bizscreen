---
phase: 71-cleanup-notification-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/gdprDeletionService.js
  - src/services/geolocationService.js
  - src/services/demoContentService.js
  - src/services/dataFeedScheduler.js
  - src/services/scimService.js
  - src/hooks/usePrefetch.js
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06

must_haves:
  truths:
    - "gdprDeletionService.js no longer exists in the codebase"
    - "geolocationService.js no longer exists in the codebase"
    - "demoContentService.js no longer exists in the codebase"
    - "dataFeedScheduler.js no longer exists in the codebase"
    - "scimService.js no longer exists in the codebase"
    - "usePrefetch.js no longer exists in the codebase"
    - "No import references to any of the 6 deleted files remain anywhere in the codebase"
    - "Build succeeds after deletion"
  artifacts: []
  key_links: []
---

<objective>
Delete 6 unused service/hook files that are dead code -- they have no imports anywhere in the codebase.

Purpose: These files clutter the codebase and confuse navigation. They were identified during the v5.0 UI completeness audit as having zero importers.

Output: 6 files deleted, no remaining references, clean build.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused service and hook files</name>
  <files>
    src/services/gdprDeletionService.js
    src/services/geolocationService.js
    src/services/demoContentService.js
    src/services/dataFeedScheduler.js
    src/services/scimService.js
    src/hooks/usePrefetch.js
  </files>
  <action>
Delete the following 6 files using `git rm`:

```bash
git rm src/services/gdprDeletionService.js
git rm src/services/geolocationService.js
git rm src/services/demoContentService.js
git rm src/services/dataFeedScheduler.js
git rm src/services/scimService.js
git rm src/hooks/usePrefetch.js
```

These files have ZERO importers anywhere in the codebase (verified via grep for `import.*from.*{filename}` across all of `src/`). No other files reference them.

After deletion, run a full-codebase grep to confirm no stale import references remain:

```bash
grep -r "gdprDeletionService\|geolocationService\|demoContentService\|dataFeedScheduler\|scimService\|usePrefetch" src/ --include="*.js" --include="*.jsx" -l
```

This should return zero results.
  </action>
  <verify>
1. `ls src/services/gdprDeletionService.js 2>&1` returns "No such file"
2. `ls src/services/geolocationService.js 2>&1` returns "No such file"
3. `ls src/services/demoContentService.js 2>&1` returns "No such file"
4. `ls src/services/dataFeedScheduler.js 2>&1` returns "No such file"
5. `ls src/services/scimService.js 2>&1` returns "No such file"
6. `ls src/hooks/usePrefetch.js 2>&1` returns "No such file"
7. `grep -r "gdprDeletionService\|geolocationService\|demoContentService\|dataFeedScheduler\|scimService\|usePrefetch" src/ --include="*.js" --include="*.jsx" -l` returns empty
8. `npx vite build --mode development 2>&1 | tail -5` shows successful build
  </verify>
  <done>
All 6 dead code files are deleted. No import references remain. Build succeeds cleanly.
  </done>
</task>

</tasks>

<verification>
1. None of the 6 files exist on disk
2. No grep matches for any of the 6 filenames across `src/`
3. `npx vite build --mode development` succeeds
4. `npx eslint src/ --quiet` shows no new errors from the deletion
</verification>

<success_criteria>
- All 6 files (gdprDeletionService, geolocationService, demoContentService, dataFeedScheduler, scimService, usePrefetch) are deleted
- Zero import references remain
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/71-cleanup-notification-gaps/71-02-SUMMARY.md`
</output>
