---
phase: 71-cleanup-notification-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/NotificationSettingsPage.jsx
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "User can see device_recovery alert type in the Device Alerts category on the notification settings page"
    - "User can toggle device_recovery alerts on/off individually"
    - "User can see device_recovery_exhausted alert type in the Device Alerts category on the notification settings page"
    - "User can toggle device_recovery_exhausted alerts on/off individually"
    - "Saving preferences includes device_recovery and device_recovery_exhausted in the blacklist when disabled"
  artifacts:
    - path: "src/pages/NotificationSettingsPage.jsx"
      provides: "Device recovery alert toggles in notification settings"
      contains: "ALERT_TYPES.DEVICE_RECOVERY"
  key_links:
    - from: "src/pages/NotificationSettingsPage.jsx"
      to: "src/services/alertEngineService.js"
      via: "ALERT_TYPES import"
      pattern: "ALERT_TYPES\\.DEVICE_RECOVERY"
---

<objective>
Add device_recovery and device_recovery_exhausted alert types to the Notification Settings page so users can enable/disable these recovery alerts.

Purpose: Phase 68 added device recovery and recovery-exhausted alert types to the alert engine, NotificationBell, and Alerts Center, but the Notification Settings page was not updated. Users cannot currently toggle these alert types on or off.

Output: Updated NotificationSettingsPage.jsx with both recovery alert types visible and toggleable.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/NotificationSettingsPage.jsx
@src/services/alertEngineService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add device recovery alert types to NotificationSettingsPage</name>
  <files>src/pages/NotificationSettingsPage.jsx</files>
  <action>
In `src/pages/NotificationSettingsPage.jsx`, make two changes:

1. **Add to ALERT_CATEGORIES.device.types array** (around line 34-39): Add `ALERT_TYPES.DEVICE_RECOVERY` and `ALERT_TYPES.DEVICE_RECOVERY_EXHAUSTED` to the end of the `device` category's `types` array. This places them alongside other device alerts (offline, screenshot failed, cache stale, error).

2. **Add to TYPE_LABELS object** (around line 63-75): Add two new entries:
   - `[ALERT_TYPES.DEVICE_RECOVERY]: 'Device recovers from failure'`
   - `[ALERT_TYPES.DEVICE_RECOVERY_EXHAUSTED]: 'Device recovery attempts exhausted'`

These labels follow the existing pattern of describing WHEN the alert fires (e.g., "Device goes offline", "Screenshot capture fails").

No other changes needed -- the existing toggleType/toggleCategory logic, save handler (blacklist approach), and load handler all operate on the full `ALERT_TYPES` set, so adding these types to the categories and labels is sufficient.
  </action>
  <verify>
Run `npx eslint src/pages/NotificationSettingsPage.jsx` -- no errors.
Grep for `DEVICE_RECOVERY` in the file and confirm both types appear in ALERT_CATEGORIES and TYPE_LABELS.
Run `npx vite build --mode development 2>&1 | tail -5` to confirm no build errors.
  </verify>
  <done>
NotificationSettingsPage shows "Device recovers from failure" and "Device recovery attempts exhausted" toggles in the Device Alerts category. Both can be individually toggled. Save persists the preference via the existing blacklist mechanism.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/pages/NotificationSettingsPage.jsx` passes with no errors
2. ALERT_CATEGORIES.device.types includes both DEVICE_RECOVERY and DEVICE_RECOVERY_EXHAUSTED
3. TYPE_LABELS has entries for both new alert types
4. Build succeeds without errors
</verification>

<success_criteria>
- Device recovery and device recovery exhausted alert types appear in the Device Alerts category of the notification settings page
- Users can toggle each type independently
- Save correctly includes/excludes these types in the blacklist
</success_criteria>

<output>
After completion, create `.planning/phases/71-cleanup-notification-gaps/71-01-SUMMARY.md`
</output>
