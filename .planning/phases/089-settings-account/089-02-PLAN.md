---
phase: 089-settings-account
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/WhiteLabelSettingsPage.jsx
  - src/pages/EnterpriseSecurityPage.jsx
  - src/pages/TeamPage.jsx
  - src/components/compliance/DataPrivacySettings.jsx
autonomous: true
requirements: [SET-03, SET-04, SET-05]

must_haves:
  truths:
    - "EnterpriseSecurityPage security policy tab saves password policy and session timeout via savePasswordPolicy/saveSessionPolicy"
    - "EnterpriseSecurityPage data deletion tab DELETE MY DATA confirmation flow calls requestDataDeletion"
    - "TeamPage invite modal sends inviteMember, role change modal calls updateMemberRole, revoke calls revokeMember"
    - "WhiteLabelSettingsPage renders domain list with Badge components and saves branding settings via updateWhiteLabelSettings"
  artifacts:
    - path: "src/pages/WhiteLabelSettingsPage.jsx"
      provides: "White-label settings with custom domains and branding overrides"
      contains: "import.*Badge.*from.*design-system"
    - path: "src/components/compliance/DataPrivacySettings.jsx"
      provides: "GDPR data privacy with export and account deletion"
      contains: "variant=\"secondary\""
    - path: "src/pages/TeamPage.jsx"
      provides: "Team management with invite, role change, and revoke"
      contains: "inviteMember"
    - path: "src/pages/EnterpriseSecurityPage.jsx"
      provides: "Enterprise security with SSO, password policy, session timeout, data deletion"
      contains: "savePasswordPolicy"
  key_links:
    - from: "src/pages/WhiteLabelSettingsPage.jsx"
      to: "src/services/domainService.js"
      via: "addDomain, removeDomain, verifyDomain, updateWhiteLabelSettings"
      pattern: "updateWhiteLabelSettings|addDomain"
    - from: "src/pages/EnterpriseSecurityPage.jsx"
      to: "src/services/passwordService.js"
      via: "savePasswordPolicy, saveSessionPolicy"
      pattern: "savePasswordPolicy|saveSessionPolicy"
    - from: "src/pages/TeamPage.jsx"
      to: "src/services/teamService.js"
      via: "inviteMember, updateMemberRole, revokeMember"
      pattern: "inviteMember|updateMemberRole|revokeMember"
---

<objective>
Audit and fix enterprise security, team management, and white-label settings pages — fix Badge collision in WhiteLabelSettingsPage, fix variant="outline" in DataPrivacySettings, and verify TeamPage + EnterpriseSecurityPage wiring.

Purpose: SET-03 requires enterprise security controls save correctly. SET-04 requires team management invite/role/remove works. SET-05 requires white-label settings save without errors. WhiteLabelSettingsPage has a critical Badge collision bug (lucide-react icon shadows design-system component) and missing Card/Button/X imports. DataPrivacySettings has 7 instances of invalid variant="outline".

Output: Four verified files with correct imports, valid variants, and functional save flows.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/WhiteLabelSettingsPage.jsx
@src/pages/EnterpriseSecurityPage.jsx
@src/pages/TeamPage.jsx
@src/components/compliance/DataPrivacySettings.jsx
@src/design-system/index.js

<interfaces>
<!-- Design-system Button valid variants: primary, secondary, ghost, danger -->
<!-- NO "outline" variant exists — use "secondary" instead -->
<!-- Known project decisions:
     - Phase 86: Button variant='outline' replaced with variant='secondary'
     - Phase 85: Badge collision fix — remove Badge from lucide-react to avoid shadowing design-system Badge
-->

From src/design-system/index.js (relevant exports):
```javascript
export { Card, CardHeader, CardTitle, CardDescription } from './components/Card';
export { Button, ButtonGroup, IconButton } from './components/Button';
export { Badge, StatusBadge } from './components/Badge';
```

WhiteLabelSettingsPage current imports (BROKEN):
```javascript
import { Badge, ... } from 'lucide-react';  // Badge is lucide ICON, not design-system component!
// MISSING: Card, Button, Badge from design-system
// MISSING: X from lucide-react (used in close button line 615)
```

WhiteLabelSettingsPage uses:
- `<Badge variant="success">` and `<Badge variant="info">` — these are design-system Badge, NOT lucide icon
- `<Card>`, `<Button>` — need design-system imports
- `<X size={20}>` on line 615 — needs lucide-react import

DataPrivacySettings.jsx:
- Has `variant="outline"` x7 — all should be `variant="secondary"`
- Design-system imports (Button, Card) already present from '../../design-system'

TeamPage.jsx:
- Already has correct design-system imports (PageLayout, PageContent, PageHeader, Card, Button, EmptyState, Modal, etc.)
- No variant="outline" issues
- Service imports (fetchTeamMembers, inviteMember, updateMemberRole, revokeMember, etc.) all present

EnterpriseSecurityPage.jsx:
- Already has correct design-system imports (PageLayout, PageContent, PageHeader, Button, Alert)
- No variant="outline" issues
- Service imports (getSSOProvider, saveSSOProvider, getPasswordPolicy, savePasswordPolicy, etc.) all present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WhiteLabelSettingsPage Badge collision, missing imports, and variant="outline"</name>
  <files>src/pages/WhiteLabelSettingsPage.jsx</files>
  <action>
1. Open `src/pages/WhiteLabelSettingsPage.jsx`

2. Fix the Badge collision (critical bug — same pattern as Phase 85 CampaignEditorPage fix):
   - REMOVE `Badge` from the lucide-react import on line 4 (it shadows the design-system Badge component)
   - The lucide-react Badge icon is NOT used anywhere as `<Badge />` icon — all Badge usage is design-system `<Badge variant="success">` style

3. Add missing `X` to the lucide-react import (it's used on line 615 as `<X size={20}>` for the modal close button but is NOT currently imported)

4. Add design-system imports after the lucide-react import block (after line 21):
```javascript
import { Card, Button, Badge } from '../design-system';
```

5. Replace both instances of `variant="outline"` with `variant="secondary"`:
   - Line ~351: Verify domain button
   - Line ~640: Cancel button in add-domain modal

6. Save the file.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -c 'variant="outline"' src/pages/WhiteLabelSettingsPage.jsx | grep -q '^0$' && echo "PASS: No outline variants" || echo "FAIL: outline variants remain"; grep "from.*lucide-react" src/pages/WhiteLabelSettingsPage.jsx | grep -qv "Badge" && echo "PASS: Badge not in lucide import" || echo "FAIL: Badge still in lucide import"; grep -q "import.*Badge.*from.*design-system\|import.*Card.*Button.*Badge.*from.*design-system" src/pages/WhiteLabelSettingsPage.jsx && echo "PASS: Badge from design-system" || echo "FAIL: Badge not from design-system"</automated>
  </verify>
  <done>WhiteLabelSettingsPage has Badge imported from design-system (not lucide-react), Card and Button from design-system, X from lucide-react, and zero variant="outline" instances. Both tabs (domains + branding) render without crashes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DataPrivacySettings variant="outline" and verify EnterpriseSecurityPage + TeamPage wiring</name>
  <files>src/components/compliance/DataPrivacySettings.jsx, src/pages/EnterpriseSecurityPage.jsx, src/pages/TeamPage.jsx</files>
  <action>
1. Open `src/components/compliance/DataPrivacySettings.jsx`
2. Replace ALL 7 instances of `variant="outline"` with `variant="secondary"`:
   - Line ~208: Download Export button
   - Line ~233: Quick Export button
   - Line ~246: Request Full Export button
   - Line ~273: Withdraw Cookie Consent button
   - Line ~316: Cancel Deletion button
   - Line ~337: Delete My Account button
   - Line ~416: Cancel button in delete modal
3. Save the file.

4. Open `src/pages/EnterpriseSecurityPage.jsx` — READ-ONLY AUDIT:
   - Verify `savePasswordPolicy` and `saveSessionPolicy` are imported from `../services/passwordService` (line 41)
   - Verify `handleSaveSecurityPolicy` calls both `savePasswordPolicy(tenantId, {...})` and `saveSessionPolicy(tenantId, {...})` via Promise.all
   - Verify `requestDataDeletion` is imported from `../services/complianceService` (line 40)
   - Verify `handleDeleteTenantData` calls `requestDataDeletion` with tenant_id, email, and reason
   - Verify `deleteConfirmText` must equal 'DELETE MY DATA' or 'DELETE' before the delete button enables
   - Verify design-system imports are correct (PageLayout, PageContent, PageHeader, Button, Alert — all present)
   - NO changes expected unless an actual bug is found

5. Open `src/pages/TeamPage.jsx` — READ-ONLY AUDIT:
   - Verify `inviteMember`, `updateMemberRole`, `revokeMember` are imported from `../services/teamService` (lines 43-46)
   - Verify `handleInvite` calls `inviteMember({ email, role })` and on success calls `loadData()`
   - Verify `handleChangeRole` calls `updateMemberRole(memberId, newRole)` and on success calls `loadData()`
   - Verify `handleRevoke` calls `revokeMember(memberId)` and on success calls `loadData()`
   - Verify Modal components are imported from design-system (Modal, ModalHeader, ModalTitle, ModalContent, ModalFooter — all present)
   - NO changes expected unless an actual bug is found
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && grep -c 'variant="outline"' src/components/compliance/DataPrivacySettings.jsx | grep -q '^0$' && echo "PASS: No outline variants in DataPrivacySettings" || echo "FAIL: outline variants remain"; grep -q "savePasswordPolicy" src/pages/EnterpriseSecurityPage.jsx && echo "PASS: savePasswordPolicy present" || echo "FAIL: savePasswordPolicy missing"; grep -q "inviteMember" src/pages/TeamPage.jsx && echo "PASS: inviteMember present" || echo "FAIL: inviteMember missing"</automated>
  </verify>
  <done>DataPrivacySettings has zero variant="outline" instances. EnterpriseSecurityPage security policy save (password policy + session timeout) and DELETE MY DATA confirmation flow verified correct. TeamPage invite, role change, and revoke flows verified correct with proper service function calls and Modal component wiring.</done>
</task>

</tasks>

<verification>
1. `grep -c 'variant="outline"' src/pages/WhiteLabelSettingsPage.jsx` returns 0
2. `grep -c 'variant="outline"' src/components/compliance/DataPrivacySettings.jsx` returns 0
3. `grep 'Badge' src/pages/WhiteLabelSettingsPage.jsx | grep lucide` returns no matches (Badge removed from lucide import)
4. `grep 'Badge.*design-system\|design-system.*Badge' src/pages/WhiteLabelSettingsPage.jsx` returns a match
5. `grep 'savePasswordPolicy\|saveSessionPolicy' src/pages/EnterpriseSecurityPage.jsx` shows both functions present
6. `grep 'inviteMember\|updateMemberRole\|revokeMember' src/pages/TeamPage.jsx` shows all three functions present
7. `npm run build 2>&1 | tail -5` completes without errors for these files
</verification>

<success_criteria>
- WhiteLabelSettingsPage renders domain list with proper Badge components (not lucide icon) and saves settings
- DataPrivacySettings renders all buttons with valid variant="secondary"
- EnterpriseSecurityPage password policy and session timeout save correctly via savePasswordPolicy/saveSessionPolicy
- EnterpriseSecurityPage DELETE MY DATA confirmation flow calls requestDataDeletion
- TeamPage invite, role change, and revoke flows call correct service functions
- Zero instances of variant="outline" across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/089-settings-account/089-02-SUMMARY.md`
</output>
