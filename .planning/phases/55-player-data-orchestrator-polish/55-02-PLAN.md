---
phase: 55-player-data-orchestrator-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/player/components/widgets/DataTableWidget.jsx
autonomous: true

must_haves:
  truths:
    - "Data table with more rows than fit on screen auto-paginates through pages with smooth fade transition"
    - "Image URL fields in data sources render as actual images (not raw URLs) on screen"
    - "First page load does NOT trigger a fade transition (no flicker on mount)"
    - "Page indicator still shows current page number"
  artifacts:
    - path: "src/player/components/widgets/DataTableWidget.jsx"
      provides: "Enhanced data table with CSS fade pagination transitions and image_url rendering"
      contains: "image_url"
  key_links:
    - from: "src/player/components/widgets/DataTableWidget.jsx"
      to: "src/services/dataSourceService.js"
      via: "FIELD_DATA_TYPES.IMAGE_URL type detection in cell rendering"
      pattern: "image_url"
---

<objective>
Add CSS fade transitions to DataTableWidget pagination and render IMAGE_URL fields as actual images instead of raw text.

Purpose: Satisfies DATA-06 (smooth auto-pagination) and DATA-07 (image URL rendering). These are visual polish improvements that are self-contained within DataTableWidget and do not depend on the orchestrator.

Output: Modified DataTableWidget.jsx with fade transitions between pages and image cell rendering
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-player-data-orchestrator-polish/55-RESEARCH.md
@src/player/components/widgets/DataTableWidget.jsx
@src/services/dataSourceService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS fade transition to DataTableWidget pagination</name>
  <files>src/player/components/widgets/DataTableWidget.jsx</files>
  <action>
Add a CSS opacity fade transition when the data table switches between pages. The approach:

1. Add state: `const [displayedPage, setDisplayedPage] = useState(0)` and `const [opacity, setOpacity] = useState(1)` and `const isFirstRender = useRef(true)`.

2. Modify the pagination timer effect: when `currentPage` changes (via the existing setInterval that cycles pages), trigger a fade-out/fade-in sequence. Add a useEffect that watches `currentPage`:
   - If `isFirstRender.current` is true, set `isFirstRender.current = false`, set `displayedPage = currentPage`, and return (no animation on first load -- prevents flicker per Pitfall 4 in research).
   - Otherwise: set `opacity = 0`, then after 300ms timeout, set `displayedPage = currentPage` and set `opacity = 1`.
   - Clean up the timeout on unmount.

3. The data rows container (the div wrapping `pageRows.map(...)`) gets: `style={{ opacity, transition: 'opacity 0.3s ease-in-out', ... }}`. Use `displayedPage` instead of `currentPage` for computing `pageRows`:
   ```
   const pageRows = rows.slice(
     displayedPage * rowsPerPage,
     (displayedPage + 1) * rowsPerPage
   );
   ```

4. The page indicator at the bottom should show `currentPage + 1` (the target page, not displayedPage) so it updates immediately when the page changes.

5. When data refreshes and row count changes: only reset `currentPage` to 0 if the total number of rows actually changed. Modify the refresh timer's setData callback and the initial load: compare `result.rows?.length !== dataRef.current?.rows?.length` before calling `setCurrentPage(0)`. This prevents unnecessary page resets when the same data comes back (per research anti-pattern).
  </action>
  <verify>
- `grep "opacity" src/player/components/widgets/DataTableWidget.jsx` shows opacity state and transition style
- `grep "isFirstRender" src/player/components/widgets/DataTableWidget.jsx` confirms first-render guard
- `grep "displayedPage" src/player/components/widgets/DataTableWidget.jsx` confirms dual-page approach
- `npm run build` passes
  </verify>
  <done>
DataTableWidget pages fade out over 300ms then fade in with new page content. First page load renders immediately without animation. Page indicator updates instantly. Page only resets to 0 when row count changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render IMAGE_URL fields as actual images in DataTableWidget cells</name>
  <files>src/player/components/widgets/DataTableWidget.jsx</files>
  <action>
In the cell rendering section of DataTableWidget (inside `visibleFields.map((field) => { ... })`), add image_url detection before the existing formatValue call:

1. Extract the data type: `const dataType = field.data_type || field.dataType;`
2. Check if `dataType === 'image_url' && rawValue`:
   - If true, render an `<img>` tag instead of text:
     ```jsx
     <div key={field.name} style={{ flex: 1, padding: '0 0.5rem', overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
       <img
         src={rawValue}
         alt=""
         style={{
           maxHeight: '100%',
           maxWidth: '100%',
           objectFit: 'contain',
           display: 'block',
         }}
         onError={(e) => { e.target.style.display = 'none'; }}
       />
     </div>
     ```
   - The onError handler hides broken images silently (consistent with silent offline fallback pattern from 51-01).
   - maxHeight: '100%' constrains the image to the row height, preventing layout shift (per Pitfall 5 in research).

3. If NOT image_url, proceed with existing `formatValue(rawValue, dataType, ...)` and text rendering as before.

Do NOT import FIELD_DATA_TYPES from dataSourceService -- just compare the string 'image_url' directly. This avoids adding a new import for a simple string comparison.
  </action>
  <verify>
- `grep "image_url" src/player/components/widgets/DataTableWidget.jsx` shows the type check
- `grep "onError" src/player/components/widgets/DataTableWidget.jsx` shows error handler on img
- `npm run build` passes
  </verify>
  <done>
DataTableWidget cells with data_type === 'image_url' render as `<img>` elements with contain fit and silent error handling. Other cells continue to use formatValue for text display.
  </done>
</task>

</tasks>

<verification>
- DataTableWidget has CSS fade transition on page changes (opacity 0 -> 1 over 300ms)
- First render has no transition (immediate display)
- image_url fields render as `<img>` elements with error handling
- Non-image fields still render as text via formatValue
- Page indicator remains functional
- `npm run build` succeeds
</verification>

<success_criteria>
- Pagination transitions are smooth fade (not instant swap)
- Image URLs appear as images in table cells
- No flicker on initial load
- Broken image URLs fail silently (hidden)
</success_criteria>

<output>
After completion, create `.planning/phases/55-player-data-orchestrator-polish/55-02-SUMMARY.md`
</output>
