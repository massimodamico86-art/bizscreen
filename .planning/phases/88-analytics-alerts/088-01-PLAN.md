---
phase: 088-analytics-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/AnalyticsPage.jsx
  - src/pages/AnalyticsDashboardPage.jsx
  - src/pages/ContentPerformancePage.jsx
  - src/pages/ContentDetailAnalyticsPage.jsx
autonomous: true
requirements:
  - ANLYT-01
  - ANLYT-02
  - ANLYT-03

must_haves:
  truths:
    - "AnalyticsPage loads summary cards, date-range filter updates data, email report toggle works"
    - "AnalyticsDashboardPage loads Overview/Content/Patterns tabs, date-range pills update all data"
    - "ContentPerformancePage loads summary cards, top scenes, device uptime table, scene detail modal opens"
    - "ContentDetailAnalyticsPage loads metrics, timeline chart renders, date-range pills update data"
    - "No console errors on any analytics page"
  artifacts:
    - path: "src/pages/AnalyticsPage.jsx"
      provides: "Analytics page with correct design-system imports and working filters"
      contains: "AnalyticsPage"
    - path: "src/pages/AnalyticsDashboardPage.jsx"
      provides: "Dashboard page with tabbed sections and date-range filter"
      contains: "AnalyticsDashboardPage"
    - path: "src/pages/ContentPerformancePage.jsx"
      provides: "Content performance with scene detail modal"
      contains: "ContentPerformancePage"
    - path: "src/pages/ContentDetailAnalyticsPage.jsx"
      provides: "Content detail analytics with timeline chart"
      contains: "ContentDetailAnalyticsPage"
  key_links:
    - from: "src/pages/AnalyticsDashboardPage.jsx"
      to: "src/services/contentAnalyticsService.js"
      via: "getContentAnalyticsSummary, getTopScenes, getContentPerformanceList, getViewingHeatmap"
      pattern: "getContentAnalyticsSummary|getTopScenes|getContentPerformanceList"
    - from: "src/pages/ContentPerformancePage.jsx"
      to: "src/services/contentAnalyticsService.js"
      via: "fetchDashboardAnalytics, fetchSceneDetailAnalytics"
      pattern: "fetchDashboardAnalytics|fetchSceneDetailAnalytics"
---

<objective>
Audit and fix the four analytics pages to ensure all charts load, date-range filters produce updated results, and content performance metrics render without errors.

Purpose: Satisfy ANLYT-01 (analytics dashboard with charts and date filters), ANLYT-02 (content performance page with per-content metrics), and ANLYT-03 (content detail analytics timeline). These pages are already well-structured; this plan verifies imports, design-system usage, and functional correctness.

Output: Four verified analytics pages with no import errors, correct design-system component usage, and working date-range filters.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/AnalyticsPage.jsx
@src/pages/AnalyticsDashboardPage.jsx
@src/pages/ContentPerformancePage.jsx
@src/pages/ContentDetailAnalyticsPage.jsx
@src/services/contentAnalyticsService.js
@src/services/analyticsService.js
@src/design-system/index.js

<interfaces>
<!-- Design-system exports used by analytics pages -->
From src/design-system/index.js:
- PageLayout, PageHeader, PageContent (layout)
- Button (actions)
- Card (data containers)
- Tabs (AnalyticsDashboardPage tabs)
- Modal (ContentPerformancePage scene detail)

From src/services/contentAnalyticsService.js:
- getContentAnalyticsSummary(dateRange, filters)
- getTopScenes(dateRange, limit)
- getContentPerformanceList(dateRange, limit)
- getViewingHeatmap(dateRange, timezone)
- fetchDashboardAnalytics(dateRange, filters) → { summary, topScenes, deviceUptime }
- fetchSceneDetailAnalytics(sceneId, dateRange) → { detail, timeline }
- getContentMetrics(contentId, contentType, dateRange)
- getSceneTimeline(sceneId, dateRange, bucket)
- getMediaPlayCounts(contentId, dateRange)
- getPlaylistAppearances(contentId)
- DATE_RANGES, formatDuration, formatHours, formatRelativeTime, getUptimeColor, getUptimeBgColor

From src/services/analyticsService.js:
- getAnalyticsSummary(dateRange, locationId)
- getScreenUptime(dateRange, locationId)
- getPlaybackByScreen(dateRange, locationId)
- getPlaybackByPlaylist(dateRange, locationId)
- getPlaybackByMedia(dateRange, locationId)
- DATE_RANGES, formatDuration, formatHours, getUptimeColor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit AnalyticsPage and AnalyticsDashboardPage imports and wiring</name>
  <files>src/pages/AnalyticsPage.jsx, src/pages/AnalyticsDashboardPage.jsx</files>
  <action>
Audit both analytics pages for import correctness and design-system compliance:

**AnalyticsPage (src/pages/AnalyticsPage.jsx):**
1. Verify all imports from `../design-system` resolve correctly: `PageLayout`, `PageHeader`, `PageContent`, `Button`, `Card` — these are all valid exports. CONFIRMED OK.
2. Verify all imports from `../services/analyticsService` resolve correctly: `getAnalyticsSummary`, `getScreenUptime`, `getPlaybackByScreen`, `getPlaybackByPlaylist`, `getPlaybackByMedia`, `DATE_RANGES`, `formatDuration`, `formatHours`, `getUptimeColor` — all exported from the service. CONFIRMED OK.
3. Verify `fetchLocations` from `../services/locationService` — this was fixed in Phase 86 with defensive extraction. Confirm the import is correct.
4. Verify `canManageTeam` from `../services/permissionsService` and `getReportSubscriptions`, `updateReportSubscription` from `../services/reportSettingsService` exist.
5. Check the `Button` variant usage: `variant="ghost"` and `variant="secondary"` — both valid. CONFIRMED OK.
6. The page uses native `<select>` elements for date range and location filters — this is acceptable (not all selects need to be design-system Select).

**AnalyticsDashboardPage (src/pages/AnalyticsDashboardPage.jsx):**
1. Verify all design-system imports: `PageLayout`, `PageHeader`, `PageContent`, `Button`, `Card`, `Tabs` — all valid exports.
2. Verify all service imports from `../services/contentAnalyticsService`: `getContentAnalyticsSummary`, `getTopScenes`, `getContentPerformanceList`, `getViewingHeatmap`, `formatDuration`, `formatHours`, `formatRelativeTime` — all exported.
3. Verify `ViewingHeatmap` import from `../components/analytics/ViewingHeatmap` exists and is a valid component.
4. Check `Tabs` usage: `tabs` prop expects `{ id, label, icon? }` array, `activeTab` string, `onChange` function. The TABS constant provides this. CONFIRMED OK.
5. Check the `useCallback` for `loadData` — dependencies are `[dateRange, showToast, logger]`. When `dateRange` changes, `loadData` reference changes, triggering the `useEffect`. This is correct.

**For both pages:** Run a grep to confirm all service functions used are exported from their respective service files. If any import is missing (e.g., `reportSettingsService` functions), check if the service file exists and has the expected exports. If a service file is missing, create a stub that returns empty/default data so the page doesn't crash.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && node -e "
      // Verify AnalyticsPage imports compile
      try { require('./src/pages/AnalyticsPage.jsx'); } catch(e) { /* JSX won't require, check file exists */ }
      const fs = require('fs');
      const page1 = fs.readFileSync('src/pages/AnalyticsPage.jsx', 'utf8');
      const page2 = fs.readFileSync('src/pages/AnalyticsDashboardPage.jsx', 'utf8');
      // Verify critical imports present
      if (!page1.includes('PageLayout')) throw new Error('Missing PageLayout in AnalyticsPage');
      if (!page1.includes('PageHeader')) throw new Error('Missing PageHeader in AnalyticsPage');
      if (!page2.includes('Tabs')) throw new Error('Missing Tabs in AnalyticsDashboardPage');
      if (!page2.includes('ViewingHeatmap')) throw new Error('Missing ViewingHeatmap in AnalyticsDashboardPage');
      console.log('PASS: Both analytics pages have required imports');
    "
    </automated>
  </verify>
  <done>AnalyticsPage and AnalyticsDashboardPage have all imports verified, no missing design-system components, no invalid Button variants, date-range filter logic is correct, and all service function imports match their exports</done>
</task>

<task type="auto">
  <name>Task 2: Audit ContentPerformancePage and ContentDetailAnalyticsPage imports and fix navigation</name>
  <files>src/pages/ContentPerformancePage.jsx, src/pages/ContentDetailAnalyticsPage.jsx</files>
  <action>
Audit both content analytics pages for import correctness and fix the navigate issue:

**ContentPerformancePage (src/pages/ContentPerformancePage.jsx):**
1. Verify all design-system imports: `PageLayout`, `PageHeader`, `PageContent`, `Button`, `Card`, `Modal` — all valid.
2. Verify all service imports from `../services/contentAnalyticsService`: `fetchDashboardAnalytics`, `fetchSceneDetailAnalytics`, `DATE_RANGES`, `formatHours`, `formatRelativeTime`, `getUptimeColor`, `getUptimeBgColor` — all valid.
3. Verify `fetchScreenGroups` from `../services/screenGroupService` and `fetchScenesForTenant` from `../services/sceneService` exist.
4. **FIX: `useNavigate` import and usage.** The page imports `useNavigate` from `react-router-dom` and calls `navigate('/scenes/${sceneId}')` in `handleViewScene`. However, this page is rendered inside App.jsx's state-based routing (key `'content-performance'`), NOT inside a `<Router>` context for that component tree path. The `useNavigate` call will crash or navigate incorrectly.
   - **Fix:** Remove the `useNavigate` import and the `handleViewScene` function. The "View Scene" button in the scene detail modal should NOT navigate to the scene editor (there is no react-router route for `/scenes/:id`). Instead, keep the Close button only in the modal footer, or have the "View Scene" button be a no-op/toast message. The simplest safe fix: remove the `useNavigate` import, remove the `handleViewScene` function, and remove the "View Scene" button from the modal footer (keep only the Close button). This avoids a crash.
   - **IMPORTANT**: Check whether `react-router-dom` is used elsewhere in App.jsx child components. If `useNavigate` is consistently used elsewhere in these pages (and App.jsx wraps everything in a Router), then keep it. Check `src/App.jsx` to see if there's a Router wrapping the content.

**ContentDetailAnalyticsPage (src/pages/ContentDetailAnalyticsPage.jsx):**
1. Verify all design-system imports: `PageLayout`, `PageHeader`, `PageContent`, `Button`, `Card` — all valid.
2. This page uses `useParams` and `useNavigate` from `react-router-dom`. It IS wired via AppRouter.jsx at `/analytics/content/:contentType/:contentId`, so react-router usage is correct here.
3. Verify all service imports from `../services/contentAnalyticsService`: `getContentMetrics`, `getSceneTimeline`, `getMediaPlayCounts`, `getPlaylistAppearances`, `formatDuration`, `formatRelativeTime`, `DATE_RANGES` — all valid exports.
4. Check the `TimelineChart` sub-component: it handles empty data, calculates maxValue with a fallback of 1, and uses flexbox bars with hover tooltips. This is functionally sound.
5. Check error state and loading state render correctly with design-system components.

**Navigation fix decision:** Check if App.jsx wraps its page map inside a BrowserRouter. If yes, `useNavigate` works. If not, it will crash. Based on App.jsx structure, determine the correct approach. If App.jsx is inside a router (check `src/index.jsx` or `src/main.jsx`), then `navigate` works but the route `/scenes/:sceneId` may not exist. In that case, the navigate call would show a 404/blank. The safest fix is to remove the navigate-to-scene button from the ContentPerformancePage modal.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && node -e "
      const fs = require('fs');
      const cpp = fs.readFileSync('src/pages/ContentPerformancePage.jsx', 'utf8');
      const cdap = fs.readFileSync('src/pages/ContentDetailAnalyticsPage.jsx', 'utf8');
      // Verify ContentPerformancePage has required imports
      if (!cpp.includes('PageLayout')) throw new Error('Missing PageLayout in ContentPerformancePage');
      if (!cpp.includes('Modal')) throw new Error('Missing Modal in ContentPerformancePage');
      if (!cpp.includes('fetchDashboardAnalytics')) throw new Error('Missing fetchDashboardAnalytics');
      // Verify ContentDetailAnalyticsPage has required imports
      if (!cdap.includes('PageLayout')) throw new Error('Missing PageLayout in ContentDetailAnalyticsPage');
      if (!cdap.includes('getContentMetrics')) throw new Error('Missing getContentMetrics');
      if (!cdap.includes('getSceneTimeline')) throw new Error('Missing getSceneTimeline');
      console.log('PASS: Both content analytics pages have required imports');
    "
    </automated>
  </verify>
  <done>ContentPerformancePage and ContentDetailAnalyticsPage have all imports verified, navigation issue resolved (no crash on View Scene click), scene detail modal opens and renders timeline chart, and date-range filters update data correctly</done>
</task>

</tasks>

<verification>
1. Open AnalyticsPage — verify summary cards load, date-range select changes data, location filter works, email report toggle saves
2. Open AnalyticsDashboardPage — verify all three tabs load (Overview with metrics/top content, Content with sortable table, Patterns with heatmap), date-range pills change data
3. Open ContentPerformancePage — verify summary cards load, top scenes chart shows, device uptime table renders, clicking a scene opens the detail modal with stats and timeline
4. Navigate to ContentDetailAnalyticsPage — verify primary metrics (avg view duration, completion rate) load, timeline chart renders with bars, date-range pills update data
5. No JS console errors on any analytics page
</verification>

<success_criteria>
- All four analytics pages render without JavaScript errors
- Date-range filters produce updated results on all pages
- Content performance page shows per-content play counts and sortable metrics
- Scene detail modal in ContentPerformancePage opens and shows timeline
- ContentDetailAnalyticsPage timeline chart renders chronological bars
- AnalyticsDashboardPage tabs switch between Overview, Content, and Patterns views
</success_criteria>

<output>
After completion, create `.planning/phases/088-analytics-alerts/088-01-SUMMARY.md`
</output>
