---
phase: 38-e2e-test-coverage-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e-gate.js
  - playwright.config.js
  - .github/workflows/ci.yml
  - package.json
autonomous: true

must_haves:
  truths:
    - "Running `node scripts/e2e-gate.js` executes Playwright up to 3 times and reports pass rate"
    - "Gate script exits 0 when any run achieves 90%+ pass rate, exits 1 otherwise"
    - "Pass rate formula is passed / (passed + failed), skipped excluded from both numerator and denominator"
    - "Playwright JSON reporter produces machine-readable results file after each test run"
    - "CI workflow uses gate script instead of direct npm run test:e2e"
    - "CI artifacts (HTML report + JSON results) are uploaded on every run, not just failures"
  artifacts:
    - path: "scripts/e2e-gate.js"
      provides: "Best-of-3 pass rate gate script"
      min_lines: 80
    - path: "playwright.config.js"
      provides: "JSON reporter configuration alongside existing reporters"
      contains: "json"
    - path: ".github/workflows/ci.yml"
      provides: "E2E job using gate script with 60-min timeout"
      contains: "e2e-gate.js"
    - path: "package.json"
      provides: "test:e2e:gate npm script entry"
      contains: "test:e2e:gate"
  key_links:
    - from: "scripts/e2e-gate.js"
      to: "playwright.config.js"
      via: "JSON reporter output file path"
      pattern: "e2e-results"
    - from: ".github/workflows/ci.yml"
      to: "scripts/e2e-gate.js"
      via: "node scripts/e2e-gate.js command"
      pattern: "e2e-gate"
---

<objective>
Build the E2E test pass rate gate infrastructure: a Node.js script that runs the full Playwright suite up to 3 times, computes pass rate from JSON reporter output, and enforces a 90% threshold. Integrate into CI workflow.

Purpose: Provide tooling to measure and enforce the 90% pass rate gate before triage work begins.
Output: Gate script, updated Playwright config, updated CI workflow, npm script entry.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-e2e-test-coverage-gate/38-CONTEXT.md
@.planning/phases/38-e2e-test-coverage-gate/38-RESEARCH.md
@playwright.config.js
@.github/workflows/ci.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gate script and add JSON reporter</name>
  <files>scripts/e2e-gate.js, playwright.config.js, package.json</files>
  <action>
  1. **Create `scripts/e2e-gate.js`** -- a Node.js script (CommonJS, no dependencies beyond Node built-ins) that:
     - Defines constants: `THRESHOLD = 0.9`, `MAX_RUNS = 3`, `RESULTS_DIR = 'test-results'`
     - For each run (1 to MAX_RUNS):
       a. Sets `PLAYWRIGHT_JSON_OUTPUT_FILE` env var to `test-results/e2e-results.json`
       b. Runs `npx playwright test --reporter=json,html` via `child_process.execSync` with `stdio: 'inherit'`
       c. After each run (whether it succeeds or fails), reads and parses the JSON results file
       d. Walks the nested structure recursively: `report.suites[] > suites[] > specs[] > tests[]`
       e. For each `test` object, reads `test.status`:
          - `"expected"` -> passed
          - `"flaky"` -> passed (flaky counts as passed per user decision)
          - `"unexpected"` -> failed
          - `"skipped"` -> skipped (excluded from rate calculation)
       f. Computes: `passRate = passed / (passed + failed)` -- skipped excluded from both
       g. Prints run summary: passed, failed, skipped, flaky, pass rate, threshold
       h. If passRate >= THRESHOLD, prints success message and exits 0
       i. If passRate < THRESHOLD and more runs remain, prints retry message
     - After all runs exhausted, prints failure message with best result across all runs, exits 1
     - Tracks skipped count as separate metric (prints it but does not affect gate)
     - Handles edge case: if JSON file does not exist after a run (e.g., crash), treat as 0% pass rate for that run
     - Uses `process.argv` to accept optional `--threshold=X` override (default 0.9) and `--max-runs=N` (default 3) for local testing flexibility

  2. **Update `playwright.config.js`** -- Add JSON reporter to the reporter array:
     ```js
     reporter: [
       ['list'],
       ['html', { open: 'never', outputFolder: 'playwright-report' }],
       ['json', { outputFile: 'test-results/e2e-results.json' }],
     ],
     ```
     Note: The gate script also sets `PLAYWRIGHT_JSON_OUTPUT_FILE` env var as a belt-and-suspenders approach, but the config ensures JSON output even when running `npm run test:e2e` directly.

  3. **Update `package.json`** -- Add script entry:
     ```json
     "test:e2e:gate": "node scripts/e2e-gate.js"
     ```
     Place it after the existing `test:e2e` entry.

  Important: The gate script must use `require()` (CommonJS) since it uses `child_process` and `fs` directly. Use `.js` extension and add a shebang `#!/usr/bin/env node` at the top. The project is `"type": "module"` in package.json, so the gate script needs either a `.cjs` extension OR use ESM `import` syntax. Given the existing `seed-ci-test-user.cjs` precedent, use `.cjs` extension instead: **name the file `scripts/e2e-gate.cjs`** and update the npm script to `"test:e2e:gate": "node scripts/e2e-gate.cjs"`.

  Correction to file list: The actual file is `scripts/e2e-gate.cjs` (not `.js`). Update CI workflow references accordingly.
  </action>
  <verify>
  - `node scripts/e2e-gate.cjs --help` runs without syntax errors (or prints usage)
  - `cat playwright.config.js | grep json` shows the JSON reporter configured
  - `cat package.json | grep "test:e2e:gate"` shows the script entry
  - Run `node -c scripts/e2e-gate.cjs` to syntax-check the file
  </verify>
  <done>
  Gate script exists at scripts/e2e-gate.cjs, accepts --threshold and --max-runs args, parses Playwright JSON reporter output using the correct schema (test.status: expected/unexpected/flaky/skipped), computes pass rate excluding skipped tests, and exits with appropriate code. Playwright config includes JSON reporter. Package.json has test:e2e:gate script.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow to use gate script</name>
  <files>.github/workflows/ci.yml</files>
  <action>
  Modify the existing `e2e` job in `.github/workflows/ci.yml`:

  1. **Increase timeout:** Change `timeout-minutes: 20` to `timeout-minutes: 60` (worst case: 3 runs x ~15 min + overhead).

  2. **Replace test command:** Change the "Run E2E tests" step:
     ```yaml
     - name: Run E2E tests (coverage gate)
       run: node scripts/e2e-gate.cjs
       env:
         CI: true
         TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
         TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
         TEST_CLIENT_EMAIL: ${{ secrets.TEST_CLIENT_EMAIL }}
         TEST_CLIENT_PASSWORD: ${{ secrets.TEST_CLIENT_PASSWORD }}
         VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
         VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
     ```
     Keep all existing env vars exactly as they are.

  3. **Change artifact upload condition:** Change both `Upload Playwright report` and `Upload test results` steps from `if: failure()` to `if: always()`. This allows inspection of pass rates even on passing runs.

  4. **Add JSON results artifact:** Add a new upload step after the existing ones:
     ```yaml
     - name: Upload E2E gate results
       uses: actions/upload-artifact@v4
       if: always()
       with:
         name: e2e-gate-results
         path: test-results/e2e-results.json
         retention-days: 7
     ```

  Do NOT change: the checkout, node setup, cache, dependency install, playwright install, or supabase seed steps. Leave those exactly as-is.
  </action>
  <verify>
  - `cat .github/workflows/ci.yml | grep "e2e-gate"` shows the gate script reference
  - `cat .github/workflows/ci.yml | grep "timeout-minutes: 60"` shows increased timeout
  - `cat .github/workflows/ci.yml | grep "if: always()"` shows always-upload condition (should appear 3 times)
  - Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` (if python3/yaml available)
  </verify>
  <done>
  CI workflow uses gate script with 60-minute timeout, uploads artifacts on every run (not just failures), and includes JSON results as a separate artifact. All existing env vars and setup steps preserved.
  </done>
</task>

</tasks>

<verification>
- Gate script syntax-checks cleanly: `node -c scripts/e2e-gate.cjs`
- Playwright config has 3 reporters: list, html, json
- CI workflow references e2e-gate.cjs, has 60-min timeout, uploads always
- Package.json has test:e2e:gate script
- No existing functionality broken (test:e2e still works as before)
</verification>

<success_criteria>
- scripts/e2e-gate.cjs exists and is syntactically valid
- Gate script implements best-of-3 logic with 90% threshold
- Gate script correctly parses Playwright JSON reporter (expected/unexpected/flaky/skipped statuses)
- playwright.config.js includes JSON reporter output to test-results/e2e-results.json
- .github/workflows/ci.yml uses gate script, 60-min timeout, always-upload artifacts
- package.json has test:e2e:gate script entry
</success_criteria>

<output>
After completion, create `.planning/phases/38-e2e-test-coverage-gate/38-01-SUMMARY.md`
</output>
