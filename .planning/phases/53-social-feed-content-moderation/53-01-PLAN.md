---
phase: 53-social-feed-content-moderation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/player/components/widgets/SocialFeedWidget.jsx
  - src/player/components/SceneRenderer.jsx
  - src/player/components/widgets/index.js
autonomous: true

must_haves:
  truths:
    - "Social feed widget renders social posts on the player screen when assigned via scene"
    - "Player SocialFeedWidget delegates to existing SocialFeedRenderer (no rendering logic duplicated)"
  artifacts:
    - path: "src/player/components/widgets/SocialFeedWidget.jsx"
      provides: "Player social feed widget wrapper"
      min_lines: 15
    - path: "src/player/components/widgets/index.js"
      provides: "Barrel export including SocialFeedWidget"
      contains: "SocialFeedWidget"
    - path: "src/player/components/SceneRenderer.jsx"
      provides: "SceneWidgetRenderer routing for social-feed"
      contains: "social-feed"
  key_links:
    - from: "src/player/components/SceneRenderer.jsx"
      to: "src/player/components/widgets/SocialFeedWidget.jsx"
      via: "SceneWidgetRenderer switch case"
      pattern: "case 'social-feed'"
    - from: "src/player/components/widgets/SocialFeedWidget.jsx"
      to: "src/components/player/SocialFeedRenderer.jsx"
      via: "import and render delegation"
      pattern: "SocialFeedRenderer"
---

<objective>
Create the player-side SocialFeedWidget wrapper and wire it into SceneRenderer so social feeds render on player screens.

Purpose: Enables SOCIAL-01 (social feed widget in layout zones via scene assignment). The player widget wraps the existing SocialFeedRenderer -- no rendering logic is duplicated.
Output: SocialFeedWidget in widgets barrel, social-feed case in SceneWidgetRenderer.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/player/SocialFeedRenderer.jsx
@src/player/components/widgets/RssCardWidget.jsx
@src/player/components/SceneRenderer.jsx
@src/player/components/widgets/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create player SocialFeedWidget wrapper and barrel export</name>
  <files>src/player/components/widgets/SocialFeedWidget.jsx, src/player/components/widgets/index.js</files>
  <action>
Create `src/player/components/widgets/SocialFeedWidget.jsx` as a thin wrapper around the existing `SocialFeedRenderer` (imported from `../../../components/player/SocialFeedRenderer`).

Follow the exact pattern of `RssCardWidget.jsx` -- the component receives `{ props = {} }` and destructures the config. For social feed, destructure: `provider` (default 'instagram'), `accountId`, `layout` (default 'carousel'), `maxItems` (default 6), `showCaption` (default true), `showLikes` (default true), `showDate` (default true), `showAuthor` (default true), `rotationSpeed` (default 5), `filterMode` (default 'all'), `hashtags` (default []).

Render `<SocialFeedRenderer>` passing all destructured props plus `style={{ width: '100%', height: '100%' }}`. Do NOT add any data fetching -- SocialFeedRenderer already handles cached data loading internally.

Export as both named (`export function SocialFeedWidget`) and default export, matching the RssCardWidget dual-export pattern.

Then add `export { SocialFeedWidget } from './SocialFeedWidget.jsx';` to `src/player/components/widgets/index.js`.
  </action>
  <verify>Grep for `SocialFeedWidget` in index.js and confirm the new file exists with SocialFeedRenderer import.</verify>
  <done>SocialFeedWidget.jsx exists, exports named SocialFeedWidget, imports and renders SocialFeedRenderer, and is exported from widgets barrel.</done>
</task>

<task type="auto">
  <name>Task 2: Wire social-feed into SceneWidgetRenderer</name>
  <files>src/player/components/SceneRenderer.jsx</files>
  <action>
In `src/player/components/SceneRenderer.jsx`:

1. Add `SocialFeedWidget` to the import from `./widgets` (line ~20-30 area where other widgets are imported).

2. In the `SceneWidgetRenderer` function's switch statement (after the `case 'rss-card':` block around line 154), add:

```jsx
case 'social-feed':
  return <SocialFeedWidget props={safeProps} />;
```

This follows the exact same pattern as the existing rss-ticker, rss-card, and data-table cases.
  </action>
  <verify>Grep for `social-feed` in SceneRenderer.jsx to confirm the case exists and SocialFeedWidget is imported.</verify>
  <done>SceneWidgetRenderer routes `social-feed` widget type to SocialFeedWidget component.</done>
</task>

</tasks>

<verification>
- `grep -n "SocialFeedWidget" src/player/components/widgets/index.js` shows export line
- `grep -n "social-feed" src/player/components/SceneRenderer.jsx` shows switch case
- `grep -n "SocialFeedRenderer" src/player/components/widgets/SocialFeedWidget.jsx` shows delegation
</verification>

<success_criteria>
- SocialFeedWidget exists as a thin wrapper delegating to SocialFeedRenderer
- SceneWidgetRenderer has a `social-feed` case routing to SocialFeedWidget
- Widget barrel export includes SocialFeedWidget
- No rendering logic duplicated from SocialFeedRenderer
</success_criteria>

<output>
After completion, create `.planning/phases/53-social-feed-content-moderation/53-01-SUMMARY.md`
</output>
