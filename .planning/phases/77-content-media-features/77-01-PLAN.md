---
phase: 77-content-media-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/listings/CarouselMediaManager.jsx
  - src/components/listings/PropertyDetailsModal.jsx
autonomous: true
requirements:
  - FEAT-02
  - FEAT-03

must_haves:
  truths:
    - "User can upload and attach video files (mp4, webm) in the carousel media manager alongside images"
    - "Video uploads are validated for format (mp4/webm) and duration (max 2 minutes)"
    - "Each video in carousel has a mute/unmute toggle"
    - "User can add an upcoming event with title, date, and optional start/end time on property detail page"
    - "User can edit and remove existing events from the event list"
    - "Past events are automatically hidden from the list"
  artifacts:
    - path: "src/components/listings/CarouselMediaManager.jsx"
      provides: "Video upload section with format/duration validation and per-video mute toggle"
    - path: "src/components/listings/PropertyDetailsModal.jsx"
      provides: "Upcoming events section with add/edit/remove inline actions"
  key_links:
    - from: "src/components/listings/CarouselMediaManager.jsx"
      to: "Cloudinary upload"
      via: "ImageUploadButton or useCloudinaryUpload pattern for video"
      pattern: "onImageUploaded|useCloudinaryUpload"
    - from: "src/components/listings/PropertyDetailsModal.jsx"
      to: "formData.upcomingEvents"
      via: "setFormData spread pattern"
      pattern: "setFormData.*upcomingEvents"
---

<objective>
Wire video upload in carousel media manager and build property events management.

Purpose: Complete two content/media features in the listings/property domain -- video support in carousels (FEAT-02) and upcoming event management on property detail pages (FEAT-03).
Output: CarouselMediaManager with working video upload; PropertyDetailsModal with event list CRUD.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/listings/CarouselMediaManager.jsx
@src/components/listings/PropertyDetailsModal.jsx
@src/components/listings/MediaSection.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire video upload in CarouselMediaManager</name>
  <files>src/components/listings/CarouselMediaManager.jsx</files>
  <action>
Replace the placeholder video upload button (line 81-87, the "Video upload coming soon!" toast) with a working video upload flow in `CarouselMediaManager.jsx`.

**Requirements (from CONTEXT.md locked decisions):**
- Max video duration: 2 minutes, enforced on upload
- Accepted formats: mp4, webm
- Each video has a mute/unmute toggle
- Audio is configurable per video

**Implementation:**

1. Add missing `X` import from lucide-react (it's used on line 38 but not imported).

2. Store carousel videos in `formData.carouselVideos` as an array of objects: `{ url: string, muted: boolean }`. Initialize from existing formData or empty array.

3. Replace the placeholder button with a file input approach:
   - Create a hidden `<input type="file" accept="video/mp4,video/webm" />` ref
   - Add a styled button that triggers the file input click
   - On file selection, validate:
     a. File type is `video/mp4` or `video/webm` -- reject with toast `'Only MP4 and WebM formats are accepted'`
     b. Load video into a `<video>` element to check `duration` via `loadedmetadata` event. If duration > 120 seconds, reject with toast `'Video must be 2 minutes or shorter'`
   - On validation pass, use Cloudinary upload widget pattern from `ImageUploadButton` (import it from `../media/ImageUploadButton`), OR create an inline upload using `useCloudinaryUpload` hook. The simplest approach: create a `<video>` element to validate duration, then pass the file URL to `formData.carouselVideos`.
   - For upload, reuse the existing Cloudinary widget approach: Use an `ImageUploadButton` but configure it for video by setting `resourceType="video"` and appropriate `accept` prop. If `ImageUploadButton` does not support video, use a manual file input with Cloudinary upload widget (check existing pattern in the codebase -- the Cloudinary widget is loaded via script tag in index.html).

4. Display uploaded videos in a grid similar to images:
   - Show video thumbnail (use `<video>` element with `poster` or first frame)
   - Add delete button (X icon, red circle, same style as image delete)
   - Add mute/unmute toggle per video: a small button with Volume2/VolumeX icon from lucide-react
   - Show "0/1" or "1/1" counter (max 1 video per the existing UI showing 0/1)

5. Import `Volume2, VolumeX` from lucide-react for mute toggle icons.

**Style:** Match existing image grid styling (rounded-lg, aspect-video, border-2, hover states). Use same card pattern as images above.

**Do NOT** add file size validation (user decision: no file size cap beyond storage limits). Duration is the only upload constraint.
  </action>
  <verify>
Open CarouselMediaManager in dev tools or visual inspection. Verify:
1. Video upload button renders (not "coming soon" toast)
2. No ESLint errors: `npx eslint src/components/listings/CarouselMediaManager.jsx`
3. Video section shows upload area with format/size guidance
4. Mute toggle icon renders for each video
  </verify>
  <done>
Video upload section renders with file input, format validation (mp4/webm), 2-minute duration check, video thumbnail display with delete and mute/unmute toggle. "Coming soon" placeholder is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build upcoming events management in PropertyDetailsModal</name>
  <files>src/components/listings/PropertyDetailsModal.jsx</files>
  <action>
Replace the placeholder "upcoming events" section (lines 712-726, the "Add upcoming events feature coming soon!" toast area) with a working event list CRUD.

**Requirements (from CONTEXT.md locked decisions):**
- Event fields: title, date, and optional start/end time -- NO description or image
- Display as upcoming chronological list
- Past events auto-hide (removed from list once date passes)
- All events are one-time -- no recurrence
- Add/edit/remove via inline actions on the event list

**Implementation:**

1. Store events in `formData.upcomingEvents` as an array of objects:
   ```
   { id: string, title: string, date: string (YYYY-MM-DD), startTime: string|null (HH:MM), endTime: string|null (HH:MM) }
   ```

2. Replace the placeholder section with an "Upcoming Events" card:
   - Header: "Upcoming Events" with a `<Plus>` "Add Event" button on the right
   - Info text below header: "Events display on Layout 2 and 4"

3. Filter and sort: Only show events where `event.date >= today` (auto-hide past events). Sort chronologically (earliest first).

4. Event list rendering:
   - Each event row shows: title, formatted date (e.g., "Mar 15, 2026"), time range if set (e.g., "2:00 PM - 4:00 PM"), with edit (Pencil icon) and delete (Trash2 icon) action buttons on the right
   - Use compact list items with border-b separator, consistent with other sections in the modal

5. Add event form (inline, not a separate modal):
   - When "Add Event" is clicked, show an inline form at the top of the list with:
     - Title input (required, text)
     - Date input (required, type="date", min={today})
     - Start time input (optional, type="time")
     - End time input (optional, type="time")
     - Save and Cancel buttons
   - On save, generate an `id` using `Date.now().toString()`, add to `formData.upcomingEvents` array

6. Edit event:
   - Clicking edit icon replaces that event row with the same inline form, pre-filled
   - Save updates the event in the array; Cancel reverts

7. Delete event:
   - Clicking trash icon removes the event from the array (no confirmation needed for individual events)

8. Empty state: When no upcoming events, show a subtle message "No upcoming events" with the Add button.

9. Import `Pencil, Trash2, Calendar as CalendarIcon` from lucide-react (Calendar may already be imported for other uses -- check and avoid duplicate import).

**Style:** Use the same `border rounded-lg p-4` card pattern used throughout the modal. Event rows use `py-3 border-b last:border-b-0` pattern.
  </action>
  <verify>
1. No ESLint errors: `npx eslint src/components/listings/PropertyDetailsModal.jsx`
2. Events section renders with Add button instead of "coming soon"
3. Inline form appears on Add click with title, date, start/end time fields
4. Events sort chronologically, past events hidden
  </verify>
  <done>
Upcoming events section renders with chronological list, add/edit/remove inline actions, date/time fields, past-event auto-hide. "Coming soon" placeholder is gone. Events persist in formData.upcomingEvents for save.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/components/listings/CarouselMediaManager.jsx src/components/listings/PropertyDetailsModal.jsx` -- no errors
2. `npx vite build 2>&1 | tail -5` -- build succeeds without errors
3. Both "coming soon" placeholders are replaced with functional UI
</verification>

<success_criteria>
- CarouselMediaManager video section: upload button, format/duration validation, thumbnail display, mute toggle
- PropertyDetailsModal events section: add/edit/remove events, chronological sort, past auto-hide
- Both files lint cleanly and the app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/77-content-media-features/77-01-SUMMARY.md`
</output>
