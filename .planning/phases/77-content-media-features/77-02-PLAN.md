---
phase: 77-content-media-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout-editor/LeftSidebar.jsx
  - src/pages/ContentDetailAnalyticsPage.jsx
  - src/services/contentAnalyticsService.js
autonomous: true
requirements:
  - FEAT-04
  - FEAT-05

must_haves:
  truths:
    - "User can browse categorized graphics (icons, backgrounds, shapes, logos) in the layout editor sidebar"
    - "User can click a graphic to insert it at canvas center"
    - "User can view play count over time as a timeline chart for media and playlist content types"
    - "User can see playlist appearances with per-playlist breakdown on content detail analytics page"
  artifacts:
    - path: "src/components/layout-editor/LeftSidebar.jsx"
      provides: "Graphics library section with categorized browsable graphics and click-to-insert"
    - path: "src/pages/ContentDetailAnalyticsPage.jsx"
      provides: "Timeline chart and playlist appearances for media and playlist content types"
    - path: "src/services/contentAnalyticsService.js"
      provides: "Service functions for media/playlist play counts and playlist membership"
  key_links:
    - from: "src/components/layout-editor/LeftSidebar.jsx"
      to: "useMedia hook"
      via: "fetches graphics-tagged media assets"
      pattern: "useMedia|handleInsertMedia"
    - from: "src/pages/ContentDetailAnalyticsPage.jsx"
      to: "src/services/contentAnalyticsService.js"
      via: "getContentMetrics and new playlist functions"
      pattern: "getContentMetrics|getPlaylistAppearances|getMediaPlayCounts"
---

<objective>
Build graphics library in layout editor sidebar and extend content analytics with timeline for media/playlists.

Purpose: Complete two features -- a browsable graphics library panel in the layout editor sidebar (FEAT-04) and play count timeline + playlist appearance analytics for media and playlist content types (FEAT-05).
Output: LeftSidebar with populated graphics section; ContentDetailAnalyticsPage showing timeline for all content types.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/layout-editor/LeftSidebar.jsx
@src/pages/ContentDetailAnalyticsPage.jsx
@src/services/contentAnalyticsService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate graphics library in layout editor LeftSidebar</name>
  <files>src/components/layout-editor/LeftSidebar.jsx</files>
  <action>
Replace the "Graphics library coming soon" placeholder in `ElementsTabContent` (lines 873-884) with a working graphics library.

**Requirements (from CONTEXT.md locked decisions):**
- Source: combination of built-in platform graphics and user's uploaded media
- Organization: categories/folders (icons, backgrounds, shapes, logos, etc.)
- Insert behavior: click to insert at canvas center

**Implementation:**

1. Create a `GraphicsLibrarySection` component (defined within LeftSidebar.jsx or as an inline function) that replaces the placeholder div.

2. Define built-in graphic categories as a constant:
   ```js
   const GRAPHICS_CATEGORIES = [
     { id: 'icons', label: 'Icons', icon: Sparkles },
     { id: 'backgrounds', label: 'Backgrounds', icon: Image },
     { id: 'logos', label: 'Logos', icon: Crown },
     { id: 'decorations', label: 'Decorations', icon: Star },
   ];
   ```

3. Use the existing `useMedia` hook (already imported in LeftSidebar.jsx) to fetch platform graphics. Filter media assets to show:
   - Global media assets (those without an `owner_id`, i.e., platform-provided)
   - User's uploaded images (their own media library)
   - Filter by type: `image` only (graphics are images)

4. Add a category filter (horizontal pills, same style as `MEDIA_TYPE_FILTERS`):
   - "All" shows all graphics
   - Category tabs filter by folder name or tag matching the category
   - Since the media system uses folders (`useMediaFolders`), map categories to folder names. If no matching folders exist, show all media in a flat grid.

5. Render graphics as a 3-column grid of small square thumbnails (similar to the media grid but smaller -- `grid-cols-3 gap-2`):
   - Each item is clickable
   - On click, call `onAddShape` parent's `handleAddImage` (which is passed from `ElementsTabContent`'s parent). Since `ElementsTabContent` currently only receives `onAddShape`, the `GraphicsLibrarySection` needs the `onAddImage` handler.

6. **Wiring change:** Modify `ElementsTabContent` to also accept `onAddImage` prop. In the parent render (line 287-289), pass `onAddImage={handleAddImage}` alongside `onAddShape`:
   ```jsx
   {activeTab === 'elements' && (
     <ElementsTabContent onAddShape={handleAddShape} onAddImage={handleAddImage} />
   )}
   ```

7. In `GraphicsLibrarySection`, on graphic click, call `onAddImage(asset.url, { mediaId: asset.id, name: asset.name, width: asset.width, height: asset.height, type: 'image' })` -- this reuses the existing `handleAddImage` function which creates an image element on the canvas at center.

8. Add a search input within the graphics section for filtering by name.

9. Add a "See all" button at the top that could expand the section or load more. For now, show up to 12 items with a "Load More" button if `hasMore` is true.

10. Use the `useMedia` hook with `{ type: 'image', includeGlobal: true, pageSize: 12 }` for the graphics data source. This naturally includes both platform and user media.

**Style:** Match the existing sidebar patterns -- use `YODECK_COLORS.primary` (#f26f21) for hover states, same border/rounded patterns as shapes grid. Thumbnails should be `aspect-square rounded-lg border border-gray-200 hover:border-[#f26f21]`.

**Do NOT** implement drag-and-drop (mentioned in CONTEXT but not a locked decision -- click-to-insert is the primary interaction).
  </action>
  <verify>
1. `npx eslint src/components/layout-editor/LeftSidebar.jsx` -- no errors
2. Graphics section in Elements tab shows a grid of media items instead of "coming soon"
3. Clicking a graphic item calls onAddImage (verified by code inspection)
  </verify>
  <done>
Graphics library section in layout editor sidebar shows categorized, searchable graphics from platform and user media. Clicking inserts the graphic at canvas center. "Coming soon" placeholder is replaced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend content analytics timeline for media and playlists</name>
  <files>src/pages/ContentDetailAnalyticsPage.jsx, src/services/contentAnalyticsService.js</files>
  <action>
Extend `ContentDetailAnalyticsPage.jsx` to show play count timeline and playlist appearances for media and playlist content types (currently only scenes have timeline).

**Requirements (from CONTEXT.md locked decisions):**
- Data shown: play count over time + playlist appearances with per-playlist breakdown
- No screen count, duration, or peak hours metrics in this phase
- Chart style: bar chart (per Claude's discretion -- bar charts suit count data well, and the existing `TimelineChart` component already renders bars)
- Analytics data source: assess whether real play log data exists. The existing `getContentMetrics` RPC already works for media and playlist content types (accepts `p_content_type: 'media' | 'playlist'`). For timeline data, the `getSceneTimeline` RPC only works for scenes. We need to build mock/realistic data for media and playlist timelines since no dedicated RPC exists.

**Implementation in contentAnalyticsService.js:**

1. Add a `getMediaPlayCounts` function that generates realistic mock timeline data for media items:
   ```js
   export async function getMediaPlayCounts(contentId, dateRange = '7d') {
     const { fromTs, toTs } = getDateRange(dateRange);
     // Generate mock timeline buckets based on date range
     // ... create array of { bucket_start, play_count } objects
   }
   ```
   Generate mock data: create hourly/daily buckets spanning the date range, with randomized play counts (0-50 per bucket) that look realistic (higher during business hours, lower at night).

2. Add a `getPlaylistAppearances` function that returns mock playlist membership data:
   ```js
   export async function getPlaylistAppearances(contentId) {
     // Return mock data showing which playlists contain this content
     // { playlist_name, playlist_id, play_count, last_played_at }
   }
   ```
   Return 2-4 mock playlist entries with realistic names like "Lobby Playlist", "Main Display", "Weekend Rotation".

**Implementation in ContentDetailAnalyticsPage.jsx:**

1. Import the new service functions: `getMediaPlayCounts`, `getPlaylistAppearances`.

2. In `loadData`, extend the timeline loading logic:
   - For `contentType === 'scene'`: keep existing `getSceneTimeline` call (unchanged)
   - For `contentType === 'media'`: call `getMediaPlayCounts(contentId, dateRange)` and set timeline
   - For `contentType === 'playlist'`: call `getMediaPlayCounts(contentId, dateRange)` (same timeline shape) and set timeline

3. Add new state: `const [playlistAppearances, setPlaylistAppearances] = useState([])`.

4. In `loadData`, for media content type, also call `getPlaylistAppearances(contentId)` and store result.

5. Remove the "Timeline visualization is available for scenes only" message (lines 388-399). Replace with a timeline chart for ALL content types using the existing `TimelineChart` component.

6. Update the `TimelineChart` component to also handle `play_count` field (currently handles `view_count` and `total_duration_seconds`). Add `bucket.play_count` to the value resolution:
   ```js
   const value = bucket.view_count || bucket.play_count || bucket.total_duration_seconds || 0;
   ```
   Update tooltip: show "X plays" for play_count data.

7. Add a "Playlist Appearances" card below the timeline (only for media content type):
   - Card header: "Playlist Appearances" with ListVideo icon
   - Table/list showing each playlist: name, play count, last played date
   - If no appearances, show "Not assigned to any playlists"

8. Update the timeline card title for media/playlist:
   - Scene: "View Activity Timeline" (unchanged)
   - Media: "Play Count Timeline"
   - Playlist: "Play Count Timeline"

**Style:** Use existing `Card` component pattern. Playlist appearances table: use `divide-y` rows, each row with playlist name, play count badge, and relative time. Match existing Secondary Metric styling.

**Import `ListVideo` from lucide-react** for the playlist appearances card icon.
  </action>
  <verify>
1. `npx eslint src/pages/ContentDetailAnalyticsPage.jsx src/services/contentAnalyticsService.js` -- no errors
2. ContentDetailAnalyticsPage shows timeline for media and playlist content types (not just scenes)
3. Playlist appearances section renders for media content type
4. "scenes only" message is removed
  </verify>
  <done>
Content detail analytics page shows play count timeline for all content types (scene, media, playlist). Media content type also shows playlist appearances with per-playlist breakdown. Mock data provides realistic analytics visualization. "Scenes only" limitation message is removed.
  </done>
</task>

</tasks>

<verification>
1. `npx eslint src/components/layout-editor/LeftSidebar.jsx src/pages/ContentDetailAnalyticsPage.jsx src/services/contentAnalyticsService.js` -- no errors
2. `npx vite build 2>&1 | tail -5` -- build succeeds
3. Graphics library is browsable in layout editor Elements tab
4. Content analytics timeline renders for media and playlist types
</verification>

<success_criteria>
- Layout editor sidebar Elements tab: graphics section shows browsable, categorized media thumbnails with click-to-insert
- Content detail analytics: timeline chart renders for scene, media, and playlist content types
- Media analytics includes playlist appearances section with per-playlist breakdown
- All files lint cleanly and app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/77-content-media-features/77-02-SUMMARY.md`
</output>
