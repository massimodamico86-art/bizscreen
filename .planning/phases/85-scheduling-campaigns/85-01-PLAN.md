---
phase: 85-scheduling-campaigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SchedulesPage.jsx
  - src/pages/ScheduleEditorPage.jsx
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03

must_haves:
  truths:
    - "SchedulesPage renders without JS errors — list, create modal, search, action menu all functional"
    - "ScheduleEditorPage renders without JS errors — calendar grid, event modal, sidebar all visible"
    - "Schedule conflict detection displays ConflictWarning when overlapping entries exist"
    - "WeekPreview and DaypartPicker controls render and respond to interaction"
    - "Schedule entries can be created with time/day rules and content assignment via the event modal"
  artifacts:
    - path: "src/pages/ScheduleEditorPage.jsx"
      provides: "Schedule editor with all sub-components imported and working"
      contains: "import.*WeekPreview|import.*FillerContentPicker|import.*ConflictWarning"
    - path: "src/pages/SchedulesPage.jsx"
      provides: "Schedule list page with CRUD operations"
      contains: "SchedulesPage"
  key_links:
    - from: "src/pages/ScheduleEditorPage.jsx"
      to: "src/components/schedules/WeekPreview.jsx"
      via: "named import from barrel"
      pattern: "import.*WeekPreview.*from.*schedules"
    - from: "src/pages/ScheduleEditorPage.jsx"
      to: "src/components/schedules/ConflictWarning.jsx"
      via: "named import from barrel"
      pattern: "import.*ConflictWarning.*from.*schedules"
    - from: "src/pages/ScheduleEditorPage.jsx"
      to: "src/design-system"
      via: "named import for Button, Card, Badge"
      pattern: "import.*Button.*Card.*Badge.*from.*design-system"
---

<objective>
Fix all import errors and render bugs in SchedulesPage and ScheduleEditorPage so both pages load without crashes, and verify that schedule CRUD, conflict detection, dayparting, and weekly preview are functional.

Purpose: ScheduleEditorPage has ~12 missing imports (Button, Card, Badge from design-system; WeekPreview, FillerContentPicker, ConflictWarning, DaypartPicker, DateDurationPicker, PriorityBadge, CampaignPicker, AssignScreensModal from schedules barrel; X from lucide-react). SchedulesPage appears clean but needs verification. This plan fixes all import issues first, then verifies functional behavior.

Output: Both schedule pages render and function correctly.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/ScheduleEditorPage.jsx
@src/pages/SchedulesPage.jsx
@src/components/schedules/index.js
@src/design-system/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ScheduleEditorPage missing imports and Badge collision</name>
  <files>src/pages/ScheduleEditorPage.jsx</files>
  <action>
    ScheduleEditorPage.jsx has critical missing imports that will crash the page:

    1. **Badge collision**: Line 6 imports `Badge` from `lucide-react` (it's an icon, not a component). Line 841 uses `<Badge variant="error" size="sm">Conflict</Badge>` which expects the design-system Badge component. Remove `Badge` from the lucide-react import and import `Badge` from `../design-system`.

    2. **Missing design-system imports**: `Button`, `Card` are used throughout but never imported. Add them to a design-system import: `import { Button, Card, Badge } from '../design-system';`

    3. **Missing lucide-react icon**: `X` is used on line 914 but not imported from lucide-react. Add `X` to the existing lucide-react import.

    4. **Missing schedule sub-component imports**: The following are used but never imported. Import them from the `../components/schedules` barrel:
       - `WeekPreview` (line 626)
       - `FillerContentPicker` (line 871)
       - `AssignScreensModal` (line 1163)
       - `ConflictWarning` (line 1138)
       - `DaypartPicker` (line 1030)
       - `DateDurationPicker` (line 1049)
       - `PriorityBadge` (lines 835, 1007) — note: `DEFAULT_PRIORITY` is already imported, just add `PriorityBadge` to the same import
       - `CampaignPicker` (line 1020)

       Update the existing `import { DEFAULT_PRIORITY } from '../components/schedules';` to:
       ```js
       import {
         DEFAULT_PRIORITY,
         WeekPreview,
         FillerContentPicker,
         AssignScreensModal,
         ConflictWarning,
         DaypartPicker,
         DateDurationPicker,
         PriorityBadge,
         CampaignPicker,
       } from '../components/schedules';
       ```

    5. **Verify `Button variant="secondary"`**: The page uses `Button variant="secondary"` in several places. Check the design-system Button component to confirm "secondary" is a valid variant. If not (the design-system uses "outline" instead), update to "outline".

    After fixing imports, run the dev server and verify the page renders without errors.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/schedules.spec.js --reporter=list 2>&1 | tail -20</automated>
    <manual>Open http://localhost:5173 in browser, navigate to Schedules page, verify it loads without console errors. Click into a schedule editor and verify the calendar, sidebar, and event modal all render.</manual>
  </verify>
  <done>ScheduleEditorPage renders without JS errors. All sub-components (WeekPreview, ConflictWarning, DaypartPicker, DateDurationPicker, PriorityBadge, CampaignPicker, FillerContentPicker, AssignScreensModal) are imported and visible in the editor. SchedulesPage list, search, create modal, and action menus all work.</done>
</task>

<task type="auto">
  <name>Task 2: Verify schedule CRUD, conflict detection, and daypart controls via Playwright</name>
  <files>src/pages/ScheduleEditorPage.jsx, src/pages/SchedulesPage.jsx</files>
  <action>
    With imports fixed, use Playwright MCP browser to verify the following success criteria:

    1. **SCHED-01 — Schedule CRUD with time/day rules**: Navigate to Schedules page. Create a new schedule. Open the schedule editor. Click the "+" button to open the event modal. Verify the event modal shows: event type radio (Content/Screen Off), content type selector, date/time pickers (DateDurationPicker), repeat options. Fill in a content assignment and save. Verify the event appears in the calendar grid and sidebar event list.

    2. **SCHED-02 — Conflict detection**: If there are multiple overlapping entries, verify ConflictWarning renders with a red/warning indicator. Check that the sidebar event list shows "Conflict" badges on overlapping entries (uses conflictingEntryIds set).

    3. **SCHED-03 — Weekly preview and daypart configuration**: In the schedule editor, verify WeekPreview renders above the calendar. Verify DaypartPicker renders in the event modal under "Quick Apply" section and can apply preset time blocks. Verify the calendar week navigation (Today, left/right arrows) works.

    Fix any additional runtime errors encountered during verification. If any sub-component crashes (e.g., missing props, undefined references), add guard clauses or fix the root cause.
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/schedules.spec.js --reporter=list 2>&1 | tail -20</automated>
    <manual>In the schedule editor: event modal opens with all controls, WeekPreview shows above calendar, DaypartPicker presets are clickable, conflict badges appear on overlapping entries</manual>
  </verify>
  <done>Schedule creation with time/day rules works end-to-end. Conflict detection surfaces warnings when overlapping rules exist. WeekPreview and DaypartPicker controls are functional. All 3 SCHED requirements pass.</done>
</task>

</tasks>

<verification>
1. SchedulesPage loads without JS errors — list renders, create modal opens, search filters, action menu works
2. ScheduleEditorPage loads without JS errors — all 8 sub-components render (WeekPreview, ConflictWarning, DaypartPicker, DateDurationPicker, PriorityBadge, CampaignPicker, FillerContentPicker, AssignScreensModal)
3. Event modal opens with content selection, date/time pickers, repeat options, and priority selector
4. Conflict detection shows warnings on overlapping schedule entries
5. Existing E2E tests pass: `npx playwright test tests/e2e/schedules.spec.js`
</verification>

<success_criteria>
- SchedulesPage and ScheduleEditorPage render without crashes
- Schedule CRUD (create, edit, delete) completes without errors
- Conflict detection displays ConflictWarning for overlapping entries
- WeekPreview and DaypartPicker controls are visible and interactive
- All schedule E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/85-scheduling-campaigns/85-01-SUMMARY.md`
</output>
