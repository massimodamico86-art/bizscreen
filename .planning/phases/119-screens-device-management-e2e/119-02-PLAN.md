---
phase: 119-screens-device-management-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/screen-management-screenshots.spec.js
autonomous: true
requirements:
  - SCRN-06
  - SCRN-07
  - SCRN-08
  - SCRN-09
  - SCRN-10
  - SCRN-11

must_haves:
  truths:
    - "Running the screen management spec produces screenshots of screen group management with tag chips"
    - "Running the screen management spec produces screenshots of playlist/layout assignment to screen"
    - "Running the screen management spec produces screenshots of orientation toggle"
    - "Running the screen management spec produces screenshots of master PIN modal"
    - "Running the screen management spec produces screenshots of emergency push for groups"
    - "Running the screen management spec produces screenshots of working hours schedule"
  artifacts:
    - path: "tests/e2e/screen-management-screenshots.spec.js"
      provides: "Screenshot E2E tests for screen group management and device settings"
      min_lines: 180
    - path: "screenshots/119/"
      provides: "Screenshot evidence for steps 06-11"
  key_links:
    - from: "tests/e2e/screen-management-screenshots.spec.js"
      to: "tests/e2e/helpers/index.js"
      via: "import screenshotStep, loginAndPrepare, waitForPageReady, dismissAnyModals, assertAppReady"
      pattern: "import.*screenshotStep.*from.*helpers"
    - from: "tests/e2e/screen-management-screenshots.spec.js"
      to: "tests/e2e/fixtures/index.js"
      via: "import test from fixtures"
      pattern: "import.*test.*from.*fixtures"
---

<objective>
Create Playwright screenshot E2E tests for screen management features: group management with tags, playlist/layout assignment, orientation toggle, master PIN modal, emergency push for groups, and working hours schedule.

Purpose: Provides visual regression evidence for SCRN-06 through SCRN-11.
Output: tests/e2e/screen-management-screenshots.spec.js with 6 test cases producing screenshots in screenshots/119/.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key patterns from prior phases:
- Use `import { test } from './fixtures/index.js'` and helpers from `./helpers/index.js`
- Use `screenshotStep(page, '119', 'NN-description')` for screenshots
- Use `page.route()` to mock Supabase REST API
- Use `dispatchEvent('click')` to bypass `.fixed.inset-0` overlay interception on modals
- Screen Groups page navigates via `window.__setCurrentPage('screen-groups')`
- Screens page navigates via `window.__setCurrentPage('screens')`
- Screenshot step numbers: 06-11 for this plan

Source files to understand UI structure:
@src/pages/ScreensPage.jsx (master PIN modal, edit screen modal with orientation/assignment)
@src/pages/ScreenGroupsPage.jsx (group list, tags, push playlist modal)
@src/pages/components/ScreensComponents.jsx (EditScreenModal, KioskModeModal, WorkingHoursEditor)
@src/components/screens/PushPlaylistModal.jsx
@src/components/screens/WorkingHoursEditor.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screen management screenshot spec with SCRN-06, SCRN-07, SCRN-08</name>
  <files>tests/e2e/screen-management-screenshots.spec.js</files>
  <action>
Create tests/e2e/screen-management-screenshots.spec.js following campaigns-screenshots.spec.js pattern.

1. Import `{ test }` from `'./fixtures/index.js'` and helpers from `'./helpers/index.js'`.

2. Create mock data constants:
   - MOCK_SCREEN_GROUPS: 3 groups with id, name, description, location_id, location_name, device_count, online_count, active_scene_id, active_scene_name, tags (array of strings like ['lobby', 'retail'])
   - MOCK_SCREENS: 4 screens with id, device_name, status, player_type, orientation, playlist_id, playlist_name, layout_id, layout_name, location_name, otp_code, working_hours
   - MOCK_PLAYLISTS: 3 playlists with id, name, item_count
   - MOCK_LAYOUTS: 2 layouts with id, name, orientation
   - MOCK_LOCATIONS: 2 locations with id, name
   - MOCK_TAGS: ['lobby', 'retail', 'outdoor', 'meeting-room']

3. Create `setupScreenManagementMocking(page)` function that intercepts:
   - `**/rest/v1/screen_groups?*` GET -> MOCK_SCREEN_GROUPS with content-range header
   - `**/rest/v1/screens?*` GET -> MOCK_SCREENS
   - `**/rest/v1/playlists?*` GET -> MOCK_PLAYLISTS
   - `**/rest/v1/layouts?*` GET -> MOCK_LAYOUTS
   - `**/rest/v1/locations?*` GET -> MOCK_LOCATIONS
   - `**/rest/v1/rpc/get_plan_limits` POST -> { maxScreens: 25 }
   - `**/rest/v1/rpc/get_master_pin_status` POST -> { isSet: true, setAt: '2025-12-01T00:00:00Z' }
   - `**/rest/v1/rpc/get_all_group_tags` POST -> MOCK_TAGS
   - `**/rest/v1/rpc/get_screens_in_group` POST -> MOCK_SCREENS.slice(0, 2)
   - `**/rest/v1/rpc/get_unassigned_screens` POST -> MOCK_SCREENS.slice(2)
   - `**/rest/v1/schedules?*` GET -> [{ id: 's1', name: 'Business Hours' }]

4. Create test.describe('Screen Management Screenshots') with beforeEach:
   - Skip non-chromium
   - setupScreenManagementMocking(page)
   - loginAndPrepare(page)
   - assertAppReady(page, test)
   - dismissAnyModals(page)

5. Test SCRN-06: Screen group management with tag chips
   - Navigate via `window.__setCurrentPage('screen-groups')`
   - waitForPageReady(page)
   - Wait for group table or empty state (Promise.race)
   - screenshotStep(page, '119', '06-screen-groups-with-tags')
   - The mock groups have tags that should render as tag chips in the group rows

6. Test SCRN-07: Playlist/layout assignment to screen
   - Navigate to screens page via `window.__setCurrentPage('screens')`
   - waitForPageReady(page)
   - Wait for table
   - Find the first screen row's action menu (MoreVertical / three-dot button), click via dispatchEvent
   - Look for "Edit" menu item, click it to open EditScreenModal
   - Wait for dialog to appear
   - The EditScreenModal has playlist and layout assignment dropdowns
   - screenshotStep(page, '119', '07-screens-playlist-layout-assignment')

7. Test SCRN-08: Screen orientation toggle
   - Same flow as SCRN-07: navigate to screens, open action menu, click Edit
   - In the EditScreenModal, look for orientation toggle/select (portrait/landscape)
   - If a Switch or select for orientation is visible, capture the state
   - screenshotStep(page, '119', '08-screens-orientation-toggle')
   - Note: EditScreenModal in ScreensComponents.jsx includes orientation selection
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/screen-management-screenshots.spec.js --project=chromium --grep "SCRN-06|SCRN-07|SCRN-08" --timeout=30000 2>&1 | tail -20</automated>
  </verify>
  <done>Tests SCRN-06, SCRN-07, SCRN-08 produce screenshots in screenshots/119/ showing group tags, playlist/layout assignment, and orientation toggle</done>
</task>

<task type="auto">
  <name>Task 2: Add SCRN-09, SCRN-10, SCRN-11 tests for master PIN, emergency push, working hours</name>
  <files>tests/e2e/screen-management-screenshots.spec.js</files>
  <action>
Add three more tests to the existing screen-management-screenshots.spec.js file:

1. Test SCRN-09: Master PIN modal for screen locking
   - Navigate to screens page via `window.__setCurrentPage('screens')`
   - waitForPageReady(page)
   - Find the "Master PIN" button in the page header (has Key icon, text "Master PIN")
   - Click it via dispatchEvent('click') to open the master PIN modal
   - Wait for the modal to appear (look for text /Master Kiosk PIN/i or a .fixed.inset-0 overlay with PIN input)
   - The modal has two password inputs (PIN and Confirm PIN) and a Save button
   - screenshotStep(page, '119', '09-screens-master-pin-modal')
   - Optionally fill in 4 digits to show filled state
   - screenshotStep(page, '119', '09b-screens-master-pin-filled')

2. Test SCRN-10: Emergency push modal for screen groups
   - Navigate to screen groups via `window.__setCurrentPage('screen-groups')`
   - waitForPageReady(page), wait for table
   - Find first group row's action menu (MoreVertical button), click via dispatchEvent
   - Look for "Push Playlist" menu item, click it
   - Wait for PushPlaylistModal dialog to appear
   - screenshotStep(page, '119', '10-screen-groups-emergency-push')
   - If push modal not found (menu item text different), try clicking the group row to find push option in detail page

3. Test SCRN-11: Working hours schedule per screen
   - Navigate to screens page via `window.__setCurrentPage('screens')`
   - waitForPageReady(page), wait for table
   - Open action menu for a screen, click Edit to open EditScreenModal
   - Wait for dialog
   - Scroll down in the modal or look for "Working Hours" section (WorkingHoursEditor component)
   - The WorkingHoursEditor has an enable toggle, start/end time inputs, and day checkboxes
   - screenshotStep(page, '119', '11-screens-working-hours')

Important UI details from source:
- Master PIN modal is rendered directly in ScreensPage.jsx (not through Modal design system component) -- it uses a fixed inset-0 overlay with bg-black/50
- PushPlaylistModal is in src/components/screens/PushPlaylistModal.jsx
- WorkingHoursEditor is in src/components/screens/WorkingHoursEditor.jsx
- EditScreenModal uses the design system Modal component
- The ScreenActionMenu options are rendered as a dropdown in ScreenRow component
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/screen-management-screenshots.spec.js --project=chromium --grep "SCRN-09|SCRN-10|SCRN-11" --timeout=30000 2>&1 | tail -20</automated>
  </verify>
  <done>Tests SCRN-09, SCRN-10, SCRN-11 produce screenshots showing master PIN modal, emergency push for groups, and working hours schedule</done>
</task>

</tasks>

<verification>
All 6 SCRN tests (06-11) pass and produce distinct screenshots in screenshots/119/:
- 119/06-screen-groups-with-tags.png
- 119/07-screens-playlist-layout-assignment.png
- 119/08-screens-orientation-toggle.png
- 119/09-screens-master-pin-modal.png
- 119/10-screen-groups-emergency-push.png
- 119/11-screens-working-hours.png
</verification>

<success_criteria>
- npx playwright test tests/e2e/screen-management-screenshots.spec.js --project=chromium passes
- Screenshots directory screenshots/119/ contains at least 6 PNG files from this spec
- Each test captures a distinct UI state per its requirement
</success_criteria>

<output>
After completion, create `.planning/phases/119-screens-device-management-e2e/119-02-SUMMARY.md`
</output>
