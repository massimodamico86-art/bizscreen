---
phase: 119-screens-device-management-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/screens-screenshots.spec.js
autonomous: true
requirements:
  - SCRN-01
  - SCRN-02
  - SCRN-03
  - SCRN-04
  - SCRN-05

must_haves:
  truths:
    - "Running the screens screenshot spec produces screenshots of screen list with status indicators"
    - "Running the screens screenshot spec produces screenshots of screen creation modal with pairing code"
    - "Running the screens screenshot spec produces screenshots of OTP pairing flow"
    - "Running the screens screenshot spec produces screenshots of device diagnostics drawer"
    - "Running the screens screenshot spec produces screenshots of remote command buttons"
  artifacts:
    - path: "tests/e2e/screens-screenshots.spec.js"
      provides: "Screenshot E2E tests for screens page core flows"
      min_lines: 150
    - path: "screenshots/119/"
      provides: "Screenshot evidence directory for phase 119"
  key_links:
    - from: "tests/e2e/screens-screenshots.spec.js"
      to: "tests/e2e/helpers/index.js"
      via: "import screenshotStep, loginAndPrepare, waitForPageReady, dismissAnyModals, assertAppReady"
      pattern: "import.*screenshotStep.*from.*helpers"
    - from: "tests/e2e/screens-screenshots.spec.js"
      to: "tests/e2e/fixtures/index.js"
      via: "import test from fixtures"
      pattern: "import.*test.*from.*fixtures"
---

<objective>
Create Playwright screenshot E2E tests for the Screens page covering screen list with status indicators, screen creation with pairing code, OTP pairing flow, device diagnostics, and remote commands.

Purpose: Provides visual regression evidence for the 5 core screen management features (SCRN-01 through SCRN-05).
Output: tests/e2e/screens-screenshots.spec.js with 5 test cases producing screenshots in screenshots/119/.
</objective>

<execution_context>
@/Users/massimodamico/.claude/get-shit-done/workflows/execute-plan.md
@/Users/massimodamico/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key patterns from prior phases:
- Use `import { test } from './fixtures/index.js'` and helpers from `./helpers/index.js`
- Use `screenshotStep(page, '119', 'NN-description')` for screenshots
- Use `page.route()` to mock Supabase REST API when backend data needed
- Use `dispatchEvent('click')` to bypass `.fixed.inset-0` overlay interception on modals
- Use `window.__setCurrentPage('screens')` for navigation
- Screenshot step numbers: 01-05 for this plan (list, create, OTP, diagnostics, commands)

Source files to understand UI structure:
@src/pages/ScreensPage.jsx
@src/pages/components/ScreensComponents.jsx
@src/components/ScreenDetailDrawer.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screens screenshot spec with API mocking and SCRN-01 through SCRN-03</name>
  <files>tests/e2e/screens-screenshots.spec.js</files>
  <action>
Create tests/e2e/screens-screenshots.spec.js following the campaigns-screenshots.spec.js pattern exactly.

1. Import `{ test }` from `'./fixtures/index.js'` and helpers from `'./helpers/index.js'` (screenshotStep, loginAndPrepare, waitForPageReady, dismissAnyModals, assertAppReady).

2. Create mock data constants:
   - MOCK_SCREENS: array of 4-5 screen objects with fields: id, device_name, otp_code, status ('online'/'offline'), player_type ('bizscreen_player'/'chrome'), orientation ('landscape'/'portrait'), last_seen (ISO date), playlist_id, playlist_name, layout_id, layout_name, location_name, screen_group_name, working_hours (object with enabled, start, end, days). Include a mix of online/offline, landscape/portrait, with/without assignments.
   - MOCK_PLAYLISTS: 2-3 playlists with id, name
   - MOCK_LAYOUTS: 2-3 layouts with id, name, orientation
   - MOCK_SCHEDULES: 2 schedules with id, name
   - MOCK_LOCATIONS: 2 locations with id, name
   - MOCK_SCREEN_GROUPS: 2 groups with id, name

3. Create `setupScreensMocking(page)` function that intercepts:
   - `**/rest/v1/screens?*` GET -> returns MOCK_SCREENS with content-range header
   - `**/rest/v1/playlists?*` GET -> MOCK_PLAYLISTS
   - `**/rest/v1/layouts?*` GET -> MOCK_LAYOUTS
   - `**/rest/v1/schedules?*` GET -> MOCK_SCHEDULES
   - `**/rest/v1/locations?*` GET -> MOCK_LOCATIONS
   - `**/rest/v1/screen_groups?*` GET -> MOCK_SCREEN_GROUPS
   - `**/rest/v1/rpc/get_plan_limits` POST -> { maxScreens: 25 }
   - `**/rest/v1/screen_diagnostics?*` GET -> mock diagnostics data (cpu_percent, memory_percent, disk_percent, browser_version, os_version, ip_address, resolution, uptime_seconds, js_heap_used, js_heap_total, last_heartbeat)
   - `**/rest/v1/rpc/get_master_pin_status` POST -> { isSet: true, setAt: '2025-12-01T00:00:00Z' }

4. Create test.describe('Screens Screenshots') with beforeEach:
   - Skip non-chromium browsers
   - Call setupScreensMocking(page)
   - loginAndPrepare(page)
   - assertAppReady(page, test)
   - dismissAnyModals(page)

5. Test SCRN-01: Screen list with status indicators
   - Navigate via `window.__setCurrentPage('screens')`
   - waitForPageReady(page)
   - Wait for table to appear (page.locator('table')) OR empty state -- use Promise.race
   - screenshotStep(page, '119', '01-screens-list-status')

6. Test SCRN-02: Screen creation with pairing code
   - Navigate to screens page
   - Click "Add Screen" button via `page.getByRole('button', { name: 'Add Screen', exact: true })`
   - If button not found, try dispatchEvent
   - Wait for dialog to appear
   - screenshotStep(page, '119', '02-screens-create-modal')
   - Fill in the name field if a text input is visible
   - screenshotStep(page, '119', '02b-screens-create-filled')

7. Test SCRN-03: OTP pairing flow
   - Navigate to screens page
   - Click Add Screen button, wait for modal
   - Look for "Create" or submit button in modal, click it (use dispatchEvent if overlay intercepts)
   - Mock the POST to `**/rest/v1/screens` to return a screen with otp_code: 'ABC123'
   - Wait for OTP code display (look for text matching /pairing code|OTP|ABC123/i)
   - screenshotStep(page, '119', '03-screens-otp-pairing')
   - If OTP not visible (backend unavailable), capture whatever state is shown
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/screens-screenshots.spec.js --project=chromium --grep "SCRN-01|SCRN-02|SCRN-03" --timeout=30000 2>&1 | tail -20</automated>
  </verify>
  <done>Tests SCRN-01, SCRN-02, SCRN-03 produce screenshots in screenshots/119/ showing screen list with status indicators, creation modal, and OTP pairing flow</done>
</task>

<task type="auto">
  <name>Task 2: Add SCRN-04 and SCRN-05 tests for diagnostics and remote commands</name>
  <files>tests/e2e/screens-screenshots.spec.js</files>
  <action>
Add two more tests to the existing screens-screenshots.spec.js file (append within the describe block):

1. Test SCRN-04: Screen detail page with device diagnostics
   - Navigate to screens page, wait for table
   - Find a screen row in the table, look for the action menu button (MoreVertical icon or three-dot button)
   - Click the action menu via dispatchEvent('click')
   - Look for "View Details" or "Details" menu item, click it
   - Wait for the ScreenDetailDrawer to appear (look for text like /diagnostics|device info|screen detail/i, or a drawer/panel sliding in)
   - If drawer not found, try clicking on the screen name directly
   - Mock diagnostics endpoint should provide data showing CPU, memory, disk, browser, IP
   - screenshotStep(page, '119', '04-screens-device-diagnostics')

2. Test SCRN-05: Remote commands (reboot, reload, clear cache)
   - Navigate to screens page, wait for table
   - Open action menu for a screen row
   - Look for command buttons: text matching /reboot|reload|clear cache|restart/i
   - If commands are in a dropdown/menu, capture the open menu
   - screenshotStep(page, '119', '05-screens-remote-commands')
   - If commands are in the detail drawer instead, navigate there and capture

Important patterns:
- ScreenRow component has ScreenActionMenu with options: Edit, View Details, View Analytics, Device Commands (reboot, reload, clear cache), Kiosk Mode, Delete
- The action menu is triggered by MoreVertical button in each row
- Use page.locator('button').filter({ has: page.locator('[data-lucide="more-vertical"], svg') }) or similar to find the action button
- Commands may be in a submenu or the action menu directly
  </action>
  <verify>
    <automated>cd /Users/massimodamico/bizscreen && npx playwright test tests/e2e/screens-screenshots.spec.js --project=chromium --grep "SCRN-04|SCRN-05" --timeout=30000 2>&1 | tail -20</automated>
  </verify>
  <done>Tests SCRN-04 and SCRN-05 produce screenshots showing device diagnostics drawer and remote command options</done>
</task>

</tasks>

<verification>
All 5 SCRN tests (01-05) pass and produce distinct screenshots in screenshots/119/:
- 119/01-screens-list-status.png
- 119/02-screens-create-modal.png
- 119/03-screens-otp-pairing.png
- 119/04-screens-device-diagnostics.png
- 119/05-screens-remote-commands.png
</verification>

<success_criteria>
- npx playwright test tests/e2e/screens-screenshots.spec.js --project=chromium passes
- Screenshots directory screenshots/119/ contains at least 5 PNG files
- Each test captures a distinct UI state per its requirement
</success_criteria>

<output>
After completion, create `.planning/phases/119-screens-device-management-e2e/119-01-SUMMARY.md`
</output>
